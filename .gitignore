# Secrets / env
.env
.env.*
*.env
*.local
*.secret
secrets.*
**/secrets.*

# Supabase local temp
supabase/.temp
supabase/.branches

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/.claude/

# Scratch / proof harness output (large, transient, never shipped)
_harness/

# Local Desktop/repo health-check routine output (machine-local, never shipped)
docs/runtime/health/
