import type { NextConfig } from "next";

// cc-0013 Stage B — minimal Next.js 15 App Router scaffold.
// No experimental features at v1. Node runtime for the entire app (no Edge).
// Adding routes / features lands in Stage C+ under separate briefs.
const nextConfig: NextConfig = {};

export default nextConfig;
