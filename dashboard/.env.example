# cc-0013 dashboard — local env template.
#
# Copy to .env.local and populate. .env.local is gitignored; do not commit.
# Do NOT prefix any of these with NEXT_PUBLIC_ — they are server-only.
#
# SUPABASE_URL: Supabase project URL (https://<project-ref>.supabase.co).
# SUPABASE_SERVICE_ROLE_KEY: service-role JWT. Server-side only.
#   - Used by lib/supabase/server.ts via process.env.
#   - NEVER expose to client (no NEXT_PUBLIC_ prefix; no client-side import of
#     lib/supabase/server.ts; `server-only` package raises a build error on
#     accidental client import).
#
# DASHBOARD_FRICTION_FAB_ENABLED: cc-0014 Stage D operator gate.
#   - Set to the literal string "true" to render the friction-capture FAB and
#     accept submissions through the emitFriction Server Action.
#   - Any other value (including unset, "1", "yes", "TRUE") leaves the FAB
#     hidden and the action refuses with an explicit error.
#   - Must be set BEFORE `npm run dev` starts — Next.js reads .env.local once
#     at server boot. Restart the dev server after changing it.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
DASHBOARD_FRICTION_FAB_ENABLED=true
