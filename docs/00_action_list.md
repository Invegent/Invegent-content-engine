# ICE — Action List

> **Single active action index for what's queued, in flight, blocked, or frozen.**
> Source-of-truth details remain in sync_state, run states, decisions, briefs, and commits.
> Read at the start of every session alongside `docs/00_sync_state.md`.
> **🧭 OWNERSHIP (planning-docs boundary):** Authoritative **for the active task queue** (Today / Next-5 + carries) — **not** truth-of-record. Siblings: product roadmap → `04_phases.md` · live truth-of-stack → `00_sync_state.md`.
>
> Last updated: 2026-06-23 Sydney (**v3.91 — ICE SUBAGENTS: creative-graph-auditor REGISTERED in the team contract (docs-only; NO production change). Precondition verified: agent file committed+pushed by a separate session — .claude/agents/creative-graph-auditor.md tracked on origin/main (commit 37021c5; HEAD==main==origin/main; clean tree apart from standing-untracked .claude/hooks/ + _harness/). Docs: CLAUDE.md 'The team (v1 — built)' table gains a creative-graph-auditor row + a 'Creative Library static lane' note. Described accurately: read-only (Read/Grep/Glob); static auditor of the Creative Library v2 declarative graph (JSON/schema shape, key uniqueness, reference resolution, evidence-SHAPE, runtime-import guard, vendored drift); returns PASS/FAIL/ESCALATE. Non-capabilities recorded (no overclaim): does NOT query DB, NOT verify live render logs, NOT validate RLS/grants or audit resolve_brand_assets(), NOT judge style-guide conformance, NOT approve/mark-proven any creative object, NOT mutate/commit/deploy — live DB/asset/render truth handed to db-rls-auditor, brand-conformance stays with PK. Honest status: built+committed (37021c5); spec exercised MANUALLY in A1.4 (agent-type not registered-invocable that session), NOT yet run as a registered subagent (no false PROVEN claim). No contradiction with existing agents; no governance weakening (PK gate + external-review gate + every May-NOT boundary intact). This pass CE docs/registers-only (0 code/DB/migration/dashboard/resolver/worker/provider/render/deploy/Creative-Library-registry/agent-file/new-agent). Supersedes -> prior v3.90 — CREATIVE LIBRARY v2 A1.4 TEMPLATE FAMILIES v1 (declarative-only; NO production change, NO worker consumption). Supersedes -> prior v3.89 — CREATIVE LIBRARY v2 A1.3 PROPERTY PULSE STYLE GUIDE v1 (declarative-only; NO production change, NO worker consumption). Supersedes -> prior v3.88 — CREATIVE LIBRARY v2 A1.2 PATTERN LIBRARY v1 (declarative-only; NO production change, NO worker consumption). A1.2 closed. Docs: new docs/creative-library/property-pulse-pattern-library-v1.md (catalogue); property-pulse.json patterns formalised draft->candidate (purpose/used_by_template_families/accessibility_rules/proof_posture/contracts); registry-schema-v2.md extended (Pattern fields + proof_posture vocabulary draft/candidate/supported_by_host_render/proven + supporting_render_log_ids). 5 patterns all candidate: pp_branding_strip/headline_block/category_badge/background_plus_scrim -> supported_by_host_render (appear in BOTH proven host variants Gate C 7243e040 + Gate D2 508b4365 as supporting evidence, NOT pattern proof); pp_stat_card -> candidate (forward-looking, removed from family composed_of, used_by empty). Optional pp_cta_strip + pp_source_attribution NOT added (evidence insufficient). Proof discipline: NO pattern claims proven; only Template-Family variants carry render-based proof (real render_log_ids); family unproven. Validation: JSON valid, keys unique, used_by/composed_of resolve, asset refs governed, style_guide ref valid, implementations[] unchanged, 0 issues. External review CLEAN. Foundation preserved (additive): governed assets, resolve_brand_assets(), resolver scope, provider-ownership, evidence-first, intake, review-packets, PK gate. Next: A1.3 Property Pulse Style Guide v1. This pass CE docs/registry-only (0 code/DB/migration/dashboard/resolver/worker/provider/render/deploy/runtime-consumption/DB-backed-registry/new-provider-template). Supersedes -> prior v3.87 — CREATIVE LIBRARY v2 A1.1 REGISTRY SCHEMA EVOLUTION (declarative-only; NO production change, NO worker consumption). A1.1 closed. Docs: new docs/creative-library/registry-schema-v2.md (schema for Style Guide, Creative Patterns, Template Families+Variants, Creative Instances, generalised Evidence; pattern_type enum; validation + posture); docs/creative-library/property-pulse.json evolved v0.1->v0.2 (additive). PP v0.2 (existing proven evidence only): style_guide property-pulse-styleguide-v1 (draft/unproven); 5 patterns (pp_branding_strip/headline_block/stat_card/category_badge/background_plus_scrim, draft/unproven); first-class template_families[] property-pulse-news composing patterns+assets with 2 PROVEN variants (centred-scrim-16x9 Gate C 7243e040, centred-scrim-9x16-video Gate D2 508b4365); creative_instances documented as render events (m.post_render_log + render_spec); legacy implementations[] retained. Proof discipline: no proven without evidence; no render-based proven without real render_log_id; Style Guide+Patterns stay draft. Validation: JSON valid, pattern refs valid, asset refs governed, 0 proof violations. Foundation preserved (additive): governed assets, resolve_brand_assets(), resolver scope, provider-ownership, evidence-first, intake, review-packets, PK gate. Next: A1.2 Pattern Library v1. This pass CE docs/registry-only (0 code/DB/migration/dashboard/resolver/worker/provider/render/deploy/runtime-consumption/DB-backed-registry). Supersedes -> prior v3.86 — CREATIVE LIBRARY v2 A1.0 ARCHITECTURE RATIFICATION (Governed Creative Design System; docs-only, NO production change). Ratified authoritative foundation for all subsequent Creative Library work; A1.0 CLOSED. Doc: docs/creative-library/creative-library-v2-architecture.md. Model: Creative Library is now a Governed Creative Design System (not Assets+Templates). Core principle: ICE governs · providers execute · PK approves · AI proposes, never approves. Layers: Style Guide -> (governs) -> Assets + Patterns (SIBLING libraries; patterns may reference assets; assets not beneath patterns) -> (compose) -> Template Families -> (instantiated as) -> Creative Instances -> Evidence. First-class: Style Guides, Assets, Creative Patterns, Template Families, Creative Instances, Evidence. Foundation PRESERVED (additive, not a rewrite): governed assets, resolve_brand_assets(), resolver scope, provider-ownership, evidence-first governance, intake framework, review-packet workflow, PK gate. Registry direction (declarative only, NOT consumed by workers): add style_guide, patterns[], composed_of_patterns[], generalised evidence. Sprint reframe: Lane A — Template Factory -> Lane A — Creative Design System; phases A1.0(closed)/A1.1 Registry Schema/A1.2 Pattern Library v1/A1.3 Style Guide v1/A1.4 Template Families/A1.5 Dashboard (future)/A1.6 Intake Generalisation. Dashboard: no redesign/no nav change (IA revisit deferred to A1.5 carry). Constraints: no tables/migrations/resolver-change/prod-registry-consumption/dashboard-redesign/invented-infra/governance-weakening. This pass CE docs-only (0 code/DB/dashboard/resolver/worker/provider/render/deploy/registry-JSON/asset). Supersedes -> prior v3.85 — CREATIVE LIBRARY INTAKE v0 PHASE-1 OPERATIONAL — Property Pulse now has 4 governed assets + dashboard synced to prod (CE-register reconciliation of an intake + dashboard-repo shipment). Completed: Intake Lane 1 scaffolding (manifest schema, review-packet format, Creative Score, intake agent spec; hard rule no license -> no governance); Sydney+Brisbane review packets PK-approved (S1 Pexels 34789111 Warren Yip, B2 Pexels 30604032 Shiyong Lim); bg_sydney_cbd (3769be84) + bg_brisbane_cbd (47f489f4) uploaded to brand-assets (verified 206) + governed in c.client_brand_asset with full trail (Pexels license/source_url/photographer/attribution=false/approved_by=PK/approval_status=governed; db-rls-auditor PASS + external review CLEAN); resolve_brand_assets now resolves 4 governed PP assets; Creative Library Dashboard v1.1 promoted to PROD (invegent-dashboard main=838ad37, Vercel READY, dashboard.invegent.com/creative-library; read-only expectedKeys 2->4, DEFERRED_ASSETS [2]->[]). Dashboard displays: 4 governed assets GREEN, static_news GREEN, animated_news AMBER, no deferred Sydney/Brisbane. Carries (recording only): resolver migration-ledger parity; registry vendoring drift; image-worker QA-visibility gap; animated_news still requires its first governed render; template expansion + asset intake continue under the Creative Library roadmap. This pass CE docs-only (0 code/DB/migration/dashboard/storage/provider/render/deploy/registry/asset). Supersedes -> prior v3.84 — CREATIVE LIBRARY DASHBOARD v1 SHIPPED TO PRODUCTION — operator visibility for the Creative Library v0.1 evidence chain (CE-register reconciliation of a dashboard-repo shipment). LIVE: invegent-dashboard main=952d68d, Vercel prod READY (dashboard.invegent.com), route dashboard.invegent.com/creative-library (read-only; 307->/login via auth middleware). Gates: tsc+next build clean, mutation-scan none, branch-warden safe, external review CLEAN (483a4d2b / 843690e7), PK preview+prod approval, ff-merge+push. Proven state: Creative Library Foundation complete; still-image governed; animated proven (smoke); governed assets operational; secure resolver operational; governed render evidence operational; dashboard visibility production. Readiness: static_news GREEN, animated_news AMBER, pp_logo_primary GREEN, bg_perth_cbd GREEN, bg_sydney_cbd/bg_brisbane_cbd GREY (deferred). Carries (recording only): resolver migration-ledger parity; Sydney background deferred; Brisbane background deferred; auto-selection not started; publish integration not started. This pass CE docs-only (0 code/DB/migration/dashboard/worker/registry/render/deploy). Supersedes -> prior v3.83 — CREATIVE LIBRARY v0.1 LANES 1–3B COMPLETE / PROVEN — Property Pulse governed asset -> registry -> safe resolver -> manual governed render end-to-end. Lane 1 c.client_brand_asset rows pp_logo_primary (b7530c55-c320-43be-90d9-98c804694921) + bg_perth_cbd (f9caed52-0859-4e22-91f6-7dc998485d77), approved+active (Sydney/Brisbane deferred, 404). Lane 2 registry docs/creative-library/property-pulse.json (b3e1acb). Lane 3A resolver public.resolve_brand_assets(text,text[]) SECURITY DEFINER search_path='' EXECUTE service_role-only. Lane 3B image-worker v3.10.2->v3.11.0 (merged 0580d27, --no-verify-jwt, verify_jwt=false) creative_library_manual_render: resolves asset_keys via resolver (NO raw-url fallback), render_log e7d096b8-6d6c-4af8-a245-dc35f83bd291 succeeded, render_engine=creatomate, ice_format_key=image_quote, post_draft_id null, render_spec.template asset_ids [b7530c55...,f9caed52...] resolver_used=true fallback_taken=false, JPG 320803 bytes 206, 0 publish/queue/draft side effects, production loop did not run. Carry: resolver LIVE in prod but NOT yet backfilled to repo migration ledger/supabase/migrations; Sydney/Brisbane deferred; dashboard visibility = next lane. This pass docs-only. Supersedes -> prior v3.82 — CREATIVE LIBRARY v0 GATE D2 COMPLETE / CLOSED — animated/video Creative Library template path PROVEN through ICE-controlled video-worker smoke. Shipped (merged main 9d1c411; reviewed_input_hash 368252bd, review_id 7d3ed9fa): video-worker v3.1.5->v3.2.0 manual-only Creatomate TEMPLATE-mode video smoke branch (isSmokeRequest gate; returns before production draft selection; production source-mode path + render_engine byte-unchanged; new module template_smoke.ts; composeRenderSpec keeps production render_spec {qa} byte-identical). Deployed video-worker-v3.2.0 (--no-verify-jwt, verify_jwt=false preserved). Gates: deno check 0 + 7/7 hermetic + qa 9/9 regression + branch-warden safe + external review CLEAN (368252bd / 7d3ed9fa) + PK deploy/smoke/merge approvals. Gate D2 smoke VERIFIED (exactly ONE invocation): m.post_render_log row 508b4365-9dfc-4ec8-b491-2a3a1014d26b succeeded, render_engine=creatomate, ice_format_key=video_short_stat (governed label only), post_draft_id/client_id null, render_spec.label=creative_library_video_smoke, render_spec.qa present (composition, render_wallclock, 1080x1920 / 9:16) + render_spec.template present (template_id=pp-news-centred-scrim-9x16-video, v1, property-pulse-news, centred-scrim-9x16-video, provider=creatomate, provider_template_id=bc32f52f-f9da-4749-90aa-03f7572f0719, props_hash=0369d6bb, fallback_taken=false, asset_ids=[]), MP4 stored post-videos/_smoke/pp_news_centred_scrim_9x16_video_v1.mp4 (3,407,414 bytes), render_duration_ms=30166, credits_used=null. Side effects clean: 0 draft / 0 queue / 0 publish / 0 advisor / 0 dashboard / 0 HeyGen-AGP / 0 taxonomy / 0 migration; exactly 1 render-log row. ANIMATED/VIDEO bridge PROVEN. This pass docs-only. Supersedes -> prior v3.81 — CREATIVE LIBRARY v0 GATE C COMPLETE / PROVEN end-to-end (ICE-controlled template render writes render_spec.template; code shipped, this pass docs-only). Still-image bridge PROVEN: approved design -> Creatomate template -> ICE render -> render_spec.template evidence in m.post_render_log, no production-path disruption. Shipped (merged main b1feb54): image-worker v3.10.0->v3.10.2 — manual-only template-mode smoke branch (body.mode=template_smoke; returns before production loops; production selection + render_engine=creatomate + publish/queue/advisor byte-unchanged); renderUploadAndLog gained optional renderSpec (additive; existing callers->null) + logMustSucceed (template_smoke only -> render-log write failure is HARD error). Deployed image-worker-v3.10.2 (--no-verify-jwt, verify_jwt=false). Gates: deno check 0 + 3/3 hermetic + branch-warden safe + external review CLEAN (72c125eb / 0ca2e5d4) + PK deploy/smoke/merge approvals. Smoke VERIFIED: m.post_render_log row succeeded, render_engine=creatomate, ice_format_key=image_quote, post_draft_id/client_id null, render_spec.label=creative_library_smoke, render_spec.template (template_id=pp-news-centred-scrim-16x9, v1, property-pulse-news, centred-scrim-16x9, provider=creatomate, provider_template_id=48cba556..., props_hash, fallback_taken=false), storage+output URLs, duration 4141ms, credits_used=null (Creatomate did not return), render_spec.qa absent (image-worker not QA-instrumented). Side effects clean: 0 queue / 0 publish / 0 drafts touched / exactly 1 render-log row. KEY LEARNING: m.post_render_log.ice_format_key is BOTH NOT NULL AND FK to t.5.3_content_format -> Option A: smoke uses ice_format_key=image_quote, true identity in render_spec.template, smoke identity in render_spec.label. NEXT real lane: Gate D animated/video Creative Library template (own brief + gates). Carried future gap (NOT v0): image-worker QA instrumentation (no render_spec.qa on image renders). 0 code/DB/migration/deploy/provider/render/publish/dashboard/AGP/secret this pass. Branch B NOT AUTHORISED. P2 NOT READY. A1 observation pending. Authority impact: none.**) **) Prior — (**v3.80 — CREATIVE LIBRARY v0 GATE B1 VISUAL/PROVIDER SMOKE PASSED (manual Creatomate export; provider-side visual proof ONLY; docs-only record). Template PP_NEWS_CENTRED_SCRIM_16x9_v1 (id 48cba556) produced a correct 1920x1080 export; visual review PASS (headline readable, subtitle acceptable, logo clear, scrim works, no SafeMargin visible, background correct). Minor non-blocking (future tuning): logo dominant, large top sky, footer/date tunable. SCOPE = provider-side VISUAL ONLY; did NOT prove ICE render path / m.post_render_log / render_spec.template / render_spec.qa / storage / publish. NEXT was Gate C (ICE-controlled render smoke). Authority impact: none.**) Prior — (**v3.79 — CREATIVE LIBRARY v0 GATE A COMPLETE (Creatomate template created manually; NO render; docs-only record). Template PP_NEWS_CENTRED_SCRIM_16x9_v1; id 48cba556-0a53-4001-90f0-05420d10efc0; 1920x1080 16:9 JPEG. 8 editable modification keys (Background.source/Logo.source/CategoryBadge.text/Headline.text/Subtitle.text/Location.text/Date.text/Footer.text); Scrim fixed (exported, not a key); SafeMargin not present/not exported. Deviations (no visual blocker): temp Unsplash background + placeholder logo (replace via Logo.source); CategoryBadge letter-spacing removed (Creatomate validation); Scrim fill_rotation dropped, gradient still renders. NO render smoke. Brief updated: provider_template_id resolved placeholder->real id. NEXT was Gate B. Authority impact: none.**) Prior — (**v3.78 — CREATIVE LIBRARY v0 PILOT BRIEF LANDED (Property Pulse News · Centred Scrim 16:9 spec; docs-only). Brief docs/briefs/creative-library-v0-property-pulse-news-centred-headline-16x9.md (Creatomate TEMPLATE mode). Selected design pack: Property Pulse News — Centred Scrim (master 16:9/1920x1080 + deferred 9:16/1:1/4:5). First provider target template PP_NEWS_CENTRED_SCRIM_16x9_v1, 16:9 master only. 8 fields (headline required) + 8 modification keys; Scrim fixed (exported, not a key); SafeMargin guide only (not exported). Evidence: additive render_spec.template sibling to render_spec.qa (no new table/RPC change). Resolved: manual-only smoke, no ice_format_key, no new key; font Montserrat-or-PP-compatible; photo PK-supplied/licensed only no scraping. Gate sequence: Gate A = create template (16:9 only, no render); Gate B = manual render smoke. Avatars/HeyGen excluded. branch-warden safe + external review CLEAN (1375c229) + PK commit approval. Authority impact: none.**) Prior — (**v3.77 — CREATIVE LIBRARY v0 PLANNING DECISION RECORDED (shrunk, doc/config-backed; PARKED/PLANNED, NOT active implementation; NO build lane). Principle approved; Property Pulse = first pilot; v0 deliberately SHRUNK: doc/config-backed NOT DB-backed — no new registry tables, no Template Intelligence Layer, no multi-brand onboarding, no automated QA engine. Avatars/characters EXCLUDED (pending AGP / c.brand_avatar; Branch B NOT AUTHORISED). Architecture rules: template scoping keys from ice_format_key NOT render_engine; evidence EXTENDS m.post_render_log.render_spec as render_spec.template (NO new table). Seed = uploaded Property Pulse design; first candidate variant Centred Headline 16:9. No new ice_format_key in v0; first smoke manual/template-only unless PK approves pipeline binding. Still PARKED: Brand Asset Library, full registry, AGP/avatar, music, template automation. Decision record only. Authority impact: none.**) Prior — (**v3.76 — POST-QA-VISIBILITY OBSERVATION HEALTHY; active polling -> STANDING WATCH (no in-scope render opportunity yet). Session-2 read-only reassessment (DB time 2026-06-21 03:31 UTC): no regressions; no new voice failures; no queue/publish leakage; publishing normal. 3 natural rows since QA v0 (00:11 UTC) ALL image_quote -> out of QA-v0 scope (panel labels out, not defects -> pre-prod scope fix works in prod); 0 natural in-scope video-worker/HeyGen renders; 0 eligible video/avatar drafts. First in-scope QA confirmation pending OPPORTUNITY not failure; HeyGen qa deployed but unconfirmed (no avatar opportunity), non-blocking. No voice regression; no publish leakage. Standing watch (passive): first natural video-worker QA row; first natural HeyGen QA row; CFW voice-map decision; historical failed-voice/backfill carries; render_engine success/failure asymmetry. No new build lane. Authority impact: none.**) Prior — (**v3.75 — DASHBOARD QA VISIBILITY PANEL v0 SHIPPED TO PRODUCTION (operator-facing render_spec.qa surface; CE-register reconciliation of a dashboard-repo shipment): read-only Render QA panel on /overview (after ICE Health) in the SEPARATE invegent-dashboard repo (dashboard.invegent.com). use-server action (exec_sql service-role) + server-component card; no secret in client bundle. Prod: dashboard main=origin f9ab96e; Vercel prod deploy dpl_5E6GRFGZGusyoVuHnijy6BTpFEbG READY; /overview 307->/login. 3 dashboard files. Creatomate scoped to QA-v0 video formats -> live-confirmed 1/1 with qa (render_wallclock); HeyGen grey pending (not red). PRE-PROD BUG FIXED (preview): image_quote (render_engine=creatomate) wrongly counted missing-qa -> re-scoped by ice_format_key; image/other excluded + labelled out. CLEAN re-review 7fbfcc19/753042b9. NO content-engine runtime change (read-only over m.post_render_log + c.client). HeyGen qa pending natural avatar render (carry). Authority impact: none.**) Prior — (**v3.74 — QA VISIBILITY v0 DEPLOYED + MERGED + Creatomate LIVE-CONFIRMED (additive render_spec.qa observability): video-worker v3.1.4->v3.1.5 + heygen-worker v2.2.0->v2.3.0 deployed --no-verify-jwt (verify_jwt=false preserved), merged main 7709401. New pure fail-safe module qa.ts (buildRenderQa+safeQa, byte-identical both dirs). EXTENDS existing m.post_render_log/write_render_log spine, NO migration. PK correction: Creatomate qa.duration_semantics submit_to_poll_complete->render_wallclock; HeyGen submit_to_poll_detection. qa.engine NORMALIZED; p_render_engine COLUMN byte-unchanged. Gates: deno check 0 + 27/27 tests + branch-warden safe + external review CLEAN after correction (ccab89b5/bf6adec5). Creatomate LIVE-CONFIRMED (controlled smoke PP 3ee1c0ba): render_spec.qa present, render_wallclock, file_size_bytes=607584; 0 queue/0 publish. HeyGen qa deployed but pending natural avatar render (carry). Authority impact: none.**) Prior — (**v3.73 — VOICE PATH PRODUCTION RECOVERY COMPLETE / CLOSED (PP + NDIS recovered end-to-end; docs-only pass): video-worker v3.1.4 deployed+merged e388c33 (fn version 50, --no-verify-jwt). Proven root cause: service_role lacks SELECT on c.client -> getBrand() returns client UUID -> getVoiceId(UUID) resolves nothing. CORRECTION: NOT a stale PostgREST schema cache (reload tested, did not fix) and NOT a missing PP/NDIS voice secret (both present). Fix: voice_id.ts getVoiceIdForDraft resolves by client_id FIRST. Secondary blocker: ELEVENLABS_API_KEY invalid (401) -> PK rotated. Nuance: c.client SELECT grant gap REMAINS, not granted (code-hardening chosen), getBrand().clientSlug + storage-path unchanged. Verified: PP draft 3ee1c0ba + NDIS draft a9788b69 both succeeded end-to-end (ElevenLabs->Creatomate->storage); 0 queue / 0 publish. F-VOICE-LOWVOL-BACKFILL CLOSED historical / no bulk re-render (15 PP/NDIS + 3 CFW optional; LinkedIn text-only excluded). v3.71 runbook + v3.72 sprint-plan brief language corrected. Authority impact: none.**) Prior — (**v3.72 — PRODUCTION QUALITY RECOVERY SPRINT PLAN LANDED (docs-only): docs/briefs/production-quality-recovery-sprint-plan.md. Production-quality-before-engagement execution brief (NOT a roadmap; 04_phases.md untouched). Sequencing: D1 Voice Path Production Recovery FIRST -> D2 YouTube privacy read-only audit -> D3 Production QA Visibility; analytics/collection PARKED (FB reach #100, CFW FB collection stall). Estimate 3-5 lane-days gated on PK turnaround. D3 QA Visibility EXTENDS existing m.post_render_log/write_render_log spine (NOT greenfield). [NOTE: D1 now COMPLETE — see v3.73; the D1 candidate-secret framing was superseded (secrets were present; root cause = service_role SELECT gap on c.client).] 0 secret/code/DB/deploy/render/publish this pass. Branch B NOT AUTHORISED. P2 NOT READY. A1 observation pending. Authority impact: none.**) Prior — (**v3.71 — VOICE PATH PRODUCTION RECOVERY RUNBOOK LANDED (docs-only): docs/briefs/voice-path-production-recovery-runbook.md. The creatomate+elevenlabs failures are GENUINE voice-format failures — all 9 failed renders are video_short_kinetic_voice with narration; NOT routing/Creatomate/storage/false-alarm. Root cause: getVoiceId(clientSlug)=null -> video-worker fail-loud throw BEFORE TTS/Creatomate/storage (no provider spend). client_slug populated: ndis-yarns, property-pulse. Candidate secrets: ELEVENLABS_VOICE_ID_PP/PROPERTY_PULSE (PP likely missing), ELEVENLABS_VOICE_ID_NDIS/NDIS_YARNS. Material: video-worker approval-gated; the 3 pending voice drafts ALL approval_status=rejected -> wont render naturally; 0 approved pending voice drafts. Options A (set secrets, PK hard gate) / B (hold voice formats) / C (later explicit per-client mapping). Verification needs a PK-gated approved voice render. Rollback: if still failing after secret set, do NOT retry blindly; keep non-voice unaffected; never publish broken voice assets. 0 secret read / 0 write / 0 env / 0 worker / 0 provider / 0 render / 0 DB / 0 code / 0 deploy / 0 re-approval. DO NOT IMPLEMENT YET. Authority impact: none.**) Prior — (**v3.70 — RENDER PROVIDER CAPABILITY AUDITS: HeyGen + Creatomate briefs LANDED (docs-only): docs/briefs/render-provider-heygen-capability-audit.md + docs/briefs/render-provider-creatomate-capability-audit.md. HeyGen (identity-based): TWO surfaces — (1) heygen-worker RENDER path (talking_photo_id + voice_id + text TTS + single scene + hardcoded 720x1280 + submit/poll); (2) heygen-avatar-creator/heygen-avatar-poller ASSET-PROVISIONING path (photo-avatar generate/train/group/look extraction; reverse-engineered api2.heygen.com look-endpoint risk; standing DO-NOT-REDEPLOY). Cost opaque at render time, credits_used null. Safe future slice: estimated render-cost telemetry (NO provider call). Creatomate (composition-based): SOURCE mode (NOT templates) — video-worker builds full composition JSON in code for kinetic/stat NON-avatar paths (manual captions, ElevenLabs TTS for voice; hardcoded 1080x1920). Returns credits/cost but render_spec null. Major reliability risk: blocking in-request poll near Supabase EF time limit. Safe future slice: render_spec enrichment (observability-only); follow-up: async-ify Creatomate polling (HeyGen-style submit/poll) = separate gated code lane. Shared RI abstraction: provider-neutral interface = submit/poll/cost(reported-vs-estimated)/capabilities/renderMode; m.post_render_log = telemetry spine; provider IDs/payloads stay inside adapters. 0 DB / 0 migration / 0 code / 0 RPC / 0 deploy / 0 provider / 0 token / 0 render / 0 marker / 0 selection / 0 AGP. Branch B NOT AUTHORISED. P2 NOT READY. A1 observation pending. Authority impact: none.**) Prior — (**v3.69 — CREATIVE + RENDER INTELLIGENCE character-architecture brief LANDED (docs-only): docs/briefs/creative-render-intelligence-character-architecture.md (companion to character-model-v0). CI = what to say + who says it; RI = how to physically produce it; Character Model v0 = identity contract between them. CI owns topic/angle/hook/platform/format/narrator_mode/tone/series/host-or-persona SUGGESTION; RI owns provider/avatar-vs-non-avatar/render_style/asset-eligibility/cost/latency/quality/platform-capability/fallback. Boundary: CI may SUGGEST identity/persona, RI RESOLVES to eligible asset/provider; CI must NOT pick avatar assets; RI must NOT pick topic/angle/hook. Realistic vs animated = asset-level render_style / RI decision (same host identity, not a separate character, not a CI decision; animated dormant/unproven, do NOT activate, do NOT conflate with avatar-vs-non-avatar path). Instrument LATER (record-only): CI attribution completeness (host identity/narrator_mode/platform/format/tone/maybe hook-angle-topic) + Render-path telemetry (provider/render_style/cost/latency/success-failure/fallback_taken/quality). DO NOT automate yet: no Creative panel, no live avatar/persona/style-driven selection, no provider auto-switch, no animated activation, no Branch B, no AGP activation, no optimization until real engagement signal. Future slices (DO NOT IMPLEMENT YET): CI attribution completeness (record-only) + RI render-path telemetry (record-only). Deps: A1 observation must land first; AGP Phase 3.3 ON+parity before selection-driving; real signal before optimization; any future EF/code deploy = PK gate + --no-verify-jwt; host marker DB write separate+gated. 0 DB / 0 migration / 0 code / 0 RPC / 0 deploy / 0 provider / 0 marker / 0 selection / 0 AGP. Branch B NOT AUTHORISED. P2 Creative Format Evidence NOT READY. Authority impact: none.**) Prior — (**v3.68 — CHARACTER MODEL v0 + BRAND HOST DESIGNATION planning brief LANDED (docs-only): docs/briefs/character-model-v0-brand-host-designation.md. Avatar/character = governed identity system (intent→identity→asset→provider; brand host / narrator mode / stakeholder persona / character identity / avatar asset / provider identity); sequence brand host first → personas → character library; one host per brand = ceiling. Verified inventory: Invegent 0 / CFW 0 / NDIS-Yarns 14 (1 active A2-pinned HeyGen `83ff167d-a844-4e1c-9d1a-d8ff257c11bc`) / Property Pulse 14 (1 active `d6c422fb-9dc5-4bcc-aba6-2e6e942f2cdd`); all is_primary/is_default_host false/unset; NY/PP each 7 realistic + 7 ANIMATED (dormant; verified NOT 'cartoon'). Session-2 governance CLEAN TO DOC (stock/licensed HeyGen, avatar_type=stock, consent_required=false, no likeness gap; light carry = optional PK-gated provider ID verify). Markers non-consumed by LIVE selection (lookupAvatar doesn't read them; A2 pin drives) — dormant shadow resolver reads them, and it's OFF (AVATAR_SHADOW_TELEMETRY unset, 0 rows). Recommended NEXT (gated, NOT now): Brand Host Designation — set is_default_host=true for 83ff167d + d6c422fb only (exclude Invegent/CFW) via auditable migration + PK gate + external review, zero live-selection change, reversible. Pillars: Creative Intelligence attribution LATER (P2 NOT READY); Render Intelligence = how/provider/style/cost/quality; AGP selection-driving waits on Phase 3.3. 0 DB / 0 migration / 0 code / 0 marker write / 0 selection change / 0 AGP activation / 0 provider call. Branch B NOT AUTHORISED. Authority impact: none.**) Prior — (**v3.67 — ICE STATE-OF-RECORD RECONCILIATION (docs-only): current truth captured before avatar/characters · Creative Intelligence · Render Intelligence planning. (ICE = the Invegent Content Engine PRODUCT; Invegent is the company/platform owner.) Verified read-only: CE main `2b436d9` (0/0); insights-worker deployed `insights-worker-v14.5.0` (fn version 69, verify_jwt=false); r.avatar_resolution_shadow rows=0 (AVATAR_SHADOW_TELEMETRY OFF). (1) OPINTEL-V1-ICE-HEALTH-CARD LIVE/RECONCILED/CLOSED (v3.66, dashboard prod `ea45ab9`). (2) AGP D-01 Gate-3 Phase 3.3 activation+soak runbook COMMITTED+PUSHED (`f707908`, docs/briefs/agp-d01-gate3-phase3.3-activation-soak-runbook.md) — planning only; telemetry OFF, no activation / no avatar-selection / no Branch B / no lookupAvatar change; flag-enable a standing PK BLOCK. (3) ENGAGEMENT-SIGNAL-RECOVERY A1 MERGED+DEPLOYED (`2b436d9`, insights-worker v14.5.0; reach fallback ["post_impressions_unique","post_impressions"]) — DEPLOYED but OUTCOME-PENDING, NOT closed: await Invegent ~03:15 UTC cron → inspect m.post_performance.raw_payload.calls → Outcome A post_impressions 200 = reach recovered / Outcome B both #100 = A1 safe no-op → PK-gated live Graph probe. 0 DB / 0 migration / 0 schema / 0 RPC / 0 token / 0 provider / 0 dashboard / 0 AGP change. (4) P2 CREATIVE FORMAT EVIDENCE NOT READY / BLOCKED (engagement/reach signal too weak / partially blind) — do NOT build the Creative panel yet. (5) YouTube privacy HISTORICAL only (recent publishes public; current YT blocker = low audience/distribution, not privacy). (6) Next strategic areas PLANNING ONLY, not active implementation: avatar/characters · Creative Intelligence · Render Intelligence. Branch B NOT AUTHORISED. Authority impact: none.**) Prior — (**v3.66 — OPINTEL-V1-ICE-HEALTH-CARD SHIPPED TO PRODUCTION (P5 Operator Intelligence v1, first slice): read-only ICE Health card live on /overview (dashboard.invegent.com), zones Publishing Health + Data Freshness. Dashboard repo `invegent-dashboard` prod HEAD `ea45ab9` (local=remote, 0/0), Vercel production deploy READY (`dpl_7yDTHAJTbGB5xmFdaCocZJ4qF9YB`). 3 dashboard files (actions/ice-health.ts, components/overview/IceHealthCard.tsx, app/(dashboard)/overview/page.tsx); read-only over existing m.*/c.* via exec_sql — **0 DB / 0 RPC / 0 EF / 0 content-engine runtime change / 0 write-path**. Gates: tsc clean + 18/18 thresholds + external review `fae67bcd` (agree) + Session 2 review CLEAN + PK preview visual check + microcopy fix (ICE health→ICE Health) + PK production approval. Follow-up: usage soak before next Operator slice; default next: Needs You Now; alt: Fresh Samples Panel; 24h Pulse deferred (overlap). This pass: CE-register reconciliation only (sync_state + action_list), 0 dashboard-code / 0 DB / 0 EF / 0 deploy / 0 next-slice. Authority impact: none.**) Prior — (**v3.65 — F-INSIGHTS-WORKER-COLLECTION-RUNTIME DEPLOYED+VERIFIED: insights-worker v14.4.0→v14.4.1, MAX_POSTS_PER_CLIENT 50→25 (smallest safe fix; pure constant + version bump, aggregation-first + per-client selector byte-identical, no parallelization, 0 DB / 0 migration / 0 cron / 0 grant). Root cause: at ~3s/post a 50-post client (NDIS 253 / PP 240) ran ~150s, hitting the EF wall-clock + 120s cron timeout (freshness NOT at risk — aggregation-first). Commit `4077b02` (ff-merged + pushed, origin 0/0); fn version 68 DEPLOYED (PK-run `--no-verify-jwt`, verify_jwt=false preserved). Gates: deno check 0 + branch-warden safe + external review `8ee7b35c` (escalated, no defect) → PK approved cap=25. Post-deploy (live, read-safe): GET v14.4.1; NDIS large-client run → HTTP 200 in 53.2s, scope_mode=client_scoped, total_processed=25 (cap honored), first_time=8 (never-collected drained first), windows_computed=50 (30/90 fresh). Carry: F-INSIGHTS-WORKER-COLLECTION-RUNTIME → CLOSED. Register commit pending PK. Authority impact: none.**) Prior — (**v3.64 — F-PUBLISHER-ASSETGUARD-LINKEDIN DEPLOYED+VERIFIED: LinkedIn interim assetless-publish guard shipped (publisher asset-guard coverage complete). `linkedin-zapier-publisher` v1.1.0→v1.2.0 DEPLOYED (fn version 33, PK-run `--no-verify-jwt`, `verify_jwt=false` preserved); `linkedin-publisher` v1.2.0→v1.3.0 committed/undeployed (gated B24/F06). Ports the FB guard to BOTH LinkedIn EFs: non-text recommended_format → BLOCK (`asset_guard_blocked:<reason>` + `m.post_publish` failed audit row, draft preserved, never text-fallback), text → publish unchanged; + defensive platform-isolation skip. Commit `891f6eb` (rebased onto 51415ed, ff-merged + pushed, origin 0/0). Gates: deno check 0 + deno test 22/22 + branch-warden safe + pre-deploy queue 0-stranded + external review `e1d81a76` (agree, hash fbafe50e). Post-deploy (read-only, not live-invoked): GET v1.2.0, verify_jwt=false, 0 LinkedIn false-positive blocks, 7 text rows intact. GUARD-ONLY (asset_backstop.ts parity = future follow-up). Carry: F-PUBLISHER-ASSETGUARD-LINKEDIN → CLOSED. Register commit pending PK. Authority impact: none.**) Prior — (**v3.63 — F-INSIGHTS-WORKER-WINDOW30-90-FRESHNESS RECOVERY DEPLOYED+VERIFIED: insights-worker v14.4.0 (commit `e0fc5e4`, fn version 67, deployed `--no-verify-jwt`, `verify_jwt=false` preserved) — (#1) aggregation-first (`computeFormatPerformance` global, runs BEFORE collection) + (#2) honors the per-client cron body selector `client_publish_profile_id` (D-A2: missing/invalid body → aggregation-only, never the full sweep); single-file EF change, byte-identical hot path, **0 DB / 0 migration / 0 cron / 0 grant**. Root cause: ~150s wall-clock 504/546 from the all-4-client sweep (EF ignored the selector the crons already send) starved the tail window-30/90 writer (frozen since 06-12/06-13). Live-verified: GET v14.4.0; bodyless→`no_client_scope` (windows_computed=50, 3.8s); Invegent `client_scoped` 36/36; windows 30/90 `computed_at` 2026-06-19 04:42 (50 per-row worker stamps), 0/7 unaffected. External reviews `6754590e` (design) + `53724307` (final diff, agree); branch-warden safe. Residual (mitigated, NOT freshness-blocking): NDIS-Yarns 253 + Property-Pulse 240 cap at 50/run → collection ~150s may still 504, but aggregation-first keeps 30/90 fresh regardless. Carry change: **F-INSIGHTS-WORKER-WINDOW30-90-FRESHNESS → CLOSED**; **OPENED F-INSIGHTS-WORKER-COLLECTION-RUNTIME (P3/P4)**. Register commit pending PK. Authority impact: none.**) Prior — (**v3.62 — AGP GATE-3 AUTHENTICATOR-REVOKE-CLEANUP CLOSED: inherited `authenticator` SELECT on `r.avatar_resolution_shadow` REVOKED (docs-only register reconciliation; 0 DB / 0 migration / 0 deploy / 0 code / 0 flag-enable / 0 Branch B / 0 marker backfill / 0 resolver change / 0 production action this pass). Prior step (NOT this pass): commit `ee5d432` pushed the migration; applied successfully as `20260618092646_agp_gate3_revoke_authenticator_select_avatar_shadow` (single `REVOKE SELECT ON r.avatar_resolution_shadow FROM authenticator`; bare REVOKE → idempotent; touches nothing else). End-state (from apply step): `authenticator` SELECT removed; `anon`+`authenticated` remain without SELECT (anon never targeted — already held none); `service_role` retains required access; RLS ENABLED with 0 policies (deny-by-default unchanged); telemetry rows=0; no EF deploy; `AVATAR_SHADOW_TELEMETRY` OFF/unset; no Branch B / marker backfill / live resolver change. Not a live exposure (hardening only): schema `r` not REST-exposed (PGRST106) + deny-by-default RLS; `authenticator` is the pre-role-switch connection role, not client-reachable. Verified read-only (LOCAL GIT ONLY): `ee5d432` on `main`, one 38-line migration file; repo↔prod timestamp drift recorded — repo file `20260618133000_…` vs applied `20260618092646_…` (identical name suffix). Carry change: AGP-GATE3-AUTHENTICATOR-REVOKE-CLEANUP → CLOSED. Phase 3.3 flag-enable still BLOCKED pending explicit PK; #4 cutover BLOCKED; Branch B NOT AUTHORISED. Authority impact: none.**) Prior — (**v3.61 — INSIGHTS FIX 2 CLOSURE RECONCILIATION: F-INSIGHTS-FIX2-AGGREGATION-OWNERSHIP CLOSED as stale / already-implemented (read-only readiness review + docs-only register reconciliation; 0 DB / 0 code / 0 migration / 0 deploy / 0 Advisor behaviour change / 0 production mutation this pass). Live `public.refresh_post_format_performance()` (SECDEF, `search_path=pg_catalog,public,m`) windows CTE reads `unnest(ARRAY[7, 0])` — `30` appears nowhere (`mentions_30=false`); cron no longer writes rolling window 30. insights-worker (`supabase/functions/insights-worker/index.ts:319`) loops `for (const windowDays of [30, 90])` → worker remains sole owner of windows 30 & 90. Ownership now DISJOINT: cron {7,0} ∩ worker {30,90} = ∅ → the 03:15 dual-writer clobber is structurally eliminated (Option B end-state, PK 2026-06-13). Row evidence (`m.post_format_performance`, 4 clients): window-0/7 fresh through 2026-06-18 03:15 (cron); window-30 last 2026-06-13 03:15; window-90 last 2026-06-12 03:16. Original apply packet STALE / no-op → RETIRED; no migration apply needed. Repo-provenance drift recorded: live {7,0} not from a tracked post-brief migration — only `20260320075822 format_performance_write_fn` + `20260331092416 refresh_post_format_performance_function` registered, both PREDATE the brief (2026-06-13), neither has a repo SQL file (known repo↔prod drift); end-state correct, provenance missing (optional future repo-migration backfill). Carry change: F-INSIGHTS-FIX2-AGGREGATION-OWNERSHIP → CLOSED. OPENED separately (explicitly NOT part of Fix 2): F-INSIGHTS-WORKER-WINDOW30-90-FRESHNESS (P3 OPEN) — windows 30/90 stale since 2026-06-13 / 2026-06-12; `ai-worker/index.ts:382` reads window-30 for format recommendations; needs separate read-only investigation; NOT investigated/fixed this lane. Not done: migration/function/worker/cron/Advisor/AGP/Branch B/security change; freshness follow-up not investigated. Authority impact: none.**) Prior — (**v3.60 — AGP D-01 GATE-3 PHASE 3.2 CLOSURE: shadow avatar resolver + telemetry APPLIED + DEPLOYED + INDEPENDENTLY VERIFIED (docs-only register reconciliation; 0 production action this pass). Migration `20260618084054_agp_d01_3_avatar_shadow_resolver_telemetry` registered; `r.avatar_resolution_shadow` exists (RLS deny-by-default, 0 policies, service_role-only); `public.resolve_and_record_avatar_shadow(uuid,uuid,text,text,text,text,text,uuid)` exists (SECDEF, search_path=public, service_role-only EXECUTE); heygen-worker v2.2.0 / EF version 36 deployed; verify_jwt=false preserved; AVATAR_SHADOW_TELEMETRY OFF/unset; telemetry rows=0; live avatar selection + lookupAvatar UNCHANGED; no Branch B; no marker backfill (is_primary=0/is_default_host=0/28). Gate map: #1 CLOSED; #2 CLOSED; #2-migration APPLIED/VERIFIED; #3 shadow resolver + telemetry APPLIED+DEPLOYED+VERIFIED (Phase 3.2 CLOSED; Phase 3.3 flag-enable BLOCKED pending explicit PK instruction); #4 cutover BLOCKED. Follow-up backlog: AGP-GATE3-AUTHENTICATOR-REVOKE-CLEANUP (low-risk posture cleanup; revoke inherited `authenticator` SELECT on `r.avatar_resolution_shadow`; not a live exposure — RLS deny-by-default + schema `r` not REST-exposed; defer as small separately-gated migration). Prior: v3.59 — OPERATIONS RECONCILIATION: D-002 security remediation FORMALLY CLOSED (`open_findings.md` authoritative; grant batches 1–10 + `0011` search_path batches 1–5; `auth_client_id()` retained for RLS) + Governor architecture Phase 0 LANDED (`CLAUDE.md` review-protocol + `docs/governance/governor-architecture.md`; NO implementation). Prior: v3.58 — AVATAR-GOVERNANCE-PLANNING D-01 #2-migration APPLIED & VERIFIED (additive dormant metadata; selection inert — A2 pin still drives). Migration `agp_d01_2_migration_brand_avatar_priority_default_markers` (version 20260616015419) via Supabase apply_migration only (no repo SQL commit): `c.brand_avatar` gains `is_primary` + `is_default_host` (boolean NOT NULL DEFAULT false) + partial unique `uq_brand_avatar_primary_per_role_style` (stakeholder_id, render_style WHERE is_primary) and `uq_brand_avatar_default_host_per_client_style` (client_id, render_style WHERE is_default_host). Review `bebea250-639a-43cc-9092-495ff7503e96` agree/low/high/proceed (non-escalated; resolved_by=PK). Verified (db-rls-auditor, read-only): columns+indexes present; no backfill (primaries=0, default_hosts=0, total=28; all markers false); no resolver/avatar activation. Gate map: #1 CLOSED; #2 CLOSED; #2-migration APPLIED/VERIFIED; #3 shadow resolver + telemetry PENDING (next); #4 cutover BLOCKED. Not authorised: backfill/resolver/selection change/avatar activation/Branch B; further DDL via apply_migration only. This register pass docs-only; 0 production mutation this pass. Authority impact: none.**) Prior — (**v3.57 — AVATAR-GOVERNANCE-PLANNING D-01 #2 schema direction RATIFIED by PK (planning only). Ratified: two booleans on `c.brand_avatar` — `is_primary` (role_exact) + `is_default_host` (default_host fallback), NOT NULL DEFAULT false; partial unique — one primary per `stakeholder_id`+`render_style`, one default host per `client_id`+`render_style`; `brand_avatar_id` ASC final tiebreaker only; markers are preferences among eligible rows only (eligibility separate: is_active/consent/heygen renderability/render_style). Rejected for now: integer priority (future-compatible option only), mapping table (over-engineered). Review `abcf77cb-22ff-4c6c-bce7-3dd20502d179` escalated (partial/medium/medium) but resolved by PK at 2026-06-16 00:49 UTC (integer-priority → future-compatible, backfill concern → D-01 #3). Gate map: #1 CLOSED; #2 CLOSED; #2-migration PENDING (next); #3 shadow proof PENDING; #4 cutover BLOCKED. Not authorised: migration/column/index/backfill/resolver/avatar activation/Branch B; any future DDL via `apply_migration` only. Read-only docs; 0 code/DB/migration/deploy/production mutation. Authority impact: none.**) Prior — (**v3.56 — AVATAR-GOVERNANCE-PLANNING D-01 #1 deterministic avatar-resolution design RATIFIED by PK (planning only). Ratified: meaningful priority/default semantics; `brand_avatar_id` ASC tiebreaker-only; A2 pin stays active; shadow-first; 100% parity + 0 drift before cutover. Review `75b1ef94` escalated but resolved by PK (empirical concern → D-01 #3 shadow proof). Gate map: #1 RATIFIED/CLOSED; #2 schema PENDING; #3 shadow proof PENDING; #4 cutover BLOCKED. Not authorised: schema/migration/code/deploy/avatar activation/lookupAvatar/Branch B. Read-only docs; 0 code/DB/deploy/migration/production mutation. Authority impact: none.**) Prior — (**v3.55 — AVATAR-GOVERNANCE-PLANNING read-only Branch B readiness audit COMPLETE. Verdict READY WITH PRECONDITIONS; Branch B implementation NOT AUTHORISED. `c.brand_avatar` only live substrate; NY/PP 14 avatars each / 1 active A2 pin; CFW+Invegent zero inventory; `lookupAvatar` role-capable but 0 role_filter renders; `stakeholder_role` 0/80 on drafts; `LIMIT 1` no `ORDER BY`. Preconditions: determinism n>1, suggestion→consumption bridge, inventory/no-match policy, activation governance, role_filter production validation, lookupAvatar hardening, consent review. Read-only; 0 code/DB/deploy/migration/D-01/avatar activation. Authority impact: none.**) Prior — (**v3.54 — F-SERIES-AVATAR-DIFFERENTIATION Stage 1.5 — HYBRID NARRATOR (opt-in multi-perspective presenter mode) DEPLOYED / VERIFIED WITH OBSERVATIONS. series-outline v1.5.0→v1.6.0 (commit `d5ee389`, Supabase function version 49, live GET `series-outline-v1.6.0`, `verify_jwt=false` preserved, `deno check` exit 0); D-01 `c02ab9af-c384-42e9-8fcf-44a003fd5025` (agree/low/high); PK approved Option C (Hybrid). Conservative `detectMultiPerspective` (signal phrase OR ≥3 distinct active brand roles); default Brand Host path byte-identical to v1.5.0; multi-perspective mode reads active `c.brand_stakeholder` (read-only) + injects closed role set, `avatar_preference` encodes `role_code=`/`presenter=`; adds `narrator_mode`/`narrator_mode_trigger` telemetry. SHADOW-ONLY downstream: no consumed `stakeholder_role` write, no heygen-worker/ai-worker change, no avatar activation, no schema/migration; `save_series_outline` args unchanged. Observation: verified deploy/version/flag only — trigger/role-injection NOT exercised against a live series this lane. Carry change: F-SERIES-AVATAR-DIFFERENTIATION Stage 1.5 → CLOSED as DEPLOYED/VERIFIED WITH OBSERVATIONS; Branch B (avatar/voice RENDER per persona) stays P3 DEFERRED; OPENED AVATAR-GOVERNANCE-PLANNING (P3, PLANNING ONLY). Authority impact: none. Docs-only this pass; 0 production mutation.**) Prior — **v3.53 — F-SERIES-FORMAT-DIVERSITY COMPLETE / DEPLOYED / VERIFIED (per-platform format resolution; avatar-collapse + YouTube-drop failure modes CLOSED). Stage 2 (series-outline v1.5.0 UNION, live) + Stage 0 (`platform_support` corrected to publisher reality on 9 formats; migration `…stage0_platform_support_publisher_reality` commit `0aecc23f`) + Stage 1 (`fan_out_episode` per-platform resolver; migration `…stage1_fan_out_episode_per_platform` commit `aa05c168`; md5 `7934ce55`→`d305aec0`). D-01 `fa5c1ab6-c034-4979-b698-754296d55ff2` (agree/medium/high). Verified read-only: retry_episode/create_manual_slot_internal/get_studio_capabilities UNCHANGED; FB→carousel/image_quote/text (no video), IG→carousel/image_quote/video_short_avatar, LI→text, YT→video_short_avatar+kinetic/stat variants; no platform dropped; no publisher-impossible FB/LI format. Remaining OPEN: YT positive-publish watch, YT-CROSSPUB-HISTORICAL-REMEDIATION (P2), F-AVATAR-RENDER-LATENCY-RECOVERY (PARTIAL/OPEN), F-SLOT-SCHEDULE-FIDELITY (P4 WATCH), F-SERIES-AVATAR-DIFFERENTIATION (Branch B, P3 DEFERRED). Authority impact: none. Docs-only this pass; 0 production mutation.**) Prior — **v3.52 — F-AVATAR-RENDER-LATENCY-RECOVERY Fix 1 DEPLOYED / VERIFIED (lane stays OPEN — PARTIAL COMPLETE). Added `video_short_avatar` to the video-format IN-list in BOTH `m.gate_queue_on_asset_status()` + `m.release_queue_on_asset_ready()` (migration `favatar_render_latency_recovery_fix1_gate_release_add_avatar`, commit `c7a41af`; D-01 `6516ed5a-8fea-4d20-af6a-304b9cd70a3d` agree/low/high; gate md5 `38fd402d`→`658a09eb`, release `81a38388`→`dbeda9b9`). Verified (PGlite 12/12 + live BEGIN…ROLLBACK): triggers bound; avatar present in both video lists, absent from image lists; avatar pending→held +4h; avatar generated→released to intended slot time (no NOW()-collapse, not stuck +4h); failed/skipped/dead historical rows untouched; 0 test rows leaked. Forward mechanism repaired; historical population intentionally UNTOUCHED (no remediation/requeue/retry/publisher changes). Still open: ~20-row historical contaminated population, IG generated/published-readiness question, recovery-sweep design, YT historical-remediation dependency. Authority impact: none. Docs-only this pass; 0 production mutation.**) Prior — **v3.51 — Three-series settlement + WordPress direct-read audit. **WP-DIRECT-READ-ISOLATION-AUDIT → COMPLETE / VERIFIED (NO defect):** `wordpress-publisher` v1.0.0 (sha `a446e9ab`) is direct-read but **deliberately filters `pd.platform='facebook'`** (+approved/title-body/len>50/NOT EXISTS website publish/LIMIT 3) and posts FB text as a WP article — never selects ig/li/yt; `website` is a non-router owned_publish platform, never a `post_draft.platform`. Blast radius 26 rows all-time, CFW-only, facebook-source, all published; 11/26 image/video-source rendered text-only = content-fidelity caveat (editorial), NOT an isolation defect → no fix, no historical remediation. Three-series proof settled: F-SERIES-PERSONA-CAPTURE stays COMPLETE (5 PASS, 33 child slots); schedule-fidelity HEALTHY → F-SLOT-SCHEDULE-FIDELITY stays P4/WATCH; YT isolation holding (0 cross-publish) → YT positive-publish stays WATCH/OPEN. **NEW carry F-AVATAR-RENDER-LATENCY-RECOVERY (OPEN)** — late `video_short_avatar` renders not auto-recovered after asset-guard skip (`release_queue_on_asset_ready` excludes video_short_avatar; `heal_reset_stuck_queue` resets only running/locked). Unchanged: YT-CROSSPUB-HISTORICAL-REMEDIATION (P2 OPEN), F-SERIES-AVATAR-DIFFERENTIATION (P3 DEFERRED), F-SERIES-FORMAT-DIVERSITY (P3 DEFERRED). Authority impact: none. Docs-only this pass; 0 production mutation.**) Prior — **v3.50 — F-YT-PLATFORM-ISOLATION YouTube cross-publish P0 DEPLOYED / VERIFIED / CLOSED for future publishing. youtube-publisher **v1.12.0** (commit `258d213f`; version 52; `verify_jwt=false`; deno 0; live GET v1.12.0) — direct-read SELECT now has `.eq('platform','youtube')` + a defensive in-loop `draft.platform !== 'youtube'` skip, so non-YouTube drafts with a generated `video_short_avatar` asset can no longer be uploaded to YouTube. D-01 `e0c87546-ffe0-409a-ba60-40a037793133` (proceed/agree/medium/high); deployed `--no-verify-jwt`. Post-deploy: 0 new cross-publishes, 0 non-YouTube drafts stamped with YouTube IDs, no runtime errors. **Positive-publish = WATCH** (no eligible YT draft post-deploy; first legit YT draft must publish while FB/IG/LI siblings get NO YouTube row). **Carries OPEN:** YT-CROSSPUB-HISTORICAL-REMEDIATION (P2 — already-uploaded cross-platform YT videos UNRESOLVED); WP-DIRECT-READ-ISOLATION-AUDIT (P2 — audit WordPress publisher for same class). No DB / no WordPress patch / no historical remediation this lane. Authority impact: none. Docs-only this pass; 0 production mutation.**) Prior — **v3.49 — F-SERIES-PERSONA-CAPTURE persona INTENT capture/persistence COMPLETE / DEPLOYED / VERIFIED (avatar differentiation explicitly DEFERRED). DB: `save_series_outline` persists `persona_label`/`avatar_preference`/`persona_notes` (md5 `ec5db304`→`93cf6ff9`; DB D-01 `2db87754-5af1-4727-9f0b-440bbcea8d91`). EF: series-outline **v1.4.0** (commit `4d9b82c`; version 47; `verify_jwt=false`; deno 0; live GET v1.4.0; persona-only delta — prompt requests persona + episodeRows maps via cleanStr; resolver logic unchanged; EF D-01 `d311d11c-bae5-487f-b0d4-27301baa3a2a` partial/escalate=true, PK approved despite escalation). `fan_out_episode` UNCHANGED, already carries persona into child brief + `creative_intent.source_material.persona`. 0 prod series mutated. **Boundary:** avatar differentiation NOT complete — deferred Branch B / avatar-governance (no `stakeholder_role`/heygen/`brand_avatar`/A2/avatar-selection change). New carry **F-SERIES-AVATAR-DIFFERENTIATION (Branch B, P3 DEFERRED)**. **Stage 4 (UI) NOT started.** Rollback: EF redeploy v1.3.0 `--no-verify-jwt`; DB re-apply prior `save_series_outline` md5 `ec5db304`. Authority impact: none. Docs-only this pass; 0 production mutation.**) Prior — **v3.48 — F-SLOT-SCHEDULE-FIDELITY DOWNGRADED to P4 / WATCH (post-Fix-1 verification window). Backlog/rule-slot latency discovery (read-only) complete: rule-slot early ~23.5h bucket was the OLD release_queue_on_asset_ready NOW()-collapse → already fixed by Fix 1 (rule-slot semantics explained/resolved unless watch disproves); multi-day late tail = historical Apr/May YouTube OAuth outage + backlog drain (no June-origin recurrence); no capacity issue (12 queued / 0 needs_review / 1 queued >3d); `portal_approve_draft` has ZERO production usage ever → portal-path drift is dormant / a client-portal launch prerequisite, not current drift; `fan_out_episode` past-date clamp (now()+1h) = separate design question, not an active schedule bug. Watch criteria: early_1h_1d → within_1h; no new June-origin >3d tail; median abs delay → publisher-cron cadence. **NOT fully closed** (P4/WATCH). Portal-path fix carried as Phase-3 client-portal launch prerequisite. No implementation lane opened. Authority impact: none. Docs-only this pass; 0 production mutation.**) Prior — **v3.47 — F-SLOT-SCHEDULE-FIDELITY Fix 1 APPLIED & VERIFIED (early-publish-on-asset-ready closed); carry STAYS OPEN / PARTIALLY RESOLVED. `m.release_queue_on_asset_ready()` redefined (md5 `0fa656e1`→`0cdede8d`) so releasing a held queue row on asset-ready no longer collapses `queue.scheduled_for` to NOW() — now `GREATEST(COALESCE(post_draft.scheduled_for, slot.scheduled_publish_at), NOW())` (existing queue.scheduled_for NOT used as fallback — may be the artificial +4h hold); future intended stays future, past/no-intent releases now. Migration `fslot_schedule_fidelity_fix1_release_preserve_intent`; D-01 `dc6d3bde-051f-4ea3-9fca-9f09217b1033` (partial/escalate=true — PK approved Option A and applied despite reviewer escalation). Trigger `trg_release_queue_on_asset_ready` bound; `gate_queue_on_asset_status` + `publisher_lock_queue_v1`/`v2` unchanged. Verified: future-intended stayed future / past-intended released now / no-intent released now / trigger rollback test 0 rows leaked / early-publish mechanism closed / 0 prod test rows. **F-SLOT-SCHEDULE-FIDELITY remains OPEN** — follow-ups: (1) portal-path drift (`draft_approve_and_enqueue`/`portal_approve_draft` use `get_next_scheduled_for`, can discard intent); (2) `fan_out_episode` past-date clamp `now()+1h`; (3) rule-slot 24h fill-window/recurring semantics. Authority impact: none. Docs-only this pass; 0 production mutation.**) Prior — **v3.46 — Series v2 Stage 3.5b (platform-aware `retry_episode`) APPLIED & VERIFIED in production; **Stage 3.5 overall CLOSED**. `retry_episode` redefined (md5 `d85366fd`→`3d21cd0f`, signature `p_episode_id uuid, p_mode text, p_created_by text` unchanged) so re-created child slots re-resolve a valid format via `get_studio_capabilities` before `create_manual_slot_internal` — keep original `format_preference` if still valid / pick deterministic first-valid alternative / fail loud `no_valid_format_for_platform` if none (no doomed re-create); adds `format`/`format_changed` audit fields. Migration `stage3_5b_retry_episode_platform_aware` (commit `5fc0fa4`); D-01 `6917cc0e-a944-40b2-ab98-77ec27f448ad` (proceed/agree/medium/high). `create_manual_slot_internal` unchanged (md5 `9ec86b61`, hard backstop retained); published children `already_published`-protected, in-flight `in_flight_preserved`, dead/failed history retained; 0 test rows leaked, 0 production series mutated. **Repo↔prod apply-state drift reconciled:** the migration file/commit header reads "STAGED / NOT APPLIED" but production IS applied (live `retry_episode` carries the 3.5b logic) — the action list records prod truth, not the stale header (migration file not edited this pass). Deterministic first-valid repair = **intentional interim** (NOT Stage 4 format optimization). **Stage 4 NOT started.** Carries preserved: F-SERIES-FORMAT-DIVERSITY (P3 DEFERRED), F-SLOT-SCHEDULE-FIDELITY (P3 OPEN), F-SERIES-PERSONA-CAPTURE (P3 DEFERRED). Docs-only this pass; 0 production mutation. (sync_state v3.46 banner pending a separate pass.)**) Prior — **v3.45 — Series v2 Stage 3.5a (platform-aware outline generation) DEPLOYED & VERIFIED — series-outline → `series-outline-v1.3.0` (commit `65d6dc43`; single file `supabase/functions/series-outline/index.ts`; deployed CLI `--no-verify-jwt`, `verify_jwt=false` preserved; D-01 `b45da2c8-9189-491f-ad83-e9bc90936e8b` proceed/agree/medium/high). Outline now reads `platforms[]`, calls `get_studio_capabilities`, constrains episode format to the resolver-valid common set across selected platforms (video formats now possible for YouTube-inclusive series; text excluded where unsupported), fails loud `no_valid_format_for_platform_set` (422) on empty set — never silently text. Interim: FB+IG+LI+YT resolves to `video_short_avatar` only (intentional safety interim; per-platform-per-episode diversity deferred). Downstream gates (fan_out_episode/create_manual_slot_internal/Advisor/compliance/render/publisher) UNCHANGED. **Stage 3.5a COMPLETE/DEPLOYED/VERIFIED; Stage 3.5b (retry_episode platform-awareness) OPEN next carry.** Carries: F-SERIES-RETRY-PLATFORM-AWARENESS (P2), per-platform-per-episode format diversity (P3), F-SLOT-SCHEDULE-FIDELITY (P3), persona capture lane (P3). Docs-only this pass; 0 production mutation.**) Prior — **v3.44 — Facebook Publisher Assetless-Release Guard CLOSED & DEPLOYED (publisher v1.9.0; FB asset-less publish leak RESOLVED for Facebook; merge `0216b79` / close-out `7a4e3c3`; live version publisher-v1.9.0 / Supabase function version 91; `verify_jwt=false` confirmed; D-01 `fcf2b32d-f234-4a36-bb82-60cfe8038e82`). Interim guard / "T2-Lite" (existence/status only), NOT full T2. image / carousel / video / unknown formats no longer fall back to text on FB; failed/missing/pending-timeout assets → skipped/blocked with `asset_guard_blocked` reason; unsupported FB video → `fb_video_publish_not_supported_interim`. Carries (NEW): F-PUBLISHER-ASSETGUARD-LINKEDIN (P2 — interim assetless guard for the LinkedIn publisher; narrowed from F-PUBLISHER-ASSETGUARD-LI-IG-YT after the non-FB publisher asset-guard audit found Instagram + YouTube not exposed); full T2 = durable replacement (deferred); FB video publishing = separate feature work. Rollback: redeploy publisher-v1.8.0 blob `56b1d1da` via Supabase CLI `--no-verify-jwt`, no DB rollback. Docs-only this pass; 0 production mutation.**) Prior — **v3.43 — Content Studio T0 (governed manual path) + T1 (Creative Intent / Content Package parent) + T1 Visibility (Ideas tab) + active-client roster fix + Fix 1 (insights-worker v14.2.0 FB engagement repair) ALL CLOSED & DEPLOYED — register reconciliation only, docs-only, 0 production mutation this pass. Note: "Content Studio T0/T1/T2" tiers are distinct from the existing "T1 authority doc" (`current-ice-decision-tree.md`). Carries opened/kept: F-INSIGHTS-FIX2-AGGREGATION-OWNERSHIP (P2 — next workstream); F-INSIGHTS-DEPLOY-VERSION-DRIFT (P3); F-SLOT-SCHEDULE-FIDELITY (P3); F-INTENT-STATUS-ROLLUP (P3); F-PUBLISH-QUEUE-DANGLING-QUEUEID (P4); META-FIX1-PAGES-READ-ENGAGEMENT non-blocking; F-CONTENT-STUDIO-CLIENT-ROSTER CLOSED. T2 deferred until more rendered-asset evidence.** Prior — **v3.42 — F-OPTIONC-ENGAGEMENT-EVIDENCE-NULL diagnosed + carried P2 (engagement evidence feed structurally broken: D1 FB metric failure / D2 dual-writer clobber / D3 YT exclusion; Advisor fed fabricated zeros; Option C stays parked; register-only, 0 production mutation). **v3.42-R Stage R amendments:** FB root cause SPLIT (#100 dead metrics `post_engaged_users`+`post_impressions` under served v24.0 vs requested v19.0; #10 permission-blocked fields fallback); `post_reactions_by_type_total` verified 200 (reduced-basis replacement); Fix 1 brief amended (insights-worker v14.2.0 shape — NOT implemented/deployed); pg_net `paging` security incident purged (`159957`, audited path) + CFW token rotation PK follow-up + standing rule L-StageR-paging; Meta-side `pages_read_engagement` carry split out (non-blocking).** Prior — **v3.41 — A2 BRANCH A APPLIED: interim primary brand host v1 (`pin_primary_brand_host_v1`) — NDIS Yarns pinned to Support Coordinator (Realistic), Property Pulse to Buyer's Agent (Realistic); 26 `c.brand_avatar` rows deactivated / 0 deleted; CFW/Invegent untouched; rollback SQL held; D-01 `ab14f105` type-(c) accepted + PK exact approval phrase; Stage 3 passive telemetry watch OPEN; Option C remains PARKED (reference hypothesis only).** Prior — **v3.40 — F-ADVISOR-LI-OVERRIDE CLOSED (diagnosed — expected behaviour; evidence → Option C/A2 gate) + F-AIW-PREF-COL-HARDCODE amended (latent config trap) + F-PUBLISHER-STATUS-FLIP corroborated (register-only, 0 production mutation).** Prior — **v3.39 — F-FORMAT-MIX-NOFORMAT CLOSED (diagnosed — subsumed) + F-ADVISOR-RESPIN-ORPHAN-SLIDES 2nd instance (register-only, 0 production mutation).** Prior — **v3.38 — MONITOR DASHBOARD VISIBILITY (Slices 4–6) LIVE on production.** Three more read-only Monitor panels on `dashboard.invegent.com` (`invegent-dashboard`), below Slices 1–3: **Render + Avatar Health** (`m.post_render_log`: provider/format counts, success rate, error categories, `credits_used` null-rate, avatar_identity populated/missing + role coverage), **Performance Freshness** (client×platform: last collected, stale >48h + missing flags; YouTube stale / FB fresh / IG-LI-website gaps), **Pipeline Exceptions** (stuck/dead jobs, failed slots, slot skips by category, compliance-skip vs `dead_reason` conflation, queue orphans, approved-unpublished >3d). Read path = existing `exec_sql` service-role server actions (SELECT-only); **no DB objects, no migrations, no mutation, no worker/provider calls.** dashboard PR #7 MERGED (`417210c`); Vercel prod deploy `dpl_A9dr9scRF1u2346FbaEoQsaR9UJ4` READY (alias `dashboard.invegent.com`) — LIVE (verified by SHA + alias). Branch `monitor/render-perf-exceptions-panels` @ `1e26bc2` (5 files); tsc+build exit 0; screenshot-verified on local preview. Open follow-up: `/overview` `created_at` error (pre-existing, unrelated). 0 DB / 0 classifier / 0 Advisor / 0 policy / 0 Option C / 0 render-logic / 0 publisher / 0 compliance / 0 cron / 0 worker / 0 provider. Dashboard-visibility lane only. Authority impact: none. Prior — v3.37 — MONITOR DASHBOARD VISIBILITY (Slices 1–3) LIVE on production.** Three read-only panels under Monitor on `dashboard.invegent.com` (`invegent-dashboard`): Classifier v2 Health, Advisor Override, Format Mix — exposing classifier flattening, defaulted-vs-rule-matched, advisor override, and format mix. Read path = existing `exec_sql` service-role server actions (SELECT-only); **no DB objects, no migrations, no mutation.** PR #6 MERGED (`c168d2c`); Vercel prod deploy `dpl_7we82KUmm47j8jSEC4UsbSAa6tnc` READY (alias `dashboard.invegent.com`); **panels visually confirmed live by PK.** Branch `monitor/classifier-advisor-format-panels` @ `a2d4c82` (7 files). Open follow-up: pre-existing `/overview` `created_at` SQL error (unrelated; not investigated). 0 DB / 0 classifier / 0 Advisor / 0 policy / 0 Option C / 0 render-publisher change / 0 extra panels. Dashboard-visibility lane only (separate repo). Authority impact: none. Prior — v3.36 — ICE CLASSIFIER v2 REPAIR APPLIED to prod + validated.** Stage-1 bounded repair of the rule-based PL/pgSQL classifier per the 2026-06-12 concentration audit (`ef53676`). Applied to `mbkmaxqhsohbtwsqolns` (migration `classifier_v2_repair_and_telemetry` + 2 apply-time hotfixes): 3 dead source rules fixed (stat exact→substring, timely→gov/news source_name, analytical→email_newsletter), 6 regex/FP repairs, recency→immutable `created_at`, +9 explanation-telemetry columns on `f.canonical_content_body` (outward `content_class` unchanged), `v1`→`v2`, NULL-only picker + opt-in `m.reclassify_canonicals`. **Smoke PASSED** (A/B/C/D live). **100-row bounded backfill DONE** (70 analytical → 8 rule_matched / 62 defaulted — backstop-conflation observability win). **Remaining ~4,371 v1 rows INTENTIONALLY UNTOUCHED** (forward cron job 68 classifies new items v2; further backfill is a separate gated run). Branch `repair/classifier-v2-telemetry` @ `48ea045` pushed; **PR #7 MERGED** (merge commit `2be28040` — repo↔prod reconciled; merge ≠ deploy). NEW carries: remaining-v1 bounded backfill (opt-in, gated); `first_seen_at` drift data-quality bug; repo↔prod migration-version drift. **0 Advisor change / 0 policy reseed / 0 Option C reconnection / 0 deploy / 0 publisher/render change. Authority impact: none.** Prior — v3.35 cycle CLOSE — Option C decision-state arc recorded + parked (docs-only).** **Option C (policy envelope + advisor fit) = PREFERRED REFERENCE HYPOTHESIS, NOT approved for implementation**; advisor-led/constants model stays live; narrative-vs-format OPEN; **review gate w/c 2026-06-15 or on meaningful `avatar_identity` telemetry**; A2 + format-policy fork = ONE decision surface (avatar share unresolved). Dormant demand-grid subsystem documented (T1 §7 `baba5537`/`8e19dd9f`; ran live 2026-04-25, orphaned by the slot rebuild, never formally rejected); mix seed ~19% taxonomy-illegal; **no implementation/migration/wiring approved.** New carry: OPTION-C / FORMAT-POLICY FORK (OPEN, deferred to gate); A2 + F-FORMAT-MIX-UNWIRED amended. Governance Rules 1/2/3/7/6a + `Authority impact:` line + CCD >80KB ownership ADOPTED; **Rule 6b detection wiring outstanding.** 0 production / 0 code / 0 DB / 0 migration / 0 Edge Function / 0 deploy / 0 provider / 0 cron. Prior — v3.35 — Register cycle: authority repair recorded + slot carries queued (docs-only).** T1 authority doc `docs/architecture/current-ice-decision-tree.md` established (`160678b8`, with `last_verified` contract) + docs index routed to T1 in the same atomic commit; signage banners on `03_blueprint.md`/README/phases/concepts (`9d97f726`); authority spec `3958e4f`; v3.34 doc sync `97e25bd`. Queued 4 slot-level carries from the slot decomposition (`b74c8e50`): **P2 F-PUBLISH-LEDGER-STATUS-MISMATCH, P2/P3 F-IG-TEXT-PALETTE-GAP, P3 F-PUBLISHER-STATUS-FLIP-INCONSISTENT, P3 F-CFW-COMPLIANCE-PREGEN.** Governance adopted: T1 authority-doc rule, docs-index authority section, mandatory "Authority impact:" completion line, CCD ownership for >80KB register/authority patches, `last_verified` contract in T1. Rule 6b NOT implemented (no `05_risks` change, no Cowork reconciliation wiring — deferred). 0 production / 0 code / 0 DB / 0 migration / 0 Edge Function / 0 deploy / 0 provider / 0 cron. Prior — v3.34 — Content decision trace audit COMPLETE (read-only): report `docs/runtime/sessions/2026-06-11-content-decision-trace-audit.md` (commit `6fd5f4d`). All 5 backward traces GREEN (publish→…→feed_source→ingest_run); digest tables not in the live chain; ai-worker `callFormatAdvisor` is the final format decision-maker; the YouTube avatar preference is hardcoded in `m.materialise_slots` (the true home of A2); format = narrative proxy; avatar path = arbitrary-first (`lookupAvatar` LIMIT 1, no ORDER BY; `stakeholder_role` never emitted) despite NY 7 / PP 8 role-tagged active avatars. F-HEYGEN-RENDER-TELEMETRY first-row verification SATISFIED → CLOSED (2 heygen rows in `m.post_render_log` 2026-06-10, drafts `dcab6fba` + `d870f20a`). Conclusion recorded: **no-build recommended at this time**; **narrative engine not justified by current evidence** — the narrative layer remains an open architecture question, not a closed design decision. 5 NEW P3 carries: F-ADVISOR-RESPIN-ORPHAN-SLIDES, F-YT-QUEUE-ORPHAN-RECURRENCE, F-FORMAT-MIX-UNWIRED, F-AIW-PREF-COL-HARDCODE, F-EFDRIFT-REGISTER-STALE-YTPUB; A2 carry amended with three branches. 0 production mutation / 0 DB / 0 D-01 / 0 deploy / 0 provider call / 0 cron change. Prior — v3.33 — Asset Production routing synthesis (read-only): NO production routing defect. "Avatar zero-output" was a measurement error — correct metric is YouTube video_status/youtube_video_id, not social approval_status. Current 30-day window: avatar realised 18/22, share ≈8% overall / ≈12% NDIS+PP; prior ~19.7% skew NOT reproduced. 20/22 avatar selections are A2 overrides → remaining issue is policy (keep volume vs relax content-fit), not a defect. OBS Stage 0 de-urgenced for routing; OBS stays contained/parked; Stage 0B not started; no observer/scheduler. Next step unchanged: first genuine HeyGen telemetry row (F-HEYGEN), then monitor avatar tail. New P3 carry: A2 avatar-override policy decision. Docs-only; 0 mutation / 0 DB / 0 D-01 / 0 deploy / 0 routing change / 0 OBS / 0 cc-0020. Prior — v3.32 — CFW "silent image-generation loss" DIAGNOSED (read-only): NOT a production defect / NOT an image-gen failure — monitoring/accounting gap on intended workflow. 61 CFW "silent" dead drafts (recommended_format=NULL, approval_status='dead', dead_reason col NULL, 2026-05-04→06-09) are 100% intentional relevance/compliance skips (draft_format.compliance_skip=true 61/61 + reason populated 61/61; 0 empty); reason in draft_format.reason not dead_reason → false silent-loss signal. CFW image_quote live + publishing through 06-10 (52/60 have image_url; 0 image_quote failures; 8 no-url are late-Apr historical). Scope: CFW 61 + Invegent 6 (same signature); NDIS-Yarns + Property-Pulse ZERO. Impact LOW. No fix implemented. Carries: intentional-skip observability improvement (P3) + optional CFW feed-scope review (P3). 0 DB / 0 D-01 / 0 deploy / 0 provider / 0 mutation / 0 OBS / 0 cc-0020. Prior — v3.31 — HeyGen render-telemetry v2.1.0 DEPLOYED + D-01 `0d9bb255` CLOSED (docs-only sync): `heygen-worker` v2.0.0→v2.1.0 (commit `690a295b`) via the safe-deploy CLI wrapper (--allow-warn, no MCP path); D-01 `0d9bb255` PK-approved + CCD-deployed + CCH-verified & closed `completed`; post-deploy health=v2.1.0, deployed_hash_normalised==repo==`74bdbada…` (A-LE/clean), telemetry-only (no schema/migration/new-HeyGen-call); functional first-row telemetry evidence PENDING the next genuine terminal avatar render (new carry). Asset Production Rank #1 first step EXECUTED. See the HeyGen telemetry row + "Next-session priorities". Prior — v3.30 — priority re-rank (docs-only): Asset Production → Rank #1; cc-0020 Stage 5 → Rank #2/idle-guard; cc-0020 Unit A OAuth may proceed PK-side; OBS stays contained/parked (no observer/scheduler/0B); Asset Production enters via HeyGen render-telemetry v2.1.0 / D-01 `0d9bb255`, not OBS. See "Next-session priorities". Prior — v3.28 — OBS Stage 0A governance reconciliation + Option C containment APPLIED (2026-06-10):** G-RP `20260609081127_obs_readonly_production_read_path_g_rp` (D-01 `5fed9a06`) + G-RP-RLS `20260609103639_obs_readonly_rls_slot_origin_select_g_rp_rls` (D-01 `9b03b489`) read-path applied to prod `mbkmaxqhsohbtwsqolns` 2026-06-09 (after the 2026-06-08 recommendation-C hold) but the `00_` files were not updated at the time. Reconciliation found `obs_readonly` was not effectively read-only — reachable `m`/`public` SECURITY DEFINER functions created a write-capable RPC surface. **Option C containment** `ALTER ROLE obs_readonly NOLOGIN;` approved by PK via D-01 `18ba635d` / `18ba635d-9492-4910-bc03-4ba9ab77fd7a` and applied + verified in prod (migration `20260609221624`; `rolcanlogin` true→false; `NOBYPASSRLS` preserved; no memberships; no write table grants; 45 column grants + RLS policy + prior migrations preserved; zero active connections; four OBS-0A rows untouched). **Containment only — NOT ratified, NOT rolled back, role NOT dropped;** observer parked, scheduler disabled, Stage 0B not started, HeyGen untouched. **OBS-0A rows `51752332` / `5fed9a06` / `c5a7cb3c` / `9b03b489` remain open/escalated — deliberately NOT closed.** Open next decision: PK ratify-vs-rollback + reconcile the four OBS-0A rows. This lane = `00_` docs + repo-parity migration backfill (`20260609221624`) only; **0 DB mutation this lane** (NOLOGIN pre-applied 2026-06-09, verified). **Prior: v3.27 — video-worker caption-hierarchy polish (v3.1.3) DEPLOYED to prod:** `video-worker` → `video-worker-v3.1.3` (main @ `fc3ec12`) via `./scripts/safe-deploy.sh video-worker --allow-warn` (1 EF deploy; 1 FF-merge `f0c9682..fc3ec12`; **6 targeted `m.ef_drift_log` refreshes** — 4 stale `A-LE` during GitHub raw-CDN propagation polling + 1 confirming `B-FD` pre-deploy + 1 final post-deploy `A-LE`; **0 schema / 0 migration / 0 cron/queue / 0 publisher/social / 0 image-worker / 0 webhook / 0 render / 0 ElevenLabs-Creatomate call / 0 production draft mutation / 0 backfill**). Caption-styling-only P3 polish for `video_short_kinetic_voice` burned-in captions (read as secondary/subtitle vs main on-card kinetic text). Branch `polish/video-worker-caption-hierarchy`, commits `f9be78c` (styling, `index.ts` +5/−5) + `fc3ec12` (VERSION `v3.1.2→v3.1.3`). **Shipped:** caption `font_size` 52px→40px; `font_weight` 700→500; scrim `opacity` 0.55→0.62; colour remains `#FFFFFF`; `CAP_MAX_CHARS` remains `64`; comments updated to `40px`. **Geometry / timing / chunking / audio / voice / `getVoiceId` / templates / webhook / queue / DB / stat / non-kinetic formats UNCHANGED.** `deno check` exit 0; **CCH code review PASS + CCB visual review PASS** (local Chrome-headless before/after + scrim-contrast renders from the git-ignored `_harness/`, served over `localhost:1234`). **In-design deploy path (wrapper only):** FF-merge → push `main` → targeted `drift-check?write=true&slug=video-worker` refresh → **B-FD** (repo v3.1.3 ahead of deployed v3.1.2; `repo_hash 0991ff65… ≠ deployed fad6f623…`) → `--check-only` BLOCK exit 1 (expected gate) → `--check-only --allow-warn` PASS exit 0 → `--allow-warn` deploy exit 0 → re-refresh → **A-LE / clean / in-spec** (`repo_version 3.1.3 == deploy_version 3.1.3`; `repo_hash_normalised == deployed_hash_normalised == 0991ff6554fbf7c8dcba1debe47e179efcf95d9ff315ce9f41b53fd4b4088bd7`; health GET `{"version":"video-worker-v3.1.3"}` HTTP 200). **D-01 CCD-inline** (PK phrase: `I approve ef_deploy for video-worker F-CREATOMATE-CAPTION-HIERARCHY from branch polish/video-worker-caption-hierarchy at commit fc3ec1279f1591c364bc101a14d9de496167b17e.`) — no `m.chatgpt_review` row fabricated. **L-v3.27-a:** `safe-deploy` reads the CACHED `m.vw_ef_drift_current`, so the mandatory path is merge → targeted refresh → B-FD → `--allow-warn` deploy → final refresh → A-LE; GitHub raw-CDN propagation lag can briefly mis-report A-LE post-push — poll the targeted refresh until the persisted register shows B-FD before deploying. **F-CREATOMATE-CAPTION-HIERARCHY now CLOSED (v3.27).** **OPEN deferred (separate carry): F-VOICE-LOWVOL-BACKFILL (P3)** — ~21 PP+NDIS already-rendered voice videos at pre-fix ~−58 LUFS, re-render decision is PK's; **branch carry:** `polish/video-worker-caption-hierarchy` is merged but **still exists locally + origin** — deletion requires separate approval. Session `docs/runtime/sessions/2026-06-02-v3.27-video-worker-caption-hierarchy-deploy.md`. cc-0015 / cc-0020 / cc-0016 / PRV / IG / YT lanes preserved. **Prior: v3.26 — video-worker getVoiceId guard (v3.1.2) DEPLOYED to prod:** `video-worker` → `video-worker-v3.1.2` (main @ `851bc81`) via `./scripts/safe-deploy.sh video-worker --allow-warn` (1 EF deploy; 1 FF-merge; 2 targeted `m.ef_drift_log` refreshes; **0 schema / 0 migration / 0 cron/queue / 0 publisher/social / 0 image-worker / 0 webhook / 0 render / 0 ElevenLabs-Creatomate call / 0 production draft mutation**). Narrow voice-selection guard (branch `feat/video-worker-getvoiceid-guard`, commit `851bc81`, `index.ts` only +17/−3): `getVoiceId()` final fallback → `return null` (no silent NDIS default for unknown slugs); caller throw → `No ElevenLabs voice ID configured for client_slug=<slug>`; VERSION `v3.1.0→v3.1.2`. Exact `ELEVENLABS_VOICE_ID_<SLUG>` + ndis/property(pp) aliases unchanged; unknown/unmapped slug now **fails loudly** instead of wrong (NDIS) voice. CCH read-only review APPROVED; `deno check` exit 0; Tier-1 scratch env-matrix 6/6 PASS (no test scaffold committed). **In-design deploy path:** FF-merge `fb254bc..851bc81` → targeted `drift-check?write=true&slug=video-worker` → **B-FD** (repo v3.1.2 ahead of deployed v3.1.0) → deploy `--allow-warn` → re-refresh → **A-LE / clean** (`repo_hash == deployed_hash == fad6f623…`; health GET `v3.1.2` HTTP 200). D-01 CCD-inline (PK phrase `APPROVE ef_deploy D-01: deploy video-worker 851bc81 to production with --allow-warn`). **Voice/audio safety pass COMPLETE in prod:** v3.1.0 captions + v3.1.1 volume fix + v3.1.2 voice-mapping guard. **L-v3.26-a:** silent per-client config fallbacks are onboarding traps — prefer fail-loud over silent default. **OPEN deferred:** F-CREATOMATE-CAPTION-HIERARCHY (P3, carried); **F-VOICE-LOWVOL-BACKFILL (P3, NEW)** — ~21 PP+NDIS already-rendered voice videos at pre-fix ~−58 LUFS, re-render decision is PK's; optional merged-branch cleanup (needs separate approval). Session `docs/runtime/sessions/2026-06-02-v3.26-video-worker-getvoiceid-guard-deploy.md`. cc-0015 / cc-0020 / cc-0016 / PRV / IG / YT lanes preserved. **Prior: v3.25 — video-worker Creatomate Pass-1 DEPLOYED to prod:** `video-worker` → `video-worker-v3.1.0` (main @ `de749ed`) via `./scripts/safe-deploy.sh video-worker --allow-warn` (1 EF deploy; 1 FF-merge; 2 targeted `m.ef_drift_log` refreshes; 2 isolated harness renders; **0 schema / 0 migration / 0 cron/queue / 0 publisher/social / 0 image-worker / 0 webhook / 0 production draft mutation**). Shipped: (1) Pass-1 burned-in captions for `video_short_kinetic_voice` ONLY; (2) Creatomate audio-volume contract fix (voice `1.0→"100%"`, music `0.15→"15%"` in kinetic + stat builders) — Creatomate `volume` is a percentage string, numeric `1.0` was read as 1% (−40 dB; source mp3 −18.1 LUFS vs broken mp4 −58.2 LUFS → fixed mp4 −17.4 LUFS, human-confirmed loud/clear). **In-design deploy path (never bypass the wrapper):** merge to main → targeted `drift-check?write=true&slug=video-worker` refresh → **B-FD** (repo v3.1.0 ahead of deployed v3.0.0) → deploy `--allow-warn` → re-refresh → **A-LE / clean** (`repo_hash == deployed_hash == cfbacd8b…`). D-01 CCD-inline (PK phrase `APPROVE ef_deploy D-01: deploy video-worker de749ed to production with --allow-warn`). **L-v3.25-a:** `safe-deploy` reads the cached `m.vw_ef_drift_current`, NOT a live recompute — a feature-branch deploy needs merge→targeted-refresh→B-FD→`--allow-warn`→refresh→A-LE. **L-v3.25-b:** Creatomate volume is a percentage string; verify audio by measuring LUFS, not by ear. **OPEN deferred — F-CREATOMATE-CAPTION-HIERARCHY (P3):** make voiceover captions visually distinct from main on-card text (caption-styling-only; not in this patch). Session `docs/runtime/sessions/2026-06-02-v3.25-video-worker-pass1-creatomate-deploy.md`. cc-0015 / cc-0020 / cc-0016 / PRV / IG / YT lanes preserved. **Prior: v3.23 PRV Stage 2 applied + verified:** PRV read-RPC transport is now live via service-role-only SECURITY DEFINER RPCs over `op.*`; Stage 3 dashboard port to `/reconciliation` under REPORTS remains gated; localhost PRV remains temporary reference only until Stage 4 parity acceptance. **Prior: v3.20 — youtube-publisher v1.11.0 DEPLOYED (future YouTube uploads default PUBLIC) + repo↔deployed parity** (1 EF deploy; **0 DB mutation / 0 SQL / 0 draft reset / 0 forced publish / 0 YouTube API call beyond normal publisher behaviour / 0 backfill / 0 historical cleanup / 0 feature expansion**; 1 `ef_deploy` D-01 PK-approved). Fixed the hardcoded `privacyStatus: 'unlisted'` (upload) + `privacy_status: 'unlisted'` (audit) in `supabase/functions/youtube-publisher/index.ts` → single named constant **`DEFAULT_PRIVACY_STATUS = 'public'`** (`index.ts:98`) referenced by BOTH the upload `privacyStatus` (`:293`) and the audit `response_payload.privacy_status` (`:318`) so recorded metadata cannot drift from intended visibility; VERSION → `youtube-publisher-v1.11.0`; v1.11.0 changelog block added; everything else byte-unchanged from v1.10.0. **No env/config/UI/per-client setting, no schema, no migration** (env-driven knob explicitly rejected for this patch). Deployed via `supabase functions deploy youtube-publisher --no-verify-jwt --project-ref mbkmaxqhsohbtwsqolns` (SUCCESS; Docker-not-running warning benign); **V-1 GET health/version = `{"version":"youtube-publisher-v1.11.0"}` HTTP 200; no unexpected deploy error.** **Safety rests on the unchanged approval gate** (`approval_status IN ('approved','published')` + `video_status='generated'` + 5-format allow-list + `draft_format->youtube_video_id IS NULL`) — if that predicate is ever weakened the Public default becomes unsafe and must be re-reviewed (recorded in-code). No backfill (only the multipart `videos.insert` write exists; already-published drafts fail the SELECT + reconcile guard → never re-selected/re-uploaded). Non-YouTube publishers unaffected (grep: `privacyStatus`/`privacy_status`/`unlisted` match ONLY `youtube-publisher/index.ts`). **OPEN follow-up (runtime, non-blocking) — F-YT-PUB-PUBLIC-VISIBILITY post-publish verification:** after the next controlled/real YouTube publish, confirm visibility; **authoritative proof MUST be YouTube Studio OR `videos.list?part=status` showing `status.privacyStatus == 'public'`** — the ICE `m.post_publish.response_payload.privacy_status: 'public'` is **corroborating only, NOT authoritative**. This commit restores repo↔deployed parity (production was already on v1.11.0). _(Renumbered v3.19→v3.20: the concurrently-committed friction-register session `a62a31e` already claimed v3.19.)_ cc-0020 / cc-0016 / cc-0015 / PRV / IG lanes preserved unchanged from v3.19. `00_`-only close.) **Prior: v3.19 — friction-register manual operating passes (1+2) + Facebook insights-coverage gap finding** (production mutation via single-case `friction.case` SECURITY DEFINER RPC calls only — one-at-a-time, each `sql_destructive` D-01 + PK exact approval, each post-call verified; **0 schema / 0 EF / 0 cron / 0 migration / 0 dashboard code / 0 decisions.md**; recorded as session file `docs/runtime/sessions/2026-05-29-v3.19-friction-register-manual-passes.md`, commit `a62a31e`). First full friction-register operating loop (capture→triage→**resolve**, which had never fired): register **46/45 open/1 resolved/3 new → 46/42 open/4 resolved/0 new**. Pass 1 (D-01 `a94c9f23`, 3 writes): track `c53f5af6` + `220bc43a`; resolve `a6c6032f` (NY FB jobid-48 starvation) `tracked_done`; held `15d57015` (UI fix) for PK. Pass 2 (D-01 `9432e82a`, observer_stale ×3): `d5efc053` NY×YouTube → suppress (no YouTube insights pipeline — structural false-positive); `26436794` NY×Facebook → track + recat pipeline_integrity (**GENUINE GAP** — `m.post_performance` insights stopped 2026-05-23; insights-worker v14.1.0 did not restore NY-fb coverage); `fcb5612e` Invegent×Facebook → resolve `tracked_done` (residual; insights resumed). **Structural finding:** `/operations` UI cannot close a case (`CaseRow` only calls `fn_triage_case`; `resolve_case`/`mark_duplicate` not wired into dashboard) — flagged as arguably higher value than the Stage C batch UI. **Known artefact:** `d5efc053` `triaged_by=postgres` (call-1 positional-arg error, corrected functionally; `fn_triage_case` cannot reset it; direct DML forbidden). **Real next lane: NY-fb (+ CFW-fb) Facebook insights-coverage gap.** Both D-01s closed-loop. Independent of the v3.20 youtube-publisher work above.) **Prior: v3.18 — cc-0020 Stage 5 Unit B code-readiness MERGED on `main` via PR #6 — session close** (docs-only; **0 deploy / 0 OAuth / 0 Gmail/token access / 0 Pub/Sub / 0 cron / 0 DB write / 0 migration / 0 production runtime change / 0 D-01 fired this session**). v3.17 (Stage 5 scoping + governance G1 close + G2 accepted) is already mainline (PR #5 / `883cbf72`). **PR #6: `feat(cc-0020): Stage 5 Unit B code readiness`** — head `1888b56192149802a92ed7a9d16cea22cf38d5b1`; merge commit **`9e4d3dbcb1588d91079ab641dd820849ce36d165`** (parents `df8c54e` prior main + `1888b56` Unit B head). Current origin/main HEAD: `3ddac58a103ff37a9114dd20ecaf9ddc9843ad44` (one automated nightly-health-check no-ready-briefs run-state report `3ddac58` landed post-merge, unrelated to PR #6). **Unit B merged contents:** metadata-only Gmail fetch (`gmail.ts`: `format=metadata` hardcoded, `metadataHeaders=From,Subject,Date`, sender allowlist + `maxResults≤25` + `windowDays≤7` bounds, fail-closed env-flag guard `SUBSCRIPTION_GMAIL_INGEST_ENABLED=false` default, token never logged / never stringified / never returned); RPC-only `ServiceRoleIngestRepository` in `repository.ts` (calls `public.ingest_subscription_import_candidate` SECURITY DEFINER write RPC; dashboard-only methods throw clear pointers to `public.get_subscription_*` / `public.review_subscription_candidate`); async `IngestRepository` interface; `index.ts` dual-layer kill-switch + lazy service-role client + version banner `subscription-email-ingest-v1.0.0-disabled`. **Tests: 41 PASS hermetic** (11 parser + 6 ingest async-refactor + 9 gmail stub-fetch + 8 repository RPC-contract proof + 7 static_canary V-B3/V-B4/V-B7 + meta). **V-B7 added PRE-MERGE to fix CCH plan_review PARTIAL** — initial Unit B commit `75cfba3` wrote to schema `k` via PostgREST (`.schema("k").from("subscription_import_candidate").upsert(...)`) which violates the cc-0020 access rule (`k.*` is NOT PostgREST-exposed); V-B7 patch `1888b56` replaced direct PostgREST with `.rpc(INGEST_CANDIDATE_RPC, ...)`, reduced `SupabaseLike` type to expose only `.rpc()` (codifies the access rule at the type level), and added the V-B7 canary that **fails the build** if any production file reintroduces `.schema(` / `.from("subscription_…")` / `"k.subscription_…"` literals. **CCH re-review CLEAN after V-B7 patch**; PR #6 merged. **Unit B is NOT DEPLOYED and HAS NO RUNTIME EFFECT** — code is on `main` but the function is not deployed; even if deployed, `SUBSCRIPTION_GMAIL_INGEST_ENABLED=false` short-circuits at `gmail.ts:gmailFetchEnabled()` before any OAuth/network call. **Unit C BLOCKED** until: (1) `public.ingest_subscription_import_candidate(p_gmail_message_id text, p_vendor_raw text, p_vendor_normalised text, p_matched_subscription_id uuid, p_amount numeric, p_currency text, p_billing_date date, p_cadence text, p_event_type text, p_confidence numeric, p_source_from_domain text, p_source_subject text, p_source_received_at timestamptz, p_parser_version text, p_content_hash text, p_review_status text) RETURNS TABLE(candidate_id uuid, inserted boolean)` SECURITY DEFINER write-RPC migration authored against the Stage 2 schema; (2) `sql_destructive` D-01 + PK exact approval phrase (matches Stage 4-B precedent); (3) real-PG / PGlite proof of the RPC's `ON CONFLICT (gmail_message_id) DO NOTHING` idempotency contract; (4) U-C deploy-disabled gate (`ef_deploy` D-01 + PK escalate=true) fires separately. Required RPC signature is recorded in `repository.ts` (constants `INGEST_CANDIDATE_RPC` + `INGEST_CANDIDATE_RPC_PARAMS` + `CANDIDATE_RPC_SQL`) so the eventual migration can be reviewed against the client contract. **Unit A required before first live smoke** — independent of Unit C: (a) OAuth app **Production publishing status** per F-YT-OAUTH-TESTING-MODE v3.05 lesson (7-day Testing-mode token cap); (b) scope = `https://www.googleapis.com/auth/gmail.readonly` only; (c) Supabase Edge-Function secrets set (`SUBSCRIPTION_GMAIL_REFRESH_TOKEN` / `_CLIENT_ID` / `_CLIENT_SECRET` / `_SENDER_ALLOWLIST`); (d) **secret-name PRESENCE verified without printing values** (per privacy contract). Unit A does NOT block Unit B merge (already done) and does NOT block Unit C deploy-disabled (function returns `gmail_fetch_missing_env` fail-closed when secrets absent), but Unit A IS a prerequisite for U-D first smoke. **Unit D first live smoke BLOCKED** until Unit A + Unit C both complete. **Unit E cron activation** = separate later gate (`sql_destructive` D-01 for `cron.schedule`). **Unit F Pub/Sub** = DEFERRED to Stage 5b. **Walls intact this session:** no deploy / no OAuth / no Gmail/token access / no Pub-Sub / no cron / no DB write / no migration / no production runtime change / no D-01 fired by CCD. cc-0016 + cc-0015 + YT + PRV + IG lanes preserved unchanged from v3.17. `00_`-only close per v3.13.2/v3.13.3 precedent.) **Prior: v3.17 — cc-0020 Stage 5 scoping AUTHORED + governance G1 RESOLVED + G2 ACCEPTED-AS-APPLIED** (docs-only; **0 OAuth / 0 Gmail/token access / 0 Pub/Sub / 0 EF deploy / 0 cron / 0 DB write / 0 migration / 0 production mutation / 0 D-01 fired this session**). On branch `feat/cc-0020-stage-5-scoping` (off `main` `883cbf72`). **(1) cc-0020 Stage 5 execution plan AUTHORED** — `docs/briefs/cc-0020/stage-5-live-gmail-execution.md` covers all 9 required sections plus risks/OQs/provenance: stage objective + E-1…E-7 entry criteria (from v3.16) + P-1…P-5 prerequisites + components touched + **6-unit sequencing A→B→C→D→E→F** (U-A token mint manual by PK / U-B code change CCD / U-C DISABLED deploy / U-D first live smoke ≤24h ≤10 messages PK eyes-on / U-E cron / **U-F Pub/Sub DEFERRED to Stage 5b**) + per-unit V-checks (V-A1…V-A5, V-B1…V-B5 incl. static-grep zero-hit for `format=full`/tokens, V-C1…V-C5 disabled-state, V-D1…V-D8 smoke incl. re-invoke for idempotency proof + ledger-unchanged guard, V-E1…V-E4 cron, V-PRIV cumulative) + **per-unit D-01+PK approval phrases** (U-B plan_review escalate=false / U-C ef_deploy escalate=true / U-D 2-step ops_change + verbal escalate=true / U-E sql_destructive escalate=true) + **global kill switch** (`SUBSCRIPTION_GMAIL_INGEST_ENABLED=false` short-circuits at `gmail.ts:27` — no code/migration/deploy needed to disable) + per-unit rollback + 11-item out-of-scope list + recommended **first-live-smoke design** (smallest blast radius: `newer_than:1d`, `maxResults:10`, `format=metadata` ONLY — no body crosses the wire, sender-domain allowlist, single manual invoke, flag flipped back OFF after) + 10-item risk table + 6 open questions + authoring provenance. Surgically linked from cc-0020 main brief §3 Stage 5 row. **Stage 5 execution remains future/gated** — every unit fires its own gate before any production effect. **(2) Governance G1 RESOLVED for `6d9a4bd3-d665-47c0-8dd9-165d3bc75bd6`** (CCH read-only verification + close-loop record): migrations `20260527114041` (tables) + `20260527114144` (read RPCs) now have a **complete in-row governance trail** — `m.chatgpt_review.6d9a4bd3` updated to `status='resolved'`, `escalation_resolved_at='2026-05-28 10:55:17.310207+00'`, `resolved_by='PK (retrospective confirmation 2026-05-28) + CCH (close-loop record)'`; first close-loop UPDATE returned `row_count=1`, guarded re-run returned `row_count=0` (idempotency proved). The v3.16 "partially evidenced, not cleanly closed" wording is **SUPERSEDED for G1 only**. **Residual G2 (`20260527114333` REVOKE-anon/authenticated hotfix) ACCEPTED-AS-APPLIED** by PK directive 2026-05-28 (docs-only). The hotfix was a **pure grant-tightening operation** (REVOKE EXECUTE FROM `anon`,`authenticated` on the 4 cc-0020 RPCs) — no data effect, no schema change; live posture already verified correct (RPC EXECUTE on all 4 `public.*` SECURITY DEFINER RPCs = service_role + postgres only, no anon/authenticated/PUBLIC); residual risk = none known; process gap recorded as accepted. **NOT precedent for skipping D-01 on future grant changes** — grant-tightening hotfixes ordinarily fire `sql_destructive` D-01 like any other DDL; the `114333` case is a one-time retrospective acceptance because the grant change was strictly tightening and the resulting posture was verified safe. **Walls intact this session:** no OAuth / no Gmail/token access / no Pub-Sub / no EF deploy / no cron / no DB write by CCD (CCH's G1 UPDATE was orchestrated separately and reported back to CCD read-only) / no migration / no production mutation / no D-01 fired by CCD. cc-0016 + cc-0015 + YT + PRV + IG lanes preserved unchanged from v3.16. `00_`-only close per v3.13.2/v3.13.3 precedent.) **Prior: v3.16 — four-point docs-only reconciliation** (0 SQL apply / 0 DB write / 0 deploy / 0 cron / 0 PR/merge / 0 OAuth/token/Gmail action / 0 lifecycle cleanup run / 0 D-01 fired this session). **(1) cc-0016 Stage E dry-run design AUTHORED** — `docs/briefs/cc-0016/stage-e-dryrun-design.md` (7 sections incl. cleanup objective + 10 boundaries B-1…B-10 + no-op guarantee; lifecycle tables `friction.event` / `storage.objects` / `storage.buckets` / `cron.job` / `pg_proc`; 9 SELECT-only queries Q-1…Q-9 + Q-10 PSEUDOCODE-not-created; JSON output format; 9 acceptance criteria AC-1…AC-9 incl. counter-validation invariant + AC-7 NO-OP + AC-8 ≥14-day maturity; **separate future gate G-E1/G-E2/G-E3** = D-01 sql_destructive carrying dry-run JSON w/ ≤7-day freshness + PK exact approval phrase w/ cutoff+N+B locked + ≥14 prod days + no open P0/P1; R-A1…R-A4 authorship + R-B1…R-B7 future-run risks + rollback/no-op guarantees; surgically linked from cc-0016 main brief §7; **future D-01 + PK approval STILL REQUIRED before any destructive run**). **(2) cc-0020 status CORRECTED — NO LONGER PARKED / HELD / UNAPPLIED** (supersedes v3.15 wording verbatim): content-engine `main` advanced via merge **`49a6c0e`** ("Merge cc-0020 subscription email-ingest lane into main (Stage 0–4B)"); **3 migrations APPLIED to prod `mbkmaxqhsohbtwsqolns`** — `20260527114041` creates `k.subscription_import_candidate` + `k.subscription_spend_event`; `20260527114144` creates 4 `public.*` SECURITY DEFINER RPCs (`get_subscription_import_candidates(text,integer)`, `get_subscription_spend_events(integer)`, `get_subscription_spend_trends(integer)`, `review_subscription_candidate(uuid,text,uuid)`) over the deny-by-default `k.*` tables; `20260527114333` security hotfix REVOKE EXECUTE FROM anon,authenticated (PUBLIC-only revoke was insufficient because Supabase default-privileges GRANT EXECUTE directly to anon/authenticated). RPC EXECUTE state: **service_role + postgres only** (verified in repo files 114144 lines 269–286 + hotfix file 114333 lines 16–19). Repo↔prod migration-history reconciliation v2 is **merged on `main`** via PR #3 merge commit **`d7543078`** (parents `3cd144f` v1 + **`fa2aefc`** v2); canonical applied-version files + `_proposed_` tombstone placeholder slots are now the mainline state. Tombstones (`SELECT 1 WHERE false;`) permanently consume the version slots so a future `supabase db push` cannot silently re-execute the DRAFT DDL; canonical files carry idempotency-abort guards via `DO $$ RAISE EXCEPTION 'ABORT: … already exists' $$` (PGlite 12/12 dry-run pass on the v2 layout). Dashboard `main` advanced with **Stage 4-A UI + Stage 4-B service-role RPC routes** consuming the new `public.*` RPCs (live `dashboard.invegent.com/system/subscriptions`). Stage 5 wall (Gmail OAuth + Pub/Sub + edge-function deploy + cron) **still intact / future-gated** — ingest function is in-repo (under `supabase/functions/subscription-email-ingest/`) but **not deployed**. **Governance trail (HONEST — partially evidenced, not cleanly closed):** CCH completed a read-only sweep against `m.chatgpt_review` and resolved the prior generic "evidence gap" into a specific finding. Migrations `20260527114041` (tables) + `20260527114144` (read RPCs) are governed by review row **`6d9a4bd3-d665-47c0-8dd9-165d3bc75bd6`** (`action_type=plan_review`, `verdict=partial`, `risk_level=medium`, `confidence=medium`, `requires_pk_escalation=true`, `routing_decision`/`action_taken=escalate_explicit_flag`, `status=escalated`, `resolved_by=NULL`, `escalation_resolved_at=NULL`). The apply is **partially evidenced, not cleanly closed**. **Residual G1:** `6d9a4bd3` close-the-loop was never recorded in-row — PK discharge is asserted by the proposal and implied by timing but not captured (`resolved_by`+`escalation_resolved_at` both NULL). **Residual G2:** migration `20260527114333` REVOKE-anon/authenticated security hotfix has **no D-01 / governance row found** in `m.chatgpt_review`. **Live grant posture verified correct** (RPC EXECUTE = service_role + postgres only, no anon/authenticated/PUBLIC). Residuals are **governance-trail debt, not a live-permissions risk** — new carries added below for the close-the-loop UPDATE (G1) + retrospective governance decision on `114333` (G2). **(3) YT backlog drain CLOSED** (per CCH): `m.post_publish` YouTube rows = **61**; latest publish row at **2026-05-28 08:15:08Z**; **published-draft↔youtube-publish-row gap = 0** for the v3.05 fresh-release cohort (NY 6 + PP 7 = 13 drafts) — fully drained with audit-row parity (v3.11 audit-gap fix carried forward). **Carry separately:** 2 stale NY `generated` stragglers + stale `pending` drafts are **pre-existing / predicate-ineligible** (fail the publisher's `video_status='generated'` + `approval_status IN ('approved','published')` + `youtube_video_id IS NULL` + `video_url IS NOT NULL` + `recommended_format` ∈ allow-list predicate) and are **NOT the released backlog** — treat as separate carry. F-YT-FAILED-NO-RETRY arc complete (B1+B2 + audit gap closed v3.10/v3.11); B3 9 = onboarding debt. **(4) cc-0015 Gate 11 observation window ELAPSED/CLOSED as a time-bound watch** — the 1-week window (2026-05-19 → 2026-05-26) ended 2026-05-26; the watch itself is now closed by elapsed time. **Stage C/D/E remain separately PK-gated** (each requires its own PK-directed start); ICE-PROC-002 +4-week review ≈ **2026-06-26** is the next process milestone. Stage A + Stage A.5 + Stage F + Stage B RPC + Stage B frontend + Stage G doc V-G1 unchanged; cc-0015 still NOT complete by overall closure — the rank-1 watch is what closed. **Walls intact this session:** no DB connection / no Gmail/OAuth/tokens / no edge-function deploy / no cron change / no PR/merge / no friction.* write / no destructive Stage E run / no governance-row creation. `00_`-only close per v3.13.2/v3.13.3 precedent.) **Prior: v3.15 — Instagram diagnosis lane + cc-0020 subscription-email-ingest lane PARKED — session close** _(v3.15 cc-0020 "Stage 4 HELD / unmerged / undeployed / migration UNAPPLIED" wording is **SUPERSEDED** by v3.16 point (2); preserved here as historical record only)_ (docs-only; **0 code, 0 migration apply, 0 DB connection, 0 deploy, 0 Gmail/OAuth/token, 0 cron/Pub-Sub, 0 PR/merge, 0 D-01**). **(A) Instagram diagnosis (CCH lane)** parked/closed — runbook `b911d91` (`docs/runbooks/instagram-not-publishing-diagnostic.md`); NY/PP `publish_enabled` flipped via dashboard UI → cc-0019 fill-gate cleared; self-heal ≈ 2026-05-29; no further IG action unless PK reopens (CCD did not run the IG lane). **(B) cc-0020 Subscription Email Ingest (CCD lane)** parked — branch `feat/cc-0020-subscription-email-ingest` @ remote HEAD `7497e74` (pushed, **unmerged, undeployed**); Stage 0/1/2/2a/2b accepted, Stage 3 conditionally accepted (fixture-only ingest scaffold; pure parser + 8 synthetic fixtures + 17 Deno tests; in-memory repo models `ON CONFLICT (gmail_message_id) DO NOTHING` + `UNIQUE(source_candidate_id)`; fail-closed Gmail stub; no Supabase client), Stage 3a-P closed (branch pushed + CCH DDL/scaffold inspection, no blockers); **Stage 3a DB-layer validation OPEN (Docker/local-PG unavailable → real-PG proof deferred) — blocks Stage 4; Stage 4 HELD**; draft migration `supabase/migrations/20260527120000_proposed_cc_0020_subscription_email_ingest_tables.sql` (two append-only `k.*` tables; RLS deny-by-default; `event_type` authoritative w/ sign-vs-type CHECKs; ledger `event_type` no default) remains **UNAPPLIED** (production `mbkmaxqhsohbtwsqolns` never touched). Walls intact (no live Gmail/OAuth/tokens/apply/deploy/cron/Pub-Sub/`k.subscription_register` write); future: register matching, `content_hash` secondary signal, optional currency CHECK, apply-time `service_role` USAGE-on-`k` preflight. **UI destination (PK directive 2026-05-27):** the user-facing review + spend-history UI belongs in **`invegent-dashboard`**, extending the live subscription area **`dashboard.invegent.com/system/subscriptions`** (tabs/panels/drawers/sub-routes OK) — localhost is dev/test only, NOT the deliverable; explicitly **not** the PRV local-only pattern. Repo split: backend/ingest stays in `content-engine` (branch `feat/cc-0020-subscription-email-ingest`); live UI on its own `invegent-dashboard` branch when Stage 4 is released. **Stage 4** = extend the real dashboard subscription experience (not a standalone local surface); entry reqs: (1) Stage 3a DB-layer validation closed/explicitly-accepted-as-apply-time-proof; (2) a read path into the deny-by-default `k.*` tables — a `public` SECURITY DEFINER RPC (cf. `public.get_subscriptions()`) OR a service-role server route, **no direct client/PostgREST access to `k.*`**. **Preserved unchanged:** PRV pushed/unmerged/undeployed; cc-0015 parked (Stage C/D/E held). Detail: `docs/runtime/sessions/2026-05-27-v3.15-instagram-cc0020-sync-close.md`.) **Prior: v3.14 — Platform Reconciliation View (PRV) read-only MVP COMMITTED + PUSHED** (feature-branch only; **0 deploy, 0 merge, 0 PR, 0 PostgREST exposure**; 1 prior role-DDL migration + close-loop). Route `/recon` in the content-engine `dashboard/` app reads the 3 cc-0012 `op.*` views (`v_reconciliation_summary`/`v_freshness_rollup`/`v_drift_rollup`) via a server-only DIRECT Postgres pooler transport (`dashboard/lib/db.ts`; read-only session + SELECT-only guard) as least-privilege role `prv_readonly` (mig `20260527011420_prv_readonly_login_role`; D-01 `d86b7db8` agree/proceed + PK phrase `PK APPROVES PRV DB/RPC MUTATION`; backfilled byte-exact md5 `a5771ad7…`; close-loop done — `d86b7db8` completed, `57fd738b`+`43140c7f` superseded→resolved). `op` is NOT PostgREST-exposed → direct PG bypasses PGRST106. Live render PASS (7d expected 56/matched 16/late 0/suppressed 12, on-time 28.57%; 14 freshness pairs; 39 drift/26 actionable) + CCB visual PASS. Commit `7287a1e` pushed to `origin/feature/platform-reconciliation-view-readonly` (new branch; **unmerged, undeployed**; `main` untouched `911d0d4`; no CI/Vercel automation wired to this repo). `.env.local` (DSN) local-only + untracked. Deferred/gated: polish (KPI cards/badges/table width) + the two grant-covered views (`op.v_per_client_rollup`/`op.v_per_platform_rollup`); production deploy = `PK APPROVES PRV READ-ONLY PRODUCTION DEPLOY`. Deployed roadmap (separate `invegent-dashboard` repo) NOT touched. PHASES leg N/A.) **Prior: v3.13.3 — cc-0015 Stage G ICE-PROC-002 V-G1 APPROVED** (documentation-only; **0 SQL, 0 migration, 0 deploy, 0 production mutation, 0 dashboard code**). PK confirmed V-G1 approval for `docs/process/ICE-PROC-002-pooled-resolution.md` — now the operating reference for the current cc-0015 `/operations` saved-views + pooled-resolution surface. With V-G1 (approved) + V-G2 (committed `d5b296f`) + V-G3 (docs-index link) all satisfied, **Stage G is complete as a doc artefact.** Recorded in the doc Status + change log, the cc-0015 brief §9 (status → AUTHORED + V-G1 APPROVED), and the `00_` indexes. **Stage C/D/E remain HELD; source filter + sort + FAB category-desc hydration remain deferred; cc-0015 still NOT complete.** Next ICE-PROC-002 milestone = **+4-week process review ≈ 2026-06-26**. PHASES leg N/A.) **Prior: v3.13.2 — cc-0015 Stage G process doc AUTHORED** (documentation-only; **0 SQL, 0 migration, 0 deploy, 0 production mutation, 0 dashboard code**). Authored `docs/process/ICE-PROC-002-pooled-resolution.md` (pooled-resolution rhythm) per the established `ICE-PROC-NNN` convention: all 7 required sections (why pooling / triage defaults — `track` default, `act_now` reserved, `critical` interrupts / cadence — Fri 09:00 Sydney, Dashboard UI weekly, Register reconciliation weekly-if-≥3-else-biweekly, Pipeline-friction triggered, All-tracked + New-untriaged maintenance / session protocol / suppression discipline / anti-patterns incl. "Pipeline pool" collision wording / +4wk review) + COEXIST model + 5 saved views + known deferrals. Linked from `00_docs_index.md` (new `process/` subdirectory row; V-G3). Scoped to the shipped surface — does NOT assume Stage C/D/E exist; does NOT claim cc-0015 complete. V-G1 (PK draft approval) remains. cc-0015 NOT complete. PHASES leg N/A.) **Prior: v3.13.1 — cc-0015 Stage A.5 recategorisation APPLIED + verified** (1 guarded UPDATE migration; **0 schema change, 0 deploy, 0 Repair-Board-v0 change**). Migration `20260526222125_cc_0015_a5_recat_operator_friction_to_dashboard_ui` moved 3 dashboard-UI cases `53f3e533` / `2cf0cd4f` / `b7369dc9` `operator_friction`→`dashboard_ui` (guarded UPDATE only, no `fn_triage_case`). Verified `dashboard_ui` 0→3, `operator_friction` 6→3, `client_commitment` 16 + `pipeline_integrity` 22 unchanged, total 44; the YouTube content-series request + 2 cc-0016 smokes + the overview/auto-approver case correctly left behind. sql_destructive D-01 `bbe9a0fa` (agree/low/high/escalate=false) + PK exact phrase; close-the-loop on `m.chatgpt_review` completed. Migration file backfilled verbatim (L-v3.06-a; body md5 `5fdb7de72b362c1cdf0cbb8400fbe8b0` == DB statements), committed+pushed `41576f8`; the 3 cases now surface under the live `/operations?category=dashboard_ui` Dashboard UI saved view. cc-0015 NOT complete (Stage C/D/E held; source/sort + FAB category-desc hydration deferred; Stage G process doc not authored). PHASES leg N/A.) **Prior: v3.13 — cc-0015 Stage B FRONTEND MERGED + DEPLOYED** (dashboard frontend merge + Vercel production deploy; **0 SQL, 0 migration, 0 Repair-Board-v0 change this pass**). `/operations` filter bar + 5 PK-approved saved views + URL-param filters + category-description hydration on the extended `friction.fn_recent_cases`; CCB preview visual PASS (V-B3 URL filters / V-B4 saved views / V-B5 Repair-Board-v0-untouched / V-B6 default byte-identical; stale-wording CLEAN) on `feat/cc-0015-stage-b-operations-filters` `25bcdb7` → FF-merge dashboard `main` `25bcdb7` (tsc --noEmit clean + next build exit 0) → production deploy READY (Vercel `dpl_Hb2BkFnK3cL4cCAHXbkAz1tDnHjW`, `dashboard.invegent.com`; liveness `/operations` 307→`/login` 200, no 5xx; behind-auth visual was CCB preview). Category-desc hydration COMPLETE for `/operations` (FAB deferred); Repair-Board v0 untouched (V-B5); source/sort deferred; Stage C/D/E + Stage A.5 still held; **cc-0015 NOT complete**. PHASES leg N/A.) **Prior: v3.08 — Avatar→YouTube Shorts milestone (repo reconciliation of an already-live pipeline; 0 production mutation, 0 new deploy, 0 D-01).** ai-worker v2.13.0 (A2/A3 avatar override + narration branch) + heygen-worker v2.0.0 (async two-phase; 720×1280) + youtube-publisher v1.7.0 reconciled to `main` from branch `feat/ai-worker-v2.13.0-avatar-youtube` (Option B bridge-native squash; EFs already deployed + verified-publishing). **F-YT-PUB-AVATAR-EXCLUSION CLOSED** (`video_short_avatar` added to the publisher allow-list); first live unlisted Short `sfQvSM2Osus` (NDIS-Yarns, draft `40f9fa25`, `m.post_publish` `5dbfcc0f`, 720×1280, 2026-05-25 11:42:07 UTC). **F-YT-FAILED-NO-RETRY re-pointed → youtube-publisher v1.8.0 (still OPEN)** — v1.7.0 was consumed by the avatar allow-list, not the retry fix. cc-0019 live-version corrected v2.12.0→v2.13.0; **Unit B re-verified absent against ai-worker v2.13.0 (`8204a5c7`) — STILL OPEN / optional (post-milestone narrow patch 2026-05-26)**. Reconciliation commits `521b053b`+`194e0792` (EF parity), `04294d23` (continuity docs), this `00_` rebuild; all 12 files byte-identical to branch (sha-verified). PHASES leg N/A.) **Prior: v3.07 — doc-patch debt (repo doc-only; 0 production mutation).** cc-0017c **main brief → v1.2 PATCHED + CORRECTED** (`6d5c3ae6` apply-record backfill → `81dcb683` no-fabrication correction): status→APPLIED-WITH-VCHECK-CORRECTION, apply date 2026-05-19, migrations `20260519004545`+`20260519005322`, 3 D-01 refs (v1.0 `a37eff28-…` / v1.1 `9e602a2d-…` truncated-in-source / apply `d18fa6db-…` AGREE), V-B4 12-param pointer; correction softened an unverified `cc-0017d v2.90` successor tag. **cc-0017a / cc-0010A / cc-0011 / cc-0012 + cc-0017c/vchecks.md sub-file DEFERRED to CC** — insufficient documented scope + full-file re-emit risk (GitHub bridge has no str_replace; 49–107 KB briefs unsafe to chat-re-emit). **vchecks.md path located:** `docs/briefs/cc-0017c/vchecks.md` (the V-B4 12-param fix is grounded; sub-file edit → CC). **decisions.md still unlocated** (parked per PK steer; cc-0014 sync_state framing leg already moot per v3.06). **Lessons NOT promoted** — canonical L37–L65 home + numbering convention not locatable in repo; PK option (c): defer to next-lesson-cycle/CC; nothing promoted by assumption. Session `b94c042d`. PHASES leg N/A.) **Prior: v3.06 — deployed↔repo drift close + cc-0019 correction.** IG publisher synced to repo v2.0.0→v2.4.0 (`bc78511e`, byte-verified — rollback hazard closed); 7 chat-applied 2026-05-24 migrations backfilled to `supabase/migrations/` (`8738c3a0`, recovered from `schema_migrations.statements`) — **DB→repo migration drift CLOSED**; F-YT-FAILED-NO-RETRY brief authored (`bee872aa`); **cc-0019 CORRECTED → Unit A IMPLEMENTED** (live DB has `m.is_publish_eligible` + `fill_pending_slots` gate; migration `20260524091020`; governed D-01 `a2e5f3f5` + PK approval per header) but **Unit B (ai-worker preflight) NOT implemented** (verified absent in deployed ai-worker v2.12.0). Session `1219aff0`. 0 production mutation; **F-YT-FAILED-NO-RETRY plan_review CLEAN** (`abf6f63c` partial/type-c → `f89c23cf` agree; prior partial resolved by the Unit-B 3-bucket brief patch `a3f329f3`) — **2 plan_review D-01, non-mutating**. IG-arc OWED close substantially discharged. PHASES leg N/A.) **Prior: v3.05 — YouTube OAuth Production restore.** Root cause definitively found: the Google OAuth app was in **Testing** publishing status → refresh tokens capped at **7 days** (NY token lasted 7.00d, PP 7.92d after the 5 May reconnect). PK moved the OAuth app → **Production** + re-exchanged tokens (NY token_updated_at 10:48:22, PP 10:49:05 UTC); two single-draft live tests **published successfully** (PP `ma6EG1fz4XQ` 10:57 UTC, NY `qp-ZGm8lNIo` 11:02 UTC) confirming both channels refresh with no `invalid_grant`. Backlog **released** (fresh ≥12 May → `generated`: NY 6 + PP 7 = 13, draining 2/tick jobid 34) + **soft-retired** (stale <12 May → `archived_stale`: NY 3 + PP 11 = 14; retire-not-delete to keep audit + dodge FK risk). Migrations `yt_token_verify_reset_one_pp_draft` / `yt_token_verify_reset_one_ny_draft` / `yt_backlog_release_fresh_softretire_stale` (all 20260524; draft `video_status` only, guarded). 2 publisher invokes (real publishes). NEW findings F-YT-OAUTH-TESTING-MODE (root cause RESOLVED+recorded), F-YT-FAILED-NO-RETRY (publisher marks transient auth failure terminal `failed` → silently freezes backlog; candidate brief), F-YT-EXPIRY-DISPLAY-FAKE (callback hardcodes now+5y expiry → fake "Valid 2031"). Also **cc-0019 brief** (publish-eligibility gate before AI draft generation) authored `81cb414` + hardened `01a6cdf`; plan_review `a75c78f6`→`ff05c65e` clean — **brief only, NOT implemented**. 4 D-01 (2 plan_review + 2 sql_destructive `82443c8c`/`5f7bfc52`). 0 cron change / 0 EF deploy / 0 client enablement / 0 hard delete / 0 friction.* / 0 cc-0015 start / 0 Stage E / 0 Q-005 closure / 0 decisions.md / 0 dashboard edit. Dashboard roadmap/PHASES leg N/A (operational, no phase change). OWED: pre-compaction IG-publish-restore arc 4-way close.) **Prior: v3.04 — Dashboard Slice 0A MERGED to `invegent-dashboard` main at `3ec489b`** (Pre-Phase 0 / Gate-11-window sidebar IA shell + Visual Tokens v1; NOT real Phase 0). **Today/Next 5 core ranks (unchanged v3.02–v3.05)**: **cc-0015 Gate 11 watch → rank 1 (closes 2026-05-26)**; cc-0016 Stage E scoping/dry-run design ONLY → rank 2 conditional; Wave 0f scoping → rank 3; PRV → rank 4 (deferred); close-the-loop → rank 5.

---

## How this file works

At session start, chat reads this file and: (1) rebuilds Today/Next 5; (2) runs Standing checks (S1–S29); (3) verifies D186 closure budget; (4) asks PK about Personal businesses; (5) surfaces Time-bound items.

**Standing rules unchanged from v3.01.** D-01 + D-186 + D-YT-OAUTH-1 + D-PREV-16 + Lesson #62 (L46) + #68 + L33–L65 + L-v2.76-a-f + L-v2.78-a + L47 + L-v2.81-a + **L-v2.83-a (STRONG; 18+)** + L-v2.84-a/b/c/d + L-v2.85-a (HIGH-SIGNAL; 4 occurrences) + L-v2.85-b/c/d + **L-v2.85-e PROMOTION-CONFIRMED** + 5 L-v2.86 candidates + **L-v2.88-a** + L-v2.88-b/c/d + L-v2.89-a + **L-v2.90-a through L-v2.90-f** + **L62 strongly reinforced (cc-0016 6-fire series; v3.05 both YT sql_destructive partial/type-c)** + **L-v2.94 convention NEW candidate** + **L-v3.02-a / L-v3.03-a / L-v3.04-a NEW candidates** + **L-v3.05-a NEW candidate (record root CAUSE not just symptom; re-run stale audits against live data on pushback)** + **L41 re-exercised v3.02.1 + reinforced v3.03 + v3.04 + v3.05**. **D-IOL-001 (v2.77)** + **D-CC-0017B-Q1** carried.

**v3.05 ADDITIONS:**

- **YouTube publishing RESTORED.** Root cause definitively identified + the fix verified by two live publishes.
  - **Root cause: OAuth app in Google "Testing" publishing status → refresh tokens capped at 7 days.** NY token lasted 7.00d, PP 7.92d after the 5 May reconnect (`invalid_grant: Token expired or revoked`). Two independent accounts dying at the 7-day boundary = the Testing-mode fingerprint. The dashboard's "Expires 2031" is a hardcoded `now()+5y` placeholder in `app/api/youtube/callback/route.ts` ("tokens don't expire"), cosmetic, never read by the publisher. The stored token is a genuine `1//` refresh token, correctly stored. **The cause was never recorded before — only the symptom (reconnect token) — so it recurred ≥3×. Now recorded.**
  - **Fix:** PK moved the OAuth app Testing→Production in Google Cloud Console + re-exchanged tokens (NY token_updated_at 2026-05-24 10:48:22 UTC, PP 10:49:05 UTC; both `1//`, 103 chars).
  - **Verification (governed):** D-01 `82443c8c` (sql_destructive). mig `yt_token_verify_reset_one_pp_draft_20260524` (PP draft `2afce74e…` `failed→generated`) → invoke req 126325 → **published `ma6EG1fz4XQ`** (10:57:23 UTC, `m.post_publish` row). mig `yt_token_verify_reset_one_ny_draft_20260524` (NY draft `b9860637…`) → invoke req 126339 → **published `qp-ZGm8lNIo`** (11:02:24 UTC). Both tokens refresh, no `invalid_grant`.
  - **Backlog:** D-01 `5f7bfc52` (sql_destructive). mig `yt_backlog_release_fresh_softretire_stale_20260524` (two guarded UPDATEs, cutoff 2026-05-12): released ≥12 May (`failed→generated`: NY 6 + PP 7 = 13, draining 2/tick jobid 34); soft-retired <12 May (`failed→archived_stale`: NY 3 + PP 11 = 14, terminal/publisher-invisible/reversible — PK chose retire over hard-delete to preserve content+audit + avoid FK risk on `slot.filled_draft_id`/`slot_fill_attempt`/`ai_job`). Untouched: failed-out-of-guard NY 3 + PP 9 (non-uploadable format / non-approved).
  - **youtube-publisher v1.6.0 unchanged.** SELECT predicate `video_status='generated'` AND `approval_status='approved'` AND `youtube_video_id IS NULL` AND `video_url NOT NULL` AND `recommended_format IN (4 kinetic/stat)`, limit 2. **F-YT-FAILED-NO-RETRY:** catch handler sets transient auth failure to terminal `failed` (never re-selected) → a token outage silently bricks the whole rendered backlog. Candidate brief.
  - **Both D-01s partial/type-c, escalated→PK, PK pre-approved.** NY test reused PP verdict `82443c8c` (same action/guard/arc, same UTC day).

- **cc-0019 brief — publish-eligibility gate before AI draft generation (BRIEF ONLY).**
  - Read-only audit found `m.fill_pending_slots` creates a skeleton `post_draft` + `queued` `ai_job` (token spend) with no publish-eligibility check → disabled NY/PP IG generated ~12% wasted ai_jobs/7d. Bonus: `m.materialise_slots` reads `publish_enabled` only inside the per-platform format-preference lookup (NULL format → slot still inserted, defaults `image_quote`) — disablement wired to format-selection, not skip. Two switches never unified (`client_publish_profile.publish_enabled` vs `client_publish_schedule.enabled`).
  - Brief `docs/briefs/cc-0019-publish-eligibility-gate.md` — `81cb414` (create) → `01a6cdf` (harden §4 eligibility transition handling). Design: `m.is_publish_eligible(client_id, platform)` SECURITY DEFINER + fill-gate in `fill_pending_slots` + ai-worker preflight. Cron dimension out of v1 (operational: `publish_enabled=false` also stops generation).
  - plan_review `a75c78f6` (partial/type-c "add transition fail-safes") → brief hardened → re-fire `ff05c65e` (**agree/clean**).
  - **NOT implemented.** Execution gated on sql_destructive (Unit A migration recreating `fill_pending_slots` + new function) + ef_deploy (Unit B ai-worker) D-01s + PK approval, in a separate supervised session.

- **Items explicitly preserved v3.05:**
  - cc-0015 Gate 11 watch — rank 1; window closes 2026-05-26; do NOT start before.
  - cc-0016 Stage E — future/separately-approved-only.
  - PRV brief authoring — deferred per D-FR-RECON-001 §7.D.
  - Q-nightly-health-check-v1-005 — OPEN, non-blocking, next-fire watch (brief v3.1.1).
  - Dashboard Slice 0A merged at `3ec489b` (cosmetic, NOT Phase 0); mobile CLOSED/PASS; backend/shared-metrics refactor deferred.

- **Hard stops respected v3.05:**
  - cc-0019 brief only — 0 gate migration applied, 0 EF deploy, `fill_pending_slots` unchanged, `is_publish_eligible` NOT created
  - YT mutations: exactly the 3 listed migrations (draft `video_status` only, bounded+guarded) + 2 publisher invokes (real publishes)
  - 0 cron change / 0 EF deploy / 0 client enablement change / 0 queue/profile mutation / 0 hard delete
  - 0 friction.* write / Q-005 untouched / cc-0015 not started / cc-0016 Stage E not started / CFW+Invegent untouched
  - 0 decisions.md change / 0 memory edits / 0 dashboard edit (roadmap/PHASES leg N/A — operational session)
  - 0 full-file rewrite from stale context (session file new `f9ddd51`; sync_state + action_list patched read-HEAD-first with blob SHAs passed — sync_state `7d2b0eb8`, action_list `ecaf50f8`)

- **Closed Active rows v3.05:** YouTube publishing restore (RESTORED/VERIFIED — both channels publishing); cc-0019 plan_review (CLEAN — brief ready for execution). Q-005 stays OPEN by design; ranks unchanged.
- **NO decisions.md change v3.05.**
- **Production mutations: 3 apply_migration (draft video_status only) + 2 publisher invokes. D-01 fires: 4 (2 plan_review + 2 sql_destructive). T-MCP-02 cum: ~96 (v3.05 +4), needs re-baseline incl. unrecorded pre-compaction IG arc (~+5). State-capture exceptions: 1 unchanged.**

**v3.04 ADDITIONS (preserved):**

- **Dashboard Slice 0A MERGED to `invegent-dashboard` main — RECORDED (documentation-only).** Dashboard main HEAD `3ec489b6fb1e4ad706aac9d32f7fefa4ad43b9c5` (parent `d17b604`; squash of `e65b812` + `399c087`); files `components/sidebar.tsx` + `tailwind.config.ts` + `app/globals.css`; typecheck exit 0; CCD/CCB/CCB-polish PASS; SHA + content independently verified (Invegent GitHub `list_recent_commits` + live `sidebar.tsx` read). Locked IA NOW/CLIENTS/CREATE/REPORTS/ADMIN; 5 status colour scales + 10 typography/spacing helpers additive; mobile drawer preserved. **NOT real Dashboard Phase 0** (review §9 groundwork future/gated). No routes/redirects/Roadmap/PHASES touched.
- **Production mutations: 0. D-01 fires: 0.**

**v3.03 ADDITIONS (preserved):**

- **Q-005 Option A resolution trail RECORDED (brief now v3.1.1; Q-005 stays OPEN).** A-005 `56e992b4` → v3.1 `7005865` (9-key mapping) → CCD finding (v3.1 would fail live emission_rule acceptance; only `health_check/true_stuck` enabled) → v3.1.1 `9ceb78a` (true_stuck restored; 8 keys PARKED markdown-only). Close fork: (A) seed `friction.emission_rule` rows + restore full §12.2a mapping + verify; OR (B) PK formally narrows scope to P1-true-stuck-only + verify. Does NOT block the next cycle.
- **Production mutations: 0. D-01 fires: 0.**

**v3.02 (+v3.02.1 +v3.02.2) ADDITIONS (preserved):**

- **Mobile/narrow viewport verification RECORDED CLOSED/PASS** (CCB 306×498 Nexus 5 DPR2). **Mobile-fix commit SHA backfilled v3.02.2** — `d17b604`. **v3.02.1 reconciliation:** original v3.02 full-file push clobbered v3.01 `00_` content; repaired read-HEAD-first; L41 re-exercised.
- **Production mutations: 0. D-01 fires: 0.**

**v3.01 ADDITIONS (preserved):**

- **Cowork brief lifecycle gating WARN CLOSED.** v2.94 convention validated end-to-end vs the 2026-05-20T16:02:37Z natural fire. Brief reset to `ready`. Q-005 remains OPEN, non-blocking. Core rank 1 = cc-0015 Gate 11 watch.

---

## 📊 Closure budget tracking (per D186)

| Metric | Current | Limit | Status |
|---|---|---|---|
| Open findings + investigations (P0+P1) | 0 (recon CLOSED v2.93; PHASES streak CLOSED v2.95; top alert bar CLOSED v2.96; Stage B CLOSED v2.99; Stage D CLOSED v3.00; Cowork WARN CLOSED v3.01; mobile viewport CLOSED v3.02; **YouTube publishing RESTORED v3.05**; **F-YT-PUB-AVATAR-EXCLUSION CLOSED v3.08**). *(Q-005 P3 non-blocking; F-YT-FAILED-NO-RETRY + F-YT-EXPIRY-DISPLAY-FAKE P2/P3 candidate-brief, not counted as open P0/P1.)* | 20 | ✅ within budget |
| Trailing-14-day closure hours | ~40h (cumulative v2.83–v3.05) | 8.0 floor | ✅ above floor |
| Pause trigger active? | NO | — | New automation authoring allowed |

**v3.34 cycle: ~read-only trace audit + 1 doc commit (`6fd5f4d`) + this `00_` close. 0 production mutation. 0 DB write. 0 D-01. 0 deploy. 0 provider call. 0 cron change.**
**v3.27 cycle: ~1 EF deploy + docs closure** — video-worker caption-hierarchy polish: FF-merge `f0c9682..fc3ec12` + push `main`; deployed `video-worker` → `video-worker-v3.1.3` via `./scripts/safe-deploy.sh video-worker --allow-warn` (wrapper only). **6 targeted EF-drift refreshes** (4 stale A-LE during GitHub raw-CDN propagation polling + 1 confirming B-FD pre-deploy + 1 final post-deploy A-LE) → lifecycle A-LE(3.1.2) → B-FD(3.1.3 pending) → A-LE(3.1.3 in-spec); repo==deployed hash `0991ff65…b4088bd7`; health `v3.1.3` HTTP 200. **1 production mutation = 1 EF deploy (+ 1 FF-merge + 6 advisory drift-log refreshes). 1 `ef_deploy` D-01 CCD-inline (PK phrase recorded verbatim; no `m.chatgpt_review` fabricated). 0 schema / 0 migration / 0 cron/queue / 0 publisher/social / 0 image-worker / 0 webhook / 0 render / 0 Creatomate-ElevenLabs call / 0 production draft mutation / 0 backfill / 0 code change beyond the approved caption-hierarchy commit.** CCH code review PASS + CCB visual PASS gated the merge/deploy. F-CREATOMATE-CAPTION-HIERARCHY CLOSED; F-VOICE-LOWVOL-BACKFILL remains OPEN P3 (separate carry); branch cleanup deferred. Session file `docs/runtime/sessions/2026-06-02-v3.27-video-worker-caption-hierarchy-deploy.md`.
**v3.05 cycle: ~1.5h** (YouTube full-depth audit + root-cause + 2 live test publishes + backlog release/retire + cc-0019 brief authoring/harden/review + full session close). 3 apply_migration (draft video_status only). 2 publisher invokes. 4 D-01 fires. **State-capture exception count: 0** (cumulative 1).
**v3.04 cycle: ~0.4h** (dashboard Slice 0A merge verification + cross-repo recording; docs-only). 0 schema mutations. 0 D-01.
**v3.07 cycle: ~repo doc-only** (cc-0017c v1.2 patch + correction; large-brief + lesson deferrals; close). 0 production mutation. 0 Supabase. 0 D-01.
**v3.08 cycle: ~repo doc-only** (avatar→YouTube repo reconciliation — EF source parity + continuity docs + `00_` rebuild for an already-live pipeline; + post-milestone cc-0019 Unit B re-verification, doc-only). 0 production mutation. 0 new deploy. 0 Supabase. 0 D-01.
**v3.13 cycle: ~dashboard merge + deploy** (cc-0015 Stage B frontend FF-merge to dashboard `main` `25bcdb7` + Vercel production deploy `dpl_Hb2BkFnK3cL4cCAHXbkAz1tDnHjW` after CCB preview visual PASS; + content-engine docs closeout). **1 production mutation = 1 frontend deploy.** 0 SQL / 0 migration / 0 new D-01 (merge+deploy gated on CCB visual PASS + PK directive — same pattern as the Stage F merge; the Stage B RPC's plan_review/sql_destructive D-01s already fired/closed v3.12).
**v3.13.1 cycle: ~Stage A.5 recategorisation + docs** (1 guarded UPDATE migration `20260526222125` moving 3 cases `operator_friction`→`dashboard_ui`; + read-only verification; + this docs closeout). **1 production mutation = 1 guarded UPDATE (3 rows).** 1 sql_destructive D-01 `bbe9a0fa` (agree/escalate=false) + close-the-loop. Migration file backfilled+pushed `41576f8`.
**v3.13.2 cycle: ~doc authoring** (cc-0015 Stage G — authored `docs/process/ICE-PROC-002-pooled-resolution.md` + `00_docs_index.md`/`00_`/brief updates). **0 production mutation. 0 SQL. 0 D-01.** `00_`-only close (no separate session file, v3.07 precedent).
**v3.13.3 cycle: ~doc record** (cc-0015 Stage G ICE-PROC-002 V-G1 PK approval recorded in the doc + `00_`/brief). **0 production mutation. 0 SQL. 0 D-01.** `00_`-only close. Stage G now complete as a doc artefact (V-G1/V-G2/V-G3 satisfied); next milestone = +4-week review ≈ 2026-06-26.
**v3.14 cycle: ~PRV read-only MVP lane + 4-way sync** — 1 apply_migration (`20260527011420_prv_readonly_login_role`, least-privilege read role `prv_readonly`; D-01 `d86b7db8` agree/proceed + PK phrase `PK APPROVES PRV DB/RPC MUTATION`; backfilled byte-exact) + close-the-loop on `m.chatgpt_review` (`d86b7db8` completed; `57fd738b`+`43140c7f` superseded→resolved); `/recon` MVP committed `7287a1e` + pushed to `origin/feature/platform-reconciliation-view-readonly` (unmerged, undeployed). **1 production mutation = 1 role-DDL migration; 0 deploy; 0 PostgREST exposure.** D-01: 1 cleared (`d86b7db8`) + 2 superseded.
**v3.15 cycle: ~docs-only session close** (recorded two parked lanes — Instagram diagnosis [CCH lane] + cc-0020 subscription-email-ingest [CCD lane]). **0 production mutation. 0 SQL. 0 migration apply. 0 DB connection. 0 deploy. 0 Gmail/OAuth/token. 0 cron/Pub-Sub. 0 PR/merge. 0 D-01.** Docs-only on `main`: v3.15 session note + `00_` sync_state/action_list. _(NOTE v3.16 SUPERSEDES the cc-0020 "draft UNAPPLIED / branch unmerged/undeployed" framing — cc-0020 is now LIVE IN PROD; see v3.16 cycle line for details.)_
**v3.20 cycle: ~1 EF deploy + repo parity** — youtube-publisher v1.10.0→v1.11.0: future YouTube uploads default to PUBLIC. Single named constant `DEFAULT_PRIVACY_STATUS='public'` (`index.ts:98`) used by both the upload `privacyStatus` (`:293`) and the audit `response_payload.privacy_status` (`:318`); VERSION→`youtube-publisher-v1.11.0`. Deployed `supabase functions deploy youtube-publisher --no-verify-jwt --project-ref mbkmaxqhsohbtwsqolns` (SUCCESS); V-1 GET health/version=`youtube-publisher-v1.11.0` HTTP 200. **1 production mutation = 1 EF deploy. 1 `ef_deploy` D-01 (PK-approved). 0 DB mutation / 0 SQL / 0 draft reset / 0 forced publish / 0 YouTube API call / 0 backfill / 0 historical cleanup / 0 feature expansion.** Safety rests on the unchanged approval gate (`approval_status IN ('approved','published')` + `video_status='generated'` + format allow-list + `youtube_video_id IS NULL`). **OPEN follow-up (runtime, non-blocking):** post-publish authoritative visibility verification after the next controlled/real publish — proof MUST be YouTube Studio or `videos.list?part=status` showing `status.privacyStatus == 'public'`; ICE audit `privacy_status: 'public'` corroborating only. Repo↔deployed parity restored (prod was already on v1.11.0).
**▶ YouTube insights-worker — STAGE 1 DEPLOYED + MANUALLY VERIFIED + MERGED TO MAIN ✅ (2026-05-29)** (`docs/briefs/youtube-insights-worker.md` §12). `youtube-insights-worker-v1.0.0` deployed from review commit `50332b6` (after a fail-safe first deploy `9428b9d` + token-resolution fix) and **fast-forwarded into `main` (`6d31950`→`50332b6`)**. Manual verification all-PASS: token_source=`client_channel.config` both clients, scopes incl. `youtube.readonly`, **62 YouTube `m.post_performance` rows written** (PP 46 + NDIS 16), `raw_payload.privacy_status` populated, **FB rows unchanged at 332**, zero-view engagement-rate guard OK, invoke #2 idempotent (62 stable, no dupes, timestamps advanced), no NEEDS_RECONSENT / no rotation / no cross-platform collision / tokens never printed. **No cron** (gated `x-worker-key` manual invoke only). **NEXT GATED ACTION = cron scheduling proposal ONLY** — cron requires a **separate `sql_destructive` D-01**, AND **durable alerting/rotation+collision marker must replace the current JSON-only surfacing before any unattended cron run**. **v3.20 follow-up REMAINS OPEN** — Stage 1 only *arms* the privacyStatus verification; not closed until a genuinely **post-v1.11.0** YouTube upload is observed via API as `public` (historical/probe `public` rows do not prove the v1.11.0 path). **⚠️ Process note:** a parallel FB `insights-worker` v14.2.0 lane shared the SAME git checkout and the review branch pointer flipped mid-task (no work lost; verified before every git write) — **recommend isolated git worktrees for concurrent CCD/FB-lane work.** Deferred (each its own gate): typed columns / `m.channel_performance` / dashboard UI / thumbnails / playlists / captions / Analytics API / CFW onboarding / historical-snapshot trend lines.
**◦ (history) Stage 0 probe + brief** (`docs/briefs/youtube-insights-worker.md`). Investigation + WCCH-hardened spec + executed probe + Stage 1 implementation brief all done. **Stage 0 result (temporary Option-B EF `youtube-scope-probe` deployed→run-once→deleted→verified-absent under a written `ef_deploy` D-01):** **Property Pulse = `STAGE1_READY`** (46 YT rows; scopes incl. youtube.readonly; videos.list+channels.list pass; privacyStatus=public; no rotation), **NDIS-Yarns = `STAGE1_READY`** (16 YT rows; same), **CFW = `UNTESTED_NO_YOUTUBE_ROWS`** (0 YouTube published rows — not a failure; re-probe required if/when CFW publishes YouTube). `quota_units_consumed=4`; R1+R6 did NOT materialize; **0 DB write / 0 schema / 0 cron / 0 YouTube mutation; probe EF confirmed deleted (functions list absent + GET 404).** **Stage 1 brief WCCH-reviewed (2026-05-29, "approve with changes") — all 9 required hardenings applied** (per-run cap `MAX_VIDEOS_PER_RUN` + duplicate-safe LATERAL selection + backpressure; **live unique-constraint verification required before build** + runtime cross-platform-clobber guard if `platform` not in the key; concrete unattended token-rotation behaviour — durable marker + no silent cron continue; engagement-rate divide-by-zero guard; sharpened `viewCount→impressions` lossy-mapping warning; **v3.20 corrected to ARMED-not-closed**; named history-loss + channel-stat-at-video-grain limitations; allowlist kept a code constant; batch-of-50 chunking mandated as implemented). **Next gated action = Stage 1 build/deploy — NOT yet approved** (own `ef_deploy` D-01 for the worker + separate `sql_destructive` for cron; no schema migration for MVP). Stage 1 design (brief §11): new `youtube-insights-worker` reads `m.post_publish` (platform='youtube', status='published', platform_post_id NOT NULL) for the 2 STAGE1_READY clients only (CFW skipped), `videos.list`+`channels.list`, idempotent upsert into existing `m.post_performance` (platform='youtube'; raw_payload carries authoritative `privacy_status`), reuse table (no DDL). Stage 1 **arms** (does not by itself close) the open v3.20 post-publish `privacyStatus` verification — closure needs a genuinely post-v1.11.0 upload observed `public`. `known_weak_evidence`: live-verify upsert constraint; cross-platform id disjointness load-bearing if platform not in key; v3.20 armed-not-completed. Deferred: typed columns / `m.channel_performance` / dashboard UI / thumbnails / playlists / captions / Analytics API / CFW onboarding / historical-snapshot trend lines.
**v3.19 cycle: ~friction-register manual operating passes (1+2)** — first full capture→triage→**resolve** loop on the friction register (recorded as session file `docs/runtime/sessions/2026-05-29-v3.19-friction-register-manual-passes.md`, commit `a62a31e`). Register **46/45 open/1 resolved/3 new → 46/42 open/4 resolved/0 new**. Pass 1 (D-01 `a94c9f23`, 3 writes): track `c53f5af6` + `220bc43a`; resolve `a6c6032f` `tracked_done`; held `15d57015`. Pass 2 (D-01 `9432e82a`, observer_stale ×3): `d5efc053` suppress (no YouTube insights pipeline), `26436794` track+recat pipeline_integrity (GENUINE NY-fb insights gap — `m.post_performance` stopped 2026-05-23), `fcb5612e` resolve `tracked_done` (residual). **Production mutation = single-case `friction.case` SECURITY DEFINER RPC calls only, one-at-a-time under D-01 + PK exact approval, each post-call verified. 0 schema / 0 EF / 0 cron / 0 migration / 0 dashboard code / 0 decisions.md.** Structural finding: `/operations` UI cannot close a case (`resolve_case`/`mark_duplicate` not wired in). Real next lane: NY-fb (+ CFW-fb) Facebook insights-coverage gap. Both D-01s closed-loop.
**v3.18 cycle: ~docs-only session close + Stage 5 Unit B PR #6 merged** — code-only Unit B readiness for `supabase/functions/subscription-email-ingest` landed on `main` via PR #6 (merge `9e4d3db`; head `1888b56`; parents `df8c54e` + `1888b56`). Two-commit progression on the branch: `75cfba3` (original Unit B: metadata-only Gmail fetch + async repo interface + lazy service-role client + version banner `v1.0.0-disabled`) followed by `1888b56` (V-B7 fix: RPC-only `ServiceRoleIngestRepository` calling `public.ingest_subscription_import_candidate` + `SupabaseLike` type reduced to `.rpc()` only + V-B7 canary). 41-test hermetic suite PASS pre-merge. CCH plan_review CLEAN after V-B7 patch. **Unit B NOT deployed; HAS NO RUNTIME EFFECT.** Unit C BLOCKED on (a) the public write RPC migration (b) sql_destructive D-01 + PK exact approval (c) real-PG/PGlite proof (d) U-C ef_deploy gate. Unit A required before U-D first smoke (OAuth Production status + gmail.readonly scope + Supabase secrets set + secret-name presence verified without printing values). Unit D blocked until U-A + U-C complete. Unit E cron = separate later gate. Unit F Pub/Sub = DEFERRED Stage 5b. **0 deploy / 0 OAuth / 0 Gmail/token access / 0 Pub/Sub / 0 cron / 0 DB write / 0 migration / 0 production runtime change / 0 D-01 fired this session.** Docs-only on `main`: `00_` sync_state/action_list updated to record v3.18 close. No session file (per v3.13.2/v3.13.3 precedent).
**v3.17 cycle: ~docs-only Stage 5 scoping + governance G1 close + G2 accepted-as-applied** — (1) cc-0020 Stage 5 execution plan AUTHORED at `docs/briefs/cc-0020/stage-5-live-gmail-execution.md` (6-unit split A→B→C→D→E→F; U-F Pub/Sub DEFERRED Stage 5b; per-unit V-checks + D-01 + PK approval phrases + rollback; global kill switch `SUBSCRIPTION_GMAIL_INGEST_ENABLED=false`; smallest-blast-radius smoke design: `newer_than:1d` + `maxResults:10` + `format=metadata` ONLY + sender allowlist + single manual invoke + flag-flipped-OFF-after; 10-item risk table; 11-item OOS; 6 OQs); surgically linked from cc-0020 main brief §3 Stage 5. (2) Governance G1 RESOLVED: `m.chatgpt_review.6d9a4bd3-d665-47c0-8dd9-165d3bc75bd6` closed-loop by CCH (`status='resolved'`, `escalation_resolved_at='2026-05-28 10:55:17.310207+00'`, `resolved_by='PK (retrospective confirmation 2026-05-28) + CCH (close-loop record)'`; first UPDATE row_count=1, guarded rerun row_count=0 — idempotency proved). Migrations `20260527114041` + `20260527114144` now have complete in-row governance trail. **G2 (`20260527114333` hotfix) ACCEPTED-AS-APPLIED by PK directive 2026-05-28** (docs-only): pure grant-tightening (REVOKE EXECUTE FROM `anon`,`authenticated`); no data effect; no schema change; residual risk = none known; process gap recorded. **NOT precedent for skipping D-01 on future grant changes.** Live grant posture remains verified correct (RPC EXECUTE = service_role + postgres only). **0 OAuth / 0 Gmail/token access / 0 Pub/Sub / 0 EF deploy / 0 cron / 0 DB write by CCD (G1 UPDATE orchestrated separately by CCH; reported back read-only to CCD) / 0 migration / 0 production mutation / 0 D-01 fired by CCD.** Docs-only on branch `feat/cc-0020-stage-5-scoping`: `00_` sync_state/action_list + cc-0020 main brief §3 link + new `docs/briefs/cc-0020/stage-5-live-gmail-execution.md`.
**v3.16 cycle: ~docs-only four-point reconciliation patch** — (1) cc-0016 Stage E dry-run design authored (`docs/briefs/cc-0016/stage-e-dryrun-design.md`); (2) cc-0020 status CORRECTED from "parked" to LIVE IN PROD (merge `49a6c0e` + 3 migrations applied `20260527114041/114144/114333` + repo↔prod migration-history reconciliation v2 **merged on `main`** via PR #3 merge commit `d7543078` (parents `3cd144f` + `fa2aefc`; canonical applied-version files + tombstone slots are mainline) + dashboard `main` Stage 4-A/4-B routes live; **governance trail — partially evidenced, not cleanly closed**: CCH read-only sweep resolved the prior gap — migrations `114041`+`114144` governed by review row `6d9a4bd3-d665-47c0-8dd9-165d3bc75bd6` (plan_review, verdict=partial, escalated, NOT closed in-row); residual G1 = `6d9a4bd3` close-the-loop never recorded; residual G2 = `114333` hotfix has no D-01/governance row; live grant posture verified correct = service_role+postgres only (not a live-permissions risk)); (3) YT backlog drain CLOSED (61 m.post_publish YouTube rows / latest 2026-05-28 08:15:08Z / gap=0; carry separately 2 NY `generated` stragglers + stale pending pre-existing/predicate-ineligible); (4) cc-0015 Gate 11 observation window ELAPSED/CLOSED 2026-05-26 as a time-bound watch; Stage C/D/E remain separately PK-gated. **0 production mutation. 0 SQL. 0 migration apply. 0 DB connection. 0 deploy. 0 Gmail/OAuth/token. 0 cron/Pub-Sub. 0 PR/merge. 0 D-01 fired this session.** Docs-only: `00_` sync_state/action_list + cc-0016 main brief §7 link + new `docs/briefs/cc-0016/stage-e-dryrun-design.md`.

---

## ⭐ Today / Next 5 (core ICE ranks)

> Last rebuilt: 2026-05-24 Sydney (core ranks carried unchanged from v3.02; v3.05 added YouTube + cc-0019 carries below the core table — no core rank change). v3.07: no core rank change (doc-only). v3.08: no core rank change (repo reconciliation, doc-only).

| Rank | Item | Priority | Why now | Next action |
|---|---|---|---|---|
| 1 | **cc-0015 friction-pool-view — IN EXECUTION (v3.13.3)** | **P2, rank 1** | Gate PK-confirmed (cc-0014 early-close D-IOL-001). **Stage A APPLIED** (mig `cc_0015_a_pool_schema_additions` v`20260526110657`; D-01 `58d89efe`; V-A1–A4 PASS). **Stage A.5 APPLIED + verified (v3.13.1)** — mig `20260526222125_cc_0015_a5_recat_operator_friction_to_dashboard_ui` moved 3 dashboard-UI cases (`53f3e533`/`2cf0cd4f`/`b7369dc9`) `operator_friction`→`dashboard_ui` (guarded UPDATE only; sql_destructive D-01 `bbe9a0fa` agree/escalate=false + PK phrase; `dashboard_ui` 0→3, `operator_friction` 6→3, others unchanged, total 44; file backfilled+pushed `41576f8`). **Stage F SHIPPED — CCB V-F1/V-F2 PASS, merged + deployed to production** (dashboard main `f9d51a96`; Vercel prod `dpl_FftxSjeBNyurxZetJedi3zDsan6b` READY @ `dashboard.invegent.com`). **Stage B RPC APPLIED** (COEXIST; `fn_recent_cases` filter extension, mig `cc_0015_b_fn_recent_cases_filters` v`20260526124005`; plan_review `58d89efe` + sql_destructive `17db8b27` + PK phrase; V-B2 byte-identical; file backfilled). **Stage B FRONTEND MERGED + DEPLOYED (v3.13)** — `/operations` filter bar + 5 saved views + URL-param filters + category-desc hydration; CCB preview V-B3/V-B4/V-B5/V-B6 PASS (stale-wording CLEAN) on `feat/cc-0015-stage-b-operations-filters` (`25bcdb7`) → FF-merge to dashboard main `25bcdb7` (tsc clean + build exit 0) → production deploy READY (Vercel `dpl_Hb2BkFnK3cL4cCAHXbkAz1tDnHjW`, `dashboard.invegent.com`; liveness `/operations` 307→`/login` 200, no 5xx). Category-desc hydration COMPLETE for `/operations` (FAB deferred). **Stage G AUTHORED + V-G1 PK-APPROVED (v3.13.3)** — `docs/process/ICE-PROC-002-pooled-resolution.md` is the operating reference for the current cc-0015 surface; V-G1+V-G2 (`d5b296f`)+V-G3 all satisfied → Stage G complete as a doc artefact. **Stage C/D/E excluded; `/operations/pools` v0 untouched (V-B5); source/sort deferred.** **cc-0015 NOT complete.** | PK → CCD | (a) ✅ Stage A + Stage A.5 + Stage F + Stage B RPC + Stage B frontend + **Stage G doc (V-G1 approved)** all shipped/live/approved; (b) **next:** Stage C (batch) / D (status-strip counts) / E (pool-session UI) — each PK-gated, separate; (c) source filter/sort + FAB hydration when PK directs; **ICE-PROC-002 +4-week review ≈ 2026-06-26**. |
| 2 | **cc-0016 Stage E dry-run design AUTHORED (v3.16) — non-mutating** | **P2, rank 2** | cc-0016 evidence path complete A→B→C→D + mobile-verified; only Stage E (lifecycle cleanup) remains. Option A satisfied: `docs/briefs/cc-0016/stage-e-dryrun-design.md` authored 2026-05-28 (cleanup objective + 10 boundaries + no-op guarantee; 9 SELECT-only verification queries; JSON output format; 9 acceptance criteria; future destructive gate G-E1/G-E2/G-E3 explicitly required). First destructive run STILL requires separate D-01 + PK exact approval phrase + ≥14 prod days. | PK → chat (when ready for the destructive gate) | When PK directs: review dry-run design, run Q-1…Q-9 read-only against live, commit dry-run JSON to a session note, then fire D-01 G-E1 with that JSON + PK exact approval phrase G-E2 + verify G-E3 maturity → apply migration `cc_0016_e_attachment_lifecycle`. |
| 3 | **Wave 0f scoping** | **P3 brief-authoring only, opportunistic during Gate 11 window** | Non-mutating. Candidates: items B/E/F/G from cc-0017e + `purge_test_case` helper case_history extension (L-v2.90-d). | chat → PK | When PK directs (during Gate 11 window → 2026-05-26). |
| 4 | **Platform Reconciliation View (PRV) — read-only MVP COMMITTED + PUSHED (v3.14)** | **P2; feature-branch (unmerged, undeployed)** | `/recon` local read-only MVP committed `7287a1e` + pushed to `origin/feature/platform-reconciliation-view-readonly`; reads 3 cc-0012 `op.*` views via server-only direct-PG as least-privilege `prv_readonly` (mig `20260527011420`; D-01 `d86b7db8` + PK phrase; close-loop done); live render + CCB visual PASS; `op` not PostgREST-exposed. | PK → CCD | PR / production deploy = separate PK gate (`PK APPROVES PRV READ-ONLY PRODUCTION DEPLOY`); next read-only increments = surface `op.v_per_client_rollup` + `op.v_per_platform_rollup`; polish deferred. |
| 5 | **5-row close-the-loop batch sweep / Pre-sales criteria / `purge_test_case` helper case_history extension** | **P2/P3 carry** | Outstanding close-the-loop UPDATEs (net not recomputed). | chat → PK | When PK directs. |

**YouTube carries (NEW v3.05, not core-ranked — operational):**
- **YT backlog drain watch — CLOSED v3.16** (per CCH 2026-05-28): `m.post_publish` YouTube rows = **61**; latest publish row at **2026-05-28 08:15:08Z**; **published-draft↔youtube-publish-row gap = 0** for the v3.05 fresh-release cohort (NY 6 + PP 7 = 13). Carry separately (NOT the released backlog): 2 stale NY `generated` stragglers + stale `pending` drafts are **pre-existing / predicate-ineligible** (fail the publisher's `video_status='generated'` + `approval_status IN ('approved','published')` + `youtube_video_id IS NULL` + `video_url IS NOT NULL` + `recommended_format` ∈ allow-list predicate).
- **YT token durability** — spot-check NY/PP tokens still work past ~31 May (confirms Production removed the 7-day cap, not just reset the clock).
- **CFW + Invegent YouTube onboarding** — never connected (no `c.client_channel` YT row, no refresh token); CFW failed drafts also `approval_status='published'`. First-time setup, not a reconnect.
- **F-YT-FAILED-NO-RETRY** — **Unit A DEPLOYED + verified v3.09** (`youtube-publisher` **v1.8.0** live, `--no-verify-jwt`; from `main` `b983a38` = EF commit `631fa93a`; D-01 `ef_deploy` `818ee949` + PK directive; live GET `youtube-publisher-v1.8.0` HTTP 200). plan_review CLEAN v3.06 (`abf6f63c`→`f89c23cf`; brief `bee872aa`→`a3f329f3`). **Unit B1-only APPLIED + CCD-verified v3.09** — migration `yt_failed_backlog_recovery_unit_b_b1_only_20260526` (v`20260526054427`), D-01 `sql_destructive` `33eeb164…` (`agree`/`completed`); **5 quota drafts recovered** (`failed` 31→26; 0 quota remain). **Unit B-2 EXECUTED v3.10** — v1.9.0 deployed (ef_deploy `4719f53b`; predicate → `IN ('approved','published')`) + **B2 17 token-casualty recovered** (sql_destructive `2135ae11` + PK phrase; mig `…unit_b2_token_casualty_20260526`; 17 `failed→generated`, `approval_status` preserved=`published`; `failed`→9 B3-only; 2/17 already published). **Full Unit B (B1+B2) COMPLETE; B3 9 = onboarding debt.** ✅ **F-YT-PUB-PUBLISH-AUDIT-GAP CLOSED v3.11** — v1.10.0 deployed (ef_deploy `5bd19069`; next-available attempt_no + non-silent insertErr) + 11 historical audit rows backfilled (sql_destructive `998c90a3` + PK phrase; mig `yt_pub_publish_audit_gap_backfill_20260526`; gap=0). **Entire YouTube failed-backlog recovery arc complete: B1+B2 done, B3 onboarding debt only.** *(v3.11.1: the 3 apply_migration-applied recovery migrations — B1/B2/audit-gap-backfill — backfilled verbatim to `supabase/migrations/` from `schema_migrations.statements` (md5-verified byte-exact); DB→repo migration drift CLOSED.)*
- **F-YT-EXPIRY-DISPLAY-FAKE cleanup** — candidate (dashboard callback hardcoded 5y expiry).
- **cc-0019 execution** — when PK directs: sql_destructive (Unit A migration) + ef_deploy (Unit B ai-worker) + PK approval. Brief reviewed clean (`ff05c65e`). *(v3.06: Unit A in fact IMPLEMENTED; Unit B optional. v3.08: Unit B re-verified absent against the now-live ai-worker v2.13.0 (`8204a5c7`) — STILL OPEN / optional; the Unit-A fill-gate already stops the primary waste vector.)*
- **Avatar→YouTube Shorts pipeline (NEW v3.08)** — LIVE + repo-reconciled; first unlisted Short `sfQvSM2Osus`. Watch async renders land in `m.post_publish`; no avatar draft stuck in `rendering`.

**Carries flagged (NOT actively ranked):** **Q-nightly-health-check-v1-005 (OPEN, non-blocking, next-fire watch; brief v3.1.1)**; **OWED IG-publish-restore arc 4-way close (pre-compaction 2026-05-24)** *(v3.06: substantially discharged)*; 3 no-fire scheduler days (2026-05-16/18/19, P3 informational); **doc-patch debt residual (v3.07): cc-0017a v1.2 / cc-0010A / cc-0011 / cc-0012 / cc-0017c-vchecks.md V-B4 → CC surgical pass; lessons promotion → next-lesson-cycle/CC; decisions.md → locate first.** **cc-0020 subscription-email-ingest — LIVE IN PROD (v3.16 correction; v3.15 "parked" wording SUPERSEDED):** Stage 0–4B merged to content-engine `main` via `49a6c0e`; 3 migrations APPLIED to prod `mbkmaxqhsohbtwsqolns` (`20260527114041` tables, `20260527114144` 4 `public.*` SECURITY DEFINER RPCs, `20260527114333` security hotfix REVOKE EXECUTE FROM anon,authenticated); `k.subscription_import_candidate` + `k.subscription_spend_event` exist; RPC EXECUTE = service_role + postgres only; repo↔prod migration-history reconciliation v2 **merged on `main`** via PR #3 merge commit `d7543078` (parents `3cd144f` v1 + `fa2aefc` v2; canonical applied-version files + `_proposed_` tombstone slots are mainline); dashboard `main` advanced w/ Stage 4-A UI + 4-B service-role RPC routes (live `dashboard.invegent.com/system/subscriptions`); ingest function in-repo (`supabase/functions/subscription-email-ingest/`) but **not deployed** — Stage 5 wall (Gmail OAuth + Pub/Sub + EF deploy + cron) still intact / future-gated. **Open carries:** (a) **governance trail — G1 CLOSED v3.17 (`6d9a4bd3` resolved); G2 ACCEPTED-AS-APPLIED v3.17 (`20260527114333` accepted by PK, docs-only).** **G1 (CLOSED):** migrations `20260527114041` (tables) + `20260527114144` (RPCs) governed by review row `6d9a4bd3-d665-47c0-8dd9-165d3bc75bd6` — now `status='resolved'`, `escalation_resolved_at='2026-05-28 10:55:17.310207+00'`, `resolved_by='PK (retrospective confirmation 2026-05-28) + CCH (close-loop record)'` (first UPDATE `row_count=1`, guarded re-run `row_count=0` — idempotency proved); v3.16 G1 "not cleanly closed" wording SUPERSEDED. **G2 (ACCEPTED-AS-APPLIED):** migration `20260527114333` REVOKE-anon/authenticated security hotfix has no `m.chatgpt_review` row; **PK directive 2026-05-28** accepts the hotfix as-applied (docs-only) — pure grant-tightening (REVOKE EXECUTE FROM `anon`,`authenticated` on the 4 cc-0020 RPCs), no data effect, no schema change, residual risk = none known, process gap recorded. Live grant posture remains verified correct (RPC EXECUTE = service_role + postgres only, no anon/authenticated/PUBLIC) — G2 is now CLOSED as a docs-only governance carry. **NOT precedent for skipping D-01 on future grant changes** (grant-tightening hotfixes ordinarily fire `sql_destructive` D-01; the `114333` case is a one-time retrospective acceptance because the change was strictly tightening and the resulting posture was verified safe). (b) **Stage 5 progression v3.18:** **Unit B code-readiness MERGED on `main` via PR #6** (merge `9e4d3db`; head `1888b56`; two-commit progression `75cfba3` original + `1888b56` V-B7 RPC-only fix). Function NOT deployed; HAS NO RUNTIME EFFECT (`SUBSCRIPTION_GMAIL_INGEST_ENABLED=false` default short-circuits before any OAuth/network call). Code is RPC-only against `public.ingest_subscription_import_candidate` (the future write RPC); direct `k.*` PostgREST access forbidden at the type + canary level. 41 hermetic tests PASS. **Unit C BLOCKED** until (1) `public.ingest_subscription_import_candidate(...)` SECURITY DEFINER write RPC migration authored (signature recorded in `repository.ts` as `INGEST_CANDIDATE_RPC_PARAMS`); (2) `sql_destructive` D-01 + PK exact approval phrase; (3) real-PG / PGlite proof of idempotency contract; (4) U-C `ef_deploy` deploy-disabled gate fires separately. **Unit A required before U-D first live smoke** (independent of U-C): OAuth app **Production publishing status** per F-YT-OAUTH-TESTING-MODE v3.05; scope = `gmail.readonly` only; Supabase Edge-Function secrets set (`SUBSCRIPTION_GMAIL_REFRESH_TOKEN` / `_CLIENT_ID` / `_CLIENT_SECRET` / `_SENDER_ALLOWLIST`); **secret-name PRESENCE verified without printing values**. **Unit D first live smoke BLOCKED** until Unit A + Unit C complete (smoke design: `newer_than:1d`, `maxResults:10`, `format=metadata` only, sender allowlist, single manual invoke, flag flipped back OFF after). **Unit E cron activation** = separate later gate (`sql_destructive` D-01 for `cron.schedule`). **Unit F Pub/Sub** = DEFERRED to Stage 5b. Stage 5 execution plan (`docs/briefs/cc-0020/stage-5-live-gmail-execution.md`) remains the canonical reference; per-unit gates unchanged. **Instagram "not publishing" diagnosis (v3.15 parked):** runbook `b911d91`; NY/PP `publish_enabled` flipped via UI → cc-0019 fill-gate cleared; self-heal ≈ 2026-05-29; closed unless PK reopens.

**Time-bound nudge:** cc-0015 Gate 11 observation window **ELAPSED/CLOSED 2026-05-26** (1-week window 2026-05-19 → 2026-05-26 ended; the time-bound watch is now closed). Stage C/D/E remain separately PK-gated. Next process milestone: ICE-PROC-002 +4-week review ≈ **2026-06-26**.

## ⭐ Next-session priorities (set v3.30 — supersedes v3.18 ranking)

> **v3.30 strategic re-rank (2026-06-10, PK decision; docs-only priority ranking — 0 production mutation / 0 DB / 0 deploy / 0 D-01 / 0 HeyGen deploy / 0 OBS mutation / 0 observer / 0 scheduler / 0 Stage 0B / 0 cc-0020 implementation).** Build bandwidth goes to **Asset Production (Rank #1)**; **cc-0020 Stage 5 → Rank #2 / idle-guard**. Rationale: cc-0020 is useful but internal and self-contained; Asset Production is the core ICE value lane; parking cc-0020 is low-regret (work inert, kill-switch `SUBSCRIPTION_GMAIL_INGEST_ENABLED=false` OFF, no runtime decay); leaving Asset Production gaps open has ongoing cost (HeyGen/avatar telemetry blindness, CFW silent image-gen loss, routing-skew quantified/closed-as-unknown (reclassified v3.33 as the A2 avatar-override policy decision — keep volume vs relax content-fit — NOT a routing defect), voice-lowvol backlog). **Asset Production enters via the DECOUPLED slice — HeyGen render-telemetry v2.1.0 / D-01 `0d9bb255` (PK exact-phrase approval required) — NOT via OBS or Stage 0B.** OBS remains contained/parked (`obs_readonly` NOLOGIN; ratify-vs-rollback pending) — no observer / scheduler / 0B. **cc-0020 Unit A OAuth may proceed PK-side in parallel** (PK legwork, not build bandwidth). cc-0020 remains the fallback/idle-guard whenever Asset Production waits on a PK approval. The cc-0020 6-unit sequence below is retained verbatim as the Rank #2 idle-guard detail (no longer the headline next build item).

> **Rank #1 — Asset Production.** First step: HeyGen render-telemetry v2.1.0 deploy (D-01 `0d9bb255`). Then voice-lowvol backfill + CFW silent image-gen investigation (both OBS-decoupled). Provider-routing instrumentation stays gated behind the OBS ratify/rollback decision.
>
> **Rank #2 / idle-guard — cc-0020 Stage 5** (sequence retained below).

**cc-0020 Stage 5 (Rank #2 / idle-guard) — sequence retained from v3.18:**

1. **cc-0020 Stage 5 Unit C-prep** — author the `public.ingest_subscription_import_candidate(...)` SECURITY DEFINER write RPC migration against the Stage 2 schema, plus the proof plan (real-PG / PGlite test of `ON CONFLICT (gmail_message_id) DO NOTHING` idempotency + dashboard/RLS posture verification). Required RPC signature is recorded in `supabase/functions/subscription-email-ingest/repository.ts` as `INGEST_CANDIDATE_RPC_PARAMS` + `CANDIDATE_RPC_SQL`. **No apply, no deploy in this prep step** — author the migration file + author the proof harness; the apply is a separate later gate (`sql_destructive` D-01 + PK exact approval phrase).
2. **Unit A readiness** — execute the OAuth + secrets checklist independently (can run in parallel with #1): OAuth app **Production publishing status** verified (NOT Testing, per F-YT-OAUTH-TESTING-MODE v3.05); scope = `https://www.googleapis.com/auth/gmail.readonly` only (not `.modify`, not `.send`); refresh token minted by PK in a one-shot consent flow; Supabase Edge-Function secrets set (`SUBSCRIPTION_GMAIL_REFRESH_TOKEN` / `_CLIENT_ID` / `_CLIENT_SECRET` / `_SENDER_ALLOWLIST`). **CCD must verify secret-name PRESENCE without printing values** (e.g. `supabase secrets list` output grep, never read the values).
3. **Unit C deploy-disabled** — **only after #1 is complete** (RPC migration applied to prod + proof captured + close-loop recorded). Then fire `ef_deploy` D-01 (escalate=true) to deploy `subscription-email-ingest` v1.0.0-disabled. The deployed function returns `gmail_fetch_disabled` for `mode='gmail'` because the kill switch defaults OFF — verified by V-C2 / V-C3 / V-C4 / V-C5.
4. **Unit D first live smoke** — **only after both Unit A (#2) and Unit C (#3) complete**. Two-step gate: (a) `ops_change` D-01 (escalate=true) for the flag flip `SUBSCRIPTION_GMAIL_INGEST_ENABLED=false→true`; (b) PK exact approval phrase for the single manual invoke (`newer_than:1d`, `maxResults:10`, `format=metadata` only). PK eyes-on dashboard `/system/subscriptions` Import Candidates tab; flag flipped back OFF after smoke.
5. **Hold all live runtime switches closed until PK exact approval** — every step above (Unit C deploy, Unit D flag flip + invoke, Unit E cron) fires its own D-01 + PK exact approval phrase. CCD does not advance unilaterally. The Stage 5 execution plan (`docs/briefs/cc-0020/stage-5-live-gmail-execution.md`) §6 governs the per-unit gate flow; do not bypass.

## 🟢 D-002 security remediation + Governor Phase 0 — STATUS BLOCK (NEW v3.59 — D-002 CLOSED / GOVERNOR PHASE 0 LANDED, NO IMPL)

- **SEC-D-2026-06-16-002 / D-002 → FORMALLY CLOSED (closed-action-taken).** Authoritative record: `docs/audit/open_findings.md` (Session 1; commit `628e3b1`). Grant cleanup **Batches 1–10 complete**; `0011` search_path **Batches 1–5 complete** (`d53d629`). **`auth_client_id()` retained INTENTIONALLY for RLS** (design carve-out, NOT unresolved exposure). No further D-002 action queued. **`open_findings.md` is the live source for D-002 — do NOT duplicate D-002 status here.**
- **Governor architecture Phase 0 → LANDED (spec only; NO implementation).** `CLAUDE.md` external-review protocol strengthened (`15e6c7e` — `reviewed_input_hash` + mandatory triage classification + routing); full spec `docs/governance/governor-architecture.md` (`a6df5c2`). `branch-warden` = reference implementation; Governors stateless / read-only / no-memory / no-mutation.
- **NOT started / gated (no implementation authorised):** Packet Governor (**Phase 1 — gated on the spec §9 historical backtest** before any live use), Closure Governor, Deployment Governor, Register Generator (deterministic script). Each is its own future PK-gated lane.

## ⭐ Dashboard work (separately ranked; v3.04 status note)

| Rank | Item | Priority | Why now | Next action |
|---|---|---|---|---|
| D1 | **cc-0015 friction-pool-view UI** | P2, IN EXECUTION v3.13.3 | Stage A schema APPLIED (verified). **Stage A.5 recategorisation APPLIED + verified (v3.13.1)** — 3 dashboard-UI cases moved `operator_friction`→`dashboard_ui` (mig `20260526222125`; D-01 `bbe9a0fa`; `41576f8`); now visible under the live `/operations?category=dashboard_ui` saved view. **Stage F (operator copy) SHIPPED** — CCB V-F1/V-F2 PASS → dashboard main `f9d51a96` → prod READY. **Stage B RPC APPLIED + Stage B FRONTEND MERGED + DEPLOYED (v3.13)** — COEXIST model executed: `/operations` filter bar + 5 saved views + URL-param filters + category-desc hydration; CCB preview V-B3/V-B4/V-B5/V-B6 PASS → FF-merge dashboard main `25bcdb7` → production deploy READY (`dpl_Hb2BkFnK3cL4cCAHXbkAz1tDnHjW`, `dashboard.invegent.com`). **Stage G process doc AUTHORED + V-G1 PK-APPROVED (v3.13.3)** — `docs/process/ICE-PROC-002-pooled-resolution.md` is the operating reference for the current cc-0015 surface (V-G1+V-G2 `d5b296f`+V-G3 satisfied). **Repair-Board v0 (`/operations/pools`) untouched (V-B5) — NOT superseded.** Stage C/D/E held. | PK → CCD | ✅ Stage A.5 + Stage B frontend live + Stage G doc V-G1-approved. Next: Stage C/D/E (each PK-gated); source/sort + FAB hydration deferred; **ICE-PROC-002 +4-week review ≈ 2026-06-26**. |
| D2 | **Platform Reconciliation View surface** (`/recon`) | P2; feature-branch (unmerged, undeployed) | Read-only MVP committed `7287a1e` + pushed to `origin/feature/platform-reconciliation-view-readonly`; server-only direct-PG `prv_readonly` reads `op.v_reconciliation_summary`/`op.v_freshness_rollup`/`op.v_drift_rollup`; live render + CCB visual PASS. | PK → CCD | PR/deploy = separate PK gate; next: surface `op.v_per_client_rollup` + `op.v_per_platform_rollup`; polish (KPI cards/badges/table width) deferred. |

*(Mobile viewport CLOSED/PASS v3.02. **Dashboard Slice 0A IA shell + Visual Tokens v1 MERGED v3.04 at `3ec489b` — Pre-Phase 0 cosmetic, NOT a ranked build and NOT real Phase 0.**)*

**Deferred carry (not actively ranked):** Backend/shared-metrics refactor.

**Standing P0:** Personal businesses check-in. Crazy Domains carry from v2.51.

**Secondary follow-up (P3)**: 3 no-fire scheduler days for `nightly-health-check-v1` — 2026-05-16, 2026-05-18, 2026-05-19.

**Passive observation v3.16**: Cron 82-86 firing normally. **jobid 34 (youtube-publisher, `15,45 * * * *`) active — released YT backlog DRAINED (gap=0, latest 2026-05-28 08:15:08Z, 61 m.post_publish YouTube rows; v3.16 close).** Brief `nightly-health-check-v1` v3.1.1 — next 16:00 UTC fire safe. Dashboard `dashboard.invegent.com`: slices 1–3 + 4A–4B + cc-0016 Stage B/D shipped + verified; mobile-verified; Slice 0A IA shell merged at `3ec489b`; **`/system/subscriptions` Stage 4-A UI + 4-B service-role RPC routes LIVE (cc-0020 v3.16 correction)**. V-A5 smoke artefacts retained — do not delete.

---

## 🟢 Doc-patch debt — STATUS BLOCK (NEW v3.07)

**cc-0017c main brief: PATCHED to v1.2 + CORRECTED. All other located briefs + lessons: DEFERRED.** Only the small cc-0017c main brief (5.8 KB, documented scope) was chat-safe; the rest hit either no-documented-scope or full-file re-emit risk.

| Item | Outcome v3.07 |
|---|---|
| cc-0017c main brief | ✅ **v1.2 PATCHED + CORRECTED** (`6d5c3ae6`→`81dcb683`). APPLIED-WITH-VCHECK-CORRECTION; apply 2026-05-19; migrations `20260519004545`+`20260519005322`; 3 D-01 refs (v1.1 UUID truncated-in-source, not invented); V-B4 12-param pointer. Source: session `2026-05-19-v2.85`. |
| cc-0017a v1.2 | ⛔ DEFERRED to CC. Facts confirmed (APPLIED v2.81; mig `20260518065610`; D-01 `adcc8385-…`; 20/20 V-checks) but **no documented v1.2 scope** + 72 KB full-file re-emit risk. |
| cc-0010A / cc-0011 / cc-0012 | ⛔ DEFERRED to CC. "Minor doc patches" with **zero documented specifics** + 107/61/49 KB re-emit risk. |
| cc-0017c/vchecks.md (sub-file) | ⛔ DEFERRED to CC. **Path located:** `docs/briefs/cc-0017c/vchecks.md`. V-B4 12-param fix grounded; sub-file re-emit → CC surgical. |
| decisions.md (runtime) | ⏸ STILL UNLOCATED (parked). Not in root / `docs/` (index) / `docs/runtime/`. cc-0014 sync_state framing leg already moot per v3.06. |
| Lessons (L41+L-v2.85-e, L-v2.83-a, L-v2.85-a) | ⏸ NOT PROMOTED. Canonical L37–L65 home + numbering convention not locatable in repo; PK option (c) — defer to next-lesson-cycle/CC; nothing promoted by assumption. |

**Reason it stops here:** the GitHub bridge exposes only full-file writes (no str_replace), so 49–107 KB briefs + the canonical-lessons promotion (undocumented home/numbering) belong to CC's surgical path, not chat re-emit. Confirmed-facts-only; no blind patch; no fabricated lesson numbers.

---

## 🟢 cc-0019 publish-eligibility-gate — STATUS BLOCK (CORRECTED v3.06; Unit B re-verified v3.08)

**Status: Unit A IMPLEMENTED (v3.06 correction); Unit B re-verified absent / STILL OPEN / optional (v3.08).** Live DB has `m.is_publish_eligible(uuid,text)` + the `m.fill_pending_slots` gate (migration `20260524091020`, repo-backfilled `8738c3a0`; governed D-01 `a2e5f3f5` + PK approval per migration header). Unit B (ai-worker preflight) **re-verified absent against the now-live ai-worker v2.13.0 (`8204a5c7`)** (post-milestone narrow patch 2026-05-26 — the avatar A2/A3 changes are orthogonal to the eligibility preflight; the v2.12.0 finding holds identically). Unit B stays **optional** — the Unit-A fill-gate already stops the primary token-waste vector. v3.05's "brief only, NOT implemented" was stale.

| Item | Value |
|---|---|
| Brief | `docs/briefs/cc-0019-publish-eligibility-gate.md` — `81cb414` (create) → `01a6cdf` (harden §4) |
| Problem | `fill_pending_slots` spends AI tokens (skeleton draft + queued ai_job) with no publish-eligibility check → ~12% waste on disabled NY/PP IG (7d) |
| Design | `m.is_publish_eligible(client_id, platform)` SECURITY DEFINER + fill-gate (skip→`slot_fill_attempt` decision='skipped' skip_reason='publish_path_disabled', no draft/ai_job) + ai-worker preflight (abandon queued job pre-model-call). Cron dimension out of v1. |
| plan_review | `a75c78f6` (partial/type-c) → re-fire `ff05c65e` (agree/clean) |
| Execution units | Unit A: apply_migration recreating `fill_pending_slots` + new function (sql_destructive D-01). Unit B: ai-worker EF deploy (ef_deploy D-01). PK approval per unit. |
| Status | Unit A IMPLEMENTED (`is_publish_eligible` created + `fill_pending_slots` gate live; migration `20260524091020`). Unit B (ai-worker preflight) **re-verified absent against ai-worker v2.13.0 (`8204a5c7`) — STILL OPEN / optional** (re-verify carry discharged v3.08). |

---

## 🟢 Series v2 Stage 3.5a — platform-aware outline — STATUS BLOCK (NEW v3.45 — LIVE)

**Status: Stage 3.5a COMPLETE / DEPLOYED / VERIFIED. Stage 3.5b (retry_episode platform-awareness) now COMPLETE / APPLIED / VERIFIED in production (v3.46 — see the Stage 3.5b STATUS BLOCK below). Stage 3.5 overall CLOSED. (Stage 4 NOT started.)**

| Item | Value |
|---|---|
| Function | `series-outline` → **series-outline-v1.3.0** (live; deployed CLI `--no-verify-jwt`; `verify_jwt=false` preserved) |
| Commit | `65d6dc4372338c2b9862097eec87cc2c8945d728` — single file `supabase/functions/series-outline/index.ts` |
| D-01 | `b45da2c8-9189-491f-ad83-e9bc90936e8b` (proceed / agree / medium risk / high confidence / escalate=false) |
| Change | reads `series.platforms[]` (fallback singular); calls `public.get_studio_capabilities(client_id)`; episode `recommended_format` constrained to the resolver-valid set ∈{enabled,enabled_unproven} **intersected** across all selected eligible platforms (+ `video_only` detection); valid set feeds the prompt AND the validation whitelist (no longer hardcoded `text\|image_quote\|carousel`); empty set → **fail loud `no_valid_format_for_platform_set` (422)**, never silent text; capability resolve runs BEFORE `outline_pending` so failure leaves series unchanged |
| Interim behaviour | mixed **FB+IG+LI+YT → `video_short_avatar` only** (cross-platform intersection under current taxonomy) — **intentional interim for safety**; per-platform-per-episode format diversity DEFERRED |
| Verification | `deno check` exit 0; live GET `series-outline-v1.3.0`; POST missing/invalid `x-series-key` → 401; header-less GET → 200 (verify_jwt not regressed); read-only resolver dry-check (function NOT invoked): YT-only→all video, IG excludes text, FB+IG+LI+YT ∩ = `[video_short_avatar]`, empty→422 (code-verified); **0 production series mutated** |
| Downstream | `fan_out_episode` / `create_manual_slot_internal` / Advisor / compliance / render / publisher **UNCHANGED** (hard backstops retained) |
| Scope | series-outline EF only; no retry_episode / fan_out_episode / DB / migration / UI / dashboard / publisher change |

**Carries (NEW v3.45 — carried-forward, NOT done):**

| Carry | Detail | Priority | Status |
|---|---|---|---|
| **F-SERIES-RETRY-PLATFORM-AWARENESS** | Stage 3.5b — `retry_episode` now capability/platform-aware (re-resolves a valid format per platform before re-create; mirrors 3.5a). Migration `stage3_5b_retry_episode_platform_aware` (commit `5fc0fa4`), D-01 `6917cc0e-a944-40b2-ab98-77ec27f448ad`. | P2 | **COMPLETE / APPLIED / VERIFIED (v3.46)** |
| **F-SERIES-FORMAT-DIVERSITY** | Per-platform format resolution — replaced the cross-platform INTERSECTION (avatar-collapse / YouTube-drop) with: series-outline UNION (v1.5.0, Stage 2) + `platform_support` publisher-reality correction (Stage 0, 9 formats) + `fan_out_episode` per-platform resolver (Stage 1; md5 `7934ce55`→`d305aec0`). FB→carousel/image_quote/text, IG→carousel/image_quote/video_short_avatar, LI→text, YT→video_short_avatar+kinetic/stat; no platform dropped, no publisher-impossible FB/LI format. D-01 `fa5c1ab6-c034-4979-b698-754296d55ff2`; migrations `…stage0…`(`0aecc23f`) + `…stage1…`(`aa05c168`). Per-stage rollback inlined in migration footers. | P3 | **COMPLETE / DEPLOYED / VERIFIED (v3.53)** |
| **F-SLOT-SCHEDULE-FIDELITY** | Schedule fidelity lane (slot→queue drift; pre-existing carry, Series v2 inherits it) | P4 | **WATCH (downgraded v3.48)** — Fix 1 applied/verified v3.47 (release_queue_on_asset_ready preserves intended schedule); v3.48 discovery downgraded to post-Fix-1 watch (rule-slot bucket resolved by Fix 1; late tail historical OAuth backlog; no June recurrence). Carried: portal-path drift = Phase-3 launch prerequisite (dormant); fan_out past-date clamp = separate design question |
| **F-SERIES-PERSONA-CAPTURE** | Persona **intent** capture/persistence — outline EF requests `persona_label`/`avatar_preference`/`persona_notes`, `episodeRows` maps them, `save_series_outline` persists them to `c.content_series_episode`, `fan_out_episode` carries them into `creative_intent.source_material.persona`. | P3 | **COMPLETE / DEPLOYED / VERIFIED (v3.49)** — DB `save_series_outline` md5 `ec5db304`→`93cf6ff9` (D-01 `2db87754-5af1-4727-9f0b-440bbcea8d91`); EF series-outline **v1.4.0** (commit `4d9b82c`, D-01 `d311d11c` partial/escalate=true, PK-approved). Avatar differentiation **NOT** included → see F-SERIES-AVATAR-DIFFERENTIATION. A dedicated operator UI surface = Stage 4 (NOT started). |
| **F-SERIES-AVATAR-DIFFERENTIATION Stage 1.5 (Hybrid Narrator — shadow)** | Opt-in multi-perspective presenter mode in **series-outline v1.6.0** (commit `d5ee389`): conservative `detectMultiPerspective` (signal phrase OR ≥3 distinct active brand roles), closed `c.brand_stakeholder` role-set injection, `avatar_preference` `role_code=`/`presenter=` encoding, `narrator_mode`/`narrator_mode_trigger` telemetry; default Brand Host path byte-identical to v1.5.0. **SHADOW-ONLY** — no consumed `stakeholder_role` write, no heygen/ai-worker change, no avatar activation, no schema/migration; `save_series_outline` args unchanged. D-01 `c02ab9af-c384-42e9-8fcf-44a003fd5025` (agree/low/high); PK approved Option C (Hybrid). | P3 | **CLOSED — DEPLOYED / VERIFIED WITH OBSERVATIONS (v3.54)** — verified deploy/version/flag only (function version 49, `verify_jwt=false`, `deno check` exit 0); trigger/role-injection NOT exercised against a live series this lane; downstream stays shadow telemetry. |
| **F-SERIES-AVATAR-DIFFERENTIATION (Branch B)** | Render **distinct avatars / character / voice per persona** + resolve the pin-vs-persona governance (persona is captured as intent in v3.49 but does NOT yet drive avatar selection). Touches `stakeholder_role` / heygen-worker / `c.brand_avatar` / A2–Branch-A pin — **none changed by v3.49.** | P3 | DEFERRED (Branch B / avatar-governance lane — separate, gated) |
| **AVATAR-GOVERNANCE-PLANNING** | Design how narrator/persona intent becomes **governed avatar selection** — the consumed `stakeholder_role` write, pin-vs-persona precedence, heygen-worker activation, `c.brand_avatar` mapping. Successor planning lane bridging Stage 1.5 shadow → Branch B render. | P3 | **AVATAR-GOVERNANCE-PLANNING (Branch B readiness, P3, planning-only) — AUDIT COMPLETE (v3.55); D-01 #1 RATIFIED (v3.56); D-01 #2 SCHEMA DIRECTION RATIFIED (v3.57); D-01 #2-MIGRATION APPLIED/VERIFIED (v3.58).** D-01 #2-migration: `agp_d01_2_migration_brand_avatar_priority_default_markers` (version 20260616015419) via Supabase apply_migration only — `c.brand_avatar` gains `is_primary` + `is_default_host` (boolean NOT NULL DEFAULT false) + partial unique `uq_brand_avatar_primary_per_role_style` (stakeholder_id, render_style WHERE is_primary) and `uq_brand_avatar_default_host_per_client_style` (client_id, render_style WHERE is_default_host). Additive dormant metadata — selection INERT (A2 pin still drives). Review `bebea250-639a-43cc-9092-495ff7503e96` agree/low/high/proceed (non-escalated; resolved_by=PK); PK phrase recorded. Verified (db-rls-auditor read-only): columns+indexes present; no backfill (primaries=0, default_hosts=0, total=28, all markers false); no resolver/avatar activation. **Gate map: #1 Deterministic Resolution — CLOSED; #2 Schema direction — CLOSED; #2-migration — APPLIED/VERIFIED; #3 shadow resolver + telemetry — PENDING (next); #4 cutover — BLOCKED.** Not authorised: backfill/resolver/selection change/avatar activation/Branch B; further DDL via apply_migration only. Note `docs/runtime/sessions/2026-06-16-agp-d01-2-migration-brand-avatar-markers-applied.md` (v3.58; prior: v3.55 audit, v3.56 #1, v3.57 #2). Each downstream step its own gate (D-01 + PK phrase). **Branch B NOT AUTHORISED.** |

---

## 🟢 F-YT-PLATFORM-ISOLATION — YouTube cross-publish P0 — STATUS BLOCK (NEW v3.50 — DEPLOYED/VERIFIED/CLOSED)

**Status: P0 cross-platform-publish isolation bug DEPLOYED / VERIFIED / CLOSED for future publishing. Positive-publish = WATCH. Historical remediation + WordPress audit = OPEN carries.**

| Item | Value |
|---|---|
| Function | `youtube-publisher` → **youtube-publisher-v1.12.0** (Supabase version 52; `verify_jwt=false`; deployed CLI `--no-verify-jwt`) |
| Commit | `258d213ff3bb93cbaa5b9a7aab91a262ed666d5f` |
| D-01 | `e0c87546-ffe0-409a-ba60-40a037793133` (proceed / agree / medium risk / high confidence) |
| Root cause | youtube-publisher v1.11.0 is a DIRECT-READ publisher whose draft SELECT lacked `platform='youtube'` → any approved draft with a generated `video_short_avatar` asset (incl. **non-YouTube** FB/IG/LI) could be uploaded to YouTube |
| Fix (isolation only) | (a) `platform` added to SELECT + HARD `.eq('platform','youtube')` filter; (b) defensive in-loop `draft.platform !== 'youtube'` skip (logs `platform_isolation_skip`). No other logic changed |
| Verification | `deno check` exit 0; live GET v1.12.0; verify_jwt false; deployed source contains `.eq('platform','youtube')` + the guard; **0 new YouTube cross-publishes** post-deploy; **0 non-YouTube drafts stamped with YouTube IDs** post-deploy; no runtime errors |
| Positive-publish | **WATCH** — no eligible YouTube draft existed post-deploy; the first legitimate YouTube draft after v1.12.0 must publish **while its FB/IG/LI siblings get NO YouTube `m.post_publish` row** |
| Scope | youtube-publisher only; no DB change; no WordPress patch; no historical remediation |
| Rollback | redeploy youtube-publisher v1.11.0 `--no-verify-jwt` (note: that reinstates the isolation bug — do not roll back without the platform filter) |

**Carries (NEW v3.50 — NOT done):**

| Carry | Detail | Priority | Status |
|---|---|---|---|
| **YT-CROSSPUB-HISTORICAL-REMEDIATION** | Already-uploaded cross-platform YouTube videos (non-YouTube drafts uploaded to YouTube before v1.12.0) remain unresolved — identify + decide takedown/relabel/leave. NOT performed this lane. | P2 | OPEN (not started) |
| **WP-DIRECT-READ-ISOLATION-AUDIT** | Audited `wordpress-publisher` v1.0.0 (sha `a446e9ab`) for the YT missing-platform-filter class. **NO defect:** it deliberately filters `pd.platform='facebook'` (+approved/title-body/len>50/NOT EXISTS website publish/LIMIT 3) and posts FB text as a WP article — never selects ig/li/yt; `website` is a non-router owned_publish platform, never a `post_draft.platform`. Blast radius 26 rows all-time, CFW-only, facebook-source, all published; 11/26 image/video-source rendered text-only = content-fidelity caveat (editorial), not isolation. Intentional owned-publish architecture, not accidental fan-out. | P2 | **COMPLETE / VERIFIED (v3.51)** — no platform-isolation fix + no historical remediation required (read-only audit; 1 EF + 5 SQL reads, 0 D-01). |
| **F-AVATAR-RENDER-LATENCY-RECOVERY** | Late `video_short_avatar` renders were not protected by the queue hold/release safeguards (gate/release excluded the avatar format). **Fix 1 (v3.52) DEPLOYED / VERIFIED:** `video_short_avatar` added to the video-format IN-list in `m.gate_queue_on_asset_status()` + `m.release_queue_on_asset_ready()` (migration `favatar_render_latency_recovery_fix1_gate_release_add_avatar`, commit `c7a41af`; D-01 `6516ed5a-8fea-4d20-af6a-304b9cd70a3d`; gate md5 `38fd402d`→`658a09eb`, release `81a38388`→`dbeda9b9`; PGlite 12/12 + live rollback test). **Completed:** avatar queue **hold** support (+4h gate) + avatar queue **release** support (intended-schedule preserve, no NOW()-collapse). **Still open:** historical 20-row contaminated population (YT cross-publish); Instagram generated/published-readiness question; recovery-sweep design; YT historical-remediation dependency. | P3 | **PARTIAL COMPLETE — Fix 1 DEPLOYED / VERIFIED (v3.52); lane OPEN** |

---

## 🟢 F-SLOT-SCHEDULE-FIDELITY — Fix 1 — STATUS BLOCK (v3.47 APPLIED; v3.48 → P4/WATCH)

**Status: Fix 1 APPLIED / VERIFIED in production. Early-publish-on-asset-ready mechanism CLOSED. F-SLOT-SCHEDULE-FIDELITY overall DOWNGRADED v3.48 → P4 / WATCH (post-Fix-1 verification window) — NOT fully closed. Watch criteria: early `1h_1d` → `within_1h`; no new June-origin `>3d` late tail; median absolute delay → publisher-cron cadence.**

| Item | Value |
|---|---|
| Function | `m.release_queue_on_asset_ready()` — body md5 `0fa656e14c7ac44087e103ea56fd73e3` → `0cdede8d1b6037b53ba78b957b64daa7` |
| Migration | `fslot_schedule_fidelity_fix1_release_preserve_intent` (APPLIED & VERIFIED in production) |
| D-01 | `dc6d3bde-051f-4ea3-9fca-9f09217b1033` — verdict **partial / escalate=true**; **PK approved Option A and applied despite the reviewer escalation** (decision recorded) |
| Change | on an image/video asset leaving `pending`, the release no longer collapses `queue.scheduled_for` to `NOW()`; it now sets `GREATEST(COALESCE(post_draft.scheduled_for, linked slot.scheduled_publish_at), NOW())`. Existing `queue.scheduled_for` is intentionally **NOT** used as fallback (may be the artificial `+4h` hold value). Future intended schedules stay future; past / no-intent schedules release now |
| Unchanged | trigger `trg_release_queue_on_asset_ready` remained bound; `gate_queue_on_asset_status` unchanged; `publisher_lock_queue_v1` / `v2` unchanged; no source/dashboard change |
| Verification | future intended schedule stayed future; past intended schedule released now; draft/slot with no intent released now; trigger live rollback test leaked **0 rows**; reproduced early-publish mechanism **closed**; no production test rows leaked |
| Authority impact | none (queue-release timing is downstream of the decision tree; as-built flow + T1 unchanged) |
| Close-out | `docs/runtime/sessions/2026-06-14-fslot-schedule-fidelity-fix1-closeout.md` |

**Open follow-ups (F-SLOT-SCHEDULE-FIDELITY stays OPEN — NOT closed):**

| Follow-up | Detail | Priority | Status |
|---|---|---|---|
| **Portal-path drift** | `draft_approve_and_enqueue` / `portal_approve_draft` use `get_next_scheduled_for` and can discard slot/draft intent. **v3.48:** `portal_approve_draft` has **zero production usage ever** → dormant, not current drift. | P3 | **LATENT — fix before Phase 3 client-portal launch** (launch prerequisite) |
| **fan_out_episode past-date clamp** | past episode times are clamped to `now()+1h` — recorded as a **separate design question, not an active schedule-fidelity bug** | P3 | OPEN (design decision) |
| **Rule-slot semantics** | the early `~23.5h` bucket was the OLD `release_queue_on_asset_ready` NOW()-collapse → **explained/resolved by Fix 1** (v3.48) unless the watch disproves it | P4 | RESOLVED-BY-FIX-1 / under watch |

---

## 🟢 Series v2 Stage 3.5b — platform-aware retry_episode — STATUS BLOCK (NEW v3.46 — APPLIED)

**Status: Stage 3.5b COMPLETE / APPLIED / VERIFIED in production. Stage 3.5 overall CLOSED (3.5a outline + 3.5b retry both live). Stage 4 NOT started.**

| Item | Value |
|---|---|
| RPC | `public.retry_episode(p_episode_id uuid, p_mode text, p_created_by text)` — **signature unchanged**; body md5 `d85366fd…` → `3d21cd0f…` |
| Migration | `stage3_5b_retry_episode_platform_aware` — file `supabase/migrations/stage3_5b_retry_episode_platform_aware.sql`; commit `5fc0fa40f45cbb07088c10061fdad0189ae9caaf` |
| D-01 | `6917cc0e-a944-40b2-ab98-77ec27f448ad` (proceed / agree / medium risk / high confidence / escalate=false) |
| Change | inside the existing per-platform re-create branch: load `get_studio_capabilities(client_id)` once, then before `create_manual_slot_internal` re-resolve a valid format for the platform — **keep** `v_intent.format_preference` if still valid / **pick** deterministic first-valid alternative if not / **fail loud** `no_valid_format_for_platform` if none (no doomed re-create); adds `format` + `format_changed` audit fields to recreated entries |
| Fixes | retry was a no-op for the exact failures it should fix — it re-created rejected slots with the ORIGINAL `format_preference`, which `create_manual_slot_internal` re-rejected (`format_not_supported_on_platform`); now retry re-resolves to a publishable format |
| Unchanged | `create_manual_slot_internal` (md5 `9ec86b61…`, hard backstop retained); mode guard, episode/intent load, brief reuse, dead-slot audit retention, intent-status recompute, fanout_result append; no schema/signature change |
| Verification | invalid YouTube `image_quote` repaired → `video_short_avatar`; valid Facebook `image_quote` kept unchanged; `no_valid_format_for_platform` branch confirmed; **published children protected** (`already_published`); **in-flight protected** (`in_flight_preserved`); dead/failed history preserved; 0 test rows leaked; **0 production series mutated** |
| Interim behaviour | deterministic **first-valid** repair is **intentional interim**, NOT Stage 4 format optimization |
| Repo↔prod drift | migration file/commit `5fc0fa4` header reads "STAGED / NOT APPLIED", but **production IS applied** (live `retry_episode` carries the 3.5b logic: `get_studio_capabilities` + `no_valid_format_for_platform` + `format_changed`). Recorded here as apply-state reconciliation; migration file header left as-is this pass (separate cleanup if desired) |
| Rollback | re-apply prior `retry_episode` definition (md5 `d85366fd…`); no schema change |
| Scope | `retry_episode` RPC only; no `fan_out_episode` / series-outline / Advisor / compliance / render / publisher / UI / schema change |

**Carries:** F-SERIES-FORMAT-DIVERSITY (P3 DEFERRED — per-platform-per-episode format diversity); F-SLOT-SCHEDULE-FIDELITY (P4 WATCH — schedule fidelity, v3.48); F-SERIES-PERSONA-CAPTURE (**persona intent capture COMPLETE/DEPLOYED/VERIFIED v3.49**); F-SERIES-AVATAR-DIFFERENTIATION (P3 DEFERRED — Branch B avatar/voice-per-persona + pin governance, NOT done). **Stage 4 (UI) NOT started.**

---

## 🟢 Facebook publisher assetless-release guard — STATUS BLOCK (NEW v3.44 — LIVE)

**Status: CLOSED / MERGED / DEPLOYED / VERIFIED. FB asset-less publish leak RESOLVED for Facebook. Interim guard / "T2-Lite" (existence/status only) — NOT full T2.**

| Item | Value |
|---|---|
| Function | `publisher` → **publisher-v1.9.0** (Supabase function version 91; **`verify_jwt=false`** confirmed) |
| Branch / merge | `feat/publisher-assetless-guard` → merge `0216b79`; close-out commit `7a4e3c3` |
| Close-out | `docs/runtime/sessions/2026-06-14-publisher-assetless-guard-closeout.md` |
| D-01 | `fcf2b32d-f234-4a36-bb82-60cfe8038e82` (agree / medium / high / proceed) |
| Incident | FB post `962399936961457_122114734587268380`, draft `1d34577bbb5a`, `image_quote`, `image_status='failed'`, no `image_url` → silently published as text (old gate only caught `pending`) |
| Changed | `supabase/functions/publisher/guard.ts` (NEW, pure `classifyFbFormat` + `decideAssetGuard`), `supabase/functions/publisher/index.ts`; harness `docs/briefs/publisher-assetless-guard-validation.test.ts` |
| Behaviour | image_quote → photo (require `image_status='generated'`+`image_url`); carousel → multi-photo (require generated + ≥2 slides, no single-image downgrade); video/animated/unknown → block (never text); pending → hold; failed/missing/pending-timeout → queue `skipped` + `asset_guard_blocked:<reason>` + audit `post_publish` failed row, **draft preserved**; text → feed unchanged |
| FB video | blocked with `fb_video_publish_not_supported_interim` (FB publisher has no video publish path) |
| Validation | `deno check` exit 0; `deno test` **21/21** against real guard fns; post-deploy read-only V-checks PASS; current FB queue (2 image_quote + 1 carousel, all generated) **not stranded** |
| Rollback | redeploy **publisher-v1.8.0** (git blob `56b1d1da`) via Supabase CLI **`--no-verify-jwt`**; **no DB rollback** (guard writes only existing columns/values) |
| Scope | **Facebook publisher only**; no DB/schema change; not full T2 (no OCR/transcript/visual/brand/audio QA); no other-publisher / dashboard / FB-video-publishing change |

**Carries (NEW v3.44 — carried-forward, NOT done):**

| Carry | Detail | Priority | Status |
|---|---|---|---|
| **F-PUBLISHER-ASSETGUARD-LINKEDIN** | Interim assetless guard for the **LinkedIn** publisher (LinkedIn can post text-only → same FB-style text-fallback exposure). Narrowed from F-PUBLISHER-ASSETGUARD-LI-IG-YT after the non-FB publisher asset-guard audit found **Instagram + YouTube not exposed**. | P2 | ✅ CLOSED (v3.64 2026-06-19 — `linkedin-zapier-publisher` v1.2.0 deployed + verified; `linkedin-publisher` v1.3.0 committed/undeployed; guard-only, backstop parity = future follow-up) |
| **F-PUBLISHER-FULL-T2** | Full T2 (rendered-asset QA: OCR / transcript / visual / brand / audio) is the **durable replacement** for this interim existence/status guard | P3 | DEFERRED |
| **F-FB-VIDEO-PUBLISH-SUPPORT** | FB video publishing is **separate feature work**; video formats currently blocked on FB by design (`fb_video_publish_not_supported_interim`) | P3 | DEFERRED / separate feature |

---

## 🟢 YouTube publishing — STATUS BLOCK (NEW v3.05 — RESTORED)

**Status: RESTORED + verified. Both channels (NY, PP) publishing. Backlog draining + stale soft-retired.**

| Item | Value |
|---|---|
| Root cause | OAuth app in Google **Testing** publishing status → refresh tokens capped at 7 days (NY 7.00d / PP 7.92d post-5 May reconnect) |
| Fix | PK moved OAuth app → Production + re-exchanged tokens (NY 10:48 / PP 10:49 UTC) |
| Verified publishes | PP `ma6EG1fz4XQ` (10:57 UTC) / NY `qp-ZGm8lNIo` (11:02 UTC) |
| Backlog | generated NY 6 + PP 7 = 13 (draining 2/tick jobid 34); archived_stale NY 3 + PP 11 = 14; failed-out-of-guard NY 3 + PP 9 (untouched) |
| Migrations | `yt_token_verify_reset_one_pp_draft` / `yt_token_verify_reset_one_ny_draft` / `yt_backlog_release_fresh_softretire_stale` (all 20260524) |
| D-01 | `82443c8c` (PP+NY tests) / `5f7bfc52` (batch) |
| F-YT-FAILED-NO-RETRY | publisher catch sets transient auth failure to terminal `failed` (never re-selected) → freezes backlog silently. Brief authored + plan_review CLEAN v3.06 (`bee872aa`→`a3f329f3`; `abf6f63c`→`f89c23cf`). **FULL UNIT B COMPLETE v3.10.** v1.8.0 deploy (`818ee949`) + B1 5-quota (`33eeb164`, mig `…b1_only`) v3.09; v1.9.0 deploy (`4719f53b`, predicate → `IN ('approved','published')`) + B2 17 token-casualty (`2135ae11` + PK phrase, mig `…b2_token_casualty_20260526`; `failed` 31→9 = B3-only) v3.10. B3 9 = onboarding debt. ✅ **F-YT-PUB-PUBLISH-AUDIT-GAP CLOSED v3.11** (v1.10.0 fix + 11-row backfill — see STATUS BLOCK). |
| ✅ F-YT-PUB-PUBLISH-AUDIT-GAP (CLOSED v3.11) | Cross-posted drafts (already FB-published) that publish to YouTube were marked `video_status='published'` with a real `youtube_video_id` but got **NO `m.post_publish` youtube row**. Root cause: `m.post_publish` `uq_publish_attempt UNIQUE(post_draft_id, attempt_no)` + the publisher hardcodes `attempt_no=1` → collides with the existing FB row → INSERT rejected; `insertErr` logged-not-thrown (index.ts:258). Uploads succeed (videos live) + no double-upload (SELECT gated on `video_status='generated'`), but the YT audit trail is incomplete. Surfaced by v1.9.0 routing `published` drafts to YouTube; all 17 B2 affected as they drain. Backfillable from `draft_format.youtube_video_id`. **Cohort was 11** (2 B2-recovered + **9 pre-existing historical**; all 11 had a FB row at attempt_no=1). **RESOLVED v3.11 (A-then-B):** Unit A `youtube-publisher` **v1.10.0 deployed** (ef_deploy `5bd19069`; next-available `attempt_no` + non-silent `insertErr`) → Unit B **11-row backfill applied** (sql_destructive `998c90a3` + PK phrase; mig `yt_pub_publish_audit_gap_backfill_20260526`; gap=0; all `platform_post_id`==draft `youtube_video_id` @ `attempt_no=2`; FB untouched). **Honest limit:** `platform_post_id` authoritative; historical `published_at`/`response_payload` reconstructed + `backfill`-tagged (original YouTube API payload never captured). Brief: `docs/briefs/yt-publisher-publish-audit-gap.md`. First 2: YouTube `VonxZesS0Ws`, `u-xYWGg1qWI`. |
| F-YT-EXPIRY-DISPLAY-FAKE | callback hardcodes `token_expires_at=now()+5y` → fake "Valid 2031" masks dead tokens. Candidate cleanup. |
| CFW + Invegent | never connected (no YT channel row, no token). Separate onboarding. |
| Durability | unproven until past ~31 May (Production-issued token should not carry the 7-day cap). |

**Do NOT:** reconnect tokens as the "fix" if YT breaks at a clean 7-day boundary (cause = publishing status, not token); bulk-reset `archived_stale`→`generated` without PK direction; treat `failed` YT drafts as auto-recoverable (F-YT-FAILED-NO-RETRY).

---

## 🟢 Avatar→YouTube Shorts pipeline — STATUS BLOCK (NEW v3.08 — LIVE + REPO-RECONCILED)

**Status: LIVE end-to-end + repo source reconciled to `main`. First live unlisted Short published. 0 production mutation / 0 new deploy / 0 D-01 (the EFs were already deployed + verified-publishing; this was repo source-parity reconciliation).**

| Item | Value |
|---|---|
| Pipeline | slot (youtube, video_short_avatar) → ai-worker **v2.13.0** (A2 avatar override + A3 narration branch; `video_status='pending'`) → heygen-worker **v2.0.0** (async two-phase submit/poll; 720×1280 portrait) → youtube-publisher **v1.7.0** (`video_short_avatar` allow-listed) |
| First Short | `sfQvSM2Osus` (NDIS-Yarns, draft `40f9fa25`, `m.post_publish` `5dbfcc0f`, 720×1280, 7.8 MB, 2026-05-25 11:42:07 UTC, unlisted; same `heygen_video_id` across submit/poll — no duplicate render) |
| F-YT-PUB-AVATAR-EXCLUSION | **CLOSED v3.08** (avatar in publisher allow-list) |
| Reconciliation commits | EF parity `521b053b`+`194e0792`; continuity docs `04294d23`; `00_` rebuild (this cycle) — all 12 files byte-identical to branch `feat/ai-worker-v2.13.0-avatar-youtube` (sha-verified) |
| Closeout doc | `docs/operations/avatar-youtube-pipeline-status-2026-05-25.md` *(its §2 cites branch build-history refs `5005b2b`/`aa07252`/`21e372d`; the authoritative `main` reconciliation commits are the four above)* |
| Retired | landscape proof `ba5b34eb` → `archived_stale` (row + MP4 preserved, non-publishable) |
| Open follow-on | F-YT-FAILED-NO-RETRY → **full Unit B COMPLETE v3.10** (v1.8.0+B1 v3.09; v1.9.0+B2 v3.10; live publisher now v1.9.0 — the table above shows the v3.08-milestone version). B3 9 = onboarding debt. 🔴 **NEW: F-YT-PUB-PUBLISH-AUDIT-GAP** (cross-posted YT publishes get no `m.post_publish` row; open follow-on — publisher attempt_no fix + backfill). |

---

## 🟢 cc-0016 friction-capture-evidence — STATUS BLOCK (v3.02 — Stage A/B/C/D closed + mobile-verified)

**Status: Stage A CLOSED + Stage C CLOSED + Stage B CLOSED/PASS + Stage D CLOSED/PASS. Evidence path COMPLETE through Stage D; mobile-verified. Stage E future/separately-approved-only.**

| Stage | Scope | Status | Reference |
|---|---|---|---|
| A | bucket + attachments column + 2 CHECK + index + view + GRANT | CLOSED | D-01 `6f2b8b1a`/`f573e684`/`9eb35144`; applied v2.97 |
| C | DROP+CREATE emit_event 13-arg + fn_emit_manual_event 8-arg; cc-0017b preserved | CLOSED | D-01 `56e65bb2`/`dbabb576`/`358c6fdd`; applied v2.98 |
| B | FAB evidence upload/read UX | CLOSED/PASS v2.99 | dashboard `36fe6ad`; V-A5 PASS |
| D | /operations evidence display | CLOSED/PASS v3.00 | dashboard `9082beb`; mobile-verified v3.02 (`d17b604`) |
| E | lifecycle cleanup automation + dry-run report | **dry-run design AUTHORED v3.16 (non-mutating)** — destructive run FUTURE/GATED | `docs/briefs/cc-0016/stage-e-dryrun-design.md` (gate G-E1/G-E2/G-E3 must be satisfied before any destructive run) |

**V-A5 evidence (DO NOT DELETE):** no-attach event `2120b2f7-219f-4d0d-be56-512d81430873`; attach event `75f0c981-1180-4047-9aa3-f725bec6eb9b`; object `friction-evidence/9e314151-be65-434e-8588-c913012f6591/0_va5-smoke.png`.

---

## 🟢 Dashboard slices — STATUS BLOCK (v3.04)

**Slices 1–3 + 4A–4B RECORDED. cc-0016 Stage B + Stage D CLOSED/PASS. Mobile viewport CLOSED/PASS. Slice 0A (Pre-Phase 0 IA shell + Visual Tokens v1) MERGED at `3ec489b`.** Slice 1 `af60953` / Slice 2 `de4501b`+`37008e5` / Slice 3 `991a92b` / Slice 4A `cd02402` / Slice 4B `f5a980f` / Stage B `36fe6ad` / Stage D `9082beb` / mobile fix `d17b604` / Slice 0A `3ec489b`. Remaining: cc-0015 UI (D1, gated on Gate 11); PRV (D2, deferred). Real Phase 0 future/gated.

---

## 🟢 Friction Register Consolidation Plan v1 — STATUS BLOCK (unchanged)

Wave 0 + 0d + 0e COMPLETE. Gates 10+12+13 CLOSED. Gate 11 (1-week observation 2026-05-19 → 2026-05-26) ACTIVE — ~2 days out. Wave 0f scoping rank 3 carry. cc-0016 Wave 8 Stage A/B/C/D closed; only Stage E (future/separately-approved) remains.

---

## 🟢 Cowork brief `nightly-health-check-v1` — STATUS BLOCK (v3.03 — v3.1.1)

**Brief version: v3.1.1.** Lifecycle gating WARN CLOSED v3.01; frontmatter `status: ready`. Q-005 Option A trail: A-005 `56e992b4` → v3.1 `7005865` → CCD finding → v3.1.1 `9ceb78a`. Next 16:00 UTC fire SAFE (true-stuck P1 emits with `condition_key=true_stuck`; 8 parked keys markdown-only). **Q-005 OPEN, non-blocking, next-fire watch.** Close fork (A) seed emission_rule + restore mapping + verify, OR (B) narrow scope + verify.

---

## 🟢 Process Upgrades — STATUS BLOCK (v3.05)

**L41**: re-exercised v3.02.1 + reinforced v3.02.2/v3.03/v3.04 + **reinforced v3.05** (session file new; sync_state + action_list patched read-HEAD-first with blob SHAs passed) + **reinforced v3.07** (cc-0017c small full-rewrite from fresh read; large briefs explicitly NOT chat-re-emitted; `00_` edits compact + sha-passed). Negative-then-positive exemplar.
**L62**: strongly reinforced via cc-0016 6-fire series; **v3.05: reinforced again** — both YT sql_destructive D-01s (`82443c8c`/`5f7bfc52`) returned partial/type-c echoes (corrected_action = the verification already planned), satisfied + proceeded on PK approval.
**L-v2.83-a**: 18+ occurrences. STRONG.
**L-v2.85-e**: PROMOTION-CONFIRMED v2.88; streak re-baseline pending.
**L-v2.88-a**: watcher; 3 cumulative.
**L-v2.94 / L-v3.02-a / L-v3.03-a / L-v3.04-a**: NEW candidates (watchers).
**L-v3.05-a**: NEW candidate (v3.05) — **record the root CAUSE, not just the symptom** (YouTube re-diagnosed as "reconnect token" ≥3× because the Testing-mode 7-day cause was never written into the durable record); sub-lesson: **re-run a stale audit against live data when the user reports the situation changed** (the 1 May YT audit was outdated; PK was right). 1 occurrence; watcher.
**L-v3.07-a**: NEW candidate (v3.07) — **the GitHub bridge has no str_replace, so large files (briefs / canonical lessons) belong to CC's surgical path, not chat full-file re-emit** (full re-emit of 49–107 KB docs is the L-v2.85-e truncation + L41 clobber hazard). Pairs with L41 + L-v3.06-a. Also reinforces no-fabrication: undocumented scope / numbering → defer with explicit note, never invent. 1 occurrence; watcher.

**Governance adoption — ADOPTED v3.35 (authority repair cycle):**
- **T1 authority-doc rule ADOPTED** — `docs/architecture/current-ice-decision-tree.md` (commit `160678b8`) is the single source of truth for the current (as-built) ICE content decision tree. Intended/historical design docs (`03_blueprint.md` / README / phases / concepts) are NOT authoritative for current-state decisions.
- **Docs-index authority section ADOPTED** — `docs/00_docs_index.md` carries an authority section routing readers to the T1 doc (switched in the same atomic commit `160678b8`).
- **Mandatory "Authority impact:" completion line ADOPTED** — every change touching the current ICE decision tree must state its authority impact; where it changes as-built behaviour, T1 must be updated in the same cycle.
- **CCD ownership for >80KB register/authority patches ADOPTED** — the large register/authority files (`00_sync_state.md`, `00_action_list.md`, T1) are CCD-routed surgical edits, not chat full-file re-emit (pairs with L41 / L-v3.07-a; this v3.35 cycle is an exemplar).
- **`last_verified` contract ADOPTED in T1** — the T1 authority doc carries a `last_verified` field that must be refreshed whenever the as-built tree is re-confirmed.
- **Rule 6b — STILL OUTSTANDING (deferred):** detection / Cowork-reconciliation wiring NOT built; no `05_risks` change. Only 6b remains outstanding.

**Governance — REAFFIRMED v3.35 cycle close (Option C decision arc):** Rules 1, 2, 3, 7, 6a remain ADOPTED — the T1 authority model, the docs-index authority section, the mandatory **`Authority impact:` completion line**, and CCD ownership for >80KB authority/register files are all in force (this Option C close cycle is itself an exemplar: CCD-routed surgical edits to the >80KB registers, every change carrying its `Authority impact:` line). **Rule 6b detection wiring remains the sole outstanding governance item.**

---

## 🔄 Standing session-start checks

S1–S29 unchanged. S30 closed PASS v2.47.

---

## 🔴 Time-bound

- **1-week empirical observation window** — 2026-05-19 → 2026-05-26. Wave 7 (cc-0015) gated. ~1 day out.
- **YT token durability spot-check (v3.05)** — verify NY/PP YT tokens still work past ~31 May (proves Production removed the 7-day cap). Not a hard calendar item; soft watch.
- **Q-nightly-health-check-v1-005 next-fire watch (v3.03)** — next natural 16:00 UTC fire under brief v3.1.1. Watch `failure_count`/`skipped_count`=0 on EMITTED array. Does NOT block the cycle.
- **ICE-PROC-002 +4-week process review (v3.13.3)** — due **2026-06-26** (the fourth Friday 09:00 Sydney pool session; Google Calendar event set on pk@invegent.com). Evaluate: are the 5 saved views useful; should Stage C/D/E proceed; should source filter / sort / FAB category-desc hydration be promoted. Process check only — not a cc-0015 completion gate. Soft calendar item.
- No new hard calendar items.

---

## 🛠 Meta-tooling — ChatGPT Review MCP

v3.05: **4 D-01 fires** — plan_review `a75c78f6` (partial) + `ff05c65e` (agree); sql_destructive `82443c8c` (PP+NY YT tests) + `5f7bfc52` (YT batch). T-MCP-02 cum **~96 (v3.05 +4)**, BUT ~+5 from the unrecorded pre-compaction IG arc → **treat cumulative as needing a clean re-baseline (~101–105 range)**. State-capture exceptions: 0 (cum 1). Close-the-loop UPDATEs: 0.

v3.06: **2 D-01 fires** — plan_review `abf6f63c` (partial/type-c) → `f89c23cf` (agree) on the F-YT-FAILED-NO-RETRY brief; both **non-mutating** (plan reviews). The prior partial was resolved by satisfying its corrected_action against live data (video_status confirmed `text`/no-CHECK; the 30 failed drafts live-bucketed 5/17/8) + patching Unit B to the 3-bucket recovery (`a3f329f3`), then re-firing clean — textbook L62 type-c resolution. T-MCP-02 cum **+2 (still pending the IG-arc re-baseline, ~103–107 range)**. State-capture exceptions: 0 (cum 1). Close-the-loop UPDATEs: 0.

v3.07: **0 D-01 fires** (repo doc-only; no production mutation, no review needed). T-MCP-02 cum unchanged. State-capture exceptions: 0 (cum 1). Close-the-loop UPDATEs: 0.

v3.08: **0 D-01 fires** (repo reconciliation of an already-live pipeline + post-milestone cc-0019 Unit B re-verification, both doc-only; no production mutation, no new deploy, no review needed). T-MCP-02 cum unchanged. State-capture exceptions: 0 (cum 1). Close-the-loop UPDATEs: 0.

v3.13: **0 D-01 fires** (cc-0015 Stage B frontend merge + production deploy gated on CCB preview visual PASS + PK directive — same no-new-D-01 pattern as the Stage F merge/deploy; the Stage B RPC's plan_review `58d89efe` + sql_destructive `17db8b27` were already fired/closed v3.12). T-MCP-02 cum unchanged. State-capture exceptions: 0 (cum 1). Close-the-loop UPDATEs: 0.

v3.13.1: **1 D-01 fire** — sql_destructive `bbe9a0fa-b872-4e2a-998f-743aa89036e0` on the cc-0015 Stage A.5 recategorisation UPDATE (verdict=agree, risk=low, confidence=high, escalate=false, no pushback — clean agree/proceed under PK exact phrase; no L46 classification needed). **Close-the-loop UPDATE on `m.chatgpt_review`: 1** (status=completed, resolved_by 'PK (approval phrase) + CCD (apply)', escalation_resolved_at set). T-MCP-02 cum **+1**. State-capture exceptions: 0 (cum 1).

v3.13.2: **0 D-01 fires** (cc-0015 Stage G doc authoring — documentation-only, no production mutation, no review needed). T-MCP-02 cum unchanged. State-capture exceptions: 0 (cum 1). Close-the-loop UPDATEs: 0.

v3.13.3: **0 D-01 fires** (cc-0015 Stage G ICE-PROC-002 V-G1 approval recorded — documentation-only, no production mutation, no review needed). T-MCP-02 cum unchanged. State-capture exceptions: 0 (cum 1). Close-the-loop UPDATEs: 0.

v3.27: **1 D-01 fire — `ef_deploy`, CCD-inline** (not routed through the ChatGPT Review MCP). PK approval phrase captured verbatim: `I approve ef_deploy for video-worker F-CREATOMATE-CAPTION-HIERARCHY from branch polish/video-worker-caption-hierarchy at commit fc3ec1279f1591c364bc101a14d9de496167b17e.` Treated as CCD-inline D-01 following the v3.25/v3.26 precedent. **No `m.chatgpt_review` row created or fabricated** (none existed; none invented). Close-the-loop UPDATEs: 0 (CCD-inline governance, not an MCP-routed review). T-MCP-02 cum unchanged. State-capture exceptions: 0 (cum 1).

v3.31: **0 new D-01 fires** (docs-only sync). **D-01 `0d9bb255`** (`ef_deploy`, heygen-worker v2.1.0) — PK exact-phrase approved + CCD-deployed (prior turn), then **independently verified + closed `completed` by CCH** (close-the-loop on `m.chatgpt_review` performed by CCH). No new review fired this sync. T-MCP-02 cum unchanged. State-capture exceptions: 0 (cum 1). Close-the-loop UPDATEs: 0 by CCD this sync (CCH owned the `0d9bb255` closure).

---

## 🤖 Cowork automation (D182)

**v3.05 update:** Cron 82/83/85/86 firing normally. **jobid 34 (youtube-publisher) active, draining the released YT backlog.** Cowork brief `nightly-health-check-v1` v3.1.1; frontmatter `status: ready`; Q-005 OPEN non-blocking next-fire watch.

---

## 🟡 Active

| ID | Item | Priority | Status | Owner | Next action |
|---|---|---|---|---|---|
| **F-OPTIONC-ENGAGEMENT-EVIDENCE-NULL (v3.42)** | `m.post_format_performance.avg_engagement_rate` effectively NULL despite post_count accumulating — blocks evidence-based Option C/format-mix decisions. **DIAGNOSED v3.42 (read-only):** three stacked defects. **D1** raw FB engagement acquisition failure: `post_engaged_users` Graph metric fails 526/526 rows (all clients; introduced v14.0.0); `engaged_users` NULL 526/526; reactions+comments+shares fallback 0 on 526/526 (failures coerced to 0, indistinguishable from genuine zero) → `engagement_rate` NULL on all FB rows even where reach exists (reach>0 on 322/526). **D2** dual-writer clobber: insights-worker `computeFormatPerformance` (per-client ~03:01–03:16, windows 30+90, all-platform) vs `refresh_post_format_performance()` cron 03:15 (windows 7/30/0, FB-only via `reach IS NOT NULL`) on the same PK — the refresh clobbers worker-produced 30-day values with NULL FB-only averages; **the Advisor reads the damaged window-30**. **D3** YouTube structurally excluded: YT rows have engagement on 20/62 but reach NULL 62/62 → refresh filter drops all; YT insights stale 05-29 / manual-only (existing carry). LI+IG have zero insight rows. Advisor receives NULL coerced to 0 → fabricated "0.0% avg engagement" injected per job (misleading, not absent). | **P2 — OPEN / diagnosed (v3.42)** | Fix sequence (each gated separately, NOT approved yet): (1) FB engagement repair in insights-worker — verify valid Graph v19 engagement metric, preserve raw failure visibility vs coercing to 0 (ef_deploy); (2) aggregation ownership — resolve dual-writer race, stop the 03:15 refresh clobber (cheapest: retire redundant cron, worker owns table, keep window-7 coverage); (3) YT reach/views mapping + YT cron carry; (4) Advisor bad-evidence shielding — don't render NULL as "0.0%". Option C stays PARKED until the feed is repaired. Session record `docs/runtime/sessions/2026-06-12-v3.42-f-optionc-engagement-evidence-null-close.md`. **STAGE R COMPLETE (v3.42-R, 2026-06-12) — Fix 1 brief AUTHORED + AMENDED (`docs/briefs/f-optionc-engagement-evidence-null-fix1-fb-engagement-acquisition.md`):** root cause SPLIT — Leg A: `post_engaged_users` AND `post_impressions` invalid (#100) under the actually served Graph version (requests sent v19.0, **Meta served v24.0** — v19 sunset, silent auto-upgrade); Leg B: reactions/comments/shares fields fallback **permission-blocked** (#10 `pages_read_engagement` / Page Public Content Access). **`post_reactions_by_type_total` verified HTTP 200** — viable reduced-basis reactions source without `pages_read_engagement` (200+empty value ≠ 400+error: zero-vs-failure cleanly distinguishable). Fix 1 remains an insights-worker code repair (v14.2.0 shape): replace dead direct metrics; handle/remove dead `post_impressions`; pin Graph version explicitly; compute engagement only from trustworthy 200-response inputs; store failed reads as NULL not 0; preserve raw failure visibility in `raw_payload`; redact/drop `paging` blocks before persistence/logging. **NOT IMPLEMENTED, NOT DEPLOYED** — remaining gates: CCD patch → D-01 → PK exact phrase → CLI deploy → V-checks. **FIX 1 DEPLOYED + CLOSED v3.43 (PASS WITH CARRY):** insights-worker v14.2.0 (merge `335ab121`, deploy record 66; D-01 `0fbbea62`; close-out `docs/runtime/sessions/2026-06-12-fix1-v14-2-0-deploy-closeout.md` `808ddd2a`). **D1 leg RESOLVED** — V-checks: 79 fresh rows; 79/79 `raw_payload.calls`; 79/79 `engagement_basis_source=reactions_by_type_total`; 316/316 per-call telemetry complete; 0 paging/token/access_token leakage; comments/shares NULL on failed `fields_rcs` (NOT coerced to 0); `engagement_rate` populated 48/48 where reach>0; old-shape contrast = 447 pre-deploy rows with calls=0/basis=0/rate=0 (drift confirmed by shape, not VERSION self-report → F-INSIGHTS-DEPLOY-VERSION-DRIFT). **D2 (dual-writer clobber) → Fix 2 = next load-bearing evidence-chain workstream (carry F-INSIGHTS-FIX2-AGGREGATION-OWNERSHIP); D3 (YT exclusion) + Advisor bad-evidence shielding remain OPEN.** Parent carry STAYS **P2 OPEN** until D2/D3 + shielding land; comments/shares coverage tracked by META-FIX1-PAGES-READ-ENGAGEMENT (non-blocking, auto-heals). | CCH diagnosis + Stage R complete; **Fix 1 deployed v3.43; Fix 2 (D2 aggregation ownership) is the next workstream** | — |
| **META-FIX1-PAGES-READ-ENGAGEMENT (NEW v3.42-R)** | Meta-side carry split out of Fix 1 brief §3a: verify/obtain `pages_read_engagement` (or Page Public Content Access) for the page tokens to regain **comments + shares** coverage later — App Review territory, aligns with the existing Meta App Review carry. **Does NOT block Fix 1** (Fix 1 proceeds on the reduced reactions[+clicks]/reach basis). **Do not mix with Option C or Advisor changes.** **v3.43:** Fix 1 DEPLOYED on the reduced reactions[+clicks]/reach basis; comments/shares still **#10-blocked** (`pages_read_engagement` / app-level Page Public Content Access) — re-confirmed on the rotated CFW token; **auto-heals with zero deploys when permission lands** (the per-row `fields_rcs` call record is the live permission-flip signal). | **P3 — OPEN (Meta-side carry)** | Stage R Leg B finding (#10 permission block, verified on the CFW token; same app across clients → high-confidence global). | PK / Meta App Review lane | Later, after Fix 1; own gate. |
| **SEC-CFW-TOKEN-ROTATION (NEW v3.42-R)** | PK-side security follow-up from the Stage R `paging` incident: **precautionary CFW page-token rotation recommended/approved** (a Stage R pg_net Graph response echoed a CFW page-token fragment via Meta `paging` URLs; response row purged same-day — see L-StageR-paging). **No token values are to be exposed in docs or chat.** | **P2 — PK follow-up (security)** | Incident contained: targeted purge of pg_net request `159957` via the audited migration path (`security_purge_pgnet_stage_r_159957`, PK-approved); rotation actioned as a precaution per Fix 1 brief §2.5. | PK | Confirm rotation complete; then close. |
| **F-INSIGHTS-FIX2-AGGREGATION-OWNERSHIP (NEW v3.43)** | **Fix 2 — the next load-bearing evidence-chain workstream.** D2 of F-OPTIONC: `m.post_format_performance` has two writers on the same PK — insights-worker `computeFormatPerformance` (per-client ~03:01–03:16, windows 30+90, all-platform, captures real values) and `refresh_post_format_performance()` cron 03:15 (windows 7/30/0, FB-only via `reach IS NOT NULL`); the 03:15 refresh **clobbers** worker-produced window-30 values with NULL FB-only averages — exactly the window the Advisor reads. Now that Fix 1 populates FB `engagement_rate`, both writers begin averaging real values, but the clobber still nulls the Advisor's window-30 nightly. **Cheapest path (recorded, NOT approved): retire the redundant 03:15 refresh cron, let the platform-inclusive worker own the table, keep window-7 coverage.** | **P2 — OPEN (next workstream; NOT started)** | Diagnosed in F-OPTIONC v3.42 (D2); unblocked-but-unsolved by Fix 1 v3.43. Own brief + D-01 + PK phrase required. **Do not start in this reconciliation.** | CCH brief → CCD (gated) | Author Fix 2 brief; decide writer ownership; gate as cron/EF change. |
| **F-INSIGHTS-DEPLOY-VERSION-DRIFT (NEW v3.43)** | Fix 1 V-check surfaced that deployed-vs-repo state cannot be trusted from the EF `VERSION` self-report — the proof that v14.2.0 was actually live was the **row-shape contrast** (79 post-deploy rows with per-call `calls[]`/`reactions_by_type_total` basis vs 447 pre-deploy rows with calls=0/basis=0/rate=0), not a version string. | **P3 — OPEN (follow-up)** | Confirmed by shape contrast during Fix 1 close-out (`808ddd2a`). Pairs with L-v3.06-a (deployed-vs-repo drift risk class). | chat → next infra lane | Drift-check by deployed **bundle hash**, not VERSION self-report. |
| **F-SLOT-SCHEDULE-FIDELITY (NEW v3.43)** | Slot `scheduled_publish_at` vs publish-queue `scheduled_for` can drift; the publisher does not reliably gate on `scheduled_for` (observed during T0/T1 verification — future-scheduled rows are not held `queued`). **Pre-existing / systemic — NOT introduced by Content Studio T0.** | **P4 — WATCH (post-Fix-1 verification window; downgraded v3.48 — NOT fully closed)** | **Fix 1 APPLIED & VERIFIED (v3.47):** `m.release_queue_on_asset_ready()` no longer collapses `queue.scheduled_for` to NOW() on asset-ready — now `GREATEST(COALESCE(post_draft.scheduled_for, slot.scheduled_publish_at), NOW())` (existing queue.scheduled_for NOT used — may be the artificial +4h hold); future intended stays future, past/no-intent releases now. Migration `fslot_schedule_fidelity_fix1_release_preserve_intent`; md5 `0fa656e1`→`0cdede8d`; D-01 `dc6d3bde-051f-4ea3-9fca-9f09217b1033` (partial/escalate=true — PK approved Option A, applied despite escalation); trigger `trg_release_queue_on_asset_ready` bound; `gate_queue_on_asset_status`/`publisher_lock_queue_v1`/`v2` unchanged; verified (future stays future / past releases now / 0-row rollback leak / early-publish closed). See the v3.47 STATUS BLOCK + `docs/runtime/sessions/2026-06-14-fslot-schedule-fidelity-fix1-closeout.md`. Observed originally during T0 close-out (`38b0d0a3`) + T1 live verification. **Carry stays OPEN.** | chat → PK (watch) | **v3.48 discovery:** rule-slot early ~23.5h bucket = OLD NOW()-collapse, **resolved by Fix 1** (rule-slot semantics explained unless watch disproves); multi-day late tail = historical Apr/May YouTube OAuth outage + backlog drain (no June-origin recurrence); no capacity issue (12 queued / 0 needs_review / 1 >3d). **Watch criteria:** early_1h_1d → within_1h; no new June-origin >3d tail; median abs delay → publisher-cron cadence. **Carried (not active bugs):** portal-path drift = **dormant / fix before Phase 3 client-portal launch** (`portal_approve_draft` zero prod usage ever); `fan_out_episode` past-date clamp = separate design question. |
| **F-INTENT-STATUS-ROLLUP (NEW v3.43)** | `m.creative_intent.status` is **creation-time only** (active/partial/failed by accepted-vs-rejected targets at fan-out); it does NOT recompute when children later die/skip/publish. The T1 Visibility UI **derives the true status on display** from child outcomes (`get_creative_intent_detail` / `list_creative_intents` counts) and labels the divergence — so operators see the truth without SQL. Durable backend rollup (trigger/cron or read-time aggregate in the detail RPC) **deferred**. | **P3 — OPEN (deferred; UI-mitigated)** | Surfaced by Content Studio T1 + T1 Visibility close-outs (`08ecb4e` / `718b252`); both live verification intents read stored `active` over dead children, derived `failed`. | chat → PK | Durable rollup fix is a separate gated lane (not ai-worker). |
| **F-PUBLISH-QUEUE-DANGLING-QUEUEID (NEW v3.43)** | Lineage-hygiene note: post-publish queue-row deletion can leave a dangling `queue_id` reference. Observed during T0 close-out; cosmetic/lineage only. **Not T0/T1-blocking.** | **P4 — OPEN (lineage hygiene)** | Observed during T0 close-out (`38b0d0a3`). | chat → next hygiene lane | Tidy queue-row lifecycle / dangling reference; low priority. |
| **Monitor dashboard visibility — Slices 4–6 (NEW v3.38)** | Three read-only Monitor panels on `dashboard.invegent.com` (`invegent-dashboard`), below Slices 1–3: **Render + Avatar Health** (`m.post_render_log`: provider/format counts, success/fail rate, error categories, retry/attempt, `credits_used` null-rate, avatar render count + avatar_identity populated/missing + stakeholder_role coverage + `avatar_selected_by`), **Performance Freshness** (client×platform: published, perf rows, last `collected_at`, hours-since, stale >48h + missing flags), **Pipeline Exceptions** (stuck/dead AI jobs, failed slots, slot skips by category, compliance-skip vs `dead_reason`, publish-queue states + orphans, approved-unpublished >3d). Read path = existing `exec_sql` service-role server actions, SELECT-only. | **DONE 2026-06-12 — LIVE on production** | n/a (recorded) | dashboard PR #7 MERGED (`417210c`); Vercel prod `dpl_A9dr9scRF1u2346FbaEoQsaR9UJ4` READY (alias `dashboard.invegent.com`) — LIVE (verified by SHA + alias). Branch `monitor/render-perf-exceptions-panels` @ `1e26bc2` (5 files); tsc+build exit 0; screenshot-verified on local preview. **No DB / no classifier / no Advisor / no policy / no Option C / no render-logic / no publisher / no compliance / no cron / no worker / no provider call.** Open follow-up: `/overview` `created_at` SQL error (pre-existing, unrelated). Authority impact: none. |
| **F-FORMAT-MIX-NOFORMAT (v3.38-recon)** | Live Format Mix panel "No Format" bucket ≈15.6% overall / CFW ≈50.5%. **DIAGNOSED v3.39 (read-only):** NOT a pipeline format failure — all 51 No-Format rows in the 30d window are intentional relevance/compliance skips (`approval_status='dead'`, `draft_format.compliance_skip=true`, reason populated 51/51, `recommended_format` unset because the draft is killed before final formatting; CFW 47 + Invegent 4; NY/PP zero; 0 downstream publish/queue rows). Dashboard query accurately reports table state; the panel has a **semantics gap** (Format Mix counts dead/compliance-skipped drafts — no `approval_status` filter). | **P3 — CLOSED v3.39 (diagnosed — subsumed by existing carries)** | Subsumed under the CFW intentional-skip observability/dashboard-semantics carry (P3); volume = CFW feed-scope review (P3); cost = F-CFW-COMPLIANCE-PREGEN (P3). NOT a new architecture/Advisor defect. Dashboard tweak NOT implemented (future, gated). Session record `docs/runtime/sessions/2026-06-12-v3.39-f-format-mix-noformat-close.md`. | n/a (closed) | — |
| **F-ADVISOR-LI-OVERRIDE (v3.38-recon)** | Live Advisor Override panel showed LinkedIn override ≈71.9% vs ~35% window-wide. **DIAGNOSED v3.40 (read-only):** expected Advisor correction behaviour under current config, NOT FB-format inheritance / NOT a policy violation / NOT publish-blocking. Cause stack: `preferred_format_linkedin` NULL for all clients → `fill_pending_slots` falls back to `image_quote` on 100% of LI slots → LI Advisor prompt gets no preference/bias instruction (FB does, via `preferred_format_facebook='image_quote'`) → Advisor correctly picks content-fit formats (`image_quote→text` 42/46, `→carousel` 4/46) for reactive/conversational LI content → `text` is LI-legal so corrections are valid. IG sits at 11.3% on the same slot default only because `text` is excluded from the IG palette. **46/46 LI override rows carried LinkedIn `m.post_publish` rows — published.** DB slot functions verified clean (correct per-platform column reads). | **P3 — CLOSED v3.40 (diagnosed — expected behaviour under current config; evidence → Option C/A2 review gate)** | LinkedIn format steering = a format-policy decision for the Option C/A2 review gate (w/c 2026-06-15); if ever wanted, set `preferred_format_linkedin` AND fix F-AIW-PREF-COL-HARDCODE first. Optional panel relabel under the dashboard-semantics carry. Session record `docs/runtime/sessions/2026-06-12-v3.40-f-advisor-li-override-close.md`. | n/a (closed) | — |
| **F-DASH-OVERVIEW-CREATED-AT (NEW v3.38-recon)** | Pre-existing `/overview` `created_at` SQL error, repeatedly observed in dev logs during Slices 1–6 preview/deploy work (v3.37 + v3.38 banners). Unrelated to the Monitor panels. | **P3 — OPEN** | Promoted from banner-only "Open follow-up" to a register carry (2026-06-12 reconciliation). | chat → PK | Reproduce on `/overview`; identify the failing query/column; scope a one-line fix in `invegent-dashboard`. |
| **Monitor dashboard visibility — Slices 1–3 (NEW v3.37)** | Three read-only Monitor panels on `dashboard.invegent.com` (`invegent-dashboard`): **Classifier v2 Health** (version split, class/method distribution, defaulted-analytical headline, top firing rules, body/insufficient rates), **Advisor Override** (override rate + by client/platform/week + slot→final matrix, 30d), **Format Mix** (final format usage overall/platform/client, complete `FORMAT_LABELS`, 30d). Read path = existing `exec_sql` service-role server actions, SELECT-only. | **DONE 2026-06-12 — LIVE on production** | n/a (recorded) | PR #6 MERGED (`c168d2c`); Vercel prod `dpl_7we82KUmm47j8jSEC4UsbSAa6tnc` READY (alias `dashboard.invegent.com`); panels visually confirmed live by PK. Branch `monitor/classifier-advisor-format-panels` @ `a2d4c82` (7 files). **No DB / no classifier / no Advisor / no policy / no Option C / no render-publisher change.** Open follow-up: `/overview` `created_at` SQL error (pre-existing, unrelated). Authority impact: none. |
| **ICE Classifier v2 repair (NEW v3.36)** | Stage-1 bounded repair of the rule-based PL/pgSQL classifier (`m.classify_canonical`/`m.evaluate_rule`/`m.classify_canonicals_unclassified`) per the 2026-06-12 concentration audit (`ef53676`): 3 dead source rules fixed, 6 regex/FP repairs, recency→`created_at`, +9 explanation-telemetry columns on `f.canonical_content_body` (outward label unchanged), `v1`→`v2`, NULL-only picker, opt-in `m.reclassify_canonicals`. | **DONE 2026-06-12 — APPLIED to prod + validated** | n/a (recorded) | Migration `classifier_v2_repair_and_telemetry` (prod `20260611223123`) + 2 hotfixes (`…223306`/`…223401`); smoke A/B/C/D PASSED live; 100-row bounded backfill done (70 analytical → 8 rule_matched / 62 defaulted). Branch `repair/classifier-v2-telemetry` @ `48ea045`, **PR #7 MERGED** (`2be28040`; repo↔prod reconciled; merge ≠ deploy). Forward cron job 68 classifies new items v2. Carries below. Authority impact: none. |
| **Classifier v2 — remaining v1 backfill (NEW v3.36)** | ~4,371 existing `classifier_version='v1'` rows keep old labels + NULL telemetry; backfill is the explicit, bounded, opt-in `m.reclassify_canonicals(batch, max)`. | **P3 — OPEN (opt-in, gated)** | INTENTIONALLY untouched (100 done under approval; no broad backfill without approval). | chat → PK | Run further bounded `m.reclassify_canonicals` batches only when PK directs. |
| **Classifier `first_seen_at` drift (NEW v3.36)** | `f.canonical_content_item.first_seen_at` advances like last_seen (93.9% drift); bypassed for classification (now uses immutable `created_at`), but the underlying data-quality bug is untouched. Audit carry. | **P3 — OPEN (data quality)** | Mitigated for classifier recency; root cause not investigated. | chat → PK | Investigate the writer that advances `first_seen_at`; out of classifier-repair scope. |
| **Classifier repo↔prod migration-version drift (NEW v3.36)** | Repo file `20260612093000_classifier_v2_repair_and_telemetry.sql` vs prod migration versions `20260611223123` (+2 hotfix). Same content, drifting version slots (cc-0020-class pattern). | **P3 — reconcile on merge** | Repo file is the consolidated correct state. | CCD → PK | PR #7 merged (`2be28040`) — version-slot reconciliation now actionable (tombstone/backfill per the cc-0020 precedent). |
| **Doc-patch debt (v3.07)** | cc-0017c main brief v1.2; cc-0017a/0010A/0011/0012 + cc-0017c/vchecks.md; decisions.md; lessons promotion | P2/P3 | **PARTIAL** — cc-0017c main brief DONE (`6d5c3ae6`→`81dcb683`); rest DEFERRED to CC (scope + re-emit risk); decisions.md unlocated; lessons not promoted | chat → CC | CC surgical pass for cc-0017a/0010A/0011/0012 + vchecks.md; locate decisions.md; lessons at next-lesson-cycle. |
| **YouTube publishing restore** | OAuth app moved Testing→Production (PK) + tokens re-exchanged; PP `ma6EG1fz4XQ` + NY `qp-ZGm8lNIo` published; backlog released (13 generated, draining) + soft-retired (14 archived_stale). Root cause F-YT-OAUTH-TESTING-MODE (7-day Testing-mode token cap). | RESTORED v3.05 | **RESTORED/VERIFIED** | n/a (recorded) | Watch drain + token durability past ~31 May. |
| **Avatar→YouTube Shorts pipeline** | HeyGen talking-avatar → YouTube Shorts end-to-end (ai-worker v2.13.0 A2/A3 + heygen-worker v2.0.0 async + youtube-publisher v1.7.0 allow-list) | milestone | **LIVE + REPO-RECONCILED v3.08** — first unlisted Short `sfQvSM2Osus` (NDIS-Yarns, draft `40f9fa25`, `m.post_publish` `5dbfcc0f`, 720×1280, 2026-05-25 11:42:07 UTC). EFs already deployed; source reconciled to `main` (EF parity `521b053b`+`194e0792`; continuity docs `04294d23`; this `00_` rebuild). 0 production mutation / 0 new deploy / 0 D-01. | n/a (recorded) | Watch async renders land in `m.post_publish`; F-YT-FAILED-NO-RETRY → v1.8.0. |
| **cc-0019 publish-eligibility-gate** | `m.is_publish_eligible` + fill-gate (Unit A) + ai-worker preflight (Unit B). Brief `81cb414`→`01a6cdf`; plan_review `ff05c65e` clean; migration `20260524091020` (backfilled `8738c3a0`). | P2 | **Unit A IMPLEMENTED (v3.06 correction); Unit B re-verified absent against ai-worker v2.13.0 (`8204a5c7`) — STILL OPEN / optional (v3.08)** | PK → chat | Optional: implement Unit B (ai-worker preflight) via ef_deploy D-01 + PK approval; else accept Unit-A-only (fill-gate already stops the main waste vector). Re-verify carry against v2.13.0 DISCHARGED v3.08. |
| **F-YT-FAILED-NO-RETRY** | youtube-publisher catch handler sets transient auth failure to terminal `video_status='failed'` (never re-selected) → token outage silently freezes rendered backlog. **Still present in the now-reconciled v1.7.0** (v1.7.0 only added the avatar allow-list, not retry). | **P2 — OPEN** | **BRIEF AUTHORED + plan_review CLEAN v3.06**; **re-pointed v3.08 → youtube-publisher v1.8.0** (v1.7.0 was consumed by the avatar allow-list, not the retry fix) (`docs/briefs/yt-publisher-failed-no-retry.md`; `bee872aa` → Unit-B 3-bucket patch `a3f329f3`; plan_review `abf6f63c` partial/type-c → `f89c23cf` agree — prior partial resolved by the patched brief) | chat → PK | ef_deploy **youtube-publisher v1.8.0** + sql_destructive (Unit B 3-bucket recovery: 5 quota recover / 17 token-casualty recover-behind-no-upload-guard / 8 never-connected excluded) + PK approval at execution; A-then-B. *(Brief text still names v1.7.0 internally — fix to v1.8.0 in a CC/follow-up pass.)* |
| **F-YT-EXPIRY-DISPLAY-FAKE** | dashboard `app/api/youtube/callback/route.ts` hardcodes `token_expires_at=now()+5y` → fabricated "Valid 2031" masks dead tokens | **P3 candidate cleanup** | LOGGED v3.05 | chat → PK | Record real expiry or omit; stop masking. |
| **CFW + Invegent YouTube onboarding** | Never connected (no `c.client_channel` YT row, no refresh token); CFW failed drafts `approval_status='published'` | **P2/P3 carry** | OPEN | PK → chat | First-time OAuth connect (not a reconnect). |
| **IG-publish-restore arc 4-way close** | Pre-compaction 2026-05-24: instagram-publisher v2.4.0 deploy; IG queue soft-purge; void+repurge migrations; cron 53 enable. | **P2 carry (sync hygiene)** | **CLOSED v3.06** — publisher synced to repo (`bc78511e`) + 7 migration files backfilled (`8738c3a0`); recording debt discharged. Residual = operational only. | PK | PK decides IG resume (re-enable enqueue cron 64, start CFW/Invegent); lower-risk now M12 root cause closed. |
| **cc-0017a v1.2 doc patch** | apply-record backfill (APPLIED v2.81; mig `20260518065610`; D-01 `adcc8385-…`) | **P3 carry → CC** | DEFERRED v3.07 — no documented scope + 72 KB full-file re-emit risk | CC | Surgical str_replace on laptop; confirm scope first. |
| **cc-0010A / cc-0011 / cc-0012 doc patches** | "minor doc patches" — no documented specifics | **P3 carry → CC** | DEFERRED v3.07 — zero scope + 107/61/49 KB re-emit risk | CC | Surgical str_replace; PK to specify scope. |
| **cc-0017c/vchecks.md V-B4 doc patch** | sub-file `docs/briefs/cc-0017c/vchecks.md` — correct V-B4 to 12-param signature | **P3 carry → CC** | DEFERRED v3.07 — path LOCATED + fix grounded; sub-file re-emit → CC | CC | Surgical str_replace with 12-param signature. |
| **decisions.md (runtime) location** | locate the runtime decisions log (cc-0014 framing leg) | **P3 carry** | UNLOCATED v3.07 — not in root/`docs/`/`docs/runtime/`; cc-0014 sync_state leg already moot | PK → chat | PK to point at path, or CC to locate. |
| **Lessons promotion** | L41+L-v2.85-e (pair), L-v2.83-a, L-v2.85-a | **P3 carry → next-lesson-cycle/CC** | NOT PROMOTED v3.07 — canonical L37–L65 home + numbering convention not locatable; do NOT promote by assumption | next-lesson-cycle / CC | Promote once canonical home + numbering confirmed. |
| **Q-nightly-health-check-v1-005** | Brief v3.1.1: P1 true-stuck emits `condition_key=true_stuck`; 8 keys PARKED pending Supabase-approved `friction.emission_rule` seed. A-005 `56e992b4` / v3.1 `7005865` / v3.1.1 `9ceb78a`. | **P3 non-blocking, next-fire watch** | **OPEN** | chat → PK | Watch next fire; close via fork (A) seed+restore+verify or (B) narrow scope+verify. Do NOT seed/re-emit/close without PK. |
| **emission_rule seed patch (P1/P2 condition keys)** | Supabase-approved patch to seed enabled `friction.emission_rule` rows for the 8 parked keys; then restore full §12.2a mapping | **P2/P3 carry (Q-005 close fork A)** | NOT STARTED. Supabase mutation — D-01 + PK approval. | PK → chat | When PK directs (fork A). Alt: fork B. |
| **Dashboard Slice 0A** | Merged `3ec489b` (Pre-Phase 0 IA shell + Visual Tokens v1; NOT real Phase 0) | RECORDED v3.04 | **MERGED/RECORDED** | n/a (recorded) | Real Phase 0 (review §9) future/gated. |
| **Mobile/narrow viewport verification** | CCB 306×498 DPR2 → MOBILE PASS | RECORDED v3.02 | **CLOSED/PASS** | n/a | n/a |
| **Cowork brief lifecycle gating WARN** | v2.94 convention validated vs 2026-05-20T160237Z fire | CLOSED v3.01 | **CLOSED** | n/a | Brief reset to `ready`. |
| **cc-0016 Stage D — /operations evidence display** | dashboard `9082beb`; mobile-verified | RECORDED v3.00 | **CLOSED/PASS** | n/a | n/a |
| **cc-0016 Stage E — lifecycle cleanup** | Cleanup automation + dry-run report | FUTURE — separately approved only | NOT STARTED. CONSTRAINT 2 binds. | PK → chat | Separate approval + dry-run before any destructive run. |
| **cc-0015 register hygiene + Stage D Phase 1** (Wave 7) | Register reconciled **48 total / 14 open / 34 resolved / 0 new** (Batches 1/2a/2b/3/4/6 via `friction.*` SECURITY DEFINER RPCs, `p_actor='pk'`, one-at-a-time/verified, CCD-inline D-01 + session-doc governance, no `m.chatgpt_review` fabricated); Stage D Phase 1 `/operations/pools` status strip + collapsed risk indicators | P2 → **COMPLETE** | **DONE 2026-06-02** — UI merged to dashboard `main` `668b6ae` + prod deploy + CCB preview & prod sanity PASS; 12 protected IDs untouched; brief `9a5dc155` superseded by execution; session `docs/runtime/sessions/2026-06-02-cc-0015-register-hygiene.md` | n/a (recorded) | — |
| **cc-0015 carry-forwards** | Stage D **Phase 2** `/operations` status strip; optional cosmetic label-truncation cleanup; **YT straggler carry case `79326fc8` — ✅ RESOLVED `tracked_done` (2026-06-10)** — archive_stale cleanup APPLIED under sql_destructive D-01 `482f7ff3` (PK exact-phrase approved + applied; closed-loop `completed`): 20 legacy YouTube `m.post_publish_queue` queued rows archived stale (`status=dead`, `dead_reason=archive_stale_legacy_queue_79326fc8_2026-06-10`) → **queued 20→0, YT dead 21→41**, 21 prior-dead siblings untouched; friction case resolved `tracked_done`; **no publisher/requeue/YouTube-API/provider call, no draft mutation**; **P3** — draft `0e486d12` (NY `video_short_stat_voice`, post_draft `a44288f7`) re-render only if still wanted; overdue tracked-cases review sweep; cc-0015 Stage C/E only if still separately gated by the cc-0015 brief | **P2/P3 carry** | OPEN — carry only (`79326fc8` sub-item ✅ DISCHARGED 2026-06-10; remaining sub-items carry) | chat → PK/CCD | When PK directs; `79326fc8` straggler decision DISCHARGED 2026-06-10 (see description). |
| **3 no-fire scheduler days — `nightly-health-check-v1`** | 2026-05-16, 2026-05-18, 2026-05-19 | **P3 secondary follow-up** | OPEN. | chat → PK | Read-only probe. |
| **Wave 0f scoping** | Brief-authoring only; opportunistic during Gate 11 | **P3, core rank 3** | NOT STARTED. | chat → PK | When PK directs. |
| **Platform Reconciliation View (PRV) read-only MVP** | `/recon` reads cc-0012 `op.*` via direct-PG `prv_readonly` | **P2, core rank 4; feature-branch** | **COMMITTED + PUSHED v3.14** — `7287a1e` on `origin/feature/platform-reconciliation-view-readonly` (unmerged, undeployed); live render + CCB visual PASS; mig `20260527011420` + D-01 `d86b7db8` + close-loop done. | PK → CCD | PR/deploy gated (`PK APPROVES PRV READ-ONLY PRODUCTION DEPLOY`); next: `op.v_per_client_rollup` + `op.v_per_platform_rollup`. |
| **5-row close-the-loop batch / Pre-sales / `purge_test_case` helper extension** | Close-the-loop UPDATEs + Pre-sales 3-clock + helper coverage gap | **P2/P3 carry, core rank 5** | OPEN. | chat → PK | When PK directs. |
| **Music library activation** | video-worker v3.0.0 env-var gated | P2 carry | PENDING PK. | PK + chat | Bucket + tracks + env + smoke. |
| **Asset Policy Stage 0 — OBS shadow observer** | Out-of-band shadow observer; isolation = Path B physical (separate OBS Supabase project `invegent-obs-stage0`, ap-northeast-2). Planning artefacts `docs/build-specs/asset-policy-stage0/` (commit `cde9cc24`). | **0A read-path APPLIED 2026-06-09 → Option C containment APPLIED 2026-06-10 (`obs_readonly` NOLOGIN); governance decision PENDING** | **Stage 0 FOUNDATION APPLIED** to the isolated OBS project ONLY (migration `obs_stage0_observation_foundation`; D-01 `fd3e519a` PK-approved + closed `completed`; CCD applied after pre-apply isolation check). OBS now holds: schema `obs`; enums `evidence_class`, `stage`, `population`, and `eligibility`; append-only `obs.observation`; per-cell `evidence_class` and `stage` enforced via `obs.value_cells_valid()`; idempotency key `(post_draft_id, observer_version, stage)`; provenance fields; 0A/0B class/stage constraints; append-only mutation guards; and indexes including pkey, idempotency unique, observed-at, `(stage, population)`, and GIN on `value_cells`. **Production `mbkmaxqhsohbtwsqolns` untouched** (re-checked: no `obs` schema, no `obs.observation`). OBS still physically isolated. **0A observer/read-path NOT started — BLOCKED.** Stage 0 read-only diagnostic complete (06 doc, commit f5c00e04) → recommendation C: HOLD 0A; residual gaps are source-instrumentation, not observer-read. 0A remains BLOCKED. **v3.28 RECONCILIATION + CONTAINMENT (2026-06-10):** 0A read-path applied 2026-06-09 (G-RP `20260609081127_obs_readonly_production_read_path_g_rp` D-01 `5fed9a06` + G-RP-RLS `20260609103639_obs_readonly_rls_slot_origin_select_g_rp_rls` D-01 `9b03b489`) — `obs_readonly` + 45 column-level SELECT grants on 4 `m.*` tables + slot-origin RLS policy on `m.post_draft`. Reconciliation found `obs_readonly` was not effectively read-only (reachable `m`/`public` SECURITY DEFINER write-capable RPC surface). **Option C containment applied 2026-06-10** — `ALTER ROLE obs_readonly NOLOGIN;` (migration `20260609221624`, D-01 `18ba635d`; verified `rolcanlogin` true→false, `NOBYPASSRLS` preserved, 45 grants + RLS policy + prior migrations preserved, zero active connections). `obs_readonly` now NOLOGIN; observer/scheduler parked; 0B not started; HeyGen untouched; **governance decision pending — OBS-0A rows `51752332` / `5fed9a06` / `c5a7cb3c` / `9b03b489` remain open/escalated, NOT closed, NOT ratified, NOT rolled back.** | PK → chat (ratify vs rollback) → CCD | **Next OBS work GATED on the v3.28 governance decision (ratify vs rollback).** Read-path was applied 2026-06-09 then contained 2026-06-10 (Option C, `obs_readonly` NOLOGIN); no observer / CDC / scheduler / runtime / 0B; HeyGen untouched. Ref `docs/build-specs/asset-policy-stage0/00_README.md`. |
| **OBS Stage 0A final governance decision (v3.28)** | Decide **ratify vs rollback** Option C containment; if ratify, re-engineer the read-only privilege model (close the `m`/`public` SECURITY DEFINER write-capable RPC surface); reconcile/close OBS-0A rows `51752332`, `5fed9a06`, `c5a7cb3c`, `9b03b489` **only after PK decision**. | **P2 carry — OPEN** | Containment applied + verified (`obs_readonly` NOLOGIN; migration `20260609221624`; D-01 `18ba635d`); the four OBS-0A rows deliberately left open/escalated; observer/scheduler parked; 0B not started; HeyGen untouched. NOT ratified, NOT rolled back, role NOT dropped. | PK → CCD | PK decides ratify vs rollback; reconcile the four OBS-0A rows only after the decision. |
| **OBS-0A review-MCP note (v3.28)** | D-01 `18ba635d` completed and attributed after exact-phrase approval; original four OBS-0A rows `51752332` / `5fed9a06` / `c5a7cb3c` / `9b03b489` remain open/escalated deliberately. | **P3 informational** | Recorded — no OBS-0A row closure; no ratify; no rollback. | n/a (recorded) | — |
| **HeyGen render telemetry — heygen-worker v2.1.0** | EF telemetry: mirror terminal HeyGen render outcomes into m.post_render_log via engine-agnostic public.write_render_log (commit `690a295b`). Code-only; no schema change; best-effort/idempotent/non-fatal. | **✅ DEPLOYED v2.1.0 (2026-06-10); D-01 `0d9bb255` completed; first-row evidence pending** | **DEPLOYED v2.1.0** (commit `690a295b`) via the safe-deploy CLI wrapper (`--allow-warn`; never MCP) under PK-approved **D-01 `0d9bb255`** (ef_deploy) — **deploy independently verified + closed `completed` by CCH**. Post-deploy PASS: health=`heygen-worker-v2.1.0` HTTP 200; deployed_hash_normalised==repo==`74bdbada…` (A-LE/clean, `security_definer_regression_risk=false`); `write_render_log` 13-param signature matches; baseline `m.post_render_log` 0 heygen / 1484 creatomate. **Functional first-row telemetry evidence PENDING the next genuine terminal avatar render** (worker NOT invoked; see carry below). | CCD (deployed) + CCH (closed) → next render (evidence) | Observe the next natural terminal avatar render; confirm first `render_engine='heygen'` row. |
| **CFW intentional-skip observability/accounting** | Distinguish intentional relevance/compliance skips from genuine failures: surface `draft_format.compliance_skip`/`reason` to monitoring (the `dead_reason` column is NULL for skips → false "silent loss"); optionally a distinct terminal state for skips vs failures. Diagnosed v3.32 (61 CFW + 6 Invegent skips). | **P3 — diagnosed, fix NOT started** | OPEN (diagnosis only; no fix proposed) | chat → PK | When PK directs; design-only, no mutation. |
| **CFW feed-scope review (optional)** | ~12/week "not relevant to CFW scope" skips suggest CFW ingest feeds surface off-topic content; review CFW `c.client_source`/feed scope to reduce off-scope inputs + generate-then-skip AI spend. | **P3 optional, low priority** | OPEN | chat → PK | When PK directs. |
| **F-HEYGEN-RENDER-TELEMETRY first-row verification (NEW v3.31)** | Confirm the v2.1.0 telemetry actually writes on a real render: observe the **next genuine terminal avatar render naturally** — **do NOT trigger the worker, do NOT call HeyGen**. Confirm: first `render_engine='heygen'` row appears in `m.post_render_log`; `provider_job_id` populated; `credits_used` null; idempotency (re-poll does not duplicate the row); Creatomate logging continues unaffected. | **P3 — CLOSED (v3.34)** | CLOSED 2026-06-11 (v3.34 trace audit, read-only): first 2 genuine `render_engine='heygen'` rows observed in `m.post_render_log` (2026-06-10; drafts `dcab6fba` PP + `d870f20a` NY; both `succeeded`; `provider_job_id` populated; `credits_used` null; durations present; no duplicates; Creatomate logging unaffected in the same window). Renders occurred naturally via cron — no worker invocation, no HeyGen call. `avatar_identity` null/absent on both (submits pre-dated heygen-worker v2.1.1); identity telemetry expected from the next fresh submit — folded into the A2 carry. | CCD/CCH | Closed — passive observation satisfied. |
| **A2 avatar-override policy decision (NEW v3.33)** | Routing synthesis (read-only v3.33) found avatar over-selection is a **policy choice, not a defect**: 20/22 avatar selections in the 30-day window are A2 advisor overrides (avatar realised 18/22; share ≈8% overall / ≈12% NDIS+PP; prior ~19.7% skew not reproduced). Decide: **keep avatar volume vs relax content-fit** (e.g. tighten the A2 override threshold/policy). | **P3 — policy decision, no defect** | OPEN — AMENDED v3.34: A2 is enforced at slot creation (`m.materialise_slots` hardcodes `video_short_avatar` for YouTube), not only in ai-worker; avatar identity is arbitrary-first (`lookupAvatar` LIMIT 1, no ORDER BY, `stakeholder_role` never emitted) despite NY 7 / PP 8 role-tagged active avatars in `c.brand_avatar`. Three branches: (i) keep volume + pin single branded host (config-only — deactivate all but one active avatar per client); (ii) keep volume + role-aware wiring (ai-worker emits `stakeholder_role`; lookup filter already exists — wiring-only); (iii) relax to content-fit (remove the `materialise_slots` hardcode — sql_destructive D-01). Hold the decision until ≥1 fresh post-v2.1.1 submit shows `avatar_identity` populated. **v3.35 linkage (Option C arc):** A2 and the OPTION-C / format-policy fork are **ONE decision surface** — the avatar-share decision remains unresolved and fresh `avatar_identity` telemetry remains the gating evidence; revisit at the review gate (w/c 2026-06-15, or earlier on meaningful telemetry). **BRANCH A APPLIED v3.41 (2026-06-12) — CLOSED as interim primary brand host v1:** telemetry arrived (2 post-v2.1.1 renders, both `avatar_selected_by='fallback_limit1'` — arbitrary-first PROVEN live; de-facto hosts NY=Participant, PP=Tenant, both unsafe as default faces); PK selected interim hosts; migration `pin_primary_brand_host_v1` executed under full gate chain (brief `docs/briefs/cc-a2-branch-a-host-pin.md` → Stage 0 preflight → D-01 `ab14f105` accepted type-(c) → PK exact approval phrase → apply → V-checks V1–V6 ALL-PASS). **NY pinned to Support Coordinator (Realistic)** (heygen `7e98bd3860f14ee18c9b4909e46ac77c`); **PP pinned to Buyer's Agent (Realistic)** (heygen `5d03454fbd0c469692f1a27ccbe6a000`); 26 rows deactivated (13/brand), 0 deleted; CFW/Invegent untouched; no provider calls, no worker calls, no forced render; rollback SQL captured in `docs/runtime/sessions/2026-06-12-pin-primary-brand-host-v1-execution.md` §5. **Inventory corrected:** 14 active rows per brand pre-pin (7 personas × realistic/animated) — the gate-prep 11/13 figure was an undercount, corrected by the selection-sheet read. Cast preserved for Branch B (role-aware wiring, DEFERRED); Branch C (relax to content-fit) DEFERRED. | chat → PK | **Stage 3 passive telemetry confirmation OPEN** (see A2-BRANCH-A-STAGE3-TELEMETRY carry); un-pin discipline carry standing (A2-BRANCH-A-UNPIN-DISCIPLINE); Branch B/C remain gated decisions at the review gate. |
| **A2-BRANCH-A-STAGE3-TELEMETRY (NEW v3.41)** | Passive confirmation of the Branch A pin: next scheduled NY and PP HeyGen renders must show `m.post_render_log.render_spec->avatar_identity->talking_photo_id` = `7e98bd3860f14ee18c9b4909e46ac77c` (NY) / `5d03454fbd0c469692f1a27ccbe6a000` (PP). **NO forced render, NO worker invocation — wait for the scheduled cycle.** Note: 2 stale NY `video_status='pending'` drafts (since 06-04) would inherit the pinned host if approved — desired. | **P2 — OPEN (passive watch)** | D-01 `ab14f105` corrected action part 2 (`docs/runtime/sessions/2026-06-12-pin-primary-brand-host-v1-execution.md` §7). | chat → PK | Verify pinned IDs on the next scheduled NY/PP avatar renders; then close. |
| **A2-BRANCH-A-UNPIN-DISCIPLINE (NEW v3.41)** | Dashboard RPC `public.assign_brand_avatar` sets `is_active=true` on any row by ID — any future dashboard/admin avatar action can silently reactivate a non-primary NY/PP avatar and break the single-active pin, returning heygen-worker `LIMIT 1` to arbitrary selection. **Standing discipline until Branch B ships: no NY/PP avatar activations via dashboard/admin flows without an explicit re-pin decision.** `get_brand_avatars` keeps the deactivated cast visible (flagged, not hidden). | **P3 — OPEN (standing discipline carry)** | D-01 `ab14f105` corrected action part 1 (execution record §6). | chat → PK | Carry until Branch B (role-aware wiring) exists; re-evaluate then. |
| **OPTION-C / FORMAT-POLICY FORK (NEW v3.35 — Option C decision arc)** | The format-policy decision surface for the **dormant demand-grid subsystem** (`t.platform_format_mix_default` + `m.build_weekly_demand_grid` + `m.match_demand_to_canonicals` + `m.diagnose_match_pool_adequacy` + `c.client_format_mix_override` + `c.client_match_weights` + `c.client_dedup_policy`). **Option C (policy envelope + advisor fit) = PREFERRED REFERENCE HYPOTHESIS** — the standard against which Option A (formal retirement) and Option B (continued dormancy) are compared at the review gate. Replay audit composition: ~70% existing / 15% stale-data / 10% wiring / 5% new-architecture; the stale mix seed (not thin pools) is the primary blocker. | **OPEN — decision DEFERRED to the review gate; NOT approved for implementation** | Recorded read-only (`docs/runtime/sessions/2026-06-11-option-c-decision-state.md`, `f95fc46`; T1 §7 amendments `baba5537` + `8e19dd9f`). **Mix-policy contradiction finding:** a dormant but structurally-valid policy subsystem exists — it **ran live 2026-04-25**, was orphaned by the emergency slot rebuild, and was **never formally rejected** by any decision/review/D-01; the 2026-04-22 mix seed contains **taxonomy-illegal allocations** (~19% of seeded cells illegal under `t."5.3_content_format".platform_support`). **No implementation / migration / mix reseed / wiring approved.** Live chain unchanged (Demand → Slot → Fill → Advisor → Draft → Render → Publish). **v3.41:** gate-prep evidence pack assembled (`docs/runtime/sessions/2026-06-12-option-c-a2-gate-prep.md`); A2 decided NARROWLY (Branch A interim host pin applied — see the A2 row) — **Option C REMAINS PARKED, reference hypothesis only; no Advisor / format-policy / mix-table reseed / narrative / policy-envelope reconnection approved.** Engagement evidence still missing (`avg_engagement_rate` effectively NULL) — the format-mix fork cannot yet be decided on evidence. | chat → PK (review gate) | **Revisit w/c 2026-06-15, or earlier on meaningful `avatar_identity` telemetry.** Depends on: fresh `avatar_identity` telemetry + per-format `m.post_performance` accumulation + classifier-accuracy evidence + render/format-cost evidence + pool-adequacy trend. **A2 and this fork are ONE decision surface.** No production change without a separate gate. |
| **F-ADVISOR-RESPIN-ORPHAN-SLIDES (NEW v3.34)** | Draft `096e3b72` (Invegent×LinkedIn) carries TWO `m.post_visual_spec` rows (carousel + text — advisor re-ran); 6 carousel slide renders executed (5 succeeded / 1 failed, Creatomate) for a draft whose FINAL `recommended_format='text'` — renderer consumed a non-final advisor spec → wasted Creatomate credits + orphan `m.post_carousel_slide` rows. 1 instance in the 7-day window. **AMENDED v3.39 (2nd instance):** CFW draft `8e90cbd4-00f7-448c-af06-f1816a8e99ec` (LinkedIn, 2026-06-01) compliance-skipped/dead yet produced 12 succeeded Creatomate renders + 0 publish/queue rows — repeated-pattern signal rather than one-off; stays P3 unless material provider-credit waste shows. Candidate fix (future, gated): renderer re-checks `recommended_format` before slide render, or a visual-spec supersede marker. | **P3 — OPEN**; 2nd instance recorded v3.39 (F-FORMAT-MIX-NOFORMAT lane). | v3.34 audit evidence (read-only); no fix implemented. | chat → PK | No action without its own gate. |
| **F-YT-QUEUE-ORPHAN-RECURRENCE (NEW v3.34)** | `enqueue-publish-queue-every-5m` keeps inserting YouTube `m.post_publish_queue` rows that youtube-publisher (draft-predicate, queue-bypassing) never consumes; published avatar drafts `d870f20a`/`dcab6fba` sit `queued` forever; the v3.29-archived class is regenerating (3 new orphans in window incl. 1 never-publishable `text`). Candidate fix (future, gated): exclude `platform='youtube'` in the enqueue cron, OR make youtube-publisher queue-consuming — PK's call; sql_destructive D-01 either way. | **P3 — OPEN** | v3.34 audit evidence (read-only); no fix implemented. | chat → PK | No cron change without its own gate. |
| **F-FORMAT-MIX-UNWIRED (NEW v3.34)** | `t.platform_format_mix_default` (evidence-based portfolio mix, seeded 2026-04-22) is wired into nothing; actual output is near-monoculture (FB/IG ≈ all `image_quote`, YT = 100% avatar) set by `preferred_format_facebook` + the YT hardcode + advisor drift. Park as design input for any future format-policy work — depends on the A2 decision; do NOT wire independently. **AMENDED v3.35 (Option C arc):** `t.platform_format_mix_default` is now known to be the **data layer of a dormant demand-grid subsystem** (with `m.build_weekly_demand_grid` / `m.match_demand_to_canonicals` / `m.diagnose_match_pool_adequacy`), not merely an orphan table — it ran live 2026-04-25, was orphaned by the slot rebuild, never formally rejected (T1 §7, `baba5537`/`8e19dd9f`); the seed carries ~19% taxonomy-illegal cells. Subsumed by the **OPTION-C / FORMAT-POLICY FORK** carry. | **P3 — PARKED (design input); folded into OPTION-C fork** | v3.34 audit + v3.35 Option C replay (read-only). | chat → PK | See the OPTION-C / FORMAT-POLICY FORK carry; decision deferred to the review gate. |
| **F-AIW-PREF-COL-HARDCODE (NEW v3.34)** | ai-worker `fetchFormatContext` reads the `preferred_format_facebook` column for EVERY platform (column name hardcoded); `preferred_format_linkedin`/`_instagram` are dead config. Harmless today (only FB rows set). Fold into the next ai-worker patch window — not standalone-deploy-worthy. **AMENDED v3.40:** Confirmed in deployed source (`8204a5c7`: `fetchFormatContext` queries the platform's own profile row but reads the `preferred_format_facebook` column) — NOT the cause of the LI override rate (inert today; LI row's FB column is NULL, same as the correct column). **Latent config trap: must be fixed BEFORE `preferred_format_linkedin`/`_instagram` is ever set**, or the new config is invisible to ai-worker. | **P3 — OPEN (bundle with next ai-worker patch)**; v3.40 source-confirmed (F-ADVISOR-LI-OVERRIDE lane) — fix is now a hard prerequisite for activating any platform-specific preferred-format config. | v3.34 audit evidence (ai-worker `8204a5c7` source read). | CCD (next ai-worker window) | No standalone deploy. |
| **F-EFDRIFT-REGISTER-STALE-YTPUB (NEW v3.34)** | `m.vw_ef_drift_current` shows youtube-publisher deploy 1.6.0 vs live v1.11.0 (v3.20 health GET evidence) — stale register row, likely missed targeted refresh. Fix = one `drift-check?write=true&slug=youtube-publisher` targeted refresh — write-flagged, so needs explicit PK go-ahead; bundle with the next drift-check tick or any deploy session. (Existing insights-worker B-RR repo-behind-deployed carry unchanged.) | **P3 — OPEN** | v3.34 audit evidence (register read vs v3.20 health GET). | CCD/CCH → PK gate | Write-flagged refresh; do not fire ad hoc. |
| **F-PUBLISH-LEDGER-STATUS-MISMATCH (NEW v3.35 — slot decomposition)** | 2 drafts in the slot-decomposition cohort carry `approval_status='published'` while `m.post_publish` holds **failed rows only** (no successful publish row) — either the asset went out and the success row was never written, or the status flipped on a failure. Ledger/status integrity question. | **P2 — OPEN** | Proposed in the slot decomposition (`b74c8e50`, session `docs/runtime/sessions/2026-06-11-slot-level-decision-tree-decomposition.md`); no fix implemented. | chat → PK | Read-only investigation first: identify the 2 drafts, check platform-side existence of the posts, find the status-flip code path. Any fix separately gated. |
| **F-IG-TEXT-PALETTE-GAP (NEW v3.35 — slot decomposition)** | 1 Instagram draft has `recommended_format='text'` despite taxonomy `t."5.3_content_format".platform_support.instagram=false` for text_post — the advisor palette (t.5.3 `platform_support` ∩ `c.client_format_config`) did not enforce the platform_support leg. Draft is approved + unpublished → likely to strand or fail at the IG publisher (IG requires media). | **P2/P3 — OPEN** | Proposed in the slot decomposition (`b74c8e50`); no patch now. | chat → PK | Add an advisor palette platform_support enforcement check in ai-worker; watch the draft's outcome as evidence either way. No patch without its own gate. |
| **F-PUBLISHER-STATUS-FLIP-INCONSISTENT (NEW v3.35 — slot decomposition)** | FB publisher flips drafts to `approval_status='published'`; LinkedIn + YouTube publishers leave successfully published drafts at `'approved'` (8 of 25 in cohort). Cosmetic but pollutes status-based reporting and inflates "approved not published" counts. **CORROBORATED v3.40:** 46/46 LinkedIn override rows in the 30d window have LinkedIn `m.post_publish` rows yet remain `approval_status='approved'`. | **P3 — OPEN (bundle with next publisher patch windows)** | Proposed in the slot decomposition (`b74c8e50`). Not standalone-deploy-worthy. | chat → PK | Align the status flip across publishers in their next respective patch windows. No standalone deploy. |
| **F-CFW-COMPLIANCE-PREGEN (NEW v3.35 — slot decomposition, efficiency)** | CFW compliance skips consume a full ai_job + draft generation before being killed (7 in cohort; ~12/week historically). Consider running scope/compliance rejection BEFORE generation (cheap relevance pre-check at fill, or a first-pass classifier before full draft synthesis). | **P3 — OPEN (cost optimisation only)** | Proposed in the slot decomposition (`b74c8e50`). **Do NOT implement now** — the post-generation kill works correctly as a safety net; this is a cost optimisation. Related to the v3.32 CFW intentional-skip carries. | chat → PK | Future gated patch only. |
| **vchecks.md V-B4 doc patch** | Correct V-B4 to 12-param signature | P3 carry | **Path LOCATED v3.07** (`docs/briefs/cc-0017c/vchecks.md`); fix grounded; DEFERRED to CC. | chat → CC | Surgical str_replace. |
| **cc-0017c v1.2 doc patch candidate** | Date + 3 D-01 refs + V-B4 signature | P3 carry | **DONE v3.07** (`6d5c3ae6`→`81dcb683`). | n/a (recorded) | — |
| **Invegent IG cap-throttle planning** | jobid 53 unblock | P2 carry | OBSERVED. *(v3.05: cron 53 enabled in pre-compaction IG arc; close owed.)* | chat → PK | Verify + dry-run + re-enable. |
| **F-YT-PUB-AVATAR-EXCLUSION** | youtube-publisher format filter | P2 → CLOSED | **CLOSED v3.08** — `video_short_avatar` added to youtube-publisher v1.7.0 eligible-format allow-list; first live Short `sfQvSM2Osus` (draft `40f9fa25`). EF source reconciled to `main` (`194e0792`). | n/a (closed) | — |
| **cc-0013 Dashboard Phase 0** | 7 confirm-defaults | P2 carry | OPEN. | PK | When PK directs. |
| **Backend/shared-metrics refactor** | Deeper scope behind v2.95 count mismatch | DEFERRED carry | OPEN. Not actively ranked. | n/a | When separately directed. |
| **L-v2.83-a promotion** | push_files response file-count verification | **P3 (18+; STRONG)** | Re-applied at sync close. NOT promoted v3.07 (canonical home/numbering unresolved). | chat → next lesson cycle / CC | Promote. |
| **L-v2.85-e** | push_files length budget — split-commit mitigation | **P3 (PROMOTION-CONFIRMED v2.88)** | NOT promoted v3.07 (canonical home/numbering unresolved). | chat → next lesson cycle / CC | **PROMOTE.** |
| **L41 / full-file-write clobber mitigation** | Re-read HEAD before any full-file sync write; prefer surgical/sha-passed edits for 00_ index files | **P2 (re-exercised v3.02.1; reinforced v3.02.2/v3.03/v3.04/v3.05/v3.06/v3.07/v3.08)** | POSITIVE exemplars v3.02.2→v3.08 (all sha-passed surgical edits; v3.08 read-before-write caught two already-landed writes — sha alone would have clobbered). NOT promoted v3.07 (canonical home/numbering unresolved). | chat → next lesson cycle / CC | **Pair-promote with L-v2.85-e.** |
| **L-v2.85-a (HIGH-SIGNAL)** | V-check function signature probe at brief authoring | P3 (4 occurrences) | Promotion-eligible. NOT promoted v3.07. | chat → next lesson cycle / CC | Promote. |
| **L-v2.94 convention (NEW)** | brief lifecycle ready→fire→review_required→PK observe→ready | P3 (1 natural cycle v3.01) | Confirm across 2-3 more cycles. | chat → next session | Watcher. |
| **L-v3.02-a / L-v3.03-a / L-v3.04-a (NEW)** | mobile breakpoint verify / downstream acceptance gates / independently verify cross-repo merge SHA | P3 (1 occurrence each) | Watchers. | chat → next session | Promote after one more occurrence each. |
| **L-v3.05-a (NEW)** | Record root CAUSE not just symptom (YT re-diagnosed as token-reconnect ≥3×); re-run stale audits against live data on user pushback | P3 (1 occurrence v3.05) | Watcher. | chat → next session | Promote after one more occurrence. Pair with L41 / no-fabrication discipline. |
| **L-v3.06-a (NEW)** | Deployed-vs-repo drift is a standing risk class: verify deployed EF/migration state against repo before any redeploy; backfill chat-applied migrations to `supabase/migrations/` same-session | P3 (1 occurrence v3.06 — IG publisher 4-version drift + 7 missing migration files) | Watcher. | chat → next session | Promote after one more occurrence. Pair with L41. |
| **L-v3.07-a (NEW)** | GitHub bridge has no str_replace → large files (briefs/canonical lessons) belong to CC surgical path, not chat full-file re-emit; undocumented scope/numbering → defer with explicit note, never invent | P3 (1 occurrence v3.07; reinforced v3.08 — sha not a reliable concurrent-edit guard, read-back is the real protection) | Watcher. | chat → next session | Promote after one more occurrence. Pair with L41 + L-v3.06-a. |
| **L-StageR-paging (NEW v3.42-R)** | Security hygiene: Meta Graph 200 responses include `paging` URLs that echo the access token — Stage R pg_net response `159957` leaked a CFW page-token fragment into a session transcript; contained same-day by a targeted purge of request `159957` via the audited migration path (`security_purge_pgnet_stage_r_159957`, PK-approved) + precautionary CFW token rotation (SEC-CFW-TOKEN-ROTATION). **Standing rule: any SQL/pg_net Graph read must strip/redact `paging` blocks BEFORE capturing or persisting response bodies; patches must redact/drop `paging` from anything persisted or logged.** | P2 (security; 1 incident, contained) | Stage R (Fix 1 brief §2.5). | chat → PK / every future Graph-read lane | Standing rule — apply in every Graph verification lane; codified in the Fix 1 patch shape. |
| **L-v3.41-a (NEW)** | PG-compat: Postgres has no `min(uuid)`/`max(uuid)` aggregate — first `pin_primary_brand_host_v1` apply aborted on `42883` in the assertion plumbing (DO block transactional: both UPDATEs rolled back; read-only re-check verified state untouched); re-applied with `min(brand_avatar_id::text)::uuid`, approved scope unchanged. Avoid `min/max(uuid)` in migration assertions unless cast or otherwise handled — add to the PG-compat pre-author checklist alongside the reserved-word check. | P3 (1 occurrence v3.41) | Watcher. | chat → next lesson cycle | Promote to checklist after next occurrence. |
| **L62 reinforcement** | Type-B vs Type-C D-01 handling | P3 (cc-0016 6-fire series; v3.05 both YT sql_destructive partial/type-c; **v3.06 F-YT plan_review partial→clean type-c resolution**) | Strong empirical record. | chat → next lesson cycle | Strong. |
| **L-v2.88-a** | Identical PK-directive loop watcher | P3 (3 cumulative) | — | chat → next lesson cycle | Pair-promote if recurs. |
| **L-v2.90-a-f** | V-D fixture / arity DROP / etc. | P3 (not re-exercised) | Watchers. | chat → next session | Watcher. |
| **L-v2.78-a / L-v2.81-a / L47 / L-v2.84-a-d / L-v2.85-b/c/d / L-v2.86-a-e / L-v2.88-b/c/d / L-v2.89-a** | Various candidates | P3 carry | Unchanged. | chat → next session/lesson cycle | Various. |
| **Other carries** | Minor doc patches (cc-0010A/0011/0012) / F-K-SCHEMA-REGISTRY-R-STALE / AI cost view / Publisher latent config / M8b / 94-row cohort / F-CRON-AUTO-APPROVER-SECRET-INLINE / morning-inbox-sweep-v1 / 22 escalated m.chatgpt_review rows / Memory cap 20/30 / F-PUB-009 / CFW dead drafts (DIAGNOSED v3.32 — intentional relevance/compliance skips, NOT a production defect; reason in draft_format.reason not dead_reason) / Vault service_role_key / 00_overview / F-AAP-NEEDS-REVIEW / F-AI-WORKER-PARSER-SKIP-BUG / 4× F-CRON-*-STALE / Emergency redeploy / f4a0dd85 health-check / feature branch `feature/cc-0009-stage-b-ef-source` / 3 pre-v2 forensic rows / Localhost FAB cleanup / 3 v2.77 D-01 close-the-loops | P2/P3 various | Unchanged. | various | various |

**Closed v3.43 (Content Studio T0/T1/Visibility + roster + Fix 1 — register reconciliation, docs-only):**
- **Content Studio T0 — governed manual path → CLOSED (PASS WITH CARRY).** Manual single-post enters ICE as governed intent (`create_manual_slot` → manual slot → fill → ai_job → Advisor → compliance → approval → render → queue → publish); queue-save bypass removed; `manual_post_insert` deprecated (retained, anon/authenticated revoked). Close-out `docs/runtime/sessions/2026-06-13-t0-content-studio-governed-path-closeout.md` (`38b0d0a31129457f8096279c87a210a8f42fb6d3`). Verification slot published to FB; `selected_canonical_ids=0`; no bypass. Carries: F-SLOT-SCHEDULE-FIDELITY (P3, pre-existing), F-PUBLISH-QUEUE-DANGLING-QUEUEID (P4).
- **Content Studio T1 — Creative Intent / Content Package parent → CLOSED (PASS WITH CARRY).** One idea → N governed platform-native sibling slots under one `m.creative_intent`; per-child Advisor/compliance/approval; additive (`creative_intent` + nullable `intent_id` FKs ON DELETE SET NULL); automated fill path byte-identical. content-engine main `9e4ceb12`, dashboard main `d48772b3`; close-out `docs/runtime/sessions/2026-06-13-t1-creative-intent-deploy-closeout.md` (`08ecb4e`). Carry: F-INTENT-STATUS-ROLLUP (P3).
- **Content Studio T1 Operational Visibility (Ideas tab) → CLOSED.** Read-only `list_creative_intents` RPC + Ideas list/drill-down; one idea → child slots → drafts → outcomes without SQL; parent status derived on display. content-engine main `2ae568ee`, dashboard main `f5c27874`; close-out `docs/runtime/sessions/2026-06-13-t1-visibility-deploy-closeout.md` (`718b252`).
- **Content Studio active-client roster fix → CLOSED (F-CONTENT-STUDIO-CLIENT-ROSTER RESOLVED).** Cause: hardcoded `CLIENTS` constant omitted CFW/Invegent → Ideas could not reach CFW. Fix: DB-driven active-client selector (read-only `list_active_clients` RPC + `/api/clients` + `useActiveClients` hook with static fallback) wired into the 4 Content Studio selectors. content-engine main `879f68ce`, dashboard main `fe12c8c9`; close-out `docs/runtime/sessions/2026-06-13-cs-active-clients-roster-closeout.md` (`df0b453`). CFW + intents `0d0a45a0`/`f8c10b91` now reachable; children/reasons/canonical=0 render.
- **Fix 1 — insights-worker v14.2.0 FB engagement repair → CLOSED (PASS WITH CARRY).** Replaced dead `post_engaged_users`/`post_impressions`, pinned Graph v24.0, `post_reactions_by_type_total` basis, NULL-not-0, raw-failure visibility, `paging` redaction. Merge `335ab121`, deploy record 66; close-out `docs/runtime/sessions/2026-06-12-fix1-v14-2-0-deploy-closeout.md` (`808ddd2a`). V-checks: 79 fresh rows / 79/79 calls / 79/79 `reactions_by_type_total` / 316/316 telemetry / 0 token leakage / comments-shares NULL-not-0 / `engagement_rate` 48/48 where reach>0; contrast vs 447 pre-deploy rows. Carries: F-INSIGHTS-FIX2-AGGREGATION-OWNERSHIP (P2, next workstream), F-INSIGHTS-DEPLOY-VERSION-DRIFT (P3), META-FIX1-PAGES-READ-ENGAGEMENT (P3, non-blocking).
- This reconciliation: **0 code / 0 DB / 0 deploy / 0 production mutation** — docs/register only. T2 deferred.
**Closed v3.42:** F-OPTIONC-ENGAGEMENT-EVIDENCE-NULL diagnostic lane → CLOSED as diagnosed (three stacked defects: D1 FB `post_engaged_users` 526/526 metric failure + zero-coerced fallback → engagement_rate NULL on all FB rows; D2 dual-writer clobber — 03:15 refresh nulls worker-produced window-30 values the Advisor reads; D3 YT structurally excluded via reach-NULL + stale/manual-only). Carry F-OPTIONC-ENGAGEMENT-EVIDENCE-NULL opened **P2 OPEN/diagnosed**. LI/IG have zero insight rows. Advisor receives fabricated "0.0% avg engagement" (NULL coerced to 0). Option C remains PARKED — cannot become evidence-based until the feed is repaired. Read-only lane: 0 code / 0 DB / 0 cron / 0 worker-provider call / 0 Advisor / 0 ai-worker / 0 Option C / 0 D-01 / 0 production change. Authority impact: none.
**Closed v3.41:** A2 Branch A → APPLIED/CLOSED as interim primary brand host v1 (`pin_primary_brand_host_v1`: NY = Support Coordinator (Realistic) `7e98bd38…`, PP = Buyer's Agent (Realistic) `5d03454f…`; 26 `c.brand_avatar` rows deactivated / 0 deleted; CFW/Invegent untouched; D-01 `ab14f105` accepted type-(c); PK exact approval phrase; V-checks V1–V6 ALL-PASS; rollback SQL held in the execution record). Stage 3 passive telemetry confirmation remains OPEN (no forced render). Option C remains PARKED (reference hypothesis only; no reconnection approved). Inventory corrected to 14 active rows/brand pre-pin. Mutation footprint of the lane: 1 applied migration (26 UPDATEs, config DML) — 0 code / 0 DDL / 0 dashboard / 0 Advisor / 0 ai-worker / 0 policy / 0 provider / 0 worker / 0 cron / 0 forced render; this register pass is docs-only. Authority impact: none; T1 untouched.
**Closed v3.40:** F-ADVISOR-LI-OVERRIDE → CLOSED (diagnosed — expected Advisor correction behaviour under current config; evidence routed to the Option C/A2 review gate; read-only investigation, register-only close under PK approval). F-AIW-PREF-COL-HARDCODE amended (source-confirmed, inert, latent config trap — fix before any platform preferred-format config). F-PUBLISHER-STATUS-FLIP-INCONSISTENT corroborated (46/46 LI publishes at `approved`). 0 code / 0 DB / 0 dashboard / 0 Advisor / 0 ai-worker / 0 policy / 0 Option C / 0 provider / 0 worker / 0 cron / 0 production change. Authority impact: none.
**Closed v3.39:** F-FORMAT-MIX-NOFORMAT → CLOSED (diagnosed — subsumed by existing carries; read-only investigation, register-only close under PK approval). F-ADVISOR-RESPIN-ORPHAN-SLIDES amended with 2nd instance (`8e90cbd4`). 0 code / 0 DB / 0 dashboard / 0 Advisor / 0 policy / 0 Option C / 0 provider / 0 worker / 0 production change. Authority impact: none.
**Closed v3.38:** Monitor dashboard visibility (Slices 4–6) → LIVE on production. dashboard PR #7 MERGED (`417210c`); Vercel prod deploy `dpl_A9dr9scRF1u2346FbaEoQsaR9UJ4` READY (alias `dashboard.invegent.com`); 3 read-only panels (Render + Avatar Health / Performance Freshness / Pipeline Exceptions) live, verified by SHA + alias; read path = existing `exec_sql` (SELECT-only). Branch `monitor/render-perf-exceptions-panels` @ `1e26bc2` (5 files); tsc+build exit 0; screenshot-verified on local preview. Open follow-up: pre-existing `/overview` `created_at` SQL error (unrelated). 0 DB / 0 classifier / 0 Advisor / 0 policy / 0 Option C / 0 render-logic / 0 publisher / 0 compliance / 0 cron / 0 worker / 0 provider / 0 extra panels. Authority impact: none.
**Closed v3.37:** Monitor dashboard visibility (Slices 1–3) → LIVE on production. PR #6 MERGED (`c168d2c`); Vercel prod deploy `dpl_7we82KUmm47j8jSEC4UsbSAa6tnc` READY (alias `dashboard.invegent.com`); 3 read-only panels (Classifier v2 Health / Advisor Override / Format Mix) visually confirmed live by PK; read path = existing `exec_sql` (SELECT-only). Branch `monitor/classifier-advisor-format-panels` @ `a2d4c82` (7 files). Open follow-up: pre-existing `/overview` `created_at` SQL error (unrelated). 0 DB / 0 classifier / 0 Advisor / 0 policy / 0 Option C / 0 render-publisher / 0 extra panels. Authority impact: none.
**Closed v3.36:** ICE Classifier v2 repair → APPLIED to prod + validated (migration `classifier_v2_repair_and_telemetry` + 2 hotfixes; smoke A/B/C/D PASSED; 100-row bounded backfill done — 70 analytical split 8 rule_matched / 62 defaulted). Branch `repair/classifier-v2-telemetry` @ `48ea045`, PR #7 MERGED (`2be28040`; merge ≠ deploy). NEW carries: remaining-v1 bounded backfill (opt-in/gated), `first_seen_at` drift, repo↔prod migration-version drift. 0 Advisor / 0 policy reseed / 0 Option C / 0 deploy / 0 publisher-render change. Authority impact: none.
**Closed v3.08:** F-YT-PUB-AVATAR-EXCLUSION → CLOSED (`video_short_avatar` in youtube-publisher v1.7.0 allow-list; first Short `sfQvSM2Osus`). Avatar→YouTube Shorts pipeline → LIVE + repo-reconciled (EF parity `521b053b`+`194e0792`; continuity docs `04294d23`; `00_` rebuild). F-YT-FAILED-NO-RETRY re-pointed → youtube-publisher v1.8.0 (still OPEN). cc-0019 live-version framing corrected v2.12.0→v2.13.0; **Unit B re-verified absent against ai-worker v2.13.0 (`8204a5c7`) — re-verify carry DISCHARGED, Unit B STILL OPEN / optional** (post-milestone narrow patch). 0 production mutation; 0 new deploy; 0 D-01.
**Closed v3.07:** cc-0017c v1.2 doc patch (DONE — `6d5c3ae6`→`81dcb683`; main brief only). NOT closed/deferred: cc-0017a/0010A/0011/0012 + cc-0017c/vchecks.md (→ CC), decisions.md (unlocated), lessons promotion (→ next-lesson-cycle/CC). 0 production mutation; 0 D-01.
**Closed v3.06:** Deployed↔repo drift CLOSED (IG publisher `bc78511e`; 7 migration backfills `8738c3a0`). cc-0019 CORRECTED → Unit A implemented / Unit B not. IG-arc recording debt discharged. F-YT-FAILED-NO-RETRY brief authored (`bee872aa`) + **plan_review CLEAN** (`abf6f63c` partial/type-c → `f89c23cf` agree; Unit-B 3-bucket patch `a3f329f3`). 0 production mutation; 2 plan_review D-01 (non-mutating).
**Closed v3.05:** YouTube publishing restore → RESTORED/VERIFIED (both channels publishing; backlog released + soft-retired). cc-0019 brief → reviewed CLEAN (ready for execution). No core/dashboard rank change.
**Closed v3.04:** Dashboard Slice 0A → MERGED/RECORDED at `3ec489b`.
**Closed v3.03:** none (recording-only; Q-005 stays OPEN by design).
**Closed v3.02:** Mobile/narrow viewport verification → CLOSED/PASS. Mobile-fix commit SHA backfill → CLOSED v3.02.2.
**Closed v3.01:** Cowork brief lifecycle gating WARN.
**Closed earlier:** v3.00 cc-0016 Stage D + evidence path; v2.99 cc-0016 Stage B + V-A5; v2.98 cc-0016 Stage C apply; v2.97 cc-0016 Stage A apply; v2.96 dashboard slices 4A–4B + top alert bar; v2.95 dashboard slices 1–3 + PHASES streak; v2.93 Reconciliation daily cadence; v2.92 Health_check V-C3; v2.91 cc-0017e v1.1; v2.90 cc-0017e apply; v2.85 cc-0017c apply; v2.82 cc-0017b apply; v2.81 cc-0017a apply; v2.77 cc-0014 archived.

---

## 💼 Personal businesses

- **Crazy Domains refund + clean-up follow-up** (carry from v2.51) — PK manual. Re-check next session.

*(Standing P0 to ask at next session start.)*

---

## 🌱 Future ideation

Unchanged from v2.76-v3.01.

---

## 📌 Backlog

**v3.62 state changes:**
- **AGP-GATE3-AUTHENTICATOR-REVOKE-CLEANUP → CLOSED** — the inherited `authenticator` SELECT on `r.avatar_resolution_shadow` is revoked. Commit `ee5d432` pushed the migration; applied successfully as `20260618092646_agp_gate3_revoke_authenticator_select_avatar_shadow` (single `REVOKE SELECT ON r.avatar_resolution_shadow FROM authenticator`; bare REVOKE → idempotent; touches nothing else — not the resolver, service_role grants, RLS, policies, search_path, table shape, EFs, the flag, or markers). End-state (recorded from the apply step): `authenticator` SELECT removed; `anon`+`authenticated` remain without SELECT (anon never targeted — already held none); `service_role` retains required access; RLS ENABLED with 0 policies (deny-by-default unchanged); telemetry rows=0; no EF deploy; `AVATAR_SHADOW_TELEMETRY` OFF/unset; no Branch B / marker backfill / live resolver change. Not a live exposure (hardening only): schema `r` not REST-exposed (PGRST106) + deny-by-default RLS; `authenticator` is the pre-role-switch connection role, not a client-reachable principal.
- **Repo↔prod migration-timestamp drift (recorded)** — repo SQL file `20260618133000_agp_gate3_revoke_authenticator_select_avatar_shadow.sql` (commit `ee5d432`) vs applied version `20260618092646_…` — identical name suffix, different timestamp (the known repo↔prod drift; end-state correct, repo provenance carries the delta).
- **Phase 3.3 (flag-enable / soak) remains BLOCKED** pending explicit PK instruction; #4 cutover BLOCKED; Branch B NOT AUTHORISED — all unchanged this pass.
- 0 production action this pass; docs-only register reconciliation (`00_sync_state.md` + `00_action_list.md`). No `decisions.md` change. Verified read-only (LOCAL GIT ONLY): `ee5d432` on `main` with one 38-line migration file.

**v3.60 state changes:**
- **AGP-GATE3-AUTHENTICATOR-REVOKE-CLEANUP** (NEW follow-up; low-risk posture cleanup) — revoke the inherited `authenticator` SELECT on `r.avatar_resolution_shadow`. **Not a live exposure:** RLS is deny-by-default (enabled, 0 policies) and schema `r` is not REST-exposed (PGRST106), so `authenticator` cannot reach rows; Session 1's applied Gate-3 migration pinned `search_path=public` + granted service_role but did not neutralise the schema-`r` default-ACL SELECT to `authenticator`. **Defer as a small, separately-gated migration** (grant-only REVOKE; `sql_destructive` D-01 + PK approval at execution). Source: AGP D-01 Gate-3 Phase 3.2 independent verification (WARN-1).
- **AGP D-01 Gate-3 Phase 3.2 CLOSED** — shadow resolver + telemetry applied (`20260618084054_agp_d01_3_avatar_shadow_resolver_telemetry`) + heygen-worker v2.2.0 / EF version 36 deployed + independently verified read-only; dormant (flag OFF/unset, 0 rows, live selection + `lookupAvatar` unchanged, no Branch B, no marker backfill). **Phase 3.3 flag-enable remains BLOCKED pending explicit PK instruction.**
- 0 production mutation this pass; docs-only register reconciliation (`00_sync_state.md` + `00_action_list.md`). No decisions.md change.

**v3.08 state changes:**
- **Avatar→YouTube Shorts pipeline LIVE + repo-reconciled** — ai-worker v2.13.0 + heygen-worker v2.0.0 + youtube-publisher v1.7.0 reconciled to `main` from branch `feat/ai-worker-v2.13.0-avatar-youtube` (EF parity `521b053b`+`194e0792`; continuity docs `04294d23`; `00_` rebuild). First live unlisted Short `sfQvSM2Osus`. EFs already deployed; repo source-parity only.
- **F-YT-PUB-AVATAR-EXCLUSION CLOSED** — `video_short_avatar` in the publisher v1.7.0 allow-list.
- **F-YT-FAILED-NO-RETRY re-pointed → youtube-publisher v1.8.0** (still OPEN; v1.7.0 consumed by the avatar allow-list, not the retry fix). Brief text still names v1.7.0 internally → CC/follow-up fix.
- **cc-0019 live-version framing corrected** v2.12.0→v2.13.0 on current-state surfaces (STATUS BLOCK + Active row); **Unit B re-verified absent against ai-worker v2.13.0 (`8204a5c7`)** — re-verify carry DISCHARGED; Unit B STILL OPEN / optional. v3.06 historical mentions left intact.
- 0 production mutation; 0 new deploy; 0 Supabase; 0 D-01; 0 dashboard edit. No decisions.md change. PHASES leg N/A.

**v3.07 state changes:**
- **cc-0017c main brief → v1.2 PATCHED + CORRECTED** (`6d5c3ae6`→`81dcb683`) — apply-record backfill (APPLIED v2.85; migrations `20260519004545`+`20260519005322`; 3 D-01 refs; V-B4 12-param pointer); correction softened an unverified cc-0017d successor version tag.
- **cc-0017a / cc-0010A / cc-0011 / cc-0012 + cc-0017c/vchecks.md DEFERRED to CC** — insufficient documented scope + full-file re-emit risk (no str_replace on the bridge; 49–107 KB briefs).
- **vchecks.md path located** — `docs/briefs/cc-0017c/vchecks.md` (V-B4 12-param fix grounded).
- **decisions.md still unlocated** — parked.
- **Lessons NOT promoted** — canonical home + numbering convention not locatable; PK option (c) defer to next-lesson-cycle/CC; nothing promoted by assumption.
- **L-v3.07-a NEW lesson candidate** (no-str_replace → large files to CC; undocumented scope/numbering → defer not invent). L41 reinforced v3.07 (compact sha-passed `00_` edits; large briefs not chat-re-emitted).
- 0 production mutation; 0 Supabase; 0 D-01; 0 dashboard edit. No decisions.md change. PHASES leg N/A.

**v3.06 state changes:**
- **Deployed↔repo drift CLOSED** — IG publisher v2.0.0→v2.4.0 synced (`bc78511e`); 7 chat-applied 2026-05-24 migrations backfilled to `supabase/migrations/` (`8738c3a0`, recovered from `schema_migrations.statements`).
- **cc-0019 CORRECTED** — Unit A IMPLEMENTED (live `m.is_publish_eligible` + `fill_pending_slots` gate; migration `20260524091020`; D-01 `a2e5f3f5` + PK approval per header); Unit B (ai-worker preflight) verified NOT implemented in deployed ai-worker v2.12.0. v3.05's "brief only" was stale. *(v3.08: Unit B re-verified still absent against the now-live ai-worker v2.13.0 `8204a5c7`.)*
- **F-YT-FAILED-NO-RETRY brief** authored (`bee872aa`); **plan_review CLEAN** (`abf6f63c` partial/type-c → `f89c23cf` agree; Unit-B 3-bucket patch `a3f329f3`); next ef_deploy + sql_destructive at execution + PK approval (A-then-B).
- **IG-publish-restore arc 4-way close** discharged (publisher + migration legs); residual operational only.
- **L-v3.06-a NEW lesson candidate** (deployed-vs-repo drift verification before redeploy; same-session migration backfill).
- **L41 reinforced v3.06** — surgical sha-passed sync recording.
- 0 production mutation; 2 plan_review D-01 (`abf6f63c`→`f89c23cf`, non-mutating). No decisions.md change. Dashboard PHASES leg N/A.

**v3.05 state changes:**
- **YouTube publishing RESTORED** — root cause OAuth Testing-mode 7-day token cap (F-YT-OAUTH-TESTING-MODE); fix = app→Production + reconnect; verified PP `ma6EG1fz4XQ` + NY `qp-ZGm8lNIo`; backlog released (13 generated) + soft-retired (14 archived_stale).
- **F-YT-FAILED-NO-RETRY** + **F-YT-EXPIRY-DISPLAY-FAKE** registered (candidate brief / cleanup).
- **cc-0019 brief** authored + reviewed clean (`81cb414`→`01a6cdf`; plan_review `ff05c65e`). Brief only.
- **emission_rule seed patch** still Active carry (Q-005 fork A).
- **IG-publish-restore arc 4-way close** registered OWED.
- **L-v3.05-a NEW lesson candidate** (record root cause not symptom; re-run stale audits on pushback).
- **L41 reinforced v3.05** — surgical sha-passed sync recording.
- No decisions.md change.

**v3.04 state changes (preserved):** Dashboard Slice 0A MERGED + RECORDED at `3ec489b` (Pre-Phase 0; NOT real Phase 0). L-v3.04-a NEW candidate. L41 reinforced. No decisions.md change.

**v3.03 state changes (preserved):** Q-005 Option A trail recorded; brief v3.1.1; OPEN next-fire watch; emission_rule seed patch registered. L-v3.03-a NEW. No decisions.md change.

**v3.02 (+.1 +.2) state changes (preserved):** Mobile viewport CLOSED/PASS; mobile-fix SHA `d17b604` backfilled; L-v3.02-a NEW; L41 re-exercised (clobber→repair); Cowork WARN CLOSED v3.01 (restored). cc-0016 A/B/C/D closed; only Stage E remains. T-MCP cum re-baseline pending.

---

## 🧊 Frozen / Deferred

Unchanged. **cc-0016 Stage E lifecycle cleanup** — future/separately-approved-only. **Backend/shared-metrics refactor** — deferred carry. **emission_rule seed patch** — Q-005 close fork A; Supabase mutation requires D-01 + PK approval. **Real Dashboard Phase 0 (review §9)** — future/gated. **cc-0019 Unit B (ai-worker preflight)** — optional; gated on ef_deploy D-01 + PK approval (Unit A already live); **re-verified absent against ai-worker v2.13.0 (`8204a5c7`) v3.08 — STILL OPEN, optional**. **F-YT-FAILED-NO-RETRY / F-YT-EXPIRY-DISPLAY-FAKE** — brief authored (plan_review CLEAN; re-pointed → youtube-publisher v1.8.0) / cleanup, not implemented. **Large-brief doc patches (cc-0017a/0010A/0011/0012) + cc-0017c/vchecks.md** — DEFERRED to CC surgical pass (no str_replace on bridge; full-file re-emit risk). **Lessons promotion** — deferred to next-lesson-cycle/CC (canonical home + numbering unresolved).

---

## 🎓 Canonical Lessons

L37–L65 + L-v2.76-a-f + L-v2.78-a + L47 + L-v2.81-a + L-v2.83-a + L-v2.84-a-d + L-v2.85-a-e + L-v2.86-a-e + L-v2.88-a-d + L-v2.89-a + L-v2.90-a-f + L-v2.94 + L-v3.02-a + L-v3.03-a + L-v3.04-a + L-v3.05-a + L-v3.06-a + L-v3.07-a candidates carried.

- **L41**: re-exercised v3.02.1; clean surgical sha-passed edits v3.02.2 + v3.03 + v3.04 + v3.05 + v3.06 + v3.07 + v3.08. Strong pair-promote candidate. *(v3.08: read-before-write caught two already-landed `00_` writes; the sha precondition was stale and would have clobbered had it been trusted — read-back is the real guard.)*
- **L62**: strongly reinforced via cc-0016 6-fire series; v3.05 both YT sql_destructive partial/type-c; v3.06 F-YT plan_review partial→clean (type-c resolution).
- **L-v2.83-a**: 18+ occurrences. STRONG.
- **L-v2.85-e**: PROMOTION-CONFIRMED v2.88.
- **L-v2.88-a**: 3 cumulative.
- **L-v2.94 / L-v3.02-a / L-v3.03-a / L-v3.04-a / L-v3.05-a / L-v3.06-a / L-v3.07-a**: NEW candidates.
- **L-v3.05-a**: record root CAUSE not just symptom; re-run stale audits against live data on user pushback.
- **L-v3.06-a**: deployed-vs-repo drift verification before redeploy; backfill chat-applied migrations same-session.
- **L-v3.07-a**: no str_replace on the GitHub bridge → large files (briefs/canonical lessons) to CC surgical path, not chat full-file re-emit; undocumented scope/numbering → defer with explicit note, never invent. *(v3.08: the bridge's optional `sha` arg is not a reliable concurrent-edit guard — it accepted a stale sha and would have overwritten; read-before-write + readback is the real protection.)*
- **L-v2.90-a-f**: watchers.

**Highest-priority promotions next lesson cycle: L41 + full-file-write mitigation (pair), L-v2.85-e, L-v2.85-a, L-v2.83-a.** *(v3.07: NOT promoted — canonical lessons home + numbering convention not locatable in repo; promotion deferred to next-lesson-cycle/CC where the convention is known. Do NOT assign baseline L-numbers by assumption.)*

---

## v3.05 honest limitations

- **YT token durability unproven today** — both publishes succeeded (tokens refresh now); a Production-issued token surviving past 7 days is the expectation but only confirmable after ~31 May.
- **"App is in Production" taken from PK + the successful live publish** — chat cannot see Google Cloud Console; the successful refresh against a previously-7-day-dying token is the empirical confirmation.
- **Both YT sql_destructive D-01s returned `partial` (type-c echoes), escalated to PK; PK had already directed the action** — proceeded on standing approval. NY test reused the PP verdict (`82443c8c`, same action/guard/arc, same UTC day).
- **T-MCP-02 count uncertain** — sync_state recorded ~92 (v3.04); pre-compaction IG arc fired ~5 more (unrecorded close, owed); v3.05 +4 → cumulative needs re-baseline (~101–105). *(v3.06 +2 plan_review.)*
- **cc-0019 is brief only** — `fill_pending_slots` unchanged; `is_publish_eligible` not created; execution gated. *(Superseded by v3.06 correction: Unit A IS live.)*
- **Production mutations:** 3 apply_migration (draft `video_status` only) + 2 publisher invokes (real publishes). EF deploys: 0. cron: 0. enablement: 0. hard deletes: 0. emission_rule rows seeded: 0. dashboard edits: 0.
- **No decisions.md change. No Stage E. No cc-0015 start. No Q-005 closure. No re-emission. CFW/Invegent untouched.**
- Memory cap 19/30 unchanged. Gate 11 closes 2026-05-26 (~2 days).

## v3.07 honest limitations

- **Only cc-0017c main brief was chat-safe** — small file (5.8 KB) with a documented scope. cc-0017a/0010A/0011/0012 + cc-0017c/vchecks.md were NOT patched: no documented scope (except cc-0017a's facts) and/or 49–107 KB full-file re-emit risk (GitHub bridge has no str_replace).
- **cc-0017c v1.1 D-01 UUID recorded truncated** — `9e602a2d-…`; the full UUID is not captured in the v2.85 source session. Tail NOT invented.
- **cc-0017d successor version softened** — only "APPLIED" confirmed this session, not the specific v2.90 tag (cc-0017e v2.90/v2.91 retained per action_list).
- **Lessons NOT promoted** — canonical L37–L65 definitions not locatable in repo (root / `docs/` index / `docs/runtime/` / handoff); promotion mechanic + numbering convention undocumented. Deferred (PK option c). Nothing promoted by assumption.
- **decisions.md still unlocated** — parked.
- **`00_` index edits done compact + sha-passed** (no large-brief chat re-emit; L41 / L-v2.85-e / L-v3.07-a discipline). Session detail in `docs/runtime/sessions/2026-05-25-v3.07-doc-patch-debt-cc0017c-v1.2.md`.
- 0 production mutation. 0 Supabase. 0 EF deploy. 0 cron. 0 dashboard. 0 D-01. 0 memory edit. Memory cap 19/30 unchanged.

## v3.08 honest limitations

- **Repo reconciliation of an already-live pipeline** — the three EFs (ai-worker v2.13.0, heygen-worker v2.0.0, youtube-publisher v1.7.0) were already deployed and verified-publishing (first Short `sfQvSM2Osus` was produced by them); this cycle reconciled their repo source + continuity docs to `main`. 0 new deploy, 0 production mutation, 0 D-01.
- **Byte-parity proof** — all 12 reconciled files' returned blob SHAs equalled the source-branch blob SHAs (definitive byte-identity). The `00_` files are rebuilds (no branch equivalent) verified by readback inspection, not SHA-match.
- **Self-caught process slip** — commit 1 initially shipped only 1 of 3 EFs (mis-scoped `files` array); flagged openly and completed via commit 1b; no wrong content written.
- **F-YT-FAILED-NO-RETRY brief text still names v1.7.0 internally** — the v1.8.0 re-point lives in this action_list only; the brief file edit is a CC/follow-up (not done this pass per PK scope). *(Update: the v1.8.0 EF + brief reconcile were subsequently committed `631fa93a`/`2ea0f83c` in the F-YT-FAILED-NO-RETRY prepare+commit stage; the brief now targets v1.8.0. The v1.7.0-internal note above refers to the milestone-pass state.)*
- **cc-0019 Unit B re-verified against v2.13.0 (post-milestone narrow patch 2026-05-26)** — confirmed STILL absent in ai-worker v2.13.0 (`8204a5c7`); the avatar A2/A3 changes are orthogonal to the eligibility preflight. No EF edit (absence is expected — Unit A's fill-gate covers the primary waste vector). The re-verify carry is discharged.
- **`sha` is not a reliable concurrent-edit guard on the GitHub bridge** — during this session two `00_` writes were already-landed from prior turns; the stale sha precondition would have overwritten rather than rejected. Read-before-write + readback is the real protection (L-v3.07-a reinforcement; bridge lesson recorded to memory).
- 0 production mutation. 0 new deploy. 0 Supabase. 0 EF deploy. 0 cron. 0 dashboard. 0 D-01. 0 memory edit (the bridge-lesson memory add was a separate prior turn). No decisions.md change.

---

## Changelog

- v1.0–v2.96: per commit history.
- v2.97–v3.00.1 (2026-05-20/21): cc-0016 Stage A/B/C/D applies + Stage D visual pass + reconciliations.
- v3.01 (2026-05-20T23:29Z, `83cd633c`): Cowork brief lifecycle gating WARN CLOSED; v2.94 convention validated; cc-0015 Gate 11 watch rank 1.
- v3.02 (2026-05-21T00:42Z, `a08945d3`): Dashboard mobile viewport CLOSED/PASS. *(DEFECT: full-file push clobbered v3.01 `00_` content.)*
- v3.02.1 / v3.02.2 (2026-05-21): clobber repair + mobile-fix SHA `d17b604` backfill (read-HEAD-first surgical).
- v3.03 (2026-05-21): Q-005 Option A trail recorded; brief v3.1.1; Q-005 OPEN next-fire watch.
- v3.04 (2026-05-21): Dashboard Slice 0A merge recorded at `3ec489b` (Pre-Phase 0; NOT real Phase 0).
- **v3.05 (2026-05-24): YouTube OAuth Production restore. Root cause = OAuth app in Google Testing publishing status → refresh tokens capped at 7 days (NY 7.00d / PP 7.92d after 5 May reconnect). PK moved app → Production + re-exchanged tokens (NY token_updated_at 10:48:22, PP 10:49:05 UTC); two live tests published (PP `ma6EG1fz4XQ` 10:57 UTC, NY `qp-ZGm8lNIo` 11:02 UTC). Backlog released (fresh ≥12 May → generated: NY 6 + PP 7 = 13, draining 2/tick jobid 34) + soft-retired (stale <12 May → archived_stale: NY 3 + PP 11 = 14). Migrations yt_token_verify_reset_one_pp_draft / yt_token_verify_reset_one_ny_draft / yt_backlog_release_fresh_softretire_stale (20260524). cc-0019 brief `81cb414`→`01a6cdf`, plan_review `a75c78f6`→`ff05c65e` clean — brief only. NEW F-YT-OAUTH-TESTING-MODE (root cause RESOLVED+recorded) / F-YT-FAILED-NO-RETRY / F-YT-EXPIRY-DISPLAY-FAKE. 4 D-01 (2 plan_review + 2 sql_destructive). 3 apply_migration + 2 publisher invokes. 0 cron / 0 EF deploy / 0 enablement / 0 hard delete / 0 friction.* / 0 cc-0015 / 0 Stage E / 0 Q-005 closure / 0 decisions.md / 0 dashboard edit. Session file `f9ddd51`; sync_state `119e54e7`; this action_list commit. Recorded via read-HEAD-first sha-passed surgical edits (sync_state blob `7d2b0eb8`, action_list blob `ecaf50f8`). Dashboard roadmap/PHASES leg N/A (operational). OWED: pre-compaction IG-publish-restore arc 4-way close.**
- **v3.06 (2026-05-25): Deployed↔repo drift close (repo reconciliation only — 0 production mutation). IG publisher v2.0.0→v2.4.0 synced (`bc78511e`, byte-verified — rollback hazard closed). 7 chat-applied 2026-05-24 migrations backfilled to `supabase/migrations/` (`8738c3a0`, recovered verbatim from `schema_migrations.statements`) — DB→repo migration drift CLOSED. F-YT-FAILED-NO-RETRY brief authored (`docs/briefs/yt-publisher-failed-no-retry.md`, `bee872aa`) + Unit-B patched to the live 3-bucket recovery spec (`a3f329f3`) + plan_review fired + CLEAN (`abf6f63c` partial/type-c → `f89c23cf` agree). 2 plan_review D-01 (non-mutating). cc-0019 CORRECTED: Unit A IMPLEMENTED (live `m.is_publish_eligible` + `fill_pending_slots` gate, migration `20260524091020`, D-01 `a2e5f3f5` + PK approval per header) — Unit B (ai-worker preflight) verified NOT implemented in deployed ai-worker v2.12.0. IG-arc OWED 4-way close discharged (publisher + migration legs). Session `1219aff0`. sync_state + action_list patched read-HEAD-first with blob SHAs passed. Dashboard PHASES leg N/A (operational).**
- **v3.07 (2026-05-25): Doc-patch debt — repo doc-only, 0 production mutation. cc-0017c main brief → v1.2 PATCHED + CORRECTED (`6d5c3ae6` apply-record backfill: status→APPLIED-WITH-VCHECK-CORRECTION, apply 2026-05-19, migrations `20260519004545`+`20260519005322`, 3 D-01 refs (v1.1 UUID truncated-in-source, not invented), V-B4 12-param pointer; → `81dcb683` correction softened an unverified cc-0017d v2.90 successor tag). cc-0017a / cc-0010A / cc-0011 / cc-0012 + cc-0017c/vchecks.md sub-file DEFERRED to CC (insufficient documented scope + full-file re-emit risk; GitHub bridge has no str_replace; 49–107 KB briefs). vchecks.md path LOCATED: `docs/briefs/cc-0017c/vchecks.md`. decisions.md STILL UNLOCATED (parked). Lessons NOT promoted — canonical L37–L65 home + numbering convention not locatable; PK option (c) defer to next-lesson-cycle/CC; nothing promoted by assumption. L-v3.07-a NEW candidate. L41 reinforced (compact sha-passed `00_` edits; no large-brief chat re-emit). Session file `b94c042d`. sync_state + action_list patched read-HEAD-first with blob SHAs passed. Dashboard PHASES leg N/A (doc-only). 0 Supabase / 0 D-01 / 0 dashboard / 0 decisions.md / 0 memory edit.**
- **v3.08 (2026-05-26): Avatar→YouTube Shorts milestone — repo reconciliation of an already-live pipeline (0 production mutation, 0 new deploy, 0 D-01). HeyGen talking-avatar → YouTube Shorts path RESOLVED end-to-end; deployed EF source reconciled to `main` from branch `feat/ai-worker-v2.13.0-avatar-youtube` (Option B bridge-native squash): ai-worker v2.13.0 (A2 avatar override + A3 narration branch) + heygen-worker v2.0.0 (async two-phase; 720×1280) + youtube-publisher v1.7.0 (`video_short_avatar` allow-listed → F-YT-PUB-AVATAR-EXCLUSION CLOSED). First live unlisted Short `sfQvSM2Osus` (NDIS-Yarns, draft `40f9fa25`, `m.post_publish` `5dbfcc0f`, 720×1280, 7.8 MB, 2026-05-25 11:42:07 UTC; no duplicate render). Landscape proof `ba5b34eb` retired `archived_stale`. F-YT-FAILED-NO-RETRY re-pointed → youtube-publisher v1.8.0 (still OPEN). cc-0019 live-version framing corrected v2.12.0→v2.13.0 on current-state surfaces; **Unit B re-verified absent against ai-worker v2.13.0 (`8204a5c7`) (post-milestone narrow patch) — re-verify carry DISCHARGED, Unit B STILL OPEN / optional**; v3.06 historical mentions left intact. Reconciliation commits `521b053b`+`194e0792` (EF parity), `04294d23` (continuity docs), `cd71f51c` (sync_state v3.08 banner), this action_list commit; all 12 EF+doc files byte-identical to branch (sha-verified). sync_state + action_list patched read-HEAD-first with blob SHAs passed (sync_state `29dffe9d`, action_list `ba02a6ff`; cc-0019 re-verify patch action_list `84e7e732`). No per-session file + Dashboard PHASES leg N/A (PK-scoped to the two `00_` files). 0 Supabase / 0 D-01 / 0 dashboard / 0 decisions.md / 0 memory edit (the bridge-lesson memory add was a separate turn).**
- **v3.27 (2026-06-02): video-worker caption-hierarchy polish (v3.1.3) DEPLOYED to prod — caption-styling-only P3 for `video_short_kinetic_voice`. FF-merge `f0c9682..fc3ec12` + push `main`; branch `polish/video-worker-caption-hierarchy` (commits `f9be78c` styling `index.ts` +5/−5 + `fc3ec12` VERSION `v3.1.2→v3.1.3`). Shipped: caption `font_size` 52→40px, `font_weight` 700→500, scrim `opacity` 0.55→0.62; colour `#FFFFFF` + `CAP_MAX_CHARS` 64 unchanged; comments→40px. Geometry/timing/chunking/audio/voice/`getVoiceId`/templates/webhook/queue/DB/stat/non-kinetic UNCHANGED. CCH code review PASS + CCB visual review PASS. Deployed via `./scripts/safe-deploy.sh video-worker --allow-warn` (wrapper only): 6 targeted EF-drift refreshes (incl. GitHub raw-CDN propagation polling, then final post-deploy refresh) → lifecycle A-LE(3.1.2)→B-FD(3.1.3 pending)→A-LE(3.1.3 in-spec); `--check-only` BLOCK exit 1 (expected gate) → `--check-only --allow-warn` PASS → `--allow-warn` deploy exit 0; repo==deployed hash `0991ff6554fbf7c8dcba1debe47e179efcf95d9ff315ce9f41b53fd4b4088bd7`; health GET `{"version":"video-worker-v3.1.3"}` HTTP 200. D-01 CCD-inline (PK `ef_deploy` phrase recorded verbatim) — no `m.chatgpt_review` row fabricated. L-v3.27-a: safe-deploy reads the cached `m.vw_ef_drift_current` register; mandatory path = merge → targeted refresh → B-FD → `--allow-warn` deploy → final refresh → A-LE; GitHub raw-CDN propagation lag can briefly mis-report A-LE post-push (poll until persisted B-FD before deploying). **F-CREATOMATE-CAPTION-HIERARCHY CLOSED.** F-VOICE-LOWVOL-BACKFILL remains OPEN P3 (separate carry); `polish/video-worker-caption-hierarchy` branch cleanup deferred (needs separate approval). 1 EF deploy / 1 FF-merge / 6 advisory drift-log refreshes; 0 schema / 0 migration / 0 cron/queue / 0 publisher/social / 0 image-worker / 0 webhook / 0 render / 0 Creatomate-ElevenLabs call / 0 production draft mutation / 0 backfill / 0 code change beyond the approved commit. Session file `docs/runtime/sessions/2026-06-02-v3.27-video-worker-caption-hierarchy-deploy.md`; sync_state + action_list patched read-HEAD-first with blob SHAs captured. Dashboard PHASES leg N/A (operational).**
