# ICE — Sync State Index

> **This file is the lightweight session pointer index.** Per-session detail lives at `docs/runtime/sessions/YYYY-MM-DD-{slug}.md`.
>
> Restructured 2026-05-03 (G1). See `docs/runtime/archive/sync_state-pre-2026-05-03.md` for pre-restructure history.

---

> **✅ v3.71 VOICE PATH PRODUCTION RECOVERY RUNBOOK — `creatomate+elevenlabs` root cause + remediation options LANDED (docs-only; nothing implemented, 2026-06-20):** The read-only production-recovery plan for the voice-format render failures is recorded as a durable runbook BEFORE any secret/config action. **0 secret read / 0 secret write / 0 env-config change / 0 worker invocation / 0 ElevenLabs call / 0 Creatomate call / 0 render creation / 0 DB mutation / 0 code edit / 0 deploy / 0 draft re-approval.** **Runbook:** `docs/briefs/voice-path-production-recovery-runbook.md`. **Confirmed (read-only, project `mbkmaxqhsohbtwsqolns`):** the `creatomate+elevenlabs` failures are GENUINE voice-format failures — **all 9 failed renders are `video_short_kinetic_voice`** (Property Pulse ×7 last 2026-06-15, NDIS-Yarns ×2 last 2026-06-18 19:00; `has_voiceid_error=true`); draft format matches render-log format; narration text present. **NOT** a routing issue, **NOT** a Creatomate issue, **NOT** a storage issue, **NOT** a silent-kinetic-short false alarm. **Root cause:** `getVoiceId(clientSlug)` returns **null** → `video-worker` throws `No ElevenLabs voice ID configured for client_slug=<X>` **fail-loud BEFORE** TTS / Creatomate submit / storage (the throw at ≈line 520; on throw sets `video_status='failed'` + writes `m.post_render_log` `p_render_engine='creatomate+elevenlabs'` status `failed`). No provider spend on the failures. **`client_slug` NOW populated** (resolves the UUID-fallback for future renders): **NDIS-Yarns=`ndis-yarns`**, **Property Pulse=`property-pulse`**. **Candidate secrets** (`getVoiceId` resolves exact `ELEVENLABS_VOICE_ID_<SLUG_UPPER>` then alias): Property Pulse = **`ELEVENLABS_VOICE_ID_PP`** (alias) or **`ELEVENLABS_VOICE_ID_PROPERTY_PULSE`** (exact); NDIS-Yarns = **`ELEVENLABS_VOICE_ID_NDIS`** (alias) or **`ELEVENLABS_VOICE_ID_NDIS_YARNS`** (exact). **PP is the likely missing/regressed mapping.** **MATERIAL FINDING (verification-blocking):** `video-worker` selection is approval-gated (`approval_status IN ('approved','published')`, ≈line 568); the **3 pending voice drafts are all `approval_status='rejected'`** (NY `video_short_kinetic_voice`, NY `video_short_stat_voice`, PP `video_short_stat_voice`) → they will **NOT render naturally** unless re-approved or a new approved voice draft exists; there are **0 approved/published pending voice drafts** → the "let one render" verification needs an APPROVED voice draft first. **Remediation options (PK-gated, none executed):** **A.** verify/set the required ElevenLabs voice-ID secret(s) — root-cause fix, **secret write = PK hard gate**, needs the voice-ID values; **B.** hold the `*_voice` formats until secrets confirmed (stop-the-bleeding, non-voice unaffected); **C.** later code hardening — explicit per-client voice mapping replacing the substring heuristic (own PK-gated code lane + external review + deploy gate). **Verification (post-fix):** confirm guard no longer throws → obtain an APPROVED voice draft (PK re-approves a rejected one OR a new one lands) → let EXACTLY ONE render run (natural cron OR single PK-approved invocation) → check `m.post_render_log` for `succeeded`/`creatomate+elevenlabs`/`has_voiceid_error=false` → spot-check non-voice unaffected. **Rollback/safety:** if voice still fails after the secret is set, **do NOT retry blindly** — capture the new error, stop, surface to PK; keep non-voice formats unaffected; never publish broken voice assets; one render at a time before re-enabling at volume. **This pass touches `00_sync_state.md` + `00_action_list.md` + the new runbook only.** Carries unchanged; **Branch B NOT AUTHORISED**; AVATAR_SHADOW_TELEMETRY OFF; **P2 Creative Format Evidence NOT READY**; **Engagement A1 cron observation still pending.** **DO NOT IMPLEMENT YET** — awaiting PK's choice of option + the voice-ID values. Authority impact: **none** (planning/runbook record; live render paths / avatar selection / A2 pin / as-built flow / T1 unchanged).

---

> **✅ v3.70 RENDER PROVIDER CAPABILITY AUDITS — HeyGen + Creatomate briefs LANDED (docs-only; nothing implemented, 2026-06-20):** The two read-only provider-specialist audits are recorded as durable docs before any Render Intelligence implementation. **0 DB / 0 migration / 0 code / 0 RPC / 0 EF deploy / 0 provider call / 0 token / 0 render creation / 0 avatar-asset creation / 0 marker write / 0 selection change / 0 AGP activation.** **Briefs:** `docs/briefs/render-provider-heygen-capability-audit.md` + `docs/briefs/render-provider-creatomate-capability-audit.md`. **HeyGen (identity-based):** TWO surfaces — **(1) `heygen-worker` RENDER path** (talking_photo_id + voice_id + text TTS + single scene + hardcoded `720x1280` + submit/poll); **(2) `heygen-avatar-creator` / `heygen-avatar-poller` ASSET-PROVISIONING path** (photo-avatar generate/train/group/look extraction; **reverse-engineered `api2.heygen.com` look-endpoint risk**; standing **"DO NOT REDEPLOY"**, extra caution). Cost opaque at render time, `credits_used` null. Safe future slice: **estimated render-cost telemetry, NO provider call**. Deferred: dimensions, multi-character, templates, webhooks, captions, quota/usage API, new assets. **Creatomate (composition-based):** **SOURCE mode (NOT templates)** — `video-worker` builds the full composition JSON in code for the **kinetic/stat NON-avatar** paths (text/shape/image/audio elements, **manual captions**, **ElevenLabs TTS** for voice; hardcoded `1080x1920`). Returns credits/cost but **`render_spec` null**. **Major reliability risk: blocking in-request poll near the Supabase EF time limit** (same wall-clock failure class fixed for HeyGen). Safe future slice: **`render_spec` enrichment, observability-only**; larger follow-up: **async-ify Creatomate polling (HeyGen-style submit/poll)** — a separate gated code lane. Deferred: templates/modifications, auto-subtitles, webhooks, video overlay, new aspect ratios. **Shared RI abstraction recorded:** HeyGen identity-based / Creatomate composition-based; future **provider-neutral interface** models **submit · poll · cost (reported vs estimated) · capabilities · renderMode (identity vs composition)**; **`m.post_render_log` stays the telemetry spine**; **provider-specific IDs/payloads stay inside adapters.** **This pass touches `00_sync_state.md` + `00_action_list.md` + the 2 new briefs only.** Carries unchanged; **Branch B NOT AUTHORISED**; AVATAR_SHADOW_TELEMETRY OFF; **P2 Creative Format Evidence NOT READY**; **Engagement A1 cron observation still pending.** Authority impact: **none** (audit records; live render paths / avatar selection / A2 pin / T1 unchanged).

---

> **✅ v3.69 CREATIVE + RENDER INTELLIGENCE — character-architecture brief LANDED (docs-only; nothing implemented, 2026-06-19):** Architecture for how Creative Intelligence (CI) and Render Intelligence (RI) work around Character Model v0. **0 DB / 0 migration / 0 code / 0 RPC / 0 EF deploy / 0 provider call / 0 marker write / 0 avatar-selection change / 0 AGP activation / 0 CI-or-RI implementation.** **Brief:** `docs/briefs/creative-render-intelligence-character-architecture.md` (companion to `character-model-v0-brand-host-designation.md`). **Definitions:** CI = "what to say + who says it"; RI = "how to physically produce it"; Character Model v0 = the identity CONTRACT between them. **Separation:** CI owns topic / angle / hook / platform-targeting / format-recommendation / narrator_mode / tone / series-structure / brand-host-or-persona **SUGGESTION**; RI owns provider / avatar-vs-non-avatar path / `render_style` / asset-eligibility / cost / latency / quality / platform-capability / fallback. **Boundary (load-bearing):** CI may SUGGEST identity/persona; RI RESOLVES identity → eligible asset/provider; **CI must NOT directly pick avatar assets; RI must NOT pick topic/angle/hook.** **Realistic vs animated** = asset-level `render_style` / RI decision — style variants of the SAME host identity, NOT a separate character, NOT a CI decision; active hosts realistic, animated dormant/unproven (do NOT activate animated; do NOT conflate render_style with the avatar-vs-non-avatar path). **Instrument LATER (record-only):** CI attribution completeness (host identity, narrator_mode, platform, format, tone, possibly hook/angle/topic if already available) + Render-path telemetry (provider, render_style, cost, latency, success/failure, fallback_taken, quality/outcome). **DO NOT automate yet:** no Creative performance panel; no live avatar/persona/style-driven selection; no provider auto-switching; no animated-variant activation; no Branch B; no AGP activation; **no performance optimisation until a real engagement signal exists.** **Recommended future slices (DO NOT IMPLEMENT YET):** CI slice = Creative attribution completeness (record-only); RI slice = Render-path telemetry (record-only) — each its own PK-gated lane. **Dependencies:** (1) **Engagement A1 observation must land** before judging own-brand evidence (A1 v14.5.0 deployed, OUTCOME-PENDING); (2) **AGP Phase 3.3 ON + parity** before character/persona/style drives LIVE selection (telemetry OFF); (3) **real engagement/audience signal** before optimisation; (4) any future EF/code deploy = **PK gate + `--no-verify-jwt`**; (5) the **host marker DB write stays separate + gated** (Brand Host Designation, auditable migration). **This pass touches `00_sync_state.md` + `00_action_list.md` + the new brief only.** Carries unchanged; **Branch B NOT AUTHORISED**; AVATAR_SHADOW_TELEMETRY OFF; **P2 Creative Format Evidence NOT READY.** Authority impact: **none** (architecture record; live avatar selection / A2 pin / as-built flow / T1 unchanged).

---

> **✅ v3.68 CHARACTER MODEL v0 + BRAND HOST DESIGNATION — planning brief LANDED (docs-only; nothing implemented, 2026-06-19):** The approved avatar/character planning model + the clean Session-2 governance review are recorded. **0 DB / 0 migration / 0 code / 0 RPC / 0 EF deploy / 0 provider/HeyGen call / 0 marker write / 0 avatar-selection change / 0 AGP activation.** **Brief:** `docs/briefs/character-model-v0-brand-host-designation.md`. **(Terminology: ICE = the Invegent Content Engine product; Invegent = company/platform owner.)** **Model:** avatar/character = a GOVERNED IDENTITY SYSTEM, not merely a HeyGen asset — layered **intent → identity → asset → provider**; distinguishes **brand host / narrator mode / stakeholder persona / character identity / avatar asset / provider identity**; sequencing **brand host FIRST → stakeholder personas → character library**; **one host per brand = current ceiling.** **Live inventory (VERIFIED read-only this pass):** Invegent **0** avatars (host asset required later); CFW **0** (deferred); **NDIS-Yarns 14** (1 active A2-pinned HeyGen-backed `83ff167d-a844-4e1c-9d1a-d8ff257c11bc` "Support Coordinator (Realistic)"); **Property Pulse 14** (1 active `d6c422fb-9dc5-4bcc-aba6-2e6e942f2cdd` "Buyer's Agent (Realistic)"); **all `is_primary`/`is_default_host` markers false/unset (0 set).** Each of NY/PP = **7 realistic + 7 animated** (active hosts realistic). **VERIFIED CORRECTION:** the dormant non-realistic style is **`render_style='animated'`, NOT `'cartoon'`** — the planning note's "cartoon" maps to the existing `animated` inventory (stays dormant/future; style selection DEFERRED; no new character fields; realistic/animated treated as asset-level render_style/capability). **Session-2 governance: CLEAN TO DOC** — NY/PP active avatars are stock/licensed HeyGen (`avatar_type=stock`, `consent_required=false`, no uploaded source footage, no custom-generation record) → **no personal-likeness consent gap** blocks docs or future dormant-marker planning; light carry = optional later **PK-gated provider lookup** to verify the HeyGen stock IDs. **Live-behaviour boundary (precise wording):** markers are **non-consumed by LIVE selection** (live `lookupAvatar` does NOT read `is_primary`/`is_default_host`; the **A2 pin** drives selection) — **NOT** "non-consumed everywhere": the **dormant** shadow resolver `resolve_and_record_avatar_shadow` DOES read them for telemetry ordering, and it is **OFF** (`AVATAR_SHADOW_TELEMETRY` unset; `r.avatar_resolution_shadow` rows = 0, verified). **No Branch B, no persona-driven selection, no marker-driven live selection.** **Recommended NEXT (separately GATED — NOT now): Brand Host Designation** — set **only `is_default_host=true`** for `83ff167d…` + `d6c422fb…` (exclude Invegent until an asset exists; exclude CFW) via **auditable migration only**, **PK gate + external review required**, **zero live-selection change**, **reversible**. **Pillar links:** Creative Intelligence → characters become **attribution dimensions LATER** (not optimised — **P2 Creative Format Evidence NOT READY**); Render Intelligence → characters = the **"who"**, render intel = the **"how/provider/style/cost/quality"**; AGP → **selection-driving** use waits on **Phase 3.3 activation + parity** (runbook landed, telemetry OFF). **This pass touches `00_sync_state.md` + `00_action_list.md` + the new brief only.** Carries unchanged; **Branch B NOT AUTHORISED**; AVATAR_SHADOW_TELEMETRY OFF. Authority impact: **none** (planning record; live avatar selection / A2 pin / as-built flow / T1 unchanged).

---

> **✅ v3.67 ICE STATE-OF-RECORD RECONCILIATION — OPINTEL-V1 closed · AGP Phase 3.3 runbook landed (OFF) · Engagement A1 deployed (outcome-pending) · P2 Creative Format Evidence NOT READY (docs-only, 2026-06-19):** Current ICE system truth captured **before** any avatar/characters, Creative Intelligence, or Render Intelligence work opens. **(Terminology: ICE = the Invegent Content Engine PRODUCT; Invegent is the company/platform owner — ICE is one product within Invegent, not the owner.)** **This pass touches `00_sync_state.md` + `00_action_list.md` only — 0 code / 0 DB / 0 migration / 0 RPC / 0 EF deploy / 0 provider/token call / 0 dashboard / 0 AGP activation / 0 new implementation lane.** **Verified read-only this pass:** CE `main`=local=origin **`2b436d9`** (0/0); **insights-worker deployed = `insights-worker-v14.5.0`** (function version 69), **`verify_jwt=false`** (header-less GET 200 + authoritative); **`r.avatar_resolution_shadow` row count = 0** (AVATAR_SHADOW_TELEMETRY still OFF — not activated). **(1) OPINTEL-V1-ICE-HEALTH-CARD → LIVE / RECONCILED / CLOSED** — read-only ICE Health card on `/overview` (dashboard.invegent.com, dashboard prod HEAD `ea45ab9`; zones Publishing Health + Data Freshness); recorded v3.66; unchanged this pass. **(2) AGP D-01 Gate-3 Phase 3.3 activation+soak RUNBOOK → COMMITTED + PUSHED (planning only, nothing activated)** — `docs/briefs/agp-d01-gate3-phase3.3-activation-soak-runbook.md` (committed at CE `f707908`; Session-2-reviewed CLEAN; non-blocking notes applied). **AVATAR_SHADOW_TELEMETRY remains OFF (0 shadow rows); no activation, no avatar-selection change, no Branch B, no `lookupAvatar` change.** Phase 3.3 flag-enable stays a standing PK BLOCK — the runbook documents the gated procedure; it authorises nothing. **(3) ENGAGEMENT-SIGNAL-RECOVERY A1 → MERGED + DEPLOYED, OUTCOME-PENDING (NOT closed)** — insights-worker v14.4.1→**v14.5.0** (commit `2b436d9`, merged+pushed; deployed fn version 69, `--no-verify-jwt`, `verify_jwt=false` preserved). Change: reach metric fail-safe fallback **`["post_impressions_unique", "post_impressions"]`** (post_impressions_unique FIRST so working cases unchanged; `fetchSingleMetric` records every attempt in `raw_payload.calls`; first 200 wins; all-fail → reach NULL, no false zero). **0 DB / 0 migration / 0 schema / 0 RPC / 0 token / 0 provider/Graph probe / 0 dashboard / 0 AGP change.** **Observation PENDING (A1 is DEPLOYED, NOT CLOSED):** await the natural Invegent FB cron (~03:15 UTC), then inspect `m.post_performance.raw_payload.calls` for Invegent (`93494a09…`). **Outcome A:** `post_impressions` returns 200 → Invegent reach blind spot RECOVERED. **Outcome B:** `post_impressions` also #100 → A1 is a safe no-op → next lane = **PK-gated live Graph probe** for the real account-scoped reach metric. (Baseline read: Invegent 36/36 reach NULL, `post_impressions_unique` 400/#100 categorically; Property Pulse gets 200 per-post on the SAME metric → the Invegent failure is account/page-scoped, not a metric-name typo → Outcome B is the likely result; A1's value is the risk-free fail-safe self-probe + definitive per-metric telemetry.) **(4) P2 CREATIVE FORMAT EVIDENCE → NOT READY / BLOCKED** — the engagement/reach signal is too weak / partially blind (Invegent reach blind spot open pending A1 outcome; engagement basis still constrained by the #10 fields-permission gap). **Do NOT build the Creative panel yet** — format-performance evidence is not trustworthy enough to drive a creative-intelligence surface. **(5) YOUTUBE PRIVACY → HISTORICAL ONLY (not a current blocker)** — recent YouTube publishes are PUBLIC; the current YouTube constraint is **low audience / distribution**, not privacy. The privacy concern is closed/historical. **(6) NEXT STRATEGIC AREAS → PLANNING ONLY, NOT ACTIVE IMPLEMENTATION:** **avatar / characters · Creative Intelligence · Render Intelligence.** Upcoming planning areas; **no implementation lane is open for any of them** (avatar/characters sits behind the AGP gate map + Branch B NOT AUTHORISED; Creative Intelligence is gated on the P2 evidence readiness above). **Carries unchanged:** AGP gate map (#1/#2/#2-migration CLOSED; #3 Phase 3.2 CLOSED; **Phase 3.3 flag-enable BLOCKED**; #4 cutover BLOCKED); Branch B NOT AUTHORISED. Authority impact: **none** (state-of-record reconciliation; no T1 / architecture / decision-tree change).

---

> **✅ v3.66 OPINTEL-V1-ICE-HEALTH-CARD — Operator Intelligence v1 FIRST SLICE SHIPPED TO PRODUCTION (P5 Operator; dashboard repo, read-only; CE-register reconciliation only, 2026-06-19):** The first Operator Intelligence slice is **live**. **What shipped — in the SEPARATE `invegent-dashboard` repo (dashboard.invegent.com), NOT this content-engine repo:** one read-only **ICE Health** card on `/overview` with two user-facing zones — **Publishing Health** (per client × channel delivery; enabled+never-delivered or enabled+silent>7d = RED, recent(14d) failure-rate ≥25% = AMBER, disabled/paused = GREY; backlog/coverage deliberately excluded from card status) and **Data Freshness** (`m.post_format_performance` windows 0/7/30/90; 30/90 stale>48h or NULL = RED; 0/7 descriptive; honesty caveats rendered: descriptive-not-causal / algorithmic attribution Phase-2, and silence is a fixed v1 heuristic — no per-channel cadence table yet). **Production evidence:** dashboard `main` = local = remote HEAD **`ea45ab9`**, ahead/behind **0/0**; **Vercel production deployment READY** (`dpl_7yDTHAJTbGB5xmFdaCocZJ4qF9YB`); production URL **https://dashboard.invegent.com/overview**. **Dashboard files (3, +357):** `actions/ice-health.ts` (NEW, `'use server'` read-only — service-role reads via the existing `exec_sql` helper), `components/overview/IceHealthCard.tsx` (NEW, server component), `app/(dashboard)/overview/page.tsx` (+6). **Runtime safety: NO content-engine runtime change — 0 DB / 0 RPC / 0 EF / 0 migration / 0 grant / 0 write-path.** Read-only over existing tables (`m.post_publish`, `c.client_publish_profile`, `c.client_channel`, `m.post_format_performance`, `c.client`); no secret reaches the client bundle (server action + server component + type-only import). **Gate trail:** built → `tsc` clean → 18/18 threshold assertions → external review `fae67bcd` (agree) → **Session 2 independent review CLEAN** → PK approved preview → preview READY → **PK visual-checked the preview** → required microcopy fix applied (`"ICE health"` → `"ICE Health"`, `ea45ab9`) → PK approved production promotion → ff-merge + prod deploy READY. **Remaining follow-up:** a **usage soak** before choosing the next Operator Intelligence slice; **default next candidate after soak: "Needs You Now"** (unified prioritised action feed); **alternative: "Fresh Samples Panel"**; **"24h Pulse" deferred** (overlap with existing surfaces). **This pass touches `00_sync_state.md` + `00_action_list.md` only — a CE-register reconciliation of a dashboard-repo shipment for cross-repo traceability; 0 dashboard-code / 0 DB / 0 EF / 0 deploy / 0 next-slice / 0 AGP / 0 Creative-Intelligence this pass.** Authority impact: **none** (read-only operator visibility; as-built decision flow + T1 unchanged).

---

> **✅ v3.65 F-INSIGHTS-WORKER-COLLECTION-RUNTIME — insights-worker v14.4.1 cap 50→25 DEPLOYED + VERIFIED (large-client collection now in budget; code-only, 2026-06-19):** The follow-up opened at v3.63 is closed. **Problem:** v14.4.0 scoped collection to one client/run (fixing freshness) but at the observed ~3 s/post a 50-post client (NDIS-Yarns 253 eligible, Property-Pulse 240) ran ~150 s — still hitting the ~150 s EF wall-clock + the 120 s cron `net.http_post` timeout (freshness itself was NOT at risk — aggregation-first commits 30/90 before collection). **Fix (smallest safe, one constant):** `MAX_POSTS_PER_CLIENT` **50 → 25** — pure tuning, version `v14.4.0 → v14.4.1`; aggregation-first, the per-client `client_publish_profile_id` selector + D-A2 fallback, `processClient`, `fetchPostInsights`, `METRICS_TO_TRY`, token handling, `raw_payload`, response shape all **byte-identical**. No parallelization (rejected as bigger/less safe — touches engagement-sensitive code); **0 DB / 0 migration / 0 cron / 0 grant.** **Deploy facts:** commit **`4077b02`** (ff-merged + pushed to `main`, origin **0/0**); **insights-worker function version 68 DEPLOYED** (PK-run, `--no-verify-jwt`, `verify_jwt=false` preserved). **Gates:** `deno check` exit 0 → branch-warden **safe** (1-file) → external review **`8ee7b35c`** (partial/escalated — generic caution, no concrete defect; freshness structurally independent of the cap) → **PK approved cap=25**. **Post-deploy verification (live, read-safe — insights collection does not post externally):** GET → v14.4.1 / `verify_jwt=false`; **large-client run NDIS-Yarns (253 eligible) → HTTP 200 in 53.2 s** (was ~150 s @ 50), `scope_mode=client_scoped`, **`total_processed=25`** (cap honored), 25/25 succeeded / 0 failed, **`first_time=8`** (the 8 never-collected posts drained first — ordering intact), **`windows_computed=50`** (aggregation-first preserved, windows 30/90 fresh). Empirical ~2.1 s/post → even doubled latency stays under budget. **Coverage:** large clients cycle full inventory in ~10 days via the per-client daily crons — inside the 30-day window, so 30/90 stays fully populated. **Carry change: F-INSIGHTS-WORKER-COLLECTION-RUNTIME → CLOSED.** **Register commit pending PK instruction — this pass PREPARES the reconciliation; no register commit/push yet.** Brief `docs/briefs/f-insights-worker-collection-runtime.md`; result `docs/briefs/results/f-insights-worker-collection-runtime.md`. Authority impact: **none** (collection-throughput tuning; as-built flow + T1 unchanged).

---

> **✅ v3.64 F-PUBLISHER-ASSETGUARD-LINKEDIN — LinkedIn interim assetless-publish guard DEPLOYED + VERIFIED (publisher asset-guard coverage complete; code-only, 2026-06-19):** The last publisher without the interim asset guard is closed. **What shipped:** the proven FB interim asset guard ported to BOTH LinkedIn EFs — a new pure `guard.ts` (`classifyLinkedinFormat` + `decideLinkedinAssetGuard`, byte-identical copy per function dir) so a draft whose `recommended_format` needs a rendered visual (image_quote/carousel/video) is **never silently published as text** when the asset is missing/failed/pending. Both EFs call it with `{ mediaPublishSupported: false }` (neither has a media path today): **text → publish (existing path unchanged); any non-text → BLOCK** (`li_media_publish_not_supported_interim` / `unknown_asset_required_format_default_deny`) — queue `status='skipped'` + `last_error='asset_guard_blocked:<reason>'` + `m.post_publish status='failed'` audit row, draft preserved, **never text-fallback**; plus a defensive `platform!=='linkedin'` isolation skip (mirror YT v1.12.0 / IG v2.0.0). **Deploy facts:** commit **`891f6eb`** (rebased onto `51415ed`, ff-merged + pushed to `main`, origin **0/0**); **`linkedin-zapier-publisher` v1.1.0→v1.2.0 DEPLOYED** (function version 33, **PK-run** via CLI `--no-verify-jwt`, **`verify_jwt=false` preserved**); **`linkedin-publisher` v1.2.0→v1.3.0 committed but NOT deployed** (repo-only, gated on B24/F06). **Gates:** rebase clean → `deno check` exit 0 (4 files) + `deno test` **22/22** → branch-warden **safe** → pre-deploy queue safety (0 non-text actionable LinkedIn rows → 0 stranded) → external review **`e1d81a76`** (agree/proceed; `reviewed_input_hash` `fbafe50e…`). **Post-deploy verification (read-only — publisher NOT live-invoked, since a live run would publish):** `get_edge_function` → v1.2.0 / `verify_jwt=false` / guard.ts deployed byte-identical; header-less GET → 200 `{version:"linkedin-zapier-publisher-v1.2.0"}`; db-rls false-positive check → **0 LinkedIn asset_guard blocks anywhere**, the 7 text LinkedIn queue rows intact/publishable (the 9 existing asset_guard blocks are all FB's pre-existing guard). **PK decision honored: GUARD-ONLY** — `asset_backstop.ts` parity (FB/IG/YT carry it; LinkedIn doesn't) is a **future follow-up**, not this lane (redundant now — the guard blocks every non-text format before any POST). **Carry change: F-PUBLISHER-ASSETGUARD-LINKEDIN → CLOSED.** **Note:** the orchestrator's `supabase functions deploy` attempt was denied at the permission layer → correctly reverted to PK-run (deploy stays a hard gate). **Register commit pending PK instruction — this pass PREPARES the reconciliation; no register commit/push yet.** Brief `docs/briefs/f-publisher-assetguard-linkedin.md`; result `docs/briefs/results/f-publisher-assetguard-linkedin.md`. Authority impact: **none** (publisher safety guard downstream of the decision tree; as-built flow + T1 unchanged).

---

> **✅ v3.63 F-INSIGHTS-WORKER-WINDOW30-90-FRESHNESS — insights-worker v14.4.0 RECOVERY DEPLOYED + VERIFIED (windows 30/90 unfrozen; code-only EF change, 2026-06-19):** The stale worker-owned `m.post_format_performance` windows 30/90 are **recovered and live-verified fresh.** **Root cause (proven, read-only):** the insights-worker EF was killed by the ~150s wall-clock limit on every run (HTTP 504/546 at ~150.5s) — its handler swept **ALL 4 FB clients sequentially** while **ignoring the per-client cron body selector** the 4 crons already send (`{"client_publish_profile_id":"<uuid>"}`, jobid 87-90, staggered 03:00/05/10/15), so the tail `computeFormatPerformance` (sole writer of windows 30/90 since Fix-2 narrowed `refresh_post_format_performance` to `{7,0}`) never committed → windows frozen since 2026-06-12/06-13. Collection looked fresh because per-post `m.post_performance` upserts land incrementally (masking the freeze); cron `job_run_details` read "succeeded/1 row" only because `net.http_post` is fire-and-forget (records the enqueue, not the EF's 504). **Fix (insights-worker v14.2.0 → v14.4.0, single file `supabase/functions/insights-worker/index.ts`):** (#1) **aggregation-first** — `computeFormatPerformance(all active client_ids)` runs FIRST, global, before collection, so the cheap window write can never be starved (a wall-clock kill is uncatchable; running the tail first is the durable freshness guarantee); (#2) **honor the existing cron selector** — parse the body defensively and scope collection to ONE client/run; **D-A2 safe default** — missing/invalid body → aggregation-only, collect nothing (`no_client_scope`), never the full sweep; additive `scope_mode` response field. Collection/metric/engagement/token/`raw_payload` logic **byte-identical**. **Deploy facts:** commit **`e0fc5e4`** merged + pushed to `main` (origin **0/0**); deployed **insights-worker function version 67** via `--no-verify-jwt` (PK-run); **`verify_jwt=false` preserved** (authoritative `get_edge_function` + header-less GET 200). **Gates:** ef-builder (isolated worktree, `deno check` exit 0) → branch-warden **safe** → external **design** review `6754590e` (escalated → PK resolved; decisions **D-A2** + keep global aggregation) → external **final-diff** review `53724307` (**agree**/proceed) → PK deploy authorization → merge+push (orchestrator) → PK-run deploy. **Live verification (read-only):** GET → `insights-worker-v14.4.0`; bodyless POST → `scope_mode=no_client_scope`, processed 0, **`windows_computed=50`**, 3.8s; client-scoped POST (Invegent) → `scope_mode=client_scoped`, **36/36** collected, 200; DB confirms **windows 30 & 90 `computed_at` ≈ 2026-06-19 04:42 UTC (50 distinct per-row worker timestamps)**, replacing the frozen 06-13/06-12; windows 0/7 unaffected (cron-owned 03:15). **Residual (mitigated, NOT freshness-blocking):** at the observed ~3s/post, the two large clients **NDIS-Yarns (253 eligible)** and **Property-Pulse (240)** cap at `MAX_POSTS_PER_CLIENT=50` so their *collection* phase runs ~150s and may still 504 — but **aggregation-first commits 30/90 in ~3.8s before any collection**, so windows stay fresh regardless, and per-post upserts make incremental progress. **Carry change: F-INSIGHTS-WORKER-WINDOW30-90-FRESHNESS → CLOSED (recovered + verified).** **OPENED this pass: `F-INSIGHTS-WORKER-COLLECTION-RUNTIME` (P3/P4)** — bring large-client collection fully under budget (lower `MAX_POSTS_PER_CLIENT` to ~25-30 and/or parallelize per-post Graph fetches); separate code lane, not freshness-blocking. **Other carries unchanged.** **Register commit pending PK instruction — this pass PREPARES the reconciliation; no register commit/push yet.** Brief `docs/briefs/f-insights-worker-window30-90-freshness.md`; result `docs/briefs/results/f-insights-worker-window30-90-freshness.md`. Authority impact: **none** (downstream telemetry freshness; as-built decision flow + T1 unchanged).

---

> **✅ v3.62 AGP D-01 GATE-3 — AUTHENTICATOR-REVOKE-CLEANUP CLOSED: inherited `authenticator` SELECT on `r.avatar_resolution_shadow` REVOKED (migration applied + pushed; docs-only register reconciliation, 2026-06-18):** The Gate-3 follow-up posture cleanup **`AGP-GATE3-AUTHENTICATOR-REVOKE-CLEANUP` is CLOSED** — **this pass touches `00_sync_state.md` + `00_action_list.md` only: 0 DB / 0 migration / 0 deploy / 0 code / 0 flag-enable / 0 Branch B / 0 marker backfill / 0 resolver change / 0 production action this pass.** **What landed (prior step, NOT this pass):** commit **`ee5d432`** ("fix(agp): revoke authenticator select on avatar shadow telemetry") pushed the migration to `main`; the migration was **applied successfully as `20260618092646_agp_gate3_revoke_authenticator_select_avatar_shadow`**. **The migration does exactly one thing:** `REVOKE SELECT ON r.avatar_resolution_shadow FROM authenticator` (bare REVOKE → idempotent no-op if re-run; touches nothing else — not the resolver, service_role grants, RLS, policies, search_path, table shape, EFs, the flag, or markers). **End-state (recorded from the apply step):** the inherited **`authenticator` SELECT on `r.avatar_resolution_shadow` is removed**; **`anon` and `authenticated` remain without SELECT** (`anon` was never targeted — it already held none); **`service_role` retains required access**; **RLS remains ENABLED with ZERO policies** (deny-by-default unchanged); **telemetry row count remained 0**; **no Edge Function deploy occurred**; **`AVATAR_SHADOW_TELEMETRY` remains OFF/unset**; **no Branch B, no marker backfill, no live resolver change.** **Not a live exposure (hardening only):** schema `r` is not REST-exposed (PGRST106) + deny-by-default RLS; `authenticator` is the pre-role-switch connection role, not a client-reachable principal. **Independently verified this pass (read-only, LOCAL GIT ONLY — no DB/production read):** commit `ee5d432` is on `main` and contains exactly one migration file (38 lines, single REVOKE). **Repo↔prod migration-timestamp drift recorded:** the repo SQL file is named **`20260618133000_agp_gate3_revoke_authenticator_select_avatar_shadow.sql`** while the applied version is **`20260618092646_…`** — identical name suffix, different timestamp (the known repo↔prod drift; end-state correct, repo provenance carries the timestamp delta). **Carry change: `AGP-GATE3-AUTHENTICATOR-REVOKE-CLEANUP` → CLOSED.** **Gate map carries unchanged:** #1/#2/#2-migration CLOSED; #3 Phase 3.2 APPLIED+DEPLOYED+VERIFIED/CLOSED; **Phase 3.3 flag-enable remains BLOCKED pending explicit PK instruction**; #4 cutover BLOCKED. **Other carries unchanged:** F-INSIGHTS-WORKER-WINDOW30-90-FRESHNESS (P3 OPEN); D-002 security remediation CLOSED; F-AVATAR-RENDER-LATENCY-RECOVERY (PARTIAL/OPEN); F-SLOT-SCHEDULE-FIDELITY (P4/WATCH); YT positive-publish (WATCH); YT-CROSSPUB-HISTORICAL-REMEDIATION (P2); F-SERIES-AVATAR-DIFFERENTIATION Branch B (P3 DEFERRED). **Branch B remains NOT AUTHORISED.** Authority impact: **none** (grant-surface hardening on dormant telemetry; deny-by-default + non-REST-exposed schema `r` unchanged; A2-pinned live selection + as-built flow + T1 authority doc unchanged). Session note pending.

---

> **✅ v3.61 INSIGHTS FIX 2 — F-INSIGHTS-FIX2-AGGREGATION-OWNERSHIP CLOSED AS STALE / ALREADY-IMPLEMENTED (read-only readiness review + register reconciliation, docs-only, 2026-06-18):** The Insights Fix 2 aggregation-ownership decision (Option B, PK 2026-06-13) is **already satisfied in production** — **this pass touches `00_sync_state.md` + `00_action_list.md` only: 0 DB / 0 code / 0 migration / 0 deploy / 0 Advisor behaviour change / 0 production mutation.** **Problem (recap):** the 03:15 cron `public.refresh_post_format_performance()` and the insights-worker `computeFormatPerformance` both wrote `m.post_format_performance` window-30 on unique key `(client_id, ice_format_key, rolling_window_days)` → dual-writer clobber; the aggregation-ownership ambiguity made whichever ran later overwrite the other. **Option B (PK 2026-06-13):** scope the cron windows `{7, 30, 0}` → `{7, 0}` so the worker becomes sole owner of window-30. **Current-state evidence (read-only this lane):** live `public.refresh_post_format_performance()` is **SECURITY DEFINER, `search_path = pg_catalog, public, m`**, and its windows CTE reads **`unnest(ARRAY[7, 0])`** — **`30` appears nowhere in the body** (`mentions_30 = false`); the cron **no longer writes rolling window 30**. The insights-worker (`supabase/functions/insights-worker/index.ts:319`) loops **`for (const windowDays of [30, 90])`** → **worker remains sole owner of windows 30 and 90.** **Ownership is now DISJOINT** — cron {7, 0} ∩ worker {30, 90} = ∅ → **the 03:15 dual-writer clobber is structurally eliminated.** This is exactly Option B's prescribed end-state. **Row evidence (`m.post_format_performance`, 4 clients):** window-0 + window-7 fresh through **2026-06-18 03:15** (cron-stamped); window-30 last written **2026-06-13 03:15**; window-90 last written **2026-06-12 03:16**. **Verdict: the original Insights Fix 2 apply packet is STALE / a no-op → RETIRED. No migration apply is needed.** **Repo-provenance drift (recorded):** the live `{7, 0}` state was NOT reached via a clear tracked post-brief migration — the only `*format_performance*` migrations registered (`20260320075822 format_performance_write_fn`, `20260331092416 refresh_post_format_performance_function`) both **PREDATE the brief (2026-06-13)** and **neither has a repo SQL file** (the known repo↔prod migration-version drift) → the end-state is correct, the repo provenance is missing (optional future repo-migration backfill to cure provenance; not done this pass). **Carry change: F-INSIGHTS-FIX2-AGGREGATION-OWNERSHIP → CLOSED (stale / already-implemented).** **OPENED this pass as a SEPARATE follow-up (explicitly NOT part of Fix 2): `F-INSIGHTS-WORKER-WINDOW30-90-FRESHNESS` (P3, OPEN)** — worker-owned windows **30 and 90 appear stale since 2026-06-13 / 2026-06-12**; `ai-worker/index.ts:382` reads window-30 (`rolling_window_days = 30 … post_count >= 3 ORDER BY avg_engagement_rate DESC`) for format recommendations, so it may be consuming stale window-30 data; **needs a separate read-only investigation** — **explicitly NOT investigated or fixed in this lane.** **Explicitly NOT done:** no migration apply, no function change, no worker change, no cron change, no Advisor/product behaviour change, no AGP/Branch B work, no security/D-002 work, no investigation of the freshness follow-up. **Carries unchanged:** AGP gate map (#1/#2/#2-migration CLOSED; #3 Phase 3.2 APPLIED+DEPLOYED+VERIFIED/CLOSED, Phase 3.3 flag-enable BLOCKED; #4 cutover BLOCKED); D-002 security remediation CLOSED; F-AVATAR-RENDER-LATENCY-RECOVERY (PARTIAL/OPEN); F-SLOT-SCHEDULE-FIDELITY (P4/WATCH); YT positive-publish (WATCH); YT-CROSSPUB-HISTORICAL-REMEDIATION (P2); F-SERIES-AVATAR-DIFFERENTIATION Branch B (P3 DEFERRED). **Branch B remains NOT AUTHORISED.** Authority impact: **none** (aggregation ownership is downstream telemetry; as-built decision flow + T1 authority doc unchanged). Session note pending.

---

> **✅ v3.60 AGP D-01 GATE-3 — SHADOW AVATAR RESOLVER + TELEMETRY APPLIED + DEPLOYED + INDEPENDENTLY VERIFIED; PHASE 3.2 CLOSED (docs-only register reconciliation, 2026-06-18):** AGP D-01 Gate-3 (shadow avatar resolver + resolution telemetry) is now **applied to production, deployed, and independently verified read-only** — **this pass touches `00_sync_state.md` + `00_action_list.md` only: 0 code / 0 DB / 0 migration / 0 grant / 0 deploy / 0 flag-enable / 0 marker backfill / 0 resolver change / 0 production mutation.** **Applied (DDL, by Session 1 via `apply_migration`):** migration **`20260618084054_agp_d01_3_avatar_shadow_resolver_telemetry`** registered (latest in `list_migrations`). **`r.avatar_resolution_shadow`** exists (append-only telemetry; **RLS enabled, deny-by-default — 0 policies**; service_role SELECT/INSERT granted; anon/authenticated SELECT+INSERT blocked). **`public.resolve_and_record_avatar_shadow(uuid, uuid, text, text, text, text, text, uuid)`** exists — 8-arg resolve-and-record (**SECURITY DEFINER, `search_path=public` pinned, plpgsql**; EXECUTE = service_role only; anon/authenticated/PUBLIC EXECUTE denied). **NOTE — canonical-build divergence:** the deployed objects are **Session 1's design** (single resolve-and-record RPC), **not** the parallel local build's pure read-only `resolve_avatar_shadow(uuid,text,text)`; the deployed heygen-worker calls the Session 1 RPC name, so DB ↔ EF are internally consistent. **Deployed (EF):** **heygen-worker v2.2.0 / Supabase function version 36**, **`verify_jwt=false` preserved** (`x-heygen-worker-key` auth). **Dormant + inert (independently verified, read-only):** **`AVATAR_SHADOW_TELEMETRY` remains OFF/unset** (secret not MCP-readable; inferred OFF from 0 rows + strict no-op-unless-truthy gate); **telemetry rows = 0**; **live avatar selection UNCHANGED**; **`lookupAvatar` byte-unchanged** (shadow hook fires AFTER the live pick; result never read back); **no Branch B**; **no marker backfill** (`is_primary`=0, `is_default_host`=0 across 28 rows). **Advisor delta:** only new finding is INFO `rls_enabled_no_policy` on `r.avatar_resolution_shadow` (intended deny-by-default); no new `function_search_path_mutable` / `*_security_definer_function_executable` regression. **Gate map: #1 CLOSED; #2 CLOSED; #2-migration APPLIED/VERIFIED; #3 shadow resolver + telemetry → APPLIED + DEPLOYED + VERIFIED (Phase 3.2 CLOSED); #4 cutover BLOCKED.** **Phase 3.3 (flag-enable / soak) remains BLOCKED pending explicit PK instruction — NOT authorised this pass.** **Follow-up backlog opened: `AGP-GATE3-AUTHENTICATOR-REVOKE-CLEANUP`** — low-risk posture cleanup: revoke the inherited `authenticator` SELECT on `r.avatar_resolution_shadow`; **not a live exposure** (RLS deny-by-default + schema `r` not REST-exposed); defer as a small separately-gated migration. **Explicitly NOT authorised / NOT done:** no telemetry-flag enable, no migration apply, no deploy, no code change, no Branch B, no marker backfill, no live-resolver change. **Minor notes:** repo↔prod migration timestamp delta (applied `20260618084054` vs repo file `…090000_…`, identical name) → reconcile in repo; markers fully unset (0/28), so shadow ordering currently exercises only the tiebreak path. Authority impact: **none** (additive dormant telemetry; A2-pinned live selection + as-built flow + T1 unchanged). **Branch B remains NOT AUTHORISED.** Session note pending.

---

> **✅ v3.59 OPERATIONS RECONCILIATION — D-002 SECURITY REMEDIATION FORMALLY CLOSED + GOVERNOR PHASE 0 LANDED (docs-only register reconciliation, 2026-06-18):** Two completed workstreams reconciled into the registers; **this pass touches `00_sync_state.md` + `00_action_list.md` only — 0 code / 0 DB / 0 migration / 0 grant / 0 deploy / 0 production mutation.** **(1) SEC-D-2026-06-16-002 / D-002 → FORMALLY CLOSED (closed-action-taken).** The **authoritative record is `docs/audit/open_findings.md`** (Session 1; commit `628e3b1` "docs: close D-002 security remediation") — **NOT duplicated here.** Grant cleanup **Batches 1–10 complete**; `0011` search_path **Batches 1–5 complete** (final reconcile commit `d53d629`). **`auth_client_id()` is retained INTENTIONALLY for RLS** — an explicit design carve-out, **not** unresolved exposure. **(2) Governor architecture Phase 0 → LANDED (spec only; NO implementation).** External-review protocol strengthened in `CLAUDE.md` (commit `15e6c7e` — mandatory `reviewed_input_hash` + triage classification + routing); full Phase 0 spec landed at `docs/governance/governor-architecture.md` (commit `a6df5c2` — Governor contract, packet schema, observation-generation rules, drift taxonomy, register generated-summary format, backtest requirements, roadmap, non-goals). **`branch-warden` is the reference implementation; Governors are stateless / read-only / no-memory / no-mutation; facts observed-not-remembered; assertion values generated-not-typed; drift classified-not-binary-STOP.** **NO Governor implementation has started** — Packet / Closure / Deployment Governor + Register Generator are all PENDING; **Phase 1 (Packet Governor) is gated on the spec §9 historical backtest before any live use.** **Carries unchanged by this pass:** AGP gate map (#1/#2/#2-migration CLOSED; #3 shadow resolver + telemetry PENDING; #4 cutover BLOCKED); F-AVATAR-RENDER-LATENCY-RECOVERY (PARTIAL/OPEN); F-SLOT-SCHEDULE-FIDELITY (P4/WATCH); YT positive-publish (WATCH); YT-CROSSPUB-HISTORICAL-REMEDIATION (P2); F-SERIES-AVATAR-DIFFERENTIATION Branch B (P3 DEFERRED). **Branch B remains NOT AUTHORISED.** Authority impact: **none** (register reconciliation only; as-built decision flow + T1 authority doc unchanged). Session note pending.

---

> **✅ v3.58 AVATAR-GOVERNANCE-PLANNING — D-01 #2-MIGRATION (BRAND_AVATAR PRIORITY/DEFAULT MARKERS) APPLIED & VERIFIED (additive dormant metadata; register reconciliation, docs-only, 2026-06-16):** The D-01 #2 schema direction is now **applied to production** as additive, dormant metadata — **selection remains INERT (the A2 pin still drives avatar selection; nothing consumes the markers yet).** **Applied (DDL, via Supabase `apply_migration` only):** migration **`agp_d01_2_migration_brand_avatar_priority_default_markers`** (version **`20260616015419`**; no repo SQL commit — apply_migration, not a file). Added two booleans to `c.brand_avatar` — **`is_primary`** and **`is_default_host`**, both **`NOT NULL DEFAULT false`** — plus two partial unique indexes: **`uq_brand_avatar_primary_per_role_style`** UNIQUE (`stakeholder_id`, `render_style`) WHERE `is_primary`, and **`uq_brand_avatar_default_host_per_client_style`** UNIQUE (`client_id`, `render_style`) WHERE `is_default_host`. **Review `bebea250-639a-43cc-9092-495ff7503e96` — agree / low risk / high confidence / proceed** (non-escalated; provenance recorded; resolved_by=PK). PK phrase: **"PK APPROVES AGP D-01 #2-MIGRATION BRAND_AVATAR PRIORITY/DEFAULT MARKERS APPLY".** **Independent read-only verification (db-rls-auditor):** migration present (version `20260616015419`); both columns boolean / NOT NULL / DEFAULT false; both partial unique indexes present with correct predicates; **no backfill — `is_primary`=0, `is_default_host`=0 across all 28 rows (all markers false);** no resolver change; no avatar activation; migration naming clean; no new advisor finding attributable. **Gate map: #1 Deterministic Resolution — CLOSED; #2 Schema direction — CLOSED; #2-migration — APPLIED/VERIFIED; #3 shadow resolver + telemetry — PENDING (next); #4 cutover — BLOCKED.** **Explicitly NOT authorised:** no backfill, no resolver/selection change, no avatar activation, no Branch B implementation; any further DDL via `apply_migration` only. **Lane footprint: 1 applied additive DDL migration (prior step, via apply_migration); this register pass is docs-only — 0 backfill / 0 resolver / 0 selection change / 0 avatar activation / 0 deploy / 0 production mutation this pass.** Authority impact: **none** (dormant metadata; A2-pinned selection + as-built flow + T1 unchanged). **Branch B remains NOT AUTHORISED.** Session note `docs/runtime/sessions/2026-06-16-agp-d01-2-migration-brand-avatar-markers-applied.md`.

---

> **✅ v3.57 AVATAR-GOVERNANCE-PLANNING — D-01 #2 SCHEMA DIRECTION RATIFIED (planning only; design ratification, docs-only, 2026-06-16):** PK ratified the **D-01 #2 schema direction** — the second of the four AGP gates. **Ratified decision (direction only, NOT implementation):** add **two booleans to `c.brand_avatar`** — **`is_primary`** (supports `role_exact` selection) and **`is_default_host`** (supports `default_host` fallback), both intended **`NOT NULL DEFAULT false`**. **Partial unique guards:** one **primary per `stakeholder_id` + `render_style`**; one **default host per `client_id` + `render_style`**. **`brand_avatar_id` ASC remains the final technical tiebreaker only;** the markers are **preferences among ELIGIBLE rows only** — eligibility stays separate (`is_active`, consent, `heygen_avatar_id`/renderability, `render_style`). **Rejected for now:** an **integer priority** column (kept only as a future-compatible option if evidence later warrants) and a **mapping table** (over-engineered for the current grain). **External review `abcf77cb-22ff-4c6c-bce7-3dd20502d179` escalated (partial / medium / medium) but RESOLVED BY PK at 2026-06-16 00:49 UTC** — no strong objection blocked ratification; the integer-priority concern was **accepted as future-compatible (not a blocker)**, the **backfill-correctness concern was assigned to D-01 #3 (shadow parity proof)**, and a direction-vs-execution confusion was noted. **Gate map: #1 Deterministic Resolution — CLOSED; #2 Schema direction — CLOSED; #2-migration — PENDING (the next gated item); #3 shadow proof — PENDING; #4 cutover — BLOCKED.** **Explicitly NOT authorised:** no migration, no column add, no index, no backfill, no resolver change, no avatar activation, no Branch B implementation. **When the #2-migration sub-gate is later authorised, all DDL MUST be applied via `apply_migration` only.** **Lane footprint: design ratification + docs-only reconciliation — 0 code / 0 DB / 0 migration / 0 column / 0 index / 0 backfill / 0 deploy / 0 resolver / 0 avatar activation / 0 production mutation.** Authority impact: **none** (planning-only; schema direction ratified, nothing built; A2-pinned as-built flow + T1 unchanged). **Branch B remains NOT AUTHORISED.** Session note `docs/runtime/sessions/2026-06-16-agp-d01-2-schema-direction-ratified.md`.

---

> **✅ v3.56 AVATAR-GOVERNANCE-PLANNING — D-01 #1 DETERMINISTIC AVATAR RESOLUTION DESIGN RATIFIED (planning only; design ratification, docs-only, 2026-06-15):** PK ratified the **D-01 #1 deterministic avatar-resolution design direction** — the first of the four AGP gates. **Ratified design (direction only, NOT implementation):** avatar selection gains **meaningful priority/default semantics**; **`brand_avatar_id` ASC is a final tiebreaker only** (never the primary selector); the **A2 pin stays active** throughout; rollout is **shadow-first**; **cutover requires 100% parity + 0 drift** against the live A2-pinned path before any switch. **External review `75b1ef94` escalated but RESOLVED BY PK** — no strong objection blocked ratification; the one empirical concern was **accepted and assigned to D-01 #3 (shadow proof)** rather than blocking the design. **Gate map: #1 Deterministic Resolution — RATIFIED / CLOSED; #2 schema decision — PENDING; #3 shadow proof — PENDING; #4 cutover — BLOCKED** (gated on #2 + #3 and the parity/drift bar). **Explicitly NOT authorised:** no schema, no migration, no code, no deploy, no avatar activation, no `lookupAvatar` change, no Branch B implementation. **Lane footprint: design ratification + docs-only reconciliation — 0 code / 0 DB / 0 migration / 0 deploy / 0 avatar activation / 0 lookupAvatar / 0 production mutation.** Authority impact: **none** (planning-only; design direction ratified, nothing built; A2-pinned as-built flow + T1 unchanged). **Branch B remains NOT AUTHORISED.** Session note `docs/runtime/sessions/2026-06-15-agp-deterministic-avatar-resolution-d01-ratified.md`.

---

> **✅ v3.55 AVATAR-GOVERNANCE-PLANNING — BRANCH B READINESS AUDIT COMPLETE (read-only; verdict READY WITH PRECONDITIONS; Branch B implementation NOT AUTHORISED; register reconciliation, docs-only, 2026-06-15):** Read-only discovery + architecture assessment of the avatar selection path. Evidence: live SELECTs + heygen-worker/index.ts @ 9acb17e8. `c.brand_avatar` is the only live avatar substrate; NY + PP each have 14 avatar rows with exactly 1 active A2-pinned avatar; CFW + Invegent have zero avatar inventory. `lookupAvatar` supports role filtering but production telemetry shows 48 fallback_limit1 / 0 role_filter. `stakeholder_role` is consumed if present but written to 0/80 avatar drafts. `LIMIT 1` without `ORDER BY` is deterministic only because exactly one row is active. **Verdict: READY WITH PRECONDITIONS.** Preconditions before Branch B implementation: deterministic resolution for n>1 active rows; suggestion→consumption bridge for `stakeholder_role`; inventory/no-match policy for all clients; activation governance; production validation of `role_filter`; `lookupAvatar` hardening; consent posture review. Branch B implementation remains **NOT AUTHORISED**. No avatar activation / no Stage 2 / no `stakeholder_role` consumption / no `lookupAvatar` change. Session note `docs/runtime/sessions/2026-06-15-avatar-governance-planning-audit-readonly.md`.

---

> **🟢 v3.54 F-SERIES-AVATAR-DIFFERENTIATION STAGE 1.5 — HYBRID NARRATOR (opt-in multi-perspective presenter mode) DEPLOYED / VERIFIED WITH OBSERVATIONS (series-outline v1.6.0; register reconciliation, docs-only, 2026-06-15):** PK approved **Option C (Hybrid)**. The series-outline generator gains an **opt-in multi-perspective presenter mode** that is **shadow-only downstream** — it records narrator intent as telemetry without changing any avatar/render behaviour. **Shipped (series-outline EF ONLY — single file `supabase/functions/series-outline/index.ts`, commit `d5ee389`, v1.5.0→v1.6.0):** (a) conservative `detectMultiPerspective` trigger (a signal phrase OR **≥3 distinct active brand roles named**); (b) **default Brand Host path byte-identical to v1.5.0** (no behaviour change when the trigger is not met); (c) **multi-perspective mode only:** reads the active `c.brand_stakeholder` taxonomy (**read-only**) and injects a **closed role set** into the prompt — `avatar_preference` encodes `role_code=<code>` and `presenter=<text>`; (d) adds `narrator_mode` + `narrator_mode_trigger` to the success JSON (**telemetry only**). **Shadow boundary (explicit):** **no consumed `stakeholder_role` write, no heygen-worker / ai-worker change, no avatar activation, no schema/migration; `save_series_outline` args unchanged.** **Deployed facts:** live version **series-outline-v1.6.0** (Supabase function **version 49**); deployed via Supabase CLI **`--no-verify-jwt`** (`verify_jwt=false` preserved — authoritative `list_edge_functions` + header-less GET 200); pre-deploy `deno check` exit 0; D-01 **`c02ab9af-c384-42e9-8fcf-44a003fd5025`** (agree / low risk / high confidence). **Verification (this lane):** live GET → `series-outline-v1.6.0`; `verify_jwt=false` confirmed authoritatively + header-less. **Observation (recorded):** verification confirmed **deploy / version / flag only** — the multi-perspective trigger + role-injection were **NOT exercised against a live series** this lane, and downstream consumption stays **shadow** (telemetry; no avatar selection / no render influence). **Carry change:** **F-SERIES-AVATAR-DIFFERENTIATION Stage 1.5 (Hybrid Narrator, shadow) → DEPLOYED / VERIFIED WITH OBSERVATIONS.** **Branch B (distinct avatar/character/voice RENDER per persona + pin-vs-persona governance) remains P3 DEFERRED — unchanged by this lane.** **OPENED this pass: AVATAR-GOVERNANCE-PLANNING (P3, PLANNING ONLY)** — design how narrator/persona intent becomes governed avatar selection (the consumed `stakeholder_role` write, pin-vs-persona precedence, heygen-worker activation, `c.brand_avatar` mapping); **planning only, no implementation.** **Remaining carries OPEN:** YT positive-publish watch; YT-CROSSPUB-HISTORICAL-REMEDIATION (P2); F-AVATAR-RENDER-LATENCY-RECOVERY (PARTIAL/OPEN); F-SLOT-SCHEDULE-FIDELITY (P4 WATCH); F-SERIES-AVATAR-DIFFERENTIATION Branch B (P3 DEFERRED). **Rollback:** redeploy series-outline v1.5.0 with `--no-verify-jwt` (independent; no DB/schema/data change to revert). **Lane footprint: 1 EF deploy (series-outline v1.6.0; prior turn this session); this register pass is docs-only — 0 code / 0 DB / 0 migration / 0 schema / 0 deploy / 0 avatar / 0 heygen / 0 ai-worker / 0 queue / 0 roadmap / 0 production mutation.** Authority impact: **none** (narrator mode is shadow telemetry upstream of avatar selection; as-built decision flow + T1 unchanged). Session note pending; Series v2 + publisher v1.9.0 + youtube-publisher v1.12.0 + persona-capture lanes preserved unchanged.

---

> **✅ v3.53 F-SERIES-FORMAT-DIVERSITY — PER-PLATFORM FORMAT RESOLUTION COMPLETE / DEPLOYED / VERIFIED (Stage 0 + Stage 1 applied; register reconciliation, docs-only, 2026-06-15):** The "avatar collapse" and the "YouTube-drop" failure modes are both **CLOSED.** **Problem:** the series-outline INTERSECTION model offered only formats valid on EVERY selected platform; for `{fb,ig,li,yt}` with publisher-real `platform_support` the only common format was `video_short_avatar` → the series collapsed to avatar everywhere (publisher-impossible — the FB publisher can't post video, the LinkedIn Zapier publisher is text-only), or YouTube was dropped to keep FB/LI publishable. **Three-stage fix (D-01 `fa5c1ab6-c034-4979-b698-754296d55ff2`, agree/medium/high/proceed; PK phrase "PK APPROVES F-SERIES-FORMAT-DIVERSITY STAGE 0+1 APPLY"):** **Stage 2 (prior lane, LIVE):** `series-outline` v1.4.0→**v1.5.0** — `resolveValidFormats` INTERSECTION→**UNION** (offered if valid on ≥1 platform; episode `recommended_format` is a PREFERRED hint; fail-loud only on empty union); verify_jwt=false. **Stage 0 (APPLIED):** `t."5.3_content_format".platform_support` corrected to publisher reality on **9 formats** (explicit full-object SET; `text` unchanged) — Facebook loses all video+animated; LinkedIn loses image_quote/carousel/video_short_avatar (Zapier text-only); Instagram loses animated (keeps avatar); YouTube unchanged. Migration `f_series_format_diversity_stage0_platform_support_publisher_reality` (commit `0aecc23f`); pre-flight baseline-md5 abort-on-drift guard + planned-md5 post-check both PASSED. **Stage 1 (APPLIED):** `public.fan_out_episode(uuid,text,timestamptz)` redefined to resolve the final format **per platform** (lifts the proven Stage-3.5b `retry_episode` resolver verbatim: load `get_studio_capabilities` once; per platform keep `recommended_format` if still valid / deterministic first-valid / else reject `no_valid_format_for_platform` — no doomed slot); persona carry, schedule stagger, `source_material`, the `creative_intent` insert, the `create_manual_slot_internal` gate, status roll-up, and return shape all preserved (additive `format`/`format_changed` only). Migration `f_series_format_diversity_stage1_fan_out_episode_per_platform` (commit `aa05c168`). **`fan_out_episode` md5 `7934ce55c1a4a6fc61a7da659378be58` → `d305aec0d8b27ed6e78a3a1612f3e443`.** **Verification (read-only; 0 rows written):** `fan_out_episode` CHANGED; `retry_episode` (`3d21cd0f`), `create_manual_slot_internal` (`9ec86b61`), `get_studio_capabilities` (`7e1cef46`) all UNCHANGED; Stage-0 matrix matches planned end-state md5; live resolver valid sets — **FB** carousel/image_quote/text, **IG** carousel/image_quote/video_short_avatar, **LI** text, **YT** video_short_avatar/video_short_kinetic/_kinetic_voice/_stat/_stat_voice; PP `{fb,ig,li,yt}` (pref avatar) → FB carousel · IG video_short_avatar · LI text · YT video_short_avatar; CFW `{fb,ig,li}` (pref carousel) → FB carousel · IG carousel · LI text. **No platform dropped; no publisher-impossible FB/LI format selected.** **Latent-future note (NOT a defect):** `video_short`/`video_long_podcast_clip`/`video_long_explainer` still carry `facebook:true` but are `is_buildable=false` (resolver reports `hidden`, never offers them) — review if any becomes buildable. **Remaining carries OPEN:** YT positive-publish watch; YT-CROSSPUB-HISTORICAL-REMEDIATION (P2); F-AVATAR-RENDER-LATENCY-RECOVERY (PARTIAL COMPLETE/OPEN); F-SLOT-SCHEDULE-FIDELITY (P4 WATCH); F-SERIES-AVATAR-DIFFERENTIATION (Branch B, P3 DEFERRED). **Rollback (per-stage, independent, zero data impact):** Stage 1 re-apply prior `fan_out_episode` body (`7934ce55`); Stage 0 restore the 9 baseline `platform_support` rows; Stage 2 redeploy series-outline v1.4.0. **Lane footprint: 2 applied migrations + 1 prior EF deploy; this register pass is docs-only — 0 code / 0 SQL / 0 migration / 0 deploy / 0 queue / 0 historical remediation / 0 publisher / 0 dashboard / 0 production mutation.** Authority impact: **none** (per-platform format resolution is downstream of the decision tree; T1 / as-built flow unchanged). Session note `docs/runtime/sessions/2026-06-15-v353-format-diversity-stage0-stage1-closeout.md`. Series v2 + publisher v1.9.0 + youtube-publisher v1.12.0 + persona-capture lanes preserved unchanged.

---

> **🟠 v3.52 F-AVATAR-RENDER-LATENCY-RECOVERY — FIX 1 DEPLOYED & VERIFIED (forward mechanism repaired; lane STAYS OPEN; register reconciliation, docs-only, 2026-06-15):** Fix 1 grants `video_short_avatar` the same queue hold/release protection the other video formats already had. **Root cause:** `video_short_avatar` was omitted from the video-format IN-list in BOTH `m.gate_queue_on_asset_status()` (BEFORE INSERT on `m.post_publish_queue`) and `m.release_queue_on_asset_ready()` (AFTER UPDATE OF image_status,video_status on `m.post_draft`) — so a pending avatar render's queue row was not held by the +4h gate and, once rendered, was not released-with-preserved-schedule. **Fix (applied, list-only):** added `'video_short_avatar'` to both video IN-lists; image-format lists, the +4h hold, the Fix-1 `GREATEST(COALESCE(post_draft.scheduled_for, slot.scheduled_publish_at), NOW())` release, the `status='queued' AND scheduled_for > NOW()+30min` guard, SECURITY posture, and both triggers all UNCHANGED. **Deployed facts:** migration `favatar_render_latency_recovery_fix1_gate_release_add_avatar` (commit `c7a41af`); D-01 **`6516ed5a-8fea-4d20-af6a-304b9cd70a3d`** (agree / low risk / high confidence / proceed). **Function md5:** gate `38fd402d845dbddca76c1d82b84760c8` → `658a09eb52712be1291c7245e35015c0`; release `81a3838841efaf60c11c40154ac8cd25` → `dbeda9b9b6518b0eaddb5620db7104f2`. **Verification (PGlite 12/12 + live controlled `BEGIN…ROLLBACK` test):** triggers remained bound (2); `video_short_avatar` present in both video lists, absent from both image lists; avatar pending → queue **held +4h**; avatar generated → **released to intended slot time** (no NOW()-collapse, not stuck at +4h); failed/skipped/dead historical queue rows **untouched**; transaction rollback left **zero** test rows; 0 production/queue rows mutated. **Forward mechanism repaired. Historical population intentionally UNTOUCHED — no remediation executed, no requeue/retry, no publisher changes.** **LANE STAYS OPEN (PARTIAL COMPLETE):** still open — the ~20-row historical contaminated population (YT cross-publish), the Instagram generated/published-readiness question, the recovery-sweep design, and the YT historical-remediation dependency. **Lane footprint: 1 applied migration (2 trigger-function redefinitions); this register pass is docs-only — 0 code / 0 SQL / 0 migration / 0 deploy / 0 queue action / 0 retry / 0 historical remediation / 0 D-01 / 0 production mutation.** Authority impact: **none** (queue hold/release timing is downstream of the decision tree; as-built flow + T1 unchanged). Session note `docs/runtime/sessions/2026-06-15-v352-avatar-render-latency-fix1-closeout.md`. Series v2 + publisher v1.9.0 + youtube-publisher v1.12.0 + persona-capture v1.4.0, cc-0015 / cc-0020 / OBS-0A / PRV / IG lanes preserved unchanged.

---

> **✅ v3.51 THREE-SERIES SETTLEMENT + WORDPRESS DIRECT-READ AUDIT — WP audit COMPLETE/VERIFIED (no defect); persona lane settled; new render-latency carry opened (register reconciliation, docs-only, 2026-06-15):** **Part A — WordPress direct-read audit → COMPLETE / VERIFIED (NO defect).** Read-only audit (1 EF read + 5 SQL reads, 0 D-01) of `wordpress-publisher` (v1.0.0, sha `a446e9ab…`) for the YouTube missing-platform-filter class: **none found.** wordpress-publisher IS direct-read on `m.post_draft` but **deliberately filters `pd.platform='facebook'`** (+ `approval_status='approved'`, title/body present, `LENGTH(body)>50`, `NOT EXISTS` website publish, `LIMIT 3`) and posts the FB draft's title+body as a WordPress **article** — it never selects ig/li/yt. This is the **opposite** of the YT class (a *missing* filter); here the source filter is present and intentional. `website` is a recognised but **non-router (`is_router_target=false`), `owned_publish`, article/link** platform (`t.5.0_social_platform` id 28), is **never** a `post_draft.platform`, and has no publish-profile/channel — repurposing the FB draft's text is the intended (only) mechanism. **Blast radius (all-time): 26 rows, CFW-only** (sole `website_publish_enabled` client), **facebook-source only**, all `published`, 2026-04-19→2026-06-14 (text 15 / image_quote 7 / video_short_stat 2 / video_short_avatar 1 / video_short_kinetic 1). 11/26 image/video-source drafts rendered text-only = a **content-fidelity caveat (editorial), NOT an isolation defect.** Classification: **expected/intentional owned-publish architecture, not accidental fan-out — no platform-isolation fix, no historical remediation required.** **Part B — three-series production proof (settled):** F-SERIES-PERSONA-CAPTURE **stays COMPLETE/DEPLOYED/VERIFIED — not reopened** (generation/persistence/propagation/influence/avatar-boundary all PASS; 33 child slots; persona reached `creative_intent.source_material.persona`; avatar differentiation remains Branch B out-of-scope). Schedule-fidelity production evidence **HEALTHY** (no NOW()-collapse, no ~23.5h-early, `published_at` always ≥ slot) → **F-SLOT-SCHEDULE-FIDELITY stays P4/WATCH.** YouTube isolation fix **holding** (0 new cross-publishes, 0 non-YT drafts stamped with YT IDs) → **YT positive-publish stays WATCH/OPEN.** **Part C — carry changes:** **CLOSED this pass:** WP-DIRECT-READ-ISOLATION-AUDIT (→ COMPLETE/VERIFIED). **OPENED this pass:** **F-AVATAR-RENDER-LATENCY-RECOVERY** (OPEN, register-surfaced — late `video_short_avatar` renders are not auto-recovered after an asset-guard skip: `release_queue_on_asset_ready` excludes `video_short_avatar`, and `heal_reset_stuck_queue` resets only running/locked rows). **Unchanged:** F-SERIES-PERSONA-CAPTURE (COMPLETE), F-SLOT-SCHEDULE-FIDELITY (P4/WATCH), YT positive-publish (WATCH/OPEN), YT-CROSSPUB-HISTORICAL-REMEDIATION (P2 OPEN), F-SERIES-AVATAR-DIFFERENTIATION (P3 DEFERRED), F-SERIES-FORMAT-DIVERSITY (P3 DEFERRED). **Lane footprint: read-only audit + docs-only reconciliation — 0 code / 0 SQL / 0 migration / 0 function / 0 deploy / 0 dashboard / 0 D-01 / 0 production mutation.** Authority impact: **none** (no T1 promotion / no architecture / no decision-tree change). Session note `docs/runtime/sessions/2026-06-15-v351-series-settlement-wordpress-audit.md`. Series v2 + publisher v1.9.0 + youtube-publisher v1.12.0 + persona-capture v1.4.0, cc-0015 / cc-0020 / OBS-0A / PRV / IG lanes preserved unchanged.

---

> **✅ v3.50 F-YT-PLATFORM-ISOLATION — YouTube cross-publish P0 FIX DEPLOYED & VERIFIED (youtube-publisher v1.12.0; register reconciliation, docs-only, 2026-06-14):** **P0 incident:** youtube-publisher v1.11.0 is a DIRECT-READ publisher whose draft SELECT **lacked a `platform='youtube'` filter** — so any approved draft carrying a generated `video_short_avatar` asset (incl. **non-YouTube** FB/IG/LI drafts) could be uploaded to YouTube. **Fix (isolation only) — youtube-publisher v1.12.0:** (a) added `platform` to the SELECT + a HARD **`.eq('platform','youtube')`** filter; (b) a **defensive in-loop `draft.platform !== 'youtube'` skip** (defence-in-depth, logs `platform_isolation_skip`). **Deployed facts:** commit `258d213ff3bb93cbaa5b9a7aab91a262ed666d5f`; D-01 **`e0c87546-ffe0-409a-ba60-40a037793133`** (proceed / agree / medium risk / high confidence); deployed via Supabase CLI **`--no-verify-jwt`**; Supabase function **version 52**; live GET `youtube-publisher-v1.12.0`; `verify_jwt=false` preserved (authoritative + header-less GET 200); `deno check` exit 0; deployed source confirmed to contain `.eq('platform','youtube')` + the in-loop guard. **Post-deploy verification:** **0** new YouTube cross-publishes after deploy; **0** non-YouTube drafts stamped with YouTube IDs after deploy; no runtime errors. **Positive-publish remains a WATCH** — no eligible YouTube draft existed post-deploy, so the first legitimate YouTube draft after v1.12.0 must be confirmed to publish **while its FB/IG/LI siblings receive NO YouTube `m.post_publish` row.** **Status: F-YT-PLATFORM-ISOLATION DEPLOYED / VERIFIED / CLOSED for future publishing.** **Carries (NOT done — explicitly open):** **YT-CROSSPUB-HISTORICAL-REMEDIATION** (P2 — already-uploaded cross-platform YouTube videos remain UNRESOLVED; no remediation performed this lane); **WP-DIRECT-READ-ISOLATION-AUDIT** (P2 — audit the WordPress publisher for the same direct-read class, possible FB→website cross-publish; NOT patched this lane). **Lane footprint: 1 EF deploy (youtube-publisher v1.12.0; prior lane); this register pass is docs-only — 0 code / 0 DB / 0 migration / 0 dashboard / 0 deploy / 0 historical-remediation / 0 WordPress change / 0 production mutation.** Authority impact: **none** (publisher is downstream of the decision tree; as-built flow + T1 unchanged). Session note `docs/runtime/sessions/2026-06-14-fyt-platform-isolation-closeout.md`. Series v2 Stage 1/2/3/3.5a/3.5b + publisher v1.9.0 + persona-capture v1.4.0, cc-0015 / cc-0020 / OBS-0A / PRV / IG lanes preserved unchanged.

---

> **✅ v3.49 F-SERIES-PERSONA-CAPTURE — PERSONA INTENT CAPTURE/PERSISTENCE COMPLETE / DEPLOYED / VERIFIED (avatar differentiation explicitly DEFERRED; register reconciliation, docs-only, 2026-06-14):** The Series v2 persona **intent** path is now end-to-end — a series outline can request structured per-episode persona fields, they persist to the episode, and the governed fan-out carries them into the child intent. **DB half (applied):** `public.save_series_outline(uuid,text,jsonb,jsonb)` now persists `persona_label` / `avatar_preference` / `persona_notes` from `p_episode_rows` into the Stage-1 `c.content_series_episode` persona columns — body **md5 `ec5db30454c0aa942716292236b8ad44` → `93cf6ff9793282b6e56397c05d043fd3`**; DB D-01 **`2db87754-5af1-4727-9f0b-440bbcea8d91`**. **EF half (deployed):** **series-outline v1.4.0** (commit `4d9b82c72b16d065ea6e778b84546dd8f0590a03`, Supabase function **version 47**, `verify_jwt=false`, `deno check` exit 0, live GET `series-outline-v1.4.0`) — the prompt now requests `persona_label`/`avatar_preference`/`persona_notes` (optional, null-safe via `cleanStr`) and `episodeRows` maps them; persona-only delta over v1.3.0 (the platform-aware resolver/whitelist/fail-loud logic is UNCHANGED). EF D-01 **`d311d11c-bae5-487f-b0d4-27301baa3a2a`** — verdict **partial / escalate=true; PK approved despite the escalation** (recorded). **`fan_out_episode` UNCHANGED** — it already carries the episode persona fields into the child brief and `creative_intent.source_material.persona` (verified; not modified this lane). **No production series mutated during verification.** **COMPLETE / DEPLOYED / VERIFIED: persona INTENT capture + persistence + carry-through.** **EXPLICIT BOUNDARY — avatar differentiation is NOT complete:** different avatars / character rendering per persona remains **deferred Branch B / avatar-governance work**; this lane does **NOT** change `stakeholder_role`, heygen-worker, `c.brand_avatar`, A2 / Branch-A pinning, or avatar selection. **Carries:** **F-SERIES-AVATAR-DIFFERENTIATION (Branch B)** (P3 DEFERRED — persona→distinct avatar/voice rendering + pin-vs-persona governance); F-SERIES-FORMAT-DIVERSITY (P3 DEFERRED); F-SLOT-SCHEDULE-FIDELITY (P4 WATCH). **Stage 4 (UI) NOT started** (persona capture is operator-prompt/model-driven via the outline EF; a dedicated UI surface is still Stage 4). **Rollback:** EF — redeploy series-outline v1.3.0 with `--no-verify-jwt`; DB — re-apply prior `save_series_outline` body (md5 `ec5db30454c0aa942716292236b8ad44`); independent, no data migration. **Lane footprint: 1 applied DB migration (save_series_outline) + 1 EF deploy (series-outline v1.4.0) — both prior lanes; this register pass is docs-only — 0 code / 0 DB / 0 migration / 0 deploy / 0 dashboard / 0 production mutation.** Authority impact: **none** (persona is planning intent captured upstream; as-built decision flow + T1 unchanged; avatar selection untouched). Session note `docs/runtime/sessions/2026-06-14-fseries-persona-capture-closeout.md`. Series v2 Stage 1/2/3/3.5a/3.5b + publisher v1.9.0, cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT lanes preserved unchanged.

---

> **🟢 v3.48 F-SLOT-SCHEDULE-FIDELITY — DOWNGRADED TO P4 / WATCH (post-Fix-1 verification window; backlog/rule-slot latency discovery complete; register reconciliation, docs-only, 2026-06-14):** Read-only discovery resolved the remaining schedule-fidelity unknowns and downgrades the carry from active defect to a **post-Fix-1 watch.** **Findings:** (1) **Rule-slot early `~23.5h` bucket** was caused by the OLD `release_queue_on_asset_ready` collapsing `queue.scheduled_for` to `NOW()` — **already fixed by Fix 1 (v3.47)**; the rule-slot semantics question is therefore **explained/resolved by Fix 1** unless the watch disproves it. (2) **Multi-day late tail** was the **historical Apr/May YouTube OAuth outage + backlog drain**, NOT a current scheduler defect; **no June-origin late recurrence found.** (3) **No current capacity issue** — 12 queued / 0 needs_review / 1 queued >3d. (4) **Portal-path discovery:** `portal_approve_draft` has **zero production usage ever** — the `draft_approve_and_enqueue` / `portal_approve_draft` drift (uses `get_next_scheduled_for`, can discard slot/draft intent) is **dormant / a client-portal launch prerequisite**, NOT current production drift. (5) **`fan_out_episode` past-date clamp** (`now()+1h`) is recorded as a **separate design question**, not an active schedule-fidelity bug. **Status change: F-SLOT-SCHEDULE-FIDELITY → P4 / WATCH (post-Fix-1 verification window) — NOT fully closed.** **Watch criteria:** early `1h_1d` bucket should collapse toward `within_1h`; no new June-origin `>3d` late tail; median absolute delay should approach the publisher-cron cadence. **Carried (NOT new lanes):** portal-path drift = **fix before Phase 3 client-portal launch** (latent launch prerequisite); `fan_out_episode` past-date clamp = separate design decision. **No implementation lane opened this pass.** **Lane footprint: read-only discovery + docs-only reconciliation — 0 code / 0 DB / 0 migration / 0 deploy / 0 dashboard / 0 source / 0 production mutation.** Authority impact: **none.** Session note `docs/runtime/sessions/2026-06-14-fslot-schedule-fidelity-watch-downgrade.md`. Series v2 Stage 1/2/3/3.5a/3.5b + publisher v1.9.0 guard, cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT lanes preserved unchanged.

---

> **🟠 v3.47 F-SLOT-SCHEDULE-FIDELITY — FIX 1 APPLIED & VERIFIED (early-publish on asset-ready closed); CARRY STAYS OPEN / PARTIALLY RESOLVED (register reconciliation, docs-only, 2026-06-14):** Fix 1 closes the early-publish mechanism where releasing a held publish-queue row on asset-ready **collapsed `queue.scheduled_for` to `NOW()`**, discarding the intended future schedule. **Shipped (production-applied):** `m.release_queue_on_asset_ready()` redefined — body **md5 `0fa656e14c7ac44087e103ea56fd73e3` → `0cdede8d1b6037b53ba78b957b64daa7`** — so when an image/video asset leaves `pending` the release now sets `queue.scheduled_for = GREATEST(COALESCE(post_draft.scheduled_for, linked slot.scheduled_publish_at), NOW())` instead of `NOW()`. The **existing `queue.scheduled_for` is intentionally NOT used as a fallback** because it may carry the artificial `+4h` hold value; **future intended schedules stay future, past/no-intent schedules release now.** **Trigger `trg_release_queue_on_asset_ready` remained bound; `gate_queue_on_asset_status` unchanged; `publisher_lock_queue_v1`/`v2` unchanged.** **Deployed facts:** migration **`fslot_schedule_fidelity_fix1_release_preserve_intent`**; D-01 **`dc6d3bde-051f-4ea3-9fca-9f09217b1033`** — verdict **partial / escalate=true**; **PK approved Option A and applied despite the reviewer escalation** (decision recorded). **Verification (production):** future intended schedule **stayed future**; past intended schedule **released now**; draft/slot with no intent **released now**; trigger live rollback test **leaked 0 rows**; reproduced early-publish mechanism **CLOSED**; **no production test rows leaked.** **Status: PARTIALLY RESOLVED — Fix 1 applied; F-SLOT-SCHEDULE-FIDELITY stays OPEN (P3).** **Open follow-ups (carried, NOT done):** (1) **portal-path drift** — `draft_approve_and_enqueue` / `portal_approve_draft` still use `get_next_scheduled_for` and can discard slot/draft intent; (2) **fan_out_episode past-date clamp** — past episode times are clamped to `now()+1h`, needs a separate design decision; (3) **rule-slot semantics** — decide whether the 24h fill-window / recurring-schedule behaviour is intended or needs adjustment. **Lane footprint: 1 applied migration (`release_queue_on_asset_ready` redefinition, prior lane); this register pass is docs-only — 0 code / 0 DB schema / 0 migration apply / 0 source / 0 dashboard / 0 deploy / 0 production mutation.** Authority impact: **none** (queue-release timing is downstream of the decision tree; as-built flow + T1 unchanged). Session close-out `docs/runtime/sessions/2026-06-14-fslot-schedule-fidelity-fix1-closeout.md`. Series v2 Stage 1/2/3/3.5a/3.5b + publisher v1.9.0 guard, cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT lanes preserved unchanged.

---

> **✅ v3.46 SERIES v2 STAGE 3.5b — PLATFORM-AWARE `retry_episode` APPLIED & VERIFIED; STAGE 3.5 OVERALL CLOSED (register reconciliation, docs-only, 2026-06-14):** Stage 3.5b completes the platform-awareness work begun in 3.5a. **Root cause (3.5b):** `retry_episode` re-created rejected child slots with `v_intent.format_preference` (the ORIGINAL format), which `create_manual_slot_internal` re-checks against `platform_support[platform]` and re-rejects with the SAME `format_not_supported_on_platform` — so retry was a **no-op for the exact failures it should fix** (e.g. a YouTube `image_quote` child stayed un-repairable). **Shipped (production-applied):** `public.retry_episode(p_episode_id uuid, p_mode text, p_created_by text)` redefined — **signature unchanged**, body **md5 `d85366fda2b12cd1e0f9d3b56f6f0bb8` → `3d21cd0f…`** — so inside the existing per-platform re-create branch it loads `get_studio_capabilities(client_id)` once, then before `create_manual_slot_internal` **re-resolves a valid format for the platform**: keep `v_intent.format_preference` if still valid / pick a **deterministic first-valid alternative** if not / **fail loud `no_valid_format_for_platform`** if none (no doomed re-create); adds `format` + `format_changed` audit fields to recreated entries. **`create_manual_slot_internal` UNCHANGED** (hard backstop retained); mode guard, episode/intent load, brief reuse, dead-slot audit retention, intent-status recompute, and `fanout_result` append are all byte-unchanged; **no schema/signature change.** **Deployed facts:** migration **`stage3_5b_retry_episode_platform_aware`** (commit `5fc0fa40f45cbb07088c10061fdad0189ae9caaf`); D-01 **`6917cc0e-a944-40b2-ab98-77ec27f448ad`** (proceed / agree / medium risk / high confidence / escalate=false). **Verification (read-only, production):** invalid YouTube static retry repaired → `video_short_avatar`; valid Facebook `image_quote` kept unchanged; `no_valid_format_for_platform` branch confirmed; **published children protected** (`already_published`); **in-flight children protected** (`in_flight_preserved`); **dead/failed history preserved**; 0 test rows leaked; **0 production series mutated**. **Interim behaviour (intentional, recorded):** the deterministic **first-valid** repair is an interim safety choice, **NOT Stage 4 format optimization** — per-platform-per-episode format diversity remains DEFERRED. **Stage 3.5 overall = CLOSED (3.5a platform-aware outline + 3.5b platform-aware retry both live). Stage 4 (UI) NOT started.** **Repo↔prod apply-state drift (recorded, separate follow-up lane):** the migration file/commit header reads "STAGED / NOT APPLIED" but production IS applied (live `retry_episode` carries the 3.5b logic — `get_studio_capabilities` + `no_valid_format_for_platform` + `format_changed`); the registers record prod truth — the migration-file header cleanup is a separate pass (NOT touched here). **Carries (carried-forward — NOT done):** F-SERIES-FORMAT-DIVERSITY (P3 DEFERRED — per-platform-per-episode diversity); F-SLOT-SCHEDULE-FIDELITY (P3 OPEN — schedule fidelity); F-SERIES-PERSONA-CAPTURE (P3 DEFERRED — episode persona capture surface). **Lane footprint: 1 applied migration (retry_episode redefinition); this register pass is docs-only — 0 code / 0 DB schema / 0 migration-file edit / 0 dashboard / 0 deploy / 0 production mutation.** Authority impact: none (retry is downstream of the decision tree; as-built flow + T1 unchanged). Series v2 Stage 1/2/3/3.5a + publisher v1.9.0 guard, cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT lanes preserved unchanged.

---

> **✅ v3.45 SERIES v2 STAGE 3.5a — PLATFORM-AWARE OUTLINE GENERATION DEPLOYED & VERIFIED (series-outline v1.3.0; register reconciliation, docs-only, 2026-06-14):** The series outline generator is now **capability-aware**. Previously it received only `series.platform` (singular) and hard-clamped `recommended_format` to `text|image_quote|carousel` in BOTH the prompt and the validation whitelist — so a YouTube-targeted series was **structurally incapable of emitting a video format** and every YouTube child was rejected downstream (zero YouTube output). **Shipped (series-outline EF ONLY — single file `supabase/functions/series-outline/index.ts`, commit `65d6dc4372338c2b9862097eec87cc2c8945d728`):** (a) reads `series.platforms[]` with fallback to `[series.platform]`; (b) calls `public.get_studio_capabilities(client_id)` — the SAME resolver Single Post / Studio uses; (c) computes the VALID format set = formats whose state ∈ {enabled, enabled_unproven} on EVERY selected eligible platform (**intersection**, so the single per-episode format is publishable on all targets) + `video_only` detection; (d) feeds that set into the prompt AND uses it as the validation whitelist (resolver-driven, **not** the old hardcoded `text|image_quote|carousel`); (e) if NO format is valid across the selected set, **FAILS LOUD `no_valid_format_for_platform_set` (HTTP 422)** — never silently picks text; capability resolution runs BEFORE the `outline_pending` move so a failure leaves the series unchanged. **Interim behaviour (intentional, recorded):** mixed **FB+IG+LI+YT currently resolves to `video_short_avatar` only** (the cross-platform intersection under current taxonomy) — **intentional interim for safety**; true per-platform-per-episode format diversity remains **DEFERRED**. **Deployed facts:** live version **series-outline-v1.3.0**; deployed via Supabase CLI **`--no-verify-jwt`** (`verify_jwt=false` preserved); pre-deploy `deno check` exit 0; D-01 **`b45da2c8-9189-491f-ad83-e9bc90936e8b`** (proceed / agree / medium risk / high confidence / escalate=false). **Post-deploy read-only V-checks (no production series mutated):** GET → `series-outline-v1.3.0`; POST with missing/invalid `x-series-key` → 401; header-less GET → 200 (verify_jwt false, not regressed); resolver dry-check (read-only `get_studio_capabilities`, function NOT invoked) — YouTube-only → all-video set, Instagram excludes text, FB+IG+LI+YT intersection = `["video_short_avatar"]`, empty intersection → 422 (code-verified); 0 `c.content_series` rows mutated during verification. **DOWNSTREAM GATES UNCHANGED:** `fan_out_episode`, `create_manual_slot_internal`, the per-platform capability rejection, Advisor, compliance, render, publisher all untouched — the whitelist + the downstream gate remain hard backstops. **Stage 3.5a = COMPLETE / DEPLOYED / VERIFIED; Stage 3.5b (retry_episode platform-awareness) = OPEN next carry (NOT started).** **Carries (NEW / carried-forward — NOT done):** **F-SERIES-RETRY-PLATFORM-AWARENESS** (P2 — Stage 3.5b: make `retry_episode` capability/platform-aware, mirroring 3.5a); **per-platform-per-episode format diversity** (P3 — replace the interim single-intersection format with per-platform-native episode formats); **F-SLOT-SCHEDULE-FIDELITY** (P3 — schedule fidelity lane, pre-existing); **persona capture lane** (P3 — episode persona_label/avatar_preference/persona_notes capture surface). **Lane footprint: 1 EF deploy (series-outline v1.3.0); this register pass is docs-only — 0 code / 0 DB / 0 migration / 0 schema / 0 UI / 0 publisher / 0 retry_episode / 0 fan_out_episode / 0 production mutation.** Authority impact: none (outline is upstream content planning; as-built decision flow + T1 unchanged). Series v2 Stage 1/2/3 + publisher v1.9.0 guard, cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT lanes preserved unchanged.

---

> **✅ v3.44 FACEBOOK PUBLISHER ASSETLESS-RELEASE GUARD — CLOSED & DEPLOYED (publisher v1.9.0; register reconciliation, docs-only, 2026-06-14):** The Facebook asset-less publish leak is **RESOLVED for Facebook**. Live incident — FB post `962399936961457_122114734587268380`, draft `1d34577bbb5a`, format `image_quote`, `image_status='failed'`, `image_url` absent — published as a silent **text feed post** because the old image hold gate only caught `image_status='pending'` (a `failed` asset fell through → `hasImage=false` → `/feed`); video formats had no asset gate at all. **Shipped fix (Facebook `publisher` edge function ONLY):** new dependency-free guard `supabase/functions/publisher/guard.ts` (`classifyFbFormat` + pure `decideAssetGuard`); updated `supabase/functions/publisher/index.ts`; validation harness `docs/briefs/publisher-assetless-guard-validation.test.ts`. **Shipped safety behaviour:** (a) **image-required formats no longer fall back to text** — `image_quote` requires `image_status='generated'` + `image_url`, else BLOCK `image_required_but_<status>`; (b) **carousel-required formats no longer downgrade silently** — require generated + ≥2 slides, else BLOCK `carousel_incomplete_slides:N`; (c) **video/animated formats no longer publish as text on FB**; (d) **unsupported FB video is blocked with `fb_video_publish_not_supported_interim`** (the FB publisher has no video publish path); (e) **failed/missing/pending-timeout assets are skipped/blocked with an `asset_guard_blocked:<reason>` reason** (queue `status='skipped'`, terminal/not-re-locked, + an audit `post_publish status='failed'` row), **draft preserved**; pending remains a retryable hold (the assetless-publish-on-timeout behaviour was removed); **text still publishes via feed** (unchanged). **This is an INTERIM guard / "T2-Lite" (existence/status only) — NOT full T2** (no OCR / transcript / visual / brand / audio QA). **Deployed facts:** merge commit `0216b79`; close-out commit `7a4e3c3`; runtime close-out `docs/runtime/sessions/2026-06-14-publisher-assetless-guard-closeout.md`; live version **publisher-v1.9.0** (Supabase function version **91**); **`verify_jwt=false` confirmed authoritatively** (deployed via Supabase CLI `--no-verify-jwt`, per the verify_jwt deploy gotcha); D-01 **`fcf2b32d-f234-4a36-bb82-60cfe8038e82`** (verdict agree / medium / high / proceed). Pre-deploy validation: `deno check` exit 0 + `deno test` **21/21 PASS** against the real guard functions; post-deploy read-only V-checks: version v1.9.0, verify_jwt false, **0 asset-required FB posts published as text since deploy**, current FB queue (2 `image_quote` generated + 1 `carousel` generated) **not stranded**. **Carries (NEW, carried-forward — NOT done):** **F-PUBLISHER-ASSETGUARD-LI-IG-YT** (P2 — equivalent interim assetless guard audit/patch for the LinkedIn, Instagram and YouTube publishers; same leak class, not yet guarded); **full T2 remains the durable replacement** for this interim existence/status guard (DEFERRED); **FB video publishing support remains separate feature work** (video formats currently blocked on FB by design). **Rollback (preserved):** redeploy prior **publisher-v1.8.0** (git blob `56b1d1da`) via Supabase CLI **with `--no-verify-jwt`**; **no DB rollback required** (guard writes only existing columns/values). **Lane footprint: 1 EF deploy (publisher v1.9.0); this register pass is docs-only — 0 code / 0 DB / 0 migration / 0 schema / 0 dashboard / 0 other-publisher / 0 FB-video-publishing / 0 production mutation.** Authority impact: none (publisher is downstream of the decision tree; as-built flow + T1 unchanged). Series v2 Stage 1/2/3, cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT lanes preserved unchanged.

---

> **✅ v3.43 CONTENT STUDIO T0/T1/VISIBILITY + ROSTER + FIX 1 — ALL CLOSED & DEPLOYED (register reconciliation, docs-only, 2026-06-13):** five completed workstreams reconciled into the registers (no implementation this pass; 0 code / 0 DB / 0 deploy / 0 production mutation). **(1) Content Studio T0 — governed manual path → CLOSED (PASS WITH CARRY):** manual single-post now enters ICE as governed intent (`create_manual_slot` → manual slot → fill → ai_job → Advisor → compliance → approval → render → queue → publish); queue-save bypass removed; `manual_post_insert` deprecated/retained. Close-out `2026-06-13-t0-content-studio-governed-path-closeout.md` (`38b0d0a3`). **(2) Content Studio T1 — Creative Intent / Content Package parent → CLOSED (PASS WITH CARRY):** one idea → N governed platform-native siblings under one `m.creative_intent`; additive nullable `intent_id` FKs; automated fill byte-identical. content-engine `9e4ceb12` / dashboard `d48772b3`; close-out `2026-06-13-t1-creative-intent-deploy-closeout.md` (`08ecb4e`). **(3) T1 Operational Visibility (Ideas tab) → CLOSED:** read-only `list_creative_intents` + Ideas list/drill-down; one idea → child slots → drafts → outcomes without SQL; status derived on display. content-engine `2ae568ee` / dashboard `f5c27874`; close-out `2026-06-13-t1-visibility-deploy-closeout.md` (`718b252`). **(4) Active-client roster fix → CLOSED (F-CONTENT-STUDIO-CLIENT-ROSTER RESOLVED):** hardcoded `CLIENTS` (NY+PP) → DB-driven `list_active_clients` selector reaching CFW + Invegent; CFW + intents `0d0a45a0`/`f8c10b91` now reachable. content-engine `879f68ce` / dashboard `fe12c8c9`; close-out `2026-06-13-cs-active-clients-roster-closeout.md` (`df0b453`). **(5) Fix 1 — insights-worker v14.2.0 FB engagement repair → CLOSED (PASS WITH CARRY):** dead metrics replaced, Graph v24.0 pinned, `post_reactions_by_type_total` basis, NULL-not-0, paging redaction. Merge `335ab121`, deploy record 66; close-out `2026-06-12-fix1-v14-2-0-deploy-closeout.md` (`808ddd2a`). V-checks: 79 fresh rows / 79/79 calls / 79/79 `reactions_by_type_total` / 316/316 telemetry / 0 token leakage / comments-shares NULL-not-0 / `engagement_rate` 48/48 where reach>0; contrast vs 447 pre-deploy rows. **Carries opened/kept:** F-INSIGHTS-FIX2-AGGREGATION-OWNERSHIP (P2 — Fix 2, the next load-bearing evidence-chain workstream; NOT started); F-INSIGHTS-DEPLOY-VERSION-DRIFT (P3 — drift-check by bundle hash not VERSION); F-SLOT-SCHEDULE-FIDELITY (P3 — pre-existing, not T0-introduced); F-INTENT-STATUS-ROLLUP (P3 — UI-derived, durable rollup deferred); F-PUBLISH-QUEUE-DANGLING-QUEUEID (P4); META-FIX1-PAGES-READ-ENGAGEMENT (P3 — comments/shares #10-blocked, non-blocking, auto-heals). **Note:** "Content Studio T0/T1/T2" tiers ≠ the existing "T1 authority doc" (`current-ice-decision-tree.md`). **T2 deferred** until more real rendered-asset evidence. Authority impact: none (as-built decision flow + T1 authority doc unchanged). cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT lanes preserved unchanged.

---

> **🟠 v3.42-R STAGE R COMPLETE — Fix 1 brief AMENDED; pg_net `paging` security incident contained (2026-06-12; amendment within v3.42 — no version bump; F-OPTIONC-ENGAGEMENT-EVIDENCE-NULL stays P2 OPEN/diagnosed; Option C stays PARKED):** Stage R (read-only Graph GETs via pg_net) verified the FB engagement root cause is **SPLIT**. **Leg A — dead metrics:** `post_engaged_users` returned `(#100) not a valid insights metric` on all four clients/tokens, and **`post_impressions` returned the same #100** — requests were issued as v19.0 but **Meta served v24.0** (v19 sunset; silent auto-upgrade removes both metrics while `post_impressions_unique`/`post_clicks` survive — matches the 459/526 success pattern). **Leg B — permission block:** the reactions/comments/shares fields fallback returned `(#10) requires 'pages_read_engagement' or Page Public Content Access` — the fallback has been silently permission-failing all along; the stored 0s were never genuine. **Viable replacement verified: `post_reactions_by_type_total` returned HTTP 200** on the same token that failed the fields call — reactions without `pages_read_engagement`; zero-vs-failure cleanly distinguishable (200+empty value ≠ 400+structured error). **Fix 1 remains an insights-worker code repair** (`docs/briefs/f-optionc-engagement-evidence-null-fix1-fb-engagement-acquisition.md`, v14.2.0 shape): replace dead direct metrics; handle/remove dead `post_impressions`; **pin the Graph version explicitly**; compute engagement only from trustworthy 200-response inputs; **store failed reads as NULL, not 0**; preserve raw failure visibility in `raw_payload`; **redact/drop `paging` blocks before persistence/logging**. **NOT IMPLEMENTED, NOT DEPLOYED** — remaining gates: CCD patch → D-01 → PK exact approval phrase → CLI deploy → V-checks. **Security incident + containment:** Stage R pg_net Graph response `159957` included Meta `paging` URLs echoing a CFW page-token fragment; targeted purge of request `159957` performed same-day through the audited migration path (`security_purge_pgnet_stage_r_159957`, PK-approved); **standing rule L-StageR-paging** — strip/redact `paging` blocks before capturing/persisting any Graph response body; **CFW token rotation recorded as PK-side precautionary security follow-up (SEC-CFW-TOKEN-ROTATION; no token values in docs or chat)**. **Meta-side carry split out (META-FIX1-PAGES-READ-ENGAGEMENT):** verify/obtain `pages_read_engagement` / Page Public Content Access for comments+shares later — does NOT block Fix 1; not to be mixed with Option C or Advisor changes. **Next work = evidence-feed repair, not Option C implementation.** Lane mutation footprint: read-only Graph GETs + the one approved narrow purge migration (already recorded); **this register pass is docs-only — 0 code / 0 DB schema / 0 cron / 0 worker-provider / 0 Advisor / 0 ai-worker / 0 Option C / 0 dashboard / 0 production-config change.** Authority impact: none; T1 untouched. A2 Branch A pin + Stage 3 passive watch unaffected; all other lanes preserved unchanged.

---

> **🔴 v3.42 F-OPTIONC-ENGAGEMENT-EVIDENCE-NULL DIAGNOSED — engagement evidence feed structurally broken; carry opened P2; Option C stays PARKED (read-only investigation + register-only close, 2026-06-12):** `m.post_format_performance.avg_engagement_rate` is effectively NULL despite healthy post_count because of three stacked defects. **D1 (primary):** insights-worker v14.1.0 computes `engagement_rate = engaged_users/reach` but `engaged_users` is never obtained — the `post_engaged_users` Graph metric fails **526/526 FB rows** across all four clients (raw_payload.failed_metrics; systematic; introduced v14.0.0) and the reactions+comments+shares fallback is **0 on 526/526** (failed field reads coerced to 0, indistinguishable from genuine zero) → engagement_rate NULL on every FB row even though reach>0 on 322/526 and FB collection is otherwise fresh (06-12) and cron-scheduled (ny 03:00 / cfw 03:05 / pp 03:10 / invegent 03:15 + insights-feedback-daily 03:30 — FB schedule layer healthy). **D2:** dual-writer clobber on `m.post_format_performance` — insights-worker's `computeFormatPerformance` (per-client ~03:01–03:16, windows 30+90, all-platform, captures YT engagement) vs `refresh_post_format_performance()` cron 03:15 (windows 7/30/0, FB-only via `reach IS NOT NULL`) on the same PK; the refresh **clobbers worker-produced window-30 values with NULL FB-only averages — exactly the window the Advisor reads**. Survivor proof: all non-NULL avg rows are window-90 or YT-only-video combos stamped 03:01/03:12. **D3:** YouTube structurally excluded — YT perf rows carry engagement on 20/62 but **reach NULL 62/62** so the refresh filter drops all; YT insights stale 05-29 / manual-only (existing carry). **LinkedIn + Instagram have zero insight rows.** **Advisor impact:** ai-worker coerces NULL→0, injecting a fabricated "0.0% avg engagement (N posts)" summary per job — misleading, not merely absent. **Recommended fix sequence (recorded, NOT approved/implemented; each gated):** (1) FB engagement repair in insights-worker incl. raw-failure visibility (ef_deploy); (2) aggregation ownership — stop the 03:15 clobber (cheapest: retire redundant refresh cron, worker owns table, keep window-7); (3) YT reach/views mapping + YT cron; (4) Advisor bad-evidence shielding (don't render NULL as 0.0%). **Option C cannot become evidence-based until the feed is repaired** — Monday gate posture confirmed. Evidence + patch text: `docs/runtime/sessions/2026-06-12-v3.42-f-optionc-engagement-evidence-null-close.md`. **0 code / 0 DB / 0 migration / 0 cron / 0 worker-provider call / 0 Advisor / 0 ai-worker / 0 Option C / 0 D-01 / 0 production mutation — 7 read-only SELECTs + 2 repo source reads.** Authority impact: none. A2 Branch A pin + Stage 3 passive watch unaffected; all other lanes preserved unchanged.

---

> **✅ v3.41 A2 BRANCH A APPLIED — interim primary brand host v1 (`pin_primary_brand_host_v1`, production `mbkmaxqhsohbtwsqolns`, 2026-06-12):** PK-selected interim hosts pinned: **NDIS Yarns → Support Coordinator (Realistic)** (`brand_avatar_id 83ff167d…`, heygen `7e98bd3860f14ee18c9b4909e46ac77c`) and **Property Pulse → Buyer's Agent (Realistic)** (`brand_avatar_id d6c422fb…`, heygen `5d03454fbd0c469692f1a27ccbe6a000`) — exactly one active `c.brand_avatar` row per brand makes heygen-worker v2.1.1's `LIMIT 1` selection deterministic. **26 rows deactivated (13/brand), 0 deleted; CFW/Invegent untouched; no provider calls, no worker calls, no forced render.** Full gate chain: brief `docs/briefs/cc-a2-branch-a-host-pin.md` (`bdc320ef`) → Stage 0 preflight ALL-PASS → D-01 `ab14f105` (escalate/partial; pushback classified type-(c) echoes of self-disclosed weak evidence; PK accepted, corrected actions absorbed) → **PK exact approval phrase** → `apply_migration` → V-checks V1–V6 ALL-PASS. **Apply incident (transparent):** first attempt aborted on `42883: function min(uuid) does not exist` (assertion plumbing only); DO-block transaction rolled back and state verified untouched; re-applied with `min(brand_avatar_id::text)::uuid` — UPDATE statements and approved scope unchanged (lesson **L-v3.41-a**: avoid `min/max(uuid)` in migration assertions unless cast). **Rollback SQL held** (reactivate the 26 captured IDs; execution record §5). **Branch A = CLOSED/APPLIED as interim primary brand host v1** — NOT "one avatar forever"; cast preserved for Branch B (role-aware wiring, DEFERRED); Branch C DEFERRED; purpose-built brand hosts desired later. **Stage 3 passive telemetry confirmation OPEN:** next scheduled NY/PP HeyGen renders must show `avatar_identity.talking_photo_id` = `7e98bd3860f14ee18c9b4909e46ac77c` (NY) / `5d03454fbd0c469692f1a27ccbe6a000` (PP) — NO forced render, NO worker invocation. **Un-pin discipline carry:** dashboard RPC `assign_brand_avatar` can silently reactivate non-primary avatars and break the pin — no NY/PP avatar activations via dashboard/admin flows without an explicit re-pin decision until Branch B exists. **Inventory correction:** 14 active rows per brand pre-pin (7 personas × realistic/animated); the gate-prep 11/13 figure was an undercount, corrected by the selection-sheet read. **Option C REMAINS PARKED — reference hypothesis only; no Advisor / format-policy / mix-table / narrative / policy-envelope reconnection approved** (gate-prep evidence pack `docs/runtime/sessions/2026-06-12-option-c-a2-gate-prep.md`; engagement evidence still effectively NULL). Sources: `docs/runtime/sessions/2026-06-12-a2-branch-a-avatar-host-selection.md` + `docs/runtime/sessions/2026-06-12-pin-primary-brand-host-v1-execution.md`. **Lane mutation footprint: 1 applied migration (26 UPDATEs on `c.brand_avatar`, 0 deletes) — 0 code / 0 DDL / 0 dashboard / 0 Advisor / 0 ai-worker / 0 policy / 0 provider / 0 worker / 0 cron change; this register pass is docs-only.** Authority impact: none (as-built decision flow + T1 unchanged). cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT / Asset-Production lanes preserved unchanged.

---

> **✅ v3.40 F-ADVISOR-LI-OVERRIDE CLOSED — diagnosed, expected Advisor correction behaviour under current config; evidence routed to the Option C/A2 review gate (read-only investigation + register-only close, 2026-06-12):** LinkedIn's ≈71.9% Advisor override (46/64; panel reproduced exactly; FB 29.3% / IG 11.3% / YT 38.9% / overall 35.5% this window) is NOT FB-format inheritance, NOT a policy violation, NOT publish-blocking. Cause stack: `preferred_format_linkedin` NULL for all clients → `fill_pending_slots` fallback stamps `image_quote` on 100% of LI slots → LI Advisor prompt receives no preference/bias instruction (FB does via `preferred_format_facebook='image_quote'`) → Advisor correctly picks content-fit formats for reactive/conversational LI content (`image_quote→text` 42/46, `→carousel` 4/46; sampled reasons uniformly cite the default-to-text rule or genuine multi-point structure) → `text` is LI-legal (`platform_support.linkedin=true`; IG excludes text, hence IG's 11.3% on the same default). **46/46 LI override rows have LinkedIn `m.post_publish` rows — published** (all still `approval_status='approved'` → F-PUBLISHER-STATUS-FLIP-INCONSISTENT corroborated). DB slot functions verified clean (`materialise_slots` reads correct per-platform columns; no inheritance). **F-AIW-PREF-COL-HARDCODE amended:** confirmed in deployed ai-worker source (`8204a5c7` — reads the `preferred_format_facebook` column for every platform) but inert today and NOT causal; latent config trap — **must be fixed before `preferred_format_linkedin`/`_instagram` is ever set.** Platform-systemic (all 4 clients elevated on LI: CFW 100% / Invegent 82.4% / NY 71.4% / PP 59.1%). The 33 LI compliance skips (v3.39) confirmed excluded from the panel base — no inflation; CFW skip concentration on LI (29/36 = 81%) noted for the CFW feed-scope carry. LinkedIn format steering = format-policy decision for the **Option C/A2 review gate (w/c 2026-06-15)**; this lane is direct evidence input. Stays P3. Evidence + patch text: `docs/runtime/sessions/2026-06-12-v3.40-f-advisor-li-override-close.md`. **0 code / 0 DB / 0 migration / 0 dashboard / 0 Advisor / 0 ai-worker / 0 policy / 0 Option C / 0 provider / 0 worker / 0 cron / 0 production mutation — 8 read-only SELECTs + 1 repo source read; 0 D-01 (read-only lane).** Authority impact: none. cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT / Asset-Production lanes preserved unchanged.

---

> **✅ v3.39 F-FORMAT-MIX-NOFORMAT CLOSED — diagnosed, subsumed by existing carries (read-only investigation + register-only close, 2026-06-12):** The Monitor Format Mix "No Format" bucket (≈15.6% overall / CFW ≈50.5%) is NOT a pipeline format failure. All 51 No-Format rows in the live 30d window (CFW 47/93 = 50.5%; Invegent 4/65 = 6.2%; NDIS-Yarns 0; Property-Pulse 0) are intentional relevance/compliance skips: `approval_status='dead'` + `draft_format.compliance_skip=true` + reason populated 51/51 + `dead_reason` column NULL + `recommended_format` unset because the draft is killed at the AI relevance step before final formatting (trace: slot `image_quote` → ai_job succeeded → kill → slot `skipped` → 0 queue/publish). Panel query reproduced exactly (51/325 = 15.7%) — dashboard accurately reports table state but the Format Mix base CTE has a **semantics gap** (counts dead/compliance-skipped drafts; no `approval_status` filter). Disposition: carried under the existing CFW intentional-skip observability/dashboard-semantics carry (P3); volume = CFW feed-scope review (P3); cost = F-CFW-COMPLIANCE-PREGEN (P3). NOT a new architecture or Advisor defect; dashboard tweak NOT implemented (future, gated). **F-ADVISOR-RESPIN-ORPHAN-SLIDES amended (2nd instance):** CFW draft `8e90cbd4-00f7-448c-af06-f1816a8e99ec` compliance-skipped/dead yet 12 succeeded Creatomate renders + 0 publish/queue rows — repeated-pattern signal; stays P3. Weekly CFW skip trend 5→10→11→14→7 (~11/wk steady). Evidence + patch text: `docs/runtime/sessions/2026-06-12-v3.39-f-format-mix-noformat-close.md`. **0 code / 0 DB / 0 migration / 0 dashboard / 0 Advisor / 0 policy / 0 Option C / 0 provider / 0 worker / 0 production mutation — 7 read-only SELECTs + 2 repo file reads; 0 D-01 (read-only lane).** Authority impact: none. cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT / Asset-Production lanes preserved unchanged.

---

> **✅ v3.38 MONITOR DASHBOARD VISIBILITY (Slices 4–6) — LIVE on production (2026-06-12):** Second tranche of the Monitor visibility layer, exposing the next audit-found blind spots. **Three read-only panels added under Monitor** (`invegent-dashboard`, `dashboard.invegent.com`), below Slices 1–3: **(4) Render + Avatar Health** (`m.post_render_log`: render success/failure, count by provider `render_engine` + format `ice_format_key`, provider error categories, retry/attempt pattern, **`credits_used` null-rate**, avatar render count + **avatar_identity populated/missing** + stakeholder_role coverage + `avatar_selected_by` distribution, avatar fields read from `render_spec` jsonb); **(5) Performance Freshness** (client × platform: published count, perf rows, last `collected_at`, hours/days-since, stale >48h + missing flags — surfaces YouTube staleness, FB freshness, IG/LI/website coverage gaps); **(6) Pipeline Exceptions** (stuck AI jobs, dead AI jobs by reason, failed slots, intentional slot skips by category, **compliance-skip vs `dead_reason` conflation** = the v3.32 finding, publish-queue states + queue orphans, approved-unpublished >3d). **Read path:** the EXISTING `exec_sql` service-role server-action path (`lib/supabase/sql.ts`), SELECT-only — **no DB objects added, no new RPCs, no migrations, no mutation, no worker/provider calls.** **dashboard PR #7 MERGED to `main`** (merge commit `417210c`); **Vercel production deploy `dpl_A9dr9scRF1u2346FbaEoQsaR9UJ4` READY** (commit `417210c`, alias `dashboard.invegent.com`) — Slices 4–6 LIVE (verified by deployment SHA + domain alias; auth-gated, visual confirmation as for Slices 1–3). Branch `monitor/render-perf-exceptions-panels` @ `1e26bc2` (5 files: `actions/monitor-health.ts`, `components/monitor/{RenderAvatarHealthPanel,PerformanceFreshnessPanel,PipelineExceptionsPanel}.tsx`, `app/(dashboard)/monitor/page.tsx`). Build: `tsc --noEmit` exit 0, `next build` exit 0; all 3 panels screenshot-verified against production data on local preview. **Open follow-up (carried):** pre-existing `/overview` `created_at` SQL error (unrelated; not investigated). **0 DB change / 0 classifier change / 0 Advisor change / 0 policy reseed / 0 Option C reconnection / 0 render-logic change / 0 publisher change / 0 compliance change / 0 cron change / 0 worker run / 0 provider call.** Dashboard-visibility lane only (repo `invegent-dashboard`). cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT / Asset-Production lanes preserved unchanged. **Authority impact: none** (read-only surfacing of existing telemetry; as-built decision flow + T1 unchanged).

---

> **✅ v3.37 MONITOR DASHBOARD VISIBILITY (Slices 1–3) — LIVE on production (2026-06-12):** The first operational visibility layer for ICE is live, exposing the signals the recent audits proved invisible (classifier flattening, defaulted-vs-rule-matched conflation, advisor override behaviour, format monotony). **Three read-only panels added under Monitor** (`invegent-dashboard`, `dashboard.invegent.com`): **(1) Classifier v2 Health** (version split, class + method distribution, defaulted-analytical headline, top firing rules, body/insufficient rates — global, classification is upstream of client assignment); **(2) Advisor Override** (override rate + by client/platform/week + slot→final override matrix, 30d); **(3) Format Mix** (final `recommended_format` usage overall/by platform/by client, complete `FORMAT_LABELS` coverage, 30d). **Read path:** the EXISTING `exec_sql` service-role server-action path (`lib/supabase/sql.ts`), SELECT-only — **no DB objects added, no new RPCs, no migrations, no mutation.** **PR #6 MERGED to `main`** (merge commit `c168d2c`); **Vercel production deploy `dpl_7we82KUmm47j8jSEC4UsbSAa6tnc` READY** (commit `c168d2c`, alias `dashboard.invegent.com`); **panels VISUALLY CONFIRMED live by PK** in the logged-in UI. Branch `monitor/classifier-advisor-format-panels` @ `a2d4c82` (7 files: `actions/classifier-metrics.ts`, `components/monitor/{ClassifierHealthPanel,AdvisorOverridePanel,FormatMixPanel,metric-bits}.tsx`, `lib/format-labels.ts`, `app/(dashboard)/monitor/page.tsx`). **Open follow-up:** pre-existing `/overview` `created_at` SQL error (surfaced in dev logs during navigation; UNRELATED to these panels; NOT investigated this lane). **0 DB change / 0 classifier change / 0 Advisor change / 0 policy reseed / 0 Option C reconnection / 0 render/publisher change / 0 extra panels.** This is a dashboard-visibility lane only — repo `invegent-dashboard`, not content-engine. cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT / Asset-Production lanes preserved unchanged. **Authority impact: none** (read-only surfacing of existing telemetry; the as-built decision flow and T1 are unchanged).

---

> **✅ v3.36 ICE CLASSIFIER v2 REPAIR — APPLIED to production + validated (2026-06-12):** Stage-1 bounded repair of the rule-based PL/pgSQL classifier (`m.classify_canonical` / `m.evaluate_rule` / `m.classify_canonicals_unclassified`) per the 2026-06-12 concentration audit (`ef53676`; 92.4% analytical, "Mixed" verdict). **Production applied** to `mbkmaxqhsohbtwsqolns` under PK exact-phrase approval — migration `classifier_v2_repair_and_telemetry` (prod version `20260611223123`) + 2 apply-time hotfixes (`…223306` `::text` array-append, `…223401` `array_agg` for uuid — both PL/pgSQL semantic bugs caught by the end-to-end test, folded into the repo file). **Changes:** (1) 3 dead source rules fixed — `stat_heavy` source-name `exact`→`substring` (matches "RBA - Media Releases"), `timely_breaking` dead source-type codes → selective gov/news `source_name` signal (~17/30d, no overlap with stat data sources), `analytical` dead codes → live `email_newsletter`; (2) 6 brittle/FP regex repairs (`^why` 30→5, `explained:` colon, "10 Money Lessons", `breaking` verb→news sense, mid-title `meet` anchored); (3) recency isolated to immutable `created_at` (was drifting `first_seen_at`, 93.9% drift); (4) **explanation telemetry** — +9 additive columns on `f.canonical_content_body` (`classification_method`/`matched_rule_id`/`_name`/`_priority`/`matched_signal`/`input_used`/`body_available`/`defaulted`/`insufficient_content`), outward `content_class` label UNCHANGED; (5) `v1`→`v2`, picker NULL-only (no auto-backfill), new opt-in `m.reclassify_canonicals`. **Smoke PASSED** (live, post-apply): Validation A (dead rules fire via deployed configs) / B (defaulted vs rule_matched distinguishable) / C (full telemetry populated) / D (distribution sanity). **100-row bounded backfill COMPLETED** (`m.reclassify_canonicals(100,100)` → 100 rows v1→v2): of 70 analytical, **8 rule_matched / 62 defaulted** — the backstop-conflation observability win made concrete (an RBA item wrongly stored analytical now classifies `stat_heavy`). **Remaining ~4,371 v1 rows INTENTIONALLY UNTOUCHED** — forward cron (job 68, `*/5`) classifies new items as v2; any further backfill is a separate gated `m.reclassify_canonicals` run. **Branch/commit:** `repair/classifier-v2-telemetry` @ `48ea045` (commits `ea0117d` migration+smoke, `48ea045` hotfix reconciliation), pushed; **PR #7 MERGED to `main`** (merge commit `2be28040`, 2026-06-11 — repo↔prod reconciliation complete; the merge was NOT a deploy, production was already live on v2). Smoke artifact `supabase/tests/classifier_v2_repair_smoke.sql`. **Carries (NEW):** remaining-v1 bounded backfill (opt-in, gated); `first_seen_at` drift data-quality bug (bypassed for classification, underlying drift untouched — audit carry); repo↔prod migration-version drift (`20260612093000` file vs `20260611223123` prod). **Out of scope / untouched:** Advisor logic, format policy, Option C / mix tables, publisher/render, classify-after-fetch (D2). **0 Advisor change / 0 policy reseed / 0 Option C reconnection / 0 deploy / 0 publisher/render change.** cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT / Asset-Production lanes preserved unchanged. **Authority impact: none** (current ICE decision tree T1 describes the classifier as the upstream signal layer; this repair improves classifier accuracy + observability without changing the as-built decision flow — T1 unchanged).

---

> **✅ v3.35 REGISTER CYCLE CLOSE — Option C decision-state arc recorded + parked (docs-only, 2026-06-11):** The 2026-06-11 format-decision-tree discovery arc (trace audit → slot decomposition → authority repair → ownership audit → format-policy history audit → decision brief → pool feasibility → Option C replay → strategic design brief) is now closed; architecture work is **parked until the telemetry review gate.** **Authority foundation (this cycle):** T1 authority doc established + docs-index authority switch + signage all landed (see the v3.35 authority-repair banner below; `160678b8` / `9d97f726`); **governance Rules 1, 2, 3, 7, 6a ADOPTED** including the mandatory **`Authority impact:` completion line** (now active on every change touching the current ICE decision tree); **two read-only T1 amendments** landed — `baba5537` (dormant format-policy-subsystem history) + `8e19dd9f` (Option C replay findings) — both authored/verified by CCH with the **live chain unchanged**. **Decision state:** **Option C (policy envelope + advisor fit) is recorded as the PREFERRED REFERENCE HYPOTHESIS** — the standard against which Option A (formal retirement) and Option B (continued dormancy) are compared at the review gate — **NOT approved for implementation.** **The advisor-led / constants model remains the live production architecture** (Demand → Slot → Fill → Advisor → Draft → Render → Publish, T1 §2–§3); **zero** policy-envelope reconnection, **zero** narrative layer, **zero** A2/avatar change approved. **Narrative-vs-format remains an OPEN question** (format is a proxy for narrative; not closed by this arc, not evidence-backed enough to act on, not a reason to delay the Option C decision). **Review gate scheduled w/c 2026-06-15, or earlier on meaningful `avatar_identity` telemetry** (heygen-worker v2.1.1+); **A2 and the format-policy fork remain ONE decision surface**, avatar-share decision unresolved, `avatar_identity` telemetry the gating evidence. **Mix-policy contradiction on record:** the dormant demand-grid subsystem ran live 2026-04-25, was orphaned by the emergency slot rebuild, and was **never formally rejected**; its 2026-04-22 mix seed carries **taxonomy-illegal allocations** (~19% of seeded cells). Decision-state record: `docs/runtime/sessions/2026-06-11-option-c-decision-state.md` (`f95fc46`). **Rule 6b still deferred** — detection / Cowork-reconciliation wiring outstanding (no `05_risks` change). **0 production mutation / 0 code / 0 DB / 0 migration / 0 Edge Function / 0 deploy / 0 provider / 0 cron change** across the entire discovery arc. cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT / Asset-Production lanes preserved unchanged.

---

> **✅ v3.35 Authority repair COMPLETE — the current ICE decision tree now has a source of truth (docs-only, 2026-06-11):** The standing "which doc is authoritative for the current (as-built) ICE content decision tree" gap is closed. **T1 authority doc established:** `docs/architecture/current-ice-decision-tree.md` (commit `160678b8`) is now the single source of truth for the current ICE content decision tree and carries a `last_verified` contract. **Docs index now routes to T1** — `docs/00_docs_index.md` was switched to point at the T1 authority doc in the **same atomic commit** (`160678b8`, +129/−8 across the index + the new doc). **Signage banners landed** (`9d97f726`, +18): `docs/01_README.md` / `docs/03_blueprint.md` / `docs/04_phases.md` / `docs/concepts/ice-pipeline-operating-model.md` now carry authority-pointer banners directing readers to T1 for current-state decisions (those docs describe intended/historical design, not the as-built tree). Supporting trail: authority spec `3958e4f`; v3.34 doc sync `97e25bd`; knowledge-capture & authority-promotion audit `e39fa3a`; slot-level decomposition `b74c8e50`. **Authority impact rule ADOPTED** — every change that touches the current ICE decision tree must state its authority impact and, where it changes as-built behaviour, update T1. **Governance adopted** (recorded in `docs/00_action_list.md`): T1 authority-doc rule, docs-index authority section, the mandatory **"Authority impact:" completion line**, CCD ownership for >80KB register/authority patches, and the `last_verified` contract in T1. **Rule 6b deliberately NOT implemented this cycle** — no `05_risks` change, no Cowork reconciliation wiring (both deferred). **0 production / 0 code / 0 DB / 0 migration / 0 Edge Function / 0 deploy / 0 provider / 0 cron change** — docs-only authority repair + register cycle. cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT / Asset-Production lanes preserved unchanged.

---

> **✅ v3.34 Content decision trace audit COMPLETE (read-only, 2026-06-11):** Full 7-day bidirectional trace audit of production content decisions; report at `docs/runtime/sessions/2026-06-11-content-decision-trace-audit.md` (commit `6fd5f4d`). **Headline: all 5 representative backward traces GREEN** (publish→draft→ai_job→slot/fill-attempt→canonical→content_item→raw→feed_source→ingest_run) across all 4 clients and 4 platforms; digest tables are NOT in the live chain (`digest_item_id` NULL on all window drafts; legacy seed-and-enqueue crons disabled). **Decision model confirmed:** `materialise_slots` stamps deterministic format preference (FB pinned `image_quote` via `preferred_format_facebook`; **YouTube hardcoded `video_short_avatar` in function source** — the true home of the A2 override); `fill_pending_slots` is deterministic (`format_preference[1]` else `image_quote`, gated by `t.format_*_policy`); **ai-worker `callFormatAdvisor` is the final format decision-maker** and routinely overrides the fill choice (proven: slot `image_quote` → final `text` / `video_short_stat`); avatar is the sole deterministic exception, with `advisor_would_have` recorded as prose only. **Format = narrative proxy** (kinetic=scenes[], stat=stat_reveal, avatar=first-person narration; no independent narrative dimension). **Avatar path classified: arbitrary-first** (`lookupAvatar` LIMIT 1, no ORDER BY, `stakeholder_role` never emitted by ai-worker) despite a 15-row role-aware `c.brand_avatar`×`brand_stakeholder` inventory (NY 7 / PP 8 active realistic). **F-HEYGEN-RENDER-TELEMETRY first-row verification SATISFIED** — 2 genuine heygen rows in `m.post_render_log` (2026-06-10, drafts `dcab6fba` PP + `d870f20a` NY, succeeded, provider_job_id + durations present); `avatar_identity` null on both (submits pre-dated v2.1.1); register shows heygen-worker **v2.1.1** A-LE (avatar-identity telemetry patch atop v2.1.0). The single unrecoverable per-post fact: which avatar face/voice was used on the 9 in-window avatar publishes. **New P3 carries (see action_list):** F-ADVISOR-RESPIN-ORPHAN-SLIDES, F-YT-QUEUE-ORPHAN-RECURRENCE, F-FORMAT-MIX-UNWIRED, F-AIW-PREF-COL-HARDCODE, F-EFDRIFT-REGISTER-STALE-YTPUB. Window health: 64 publish rows (61 published / 3 PP-FB `facebook_photo_http_500` later succeeded); CFW null-format dead drafts re-confirmed 100% intentional compliance skips (v3.32 class). **Recommendation: no-build recommended at this time** — instrumentation already landed (v2.1.1); single branded host = config-only; role-aware avatar = wiring-only; **narrative engine not justified by current evidence — the narrative layer remains an open architecture question, not a closed design decision.** **0 production mutation / 0 DB write / 0 D-01 / 0 deploy / 0 provider call / 0 cron change / 0 OBS / 0 Stage 0B / 0 cc-0020.** Repo: report file only (`6fd5f4d`) + this `00_` close. cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT / Asset-Production lanes preserved.

---

> **✅ v3.33 Asset Production routing synthesis — NO production routing defect (read-only, 2026-06-10):** The provider-routing-skew concern is **quantified and closed-as-unknown / reclassified as a policy decision, not a defect.** The earlier "avatar zero-output" reading was a **measurement error** — the correct realised-output metric is **YouTube `video_status` + `youtube_video_id`, NOT social `approval_status`.** Corrected read of the current **30-day window:** avatar **realised 18/22**; avatar share **≈8% overall / ≈12% NDIS+PP**; the prior **~19.7% skew was NOT reproduced** in the current 30-day window. **20/22 avatar selections are A2 overrides** (advisor-driven), so the remaining question is **policy (keep avatar volume vs relax content-fit), not a routing defect.** Consequence: **OBS Stage 0 is de-urgenced for routing** — there is no routing pipeline defect that an observer would surface. **OBS remains contained/parked** (`obs_readonly` NOLOGIN; ratify-vs-rollback pending); **Stage 0B not started**; no observer/scheduler. **Next step unchanged:** capture the **first genuine HeyGen telemetry row** (F-HEYGEN-RENDER-TELEMETRY; heygen-worker v2.1.0 deployed v3.31), then **monitor the avatar tail.** New P3 carry added to `00_action_list.md`: **A2 avatar-override policy decision** (keep volume vs relax content-fit). **0 production mutation / 0 DB / 0 D-01 / 0 deploy / 0 routing change / 0 observer / 0 scheduler / 0 Stage 0B / 0 provider-API call / 0 cc-0020.** Read-only synthesis only. cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT / Asset-Production lanes preserved.

---

> **✅ v3.32 CFW "silent image-generation loss" DIAGNOSED — NOT a production defect (read-only investigation, 2026-06-10):** The reported CFW silent image-gen loss (~12 drafts/week) is **not a production defect and not an image-generation failure** — it is a **monitoring/accounting gap on intended workflow.** Read-only evidence: the **61 CFW "silent" dead drafts** (`recommended_format=NULL`, `approval_status='dead'`, `dead_reason` column NULL, **2026-05-04 → 2026-06-09**) are **100% intentional relevance/compliance skips** — `draft_format.compliance_skip=true` 61/61 + populated `draft_format.reason` 61/61, **0 genuinely empty**. The AI relevance step correctly declined out-of-scope signals for CFW's narrow scope (e.g. One Nation politics, a US childcare PR release, an emergency-rescue item, clubfoot outcomes); they never reached formatting/image generation. The reason IS recorded — in `draft_format.reason`/`compliance_skip`, NOT in the `dead_reason` column the monitor keys on → false "silent loss" signal. CFW's live `image_quote` pipeline generates + publishes through 2026-06-10 (52/60 image-capable drafts have `image_url`; **0 `image_quote` failures**; the 8 no-url cases are late-April historical). **Client scope:** CFW 61 + Invegent 6 (identical signature; both narrow-scope) — **NDIS-Yarns + Property-Pulse show ZERO** silent-dead (their dead drafts all carry `dead_reason`, all historical). **Business impact LOW** — CFW is not losing publishable content; cost is false-alarm noise + minor generate-then-skip AI spend. **No fix implemented.** Carries added to `00_action_list.md`: (1) intentional-skip observability/accounting improvement (distinguish skips from failures; surface `draft_format.compliance_skip`/`reason` to monitoring) — P3; (2) OPTIONAL low-priority CFW feed-scope review — P3. **0 DB mutation / 0 D-01 / 0 deploy / 0 provider call / 0 image generation / 0 queue/draft/status mutation / 0 OBS / 0 observer / 0 scheduler / 0 Stage 0B / 0 cc-0020.** Read-only diagnosis only. cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT / Asset-Production lanes preserved.

---

> **✅ v3.31 HeyGen render-telemetry v2.1.0 DEPLOYED + D-01 `0d9bb255` CLOSED (2026-06-10) — Asset Production Rank #1 first step EXECUTED:** The decoupled Asset-Production entry slice is live. `heygen-worker` deployed **v2.0.0 → v2.1.0** (from `main` @ `690a295b`) via the `safe-deploy.sh` CLI wrapper (`--allow-warn`; **no MCP deploy path**; Docker-off warning benign) under PK exact-phrase approval of **D-01 `0d9bb255`** (`ef_deploy`), which **CCH independently verified + closed `completed`** on `m.chatgpt_review`. **Telemetry-only** (+93/−3 in `supabase/functions/heygen-worker/index.ts`): best-effort/idempotent/non-fatal `writeRenderLog()` mirrors terminal render outcomes (succeeded/failed/timeout) into `m.post_render_log` via the existing engine-agnostic `public.write_render_log` RPC — **no schema, no migration, no new HeyGen API call, no render/publish/routing/scheduling/provider-selection/business-logic change.** **Post-deploy verification (all PASS):** health GET `{"ok":true,"function":"heygen-worker","version":"heygen-worker-v2.1.0"}` HTTP 200; deployed-hash recompute via `drift-check` **dry-run (write=false — no DB write)** → class **A-LE / clean**, `deployed_hash_normalised == repo_hash_normalised == 74bdbada…aab26e66`, `deploy_version 2.1.0`, `security_definer_regression_risk=false`; `write_render_log` RPC present with the exact 13-param signature the patch calls. **Baseline telemetry:** `m.post_render_log` = 1484 `creatomate` / **0 `heygen`** — closes the HeyGen-renders-invisible gap. **Functional first-row telemetry evidence PENDING** the next **genuine cron-driven terminal avatar render** (worker NOT invoked; no HeyGen call) — tracked by new carry **F-HEYGEN-RENDER-TELEMETRY first-row verification**. Note: the cached `m.vw_ef_drift_current` register still shows stale `B-FD` (no persistent drift-log refresh — honours no-DB-mutation); it self-heals on the next scheduled `drift-check` tick. **Asset Production Rank #1 first step (v3.30 plan) now EXECUTED.** OBS stays contained/parked (`obs_readonly` NOLOGIN; ratify-vs-rollback pending) — no observer/scheduler/0B. cc-0015 / cc-0020 / OBS-0A (v3.28) / PRV / IG / YT lanes preserved unchanged.

---

> **📌 v3.30 strategic priority re-rank (2026-06-10, PK decision; docs-only — 0 production mutation / 0 DB / 0 deploy / 0 D-01 / 0 HeyGen deploy / 0 OBS mutation / 0 observer / 0 scheduler / 0 Stage 0B / 0 cc-0020 implementation / 0 provider-API call):** Build bandwidth reassigned: **Asset Production = formal Rank #1**; **cc-0020 Stage 5 = Rank #2 / idle-guard**. Rationale: cc-0020 is useful but internal and self-contained; Asset Production is the core ICE value lane; parking cc-0020 is low-regret (work inert, kill-switch `SUBSCRIPTION_GMAIL_INGEST_ENABLED=false` OFF, no runtime decay); leaving Asset Production gaps open has ongoing cost (HeyGen/avatar telemetry blindness, CFW silent image-gen loss, routing-skew uncertainty, voice-lowvol backlog). **Asset Production enters via the DECOUPLED slice — HeyGen render-telemetry v2.1.0 / D-01 `0d9bb255` (PK exact-phrase approval required) — NOT via OBS or Stage 0B.** OBS remains contained/parked (`obs_readonly` NOLOGIN; ratify-vs-rollback pending) — no observer / scheduler / 0B work. **cc-0020 Unit A OAuth may proceed PK-side in parallel** (PK legwork, not build bandwidth); cc-0020 remains the fallback/idle-guard whenever Asset Production waits on a PK approval. Docs-only priority ranking — no code, no deploy, no DB, no D-01, no HeyGen deploy. Supersedes the v3.18 "Next-session priorities" ranking in `00_action_list.md`. cc-0015 / cc-0020 / OBS-0A (v3.28) / PRV / IG / YT lanes otherwise preserved unchanged.

---

> **✅ v3.29 YT straggler carry `79326fc8` DISCHARGED — archive_stale cleanup APPLIED (2026-06-10):** The cc-0015 carry "YT straggler case `79326fc8` archive_stale-vs-requeue decision" is resolved. Under **sql_destructive D-01 `482f7ff3`** (PK exact-phrase approved + applied; closed-loop `completed` on `m.chatgpt_review`, `resolved_by='PK (exact-phrase approval) + CCD (applied)'`), the **20 legacy YouTube `m.post_publish_queue` queued rows** (NDIS-Yarns 10 + Property-Pulse 10) were archived stale (`status=dead`, `dead_reason=archive_stale_legacy_queue_79326fc8_2026-06-10`, `acknowledged_by=pk`): **YT queue queued 20→0, dead 21→41**, tagged-archived 20, **21 prior-dead siblings untouched**. Read-only review found requeue-viability **0/20** (12 already published on YouTube with `youtube_video_id` set, 7 `text`-format not YouTube-publishable, 1 unrendered) on a draft-decoupled legacy queue silent ~3 weeks. Friction case `79326fc8` resolved **`tracked_done`** (`resolved_at` set; P3 note appended to case notes via `friction.fn_triage_case`, no draft mutation). **No publisher run / no requeue / no YouTube-API / no provider call / no draft mutation / no OBS/HeyGen/Stage 0B work.** **P3 carry (NEW):** draft `0e486d12` (NDIS-Yarns `video_short_stat_voice`, post_draft `a44288f7`) — re-render only if still wanted; not blocking. The v3.24/v3.27 historical banners are preserved verbatim (the "`79326fc8` open/track" wording was true at the time). cc-0015 / cc-0020 / OBS-0A (v3.28) / PRV / IG / YT-publisher lanes preserved unchanged.

---

> **⚠️ v3.28 OBS Stage 0A governance reconciliation + Option C containment APPLIED (2026-06-10):** A 2026-06-09 session built and applied the OBS Stage 0A production read-path after the 2026-06-08 recommendation-C hold, but the `00_` files were not updated at the time. Production `mbkmaxqhsohbtwsqolns` now contains G-RP `20260609081127_obs_readonly_production_read_path_g_rp` (D-01 `5fed9a06`) creating `obs_readonly` with column-level SELECT grants on 4 `m.*` tables / 45 columns, plus G-RP-RLS `20260609103639_obs_readonly_rls_slot_origin_select_g_rp_rls` (D-01 `9b03b489`) creating policy `obs_readonly_read_slot_origin` on `m.post_draft`. Governance gap remains unresolved: OBS-0A rows `51752332` / `5fed9a06` / `c5a7cb3c` / `9b03b489` remain open/escalated and were not closed or ratified. Reconciliation found `obs_readonly` was not effectively read-only because reachable `m`/`public` SECURITY DEFINER functions created a write-capable RPC surface. Option C containment was approved by PK via D-01 `18ba635d` / `18ba635d-9492-4910-bc03-4ba9ab77fd7a` and applied to production: `ALTER ROLE obs_readonly NOLOGIN;`. Verified result: `rolcanlogin` true→false; `NOBYPASSRLS` preserved; no memberships; no write table grants; 45 column grants, RLS policy, and prior migrations preserved; zero active connections; four OBS-0A rows untouched. This is containment only: NOT ratified, NOT rolled back, role NOT dropped. Observer parked, scheduler disabled, Stage 0B not started, HeyGen untouched. Open next decision: PK to choose ratify-vs-rollback and reconcile the four original OBS-0A rows.

---

> **✅ v3.27 video-worker caption-hierarchy polish (v3.1.3) DEPLOYED to prod (2026-06-02):** **1 EF deploy (`video-worker` → `video-worker-v3.1.3`) via `./scripts/safe-deploy.sh video-worker --allow-warn`; 1 FF-merge to main (`f0c9682..fc3ec12`); 6 targeted `m.ef_drift_log` register refreshes (4 stale `A-LE` during GitHub raw-CDN propagation polling, 1 confirming `B-FD` pre-deploy, 1 final post-deploy `A-LE`); 0 schema / 0 migration / 0 cron/queue / 0 publisher/social / 0 image-worker / 0 webhook / 0 render / 0 ElevenLabs/Creatomate call / 0 production draft mutation / 0 backfill.** Caption-styling-only P3 polish so `video_short_kinetic_voice` burned-in captions read as **secondary/subtitle-like** vs the main on-card kinetic text. Branch `polish/video-worker-caption-hierarchy`, commits **`f9be78c`** (caption styling; `index.ts` only +5/−5) + **`fc3ec12`** (`VERSION` bump `video-worker-v3.1.2`→`video-worker-v3.1.3`). **Shipped behaviour:** caption `font_size` **52px→40px**; `font_weight` **700→500**; scrim `opacity` **0.55→0.62**; caption colour **remains `#FFFFFF`**; **`CAP_MAX_CHARS` remains `64`**; caption comments updated to `40px`. **Geometry, timing, chunking, audio, voice, `getVoiceId`, templates, webhook, queue, DB, stat formats, and non-kinetic formats all UNCHANGED.** **Verify:** `deno check` exit 0; **CCH code review PASS**; **CCB visual review PASS** (local Chrome-headless before/after full-frame + scrim-contrast renders, built from the real spec builders, served from the git-ignored `_harness/` over `localhost:1234`). **Deploy path (in-design, wrapper only — no raw `supabase functions deploy`):** FF-merge `f0c9682..fc3ec12` → push `main` → targeted `drift-check?write=true&slug=video-worker` refresh → **B-FD** (repo v3.1.3 ahead of deployed v3.1.2; `repo_hash 0991ff65… ≠ deployed fad6f623…`) → `safe-deploy --check-only` **BLOCK exit 1** (expected gate on B-FD) → `--check-only --allow-warn` **PASS exit 0** → `--allow-warn` deploy **exit 0** → re-refresh → **A-LE / clean / in-spec** (`repo_version 3.1.3 == deploy_version 3.1.3`; `repo_hash_normalised == deployed_hash_normalised == 0991ff6554fbf7c8dcba1debe47e179efcf95d9ff315ce9f41b53fd4b4088bd7`). Health GET = `{"ok":true,"function":"video-worker","version":"video-worker-v3.1.3"}` HTTP 200. **D-01 CCD-inline** (PK phrase: `I approve ef_deploy for video-worker F-CREATOMATE-CAPTION-HIERARCHY from branch polish/video-worker-caption-hierarchy at commit fc3ec1279f1591c364bc101a14d9de496167b17e.`) — no `m.chatgpt_review` row fabricated. **L-v3.27-a:** `safe-deploy` reads the **cached** `m.vw_ef_drift_current` register (not a live recompute), so the mandatory path is **merge → targeted refresh → B-FD → `--allow-warn` deploy → final refresh → A-LE**; GitHub raw-CDN propagation lag can briefly mis-report `A-LE` immediately post-push — **poll the targeted refresh until the persisted register shows B-FD before deploying.** **F-CREATOMATE-CAPTION-HIERARCHY now CLOSED (v3.27).** **Deferred:** **F-VOICE-LOWVOL-BACKFILL (P3, separate carry)** — ~21 PP+NDIS already-rendered voice videos carry pre-fix ~−58 LUFS audio (re-render decision is PK's); optional `polish/video-worker-caption-hierarchy` branch cleanup (merged; **still exists locally + origin**; deletion needs separate approval). Session record `docs/runtime/sessions/2026-06-02-v3.27-video-worker-caption-hierarchy-deploy.md`. cc-0015 / cc-0020 / cc-0016 / PRV / IG / YT lanes preserved unchanged.

---

> **✅ v3.26 video-worker getVoiceId guard (v3.1.2) DEPLOYED to prod (2026-06-02):** **1 EF deploy (`video-worker` → `video-worker-v3.1.2`) via `./scripts/safe-deploy.sh video-worker --allow-warn`; 1 FF-merge to main; 2 targeted `m.ef_drift_log` register refreshes; 0 schema / 0 migration / 0 cron/queue / 0 publisher/social / 0 image-worker / 0 webhook / 0 render / 0 ElevenLabs/Creatomate call / 0 production draft mutation.** Narrow voice-selection safety guard closing the new-brand onboarding risk found in the v3.25 voice/audio-unification investigation. Branch `feat/video-worker-getvoiceid-guard`, commit `851bc81` (+17/−3, `index.ts` only): `getVoiceId()` final fallback `ELEVENLABS_VOICE_ID_NDIS ?? null` → **`return null`** (no silent NDIS default for unrecognised slugs); caller throw clarified to `No ElevenLabs voice ID configured for client_slug=<slug>` (existing path marks draft `failed` + `post_render_log`); `v3.1.2` changelog block; `VERSION` bumped `v3.1.0→v3.1.2`. **Behaviour:** exact `ELEVENLABS_VOICE_ID_<NORMALISED_SLUG>` still wins; `ndis*`→`ELEVENLABS_VOICE_ID_NDIS`, `property*`/`pp*`→`ELEVENLABS_VOICE_ID_PP` unchanged; unknown/unmapped slug now **fails loudly** instead of rendering in the wrong (NDIS) voice. **Verify:** `deno check` exit 0; Tier-1 scratch env-matrix 6/6 PASS (uncommitted `_harness/`, fake values — incl. core guard: unknown brand + only NDIS env → null); no test scaffold committed. **CCH read-only review APPROVED.** **Deploy path (in-design, wrapper only):** FF-merge `fb254bc..851bc81` → targeted `drift-check?write=true&slug=video-worker` refresh → **B-FD** (repo v3.1.2 ahead of deployed v3.1.0; `repo_hash fad6f623… ≠ deployed cfbacd8b…`) → deploy `--allow-warn` (exit 0) → re-refresh → **A-LE / clean / in-spec** (`repo_version 3.1.2 == deploy_version 3.1.2`; `repo_hash_normalised == deployed_hash_normalised == fad6f623…d7f0cc`). Health GET = `{"version":"video-worker-v3.1.2"}` HTTP 200. **D-01 CCD-inline** (PK phrase `APPROVE ef_deploy D-01: deploy video-worker 851bc81 to production with --allow-warn`) — no `m.chatgpt_review` fabricated. **Voice/audio safety pass now COMPLETE in prod:** v3.1.0 kinetic_voice captions + v3.1.1 audio-volume contract fix (in deployed source) + v3.1.2 voice-mapping guard. **L-v3.26-a:** silent per-client config fallbacks are onboarding traps — prefer fail-loud over silent default for identity-bearing config. **Deferred:** F-CREATOMATE-CAPTION-HIERARCHY (P3, carried); **F-VOICE-LOWVOL-BACKFILL (P3, NEW)** — ~21 PP+NDIS already-rendered voice videos carry pre-fix ~−58 LUFS audio (deploy fixes future renders only; re-render decision is PK's); optional `feat/video-worker-getvoiceid-guard` branch cleanup (merged; deletion needs separate approval). Session record `docs/runtime/sessions/2026-06-02-v3.26-video-worker-getvoiceid-guard-deploy.md`. cc-0015 / cc-0020 / cc-0016 / PRV / IG / YT lanes preserved unchanged.

---

> **✅ v3.25 video-worker Creatomate Pass-1 (kinetic_voice captions + audio-volume fix) DEPLOYED to prod (2026-06-02):** **1 EF deploy (`video-worker` → `video-worker-v3.1.0`) via `./scripts/safe-deploy.sh video-worker --allow-warn`; 1 FF-merge to main; 2 targeted `m.ef_drift_log` register refreshes; 2 isolated harness renders; 0 schema / 0 migration / 0 cron/queue / 0 publisher/social / 0 image-worker / 0 webhook / 0 production draft mutation.** Shipped (branch `feat/video-worker-pass1-kinetic-voice-captions`, commits `10bca33` kinetic_voice burned-in captions + `de749ed` Creatomate audio-volume contract fix): (1) Pass-1 burned-in captions for `video_short_kinetic_voice` ONLY (deterministic duration-proportional timing; pure + fail-safe `buildCaptionElements`; non-voice/stat/stat_voice byte-unchanged); (2) **audio-volume fix** — Creatomate `volume` is a **percentage string** (`"0%"`–`"100%"`, default `"100%"`), not a 0–1 fraction; numeric `1.0` was read as **1%** (−40 dB voice attenuation). Fix: voice `1.0→"100%"`, music `0.15→"15%"` in both kinetic + stat builders (4 values); `v3.1.1` changelog comment; **`VERSION` const not bumped**. **Evidence:** visual-only render PASS (notes); voice-backed harness render 1 revealed low audio (source mp3 **−18.1 LUFS** vs final mp4 **−58.2 LUFS**, ffmpeg `volumedetect`/`ebur128`); diagnosis pinned the −40 dB to the percentage misread; post-fix harness render 2 restored final mp4 to **−17.4 LUFS** ≈ source **−17.3 LUFS** (Δ 0.1 LU); **human listening: loud and clear**. Harness ran **isolated** (`_harness/` untracked+gitignored; synthetic narration; no real draft; mp3→`post-videos/_harness/…` only; no `write_render_log`; mp4 local-only). **Deploy path (in-design, wrapper only — no raw `supabase functions deploy`):** `safe-deploy` reads the **cached** `m.vw_ef_drift_current`, so a fresh-merged feature branch first blocked **A-LE** ("redeploy unnecessary"); merged `de749ed` FF to main (`7ca9ea3..de749ed`), **targeted** `drift-check?write=true&slug=video-worker` refresh → **B-FD** (repo v3.1.0 ahead of deployed v3.0.0), deployed with `--allow-warn` (exit 0), re-refreshed → **A-LE / clean / in-spec** (`repo_version 3.1.0 == deploy_version 3.1.0`; `repo_hash_normalised == deployed_hash_normalised == cfbacd8b…80ebc`). Health GET = `{"version":"video-worker-v3.1.0"}` HTTP 200. **D-01 CCD-inline** (APPROVE WITH CONDITIONS; PK phrase `APPROVE ef_deploy D-01: deploy video-worker de749ed to production with --allow-warn`) — no `m.chatgpt_review` row fabricated. **Lessons:** L-v3.25-a (safe-deploy = cached register, not live recompute; the 6-step merge→refresh→B-FD→`--allow-warn`→refresh→A-LE path; never bypass the wrapper); L-v3.25-b (Creatomate volume is a percentage string; verify audio by measuring LUFS, not by ear). **Deferred follow-up: F-CREATOMATE-CAPTION-HIERARCHY** (P3 — make voiceover captions visually distinct from main on-card text; caption-styling-only; NOT in this patch). Session record `docs/runtime/sessions/2026-06-02-v3.25-video-worker-pass1-creatomate-deploy.md`. cc-0015 / cc-0020 / cc-0016 / PRV / IG / YT lanes preserved unchanged.

---

> **✅ v3.24 cc-0015 register hygiene COMPLETE + Stage D Phase 1 live in prod (2026-06-02):** friction register reconciled to **48 total / 14 open / 34 resolved / 0 new** via SECURITY DEFINER `friction.*` RPCs (Batches 1/2a/2b/3/4/6; `p_actor='pk'`; one-at-a-time, verified; CCD-inline D-01 + session-doc governance — no `m.chatgpt_review` fabricated). YT straggler carry case `79326fc8` intentionally added (open/track; archive_stale-vs-requeue decision pending, next_review 2026-06-09). Manual case `f633c279` triaged acknowledged/track (unpooled). 12 protected IDs untouched. **Track-2 Stage D Phase 1 read-only UI: implemented by CCD, CCB preview PASS, merged to main `668b6ae`, deployed to prod, CCB prod sanity PASS** — `/operations/pools` status strip + collapsed pool-card indicators live; no mutation surface; read-only footer intact. Session record `docs/runtime/sessions/2026-06-02-cc-0015-register-hygiene.md`. Carry-forwards: Stage D **Phase 2** `/operations` status strip; optional cosmetic label-truncation cleanup; YT straggler cleanup decision; overdue tracked-cases review sweep. 0 schema / 0 EF / 0 cron / 0 deploy this lane.

---

> **✅ v3.23 — PRV Stage 2: `op.*` read-RPC migration APPLIED + VERIFIED + doc-only 4-way close (2026-05-29).** Transport option (i) (public SECURITY DEFINER read RPCs over `op.*`) confirmed by PK 2026-05-29. Applied canonical migration **`20260529065224_prv_op_read_rpcs`** to `mbkmaxqhsohbtwsqolns` (proposed `20260529120000_proposed_…` tombstoned per cc-0020 pattern; stored-statement md5 `9103b26e`). **Three production RPCs live, service_role-only EXECUTE:** `public.prv_get_reconciliation_summary()`, `public.prv_get_freshness_rollup()`, `public.prv_get_drift_rollup()` (`LANGUAGE sql STABLE SECURITY DEFINER SET search_path=''`; contracts mirror the exact localhost `/recon` projections for parity-by-construction). Self-aborting 41-tuple in-migration pre-flight guard (zero drift live). D-01 `87c618a5` agree/proceed; prior `103f5a6c` (partial/type-c) satisfied via the guard + superseded; both closed-loop on `m.chatgpt_review` (status=`completed`). PK phrase `PK APPROVES PRV READ-RPC MIGRATION APPLY`. V-checks PASS: `prosecdef=t`/`provolatile=s` ×3; EXECUTE service_role only (anon/authenticated false); RPC↔view row parity 1/14/40, 26 actionable. Repo: branch `feature/platform-reconciliation-view-readonly` @ `6aa1f343` (off `main`, unmerged, inert). **Stage 3 GATED:** port PRV to **`/reconciliation`** under **REPORTS** in `invegent-dashboard` via the existing service-role `.rpc()` transport (no direct-PG/DSN in Vercel). **Stage 4** = same-DB-same-moment parity diff vs localhost + CCB → `PK APPROVES PRV READ-ONLY PRODUCTION DEPLOY`. **Stage 5** = deprecate/remove localhost `/recon` + drop `prv_readonly`. **Localhost PRV remains temporary reference only until Stage 4 parity is accepted.**

---

> **✅ v3.22 YouTube Insights Stage 1 worker DEPLOYED + manually verified + merged to main (2026-05-29):** **1 EF deploy (redeploy of the fixed build); 2 manual verification invokes; 62 YouTube `m.post_performance` rows written (approved runtime); 1 `ef_deploy` D-01 (PK written approval); 0 cron / 0 schema / 0 migration / 0 YouTube mutation / 0 CFW / 0 dashboard / 0 publisher change.** New standalone Edge Function **`youtube-insights-worker-v1.0.0`** (read-only YouTube Data API read-back into the EXISTING `m.post_performance`, `platform='youtube'`, `insights_period='lifetime'`). **Provenance:** built + WCCH-hardened on branch `review/youtube-insights-worker-v1`; deployed from commit **`50332b6`** (the fixed build); **fast-forwarded into `main` `6d31950`→`50332b6`** (worker file is the only diff). **Deploy history (honest):** first deploy was `9428b9d` which **failed safe** on manual invoke (V-4: `token_exchange_400` / `NEEDS_RECONSENT` for both clients, 0 rows written) — a regression where the review-2 change read `c.client_channel` via PostgREST (not readable that way) and fell back to a stale `credential_env_key` secret; fix `50332b6` reverted ONLY that lookup to UUID-validated `exec_sql` (the Stage-0/publisher pattern), restoring the inline `client_channel.config.refresh_token` path. **Verification on `50332b6` (all PASS):** GET version `youtube-insights-worker-v1.0.0`; live schema PK=`performance_id` + unique `(platform_post_id, insights_period)`; invoke #1 `ok:true` — Property Pulse + NDIS-Yarns only, **token_source=`client_channel.config`** both, scopes include `youtube.readonly`, **62 upserted** (PP 46 + NDIS 16), all 62 `raw_payload.privacy_status` populated, no `NEEDS_RECONSENT`, `rotation_detected=[]`, `cross_platform_collision_detected=false`, tokens never printed; **FB rows unchanged at 332** before/after; zero-view engagement guard verified (42 zero/null-view rows → `engagement_rate` NULL, `bad_guard=0`, no Infinity/NaN); invoke #2 **idempotent** (62 processed/0 first_time, YT count stable 62, no duplicates, `collected_at` advanced). **No cron created** — the worker runs only on gated (`x-worker-key`) manual invoke. **v3.20 visibility verification remains ARMED, not closed** — the 62 rows are historical videos; closure still requires a genuinely **post-v1.11.0** YouTube upload observed via API with `privacyStatus='public'`. **Deferred (each its own gate):** cron schedule (separate `sql_destructive` D-01; durable rotation/collision alerting — currently JSON-only — must be added before unattended cron), typed columns / `m.channel_performance` / dashboard / CFW onboarding / Analytics API. **⚠️ Process note:** during this lane a **parallel FB `insights-worker` v14.2.0 lane shared the SAME git checkout**, and the local `review/youtube-insights-worker-v1` branch pointer flipped to the FB lane's commits mid-task (HEAD observed momentarily at `898e965`/`17081b3`); no work was lost (verified before every git write; commits stayed file-scoped) but **isolated git worktrees are recommended for future concurrent CCD/FB-lane work.** cc-0020 / cc-0016 / cc-0015 / PRV / IG lanes preserved unchanged. `00_`-only close.

---

> **✅ v3.21 YouTube Insights Stage 0 scope probe EXECUTED + PASSED — temporary read-only EF deploy/run/delete cycle (2026-05-29):** **1 temporary EF deploy + 1 delete; 1 `ef_deploy` D-01 (PK written approval); 4 read-only YouTube API quota units; 0 DB write / 0 schema / 0 cron / 0 YouTube mutation / 0 Stage 1 work.** Stage 0 of the YouTube insights-worker lane (brief `docs/briefs/youtube-insights-worker.md`). **Step 0 decided Option B** (manual/laptop Option A infeasible — `YOUTUBE_CLIENT_ID`/`YOUTUBE_CLIENT_SECRET` are Edge-Function-only secrets, unreachable from the supervised env). Lifecycle: deployed temporary read-only **`youtube-scope-probe`** (`--no-verify-jwt`, gated by `x-probe-key == PUBLISHER_API_KEY`) → single POST invoke → **mandatory delete** (`supabase functions delete youtube-scope-probe`) → **verified absent** (`functions list` shows only `youtube-publisher` + `heygen-youtube-upload`; live GET → HTTP 404); local throwaway source removed. **Result (`probe_run_at 2026-05-29T04:21:29Z`, `quota_units_consumed=4`):** **Property Pulse `STAGE1_READY`** (46 YT published rows; token_source `client_channel.config`; `scopes_on_token` includes `youtube.readonly`+`youtube.upload`; `videos.list` pass — privacyStatus=public, views 71; `channels.list` pass — channel `UCudcAtOaVbYNc-9mXvou7Wg`, subs 1 / videoCount 29; `refresh_token_rotated=false`); **NDIS-Yarns `STAGE1_READY`** (16 YT rows; same scopes; videos.list pass — privacyStatus=public, views 96, 1 like; channels.list pass — channel `UCqCTvPSR1BwhIi5Cui9_9Mw`, subs 1 / videoCount 15; no rotation); **CFW `UNTESTED_NO_YOUTUBE_ROWS`** (0 YouTube published rows — not a failure; re-probe required if/when CFW publishes YouTube). **R1 (readonly-not-granted) and R6 (token rotation) did NOT materialize** — both live tokens carry `youtube.readonly` and are non-rotating in practice; the §4.3.0 stop-and-capture guard was armed and not triggered. Token values never printed/persisted (presence/source-labels only). **Stage 1 (`youtube-insights-worker` MVP) brief AUTHORED (brief §11) but NOT built/deployed** — it is the next gated action and requires its own `ef_deploy` D-01 (+ separate `sql_destructive` for cron); no schema migration for MVP (reuse `m.post_performance`, platform='youtube', raw_payload carries authoritative `privacy_status`); CFW skipped until probed. Stage 1 also durably closes the open v3.20 post-publish `privacyStatus` verification. youtube-publisher v1.11.0 untouched. cc-0020 / cc-0016 / cc-0015 / PRV / IG lanes preserved unchanged from v3.20. `00_`-only close.

---

> **✅ v3.20 youtube-publisher v1.11.0 DEPLOYED — future YouTube uploads default to PUBLIC + repo↔deployed parity restored (2026-05-29):** **1 EF deploy; 0 DB mutation / 0 SQL / 0 draft reset / 0 forced publish / 0 YouTube API call (beyond normal publisher behaviour) / 0 backfill / 0 historical audit cleanup / 0 feature expansion / 1 ef_deploy D-01 (PK-approved).** **Finding:** ICE YouTube Shorts/videos had been publishing as **Unlisted** because `supabase/functions/youtube-publisher/index.ts` hardcoded `privacyStatus: 'unlisted'` at the upload call **and** `privacy_status: 'unlisted'` in the `m.post_publish` audit payload. PK had already manually set the existing videos to Public, so **no backfill / no historical cleanup** was required — the fix is **future-publish only**. **Patch (v1.10.0 → v1.11.0, narrow Option A + WCCH refinement):** introduced a single named module constant **`DEFAULT_PRIVACY_STATUS: 'public' | 'private' | 'unlisted' = 'public'`** (`index.ts:98`) referenced from BOTH the YouTube upload `privacyStatus` (`index.ts:293`) AND the audit `response_payload.privacy_status` (`index.ts:318`) so recorded metadata can never drift from intended visibility (WCCH audit-field check satisfied — both sites verified by grep to use the same constant; zero executable `'unlisted'` literals remain, only changelog comment text). VERSION bumped to `youtube-publisher-v1.11.0`; v1.11.0 changelog block added in the existing style. **No env/config/UI/per-client setting, no schema/migration, no DDL/DML** (per PK directive — env-driven knob explicitly rejected for this patch). Everything else byte-unchanged from v1.10.0 (approval predicate, metadata, failure classification, bounded retry, channel auth-hold, no-YT-id guard, pre-upload reconcile guard, format allow-list, 2/tick cap, next-available `attempt_no` audit fix). **SAFETY DEPENDS ON THE APPROVAL GATE** (recorded in-code): Public is only safe because the draft SELECT gates on `approval_status IN ('approved','published')` AND `video_status='generated'` AND `recommended_format` ∈ the 5-format allow-list AND `draft_format->youtube_video_id IS NULL`; **if that predicate is ever weakened the Public default becomes unsafe and must be re-reviewed.** **No backfill (proven):** the only YouTube write is the multipart `videos.insert` in `uploadToYouTube` (no `videos.update`/privacy-patch path); already-published drafts (`youtube_video_id` set / `video_status` not `generated`) fail the SELECT + pre-upload reconcile guard, so they are never re-selected and never re-uploaded — PK's manual Public history is untouched. **Non-YouTube publishers unaffected (proven):** repo-wide grep for `privacyStatus`/`privacy_status`/`unlisted` matches ONLY `youtube-publisher/index.ts`; the other five publishers (`wordpress-`/`linkedin-`/`linkedin-zapier-`/`instagram-`/generic `publisher`) have no such field, and deploying `youtube-publisher` redeploys no other function. **Deploy (PK-approved `ef_deploy` only):** `supabase functions deploy youtube-publisher --no-verify-jwt --project-ref mbkmaxqhsohbtwsqolns` — SUCCESS (Docker-not-running warning benign; asset uploaded + deployed). **V-1 GET health/version = `{"ok":true,"function":"youtube-publisher","version":"youtube-publisher-v1.11.0"}` HTTP 200** (unauthenticated GET) — deployed version confirmed; no unexpected deploy error. **OPEN follow-up (runtime, non-blocking): post-publish authoritative visibility verification** after the next controlled/real YouTube publish — authoritative proof MUST be **YouTube Studio or `videos.list?part=status` showing `status.privacyStatus == 'public'`**; the ICE `m.post_publish.response_payload.privacy_status: 'public'` is **corroborating only, not authoritative**. This commit restores repo↔deployed parity (production was already on v1.11.0). _(Version note: renumbered to v3.20 because the concurrently-committed friction-register session `a62a31e` already claimed the v3.19 label — see the v3.19 entry directly below.)_ cc-0020 / cc-0016 / cc-0015 / PRV / IG lanes preserved unchanged from v3.19. `00_`-only close.

---

> **✅ v3.19 friction-register manual operating passes (1 + 2) + Facebook insights-coverage gap finding (2026-05-29):** **production mutation via single-case `friction.case` SECURITY DEFINER RPC calls only (one-at-a-time, each D-01 + PK exact approval, each post-call verified); 0 schema / 0 EF / 0 cron / 0 migration / 0 dashboard code / 0 decisions.md change.** Recorded as a session file (`docs/runtime/sessions/2026-05-29-v3.19-friction-register-manual-passes.md`, commit `a62a31e`); this `00_` banner is added retrospectively for index completeness. **Outcome:** completed the first full operating loop on the friction register (capture → triage → **resolve**, which had never fired — 46/46 open at start). Register moved **46 total / 45 open / 1 resolved / 3 new → 46 total / 42 open / 4 resolved / 0 new**. **Structural finding:** the `/operations` UI cannot close a case — `CaseRow` only calls `friction.fn_triage_case`; resolution (`friction.resolve_case` / `friction.mark_duplicate`) is not wired into the dashboard, so every closure is an out-of-UI RPC needing D-01 + PK approval (flagged as arguably higher value than the Stage C batch UI). **Pass 1 (D-01 `a94c9f23`, 3 PK-approved writes):** triaged `c53f5af6` (F-FB-PUB-PUBLISH-AUDIT-GAP) + `220bc43a` (F-FB-TRANSIENT-500-NO-RETRY) to track; resolved `a6c6032f` (NY FB jobid-48 starvation) `tracked_done` (live-verified); held `15d57015` (Content Studio UI fix) for PK eyeball. **Pass 2 (D-01 `9432e82a`, observer_stale ×3):** `d5efc053` (NY×YouTube) → **suppress** (structural false-positive — no YouTube insights pipeline exists; insights-worker is FB/IG only); `26436794` (NY×Facebook) → **track + recat pipeline_integrity** (GENUINE GAP — publishing healthy but `m.post_performance` insights stopped 2026-05-23; insights-worker v14.1.0 did not restore NY-fb coverage); `fcb5612e` (Invegent×Facebook) → track + recat + resolve `tracked_done` (residual — insights resumed under v14.1.0). Both D-01s closed-loop on `m.chatgpt_review`. **Known attribution artefact:** a call-1 positional-arg error left `d5efc053` with `triaged_by=postgres` (corrected functionally via a named-arg re-issue + correction note; `fn_triage_case` cannot reset `triaged_by`, and direct DML was forbidden) — recorded as a caveat, all 5 verify checks passed. **Real next engineering lane surfaced: the NY-fb (+ CFW-fb) Facebook insights-coverage gap.** This lane is independent of the v3.20 youtube-publisher work above.

---

> **✅ v3.18 session close — cc-0020 Stage 5 Unit B code-readiness MERGED on `main` via PR #6 (2026-05-29):** **docs-only — 0 deploy / 0 OAuth / 0 Gmail/token access / 0 Pub/Sub / 0 cron / 0 DB write / 0 migration / 0 production runtime change / 0 D-01 fired this session.** v3.17 (Stage 5 scoping + governance G1 close + G2 accepted) is already mainline (PR #5 / merge `883cbf72`). **PR #6 merged: code-only Unit B readiness** for `supabase/functions/subscription-email-ingest`. Merge commit **`9e4d3dbcb1588d91079ab641dd820849ce36d165`** (parents `df8c54e` prior `main` + `1888b56` Unit B head); origin/main HEAD is now `3ddac58a103ff37a9114dd20ecaf9ddc9843ad44` (one automated nightly-health-check no-ready-briefs run-state report `3ddac58` landed after the merge, unrelated to PR #6). **Unit B contents (merged):** metadata-only Gmail fetch in `gmail.ts` (`format=metadata` hardcoded, `metadataHeaders=From,Subject,Date`, sender allowlist + `maxResults≤25` + `windowDays≤7` bounds, fail-closed env-flag guard `SUBSCRIPTION_GMAIL_INGEST_ENABLED=false` default, token never logged / never stringified / never returned); RPC-only `ServiceRoleIngestRepository` in `repository.ts` (calls `public.ingest_subscription_import_candidate` SECURITY DEFINER RPC; dashboard-only methods throw with pointers to `public.get_subscription_*` / `public.review_subscription_candidate`); async `IngestRepository` interface; `index.ts` wired with dual-layer kill-switch (`gmailFetchEnabled()` check before constructing Supabase client + flag re-check inside `fetchSubscriptionEmails`); version banner `subscription-email-ingest-v1.0.0-disabled`. **Tests merged: 41 PASS, hermetic** (no real network/DB/env reads): 11 parser + 6 ingest (async refactor) + 9 gmail (stub-fetch + env injection — kill-switch / missing-env / empty-allowlist / happy-path metadata-only / off-allowlist drop / token-refresh-failure NO-token-leak / maxResults+windowDays caps) + 8 repository (RPC-contract proof: function name, full `p_*` parameter set, idempotency on `gmail_message_id`, `PGRST202` error bubble with **NO row payload leak**, dashboard-only methods throw clear pointers, SupabaseLike type surface has no `.schema()`/`.from()`) + 7 static_canary (V-B3 no `format=full`/body, V-B4 no token-in-log, V-B7 no direct `k.*` PostgREST access, RPC-contract recorded, Unit C blocker note present, kill-switch present, version banner). **V-B7 was added BEFORE merge to fix a CCH plan_review PARTIAL finding** — the initial Unit B commit (`75cfba3`) wrote directly to schema `k` via PostgREST (`.schema("k").from("subscription_import_candidate").upsert(...)`); `k.*` is NOT PostgREST-exposed in production and the cc-0020 access rule forbids direct table access. The V-B7 patch (`1888b56`) replaced direct PostgREST with `.rpc(INGEST_CANDIDATE_RPC, ...)`, reduced `SupabaseLike` to expose only `.rpc()` (codifying the access rule at the type level), and added the V-B7 canary that fails the build if any production file reintroduces `.schema(` / `.from("subscription_…")` / `"k.subscription_…"` literals. CCH re-review after the V-B7 patch was **CLEAN**, then PR #6 merged. **Unit B is NOT DEPLOYED and has NO RUNTIME EFFECT.** Code is on `main` but the function is not deployed; even if deployed, `SUBSCRIPTION_GMAIL_INGEST_ENABLED=false` (default) short-circuits at `gmail.ts:gmailFetchEnabled()` before any OAuth/network call. **Unit C BLOCKED** until: (1) `public.ingest_subscription_import_candidate(p_gmail_message_id text, p_vendor_raw text, p_vendor_normalised text, p_matched_subscription_id uuid, p_amount numeric, p_currency text, p_billing_date date, p_cadence text, p_event_type text, p_confidence numeric, p_source_from_domain text, p_source_subject text, p_source_received_at timestamptz, p_parser_version text, p_content_hash text, p_review_status text) RETURNS TABLE(candidate_id uuid, inserted boolean)` SECURITY DEFINER write-RPC migration is authored against the Stage 2 schema; (2) the migration receives `sql_destructive` D-01 + PK exact approval phrase (matches Stage 4-B precedent); (3) real-PG / PGlite proof of the RPC's `ON CONFLICT (gmail_message_id) DO NOTHING` idempotency contract is captured; (4) U-C deploy-disabled gate (ef_deploy D-01 + PK escalate=true) fires separately. The required RPC signature is recorded in `repository.ts` (constants `INGEST_CANDIDATE_RPC` + `INGEST_CANDIDATE_RPC_PARAMS` + `CANDIDATE_RPC_SQL`) for the eventual migration to be reviewed against. **Unit A remains required before first live smoke** — independent of Unit C: (a) OAuth app **Production publishing status** per F-YT-OAUTH-TESTING-MODE v3.05 lesson; (b) scope = `https://www.googleapis.com/auth/gmail.readonly` only; (c) Supabase Edge-Function secrets set: `SUBSCRIPTION_GMAIL_REFRESH_TOKEN`, `SUBSCRIPTION_GMAIL_CLIENT_ID`, `SUBSCRIPTION_GMAIL_CLIENT_SECRET`, `SUBSCRIPTION_GMAIL_SENDER_ALLOWLIST`; (d) **secret-name PRESENCE verified without printing values** (per privacy contract). Unit A does NOT block Unit B merge (already done) and does NOT block Unit C deploy-disabled (the function returns `gmail_fetch_missing_env` fail-closed when secrets absent), but Unit A IS a prerequisite for U-D first smoke. **Unit D first live smoke BLOCKED** until Unit A + Unit C both complete (smoke design unchanged: `newer_than:1d`, `maxResults:10`, `format=metadata` only, sender allowlist, single manual invoke, flag flipped back OFF after). **Unit E cron activation** = separate later gate (sql_destructive D-01 for `cron.schedule`). **Unit F Pub/Sub** = DEFERRED to Stage 5b. **Walls intact this session:** no deploy / no OAuth / no Gmail/token access / no Pub-Sub / no cron / no DB write / no migration / no production runtime change / no D-01 fired by CCD. cc-0016 + cc-0015 + YT + PRV + IG lanes preserved unchanged from v3.17. `00_`-only close per the v3.13.2/v3.13.3 precedent (no separate session file).

---

> **✅ v3.17 cc-0020 Stage 5 scoping AUTHORED + governance close G1 RESOLVED + G2 ACCEPTED-AS-APPLIED (2026-05-28):** **docs-only — 0 OAuth / 0 Gmail/token access / 0 Pub/Sub / 0 EF deploy / 0 cron / 0 DB write / 0 migration / 0 production mutation / 0 D-01 fired this session.** Two follow-ups landed atomically on `feat/cc-0020-stage-5-scoping` (off `main` `883cbf72`). **(1) cc-0020 Stage 5 execution plan AUTHORED** — `docs/briefs/cc-0020/stage-5-live-gmail-execution.md`: 13 sections covering stage objective + exact entry criteria (E-1…E-7 from v3.16 + P-1…P-5 prerequisites) + components touched + **6-unit sequencing plan A→B→C→D→E→F** (U-A token mint by PK / U-B code change CCD / U-C deploy in DISABLED state / U-D first live smoke ≤24h ≤10 messages PK eyes-on / U-E cron activation / U-F Pub/Sub DEFERRED to Stage 5b) + per-unit V-checks + **per-unit D-01 + PK approval phrases** (U-B plan_review / U-C ef_deploy escalate=true / U-D 2-step ops_change + verbal / U-E sql_destructive escalate=true) + global kill switch (`SUBSCRIPTION_GMAIL_INGEST_ENABLED=false` short-circuits at `gmail.ts:27` — no code/migration/deploy needed to disable) + per-unit rollback paths + 11-item out-of-scope list + recommended first-live-smoke design (smallest blast radius: `newer_than:1d`, `maxResults:10`, `format=metadata` ONLY — no body crosses the wire, sender-domain allowlist, single manual invoke, flag flipped back OFF after) + 10-item risk table + 6 open questions + authoring provenance. Surgically linked from cc-0020 main brief §3 Stage 5 row. **Stage 5 execution remains future/gated** — every unit fires its own gate before any production effect. **(2) Governance close G1 RESOLVED for `6d9a4bd3-d665-47c0-8dd9-165d3bc75bd6`** (CCH read-only verification + close-loop record): the cc-0020 production apply migrations `20260527114041` (tables) + `20260527114144` (read RPCs) now have a **complete in-row governance trail** — `m.chatgpt_review.6d9a4bd3` updated to `status='resolved'`, `escalation_resolved_at='2026-05-28 10:55:17.310207+00'`, `resolved_by='PK (retrospective confirmation 2026-05-28) + CCH (close-loop record)'`; first close-loop UPDATE returned `row_count=1`, guarded re-run returned `row_count=0` (idempotency proved). The v3.16 "partially evidenced, not cleanly closed" wording is **SUPERSEDED for G1 only** — migrations `114041` + `114144` are now cleanly closed in-row. **Residual G2 (migration `20260527114333` REVOKE-anon/authenticated security hotfix) ACCEPTED-AS-APPLIED** by PK directive 2026-05-28 (docs-only). The hotfix was a **pure grant-tightening operation** — REVOKE EXECUTE FROM `anon`,`authenticated` on the 4 `public.*` SECURITY DEFINER RPCs — **no data effect, no schema change.** Live posture was already verified correct (RPC EXECUTE = `service_role` + `postgres` only; `anon`/`authenticated`/`PUBLIC` denied), so the missing-D-01 was a **process gap, not a residual risk**: residual risk = none known; the process gap is now recorded as accepted by PK. **This is NOT precedent for skipping D-01 on future grant changes** — grant-tightening hotfixes ordinarily fire `sql_destructive` D-01 like any other DDL; the `114333` case is a one-time retrospective acceptance because the grant change was strictly tightening (revoke-only) and the resulting posture was verified safe. **Walls intact this session:** no OAuth; no Gmail/token access; no Pub-Sub; no edge-function deploy; no cron change; no DB write (CCH's G1 UPDATE was orchestrated separately and reported back read-only); no migration; no production mutation; no D-01 fired. cc-0016 + cc-0015 + YT + PRV + IG lanes preserved unchanged from v3.16. `00_`-only close per the v3.13.2/v3.13.3 precedent (no separate session file).

---

> **✅ v3.16 four-point docs-only reconciliation — cc-0016 Stage E dry-run design AUTHORED + cc-0020 status CORRECTED (LIVE IN PROD, not parked) + YT backlog drain CLOSED + cc-0015 Gate 11 window ELAPSED (2026-05-28):** **docs-only — 0 SQL apply, 0 DB write, 0 deploy, 0 cron change, 0 PR/merge, 0 OAuth/token/Gmail action, 0 lifecycle cleanup run, 0 D-01 fired this session.** All four facts incorporated atomically. **(1) cc-0016 Stage E dry-run design AUTHORED** — `docs/briefs/cc-0016/stage-e-dryrun-design.md` (non-mutating spec; all 7 required sections: cleanup objective + 10 in/out-of-scope boundaries B-1…B-10 incl. orphan-storage OUT + no-op guarantee; lifecycle tables to inspect — `friction.event` / `storage.objects` in `friction-evidence` / `storage.buckets` / `cron.job` / `pg_proc`; 9 SELECT-only verification queries Q-1…Q-9 + Q-10 STABLE `SECURITY INVOKER` `_DRYRUN()` wrapper PSEUDOCODE explicitly NOT created; JSON dry-run output format; 9 acceptance criteria AC-1…AC-9 incl. counter-validation invariant `total_event_rows = within_window + targetable_events + old_but_already_empty`, AC-7 NO-OP short-circuit, AC-8 ≥14-day operational maturity; **separate future destructive gate G-E1/G-E2/G-E3 explicitly required** — D-01 sql_destructive carrying dry-run JSON w/ ≤7-day freshness rule + PK exact approval phrase w/ cutoff+N+B substituted to snapshot-lock scope + ≥14 prod days + no open P0/P1; R-A1…R-A4 authorship + R-B1…R-B7 future-run risks + rollback/no-op guarantees). Surgically linked from cc-0016 main brief §7. **Future D-01 + PK approval STILL REQUIRED before any destructive run** — design does not fire D-01, does not produce a migration, does not create/schedule/call any function, does not commit an approval phrase. **(2) cc-0020 status CORRECTED — NO LONGER PARKED / HELD / UNAPPLIED** (supersedes v3.15 wording verbatim). CCH read-only verification + repo state confirms: content-engine `main` advanced via merge commit **`49a6c0e`** (Merge cc-0020 subscription email-ingest lane into main (Stage 0–4B)) bringing in the parser/ingest Deno function (inert — not deployed), fixtures/tests, and the two cc-0020 migrations; **three migrations applied to production `mbkmaxqhsohbtwsqolns`**: `20260527114041_cc_0020_subscription_email_ingest_tables` (creates `k.subscription_import_candidate` + `k.subscription_spend_event`; banner "APPLIED TO PRODUCTION at version 20260527114041 (mbkmaxqhsohbtwsqolns)"); `20260527114144_cc_0020_subscription_read_rpcs` (creates 4 `public.*` SECURITY DEFINER RPCs over the deny-by-default `k.*` tables: `get_subscription_import_candidates(text,integer)`, `get_subscription_spend_events(integer)`, `get_subscription_spend_trends(integer)`, `review_subscription_candidate(uuid,text,uuid)` + REVOKE FROM PUBLIC + GRANT to service_role + folded-in REVOKE FROM anon,authenticated); `20260527114333_cc_0020_subscription_rpcs_revoke_anon_auth` (security hotfix — REVOKE EXECUTE FROM anon,authenticated on all 4 RPCs because Supabase default-privilege GRANTs to anon/authenticated bypassed the prior REVOKE-FROM-PUBLIC). Repo↔prod migration-history reconciliation v2 is **merged on `main`** via PR #3 merge commit **`d7543078`** (parents `3cd144f` v1 + **`fa2aefc`** v2); canonical applied-version files + `_proposed_` tombstone placeholder slots are now the mainline state. Tombstones (`SELECT 1 WHERE false;`) permanently consume the version slots so a future `supabase db push` cannot silently re-execute the DRAFT DDL; canonical files carry idempotency-abort guards via `DO $$ … RAISE EXCEPTION 'ABORT: … already exists' … $$`; provenance diffs vs `schema_migrations.statements` comment-only on executable DDL (PGlite 12/12 dry-run pass on the v2 layout). RPC EXECUTE state: service_role + postgres only (verified in repo file 114144 lines 269–286 + hotfix file 114333 lines 16–19). Dashboard `main` advanced with **Stage 4-A UI** + **Stage 4-B service-role RPC routes** consuming the new `public.*` RPCs (per CCH; live at `dashboard.invegent.com/system/subscriptions`). Stage 5 wall (Gmail OAuth + Pub/Sub + edge-function deploy + cron) **still intact / future-gated** — the ingest function is in-repo but not deployed; no OAuth/token/Gmail/cron change made this lane. **Governance trail (HONEST — partially evidenced, not cleanly closed):** CCH completed a read-only sweep against `m.chatgpt_review`, which resolves the prior generic "evidence gap" into a specific finding. The cc-0020 production apply migrations **`20260527114041`** (tables) and **`20260527114144`** (read RPCs) have governance evidence via review row **`6d9a4bd3-d665-47c0-8dd9-165d3bc75bd6`** — `action_type=plan_review`, `verdict=partial`, `risk_level=medium`, `confidence=medium`, `requires_pk_escalation=true`, `routing_decision`/`action_taken=escalate_explicit_flag`, `status=escalated`, `resolved_by=NULL`, `escalation_resolved_at=NULL`. So the apply is **partially evidenced, not cleanly closed**. **Residual G1:** the `6d9a4bd3` close-the-loop was never recorded; PK discharge is asserted by the proposal and implied by timing, but **not captured in-row** (both `resolved_by` and `escalation_resolved_at` are NULL). **Residual G2:** the migration **`20260527114333`** REVOKE-anon/authenticated security hotfix has **no D-01 / governance row found** in `m.chatgpt_review` — applied without an in-`m.chatgpt_review` record. **Live grant posture verified correct:** RPC EXECUTE on all 4 `public.*` SECURITY DEFINER RPCs = `service_role` + `postgres` only (no anon / authenticated / PUBLIC); this is the safety property and it holds. Residuals G1+G2 are **governance-trail debt, not a live-permissions risk.** Carry items added below for the close-the-loop UPDATE (G1, requires PK confirmation + a DB write later) and the retrospective governance decision on `114333` (G2, requires PK decision later) — both deferred to PK direction. **(3) YT backlog drain CLOSED** (per CCH verification): `m.post_publish` YouTube rows = **61**; latest publish row at **2026-05-28 08:15:08Z**; **published-draft↔youtube-publish-row gap = 0** for the released backlog; the v3.05 fresh-release cohort (NY 6 + PP 7 = 13 drafts via jobid 34 at `:15/:45`) is fully drained with audit-row parity (v3.11 audit-gap fix carried forward — see F-YT-PUB-PUBLISH-AUDIT-GAP closed). **Carry separately:** 2 stale NY `generated` stragglers + stale `pending` drafts are **pre-existing / predicate-ineligible** (predicate is `video_status='generated'` AND `approval_status IN ('approved','published')` AND `youtube_video_id IS NULL` AND `video_url IS NOT NULL` AND `recommended_format` in kinetic/stat allow-list + avatar; the stragglers fail one of those gates) and are **NOT the released backlog**; treat as separate carry. F-YT-FAILED-NO-RETRY arc complete (B1+B2 + audit gap closed v3.10/v3.11); B3 9 = onboarding debt; YT operational rank cleared. **(4) cc-0015 Gate 11 observation window ELAPSED/CLOSED as a time-bound watch** — the 1-week window (2026-05-19 → 2026-05-26) ended **2026-05-26**; the watch itself is now closed by elapsed time. **Stage C/D/E remain separately PK-gated** (no time-bound watch on them) — Stage C (batch) / D (status-strip counts) / E (pool-session UI) each require their own PK-directed start; ICE-PROC-002 +4-week review (≈ **2026-06-26**) is the next process milestone for cc-0015 as a whole. Stage A + Stage A.5 + Stage F + Stage B RPC + Stage B frontend + Stage G doc V-G1 unchanged (shipped/live/approved). cc-0015 still NOT complete by overall closure; the rank-1 watch is what closed. **Walls intact this session:** no DB connection used; no live Gmail/OAuth/Pub-Sub; no edge-function deploy; no cron change; no PR/merge; no friction.* write; no destructive Stage E run; no governance-row creation. Cross-references: cc-0020 commits `49a6c0e` (merge), `3cd144f` + `fa2aefc` (repo↔prod reconciliation); cc-0016 Stage E artefact `docs/briefs/cc-0016/stage-e-dryrun-design.md`; main brief link added to `docs/briefs/cc-0016-friction-capture-evidence.md` §7. `00_`-only close per the v3.13.2/v3.13.3 precedent (doc-only; no separate session file).

---

> **✅ v3.15 Instagram diagnosis lane + cc-0020 Subscription Email Ingest lane PARKED — session sync + close (2026-05-27):** **docs-only — 0 application code, 0 migration apply, 0 DB connection, 0 deploy, 0 Gmail/OAuth/token, 0 cron/Pub-Sub, 0 PR/merge, 0 D-01.** Two parked lanes recorded for continuity (independent — no shared branch/commit/approval). **(A) Instagram "not publishing" diagnosis (CCH lane) — parked/closed:** diagnostic runbook committed `b911d91` (`docs/runbooks/instagram-not-publishing-diagnostic.md`; cc-0019 fill-gate trap + copy-paste SQL); NY/PP `publish_enabled` flipped via dashboard UI → fill-gate cleared; expected self-heal ≈ 2026-05-29; no further IG action unless PK reopens (CCD did not run the IG lane). **(B) cc-0020 Subscription Email Ingest (CCD lane) — parked:** branch `feat/cc-0020-subscription-email-ingest` @ remote HEAD `7497e74` (pushed, **unmerged, undeployed**). Stage 0/1/2/2a/2b **accepted**; Stage 3 **conditionally accepted** (fixture-only ingest scaffold: pure parser + 8 synthetic fixtures + 17 Deno tests; in-memory repo modelling `ON CONFLICT (gmail_message_id) DO NOTHING` + `UNIQUE(source_candidate_id)`; fail-closed Gmail stub; no Supabase client); Stage 3a-P **closed** (branch pushed + CCH DDL/scaffold inspection, no blockers); **Stage 3a DB-layer validation OPEN** (real-PG proof deferred — Docker/local PG unavailable — and it **blocks Stage 4**); **Stage 4 HELD.** Draft migration `supabase/migrations/20260527120000_proposed_cc_0020_subscription_email_ingest_tables.sql` (two append-only `k.*` tables; RLS deny-by-default; `event_type` authoritative w/ sign-vs-type CHECKs; ledger `event_type` no default) remains **UNAPPLIED** (production `mbkmaxqhsohbtwsqolns` never touched). Walls intact: no live Gmail/OAuth/tokens/apply/deploy/cron/Pub-Sub/`k.subscription_register` write. Future: register matching, `content_hash` secondary signal, optional currency CHECK, apply-time `service_role` USAGE-on-`k` preflight. **UI destination (PK directive 2026-05-27):** the user-facing review + spend-history UI belongs in **`invegent-dashboard`**, extending the live subscription area **`dashboard.invegent.com/system/subscriptions`** (tabs/panels/drawers/sub-routes OK) — localhost is dev/test only, NOT the deliverable; explicitly **not** the PRV local-only pattern. Repo split: backend/ingest stays in `content-engine` (branch `feat/cc-0020-subscription-email-ingest`); live UI on its own `invegent-dashboard` branch when Stage 4 is released. **Stage 4** = extend the real dashboard subscription experience (not a standalone local surface); entry reqs: (1) Stage 3a DB-layer validation closed/explicitly-accepted-as-apply-time-proof; (2) a read path into the deny-by-default `k.*` tables — a `public` SECURITY DEFINER RPC (cf. `public.get_subscriptions()`) OR a service-role server route, **no direct client/PostgREST access to `k.*`**. **Preserved unchanged:** PRV pushed/unmerged/undeployed; cc-0015 parked (Stage C/D/E held). Detail: `docs/runtime/sessions/2026-05-27-v3.15-instagram-cc0020-sync-close.md`.

---

> **✅ v3.14 Platform Reconciliation View (PRV) read-only MVP COMMITTED + PUSHED (2026-05-27):** **feature-branch only — 0 deploy, 0 merge, 0 PR, 0 PostgREST exposure.** The local read-only PRV dashboard MVP is built, live-validated, committed, and pushed. **Route `/recon`** in the content-engine `dashboard/` app (the cc-0013 Next 15 server-component dashboard) reads the 3 cc-0012 `op.*` views (`v_reconciliation_summary` / `v_freshness_rollup` / `v_drift_rollup`) via a **server-only DIRECT Postgres pooler transport** (`dashboard/lib/db.ts`; read-only session `default_transaction_read_only=on` + SELECT-only guard + `server-only`) — needed because schema `op` is NOT in the PostgREST exposed-schemas allowlist (PGRST106), which a direct PG connection bypasses. Connects as least-privilege role **`prv_readonly`** (migration `20260527011420_prv_readonly_login_role`: LOGIN / NOSUPERUSER / NOCREATEDB / NOCREATEROLE / NOREPLICATION / NOINHERIT / NOBYPASSRLS; SELECT-only on `op.*`; D-01 `d86b7db8` agree/proceed + PK phrase `PK APPROVES PRV DB/RPC MUTATION`; file backfilled byte-exact md5 `a5771ad7…`; close-loop done — `d86b7db8` completed, prior partials `57fd738b`+`43140c7f` superseded→resolved). **Live render PASS** (HTTP 200; summary 7d expected 56 / matched 16 / late 0 / suppressed 12, on-time 28.57%; 14 freshness pairs; 39 drift / 26 actionable) + **CCB visual PASS.** Commit **`7287a1e`** (`feat: add local platform reconciliation view`; 6 files; typecheck/lint/build exit 0) pushed to **`origin/feature/platform-reconciliation-view-readonly`** (new branch; **unmerged, undeployed**; `main` untouched `911d0d4`; push-safety: no CI/Vercel automation wired to this repo). `.env.local` (the DSN) stays local-only + untracked. **Deferred (future, gated):** cosmetic polish (KPI cards / colored badges / table width) + the two already-grant-covered views (`op.v_per_client_rollup` / `op.v_per_platform_rollup`); production deploy = separate PK gate `PK APPROVES PRV READ-ONLY PRODUCTION DEPLOY`. The deployed roadmap (separate `invegent-dashboard` repo) was NOT touched (gated per D-FR-RECON-001 §4.4). Detail: `docs/runtime/sessions/2026-05-27-v3.14-prv-readonly-mvp-pushed.md`.

---

> **✅ v3.13.3 cc-0015 Stage G ICE-PROC-002 V-G1 APPROVED (2026-05-26):** **documentation-only — 0 SQL, 0 migration, 0 deploy, 0 production mutation, 0 dashboard code.** PK confirmed **V-G1 approval** for `docs/process/ICE-PROC-002-pooled-resolution.md` — it is now the **operating reference** for the current cc-0015 `/operations` saved-views + pooled-resolution surface. With V-G1 (approved) + V-G2 (committed `d5b296f`) + V-G3 (linked from `00_docs_index.md`) all satisfied, **Stage G is complete as a doc artefact.** Recorded in the doc's Status + change log, the cc-0015 brief §9, and the `00_` indexes. **Stage C/D/E remain HELD; source filter + sort + FAB category-desc hydration remain deferred; cc-0015 still NOT complete.** Next ICE-PROC-002 milestone = the **+4-week process review ≈ 2026-06-26** (evaluate saved-view usefulness, whether Stage C/D/E should proceed, whether deferrals should be promoted). `00_`-only close (doc-only; no separate session file).

---

> **✅ v3.13.2 cc-0015 Stage G process doc AUTHORED (2026-05-26):** **documentation-only — 0 SQL, 0 migration, 0 deploy, 0 production mutation, 0 dashboard code.** Authored **`docs/process/ICE-PROC-002-pooled-resolution.md`** (the pooled-resolution operating rhythm), following the established `docs/process/ICE-PROC-NNN` convention (alongside ICE-PROC-001). Covers all 7 required sections — (1) why pooling (concentration > context-switching), (2) triage defaults (`track` default / `act_now` reserved / `critical` interrupts), (3) pool cadence (Friday 09:00 Sydney; Dashboard UI weekly, Register reconciliation weekly-if-≥3-else-biweekly, Pipeline/friction triggered, All-tracked + New/untriaged maintenance views), (4) pool session protocol, (5) suppression discipline (`track` usually > `suppress`; when `ignore`/`duplicate`), (6) anti-patterns (fix-while-triaging, one-by-one, newest-first bias, mixing Repair-Board-v0 semantics, "Pipeline pool" collision wording), (7) review at +4 weeks — plus the COEXIST model (`/operations` general register vs `/operations/pools` Repair Board v0; not conflated), the 5 current saved views, and the known deferrals (Stage C/D/E held; source filter + sort + FAB category-desc hydration deferred). Linked from `docs/00_docs_index.md` (new `process/` subdirectory row; V-G3). The doc is **scoped to the shipped surface only** — it does NOT assume Stage C/D/E exist and does NOT claim cc-0015 complete. V-G1 (PK draft approval) remains. **cc-0015 still NOT complete** — Stage C/D/E held; deferrals stand. `00_`-only close (doc-only; no separate session file, per the v3.07 precedent).

---

> **✅ v3.13.1 cc-0015 Stage A.5 recategorisation APPLIED + verified (2026-05-26):** **1 guarded UPDATE migration; 0 schema change, 0 deploy, 0 Repair-Board-v0 change.** The manual, PK-confirmed Stage A.5 backfill is done — migration **`20260526222125_cc_0015_a5_recat_operator_friction_to_dashboard_ui`** moved exactly **3** dashboard-UI friction cases from `operator_friction` → the new `dashboard_ui` category: `53f3e533` (sidebar/submenu instability), `2cf0cd4f` (clients Overview layout density), `b7369dc9` (Content-Studio content-series row spacing). **Guarded UPDATE only** (bounded to `category='operator_friction' AND case_id IN (3 ids)`; no `fn_triage_case`). **Verified:** `dashboard_ui` 0→3, `operator_friction` 6→3, `client_commitment` unchanged 16, `pipeline_integrity` unchanged 22, total unchanged 44; the 3 rows' `triage_state`/`action_decision` unchanged. **Correctly left behind:** the YouTube content-series request + 2 cc-0016 smoke/test cases stayed `operator_friction`; the overview/auto-approver case stayed `client_commitment` (workflow logic, not UI). **Governance:** sql_destructive D-01 `bbe9a0fa-b872-4e2a-998f-743aa89036e0` (verdict=agree, risk=low, confidence=high, escalate=false) + PK exact approval phrase; close-the-loop UPDATE on `m.chatgpt_review` (status=completed). **Repo:** migration file backfilled verbatim (L-v3.06-a; body md5 `5fdb7de72b362c1cdf0cbb8400fbe8b0` == DB statements, byte-exact), committed + pushed `41576f8`. **End result:** the 3 cases now surface under the live `/operations?category=dashboard_ui` Dashboard UI saved view. **cc-0015 still NOT complete** — Stage C/D/E held; source filter + sort + FAB category-desc hydration deferred; Stage G process doc not authored. Detail: `docs/runtime/sessions/2026-05-26-v3.13.1-cc-0015-stage-a5-recategorisation.md`.

---

> **✅ v3.13 cc-0015 Stage B FRONTEND MERGED + DEPLOYED (2026-05-26):** **dashboard frontend merge + Vercel production deploy; 0 SQL, 0 migration, 0 Repair-Board-v0 change this pass.** After CCB preview visual PASS (V-B3 URL-param filters / V-B4 saved views / V-B5 Repair-Board-v0-untouched / V-B6 default byte-identical / stale-wording CLEAN) on prep branch `feat/cc-0015-stage-b-operations-filters` (`25bcdb7`, pushed to a Vercel preview first), the cc-0015 Stage B frontend was **FF-merged** to dashboard `main` (`25bcdb75e79ff25047f15840037f5b22a13d7be9`; `tsc --noEmit` clean + `next build` exit 0 re-verified on main pre-push) and **deployed to production** — Vercel prod **`dpl_Hb2BkFnK3cL4cCAHXbkAz1tDnHjW`** READY (~53s build, `target=production`), live at `dashboard.invegent.com`. Production smoke = **liveness-only** (CCD has no behind-auth browser): `/operations` 307→`/login`→200, `/operations/pools` 307, **no 5xx**; the filter-UI behind-auth visual was CCB's preview PASS. **Stage B delivers:** `/operations` filter bar (category / triage_state / action_decision multi-select pills) + **5 PK-approved saved views** (Dashboard UI / Register reconciliation / Pipeline-friction cases / All tracked / New-untriaged) + URL-param-driven bookmarkable filters mapped onto the extended `friction.fn_recent_cases` + **category-description hydration COMPLETE for `/operations`** (live `friction.category.description` on the triage-form category help; FAB hydration still deferred). New files `pool-filter-bar.tsx` + `saved-views.tsx`; edits to `page.tsx` (searchParams parse + `fetchActiveCategories()`) / `friction-field-help.tsx` (optional `categoryDescriptions` prop, backward-compatible) / `case-row.tsx` (forwards it). **Repair-Board v0 (`/operations/pools`) untouched (V-B5; build size 185 B unchanged); source filter + sort DEFERRED; Stage C (batch) / D (status-strip counts) / E (pool-session UI) HELD; Stage A.5 backfill recategorisation still pending. cc-0015 NOT complete.** Detail: `docs/runtime/sessions/2026-05-26-v3.13-cc-0015-stage-b-frontend-deploy.md`.

---

> **✅ v3.12 cc-0015 STARTED — Stage A applied + Stage F prepped (2026-05-26):** Gate confirmed by PK (cc-0014 early-close D-IOL-001 satisfies the start gate). **Stage A APPLIED** (1 migration `cc_0015_a_pool_schema_additions` v`20260526110657`): new `friction.category` `dashboard_ui` + new `friction.pool_session` table (+ indexes/grants) — additive, plan_review D-01 `58d89efe` (escalate→PK) + PK approval phrase; V-A1–A4 PASS; migration file backfilled to `supabase/migrations/` same-session (L-v3.06-a). **Stage F SHIPPED** — operator help copy (`FrictionFieldHelp` + `lib/friction-help-copy.ts`) wired into FAB + triage form; copy V-F3-approved + de-experimented; **CCB visual V-F1/V-F2 PASS** on preview (`f9d51a9`) → FF-merged to dashboard `main` (`f9d51a96`) → **production deploy READY** (Vercel `dpl_FftxSjeBNyurxZetJedi3zDsan6b`, live `dashboard.invegent.com`; liveness OK; **CCB production smoke PASS** — FAB+triage help visible, expander works, counts marker correct, no stale wording, no visual issues). Category per-option DB descriptions RE-SCOPED to a follow-up; FAB capture_reason absence accepted (field absent). **Stage B RPC APPLIED** (COEXIST, PK-approved): `friction.fn_recent_cases` extended with backward-compatible category/triage/action filters (mig `cc_0015_b_fn_recent_cases_filters` v`20260526124005`; plan_review `58d89efe` + sql_destructive `17db8b27`; V-B2 byte-identical `fn_recent_cases(50)`==`(50,NULL,NULL,NULL)`==baseline 44/`af67c0ee…`; file backfilled same-session). **Stage B frontend (filter bar + saved views + category-desc hydration on `/operations`) = next separate gate**; `/operations/pools` v0 untouched; source/sort deferred; Stage C/D/E + Stage A.5 backfill pending. **cc-0015 NOT complete.** **Stage B/C/D HELD pending re-brief** reconciling cc-0015's category-based pool view vs the shipped notes-based Pools/Repair-Board v0 (`/operations/pools`, `9a4b446`) — **v0 NOT superseded**. Readiness pass also found: `friction.case` extended post-brief by cc-0017d/e; brief line-10 vs §3 contradiction (§3 authoritative). 1 migration applied; 0 deploy; 0 UI merge. Detail: `docs/runtime/sessions/2026-05-26-v3.12-cc-0015-stage-a-applied.md`.

---

> **✅ v3.11.1 YT recovery migration files backfilled (2026-05-26):** **repo-only — 0 DB change, 0 deploy, 0 production mutation.** The three `apply_migration`-applied YouTube recovery migrations (`yt_failed_backlog_recovery_unit_b_b1_only_20260526` v`20260526054427`; `…unit_b2_token_casualty_20260526` v`20260526091149`; `yt_pub_publish_audit_gap_backfill_20260526` v`20260526094403`) had no `supabase/migrations/` files (apply_migration writes DB history, not a file → DB→repo drift, the L-v3.06-a class). Recovered each verbatim from `supabase_migrations.schema_migrations.statements` (same method as v3.06) and committed one file each; **file SQL body md5 == DB `statements[1]` md5 for all three** (byte-exact, no semantic edits). The B2/audit brief artifacts differ from the applied statements by comment verbosity + their trailing read-only verification SELECTs (semantics identical); the `supabase/migrations/` files hold the DB statements (source of truth). B1 had no repo artifact — DB statement is its sole source. **DB→repo migration drift for the YT recovery arc CLOSED.** No change to recovery state: B1+B2 complete, audit gap closed, B3 onboarding debt untouched. Detail: `docs/runtime/sessions/2026-05-26-v3.11.1-yt-migration-backfill.md`.

---

> **✅ v3.11 F-YT-PUB-PUBLISH-AUDIT-GAP CLOSED (2026-05-26):** **1 EF deploy + 1 backfill migration.** The v3.10 audit-gap finding is RESOLVED, A-then-B. **Unit A:** `youtube-publisher` **v1.10.0 deployed + live** (ef_deploy D-01 `5bd19069`, clean agree/proceed; commit `fe8729c`; `--no-verify-jwt`; live GET `youtube-publisher-v1.10.0`) — the success-path `m.post_publish` insert now uses the **next available `attempt_no` per draft** (instead of the hardcoded 1 that collided with the draft's existing Facebook `attempt_no=1` row on `uq_publish_attempt`) and no longer swallows `insertErr` (durable marker). v1.9.0 predicate + all no-duplicate/retry/auth/quota guards preserved. **Unit B:** historical backfill applied (DML commit `538d9e1`; sql_destructive D-01 `998c90a3`, **escalated because it touched historical audit records → PK exact approval phrase**; migration `yt_pub_publish_audit_gap_backfill_20260526`; `ROW_COUNT=11`). Verified: gap_remaining=0; 11 backfilled rows all `platform='youtube'` with `platform_post_id`==draft `youtube_video_id` @ `attempt_no=2`; 0 duplicate YT rows; Facebook rows untouched (736); snapshot `m.yt_audit_backfill_snapshot_20260526`=11; no draft mutation; no re-upload. **Honest limitation:** `platform_post_id` is authoritative (from `draft_format.youtube_video_id`); historical `published_at`/`response_payload` are **reconstructed + `backfill`-tagged** — the original YouTube API insert payload was never captured for these drafts. **Net: the entire YouTube failed-backlog recovery arc is complete — B1 + B2 done, the publish-audit gap closed (fix-forward + backfill); B3 (9 never-connected) remains onboarding debt only.** Both D-01s closed-loop. Full detail: `docs/runtime/sessions/2026-05-26-v3.11-yt-publish-audit-gap-closed.md`. Dashboard PHASES leg N/A (operational).

---

> **⚠️ v3.10 Unit B-2 EXECUTED + a live finding (2026-05-26):** **1 EF deploy + 1 recovery migration (production mutation).** **youtube-publisher v1.9.0 DEPLOYED** (ef_deploy D-01 `4719f53b`, GNB per L46, PK directive; live GET `youtube-publisher-v1.9.0`; predicate broadened `approved`→`IN ('approved','published')`, all guards byte-unchanged) and the **17 B2 token-casualty drafts RECOVERED** (sql_destructive D-01 `2135ae11`, GNB per L46, PK exact phrase `Approved: apply Unit B-2 YT token-casualty recovery`; migration `yt_failed_backlog_recovery_unit_b2_token_casualty_20260526`). Verified: 17 `failed→generated`, **`approval_status` preserved=`published`** on all 17, guards clean, stale keys stripped, breadcrumb present, snapshot table `m.yt_b2_recovery_snapshot_20260526`=17; `failed` 26→9 (now B3-only), `generated` 2→19. Drain (09:15Z tick): **2/17 published** (YouTube `VonxZesS0Ws`, `u-xYWGg1qWI`), 0 re-failed; 15 draining ≤2/tick. **Full Unit B (B1+B2) COMPLETE; B3 (9 never-connected) = onboarding debt.** 🔴 **NEW finding F-YT-PUB-PUBLISH-AUDIT-GAP:** cross-posted YouTube publishes are marked `published` with a real `youtube_video_id` but get **NO `m.post_publish` audit row** — `uq_publish_attempt UNIQUE(post_draft_id, attempt_no)` + the publisher's hardcoded `attempt_no=1` collides with the draft's existing Facebook `post_publish` row, and `insertErr` is swallowed (index.ts:258). Uploads succeed (videos live) + no double-upload, but the YT audit trail is incomplete for cross-posted drafts (all 17 B2 affected as they drain). Backfillable from `draft_format.youtube_video_id`. Open follow-on: publisher `attempt_no` fix (v1.10.0) + post_publish backfill, gated. Full detail: `docs/runtime/sessions/2026-05-26-v3.10-unit-b2-deploy-recovery.md`. Dashboard PHASES leg N/A (operational).

---

> **✅ v3.09 youtube-publisher v1.8.0 DEPLOYED (2026-05-26):** **1 EF deploy; 0 SQL; 0 Unit B; 0 MCP.** CCD (Claude Code / laptop terminal) deployed `youtube-publisher` **v1.7.0 → v1.8.0** via the Supabase CLI — `supabase functions deploy youtube-publisher --no-verify-jwt --project-ref mbkmaxqhsohbtwsqolns` — from `main` @ `b983a38` (on-disk file byte-identical to EF commit `631fa93a`; brief `2ea0f83c`). `--no-verify-jwt` preserved; live GET returns `{"version":"youtube-publisher-v1.8.0"}` HTTP 200 unauthenticated (verified twice). This is **Unit A** of `F-YT-FAILED-NO-RETRY` (failure classification + bounded retry + channel auth hold); D-01 `ef_deploy` `818ee949` (review ID prefix, not git) + PK directive. **Unit B1-only APPLIED + CCD-verified** — the 5 quota/transient drafts were recovered via migration `yt_failed_backlog_recovery_unit_b_b1_only_20260526` (version `20260526054427`, applied by CCH via MCP), governed by D-01 `sql_destructive` `33eeb164-8f1a-47b9-b9c8-071dbd441d7b` (`verdict=agree`, `status=completed`, created `05:42:46Z`, ≈1.7 min before the apply). **CCD read-only-verified** (SELECT-only, no DML): migration row present; D-01 row present; `failed` **31→26** (−5 = the 5 B1 quota drafts gone, 0 quota errors remain); the 26 remaining `failed` = **17 `invalid_grant`/published (B2, untouched) + 9 `No refresh token`/published (B3, untouched)**; generated-pool guard clean (0 video_id / 0 post_publish / 0 stale-error). **B2 (17) is held for a separate Unit B-2; B3 (9) is never-connected onboarding debt. Full Unit B is NOT complete; B2 was NOT recovered.** A-then-B: **A done, B1 done.** *(History: an earlier pass recorded B1 as HELD because the deploy directive forbade running Unit B and B1 was applied by CCH out-of-band; CCD corrected to APPLIED only after independently verifying the migration row + D-01 row + distributions — never on say-so.)* (The first directive's avatar-branch target was correctly halted — `v1.8.0` did not exist there; the corrected `main` target was verified before deploy.) Full detail: `docs/runtime/sessions/2026-05-26-v3.09-youtube-publisher-v1.8.0-deploy.md`. Dashboard PHASES leg N/A (operational).

---

> **✅ v3.08 Avatar→YouTube Shorts milestone (2026-05-26):** Repo reconciliation of an already-live pipeline — **0 production mutation, 0 new deploy, 0 D-01.** The HeyGen talking-avatar → YouTube Shorts path is RESOLVED end-to-end; the deployed EFs' source was merged to `main` from branch `feat/ai-worker-v2.13.0-avatar-youtube` (Option B bridge-native squash — the EFs were already deployed + verified-publishing, so this is source-parity reconciliation, not a deploy). **F-HEYGEN-NEVER-PRODUCED Option A** delivered: **ai-worker v2.13.0** (A2 avatar-format override + A3 `generateVideoScript` avatar narration branch; sets `video_status='pending'`), **heygen-worker v2.0.0** (async two-phase submit/poll across cron ticks — removes the ~150s EF-request ceiling; 720×1280 portrait; raw-error JSON + stale-max-age guards), **youtube-publisher v1.7.0** (`video_short_avatar` added to the eligible-format allow-list → **F-YT-PUB-AVATAR-EXCLUSION CLOSED**). First live unlisted Short **`sfQvSM2Osus`** (NDIS-Yarns, draft `40f9fa25`, `m.post_publish` `5dbfcc0f`, 720×1280, 7.8 MB, published 2026-05-25 11:42:07 UTC; no duplicate render — same `heygen_video_id` across submit/poll). Landscape proof `ba5b34eb` retired `archived_stale`. **F-YT-FAILED-NO-RETRY remains OPEN** (publisher catch still terminal-fails) → re-pointed to **youtube-publisher v1.8.0**. Reconciliation commits on `main`: EF parity `521b053b`+`194e0792`, continuity docs `04294d23`, this `00_` rebuild. Chat verified the 3 branch version strings + the `post_publish` row read-only; all 12 reconciled files byte-identical to branch (sha-verified). Closeout: `docs/operations/avatar-youtube-pipeline-status-2026-05-25.md`. (Note: `5005b2b`/`aa07252`/`21e372d` cited in that closeout's §2 are the branch's own build-history refs; the four commits above are the authoritative `main` reconciliation commits.) Live ai-worker is now **v2.13.0** — this is the avatar milestone and is unrelated to cc-0019's Unit-B eligibility preflight (which is re-verified NOT-implemented against ai-worker v2.13.0 (`8204a5c7`); remains optional — the live Unit-A `fill_pending_slots` gate already covers the primary token-waste vector).

---

> **✅ v3.07 doc-patch housekeeping (2026-05-25):** Doc-only, repo-only — **0 production mutation, 0 D-01.** **cc-0017c main brief → v1.2** apply-record backfill: stale `AUTHORED_PENDING_D01_REFIRE` → **APPLIED-WITH-VCHECK-CORRECTION v2.85** (apply 2026-05-19; migrations `20260519004545` + `20260519005322`; 3 D-01 refs — v1.0 `a37eff28-…` partial, v1.1 `9e602a2d-…` partial/type-c [UUID tail not captured in source — recorded truncated], apply `d18fa6db-…` AGREE; V-B4 12-param signature pointer added). Commits `6d5c3ae6` + correction `81dcb683` (softened an unverified cc-0017d session-version tag — no-fabrication discipline). **Deferred to a CC surgical pass** (insufficient documented scope + full-file re-emit risk via the GitHub bridge — no str_replace): cc-0017a (72KB), cc-0010A (107KB), cc-0011 (61KB), cc-0012 (49KB) v-next doc patches, and the `cc-0017c/vchecks.md` subfile V-B4 12-param fix. **vchecks.md path LOCATED:** `docs/briefs/cc-0017c/vchecks.md` (subfile, not a hunt). **decisions.md** runtime path still unlocated (parked per PK). **Lessons NOT promoted** — canonical-home file (L37–L65 definitions) not located in root/docs/runtime/handoff + promotion numbering convention undocumented; left for a next-lesson-cycle / CC pass per PK (no promotion by assumption). No per-session file + Dashboard PHASES leg N/A (PK scoped this to the two `00_` files; doc-only, no phase change). Re-read HEAD + sha-passed per L41 (sync_state blob `df1f44ac`).

---

> **✅ v3.06 deployed↔repo drift close (2026-05-25):** Source-of-truth reconciliation — **repo-only, 0 production mutation; 2 plan_review D-01 (non-mutating).** (1) **instagram-publisher synced to repo**: deployed v2.4.0 was **4 versions ahead** of repo v2.0.0 — pushed the exact deployed source (`bc78511e`, byte-verified on every anchor; rollback hazard closed — a redeploy from repo no longer reverts the fix). (2) **7 chat-applied 2026-05-24 migrations backfilled** to `supabase/migrations/` (`8738c3a0`, recovered verbatim from `supabase_migrations.schema_migrations.statements`) — the IG fresh-restart trio + cc-0019 gate + the 3 YT v3.05 migrations; **DB→repo migration drift CLOSED**. (3) **F-YT-FAILED-NO-RETRY brief authored** (`docs/briefs/yt-publisher-failed-no-retry.md`, `bee872aa`), modelled on the IG v2.4.0 bounded-retry + channel-hold pattern; then **Unit B patched to the live 3-bucket recovery spec** (`a3f329f3`) + **plan_review fired CLEAN** (`abf6f63c` partial/type-c → `f89c23cf` agree; the prior partial was resolved by satisfying its corrected_action against live data — `video_status` confirmed `text`/no-CHECK + the 30 failed drafts live-bucketed 5 quota / 17 token-casualty / 8 never-connected — then re-firing). (4) **cc-0019 status CORRECTED**: live DB has `m.is_publish_eligible(uuid,text)` + the `m.fill_pending_slots` gate (migration `20260524091020`; governed D-01 `a2e5f3f5` + PK approval per the migration header) → **Unit A IMPLEMENTED**; but the deployed **ai-worker v2.12.0** has no eligibility preflight → **Unit B NOT implemented**. v3.05's "cc-0019 brief only, NOT implemented" was **stale** (corrected here; not reconstructing the exact apply session). The pre-compaction **IG-publish-restore arc OWED 4-way close is now substantially discharged** (publisher-code + migration-file legs). Session `1219aff0`. Repo now: `instagram-publisher` = deployed v2.4.0; all 7 migration files present. Next: YT F-YT-FAILED-NO-RETRY Unit A/B execution (ef_deploy + sql_destructive + PK approval, A-then-B) when PK directs. Dashboard PHASES leg N/A (operational).

---

> **✅ v3.05 YouTube restore (2026-05-24):** YouTube publishing **RESTORED**. Root cause definitively found — the Google OAuth app was in **Testing** publishing status, which caps refresh tokens at **7 days** (NY token lasted 7.00d, PP 7.92d after the 5 May reconnect; classic fingerprint). PK moved the OAuth app to **Production** + re-exchanged tokens (NY 10:48, PP 10:49 UTC); two single-draft live tests **published successfully** (PP `ma6EG1fz4XQ`, NY `qp-ZGm8lNIo`) confirming both channels' tokens now refresh with no `invalid_grant`. Backlog then **released** (fresh ≥12 May → `generated`: NY 6 + PP 7 = 13, draining 2/tick via jobid 34) and **soft-retired** (stale <12 May → `archived_stale`: NY 3 + PP 11 = 14; PK chose retire over hard-delete to preserve content/audit + avoid FK risk). Also: **cc-0019 brief** (publish-eligibility gate before AI draft generation) authored `81cb414` + hardened `01a6cdf`, plan_review `a75c78f6`→`ff05c65e` (clean) — **brief only, NOT implemented**; and a read-only slotting AI-cost-waste audit (fill_pending_slots spends tokens with no publish-eligibility check; materialise_slots reads `publish_enabled` only for format-selection, not skip). 3 apply_migration (draft `video_status` only, bounded+guarded) + 2 publisher invokes (real publishes). D-01 fires: `a75c78f6`/`ff05c65e` (plan_review) + `82443c8c`/`5f7bfc52` (sql_destructive). **NEW findings: F-YT-OAUTH-TESTING-MODE (root cause, RESOLVED — recorded so it stops being re-diagnosed as a token reconnect); F-YT-FAILED-NO-RETRY (publisher marks transient auth failure as terminal `failed` → bricks backlog silently; candidate brief); F-YT-EXPIRY-DISPLAY-FAKE (callback hardcodes now+5y expiry, masks dead tokens).** 0 cron change / 0 EF deploy / 0 client enablement change / 0 hard delete / 0 friction.* / 0 cc-0015 start / 0 Stage E / 0 Q-005 closure / 0 decisions.md change / 0 dashboard edit. Dashboard roadmap/PHASES leg N/A (operational, no phase change). **OWED: the pre-compaction 2026-05-24 IG-publish-restore arc (instagram-publisher v2.4.0; IG queue soft-purge; void+repurge; cron 53 enable) still needs its own 4-way close — flagged, not folded in (unverified SHAs in-context).** *(v3.06 update: this OWED is now substantially discharged — publisher synced to repo `bc78511e` + 7 migration files backfilled `8738c3a0`; residual operational only.)*

---

> **✅ v3.04 dashboard recording (2026-05-21):** Dashboard **Slice 0A** (Pre-Phase 0 / Gate-11-window sidebar IA shell + Visual Tokens v1) **MERGED to `invegent-dashboard` main**. New dashboard main HEAD `3ec489b6fb1e4ad706aac9d32f7fefa4ad43b9c5` (short `3ec489b`); parent `d17b604`; squash merge of `e65b812` (initial slice) + `399c087` (CCB polish). Files: `components/sidebar.tsx` + `tailwind.config.ts` + `app/globals.css` (exactly 3). Typecheck exit 0; CCD PASS / CCB visual PASS / CCB polish re-check PASS. Verified independently via Invegent GitHub `list_recent_commits` + live `sidebar.tsx` read (commit SHA + content both confirmed, not taken on directive faith). **Slice 0A is NOT real Dashboard Phase 0** — the locked Phase 0 schema/data groundwork (`m.attention_item` / `m.vw_pipeline_state` / `m.brief` / `m.action_event`; dashboard-review §9) remains future/gated. Recorded documentation-only into the two `00_` index files via read-HEAD-first surgical edits (sync_state blob `348c0a6e` re-read this session). 0 Supabase / 0 dashboard edits / 0 Phase 0 schema work / 0 cc-0015 start / 0 Stage E / 0 PRV / 0 Q-005 closure / 0 decisions.md change / 0 D-01.

---

> **⚠️ v3.02.1 reconciliation note (2026-05-21):** the original v3.02 close (`a08945d3`) was authored from pre-v3.01 content and its full-file `push_files` **reverted v3.01's index/ranking content** in this file and in `action_list` (Cowork WARN closure, cc-0015 promoted to rank 1, v3.01 index row). The v3.01 commit (`83cd633c`) and its session file survived; only the `00_` index content was clobbered. This file now restores v3.01's facts AND keeps v3.02's mobile-viewport closure. The v3.02 commit's claim of "L41 clobber-mitigation applied" was **false** — HEAD was not re-read before that push; v3.02.1 was the repair. L41 genuinely re-exercised there. **v3.02.2 (2026-05-21): mobile-fix commit SHA backfilled — `d17b6047411ce177d6182d86a21a79f7302459af` (short `d17b604`); no longer pending. Applied surgically (HEAD `19a4734c` confirmed, per-file blob-SHA re-read).**

---

## ⚠️ Session-start reading order (per memory entry 1)

1. **`docs/00_sync_state.md`** (this file) — pointer index + last 1-2 sessions inlined
2. **`docs/00_action_list.md`** — running queued/active/blocked/frozen backlog
3. Open the most-recent session file from the index below if deeper context is needed

---

## 📚 Session index (reverse chronological)

| Date | Slug | Headline | File |
|---|---|---|---|
| 2026-06-08 | obs-stage0-diagnostic-heygen-telemetry-d01 | **OBS Stage 0 read-only diagnostic (HOLD 0A) + HeyGen render telemetry v2.1.0 D-01 open (undeployed)** — OBS Asset Policy Stage 0: foundation APPLIED to isolated `invegent-obs-stage0` only; read-only diagnostic complete → recommendation C (HOLD 0A; residual gaps are source-instrumentation, not observer-read; 06 doc commit `f5c00e04`); 0A observer/read-path BLOCKED. heygen-worker v2.1.0 render telemetry (mirror terminal HeyGen render outcomes into `m.post_render_log` via engine-agnostic `public.write_render_log`) committed `690a295b` UNDEPLOYED; D-01 `0d9bb255` (ef_deploy) OPEN/escalated; deploy blocked pending PK exact-phrase approval + CCD pre-deploy checks. No DDL / migration / deploy / Supabase or production mutation / provider call this lane. | `docs/runtime/sessions/2026-06-08-obs-stage0-diagnostic-heygen-telemetry-d01.md` |
| 2026-06-02 | v3.27-video-worker-caption-hierarchy-deploy | **video-worker caption-hierarchy polish (v3.1.3) DEPLOYED** — caption-styling-only P3 for `video_short_kinetic_voice` (captions now read secondary/subtitle vs main on-card text). CCH code review PASS + CCB visual review PASS. FF-merge `f0c9682..fc3ec12`; branch `polish/video-worker-caption-hierarchy` (`f9be78c` styling `index.ts` +5/−5 + `fc3ec12` `VERSION` v3.1.2→v3.1.3). **Shipped:** caption `font_size` 52→40px, `font_weight` 700→500, scrim `opacity` 0.55→0.62; colour `#FFFFFF` + `CAP_MAX_CHARS` 64 unchanged; comments→40px. **Geometry / timing / chunking / audio / voice / `getVoiceId` / templates / webhook / queue / DB / stat / non-kinetic UNCHANGED.** **Deploy (wrapper only):** 6 targeted EF-drift refreshes (incl. GitHub raw-CDN propagation polling, then final post-deploy refresh) → **B-FD** (repo 3.1.3 > deployed 3.1.2) → `--check-only` BLOCK exit 1 (expected gate) → `--check-only --allow-warn` PASS → `--allow-warn` deploy exit 0 → **A-LE / clean / in-spec**; repo==deployed hash `0991ff65…b4088bd7`. Health `{"version":"video-worker-v3.1.3"}` HTTP 200. **D-01 CCD-inline** (PK `ef_deploy` phrase recorded verbatim) — no `m.chatgpt_review` fabricated. **F-CREATOMATE-CAPTION-HIERARCHY CLOSED.** Carry: **F-VOICE-LOWVOL-BACKFILL** (P3, separate); branch cleanup deferred (needs separate approval). 0 schema / 0 migration / 0 cron/queue / 0 publisher/social / 0 image-worker / 0 webhook / 0 render / 0 Creatomate/ElevenLabs call / 0 production draft mutation / 0 backfill. | `docs/runtime/sessions/2026-06-02-v3.27-video-worker-caption-hierarchy-deploy.md` |
| 2026-05-29 | v3.19-friction-register-manual-passes | **Friction-register manual operating passes 1+2 COMPLETE + FB insights-coverage gap finding** (operational/register hygiene; 4 resolves + 1 suppress via SECURITY DEFINER RPCs one-at-a-time, D-01 + PK approved; 0 dashboard/`/operations`/`/operations/pools`/Stage C/migration/schema/EF/cron/Vercel/Retool/decisions.md). First register closures ever (46/46 open at start). **46 / 45 / 1 / 3 new → 46 / 42 / 4 / 0.** Resolved (tracked_done): `a6c6032f` NY-fb starvation / `fcb5612e` Invegent-fb residual / `ac6c52ed` + `7010f5b7` PP-fb observer. Suppressed: `d5efc053` NY-YT structural false-positive (no YT insights pipeline). Tracked genuine gaps: `26436794` NY-fb / `877b7382` + `78afc70e` CFW-fb. Held: `15d57015` Content Studio. D-01 `a94c9f23` + `9432e82a` (incl. d5efc053 attribution caveat) + `4750ae49` fired+closed-loop. `d5efc053` call-1 positional-arg error: suppress disposition correct, corrected via audited named-arg RPC, `triaged_by=postgres` known artefact, pk-actor history row exists, no direct DML. **FB insights-worker v14.1.0 client-incomplete:** flowing PP-fb + Invegent-fb; NOT NY-fb (stopped 05-23) or CFW-fb (zero ever) → next lane = per-client coverage fix. `/operations` UI can triage but not close. | `docs/runtime/sessions/2026-05-29-v3.19-friction-register-manual-passes.md` |
| 2026-05-29 | v3.18-cc-0020-stage-5-unitb-merged | **cc-0020 Stage 5 Unit B code-readiness MERGED on `main` via PR #6 — session close** (docs-only; 0 deploy/OAuth/Gmail/Pub-Sub/cron/DB write/migration/production runtime change/D-01). v3.17 already mainline (PR #5 / `883cbf72`). PR #6 head `1888b56192149802a92ed7a9d16cea22cf38d5b1`; merge commit `9e4d3dbcb1588d91079ab641dd820849ce36d165` (parents `df8c54e` + `1888b56`); origin/main HEAD `3ddac58a103ff37a9114dd20ecaf9ddc9843ad44` (post-merge nightly-health-check run report `3ddac58`, unrelated). **Unit B merged contents:** metadata-only Gmail fetch (`gmail.ts`: `format=metadata` hardcoded, `metadataHeaders=From,Subject,Date`, sender allowlist + `maxResults≤25` + `windowDays≤7` bounds, fail-closed env-flag guard, token never logged/stringified/returned); RPC-only `ServiceRoleIngestRepository` (calls `public.ingest_subscription_import_candidate` SECURITY DEFINER RPC; dashboard-only methods throw pointers to `public.get_subscription_*` / `public.review_subscription_candidate`); async `IngestRepository` interface; `index.ts` dual-layer kill-switch (`SUBSCRIPTION_GMAIL_INGEST_ENABLED` checked before Supabase client construction AND inside `fetchSubscriptionEmails`); version banner `subscription-email-ingest-v1.0.0-disabled`. **Tests: 41 PASS hermetic.** **V-B7 added pre-merge** to fix CCH plan_review PARTIAL: initial commit `75cfba3` wrote to schema `k` via PostgREST (violates cc-0020 access rule — `k.*` not PostgREST-exposed); V-B7 patch `1888b56` switched to RPC-only boundary + reduced `SupabaseLike` type to expose only `.rpc()` + added the V-B7 canary that fails the build if direct `k.*` PostgREST access is reintroduced. **CCH re-review CLEAN after V-B7 patch**, then PR #6 merged. **Unit B NOT deployed and HAS NO RUNTIME EFFECT** (function not deployed; even if deployed, kill switch defaults OFF). **Unit C BLOCKED** until: (a) `public.ingest_subscription_import_candidate(...)` SECURITY DEFINER write RPC migration authored against Stage 2 schema; (b) sql_destructive D-01 + PK exact approval phrase; (c) real-PG/PGlite proof of `ON CONFLICT (gmail_message_id) DO NOTHING` idempotency; (d) U-C deploy-disabled gate (ef_deploy D-01 + PK escalate=true) separately fired. Required RPC signature recorded in `repository.ts` via `INGEST_CANDIDATE_RPC` / `INGEST_CANDIDATE_RPC_PARAMS` / `CANDIDATE_RPC_SQL` constants. **Unit A required before first live smoke** — independent of Unit C: OAuth app Production publishing status, `gmail.readonly` scope only, Supabase Edge-Function secrets (`SUBSCRIPTION_GMAIL_REFRESH_TOKEN` / `_CLIENT_ID` / `_CLIENT_SECRET` / `_SENDER_ALLOWLIST`) set, secret-name PRESENCE verified without printing values. **Unit D first live smoke BLOCKED** until Unit A + Unit C complete. **Unit E cron** = separate later gate. **Unit F Pub/Sub** = DEFERRED to Stage 5b. Other lanes (cc-0016 + cc-0015 + YT + PRV + IG) preserved unchanged from v3.17. | _(no session file — `00_`-only close per v3.13.2/v3.13.3 precedent)_ |
| 2026-05-28 | v3.17-cc-0020-stage-5-scoping-and-g1-g2-close | **cc-0020 Stage 5 scoping AUTHORED + governance G1 RESOLVED + G2 ACCEPTED-AS-APPLIED** (docs-only; 0 OAuth/Gmail/Pub-Sub/EF deploy/cron/DB write by CCD/migration/D-01). (1) `docs/briefs/cc-0020/stage-5-live-gmail-execution.md` — 13 sections: stage objective + E-1…E-7 entry criteria + P-1…P-5 prerequisites + 6-unit sequencing A→B→C→D→E→F (U-A token mint manual / U-B code / U-C DISABLED deploy / U-D first live smoke ≤24h ≤10 messages + format=metadata only + sender allowlist / U-E cron / U-F Pub/Sub DEFERRED Stage 5b) + per-unit V-checks + per-unit D-01+PK phrases + global kill switch (`SUBSCRIPTION_GMAIL_INGEST_ENABLED=false`) + per-unit rollback + 11-item OOS + smallest-blast-radius smoke design + 10-item risk table + 6 OQs. Linked from cc-0020 main brief §3 Stage 5. Stage 5 execution remains future/gated. (2) Governance G1 RESOLVED: `m.chatgpt_review.6d9a4bd3-d665-47c0-8dd9-165d3bc75bd6` updated by CCH — `status='resolved'`, `escalation_resolved_at='2026-05-28 10:55:17.310207+00'`, `resolved_by='PK (retrospective confirmation 2026-05-28) + CCH (close-loop record)'`; first UPDATE row_count=1, guarded rerun row_count=0 (idempotency proved). Migrations `20260527114041` + `20260527114144` now have complete in-row governance trail. v3.16 G1 "not cleanly closed" wording SUPERSEDED. (3) **G2 (`20260527114333` REVOKE-anon/authenticated hotfix) ACCEPTED-AS-APPLIED** by PK directive (docs-only): pure grant-tightening (REVOKE EXECUTE FROM `anon`,`authenticated`); no data effect, no schema change; live posture already verified correct (RPC EXECUTE = service_role + postgres only; anon/authenticated/PUBLIC denied); residual risk = none known; process gap recorded as accepted. **NOT precedent for skipping D-01 on future grant changes** — grant-tightening hotfixes ordinarily fire `sql_destructive` D-01; this is a one-time retrospective acceptance because the change was strictly tightening and the resulting posture was verified safe. Other lanes preserved unchanged from v3.16. | `docs/briefs/cc-0020/stage-5-live-gmail-execution.md` _(no session file — `00_`-only close per v3.13.2/v3.13.3 precedent)_ |
| 2026-05-28 | v3.16-four-point-reconciliation | **Four-point docs-only reconciliation** (0 SQL/DB/deploy/cron/PR/OAuth/D-01). (1) cc-0016 Stage E **dry-run design AUTHORED** (`docs/briefs/cc-0016/stage-e-dryrun-design.md`; 7 sections incl. 10 boundaries + 9 SELECT-only queries + 9 acceptance criteria + separate future gate G-E1/G-E2/G-E3 + risks/no-op guarantees; future D-01 + PK approval STILL REQUIRED). (2) **cc-0020 status CORRECTED — NO LONGER PARKED / HELD / UNAPPLIED** (supersedes v3.15 wording): merge commit `49a6c0e` advanced content-engine `main` w/ Stage 0–4B; 3 migrations APPLIED to prod `mbkmaxqhsohbtwsqolns` (`20260527114041` tables `k.subscription_import_candidate`+`k.subscription_spend_event` / `20260527114144` 4 `public.*` SECURITY DEFINER RPCs / `20260527114333` security hotfix REVOKE EXECUTE FROM anon,authenticated); RPC EXECUTE = service_role + postgres only; repo↔prod migration-history reconciliation v2 **merged on `main`** via PR #3 merge commit `d7543078` (parents `3cd144f` v1 + `fa2aefc` v2; canonical applied-version files + `_proposed_` tombstone slots are the mainline state); dashboard `main` advanced w/ Stage 4-A UI + 4-B service-role RPC routes (live `dashboard.invegent.com/system/subscriptions`); Stage 5 wall (Gmail OAuth/Pub-Sub/EF deploy/cron) still intact / future-gated; **governance trail (HONEST — partially evidenced, not cleanly closed):** CCH read-only sweep on `m.chatgpt_review` resolved the prior generic "evidence gap" — migrations `20260527114041` + `20260527114144` are governed by review row **`6d9a4bd3-d665-47c0-8dd9-165d3bc75bd6`** (action_type=plan_review, verdict=partial, risk=medium, confidence=medium, requires_pk_escalation=true, action_taken=escalate_explicit_flag, status=escalated, resolved_by=NULL, escalation_resolved_at=NULL); residual G1 = `6d9a4bd3` close-the-loop never recorded in-row (PK discharge implied by timing, not captured); residual G2 = migration `20260527114333` REVOKE-anon/authenticated hotfix has **no D-01 / governance row found**. Live grant posture verified correct (RPC EXECUTE = service_role + postgres only); residuals are governance-trail debt, not a live-permissions risk. New carries below (G1 + G2). (3) **YT backlog drain CLOSED**: `m.post_publish` YouTube rows = 61; latest 2026-05-28 08:15:08Z; published-draft↔youtube-publish-row gap = 0 for the released backlog. Carry separately (NOT the released backlog): 2 NY `generated` stragglers + stale `pending` drafts are pre-existing / predicate-ineligible (fail format/approval/url predicate). (4) **cc-0015 Gate 11 observation window ELAPSED/CLOSED as a time-bound watch** (1-week window 2026-05-19 → 2026-05-26 ended 2026-05-26; the watch closed by elapsed time). Stage C/D/E remain separately PK-gated; ICE-PROC-002 +4-week review ≈ 2026-06-26 next process milestone. Walls intact: 0 DB connection / 0 OAuth / 0 deploy / 0 cron / 0 PR/merge / 0 destructive Stage E run. | `docs/briefs/cc-0016/stage-e-dryrun-design.md` _(no session file — `00_`-only close per v3.13.2/v3.13.3 precedent)_ |
| 2026-05-27 | v3.15-instagram-cc0020-sync-close | **Instagram diagnosis + cc-0020 subscription-email-ingest lanes PARKED — session close** (docs-only; 0 code / migration-apply / DB / deploy / Gmail / cron / PR / D-01). (A) IG diagnosis (CCH lane) parked/closed — runbook `b911d91`; NY/PP `publish_enabled` flipped via UI → cc-0019 fill-gate cleared; self-heal ≈ 2026-05-29; no further IG action unless PK reopens. (B) cc-0020 (CCD lane) parked — branch `feat/cc-0020-subscription-email-ingest` @ `7497e74` (pushed, unmerged, undeployed); Stage 0/1/2/2a/2b accepted, Stage 3 conditionally accepted (fixture-only scaffold; 17 Deno tests; in-memory repo models ON CONFLICT(gmail_message_id)+UNIQUE(source_candidate_id); fail-closed Gmail stub), Stage 3a-P closed; **Stage 3a DB-layer validation OPEN (blocks Stage 4); Stage 4 HELD**; draft migration `20260527120000_proposed_cc_0020_…` UNAPPLIED. PRV + cc-0015 preserved unchanged. | `docs/runtime/sessions/2026-05-27-v3.15-instagram-cc0020-sync-close.md` |
| 2026-05-27 | v3.14-prv-readonly-mvp-pushed | **Platform Reconciliation View read-only MVP COMMITTED + PUSHED** (feature branch; unmerged, undeployed; 0 deploy/PR/merge; `op` not PostgREST-exposed). `/recon` in the content-engine `dashboard/` reads the 3 cc-0012 `op.*` views (`v_reconciliation_summary`/`v_freshness_rollup`/`v_drift_rollup`) via a server-only direct-PG pooler transport (`dashboard/lib/db.ts`; read-only session + SELECT-only) as least-privilege role `prv_readonly` (mig `20260527011420_prv_readonly_login_role`; D-01 `d86b7db8` agree/proceed + PK phrase; backfilled byte-exact; close-loop done — `d86b7db8` completed, `57fd738b`+`43140c7f` superseded→resolved). Live render PASS (7d 56/16, 14 freshness pairs, 39 drift/26 actionable) + CCB visual PASS. Commit `7287a1e` pushed to `origin/feature/platform-reconciliation-view-readonly`; `main` untouched `911d0d4`. `.env.local` local-only + untracked. Polish + the two grant-covered rollup views + production deploy (`PK APPROVES PRV READ-ONLY PRODUCTION DEPLOY`) deferred/gated. | `docs/runtime/sessions/2026-05-27-v3.14-prv-readonly-mvp-pushed.md` |
| 2026-05-26 | v3.13.3-cc-0015-stage-g-vg1-approved | **cc-0015 Stage G ICE-PROC-002 V-G1 APPROVED** (doc-only; 0 production mutation). PK confirmed V-G1 — `docs/process/ICE-PROC-002-pooled-resolution.md` is now the operating reference for the current cc-0015 `/operations` surface. V-G1+V-G2 (`d5b296f`)+V-G3 all satisfied → Stage G complete as a doc artefact. Stage C/D/E held; deferrals stand; cc-0015 NOT complete. Next milestone: +4-week ICE-PROC-002 review ≈ 2026-06-26. | `docs/process/ICE-PROC-002-pooled-resolution.md` _(no session file — `00_`-only close)_ |
| 2026-05-26 | v3.13.2-cc-0015-stage-g-process-doc | **cc-0015 Stage G process doc AUTHORED** (doc-only; 0 production mutation). Wrote `docs/process/ICE-PROC-002-pooled-resolution.md` — pooled-resolution rhythm: why pooling, triage defaults (`track` default / `act_now` reserved / `critical` interrupts), cadence (Fri 09:00 Sydney; Dashboard UI weekly / Register reconciliation weekly-if-≥3-else-biweekly / Pipeline-friction triggered / All-tracked + New-untriaged maintenance), session protocol, suppression discipline, anti-patterns, +4wk review; COEXIST model + 5 saved views + known deferrals. Linked from `00_docs_index.md` (new `process/` row; V-G3). Scoped to shipped surface — no Stage C/D/E assumed; cc-0015 NOT complete. V-G1 (PK approval) remains. | `docs/process/ICE-PROC-002-pooled-resolution.md` _(no session file — `00_`-only close)_ |
| 2026-05-26 | v3.13.1-cc-0015-stage-a5-recategorisation | **cc-0015 Stage A.5 recategorisation APPLIED + verified.** Guarded UPDATE migration `20260526222125_cc_0015_a5_recat_operator_friction_to_dashboard_ui` moved 3 dashboard-UI cases (`53f3e533`, `2cf0cd4f`, `b7369dc9`) `operator_friction`→`dashboard_ui`. Verified: `dashboard_ui` 0→3, `operator_friction` 6→3, `client_commitment` 16 + `pipeline_integrity` 22 unchanged, total 44; YouTube content-series request + 2 cc-0016 smokes + overview/auto-approver case correctly left behind. sql_destructive D-01 `bbe9a0fa` (agree/low/high/escalate=false) + PK phrase; close-the-loop completed. Migration file backfilled verbatim (md5 `5fdb7de7…` == DB), committed+pushed `41576f8`. 3 cases now under live `/operations?category=dashboard_ui`. cc-0015 NOT complete (Stage C/D/E held). | `docs/runtime/sessions/2026-05-26-v3.13.1-cc-0015-stage-a5-recategorisation.md` |
| 2026-05-26 | v3.13-cc-0015-stage-b-frontend-deploy | **cc-0015 Stage B FRONTEND MERGED + DEPLOYED.** `/operations` filter bar + 5 saved views (Dashboard UI / Register reconciliation / Pipeline-friction / All tracked / New-untriaged) + URL-param filters + category-description hydration, on the extended `fn_recent_cases`. CCB preview visual PASS (V-B3 URL filters / V-B4 saved views / V-B5 Repair-Board-v0-untouched / V-B6 default byte-identical; stale-wording CLEAN) on `feat/cc-0015-stage-b-operations-filters` (`25bcdb7`) → FF-merge to dashboard `main` `25bcdb7` (tsc --noEmit clean + next build exit 0) → **production deploy READY** (Vercel `dpl_Hb2BkFnK3cL4cCAHXbkAz1tDnHjW`, `dashboard.invegent.com`; liveness `/operations` 307→`/login` 200, no 5xx — visual was CCB preview). Repair-Board v0 untouched (185 B unchanged); category-desc hydration COMPLETE for `/operations` (FAB deferred); source/sort deferred; Stage C/D/E + Stage A.5 held. 0 SQL this pass. cc-0015 NOT complete. | `docs/runtime/sessions/2026-05-26-v3.13-cc-0015-stage-b-frontend-deploy.md` |
| 2026-05-26 | v3.12-cc-0015-stage-f-operator-copy | **cc-0015 Stage F SHIPPED** (operator help copy). CCB visual V-F1/V-F2 PASS on preview (`f9d51a9`) → FF-merge to dashboard main `f9d51a96` → **production deploy READY** (`dpl_FftxSjeBNyurxZetJedi3zDsan6b`, `dashboard.invegent.com`). Stage A re-verified APPLIED (mig `cc_0015_a_pool_schema_additions`; D-01 `58d89efe` completed; V-A1–A4 PASS). Category per-option descriptions RE-SCOPED follow-up; Stage B/C/D held (Repair-Board v0 re-brief). cc-0015 NOT complete. 0 SQL this pass. | `docs/runtime/sessions/2026-05-26-v3.12-cc-0015-stage-f-operator-copy.md` |
| 2026-05-26 | v3.12-cc-0015-stage-a-applied | **cc-0015 STARTED (gate PK-confirmed).** Stage A APPLIED (mig `cc_0015_a_pool_schema_additions` v`20260526110657`: `dashboard_ui` category + `friction.pool_session` table; plan_review D-01 `58d89efe` escalate→PK + PK phrase; V-A1–A4 PASS; migration file backfilled same-session). Stage F PREPARED (copy dict on dashboard prep branch `feat/cc-0015-stage-f-operator-copy` `4c31549`, not merged/deployed; awaits PK V-F3). **Stage B/C/D HELD for re-brief** (cc-0015 category pools vs Repair-Board v0 — v0 NOT superseded). 1 migration; 0 deploy; 0 UI merge. | `docs/runtime/sessions/2026-05-26-v3.12-cc-0015-stage-a-applied.md` |
| 2026-05-26 | v3.11.1-yt-migration-backfill | **DB→repo migration drift CLOSED for the YT recovery arc** (repo-only; 0 DB change/deploy/mutation). The 3 `apply_migration`-applied migrations (B1 `…b1_only_20260526` v`20260526054427`; B2 `…b2_token_casualty_20260526` v`20260526091149`; audit-gap `…backfill_20260526` v`20260526094403`) backfilled to `supabase/migrations/` verbatim from `schema_migrations.statements`; file SQL-body md5 == DB md5 for all 3 (byte-exact). Brief artifacts differ by comments + trailing verification SELECTs (semantics identical); DB statements kept as source of truth. B1+B2 complete, audit gap closed, B3 untouched. | `docs/runtime/sessions/2026-05-26-v3.11.1-yt-migration-backfill.md` |
| 2026-05-26 | v3.11-yt-publish-audit-gap-closed | **F-YT-PUB-PUBLISH-AUDIT-GAP CLOSED, A-then-B.** Unit A youtube-publisher **v1.10.0 deployed** (ef_deploy D-01 `5bd19069` clean; commit `fe8729c`; live GET `v1.10.0`) — next-available `attempt_no` for the YT audit insert + non-silent `insertErr`; v1.9.0 predicate/guards preserved. Unit B **11 historical audit rows backfilled** (DML `538d9e1`; sql_destructive D-01 `998c90a3` escalated→PK exact phrase; mig `yt_pub_publish_audit_gap_backfill_20260526`; `ROW_COUNT=11`; gap=0; all `platform_post_id`==draft youtube_video_id @ attempt_no=2; FB untouched 736; no draft mutation/re-upload). Honest limit: platform_post_id authoritative; historical published_at/payload reconstructed + backfill-tagged. **Whole YT failed-backlog arc complete: B1+B2 done, audit gap closed; B3 onboarding debt only.** | `docs/runtime/sessions/2026-05-26-v3.11-yt-publish-audit-gap-closed.md` |
| 2026-05-26 | v3.10-unit-b2-deploy-recovery | **Unit B-2 EXECUTED: youtube-publisher v1.9.0 DEPLOYED + 17 B2 token-casualty drafts RECOVERED.** ef_deploy D-01 `4719f53b` + sql_destructive D-01 `2135ae11` (both GNB/L46; PK exact phrase). mig `yt_failed_backlog_recovery_unit_b2_token_casualty_20260526`; 17 `failed→generated`, `approval_status` preserved=`published`, guards clean; `failed` 26→9 (B3-only). Drain: 2/17 published (`VonxZesS0Ws`,`u-xYWGg1qWI`), 0 re-failed. **Full Unit B (B1+B2) COMPLETE.** 🔴 NEW finding **F-YT-PUB-PUBLISH-AUDIT-GAP** — cross-posted YT publishes get no `m.post_publish` row (uq_publish_attempt UNIQUE(post_draft_id,attempt_no) + hardcoded attempt_no=1 collides w/ FB row; insertErr swallowed); uploads succeed but audit incomplete; open follow-on (fix+backfill). | `docs/runtime/sessions/2026-05-26-v3.10-unit-b2-deploy-recovery.md` |
| 2026-05-26 | v3.09-youtube-publisher-v1.8.0-deploy | **youtube-publisher v1.7.0 → v1.8.0 DEPLOYED + verified live** (CCD via Supabase CLI; `--no-verify-jwt` preserved; from `main` `b983a38` = EF commit `631fa93a`; live GET `youtube-publisher-v1.8.0` HTTP 200). **Unit A** of F-YT-FAILED-NO-RETRY; D-01 `ef_deploy` `818ee949` + PK directive. **Unit B1-only APPLIED + CCD-verified** — mig `yt_failed_backlog_recovery_unit_b_b1_only_20260526`, D-01 `sql_destructive` `33eeb164…` (`agree`/`completed`); 5 quota recovered (`failed` 31→26); **B2 17 + B3 9 untouched**; generated-pool guard clean. Full Unit B NOT complete; B2 held for Unit B-2. CCD did read-only verify only (0 DML); B1 migration applied by CCH. A-then-B: A done, B1 done. | `docs/runtime/sessions/2026-05-26-v3.09-youtube-publisher-v1.8.0-deploy.md` |
| 2026-05-25 | v3.07-doc-patch-housekeeping | **Doc-only, repo-only — 0 production mutation, 0 D-01.** cc-0017c main brief → **v1.2** apply-record backfill (status→APPLIED-WITH-VCHECK-CORRECTION v2.85; apply 2026-05-19; migrations `20260519004545`+`20260519005322`; 3 D-01 refs incl. apply `d18fa6db-…` AGREE; V-B4 12-param pointer) at `6d5c3ae6` + correction `81dcb683`. cc-0017a/0010A/0011/0012 v-next patches + `cc-0017c/vchecks.md` subfile **DEFERRED to CC** (insufficient documented scope + full-file re-emit risk via GitHub bridge — no str_replace). **vchecks.md path located** (`docs/briefs/cc-0017c/vchecks.md`); decisions.md still unlocated. **Lessons NOT promoted** (canonical home / numbering convention unresolved — no promotion by assumption, left for next-lesson-cycle/CC). No per-session file (`00_`-only close per PK). | _(no session file — `00_`-only close per PK)_ |
| 2026-05-25 | v3.06-ig-drift-close-yt-failed-no-retry-brief | **Deployed↔repo drift CLOSED** (repo-only; 0 production mutation; 2 plan_review D-01 non-mutating). instagram-publisher synced to repo (deployed v2.4.0 was 4 ahead of repo v2.0.0; `bc78511e`, byte-verified). 7 chat-applied 2026-05-24 migrations backfilled to `supabase/migrations/` (`8738c3a0`, recovered from `schema_migrations.statements`) — DB→repo migration drift closed. F-YT-FAILED-NO-RETRY brief authored (`bee872aa`) + Unit-B patched to live 3-bucket recovery (`a3f329f3`) + **plan_review CLEAN** (`abf6f63c` partial/type-c → `f89c23cf` agree; prior partial resolved by live-verifying video_status=text/no-CHECK + bucketing the 30 failed drafts). **cc-0019 CORRECTED**: Unit A IMPLEMENTED (live `is_publish_eligible` + `fill_pending_slots` gate, migration `20260524091020`, D-01 `a2e5f3f5` + PK approval per header); Unit B (ai-worker preflight) NOT implemented (verified absent in deployed ai-worker v2.12.0). IG-arc OWED 4-way close substantially discharged. Session `1219aff0`. | `docs/runtime/sessions/2026-05-25-v3.06-ig-drift-close-yt-failed-no-retry-brief.md` |
| 2026-05-24 | v3.05-youtube-oauth-production-restore | **YouTube publishing RESTORED.** Root cause = OAuth app in Google **Testing** status → refresh tokens capped at 7 days (NY 7.00d / PP 7.92d after 5 May reconnect). PK moved app → **Production** + re-exchanged tokens (NY 10:48 / PP 10:49 UTC); two live tests **published** (PP `ma6EG1fz4XQ`, NY `qp-ZGm8lNIo`). Backlog **released** (fresh ≥12 May → generated: NY 6 + PP 7 = 13, draining 2/tick jobid 34) + **soft-retired** (stale <12 May → archived_stale: NY 3 + PP 11 = 14; retire-not-delete to keep audit + dodge FK risk). Publisher v1.6.0 catch sets transient auth failure to terminal `failed` (never re-selected) → backlog was frozen invisible — **F-YT-FAILED-NO-RETRY** logged. Callback hardcodes now+5y expiry → fake "Valid 2031" masks dead tokens — **F-YT-EXPIRY-DISPLAY-FAKE**. **F-YT-OAUTH-TESTING-MODE** root cause RESOLVED + recorded (was always re-diagnosed as "reconnect token"). Also **cc-0019 brief** (publish-eligibility gate) authored `81cb414` + hardened `01a6cdf`; plan_review `a75c78f6`→`ff05c65e` clean — brief only. 3 apply_migration (draft video_status only, guarded) + 2 publisher invokes (real publishes). 4 D-01. 0 cron / 0 EF deploy / 0 enablement / 0 hard delete / 0 cc-0015 / 0 Stage E / 0 Q-005 closure / 0 decisions.md / 0 dashboard edit. IG-restore arc 4-way close OWED (flagged). | `docs/runtime/sessions/2026-05-24-v3.05-youtube-oauth-production-restore.md` |
| 2026-05-21 | v3.04-dashboard-slice-0a-merged | **Dashboard Slice 0A (Pre-Phase 0 sidebar IA shell + Visual Tokens v1) MERGED to invegent-dashboard main at `3ec489b`** (parent `d17b604`; squash of `e65b812` + `399c087` CCB polish). Files: `components/sidebar.tsx` + `tailwind.config.ts` + `app/globals.css`. Typecheck exit 0; CCD PASS / CCB PASS / CCB polish PASS. Sidebar regrouped to locked IA NOW/CLIENTS/CREATE/REPORTS/ADMIN (NOW split Daily + Investigate + collapsed Legacy); 5 status colour scales + 10 typography/spacing token helpers added additively; mobile drawer behaviour preserved verbatim. **Slice 0A is NOT real Dashboard Phase 0** — review §9 Phase 0 schema groundwork remains future/gated. Commit SHA + content independently verified (Invegent GitHub `list_recent_commits` + live `sidebar.tsx` read). Cross-repo documentation recording only. cc-0015 Gate 11 watch rank 1 (closes 2026-05-26) / Stage E future / PRV deferred / Q-005 OPEN next-fire watch ALL PRESERVED. 0 Supabase / 0 dashboard edits / 0 Phase 0 schema / 0 cc-0015 start / 0 Stage E / 0 PRV / 0 Q-005 closure. | `docs/runtime/sessions/2026-05-21-v3.04-dashboard-slice-0a-merged.md` |
| 2026-05-21 | v3.03-q005-optionA-v3.1.1-emission-guard | **Q-005 Option A trail recorded (A-005 + v3.1 + v3.1.1 guard); brief now v3.1.1; Q-005 stays OPEN, next-fire watch.** A-nightly-health-check-v1-005 ratified Option A (explicit per-finding `condition_key`) at `56e992b4`. v3.1 brief patch (`7005865`) added the 9-key mapping. **CCD read-only verification found v3.1 would FAIL live emission:** the function consumes explicit `condition_key` (correct) BUT `friction.emit_event` requires an enabled `friction.emission_rule` row for `(source, condition_key)`, and only `health_check/true_stuck` is enabled — v3.1's renamed `true_stuck_cluster` + 8 invented P2 keys would all be rejected (~`success_count=0, failure_count=5–7`), regressing the known-good P1 path. **v3.1.1 guard patch at `9ceb78a`** restores the live-enabled `condition_key=true_stuck` for true-stuck P1 emission and PARKS the other 8 P1/P2 keys (markdown-only; omitted from emission JSONB) pending a separate Supabase-approved `emission_rule` seed patch. Next natural fire is safe (true-stuck emits; parked types appear in markdown but not emitted). **Q-005 OPEN** — closes only when EITHER (A) emission_rule rows seeded + full mapping restored/verified, OR (B) PK formally narrows scope to P1-true-stuck-only + a natural fire verifies `failure_count=0`/`skipped_count=0`. cc-0015 Gate 11 watch / Stage E future / PRV deferred PRESERVED. 0 Supabase / 0 dashboard / 0 cc-0015 start / 0 Stage E / 0 nightly run / 0 re-emission. | `docs/runtime/sessions/2026-05-21-v3.03-q005-optionA-v3.1.1-emission-guard.md` |
| 2026-05-21 | v3.02-dashboard-mobile-viewport-pass | **Dashboard mobile/narrow viewport verification CLOSED/PASS.** CCB tested at 306×498 Nexus 5 Android Chrome UA, DPR 2 → MOBILE PASS. Mobile fix (`d17b604`; files `components/sidebar.tsx`, `app/(dashboard)/operations/case-row.tsx`; typecheck PASS) resolved: sidebar collapses to hamburger/drawer; desktop fixed rail no longer consumes mobile width; /operations rows readable; evidence paperclip badge visible; expanded Evidence section usable; triage guardrail usable; FAB usable; Roadmap readable; "Stop Claude" overlay remains external Claude Browser tooling. **Mobile/narrow viewport verification removed from open P3 carries.** NEW lesson candidate L-v3.02-a: mobile breakpoint verified after any primary operator-surface change. cc-0015 Gate 11 watch / Stage E future / PRV deferred / Q-005 (if open) PRESERVED. *(v3.02.1 reconciliation: original v3.02 push clobbered v3.01 `00_` index content; restored. v3.02.2: mobile-fix SHA `d17b604` backfilled — no longer pending.)* | `docs/runtime/sessions/2026-05-21-v3.02-dashboard-mobile-viewport-pass.md` |
| 2026-05-21 | v3.01-cowork-lifecycle-warn-closed | **Cowork brief lifecycle gating WARN CLOSED.** v2.94 lifecycle convention validated end-to-end on the first natural cycle: `nightly-health-check-v1` brief `ready → review_required → PK observed → ready` against the 2026-05-20T16:02:37Z natural 16:00 UTC fire (`docs/runtime/runs/nightly-health-check-v1-2026-05-20T160237Z.md`; finished 16:08:30Z; 0 schema-drift fallbacks; all 14 SQL queries verbatim; dual-write — 5 P1 → `friction.event`, 2 P2 → `friction.emit_error` `CONDITION-KEY-UNRESOLVED`). Brief frontmatter reset to `ready`; queue row closure-annotated. **WARN no longer at core rank 1.** New core rank 1 = **cc-0015 Gate 11 watch** (closes 2026-05-26). **Q-nightly-health-check-v1-005 remains OPEN** (non-blocking function-contract drift; future v3.1 / spec patch). L-v2.94 convention NEW candidate. | `docs/runtime/sessions/2026-05-21-v3.01-cowork-lifecycle-warn-closed.md` |
| 2026-05-21 | v3.00-cc0016-stage-d-evidence-display-visual-pass | **cc-0016 Stage D /operations evidence display VISUAL PASS** (+v3.00.1 reconciliation patch). Dashboard Stage D at `9082beb`. CCB VISUAL PASS: paperclip badge 📎1, Evidence (1) section, signed-URL thumbnail, PNG new tab (64×64), unattached cases normal, triage guardrail intact. **cc-0016 Stage D CLOSED/PASS.** Evidence path COMPLETE A→B→C→D. v3.00.1 corrected stale v2.97/v2.98 placeholder note (reconciled in v2.99.1 at `2db1656`). | `docs/runtime/sessions/2026-05-21-v3.00-cc0016-stage-d-evidence-display-visual-pass.md` |
| 2026-05-20 | v2.99-cc0016-stage-b-fab-upload-validated | **cc-0016 Stage B FAB upload/read UX validated + V-A5 PASS.** Dashboard Stage B at `36fe6ad`. CCB + chat backend + CCD read-back verified. V-A5 empirically PASS. Smoke artefacts retained (events `2120b2f7` + `75f0c981`, object `friction-evidence/9e314151-.../0_va5-smoke.png`). **Stage B CLOSED/PASS.** | `docs/runtime/sessions/2026-05-20-v2.99-cc0016-stage-b-fab-upload-validated.md` |
| 2026-05-20 | v2.98-cc0016-stage-c-applied | cc-0016 Stage C APPLIED/CLOSED. emit_event 13-arg + fn_emit_manual_event 8-arg; cc-0017b preserved. D-01 fires `56e65bb2` / `dbabb576` / `358c6fdd` (final APPROVED). *(Verified to exist in v2.99.1 at `2db1656`.)* | `docs/runtime/sessions/2026-05-20-v2.98-cc0016-stage-c-applied.md` |
| 2026-05-20 | v2.97-cc0016-stage-a-applied | cc-0016 Stage A APPLIED/CLOSED. Bucket + attachments column + 2 CHECK + index + view + GRANT. D-01 fires `6f2b8b1a` / `f573e684` / `9eb35144` (final APPROVED). *(Verified to exist in v2.99.1 at `2db1656`.)* | `docs/runtime/sessions/2026-05-20-v2.97-cc0016-stage-a-applied.md` |
| 2026-05-20 | v2.96-dashboard-slices-4a-4b-recorded | **Dashboard slices 4A–4B recorded.** Slice 4A `cd02402` / slice 4B `f5a980f` VISUAL PASS. Top alert bar count reconciliation CLOSED for UI-copy/linkification scope. | `docs/runtime/sessions/2026-05-20-v2.96-dashboard-slices-4a-4b-recorded.md` |
| 2026-05-20 | v2.95-dashboard-slices-1-3-recorded | **Dashboard slices 1–3 recorded.** Slice 1 `af60953` / slice 2 `de4501b` + `37008e5` / slice 3 `991a92b`. PHASES 46-streak deferral CLOSED by Slice 3. | `docs/runtime/sessions/2026-05-20-v2.95-dashboard-slices-1-3-recorded.md` |
| 2026-05-20 | v2.94-cowork-brief-lifecycle-reset-and-convention | Cowork brief lifecycle gating WARN REFRAMED + ready reset + convention patched. WARN NOT closed. | `docs/runtime/sessions/2026-05-20-v2.94-cowork-brief-lifecycle-reset-and-convention.md` |
| 2026-05-20 | v2.93-cron85-natural-fire-closed-9check-retired | Reconciliation daily cadence diagnostic CLOSED-PASS. D-FR-RECON-001 v1.0 at `fc726e3c`. | `docs/runtime/sessions/2026-05-20-v2.93-cron85-natural-fire-closed-9check-retired.md` |
| 2026-05-19 | v2.92-vc3-signal-production-closed | Health_check V-C3 CLOSED-PASS. Cowork brief lifecycle gating WARN spawned. | `docs/runtime/sessions/2026-05-19-vc3-signal-production-closed.md` |
| 2026-05-16 | v2.76-stage-e-close-and-window-open | cc-0014 FULLY CLOSED + 14-DAY WINDOW + FAB LIVE + cc-0015/0016 DRAFTED. | `docs/runtime/sessions/2026-05-16-v2.76-stage-e-close-and-window-open.md` |

*(Older sessions truncated for brevity. v2.77–v2.91 per prior index.)*

**Pre-2026-05-03 history**: frozen at `docs/runtime/archive/sync_state-pre-2026-05-03.md`.

---

## 🟢 Most recent session — inline summary

### 2026-05-25 Sydney — v3.06: deployed↔repo drift close (IG publisher + 7 migration backfills) + F-YT-FAILED-NO-RETRY brief + plan_review CLEAN + cc-0019 correction

**Outcome:** Source-of-truth reconciliation, **repo-only — 0 production mutation; 2 plan_review D-01 (non-mutating).** A deep audit of the 24 May YT + IG fixes surfaced that deployed `instagram-publisher` was **v2.4.0** while repo `main` was **v2.0.0** (a 4-version drift where a redeploy would silently revert the fix). Actions: (1) pushed the exact deployed v2.4.0 source to repo (`bc78511e`, byte-verified on every anchor — rollback hazard closed); (2) backfilled 7 chat-applied 2026-05-24 migration files to `supabase/migrations/` (`8738c3a0`, recovered verbatim from `schema_migrations.statements`) — DB→repo migration drift closed; (3) authored the F-YT-FAILED-NO-RETRY brief (`bee872aa`), then patched Unit B to the live 3-bucket recovery (`a3f329f3`) and fired its plan_review CLEAN; (4) **corrected cc-0019**: verified from the live DB that `m.is_publish_eligible` + the `fill_pending_slots` gate exist (Unit A IMPLEMENTED, migration `20260524091020`, governed D-01 `a2e5f3f5` + PK approval per header), and from the deployed **ai-worker v2.12.0** source that there is no eligibility preflight (Unit B NOT implemented). v3.05's "brief only" was stale.

**cc-0019 corrected status:** Unit A IMPLEMENTED (live); Unit B (ai-worker preflight) NOT implemented (absent in deployed ai-worker v2.12.0). Unit B is now optional — the fill-gate already stops the primary token-waste vector.

**F-YT-FAILED-NO-RETRY:** brief patched to the live 3-bucket Unit-B recovery (`a3f329f3` — 5 quota/approved recover; 17 invalid_grant/published recover only behind a no-upload guard `youtube_video_id IS NULL` + no `m.post_publish` row; 8 no-refresh-token/never-connected excluded as onboarding debt) + **plan_review CLEAN** (`abf6f63c` partial/type-c → `f89c23cf` agree; the prior partial was resolved by satisfying its corrected_action against live data — `video_status`=`text`/no-CHECK confirmed + the 30 failed drafts live-bucketed — then re-firing, textbook L62 type-c). Execution (ef_deploy Unit A v1.7.0, then sql_destructive Unit B) remains gated on PK approval, A-then-B.

**IG-arc OWED:** substantially discharged — publisher-code leg (`bc78511e`) + migration-file leg (`8738c3a0`) recorded; residual is operational only (cron 53 enabled / cron 64 paused — already documented) + PK's IG-resume decision.

**Hard stops v3.06:** 0 production mutation / 0 apply_migration / 0 EF deploy / 0 cron change / 0 enablement / 0 hard delete / 0 friction.* / 0 decisions.md / 0 memory edit / 0 dashboard edit (PHASES leg N/A — operational). Backfilled migration files are repo-only reconciliation of already-applied state (versions already recorded as applied → no re-execution), so no D-01 for them. The two F-YT plan_review fires are non-mutating reviews, not production mutations.

*(Full detail at `docs/runtime/sessions/2026-05-25-v3.06-ig-drift-close-yt-failed-no-retry-brief.md` — `1219aff0`.)*

---

### 2026-05-24 Sydney — v3.05: YouTube OAuth Production restore (root cause found + both channels publishing)

**Outcome:** YouTube publishing **RESTORED**. The prior (1 May) audit was stale; PK was right it had been fixed in early May then re-broke. **Root cause definitively identified: the Google OAuth app was in "Testing" publishing status, which caps refresh tokens at 7 days.** PK moved the app to Production + re-exchanged tokens; two single-draft live tests published successfully, proving both channels' tokens now refresh. Backlog released (fresh) + soft-retired (stale). Separately: cc-0019 brief authored + reviewed clean (brief only), and a read-only slotting AI-cost-waste audit.

**Root-cause evidence (read-only):**
| Fact | Value |
|---|---|
| Last successful publish | PP 12 May 07:45 UTC (11 total); NY 11 May 09:45 UTC (2 total) |
| Re-break | Day after each last success; `invalid_grant: Token expired or revoked` |
| Token lifetime post-5 May reconnect | **NY 7.00 days, PP 7.92 days** → Testing-mode 7-day cap fingerprint |
| Stored token | Genuine `1//` refresh token (103 chars) in `c.client_channel.config`; no expiry field stored |
| Dashboard "Expires 2031" | Cosmetic — callback hardcodes `now()+5y` ("tokens don't expire"); publisher never reads it |
| OAuth flow | `auth/route.ts` correctly sets `access_type=offline`+`prompt=consent`; `callback/route.ts` stores token + fake expiry |

**Fix + verification (governed):**
| Step | Detail |
|---|---|
| Fix | PK moved OAuth app Testing→Production + re-exchanged (NY token_updated_at 10:48:22, PP 10:49:05 UTC) |
| PP test | mig `yt_token_verify_reset_one_pp_draft_20260524` (draft `2afce74e…`) → invoke req 126325 → **`ma6EG1fz4XQ`** published 10:57:23 UTC |
| NY test | mig `yt_token_verify_reset_one_ny_draft_20260524` (draft `b9860637…`) → invoke req 126339 → **`qp-ZGm8lNIo`** published 11:02:24 UTC |
| Backlog | mig `yt_backlog_release_fresh_softretire_stale_20260524`: release ≥12 May (NY 6 + PP 7 = 13 → generated, draining); retire <12 May (NY 3 + PP 11 = 14 → archived_stale) |
| Untouched | failed-out-of-guard NY 3 + PP 9 (non-uploadable format / non-approved); CFW + Invegent (never connected) |
| D-01 | sql_destructive `82443c8c` (PP+NY tests), `5f7bfc52` (batch) — both partial/type-c, escalated, PK pre-approved |

**Publisher reliability gap (F-YT-FAILED-NO-RETRY):** youtube-publisher v1.6.0 selects only `video_status='generated'`+`approval_status='approved'`; its catch handler sets failures to terminal `video_status='failed'`, which it never re-selects. A token outage therefore silently freezes the entire rendered backlog. Candidate brief: retryable vs terminal failure, or a retry sweep. *(v3.06: brief authored — `docs/briefs/yt-publisher-failed-no-retry.md`, `bee872aa`; Unit-B patched to live 3-bucket recovery `a3f329f3`; plan_review CLEAN `abf6f63c`→`f89c23cf`; execution gated.)*

**cc-0019 brief (publish-eligibility gate, brief only):** `docs/briefs/cc-0019-publish-eligibility-gate.md` — `81cb414` (create) → `01a6cdf` (harden §4 transition handling). Design: `m.is_publish_eligible(client_id, platform)` + fill-gate in `fill_pending_slots` + ai-worker preflight, to stop AI token spend on drafts with no live publish path (measured ~12% waste on disabled NY/PP IG). plan_review `a75c78f6` (partial/type-c) → re-fire `ff05c65e` (agree/clean). **NOT implemented** — execution gated on sql_destructive (Unit A migration) + ef_deploy (Unit B ai-worker) + PK approval. *(v3.06 CORRECTION: Unit A is in fact IMPLEMENTED — migration `20260524091020` applied 24 May; Unit B (ai-worker preflight) NOT implemented per deployed ai-worker v2.12.0. This "brief only" line was stale.)*

**Slotting audit bonus finding:** `m.materialise_slots` references `publish_enabled` only inside the per-platform format-preference lookup — a disabled profile yields NULL format → slot still inserted (defaults `image_quote`). Disablement was wired to format-selection, not skip. Two switches never unified: `client_publish_profile.publish_enabled` (publish) vs `client_publish_schedule.enabled` (materialise). cc-0019 is the unification.

**Hard stops respected v3.05:**
- cc-0019 brief only — 0 gate migration, 0 EF deploy, `fill_pending_slots` unchanged, `is_publish_eligible` not created
- YT mutations: exactly 3 guarded migrations (draft `video_status` only) + 2 publisher invokes. 0 cron change / 0 EF deploy / 0 client enablement change / 0 queue/profile mutation / 0 hard delete
- 0 friction.* write / Q-005 untouched / cc-0015 not started / cc-0016 Stage E not started / CFW+Invegent untouched
- 0 decisions.md change / 0 memory edits / 0 dashboard edit (roadmap/PHASES leg N/A — operational session)

**OWED:** pre-compaction 2026-05-24 IG-publish-restore arc (instagram-publisher v2.4.0 deploy; IG queue soft-purge; void+repurge migrations; cron 53 enable) has not had its own 4-way sync close. Flagged, not folded in (unverified SHAs in-context; detail in transcript `2026-05-24-07-57-08-ice-ig-restore-fresh-restart-cost-gate.txt`). *(v3.06: substantially discharged — publisher `bc78511e` + 7 migration files `8738c3a0`; residual operational only.)*

*(Full detail at `docs/runtime/sessions/2026-05-24-v3.05-youtube-oauth-production-restore.md` — `f9ddd51`.)*

---

### 2026-05-21 Sydney — v3.04: Dashboard Slice 0A (Pre-Phase 0 sidebar IA shell + Visual Tokens v1) MERGED at `3ec489b`

**Outcome:** Dashboard **Slice 0A** merged to `invegent-dashboard` main. This is the Pre-Phase 0 / Gate-11-window reversible IA shell + lightweight visual tokens — the cosmetic work greenlit to use the Gate 11 waiting window, explicitly **not** the locked Dashboard Phase 0. Cross-repo **documentation recording only**; no dashboard edits and no Supabase from this session.

**Merge facts (independently verified):**
| Fact | Value |
|---|---|
| Dashboard main HEAD (new) | `3ec489b6fb1e4ad706aac9d32f7fefa4ad43b9c5` (short `3ec489b`) |
| Parent (prior baseline) | `d17b6047411ce177d6182d86a21a79f7302459af` (`d17b604`) |
| Merge method | squash (linear; squash of `e65b812` initial + `399c087` CCB polish) |
| Committed | 2026-05-21T08:17:18Z |
| Files changed | `components/sidebar.tsx` + `tailwind.config.ts` + `app/globals.css` (exactly 3) |
| Typecheck | `npx tsc --noEmit` → exit 0 |
| Review chain | CCD review PASS / CCB visual QA PASS / CCB polish re-check PASS |
| Verification | Invegent GitHub `list_recent_commits` (SHA + message + parent) **+** live `components/sidebar.tsx` read on main (Slice 0A `NAV`/`NavGroup`/Legacy/`ChevronDown` present; mobile-drawer chrome preserved). Not taken on directive faith. |

**Critical distinction preserved:** Slice 0A is **NOT** real Dashboard Phase 0. The locked Phase 0 (dashboard-review §9) — schema/data groundwork for `m.attention_item`, `m.vw_pipeline_state`, `m.brief`, `m.action_event`, the Inbox/Pipeline/Brief/Action-Layer surfaces — remains **future/gated** behind its own prerequisites (S30 cleared; M5–M8 reconciliation) and is not started by this work.

**Hard stops respected v3.04:** 0 Supabase / 0 Invegent-dashboard edits (recording-only) / 0 Phase 0 schema / 0 cc-0015 start / 0 cc-0016 Stage E / 0 PRV / 0 Q-005 closure / 0 route removals / 0 redirects / 0 Roadmap/PHASES edits / 0 full-file rewrite from stale context (both `00_` files re-read — sync_state blob `348c0a6e`, action_list `5734b407`) / 0 D-01 / 0 memory edits / 0 decisions.md edits.

*(Full detail at `docs/runtime/sessions/2026-05-21-v3.04-dashboard-slice-0a-merged.md`.)*

---

### 2026-05-21 Sydney — v3.03: Q-005 Option A trail (A-005 + v3.1 + v3.1.1 emission-rule guard); brief now v3.1.1; Q-005 OPEN

**Outcome:** Recorded the full Q-nightly-health-check-v1-005 resolution trail. PK ratified **Option A** (explicit per-finding `condition_key`) via `A-nightly-health-check-v1-005` at `56e992b4`. The v3.1 brief patch (`7005865`) added a 9-key `condition_key` mapping. **CCD read-only verification then found v3.1 would FAIL live emission** and would have regressed the only working path. v3.1.1 guard patch (`9ceb78a`) corrects this. **Q-005 remains OPEN** — next-fire watch with a two-way close fork. No Supabase mutations, no dashboard touched, no nightly run, no re-emission, no cc-0015 start, no Stage E.

**Q-005 status: OPEN, non-blocking, next-fire watch.** Close fork: (A) seed `friction.emission_rule` rows + restore full mapping + verify; OR (B) PK formally narrows scope to P1-true-stuck-only + verify (`failure_count=0`/`skipped_count=0`).

*(Full detail at `docs/runtime/sessions/2026-05-21-v3.03-q005-optionA-v3.1.1-emission-guard.md`.)*

---

### 2026-05-21 Sydney — v3.02: Dashboard mobile/narrow viewport verification CLOSED/PASS

**Outcome:** Dashboard mobile/narrow viewport verification PASSED after the dashboard mobile fix (`d17b604`). CCB tested at 306×498 Nexus 5 Android Chrome UA, DPR 2 — MOBILE PASS. The verification carry is CLOSED/PASS. *(v3.02.1 reconciliation restored v3.01 `00_` content clobbered by the original v3.02 push; v3.02.2 backfilled the mobile-fix SHA `d17b604`.)*

---

### 2026-05-21 Sydney — v3.01: Cowork brief lifecycle gating WARN CLOSED (preserved)

**Outcome:** Cowork lifecycle gating WARN CLOSED. The v2.94 lifecycle convention validated end-to-end against the 2026-05-20T16:02:37Z natural fire. `nightly-health-check-v1` brief reset to `ready`. Q-005 remains OPEN non-blocking. New core rank 1 = cc-0015 Gate 11 watch (closes 2026-05-26).

*(Full detail at `docs/runtime/sessions/2026-05-21-v3.01-cowork-lifecycle-warn-closed.md`.)*

---

### 2026-05-21 Sydney — v3.00 close (brief)

cc-0016 Stage D /operations evidence display VISUAL PASS (+v3.00.1 reconciliation patch). Dashboard Stage D at `9082beb`. cc-0016 Stage D CLOSED/PASS; evidence path COMPLETE A→B→C→D.

*(Full detail at `docs/runtime/sessions/2026-05-21-v3.00-cc0016-stage-d-evidence-display-visual-pass.md`.)*

---

## 🟡 Next session priorities (rebuilt v3.05; v3.06 deltas inline)

**YouTube (NEW, from v3.05):**

- **YT backlog drain watch** — 13 `generated` drafts draining at 2/tick via jobid 34 (`:15/:45`). Verify `m.post_publish` youtube rows landing + `generated` counting down; re-promote any transient failures.
- **YT token durability** — spot-check NY/PP tokens still work past ~31 May (confirms the Production change removed the 7-day cap, not just reset the clock).
- **CFW + Invegent YouTube** — never connected (no `client_channel` YT row, no refresh token); CFW failed drafts also `approval_status='published'` not `approved`. Separate first-time onboarding when PK wants those channels live.
- **F-YT-FAILED-NO-RETRY brief — AUTHORED + plan_review CLEAN v3.06** (`docs/briefs/yt-publisher-failed-no-retry.md`, `bee872aa` → Unit-B 3-bucket patch `a3f329f3`; plan_review `abf6f63c` partial/type-c → `f89c23cf` agree); next ef_deploy + sql_destructive (Unit B = 3-bucket recovery: 5 quota recover / 17 token-casualty behind no-upload guard / 8 never-connected excluded) + PK approval, A-then-B.
- **F-YT-EXPIRY-DISPLAY-FAKE cleanup** — candidate (dashboard callback hardcoded 5y expiry masks dead tokens).
- **cc-0019** — **Unit A IMPLEMENTED** (live `is_publish_eligible` + `fill_pending_slots` gate; migration `20260524091020`; D-01 `a2e5f3f5` + PK approval per header; repo-backfilled `8738c3a0`). **Unit B (ai-worker preflight) NOT implemented** (absent, re-verified against ai-worker v2.13.0 (`8204a5c7`)) — optional; the fill-gate already stops the main waste vector. Implement Unit B only if PK wants the defence-in-depth (ef_deploy D-01 + approval). Do NOT re-apply Unit A.

**Core ICE ranks (carried from v3.02–v3.04, unchanged):**

1. **cc-0015 Gate 11 watch** — P2 carry, rank 1. Window closes **2026-05-26**. Passive; when gate clears, cc-0015 friction-pool-view UI (Wave 7) unblocks.
2. **cc-0016 Stage E — scoping/dry-run design ONLY (if PK picks Option A)** — P2, rank 2 conditional. NON-MUTATING design; first destructive run requires separate PK approval + dry-run. Stage A CONSTRAINT 2 binds.
3. **Wave 0f scoping** — P3, rank 3. Opportunistic during Gate 11 window.
4. **Platform Reconciliation View brief authoring** — P2 carry, rank 4 (deferred per D-FR-RECON-001 §7.D).
5. **5-row close-the-loop batch / Pre-sales / `purge_test_case` helper case_history extension** — P2/P3 carry, rank 5.

**Carries flagged (NOT actively ranked):**
- **Q-nightly-health-check-v1-005** — OPEN, non-blocking, next-fire watch. Brief **v3.1.1**: P1 true-stuck emits with `condition_key=true_stuck`; 8 P2/P1 keys PARKED pending a Supabase-approved `emission_rule` seed patch. Close fork (A) seed + restore + verify, OR (B) PK narrows scope + verify.
- **IG-publish-restore arc 4-way close — DISCHARGED v3.06.** Publisher-code leg synced to repo (`bc78511e`) + 7 migration files backfilled (`8738c3a0`); recording debt closed. Residual is operational only: cron 53 enabled / cron 64 paused (documented) + PK's IG-resume decision (re-enable enqueue cron 64, start CFW/Invegent).
- **3 no-fire scheduler days** (2026-05-16, 2026-05-18, 2026-05-19) — P3 informational.
- **Doc-patch debt (v3.07 status):** cc-0017c main brief DONE (v1.2). **Deferred to CC surgical pass:** cc-0017a / cc-0010A / cc-0011 / cc-0012 v-next doc patches + `cc-0017c/vchecks.md` subfile V-B4 12-param fix (insufficient documented scope + full-file re-emit risk via GitHub bridge — no str_replace). **vchecks.md path located** (`docs/briefs/cc-0017c/vchecks.md`); **decisions.md** runtime path still unlocated. **Lesson promotions NOT done** — canonical-home file + numbering convention unresolved; next-lesson-cycle / CC.
- **Asset Policy Stage 0 (OBS):** Foundation APPLIED to isolated OBS project `invegent-obs-stage0` ONLY (migration `obs_stage0_observation_foundation`; D-01 `fd3e519a` closed `completed`; CCD applied after pre-apply isolation check). Production `mbkmaxqhsohbtwsqolns` untouched (no `obs` schema/table). OBS physically isolated. **Read-only diagnostic complete → recommendation C: HOLD 0A** (06 doc, commit `f5c00e04`; residual gaps are source-instrumentation, not observer-read). **0A observer/read-path BLOCKED** — new concrete plan + new D-01 + PK exact-phrase approval required before any further build. No read path / CDC / deploy / runtime / cron / provider / 0B.
- **HeyGen render telemetry — heygen-worker v2.1.0 (NEW):** heygen-worker v2.1.0 render telemetry committed `690a295b` UNDEPLOYED; D-01 `0d9bb255` OPEN/escalated; deploy blocked pending PK exact-phrase approval + CCD pre-deploy checks.

**Dashboard work (unchanged from v3.04):** Slice 0A merged at `3ec489b` (cosmetic, NOT Phase 0, NOT cc-0015). Real Phase 0 (review §9) future/gated; cc-0015 friction-pool-view (D1) gated on Gate 11 closing 2026-05-26. PRV surface (D2) deferred.

**Time-bound nudge:** cc-0015 Gate 11 window closes **2026-05-26**.

**Standing P0:** Personal businesses check-in. Crazy Domains refund + clean-up carry from v2.51.

Carries: cc-0015 (Wave 7, gated on Gate 11 2026-05-26); cc-0016 Stage E (future/separately-approved); **Q-005 (OPEN, next-fire watch; brief v3.1.1)**; **YT backlog drain watch / YT token durability / CFW+Invegent YT onboarding / F-YT-FAILED-NO-RETRY (v3.10: full Unit B COMPLETE — v1.8.0 deploy + B1 5 quota + v1.9.0 deploy + B2 17 token-casualty all applied/verified; B3 9 = onboarding debt) / **✅ F-YT-PUB-PUBLISH-AUDIT-GAP CLOSED v3.11 — v1.10.0 fix (next-available attempt_no, ef_deploy `5bd19069`) + 11-row backfill (sql_destructive `998c90a3` + PK phrase, mig `yt_pub_publish_audit_gap_backfill_20260526`); gap=0** / F-YT-EXPIRY-DISPLAY-FAKE cleanup / cc-0019 Unit B optional (Unit A implemented)**; **cc-0017c v1.2 doc patch DONE v3.07**; **cc-0017a v1.2 doc patch (DEFERRED to CC v3.07)**; **vchecks.md V-B4 doc patch (path located `docs/briefs/cc-0017c/vchecks.md`; DEFERRED to CC v3.07)**; **cc-0010A/0011/0012 minor doc patches (DEFERRED to CC v3.07)**; F-CRON-AUTO-APPROVER-SECRET-INLINE; backend/shared-metrics refactor (deferred); lesson promotions (**NOT done v3.07 — canonical home/numbering unresolved**; L-v2.83-a STRONG 18+; **L-v2.85-e PROMOTION-CONFIRMED**; L-v2.85-a HIGH-SIGNAL 4 occurrences; L62 strongly reinforced; L-v2.88-a watcher; **L-v2.94 convention NEW candidate**; **L-v3.02-a / L-v3.03-a / L-v3.04-a NEW candidates**; **L-v3.05-a NEW candidate — record the root CAUSE not just the symptom**; **L-v3.06-a NEW candidate — verify deployed-vs-repo state before redeploy + backfill chat-applied migrations same-session**; **L41 re-exercised v3.02.1 + reinforced v3.03/v3.04/v3.05/v3.06/v3.07**; L-v2.90-a-f watchers).

---

## ⛔ Carried-forward "do not touch" state

**v3.07 update — doc-patch housekeeping (doc-only):**

- **cc-0017c main brief is now v1.2** (`docs/briefs/cc-0017c-friction-register-lockdown-and-backfill.md`, commits `6d5c3ae6` + `81dcb683`) — APPLIED-WITH-VCHECK-CORRECTION apply-record backfilled. The brief's stale `AUTHORED_PENDING_D01_REFIRE` framing is corrected. No SQL/scope change.
- **DEFERRED to a CC surgical pass (do NOT full-file re-emit in chat):** cc-0017a (72KB), cc-0010A (107KB), cc-0011 (61KB), cc-0012 (49KB) v-next doc patches, and the `cc-0017c/vchecks.md` subfile V-B4 12-param fix. Reason: insufficient documented scope (only cc-0017c had an explicit v-next spec) + the GitHub bridge has no str_replace, so any edit re-emits the whole large file = L41 clobber/drift risk. The confirmed facts (cc-0017a applied v2.81, migration `20260518065610`, D-01 `adcc8385-…`) are ready for CC to apply surgically.
- **vchecks.md path LOCATED:** `docs/briefs/cc-0017c/vchecks.md` (subfile). The correct V-B4 12-param signature is known: `friction.emit_event(source, condition_key, problem_key, source_event_id, observed_at, related_object, observation_text, raw_payload, reported_by, severity_override, category_override, dynamic_context)` → `RETURNS TABLE(event_id uuid, case_id uuid, case_disposition text)` SECURITY DEFINER. Apply via CC.
- **decisions.md** (runtime decisions log) — path still unlocated; parked.
- **Lesson promotions NOT done** — canonical-home file holding the L37–L65 definitions was not located (checked repo root, `docs/` per index, `docs/runtime/`, `12_project_handoff.md`); promotion numbering convention is undocumented. Per PK: left for a next-lesson-cycle / CC pass. **Do NOT promote lessons by assumption** (do not assign L66+ or flip baseline status without the confirmed canonical home + convention). The candidates remain promotion-ready in this ledger (L41+L-v2.85-e pair, L-v2.83-a, L-v2.85-a).

**v3.06 update — source-of-truth reconciliation:**

- **Repo `instagram-publisher` now = deployed v2.4.0** (`bc78511e`). Do NOT redeploy from an older checkout. A redeploy from repo now produces working v2.4.0 (throttle + destination_id gate + container-readiness poll + image publish-retry + RE-B bounded retry).
- **All 7 chat-applied 2026-05-24 migrations are now present in `supabase/migrations/`** (`8738c3a0`, recovered from `schema_migrations.statements`). These are repo records of already-applied state — do NOT re-run them (versions already recorded as applied).
- **cc-0019 — Unit A IMPLEMENTED v3.06 (correction).** Live DB has `m.is_publish_eligible` + the `m.fill_pending_slots` gate (migration `20260524091020`; D-01 `a2e5f3f5` + PK approval per header; repo-backfilled `8738c3a0`). **Unit B (ai-worker preflight) NOT implemented** (absent in deployed ai-worker v2.12.0). v3.05's "not created" was stale. **Do NOT re-apply Unit A (already live).** Unit B optional. *(v3.08 re-verification: Unit B confirmed still absent against the now-live ai-worker v2.13.0 (`8204a5c7`) — the avatar version bump did not add/remove the preflight; the v2.12.0 reference above is the v3.06-era historical note, retained as history.)*
- **F-YT-FAILED-NO-RETRY brief authored + plan_review CLEAN** (`bee872aa` → Unit-B 3-bucket patch `a3f329f3`; plan_review `abf6f63c` partial/type-c → `f89c23cf` agree). Next: ef_deploy (Unit A v1.7.0) + sql_destructive (Unit B 3-bucket recovery) at execution + PK approval (A-then-B). Do NOT treat `failed` YT drafts as auto-recoverable before that lands.

**v3.05 update — YouTube:**

- **YouTube RESTORED v3.05.** OAuth app moved to Production (PK); NY+PP tokens re-exchanged today (`1//`, 103 chars; NY 10:48 / PP 10:49 UTC). Both verified publishing (`ma6EG1fz4XQ`, `qp-ZGm8lNIo`). **Do NOT reconnect tokens as a "fix" again** — if YT breaks at a clean 7-day boundary, the cause is OAuth publishing status (Testing), not the token. Durability past ~31 May to be spot-checked.
- **YT backlog state:** NY 6 + PP 7 = 13 `generated` (draining 2/tick via jobid 34); NY 3 + PP 11 = 14 `archived_stale` (soft-retired, reversible — do NOT hard-delete; preserve audit); NY 3 + PP 9 `failed`-out-of-guard (non-uploadable format / non-approved; untouched). **Do NOT bulk-reset `archived_stale` back to `generated`** without PK direction (they are deliberately retired stale content).
- **youtube-publisher v1.6.0** unchanged. **F-YT-FAILED-NO-RETRY**: its catch handler sets transient auth failure to terminal `failed` (never re-selected). Reliability gap — brief authored + plan_review CLEAN v3.06 (`bee872aa`→`a3f329f3`; `abf6f63c`→`f89c23cf`); do NOT treat `failed` YT drafts as auto-recoverable until v1.7.0 lands.
- **F-YT-EXPIRY-DISPLAY-FAKE**: dashboard `app/api/youtube/callback/route.ts` hardcodes `token_expires_at = now()+5y`; the "Expires …2031" display is fabricated and does NOT reflect Google token validity. Candidate cleanup.
- **CFW + Invegent YouTube** — never connected (no `c.client_channel` YT row, no refresh token). Separate onboarding; not a token reconnect.
- **cc-0019 brief** (`docs/briefs/cc-0019-publish-eligibility-gate.md`, `01a6cdf`) reviewed clean (`ff05c65e`). *(Status superseded by the v3.06 correction above: Unit A IMPLEMENTED; Unit B not.)*
- **OWED:** IG-publish-restore arc 4-way close — **DISCHARGED v3.06** (publisher `bc78511e` + 7 migration backfills `8738c3a0`); residual operational only (cron 53/64 + PK IG-resume decision).

**v3.04 update on dashboard items:**

- **Dashboard Slice 0A MERGED v3.04** — `invegent-dashboard` main now `3ec489b` (parent `d17b604`; squash of `e65b812` + `399c087`). Pre-Phase 0 / Gate-11-window sidebar IA shell + Visual Tokens v1. Files: `components/sidebar.tsx` + `tailwind.config.ts` + `app/globals.css`. Typecheck exit 0; CCD/CCB/CCB-polish PASS. **Slice 0A is NOT real Dashboard Phase 0** — review §9 Phase 0 schema groundwork remains future/gated (S30 + M5–M8 prerequisites). **No data-backed surface shipped; cc-0015 NOT started.**

**v3.02 (+v3.02.1 +v3.02.2) updates on standing items:**

- **Mobile/narrow viewport verification CLOSED/PASS v3.02.** CCB at 306×498 Nexus 5 DPR2. Removed from open P3 carries.
- **Mobile-fix commit SHA CLOSED v3.02.2** — `d17b6047411ce177d6182d86a21a79f7302459af` (short `d17b604`); dashboard files `components/sidebar.tsx` + `app/(dashboard)/operations/case-row.tsx`; typecheck PASS. *(v3.04: `d17b604` is now the parent of the Slice 0A merge `3ec489b`.)*
- **Cowork brief lifecycle gating WARN — CLOSED v3.01.** Brief reset to `ready`. Q-005 carries non-blocking.
- **cc-0016 Stage D CLOSED/PASS v3.00.** Dashboard `9082beb`. Evidence display live on `/operations` (mobile-verified).
- **cc-0016 evidence capture/display path COMPLETE through Stage D** (A→B→C→D).
- **cc-0016 Stage B CLOSED/PASS** (v2.99). Dashboard `36fe6ad`. V-A5 PASS.
- **cc-0016 Stage A CLOSED** (v2.97; verified in v2.99.1 at `2db1656`).
- **cc-0016 Stage C CLOSED** (v2.98; verified in v2.99.1 at `2db1656`).
- **cc-0016 Stage E** — future/separately-approved-only. Stage A CONSTRAINT 2 binds. Do NOT start automatically.
- **V-A5 smoke object** `friction-evidence/9e314151-be65-434e-8588-c913012f6591/0_va5-smoke.png` — DO NOT DELETE; Stage D demo artefact.
- **Smoke events** `2120b2f7` + `75f0c981` — DO NOT cleanup unless separately directed.
- **cc-0015 / PRV** unchanged — preserved open. cc-0015 do NOT start before Gate 11 closes 2026-05-26.
- **Q-nightly-health-check-v1-005 — OPEN, non-blocking, next-fire watch (v3.03).** Brief **v3.1.1** (A-005 `56e992b4` / v3.1 `7005865` / v3.1.1 `9ceb78a`). Emits P1 true-stuck with `condition_key=true_stuck`; PARKS the other 8 P1/P2 keys (markdown-only) pending a Supabase-approved `emission_rule` seed patch. **Do NOT seed emission_rule rows, re-emit, or close Q-005 without explicit PK direction.**
- **Dashboard slices 1–3 + 4A–4B RECORDED** v2.95 + v2.96 (carry).
- **Top alert bar count reconciliation CLOSED for UI-copy scope v2.96** (carry).
- **Dashboard PHASES 46-streak deferral CLOSED v2.95** (carry).
- **"Stop Claude" overlay external/non-app v2.95** (carry).
- **Backend/shared-metrics refactor** — deferred carry; not actively ranked.
- **Reconciliation daily cadence diagnostic CLOSED-PASS v2.93** — carry. **D-FR-RECON-001 v1.0 at `fc726e3c`** — carry.
- **cc-0017a/b/c/d/e APPLIED**, **cc-0014 CLOSED-ARCHIVED** — unchanged. *(v3.07: cc-0017c brief doc bumped to v1.2 to reflect APPLIED state; no schema change.)*
- **Cowork brief `nightly-health-check-v1`** — **now v3.1.1**. Frontmatter `status: ready`. Q-005 carries OPEN non-blocking.
- **cron 53 (instagram-publisher) ENABLED** (pre-compaction IG arc, 2026-05-24); cron 64 paused; NY/PP IG `publish_enabled=false`; CFW+Invegent IG enabled. *(v3.06: IG arc recording debt discharged — publisher `bc78511e` + migrations `8738c3a0`; operational resume = PK's call.)*
- **cron 82-86** firing normally.
- **L41**: **re-exercised v3.02.1**, reinforced v3.02.2/v3.03/v3.04, **reinforced v3.05 + v3.06 + v3.07** (session files / 00_ patches read-HEAD-first with sha passed). Strong negative-then-positive exemplar.
- **L40 / L46 / L58 / L62**: L62 strongly reinforced via cc-0016 series; L40/L46/L58 not exercised. *(v3.05: L62 reinforced again — both YT sql_destructive D-01s `82443c8c`/`5f7bfc52` returned partial/type-c echoes, satisfied + proceeded on PK approval. v3.06: reinforced again — F-YT plan_review `abf6f63c` partial/type-c → satisfied corrected_action against live data → `f89c23cf` clean.)*
- **L-v2.83-a**: **18+ occurrences**. STRONG CANDIDATE confirmed. *(v3.07: still promotion-ready; not promoted — canonical home/numbering unresolved.)*
- **L-v2.85-a HIGH-SIGNAL**: 4 occurrences (unchanged).
- **L-v2.85-e**: PROMOTION-CONFIRMED v2.88. *(Streak-counting muddied by the v3.02 clobber + v3.02.1 repair; re-baseline at next clean close. v3.07: still promotion-ready; not promoted.)*
- **L-v2.88-a**: watcher; 3 cumulative occurrences (v2.88 + v2.91 + v3.02 re-send).
- **L-v2.94 convention**: NEW candidate (v3.01).
- **L-v3.02-a**: NEW candidate (mobile breakpoint verification). 1 occurrence.
- **L-v3.03-a**: NEW candidate (verify downstream acceptance gates). 1 occurrence.
- **L-v3.04-a**: NEW candidate (independently verify cross-repo merge SHA + content). 1 occurrence.
- **L-v3.05-a**: NEW candidate (v3.05) — **record the root CAUSE, not just the symptom**: YouTube was re-diagnosed as "reconnect the token" across ≥3 sessions because the actual cause (OAuth app in Testing → 7-day token cap) was never written into the durable record. 1 occurrence; watcher. Pair with the "audit can be stale — verify against live data when the user pushes back" sub-lesson.
- **L-v3.06-a**: NEW candidate (v3.06) — **deployed-vs-repo drift is a standing risk class**: verify deployed EF/migration state against repo before any redeploy (IG publisher was 4 versions ahead in deploy vs repo; a redeploy would have reverted the fix), and backfill chat-applied migrations to `supabase/migrations/` in the same session (apply_migration writes the DB but commits no file). 1 occurrence; watcher. Pair with L41.
- **L-v3.07-a**: NEW candidate (v3.07) — **the GitHub bridge has no str_replace; any edit to a large doc re-emits the whole file**, so large-brief doc patches (≥~40KB) are an L41 clobber/drift risk in chat and belong in a CC surgical (str_replace) pass — not a chat full-file rewrite. Also: action_list "doc patch" carries were intentions, not specified diffs; only cc-0017c had a documented v-next scope. 1 occurrence; watcher. Pair with L41 + L-v3.06-a.
- **L-v2.90-a-f**: not empirically re-exercised. Watchers.
- **22 close-the-loop UPDATEs baseline** — unchanged.
- **T-MCP-02 quota: ~96 cumulative (v3.05 +4 D-01: 2 plan_review cc-0019 + 2 sql_destructive YT)**, ON TOP of an estimated ~+5 from the unrecorded pre-compaction IG arc → **treat cumulative as needing a clean re-baseline (~101–105 range)**, not a precise figure. **v3.06 added 2 plan_review D-01 (`abf6f63c` partial → `f89c23cf` clean; non-mutating) on the F-YT-FAILED-NO-RETRY brief.** *(v3.07: 0 D-01 fired — doc-only.)* State-capture exceptions: 1 unchanged.
- Standing don't-redeploy three (heygen-avatar-creator, heygen-avatar-poller, draft-notifier).
- **Production FAB + evidence display live on dashboard.invegent.com** (Stage B + Stage D); mobile-verified.
- **Localhost FAB cleanup pending** (`.env.local`).
- **D-CC-0017B-Q1** + **D-IOL-001** (v2.77) carried.

---

## 📜 G1 convention (the rule)

Unchanged. **v3.05 recorded via the surgical discipline: new session file `f9ddd51` created; sync_state + action_list patched with read-HEAD-first `create_or_update_file` calls passing the just-read blob SHAs (sync_state `7d2b0eb8`, action_list `ecaf50f8`) to detect concurrent edits.** **v3.06 recorded the same way: session file `1219aff0` created; sync_state + action_list patched read-HEAD-first with blob SHAs passed (action_list `23972a0a`, sync_state `d8f18cbd`). Post-close, the two `00_` files received a further narrow surgical patch recording the F-YT-FAILED-NO-RETRY plan_review CLEAN outcome (`abf6f63c`→`f89c23cf`) + Unit-B brief patch (`a3f329f3`) + the +2 plan_review D-01 tally — same read-HEAD-first sha-passed discipline.** **v3.07 (doc-patch housekeeping): NO per-session file created and Dashboard PHASES leg N/A — PK explicitly scoped the close to the two `00_` files (doc-only session, no phase change). sync_state + action_list patched read-HEAD-first with blob SHAs passed (sync_state `df1f44ac`; action_list re-read for its own write). This is a deliberate, PK-scoped deviation from the full G1 4-way; a per-session file can be added later if the full ritual is wanted.** **v3.08 (Avatar→YouTube milestone): repo reconciliation of an already-live pipeline; recorded into the two `00_` files read-HEAD-first with blob SHAs passed (sync_state `29dffe9d`; action_list `ba02a6ff`). No per-session file + Dashboard PHASES leg N/A (PK-scoped to the two `00_` files; no new deploy, no phase change). The EF source + continuity docs were reconciled to `main` in commits `521b053b`+`194e0792` (EFs) and `04294d23` (docs) ahead of this `00_` rebuild. Post-milestone narrow patch (2026-05-26): cc-0019 Unit B re-verification recorded — re-read HEAD sha-passed (sync_state `cea33088`); discharged the "re-verify against v2.13.0" carry by confirming Unit B still absent in ai-worker v2.13.0 (`8204a5c7`).** Dashboard roadmap/PHASES leg N/A this cycle (operational / doc-only, no phase change). `decisions.md` not touched.

**Lesson surfaced (v3.05):** an audit committed to memory/docs goes stale — when YT was reported "fixed then broke again," the right move was to re-run the audit against live data rather than trust the 1 May finding. And recording only the *symptom*-level fix (reconnect token) let the same root cause (Testing-mode 7-day expiry) recur silently ≥3 times. Record causes, not just fixes (L-v3.05-a).

**Lesson surfaced (v3.06):** deployed state and the repo can silently diverge — the deployed IG publisher was 4 versions ahead of repo `main`, so a routine redeploy would have reverted the live fix; and chat-applied migrations (`apply_migration`) write the DB but never commit a file, leaving the repo's migration history stale. Verify deployed-vs-repo before redeploying, and backfill chat-applied migrations same-session (L-v3.06-a). Also: when a doc asserts something is "not implemented," verify against the live DB/EF before propagating it — cc-0019 Unit A had in fact been applied. And the F-YT plan_review re-confirmed L62: a partial/type-c verdict that echoes self-disclosed weak evidence is resolved by satisfying it against live data (here: confirming `video_status`=text/no-CHECK + bucketing the 30 failed drafts, then patching Unit B), then re-firing — which returned clean.

**Lesson surfaced (v3.07):** the action_list "doc patch" carries were one-line *intentions*, not specified diffs — only cc-0017c had a documented v-next scope (in its v2.85 apply session), so only it could be patched faithfully; the rest had to be deferred rather than blind-patched. Separately, the GitHub bridge offers no str_replace, so editing any large doc means re-emitting the whole file — an L41 clobber/drift risk that makes large-brief patches (cc-0017a 72KB, cc-0010A 107KB, etc.) genuinely safer as CC surgical edits than chat full-file rewrites (L-v3.07-a). And "promote a lesson" could not be executed because the canonical L37–L65 definition home + numbering convention are not recorded anywhere locatable — promotion by assumption was correctly declined.

**Lesson reinforced (v3.08):** the Avatar→YouTube reconciliation re-exercised the bridge-native squash discipline — SHA-equality between the pushed blob and the source branch blob is the definitive byte-parity proof for a copy-style reconciliation, and a self-caught mis-scoped `files` array (commit 1 shipped 1 of 3 EFs) was flagged openly and corrected rather than passed silently. L41 reinforced again (the two `00_` writes done read-HEAD-first, sha-passed). L-v2.85-e reinforced (the 12-file reconciliation was split into EF / continuity-doc / `00_` commits to keep each push within budget). Post-milestone, cc-0019 Unit B was re-verified against the now-live ai-worker v2.13.0 (`8204a5c7`) — confirmed STILL absent (the avatar A2/A3 changes are orthogonal to the eligibility preflight), discharging the "re-verify against v2.13.0" carry without any EF edit (absence is expected, not a missing implementation, since Unit A's fill-gate already covers the primary waste vector).

---

*Last updated: 2026-06-02 Sydney — v3.27: video-worker caption-hierarchy polish (v3.1.3) DEPLOYED to prod — caption-styling-only P3 for `video_short_kinetic_voice` (font_size 52→40px, weight 700→500, scrim opacity 0.55→0.62; #FFFFFF + CAP_MAX_CHARS 64 unchanged; comments→40px; geometry/timing/chunking/audio/voice/getVoiceId/templates/webhook/queue/DB/stat/non-kinetic UNCHANGED). FF-merge f0c9682..fc3ec12; branch polish/video-worker-caption-hierarchy (f9be78c styling + fc3ec12 VERSION v3.1.2→v3.1.3; index.ts only). CCH code review PASS + CCB visual review PASS. Deploy via ./scripts/safe-deploy.sh video-worker --allow-warn (wrapper only): 6 targeted EF-drift refreshes incl. GitHub raw-CDN propagation polling + final post-deploy refresh → B-FD (repo 3.1.3 > deployed 3.1.2) → check-only BLOCK exit 1 (expected gate) → check-only --allow-warn PASS → deploy exit 0 → A-LE / clean / in-spec; repo==deployed hash 0991ff6554fbf7c8dcba1debe47e179efcf95d9ff315ce9f41b53fd4b4088bd7; health {"version":"video-worker-v3.1.3"} HTTP 200. D-01 CCD-inline (PK ef_deploy phrase recorded) — no m.chatgpt_review fabricated. F-CREATOMATE-CAPTION-HIERARCHY CLOSED; F-VOICE-LOWVOL-BACKFILL OPEN P3 separate carry; polish/video-worker-caption-hierarchy branch cleanup deferred (needs separate approval). 0 schema / 0 migration / 0 cron/queue / 0 publisher/social / 0 image-worker / 0 webhook / 0 render / 0 Creatomate/ElevenLabs call / 0 production draft mutation / 0 backfill. Session docs/runtime/sessions/2026-06-02-v3.27-video-worker-caption-hierarchy-deploy.md. Prior — v3.24: cc-0015 register hygiene COMPLETE + Stage D Phase 1 live in prod (register 48 total / 14 open / 34 resolved / 0 new via `friction.*` SECURITY DEFINER RPCs, Batches 1/2a/2b/3/4/6, `p_actor='pk'`, CCD-inline D-01 + session-doc governance; YT straggler carry `79326fc8` open/track; `f633c279` acknowledged/track; Track-2 `/operations/pools` status strip + indicators merged main `668b6ae` + prod deploy + CCB prod PASS; session `docs/runtime/sessions/2026-06-02-cc-0015-register-hygiene.md`; 0 schema / 0 EF / 0 cron / 0 deploy this lane). Prior — v3.08: Avatar→YouTube Shorts milestone (repo reconciliation of an already-live pipeline; 0 production mutation, 0 new deploy, 0 D-01). HeyGen talking-avatar → YouTube Shorts path RESOLVED end-to-end; deployed EF source reconciled to `main` from branch `feat/ai-worker-v2.13.0-avatar-youtube` (Option B bridge-native squash): ai-worker v2.13.0 (A2 avatar override + A3 narration branch) + heygen-worker v2.0.0 (async two-phase; 720×1280) + youtube-publisher v1.7.0 (`video_short_avatar` allow-listed → F-YT-PUB-AVATAR-EXCLUSION CLOSED). First live unlisted Short `sfQvSM2Osus` (NDIS-Yarns, draft `40f9fa25`, `m.post_publish` `5dbfcc0f`, 720×1280, 7.8 MB, 2026-05-25 11:42:07 UTC; no duplicate render). Landscape proof `ba5b34eb` retired `archived_stale`. F-YT-FAILED-NO-RETRY still OPEN → re-pointed to youtube-publisher v1.8.0. Reconciliation commits `521b053b`+`194e0792` (EF parity), `04294d23` (continuity docs), this `00_` rebuild; all 12 files byte-identical to branch (sha-verified). Live ai-worker now v2.13.0 (avatar milestone; cc-0019 Unit-B preflight re-verified NOT-implemented against v2.13.0 (`8204a5c7`) — remains optional, Unit-A fill-gate covers the primary waste vector). Recorded read-HEAD-first sha-passed (sync_state `29dffe9d`; post-milestone cc-0019 re-verify patch sha-passed `cea33088`). No per-session file + Dashboard PHASES leg N/A (PK-scoped to the two `00_` files). Prior — v3.07: doc-patch housekeeping (doc-only, repo-only; 0 production mutation, 0 D-01). cc-0017c main brief → v1.2 apply-record backfill (stale AUTHORED_PENDING_D01_REFIRE → APPLIED-WITH-VCHECK-CORRECTION v2.85; apply 2026-05-19; migrations `20260519004545`+`20260519005322`; 3 D-01 refs — v1.0 `a37eff28-…`, v1.1 `9e602a2d-…` [tail not captured in source — truncated], apply `d18fa6db-…` AGREE; V-B4 12-param signature pointer) at `6d5c3ae6` + correction `81dcb683` (softened an unverified cc-0017d session-version tag). DEFERRED to CC surgical pass (insufficient documented scope + full-file re-emit risk via GitHub bridge — no str_replace): cc-0017a (72KB) / cc-0010A (107KB) / cc-0011 (61KB) / cc-0012 (49KB) v-next doc patches + `cc-0017c/vchecks.md` subfile V-B4 12-param fix. vchecks.md path LOCATED (`docs/briefs/cc-0017c/vchecks.md`); decisions.md runtime path still unlocated. Lessons NOT promoted (canonical L37–L65 home + numbering convention unresolved — no promotion by assumption; left for next-lesson-cycle/CC). No per-session file + Dashboard PHASES leg N/A (PK scoped to the two `00_` files). L-v3.07-a NEW candidate (no-str_replace → large-doc re-emit risk → CC surgical). L41 reinforced (read-HEAD-first sha-passed). Re-read HEAD sha-passed (sync_state `df1f44ac`). Prior — v3.06: deployed↔repo drift close (repo-only; 0 production mutation; 2 plan_review D-01 non-mutating). instagram-publisher synced to repo v2.0.0→v2.4.0 (`bc78511e`, byte-verified — rollback hazard closed); 7 chat-applied 2026-05-24 migrations backfilled to `supabase/migrations/` (`8738c3a0`, from `schema_migrations.statements`) — DB→repo migration drift closed; F-YT-FAILED-NO-RETRY brief `bee872aa` → Unit-B 3-bucket patch `a3f329f3` → plan_review CLEAN (`abf6f63c` partial/type-c → `f89c23cf` agree); cc-0019 CORRECTED → Unit A IMPLEMENTED (live `is_publish_eligible` + `fill_pending_slots` gate, migration `20260524091020`, D-01 `a2e5f3f5` + PK approval per header), Unit B (ai-worker preflight) NOT implemented (absent in ai-worker v2.12.0; v3.08: re-verified still absent in v2.13.0 `8204a5c7`). IG-arc OWED 4-way close discharged (publisher + migration legs). Session `1219aff0`. Dashboard PHASES leg N/A. Prior — v3.05: YouTube OAuth Production restore. Root cause = OAuth app in Google "Testing" publishing status → refresh tokens capped at 7 days (NY 7.00d / PP 7.92d after 5 May reconnect). PK moved app → Production + re-exchanged tokens (NY token_updated_at 10:48:22, PP 10:49:05 UTC); two live tests published (PP `ma6EG1fz4XQ` 10:57 UTC, NY `qp-ZGm8lNIo` 11:02 UTC) — both channels' tokens refresh, no invalid_grant. Backlog released (fresh ≥12 May → generated: NY 6 + PP 7 = 13, draining 2/tick jobid 34) + soft-retired (stale <12 May → archived_stale: NY 3 + PP 11 = 14). Migrations: yt_token_verify_reset_one_pp_draft / yt_token_verify_reset_one_ny_draft / yt_backlog_release_fresh_softretire_stale (all 20260524). cc-0019 brief authored `81cb414` + hardened `01a6cdf`, plan_review `a75c78f6`→`ff05c65e` clean — brief only, NOT implemented. NEW: F-YT-OAUTH-TESTING-MODE (root cause RESOLVED+recorded), F-YT-FAILED-NO-RETRY (publisher terminal-fail gap, candidate brief), F-YT-EXPIRY-DISPLAY-FAKE (callback hardcoded 5y expiry). 4 D-01 (2 plan_review + 2 sql_destructive). 3 apply_migration + 2 publisher invokes. 0 cron change / 0 EF deploy / 0 enablement / 0 hard delete / 0 friction.* / 0 cc-0015 / 0 Stage E / 0 Q-005 closure / 0 decisions.md / 0 dashboard edit / 0 memory edit. T-MCP-02 cum ~96 (needs re-baseline incl. unrecorded IG arc). State-capture exceptions cum 1. cc-0015 Gate 11 watch preserved rank 1 (closes 2026-05-26); cc-0016 Stage E separately-approved; PRV deferred; Q-005 OPEN non-blocking. OWED: pre-compaction IG-publish-restore arc 4-way close. Session file `f9ddd51`. v3.04 + v3.03 + v3.02 (+.1 +.2) + v3.01 + v3.00 detail preserved above.*
