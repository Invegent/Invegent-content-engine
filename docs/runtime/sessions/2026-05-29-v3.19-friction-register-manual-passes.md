# 2026-05-29 Sydney — v3.19: Friction-register manual operating passes (1 + 2) + FB insights-coverage gap finding

> **Scope:** friction-register operating-loop completion only. **No Stage C build or brief. No dashboard code, `/operations`, or `/operations/pools` change. No migrations, Vercel, Retool, or decisions.md change. No schema/EF/cron change.** All writes were single-case SECURITY DEFINER RPC calls on `friction.case`, applied one-at-a-time under D-01 + PK exact approval, each verified post-call. Supabase project `mbkmaxqhsohbtwsqolns`.

## Outcome

Completed the first operating loop on the friction register: triaged the remaining new/untriaged cases and drove the verified-done work to terminal `resolved`. Register moved **46 total / 45 open / 1 resolved / 3 new → 46 total / 42 open / 4 resolved / 0 new**. Surfaced a genuine, previously-masked Facebook insights-coverage gap (NY-fb + CFW-fb) as the real next engineering lane.

## Structural findings (the loop gaps)

1. **The register had never resolved anything** — 46/46 open at start despite ~2 weeks of capture+triage, including cases whose own notes said `applied_verified_monitoring`. The loop was capture ✓ → triage ✓ → resolve ✗.
2. **The `/operations` UI cannot close a case.** `CaseRow` only calls `friction.fn_triage_case` (triage fields + `case_history` audit; never sets `resolved_at`). Resolution lives in `friction.resolve_case` / `friction.mark_duplicate`, which are **not wired into the dashboard**. So every closure is an RPC outside the UI = a production mutation needing D-01 + PK approval. Flagged as arguably higher-value than the Stage C batch UI.

Lifecycle functions confirmed by reading their bodies: `resolve_case(case_id, resolution_kind, actor)` guards `resolution_kind`↔`action_decision` (`tracked_done`↔`track`, `acted_on`↔`act_now`, `suppressed`↔`suppress`, `ignored`↔`ignore`, `deferred_done`↔`defer_intentionally`, `duplicate`↔`duplicate`), refuses already-resolved, rejects `reopened`; `mark_duplicate` sets duplicate + `resolved_at` in one call; `fn_triage_case` is a COALESCE partial-update (cannot revert a field to NULL).

## Manual pass 1 — 3 writes (D-01 `a94c9f23`)

D-01 `a94c9f23-78fc-4bfa-a5fb-e940bf983163` (sql_destructive) → escalated (type-c echo of self-disclosed weak evidence; only actionable point = independently verify the live-checkable repair, which was done). PK approved exactly 3 writes (held a 4th, `15d57015`).

- `c53f5af6` (F-FB-PUB-PUBLISH-AUDIT-GAP) → `fn_triage_case`: acknowledged / track / qf=true / next_review 2026-06-05 / actor pk. ✅
- `220bc43a` (F-FB-TRANSIENT-500-NO-RETRY) → `fn_triage_case`: acknowledged / track / qf=true / next_review 2026-06-05 / actor pk. ✅
- `a6c6032f` (NY FB jobid-48 starvation) → `resolve_case(...,'tracked_done','pk')`: resolved_at 2026-05-28 21:32:57Z. Independently verified live (NY facebook 12 published rows since 22 May, last 28 May 06:05Z). ✅
- **Held:** `15d57015` (Content Studio series fix) — UI fix, not SQL-verifiable; held for PK UI eyeball.

Close-the-loop: `m.chatgpt_review.a94c9f23` → status=resolved, action_taken=applied_pk_approved, resolved_by recorded (guarded UPDATE matched 1 row, idempotent).

## Manual pass 2 — observer_stale ×3 (D-01 `9432e82a`)

Read-only evidence pass first (publish vs insights per client×platform), then D-01 `9432e82a-10e9-491d-b07a-d19024167689` (sql_destructive) → apply_corrected / escalate=false. 4 RPC calls (one triage per case + a resolve on the residual), one-at-a-time.

- `d5efc053` (NY × YouTube) → **suppress**. Diagnosis: publishing restored post v3.05/v3.16 (14 publishes after window, last 05-28) but **no YouTube insights pipeline exists** (insights-worker is FB/IG only) → structural false-positive; matches already-suppressed siblings `38df8074`/`98f913f2`. Not marked duplicate (per PK). category left `client_commitment`.
- `26436794` (NY × Facebook) → **track**, recat **pipeline_integrity**, qf=true, next_review 2026-06-05. Diagnosis: **GENUINE GAP** — publishing healthy (10 after window, last 05-28) but `m.post_performance` insights **stopped 2026-05-23** (0 rows after window); insights-worker v14.1.0 did **not** restore NY-fb coverage.
- `fcb5612e` (Invegent × Facebook) → **track**, recat **pipeline_integrity**, qf=true, next_review 2026-06-05, then **resolve `tracked_done`** (resolved_at 2026-05-28 22:31:54Z). Diagnosis: **residual** — Invegent published 10× inside the flagged window (observer false-positive) and insights resumed under v14.1.0 (23 perf rows after window, last 05-28).

### Call-1 positional-argument error + correction (audited, no direct DML)

The first `d5efc053` suppress call was malformed — 10 positional args instead of 11: `'pk'` landed in `p_notes` and `p_actor` defaulted to `current_user` = `postgres`. Result: functional disposition correct (suppress + correct suppression_reason) but `notes='pk'` and `triaged_by='postgres'`. **Stopped immediately on the verify mismatch** (per the directive's stop condition). PK chose Option 2 (RPC-clean, no direct DML). Re-issued a corrected **named-argument** `fn_triage_case` call: notes overwritten with a correction note, actor pk recorded in a new `case_history` row. **`triaged_by=postgres` remains a known attribution artefact** — `fn_triage_case` cannot reset it (only sets `triaged_by` on transition out of `new` when NULL; state was already `acknowledged`/non-NULL), and direct DML was explicitly forbidden. All 5 verify checks passed (suppress retained; reason intact; notes = correction note not `pk`; triaged_by=postgres recorded as artefact; exactly one additional pk-actor history row → hist_total 2 / hist_pk 1 / latest actor pk).

Close-the-loop: `m.chatgpt_review.9432e82a` → resolved / applied_pk_approved, with the d5efc053 attribution caveat in `resolved_by` (matched 1 row).

## Held PP/CFW FB-observer closures — read-only verification + 2 closures (D-01 `4750ae49`)

Read-only check (the same test that exposed NY-fb) found the v14.1.0 repair is **client-incomplete**:

| client × fb | publish (after 05-24 / last) | insights `m.post_performance` (after 05-24 / total / last) | verdict |
|---|---|---|---|
| Property Pulse | 1 / 05-25 (224 total) | 143 / 223 total / 05-28 03:02 | flowing → close-eligible |
| Care For Welfare | 3 / 05-28 (14 total) | **0 / 0 total / never** | genuine gap → do NOT close |

D-01 `4750ae49-9e10-486e-9daa-a2be2eb34fdb` (sql_destructive) → escalated (verified the facts; corrected action "one at a time, verify each" was already the plan — type-c). PK approved the 2 PP closures.

- `ac6c52ed` (PP × fb observer) → `resolve_case(...,'tracked_done','pk')`: resolved_at 2026-05-28 23:06:35Z. ✅
- `7010f5b7` (PP × fb observer) → `resolve_case(...,'tracked_done','pk')`: resolved_at 2026-05-28 23:06:49Z. ✅
- **Deliberately NOT closed:** `877b7382` + `78afc70e` (CFW × fb) — CFW has **0 `m.post_performance` rows ever** despite healthy publishing; the recorded `applied_verified_monitoring` note is **contradicted by live data**. Left `track`. `26436794` (NY-fb) not mutated.

Close-the-loop: `m.chatgpt_review.4750ae49` → resolved / applied_pk_approved, records PP corroboration + why CFW/NY stay open (matched 1 row).

## The bigger finding — Facebook insights coverage is client-incomplete (next engineering lane)

insights-worker v14.1.0 confirmed flowing for **Property Pulse** (223 rows) and **Invegent** (23 rows), but **NOT** for **NDIS-Yarns** (stopped 05-23) or **Care For Welfare** (zero ever). Two of four clients have a live FB insights gap. Open cases forming the cluster: **`26436794`** (NY-fb), **`877b7382`** + **`78afc70e`** (CFW-fb). Likely root cause: a single insights-worker per-client coverage defect for NY-fb + CFW-fb. **No mutation for that lane yet** — flagged for a future engineering decision (an insights-worker per-client coverage diagnosis/fix), not a register closure.

## Final register state

- **Resolved (4):** `a6c6032f` (tracked_done), `fcb5612e` (tracked_done), `ac6c52ed` (tracked_done), `7010f5b7` (tracked_done).
- **Suppressed (1, this pass):** `d5efc053` (structural YT-no-insights false-positive).
- **Tracked genuine open gaps (3):** `26436794`, `877b7382`, `78afc70e` (the FB insights cluster).
- **Held (1):** `15d57015` (Content Studio series fix — PK UI eyeball).
- Counts: **total 46 / open 42 / resolved 4 / new 0.**

## Governance / D-01

3 sql_destructive D-01 fired + closed-loop this arc: `a94c9f23` (pass 1), `9432e82a` (observer pass 2, incl. d5efc053 caveat), `4750ae49` (2 PP-fb closures). Each `m.chatgpt_review` close-the-loop UPDATE matched exactly 1 row (idempotent). Every friction mutation was single-case, one-at-a-time, verified post-call, under PK exact approval.

## Hard stops honoured

No Stage C build/brief; no dashboard/`/operations`/`/operations/pools` change; no direct DML on `friction.case` (all via SECURITY DEFINER RPCs); no migrations/Vercel/Retool/decisions.md/memory/schema/EF/cron change. The 4 held PP/CFW cases were touched only as far as PK approved (2 PP resolved; 2 CFW left open). Smoke artefacts untouched.

## Lessons

- **L-v3.19-a (NEW candidate):** a recorded `applied_verified_monitoring` repair note is not proof — verify the repair against live downstream data before closing. The CFW-fb (and NY-fb) notes claimed insights repaired by v14.1.0; live `m.post_performance` showed zero/stopped. Caught two false closures.
- **L62 reinforced:** all 3 D-01s returned partial/type-c or escalate-with-no-specific-objection; corrected actions were either already the plan or satisfiable against live data; proceeded on PK approval.
- **Positional-arg discipline (NEW candidate):** prefer **named-argument** notation for multi-arg SECURITY DEFINER RPC calls (esp. when an optional middle arg like `suppression_reason` is populated) — the call-1 off-by-one put `pk` in `notes` and defaulted the actor to `postgres`. Some artefacts (`triaged_by`) are not RPC-reversible without direct DML.
- **L41 / L-v3.07-a:** the two `00_` index files are now ~120 KB each; full chat re-emit is the clobber hazard — per-session detail recorded here in this new session file (safe), index pointers handled with read-back verification.

*(Owed: dashboard roadmap PHASES leg N/A — operational/register hygiene; explicitly excluded this pass. `15d57015` remains held.)*
