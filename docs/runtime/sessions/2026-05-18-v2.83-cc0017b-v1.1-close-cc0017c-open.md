# 2026-05-18 — v2.83: cc-0017b v1.1 doc-patch close + L-v2.81-a re-exercised + cc-0017c authoring open

**Session type:** Documentation close + brief authoring kickoff
**Version bump:** v2.82 → v2.83
**D-01 fires:** 0
**Production mutations:** 0 (doc-only)
**Commits this session — close:** 3-file atomic push_files (`docs/00_sync_state.md` + `docs/00_action_list.md` + this file).
**Commits this session — post-close:** cc-0017c brief multi-file commit (separate, after v2.83 close lands).
**T-MCP-02 cum:** 73 unchanged.
**State-capture exceptions:** 1 cumulative (unchanged).

---

## Sequence

1. **Session open** — read `docs/00_sync_state.md` v2.82 + `docs/00_action_list.md` v2.82. v2.82 state confirmed: cc-0017b Wave 0b APPLIED-WITH-CORRECTIVE-MIGRATION + CLOSED; 6 brief defects + 2 placeholder rollback bodies flagged for v1.1 doc-only patch (deferred to separate commit at v2.82). Wave 0c authoring gate OPEN; no work started v2.82 per PK directive.
2. **PK directive received** — perform 4-way sync close to v2.83 documenting the cc-0017b v1.1 doc-only patch already landed on main across commits `65047388`–`7f40e554`. Then begin cc-0017c authoring only (no apply, no D-01 unless explicitly authorised).
3. **Verification of v1.1 patch landing on main** — confirmed via `get_file_contents`:
   - Repo HEAD on `main` at `7f40e554c0249591473a17bf7371779c3cb95ab2` matches the upper bound of PK's stated commit range (`65047388`–`7f40e554`).
   - `docs/briefs/cc-0017b-friction-register-unified-emit-event.md` index file version marker now reads `v1.1 (doc-only patch — 6 defects + 2 rollback bodies inlined)` and status reads `CLOSED-APPLIED-WITH-CORRECTIVE-MIGRATION 2026-05-18 (v2.82) — doc-only v1.1 patch 2026-05-18`.
   - `docs/briefs/cc-0017b/hardstop-rollback.md` §5.5.5c and §5.5.5d now contain verbatim cc-0014 function bodies retrieved from `supabase_migrations.schema_migrations` (rows `cc_0014_c_health_check_emitter` version `20260514233321` and `cc_0014_d_manual_emit_function` version `20260515005315`). Placeholders `<INSERT_CC0014_BODY_FROM_PROD>` no longer present.
   - v1.1 patch note at top of §5.5.5 explains the inlining and locks source-of-truth precedence: (1) brief; (2) `supabase_migrations.schema_migrations` row for the named cc-0014 migration; (3) `pg_get_functiondef` (only valid PRE-cc-0017b apply — after apply, `pg_get_functiondef` returns the cc-0017b body, not the cc-0014 body).
4. **L-v2.81-a re-exercise observed** — chat-side Claude this session did NOT author commits `65047388`–`7f40e554`. Origin: parallel agent (Claude Code session, PK direct edits via local tooling, or parallel chat-side session — unknown which without commit-author inspection, and PK has not requested inspection). Per PK directive this session: L-v2.81-a re-exercised → second occurrence → promotion-eligible at next lesson cycle.
5. **4-way sync close v2.83** — 3-file atomic push_files (sync_state + action_list + this file). Dashboard PHASES **36th** consecutive deferral. No new decisions added to `docs/06_decisions.md` this session.
6. **cc-0017c authoring open (post-close, separate commit)** — PK's explicit 4-item scope: (1) FK hardening on `friction.event.source`; (2) direct-write lockdown REVOKE; (3) `resolved_at`/`resolution_kind` backfill on existing closed cases; (4) pre-flight grant capture for exact rollback. Explicit constraints: **no apply this session; no D-01 fire unless explicitly authorised**. Brief authored to multi-file structure following cc-0017a/cc-0017b precedent.

---

## What the v1.1 doc patch resolved on main

The patch covers all 6 defects flagged in the v2.82 per-session file plus the 2 rollback body placeholders. Per the v1.1 patch note in the brief index, this is doc-only (no production behaviour change — production was already corrected via `cc_0017b_emit_event_ambiguity_fix` migration v2.82 for defect 1; defects 2–6 are V-check / pre-flight wording fixes that document what the apply session actually did).

### The 6 brief defects (resolved on main)

| # | Defect | Sub-file | What changed |
|---|---|---|---|
| 1 | emit_event Step 9 unqualified WHERE | `migration-sql-part-a.md` | Schema-qualified to `WHERE friction.event.event_id = v_event_id` to resolve SQLSTATE 42702 ambiguity with `RETURNS TABLE (event_id ...)` implicit OUT param. Production already corrected v2.82 via the corrective migration. |
| 2 | P16 SQL strict NULL handling | `preflight-pset.md` | P16 check rewritten to handle NULL from unset GUC as the "unset/inactive" state — matches the natural-language criterion the v1.0 brief stated. v1.0 hard-failed on NULL because of literal `= ''` compare. |
| 3 | V-B12/V-B13/V-B14 data-modifying CTE concurrency | `vchecks.md` | Three V-checks converted from `WITH ... AS (INSERT ... RETURNING ...) SELECT ...` pattern (which uses snapshot isolation and sees stale state) to sequential DO-block pattern (commits between INSERT and SELECT). Apply session performed this conversion in-session per PK mid-session directive; v1.1 now codifies the corrected pattern. |
| 4 | V-B15 expected cat_source typo | `vchecks.md` | Expected `cat_source = 'emitter_default'` (not `'manual_at_capture'`) for the no-category-override path. v1.0 had the wrong expected value; the manual_at_capture value only applies when caller passes `p_category_override` (which V-B15 does not). |
| 5 | V-B22 INSERT missing 3 NOT NULL FK columns | `vchecks.md` | Synthetic `r.cadence_drift_log` INSERT now includes all NOT NULL FK columns (`reconciliation_run_id` referencing a real prior succeeded reconciliation_run row, plus the other 2 FKs). Apply session used a real production reconciliation_run row (`dc4b1cca-...`) which was cleaned up in V-B27. |
| 6 | V-B27 cleanup orphan patterns | `vchecks.md` | V-B27 cleanup pattern set extended to cover the 6 orphan types discovered during apply: (a) manual-wrapper-generated `manual/<uuid>` source_event_ids, (b) problem_keys with hyphens normalised to underscores by the manual wrapper, (c) V-B23 problem_keys lacking the `cc-0017b-test` prefix. Apply session used explicit problem_key array DELETE; v1.1 codifies the array pattern. |

### The 2 rollback body inlinings (resolved on main)

| § | Function | Source row | What changed |
|---|---|---|---|
| 5.5.5c | `friction.fn_emit_health_check_findings(text, text, jsonb) RETURNS jsonb` | `supabase_migrations.schema_migrations` row `cc_0014_c_health_check_emitter` version `20260514233321` | Verbatim cc-0014 body inlined. v1.0 carried `<INSERT_CC0014_BODY_FROM_PROD>` schematic placeholder because the apply session had not yet captured them. Brief is now self-contained for rollback purposes. |
| 5.5.5d | `friction.fn_emit_manual_event(text, text, text, text, jsonb, text) RETURNS uuid` | `supabase_migrations.schema_migrations` row `cc_0014_d_manual_emit_function` version `20260515005315` | Verbatim cc-0014 body inlined. Same placeholder-to-body conversion as 5.5.5c. |

### Source-of-truth precedence (locked at top of §5.5.5)

In any future rollback scenario, precedence is:
1. This brief.
2. `supabase_migrations.schema_migrations` row for the named cc-0014 migration.
3. `pg_get_functiondef` — **only valid PRE-cc-0017b apply.** Post-apply, `pg_get_functiondef` returns the cc-0017b body, not the cc-0014 body.

No additional capture step is required at apply time. The brief now stands alone.

---

## L-v2.81-a re-exercise (occurrence 2 of N)

**Original lesson framing (v2.81):** "Parallel-session apply coordination — when production-mutating work happens in a parallel agent session, the chat session conducting D-01 must verify production state empirically before treating the work as complete. Rely on `m.chatgpt_review.action_taken` + post-apply V-check pattern when applicable; rely on `get_file_contents` HEAD + spot-check when the parallel work is doc-only."

**v2.81 occurrence 1:** cc-0017a Wave 0a migration `cc_0017a_friction_foundational_schema` (version `20260518065610`) applied by a parallel Claude session under Path B clearance from D-01 `adcc8385-...`. Chat session conducting D-01 was NOT this v2.81 session — chat-side verified production state empirically via 20 V-checks before closing. PK confirmed parallel apply; no security incident.

**v2.83 occurrence 2 (this session):** cc-0017b v1.1 doc-only patches landed on main across commits `65047388`–`7f40e554`. Chat-side Claude this session did NOT author the patches. Verification via `get_file_contents` on HEAD `7f40e554` confirms:
- 8 sub-files present in `docs/briefs/cc-0017b/`.
- Index file `docs/briefs/cc-0017b-friction-register-unified-emit-event.md` version updated to v1.1.
- `hardstop-rollback.md` §5.5.5c + §5.5.5d carry verbatim cc-0014 bodies (placeholder strings removed).

**Pattern continuity:** Both occurrences involve a parallel agent advancing the work between chat sessions. v2.81 was higher-risk (production schema apply); v2.83 is lower-risk (docs-only). The coordination signal is the same: chat-side must verify the parallel work landed correctly before documenting it as complete, and must NOT presume in either direction that the work was or wasn't done.

**Verification cost asymmetry:** for production apply, V-checks must be re-run empirically (cannot trust commit history alone for DB state). For doc-only commits, `get_file_contents` on HEAD + spot-check of the changed files is sufficient empirical verification.

**Promotion eligibility per PK directive this session:** L-v2.81-a now at 2 occurrences. **Eligible for baseline promotion at next lesson cycle** alongside L-v2.78-a (also at 2 occurrences) and ahead of L47-candidate (still at 1).

---

## cc-0017c brief authoring — opened this session (post-close)

PK's explicit 4-item scope:

1. **FK hardening on `friction.event.source`** — DROP CONSTRAINT `event_source_check` (CHECK enum); ADD CONSTRAINT `event_source_fk` FOREIGN KEY (source) REFERENCES `friction.source(source_code)`. Pre-flight validates all existing event.source values exist in friction.source.source_code; transitions a soft enum into a hard FK with referential integrity at the DB level.
2. **Direct-write lockdown REVOKE** — Amendment F enforcement. `REVOKE INSERT, UPDATE ON friction.event FROM PUBLIC, authenticated`; same for `friction.case`. Only the `friction.emit_event` SECURITY DEFINER function (and admin/service_role) can write to the spine. Closes the convention-only loophole exposed by the BYPASS-DEFENCE log line emitted by `fn_promote_event_to_case` since v2.82.
3. **`resolved_at` / `resolution_kind` backfill on existing closed cases** — Amendment G completeness backfill. For all existing cases with `action_decision IN ('suppress','ignore','duplicate','done')` AND `resolved_at IS NULL`, set `resolved_at = COALESCE(updated_at, now())` and `resolution_kind` per a documented mapping. Closes a known data gap in the case table inherited from cc-0014.
4. **Pre-flight grant capture for exact rollback (NEW)** — P-set captures the full `information_schema.role_table_grants` rows for `friction.event` and `friction.case` BEFORE the REVOKE runs, returning them as JSON for inclusion in the D-01 fire payload (`current_evidence`). Rollback path uses captured grants to restore exactly — not from memory or from the brief's idealised grant matrix. Addresses the v2.82 lesson that idealised grants in briefs can drift from production.

Explicit constraints carried from PK directive:
- **No apply this session** — brief authoring only.
- **No D-01 fire** unless PK explicitly authorises.

Brief structure: multi-file mirroring cc-0017a / cc-0017b precedent. Sub-files in `docs/briefs/cc-0017c/`. The cc-0017b apply experience (corrective migration mid-V-checks; data-modifying CTE concurrency; FK satisfier rows; orphan cleanup patterns) feeds directly into cc-0017c authoring quality.

**Out of scope for cc-0017c per PK explicit directive (deferred to cc-0017d or later Wave 0c sub-brief):**
- `ALTER TABLE friction.case ALTER COLUMN dedupe_fingerprint SET NOT NULL` (listed in cc-0017b "Out of scope (Wave 0c)" — still deferred).
- `emission_rule_history` audit trigger (listed in cc-0017b "Out of scope (Wave 0c)" — still deferred).

These will be picked up in a follow-on brief once cc-0017c is APPLIED and the dust has settled on empirical friction.event volume.

---

## v2.83 honest limitations

- 3-file atomic push_files this commit (sync_state + action_list + this per-session file). No `docs/06_decisions.md` change this session (no new decision).
- Dashboard PHASES **36th** consecutive deferral. Discipline call increasingly overdue — but not addressed this session per PK directive scope (close + cc-0017c authoring only).
- T-MCP-02 cum **73** unchanged (no D-01 fires this session).
- State-capture exceptions: 1 cumulative (unchanged).
- Memory cap 19/30 (unchanged).
- Production mutations v2.83: **0**. No cron mutations, no EF deploys, no vault writes, no memory edits, no DB rows touched (read-only `get_file_contents` for verification only; any read-only `execute_sql` queries during cc-0017c authoring are fact-finding, not mutations).
- L-v2.81-a re-exercised (1 → 2 occurrences); per PK direction should be considered for lesson promotion at next lesson cycle.
- L58 applied for this close (3-file atomic push_files); cc-0017c brief commit happens separately and is multi-file.
- 25 outstanding close-the-loop UPDATEs (22 historical CCH-locked + 3 v2.77 new) — no additions or removals v2.83.
- cc-0017b apply session corrective migration `cc_0017b_emit_event_ambiguity_fix` was used to fix the WHERE-clause ambiguity in production. v1.1 patches the brief to match production. Brief and production are now back in sync, and the brief is self-contained (no `<INSERT_CC0014_BODY_FROM_PROD>` placeholders remain).
- This session does NOT fire D-01 for cc-0017c. The brief is authored and committed; D-01 fire awaits explicit PK authorisation.

---

## Next session priorities (v2.83 rebuild)

**If this session continues with cc-0017c brief commit and PK then authorises D-01:**

1. **cc-0017c D-01 plan_review fire** — P1 rank 1 (NEW v2.83 post-authoring). Fires `ask_chatgpt_review` with: `decision_under_review=apply cc-0017c Wave 0c migration`; `production_action_if_approved=REVOKE direct writes + FK swap + resolution_kind backfill`; `current_evidence=pre-flight P-set output incl. captured grants JSON + current resolved_at/resolution_kind row counts + FK validity probe results`; `known_weak_evidence=24h-only operational soak on cc-0017b's emit_event layer`; `default_action=do not apply, ask PK`.
2. **cc-0017c apply** — P1 rank 2 (NEW v2.83). Gated on D-01 verdict + PK explicit approval. Pre-flight P-set rerun + apply_migration + V-checks + close-the-loop UPDATE on `m.chatgpt_review`.

**If session ends after v2.83 close + cc-0017c authoring without D-01 fire (most likely per PK directive):**

1. **cc-0017c D-01 fire** — P1 rank 1 (NEW v2.83). Pending PK explicit authorisation.
2. **Reconciliation daily cadence diagnostic** — P1 rank 2 carry from v2.82 rank 2. Question: did cc-0017b wrappers route correctly through `emit_event` for the next cron 85 fire? Re-run post-fire (≈2026-05-19 03:30 AEST ≈ 17:30 UTC).
3. **Health_check V-C3 + signal-production diagnostic** — P1 rank 3 carry from v2.82 rank 3.
4. **Music library activation** — P2 rank 4 carry from v2.82.
5. **Personal businesses check-in** — standing P0.

**Carries (lower priority, unchanged from v2.82 except where noted):**
- **cc-0017c apply** — gated on D-01 (NEW v2.83).
- 25 close-the-loop UPDATEs (22 historical CCH-locked + 3 v2.77 new).
- Dashboard PHASES sync — **36th** consecutive deferral (was 35th v2.82).
- cc-0017b v1.1 doc patch — **CLOSED-APPLIED-ON-MAIN v2.83** ✓ (was P3 carry v2.82). Removed from Active.
- cc-0017a v1.2 doc patch (combined defects + L60 + L63–L65 + L-v2.76 a–f framing + L-v2.78-a + L47 + L-v2.81-a if promoted) — scope expanded slightly v2.83 (L-v2.81-a now at 2 occurrences, eligible).
- v1.1 cc-0012 / v1.6 cc-0010A / v1.3 cc-0011 minor doc patches.
- F-CRON-AUTO-APPROVER-SECRET-INLINE (P2 sec, OPEN).
- Memory cap hygiene (19/30; 11 free slots).
- Localhost FAB cleanup.
- IG cron 53 re-enable.
- YT publisher diagnostic.
- Platform Reconciliation View brief authoring.
- M8b separate brief authoring.
- **L-v2.78-a baseline promotion eligibility** — 2 occurrences (unchanged).
- **L47 candidate** — 1 occurrence (unchanged).
- **L-v2.81-a candidate** — **2 occurrences v2.83 (was 1 v2.82). PROMOTION ELIGIBLE at next lesson cycle per PK directive this session.**

---

## Lesson outcomes v2.83 summary

- **L-v2.81-a re-exercised** — occurrence 2. Doc-only parallel-session coordination. Per PK directive this session: should be considered for lesson promotion cycle. Eligible at next lesson cycle.
- **L58 applied** — 3-file atomic push_files this close (sync_state + action_list + this file). Baseline correctly applied.
- **L41 not exercised** — no SQL execution this session (verification was via `get_file_contents`, not `execute_sql`).
- **L62 not exercised** — no D-01 fire this session.
- **L-v2.78-a not re-exercised** — no reviewer convergence event.
- **L47 not re-exercised** — no push-failure-with-dropped-ack event.
- **L-v2.76-a through f not re-exercised** — promotion still pending.
- All other L52–L65 and earlier baselines: not exercised this session.

---

*Last updated: 2026-05-18 Sydney late evening (post-cc-0017b apply session) — v2.83 close: cc-0017b v1.1 doc-only patch acknowledged on main (commits `65047388`–`7f40e554`); 6 defects + 2 rollback bodies resolved; L-v2.81-a re-exercised (occurrence 2, promotion-eligible); cc-0017c authoring open per PK 4-item scope (no apply, no D-01 unless authorised); T-MCP-02 cum 73 unchanged; production mutations 0; Dashboard PHASES 36th consecutive deferral; 3-file atomic push_files close + cc-0017c brief commit separate.*
