# v3.07 — Doc-patch debt: cc-0017c v1.2 PATCHED + CORRECTED; large briefs + lessons DEFERRED

**Session date:** 2026-05-25 Sydney (laptop-free; chat + GitHub MCP only)
**Predecessor:** v3.06 (deployed↔repo drift close)
**Mode:** repo doc-only — **0 production mutation, 0 Supabase, 0 EF deploy, 0 cron, 0 dashboard, 0 D-01, 0 memory edit**
**Trigger:** PK directive — clear the doc-patch + lesson-promotion debt, laptop-free, zero production mutation, small commits. Sequence: doc patches → lessons → record close in the two `00_` files.

---

## Outcome

Only **one** debt item was genuinely shovel-ready in chat: **cc-0017c main brief → v1.2**. Everything else in the "doc-patch + lesson-promotion debt" turned out under-specified or mechanically unsafe to do in chat, and was deferred with explicit notes rather than blind-patched (per PK directive: "do not blind-patch anything; commit only confirmed facts; leave an explicit not-patched note for anything uncertain").

### ✅ cc-0017c main brief — PATCHED to v1.2 (+ correction)

`docs/briefs/cc-0017c-friction-register-lockdown-and-backfill.md` (5.8 KB — small enough to full-rewrite safely from a fresh read).

The brief was **stale**: it read `AUTHORED_PENDING_D01_REFIRE` / "Apply: Still GATED" when cc-0017c was in fact **APPLIED at v2.85 (2026-05-19)**. v1.2 backfills the confirmed apply record (source-of-truth: `docs/runtime/sessions/2026-05-19-v2.85-cc0017c-applied-with-vcheck-correction.md`):

- Status → **APPLIED-WITH-VCHECK-CORRECTION**; apply date 2026-05-19; migrations `20260519004545` (main) + `20260519005322` (V-B4 smoke cleanup).
- **3 D-01 refs:** v1.0 `a37eff28-2ba1-4a7a-8fbd-3e9aba738c79` (partial), v1.1 `9e602a2d-…` (partial/type-c — **full UUID not captured in source; recorded truncated, tail NOT invented**), fresh apply `d18fa6db-3a5c-42ff-8aef-55fcb4fb4f92` (AGREE; close-the-loop resolved 2026-05-19 00:54:22 UTC).
- **V-B4 12-param signature pointer** added: deployed `friction.emit_event` is 12-param (source, condition_key, problem_key, source_event_id, observed_at, related_object, observation_text, raw_payload, reported_by, severity_override, category_override, dynamic_context) RETURNS TABLE(event_id, case_id, case_disposition) SECURITY DEFINER. The actual SQL fix lives in sub-file `cc-0017c/vchecks.md` (still stale 7-param) — flagged as a separate outstanding patch, NOT applied here.

Commits: **`6d5c3ae6`** (v1.2 patch) → **`81dcb683`** (no-fabrication correction: softened an unverified `cc-0017d APPLIED v2.90` tag in the successor chain — only "APPLIED" was confirmed this session, not that specific version; kept cc-0017e v2.90/v2.91 which the action_list supports). Final content sha `ab0f4442`.

### ⛔ Deferred to a CC surgical pass — insufficient scope + full-file re-emit risk

- **cc-0017a v1.2** — facts confirmed (APPLIED v2.81; migration `20260518065610`; D-01 `adcc8385-b9be-4573-8d64-b40510202940` resolved partial/type-c Path B; 20/20 V-checks PASS) BUT (a) **no documented v1.2 scope** (unlike cc-0017c, which had an explicit scope statement); inferring it would be blind-patching, and (b) brief is **72 KB** — the only chat write path (`create_or_update_file`) re-emits the entire file = L41 full-file-clobber risk on a doc containing migration SQL. → CC surgical str_replace.
- **cc-0010A (107 KB) / cc-0011 (61 KB) / cc-0012 (49 KB)** — action_list describes them only as "minor doc patches" with **zero specifics** (no documented scope) + same large full-file re-emit risk. → CC.

### 📍 Path-hunt resolved (one) / still open (one)

- **vchecks.md LOCATED:** it is the sub-file **`docs/briefs/cc-0017c/vchecks.md`** (per cc-0017c §5.3). The V-B4 12-param correction is fully grounded (see above) but the sub-file edit is itself a re-emit job → deferred to CC alongside the cc-0017a/0010A/0011/0012 surgical pass.
- **decisions.md (runtime) STILL UNLOCATED** — not in repo root, `docs/` (per index), or `docs/runtime/`. Parked per PK steer (don't burn quota chasing). Note: the cc-0014 sync_state framing leg appears already moot (v3.06 sync_state no longer carries the old INVALID/timing-artefact wrong framing); only a possible `decisions.md` leg may remain, pending location.

### 🎓 Lessons — NOT promoted (PK chose option (c))

Canonical lessons **L37–L65 are not defined in any locatable file** (checked: repo root, `docs/` per index, `docs/runtime/`, `12_project_handoff.md`). They appear to live distributed across originating session files; the action_list is the only consolidated **ledger** (it names the range but doesn't define them). Faithful promotion would require inventing (1) the canonical home, (2) the promotion mechanic, (3) the next baseline number (assigning L66+ assumes L65 is current max — unconfirmable). PK directive (c): **leave lessons for a next-lesson-cycle / CC pass where the convention is known; do not promote by assumption.** Nothing promoted. L41 + L-v2.85-e + L-v2.83-a + L-v2.85-a remain promotion-eligible candidates in the ledger, unchanged.

---

## Hard stops respected v3.07

0 production mutation / 0 apply_migration / 0 EF deploy / 0 cron change / 0 enablement / 0 hard delete / 0 friction.* write / 0 Supabase of any kind / 0 dashboard edit (PHASES leg N/A — docs-only, no phase change) / 0 D-01 (no review needed — repo doc-only) / 0 decisions.md change / 0 memory edit / 0 cc-0015 start / 0 Stage E / 0 Q-005 touch / 0 lesson promoted by assumption / 0 blind patch. No large-brief full-file re-emit attempted in chat.

## Commits this session

1. `6d5c3ae6` — cc-0017c v1.2 doc patch (apply-record backfill).
2. `81dcb683` — cc-0017c v1.2 correction (soften unverified cc-0017d version tag).
3. _(this close: session file + action_list + sync_state pointer edits)_

## L41 note

cc-0017c rewritten from a fresh same-turn read (small file, safe). Large-brief patches explicitly NOT re-emitted in chat (L41 full-file-clobber avoidance — routed to CC surgical). `00_` index edits done compact + sha-passed. Reinforces L-v3.06-a / L41 pairing: the available GitHub bridge has no str_replace, so large files belong to CC's surgical path, not chat re-emit.

## Next actions (carried)

- **CC surgical pass** (laptop): cc-0017a v1.2 (confirm scope first), cc-0017c/vchecks.md V-B4 12-param fix (grounded), cc-0010A/0011/0012 (need scope), decisions.md (locate first).
- **Lessons** — next-lesson-cycle / CC: promote L41 + L-v2.85-e, L-v2.83-a, L-v2.85-a once canonical home + numbering confirmed.
- All prior carries from v3.06 unchanged (YT backlog drain watch, token durability, F-YT-FAILED-NO-RETRY execution, cc-0019 Unit B optional, cc-0015 Gate 11 watch closing 2026-05-26, etc.).

---

*Last updated: 2026-05-25 Sydney — v3.07 doc-patch debt. cc-0017c main brief → v1.2 PATCHED + CORRECTED (`6d5c3ae6`→`81dcb683`). cc-0017a / cc-0010A / cc-0011 / cc-0012 + cc-0017c/vchecks.md DEFERRED to CC (insufficient scope + full-file re-emit risk). vchecks.md path located: `docs/briefs/cc-0017c/vchecks.md`. decisions.md still unlocated (parked). Lessons NOT promoted — canonical home + numbering unresolved (PK option c). 0 production mutation; repo doc-only.*
