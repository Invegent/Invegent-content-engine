# 2026-06-02 Sydney — video-worker getVoiceId guard (v3.1.2) DEPLOYED — v3.26

**Session window:** 2026-06-02 (UTC; CCD lane).
**Headline:** Shipped a narrow voice-selection safety guard to `video-worker` — `getVoiceId()` no longer silently falls back to the NDIS voice for unrecognised client slugs — completing the voice/audio safety pass started in v3.25.
**Outcome:** Production `video-worker` is live at `video-worker-v3.1.2` (main @ `851bc81`); EF-drift reconciled to **A-LE / clean / in-spec** (repo == deployed). No production draft, render-pipeline, publisher, cron, schema, or image-worker change.

---

## Stages / work units this session

### Stage 1 — Guard patch authoring (on branch)

- **Branch:** `feat/video-worker-getvoiceid-guard` off `main` @ `fb254bc`; commit **`851bc81`** ("video-worker: guard getVoiceId against silent NDIS fallback (v3.1.2)").
- **Change (`supabase/functions/video-worker/index.ts` only; +17 / −3):** `getVoiceId()` final fallback `return Deno.env.get('ELEVENLABS_VOICE_ID_NDIS') ?? null` → **`return null`**; caller throw message → `No ElevenLabs voice ID configured for client_slug=<slug>`; `v3.1.2` changelog block; `VERSION` const bumped `video-worker-v3.1.0 → video-worker-v3.1.2`.
- **Verification:** `deno check` exit 0. Tier-1 scratch env-matrix (uncommitted `_harness/voice_matrix_check.ts`, fake values) — **6/6 PASS**: ndis-yarns→NDIS alias; exact env wins over alias; property-pulse→PP alias; new-brand exact env resolves; **unknown brand + only NDIS env → null (the guard)**; matched-alias-unset → null. No test scaffold committed (per directive: no `import.meta.main` wrap, no lib extraction).

### Stage 2 — Review + merge

- **CCH read-only review: APPROVED.** Branch pushed to origin (`851bc81`); reviewed unmerged.
- **Merge:** `main` confirmed at `fb254bc` (not advanced); **fast-forward** `fb254bc..851bc81` (no merge commit; name-only diff = `index.ts` only); `deno check` exit 0; pushed `origin/main` → `851bc81`. `_harness/` kept untracked.

### Stage 3 — Drift refresh + deploy (in-design path)

- **Pre-deploy targeted refresh:** `drift-check?write=true&slug=video-worker` (slug-scoped, 1 row) → **B-FD** (repo v3.1.2 ahead of deployed v3.1.0; `repo_hash fad6f623… ≠ deployed_hash cfbacd8b…`). check-only → BLOCK exit 1; check-only + `--allow-warn` → PASS exit 0.
- **D-01:** CCD-inline; **PK phrase** `APPROVE ef_deploy D-01: deploy video-worker 851bc81 to production with --allow-warn`.
- **Deploy:** `./scripts/safe-deploy.sh video-worker --allow-warn` → **exit 0** (wrapper-invoked `supabase functions deploy video-worker`; no raw bypass). Health GET = `{"version":"video-worker-v3.1.2"}` HTTP 200.
- **Post-deploy refresh + re-gate:** targeted refresh (1 row) → **A-LE / clean / in-spec**; `repo_version 3.1.2 == deploy_version 3.1.2`; `repo_hash_normalised == deployed_hash_normalised == fad6f623…d7f0cc`.

**Lifecycle:** `A-LE (v3.1.0)` → merge + refresh → `B-FD (v3.1.2 pending)` → deploy `--allow-warn` + refresh → `A-LE (v3.1.2 in-spec)`.

---

## Voice/audio safety pass — full picture (v3.1.0 → v3.1.2)

- **v3.1.0** (lane v3.25 part 1): `video_short_kinetic_voice` burned-in captions.
- **v3.1.1** (lane v3.25 part 2; in deployed source): Creatomate audio-volume contract fix — `volume` is a percentage string; voice `1.0→"100%"`, music `0.15→"15%"`. Fixed the ~−40 dB voice attenuation globally (client-agnostic).
- **v3.1.2** (this lane): getVoiceId guard — removes the silent NDIS fallback so a new/unknown brand fails loudly instead of rendering in the wrong voice.

The v3.25 voice/audio-unification investigation proved the volume bug was **shared** (PP + NDIS both ~−58 LUFS pre-fix; same code path) and globally fixed by v3.1.1; the only residual onboarding risk was the silent-fallback, now closed by v3.1.2.

## D-01 / governance note

D-01 review was **CCD-inline** (PK phrases captured verbatim); CCH did a read-only review approval of the guard patch. No `m.chatgpt_review` row created/resolved — none fabricated.

## GENERIC-NON-BLOCKING log (L46)

None this session.

---

## Production mutations this session

1. **Git FF merge → main** `fb254bc..851bc81` + push — main now `851bc81`.
2. **EF deploy** `video-worker` (851bc81 → `video-worker-v3.1.2`) via `./scripts/safe-deploy.sh video-worker --allow-warn` — exit 0, success.
3. **`m.ef_drift_log` register writes ×2** — targeted `drift-check?slug=video-worker&write=true` (pre-deploy → B-FD; post-deploy → A-LE). Advisory drift register only.

**No** schema/migration/cron/queue/publisher/social/image-worker/webhook change, **no** render, **no** ElevenLabs/Creatomate API call, **no** production draft mutation, **no** extra function deploys, **no** secret change. `_harness/` kept untracked/uncommitted.

---

## Lessons captured this session

- **L-v3.26-a — silent config fallbacks are onboarding traps.** A per-client mapping that defaults to one client's resource (here: unknown slug → NDIS voice) renders *successfully* with the wrong asset and hides the misconfiguration. Prefer fail-loud (null → caller throws → draft `failed`) over silent default for identity-bearing config. Reinforces the v3.25 in-design deploy path (L-v3.25-a): merge → targeted drift refresh → B-FD → `--allow-warn` → refresh → A-LE; never bypass the wrapper.

---

## Follow-up findings opened / carried

### F-CREATOMATE-CAPTION-HIERARCHY (carried from v3.25)
**Status:** OPEN (deferred) — P3. Make voiceover captions visually distinct from main on-card text (caption-styling-only). Memory `followup-creatomate-caption-hierarchy.md`.

### F-VOICE-LOWVOL-BACKFILL (new, deferred)
**Status:** OPEN — P3 (data/backfill decision). ~21 PP + NDIS already-rendered voice videos (`kinetic_voice`/`stat_voice`) carry the pre-fix ~−58 LUFS audio (rendered before v3.1.1). The deploy fixes future renders only. If any are still live/visible and needed, decide whether to re-render. PK decision, not a code fix.

### Branch cleanup (optional)
`feat/video-worker-getvoiceid-guard` is merged; may be deleted later per project convention (NOT done this lane — branch deletion requires separate approval).

---

## State at session close

- **Production `video-worker` = `video-worker-v3.1.2`** (main @ `851bc81`); EF-drift **A-LE / clean**.
- Shipped: getVoiceId guard (no silent NDIS fallback; exact env + ndis/property aliases preserved); VERSION reports v3.1.2.
- 4-way sync: this session file + `docs/00_sync_state.md` v3.26 banner + `docs/00_action_list.md` v3.26 entry. No dashboard/roadmap leg (operational). Memory: L-v3.26-a + backfill follow-up.

**Next major work:** deferred F-CREATOMATE-CAPTION-HIERARCHY polish (own patch + render-verify); F-VOICE-LOWVOL-BACKFILL decision. Voice/audio safety pass (captions + volume + voice-mapping guard) is now complete and in production.
