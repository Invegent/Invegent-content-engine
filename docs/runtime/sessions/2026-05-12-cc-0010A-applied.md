# 2026-05-12 Sydney — cc-0010A v1.5 APPLIED + CLOSED (v2.67)

**Outcome:** **cc-0010A r.* DDL foundation Stage A delivered.** 6 new tables, 1 helper, 1 FK, 1 default config row, 6 k.table_registry UPSERTs, 86 k.column_registry rows via Fix A pattern, 2 m.chatgpt_review close-the-loop UPDATEs. cc-0010B + cc-0010C unblocked.

## Session arc

Multi-thread session covering cc-0010A v1.4 → v1.5 docs-only V6c tightening + v1.5 live pre-flight + v1.5 D-01 + v1.5 apply + V-checks + L45 truth check + close-the-loop + 4-way sync close. Preceded by v1.3 atomic-rollback recovery via v1.4 Fix A pattern (earlier session).

## Production mutations

| Mechanism | Count | Detail |
|---|---:|---|
| `apply_migration` (success) | 1 | `cc_0010a_r_evidence_matcher_schema_foundation` — recorded in supabase_migrations.schema_migrations |
| `apply_migration` (atomic rollback prior) | 1 | v1.3 at 08:15:13 UTC — zero persistent effect |
| `execute_sql` (write — close-the-loop) | 1 | single 2-row UPDATE on m.chatgpt_review |
| `execute_sql` (read-only) | ~25 | pre-flight + V-checks + investigation |
| `ask_chatgpt_review` (D-01) | 1 | review_id `752dfec6-6f9a-4956-b7d7-a4112009b93c` — clean agree, zero pushback |
| GitHub commits | 2 (this session) | v1.5 docs (`3db84322`) + this 4-way sync close |
| `memory_user_edits` | 0 | deferred to next session if needed |

## L46 GNB log

| D-01 # | review_id | Verdict | Pushback count | Classification | Outcome |
|---|---|---|:---:|---|---|
| 1 (v1.5) | `752dfec6-6f9a-4956-b7d7-a4112009b93c` | agree | **0** | N/A (no pushback) | proceed — first cc-0010A D-01 clean pass-through |

**Cumulative cc-0010A L46 history:** v1.3 D-01 (`8a4b93fb`) returned 2 GNB → PK Path B override → apply attempted → apply failed downstream on NOT NULL violation (failure was downstream SQL rendering defect, not L46 protocol issue). v1.4 surface improvements (Fix A pattern + §1.7b probe + V6c assertion) + v1.5 V6c tightening produced clean v1.5 D-01 with **zero pushback**. **Demonstrates that improving brief surface eliminates need for L46 overrides.** L46 baseline confirmed.

## V-checks (10 PASS + 1 accept-with-variance)

V1 ✓ | V2 ✓ | V3 ✓ (with disclosure: 4 routines not 3) | V4 ✓ | V5 ✓ | V5b ✓ | V6 ◐ accept-with-variance (purpose marker REPLACE no-op; FK fields correct) | V6b ✓ | V6c ✓ (v1.5 strict equality `= 86`) | V7 ✓ | V8 ✓

## L45 truth check (full count-delta + sanity sample + mismatch declaration)

Count-delta table: 18 row-events all match expected. Sanity sample: 5 shape-variant rows all show `ordinal_position` populated.

**Mismatch declaration:** 5 items, all **accept-with-variance**:
1. §2.7 PL/pgSQL variable `out` → `result_jsonb` rename (PG reserved word collision; semantics identical; V4 all 6 outputs match)
2. V3 function count 3→4 (`r.set_updated_at` cc-0009 carry-over)
3. V6 purpose marker absent (REPLACE no-op; substantive FK fields correct; brief footnote anticipated)
4. k.table_registry r.* baseline 2→5 (3 geography rows pre-existing per F-K-SCHEMA-REGISTRY-R-STALE-DESCRIPTION P3)
5. k.column_registry r.* baseline implicit ~17 → 95 (same root cause)

Full detail in `docs/briefs/results/cc-0010A-r-reconciliation-ddl-foundation.md`.

## Lesson-candidate vindications + new candidate

- **L38 candidate**: **VINDICATED** at cc-0010A Stage A close (cross-brief FK ALTER from cc-0009 deferred → cc-0010A added). Recommend promotion to baseline.
- **L44 (Runtime Proof Pre-flight)**: **3rd live exercise complete + baseline-eligible.** v1.3 pre-flight caught queue_id PK-name drift; v1.4 added §1.7b probe; v1.5 pre-flight all 12 probes clean.
- **L45 (Post-mutation truth check)**: **first full live exercise complete + baseline-eligible.** Count-delta + 5-row sample + 5-row mismatch declaration template all exercised end-to-end.
- **L46 (Reviewer Evidence Gate)**: **3rd live application complete + baseline confirmed.** Clean pass-through v1.5 D-01.
- **L48 (Atomicity Gate)**: split decision applied; cc-0010A delivered as atomic sub-build.
- **NEW lesson candidate v2.67**: PG reserved-word collision check for PL/pgSQL DECLARE variables (`out`, `result`, `record`, etc.) must be part of brief-authoring pre-D-01 checklist. PRV-0 §4.3 had `out`; CCD didn't catch; D-01 didn't catch; caught at apply construction.

## Production state at session close

- 6 new r.* tables live (all empty except matcher_config = 1 row)
- `r.compact_raw_json` helper live (IMMUTABLE, plpgsql, INVOKER)
- `r.expected_publication.matched_match_id` FK live (L38 vindicated)
- pg_trgm v1.6 confirmed
- 11 k.table_registry rows for schema `r` (5 baseline + 6 cc-0010A)
- 181 k.column_registry rows for schema `r` (95 baseline + 86 cc-0010A)
- D-01 row `8a4b93fb` = resolved (v1.3 retroactively closed)
- D-01 row `752dfec6` = resolved (v1.5)
- 24 unrelated historical escalated m.chatgpt_review rows — **untouched per CCH directive**
- T-MCP-02 cum: +1 = 60 (was 59 at v2.66 close)

## cc-0010B + cc-0010C unblocked notice

**cc-0010B (ice-evidence-materialiser EF end-to-end) — UNBLOCKED.** Reads `m.post_publish_queue` / `m.post_publish` / `m.post_draft` / `m.slot`. Writes `r.ice_publication_evidence` (newly created). Authoring can begin.

**cc-0010C (reconciliation-matcher EF end-to-end + Tier 1) — UNBLOCKED.** Reads `r.expected_publication` + `r.ice_publication_evidence` (via cc-0010B output) + `r.matcher_config` (newly created). Writes `r.reconciliation_match` (newly created). Tier 1 (ICE-evidence match) only; Tiers 2–5 deferred to PRV-2/3/4. Authoring blocked on cc-0010B completion.

## Truth check at close

| What | Verbatim |
|---|---|
| r.* tables live | 11 total (5 baseline + 6 cc-0010A) |
| r.* helpers live | 4 (`compact_raw_json`, `normalise_text`, `set_updated_at`, `to_sydney_local_date`) |
| r.matcher_config rows | 1 (global default) |
| r.expected_publication rows | 98 (unchanged from session start) |
| r.reconciliation_run rows | 5 (unchanged from session start) |
| Result file | `docs/briefs/results/cc-0010A-r-reconciliation-ddl-foundation.md` (this commit) |
| Open D-01 rows | 0 from cc-0010A; 24 unrelated historical (untouched) |
| sync_state delta | new session row added; v2.67 inline summary; cc-0010B/C unblocked notice |
| action_list delta | cc-0010A active row → closed; cc-0010B + cc-0010C active rows added; pre-cc-0010A gating items closed |

## Mismatch declarations (session-level — overlap with result file)

See `docs/briefs/results/cc-0010A-r-reconciliation-ddl-foundation.md` §6 for the 5 L45 mismatches and their accept-with-variance decisions.

## Next-session priorities (preview)

1. cc-0010B brief authoring (P1) — ice-evidence-materialiser EF source + deploy + cron + first invocation
2. v1.6 doc-only patch — fold `result_jsonb` rename disclosure + V6 purpose-marker fix-forward into brief
3. 5-row + 24-row close-the-loop batches still pending
4. cc-0010C authoring after cc-0010B completes
5. F-K-SCHEMA-REGISTRY-R-STALE-DESCRIPTION P3 cleanup
6. L34 trigger filter audit
7. Dashboard PHASES reconciliation (23rd consecutive deferral)

---

*Session authored 2026-05-12 Sydney by chat (Claude). cc-0010A v1.5 applied successfully via Supabase MCP apply_migration. V-checks 10 PASS + 1 accept-with-variance. L45 5 mismatches all accept-with-variance. L38 + L44 + L45 + L46 + L48 all live-exercised. cc-0010B + cc-0010C unblocked. T-MCP-02 cum 60.*
