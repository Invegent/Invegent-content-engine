# 2026-05-26 Sydney — v3.11: F-YT-PUB-PUBLISH-AUDIT-GAP CLOSED (youtube-publisher v1.10.0 fix-forward + 11-row historical backfill)

**Actor:** CCD (Claude Code / laptop terminal). EF deploy via Supabase CLI; backfill DML via Supabase `apply_migration`.
**Type:** 1 EF deploy + 1 backfill migration (production mutation) + read-only verification.
**Governance:** ef_deploy D-01 `5bd19069-ba7c-459f-b08f-a1db7afc087b` (clean agree/proceed); sql_destructive D-01 `998c90a3-6725-476d-a34a-3f02f0b5ff22` (escalate_explicit_flag → PK exact approval phrase `Approved: apply F-YT-PUB-PUBLISH-AUDIT-GAP backfill`). Both close-the-loop recorded. A-then-B.

---

## Outcome

The v3.10 finding **F-YT-PUB-PUBLISH-AUDIT-GAP is RESOLVED**, executed A-then-B. v1.10.0 stops new gaps (fix-forward) and the 11 historical missing YouTube `m.post_publish` audit rows are backfilled. **The entire YouTube failed-backlog recovery arc (F-YT-FAILED-NO-RETRY) is now complete: B1 + B2 done, the publish-audit gap closed; B3 (9 never-connected) remains onboarding debt only.**

## Unit A — youtube-publisher v1.10.0 (fix-forward)

- Deployed from repo commit `fe8729c` via `supabase functions deploy youtube-publisher --no-verify-jwt --project-ref mbkmaxqhsohbtwsqolns`. Live GET → `youtube-publisher-v1.10.0`, HTTP 200 (unauthenticated → `--no-verify-jwt` intact).
- **Fix:** the success-path `m.post_publish` INSERT now computes the **next available `attempt_no` per `post_draft_id`** (`max(attempt_no)+1`) instead of the hardcoded `attempt_no=1` that collided with the draft's existing Facebook row on `uq_publish_attempt (post_draft_id, attempt_no)`. Plus: on a residual `insertErr` it no longer swallows silently — writes a durable `youtube_audit_insert_failed` marker into `draft_format` (the draft is already `published`, never re-selected, so no re-upload).
- **Preserved byte-unchanged:** the v1.9.0 approval predicate `IN ('approved','published')`, the no-`youtube_video_id` guard, the pre-upload `m.post_publish` reconcile guard, failure classification, bounded retry, channel auth-hold, the format allow-list, unlisted privacy, the 2/tick cap.
- ef_deploy D-01 `5bd19069`: **clean agree/proceed, no pushback.** Close-the-loop recorded.

## Unit B — historical audit-row backfill

- Backfill DML committed `538d9e1` (prepared artifact `docs/briefs/results/yt-publisher-publish-audit-gap-backfill.sql`); applied via `apply_migration yt_pub_publish_audit_gap_backfill_20260526` (`success`; all gates passed; `ROW_COUNT=11`).
- sql_destructive D-01 `998c90a3`: verdict `partial` → **escalate_explicit_flag / requires_pk_escalation=true** (the bridge auto-escalated because it touches historical audit records). Objections were generic (already satisfied by the DML's snapshot-first + cohort==11 gate + null/dedupe guards + ROW_COUNT assert + read-only verification SELECTs); the `published_at`/`response_payload` reconstruction points were my own disclosed `known_weak_evidence`. **PK gave the exact approval phrase**; close-the-loop recorded with PK resolution.
- **Cohort = 11** (2 v3.10-B2-recovered + 9 pre-existing historical cross-posted publishes; all 11 had a Facebook `post_publish` row at `attempt_no=1`).
- **Post-apply verification (read-only):** remaining gap = **0**; 11 backfilled, all `platform='youtube'` with `platform_post_id` == the draft's `draft_format.youtube_video_id`, all at non-colliding `attempt_no=2`; **0** duplicate YouTube rows; Facebook rows untouched (736 unchanged); snapshot `m.yt_audit_backfill_snapshot_20260526` = 11; **no `m.post_draft` mutation; no re-upload.**

## Honest limitation (no over-claim)

- `platform_post_id` (the YouTube video ID) is **authoritative** — read directly from `draft_format.youtube_video_id`.
- The historical rows' `published_at` is **reconstructed** from `draft_format.youtube_published`, and `response_payload` is **reconstructed** and `backfill`-tagged (`backfill: 'f_yt_pub_publish_audit_gap_20260526'`).
- **The original YouTube API insert payload was never captured for these drafts and cannot be recovered.** These rows are honest backfill reconstructions, not the original publish records — explicitly marked as such.

## State after v3.11

- youtube-publisher **v1.10.0 LIVE**. The 15 B2 drafts still draining will now publish **with** correct `m.post_publish` audit rows under v1.10.0 (observable on upcoming `:15/:45` ticks).
- **F-YT-FAILED-NO-RETRY arc COMPLETE:** B1 (5 quota) + B2 (17 token-casualty) recovered; publish-audit gap closed (fix-forward + 11-row backfill). **B3 (9 never-connected) = onboarding debt, out of scope** (NOT recovered — they never connected a YouTube channel).
- **Still open (unrelated):** F-YT-EXPIRY-DISPLAY-FAKE (dashboard callback hardcoded 5y expiry); CFW + Invegent YouTube onboarding; B3 onboarding.

## Hard stops / scope (honored)

A-then-B (v1.10.0 deployed + verified before the backfill). Both D-01s fired + closed-loop; sql_destructive escalated → PK exact approval phrase obtained before apply. Backfill bounded to the exact 11; no `m.post_draft` mutation; no Facebook-row touch; **B3 NOT recovered** (excluded by construction — no `youtube_video_id`); no re-upload. The reconstruction/backfill limitation is recorded, not hidden; no claim that the original YouTube API payload was recovered.
