# 2026-05-27 Sydney — v3.15: Instagram diagnosis lane parked + cc-0020 Subscription Email Ingest lane parked — session sync + close

**Headline:** Two parked lanes recorded for durable continuity — the Instagram "not publishing" diagnosis (CCH lane) and the cc-0020 Subscription Email Ingest build (CCD lane).
**Outcome:** Docs-only close on `main`. **0 application code, 0 migration apply, 0 DB connection, 0 deploy, 0 Gmail/OAuth/token, 0 cron/Pub-Sub, 0 PR/merge, 0 D-01.** Both lanes remain parked behind their existing gates.

> Lane isolation note: the two lanes are independent. They share no branch, commit, migration, or approval. This note records both only because they are the two open lanes parked this session.

---

## Lane A — Instagram "not publishing" diagnosis (CCH lane) — parked/closed

**Status:** parked/closed for this session. (Recorded from the CCH lane / PK report; CCD did not run the Instagram lane and made no Instagram changes.)

- **Diagnostic runbook committed** at `b911d91` (`docs/runbooks/instagram-not-publishing-diagnostic.md`) — the cc-0019 fill-gate trap + copy-paste SQL.
- **Outcome:** NY (NDIS-Yarns) and PP `publish_enabled` flipped via the dashboard UI; the cc-0019 publish-eligibility fill-gate that was suppressing generation/publish is cleared.
- **Expected self-heal ≈ 2026-05-29** as the pipeline catches up.
- **No further Instagram action unless PK explicitly reopens.**
- Kept entirely separate from cc-0020.

---

## Lane B — cc-0020 Subscription Email Ingest (CCD lane) — parked

**Status:** parked. Branch `feat/cc-0020-subscription-email-ingest`, remote HEAD **`7497e7483165cbec7fab28c9c724602a8460d02a`** — pushed for preservation/reviewability; **unmerged, undeployed**. Brief: `docs/briefs/subscription-email-ingest-mvp.md` (renamed to `docs/briefs/cc-0020-subscription-email-ingest.md` **on the feature branch only** as Stage 0 housekeeping; on `main` it remains at the original path until that branch merges — merge is separately gated).

**Stage ledger (per CCH scope-control acceptance):**
- **Stage 0 — Discovery:** accepted. Confirmed `k.subscription_register` shape (catalog/current-state only); the subscriptions UI lives in the separate `invegent-dashboard` repo (not this repo's cc-0013 `dashboard/`); `k.*` is **not** PostgREST-exposed (reads go via a `public` SECURITY DEFINER RPC, cf. `public.get_subscriptions()`) — same REST-exposure theme as PRV's `op.*` (PGRST106).
- **Stage 1 — Parser + fixtures + tests:** accepted. Pure/offline/deterministic parser (`supabase/functions/subscription-email-ingest/parser.ts`), 8 synthetic fixtures, hermetic Deno tests; idempotency key `gmail_message_id` + FNV-1a `content_hash` secondary defence; privacy-by-design (domain only, never body/full address).
- **Stage 2 / 2a / 2b — Draft migration:** accepted. `supabase/migrations/20260527120000_proposed_cc_0020_subscription_email_ingest_tables.sql` (DRAFT, `proposed_` marker, DO-NOT-APPLY header). Two append-only tables in `k.*`: `subscription_import_candidate` (`UNIQUE(gmail_message_id)`) + `subscription_spend_event` (`UNIQUE(source_candidate_id)`, append-only); RLS **enabled deny-by-default**, no anon/authenticated grants, least-privilege `service_role`; `event_type` (charge/refund/credit/adjustment/unknown) is authoritative with sign-vs-type CHECKs (2b); ledger `event_type` has **no default** (explicit on promotion).
- **Stage 3 — Ingest scaffold:** conditionally accepted as scaffold complete. Parser→candidate mapping (`event_type` derivation + sign convention), in-memory repository modelling `ON CONFLICT (gmail_message_id) DO NOTHING` and `UNIQUE(source_candidate_id)`, idempotent accept→spend-event promotion (enforces the ledger NOT NULL columns), **fail-closed Gmail stub** (no network/OAuth/token), EF entrypoint using the in-memory repo only (no Supabase client). 17 Deno tests pass; `deno check` + `deno lint` clean.
- **Stage 3a-P — Branch push:** closed. Branch pushed; CCH performed direct DDL/scaffold inspection; no blockers; no fixes required.
- **Stage 3a — DB-layer validation:** **OPEN** — the real-Postgres idempotency/DDL proof is deferred because Docker/local Postgres was unavailable in the build environment (the in-memory repo modelled the constraint semantics instead). **This blocks Stage 4.**
- **Stage 4 — Review surface:** **HELD.**

**Walls intact (each unlocked only by separate explicit PK approval; + D-01 for production mutations):** no live Gmail, no OAuth/token create-refresh-store, no migration apply, no production Supabase connection, no deploy, no cron/Pub-Sub, no writes to `k.subscription_register`. The draft migration is **UNAPPLIED** anywhere (production project `mbkmaxqhsohbtwsqolns` never touched at any stage of this lane).

**Future notes (deferred, not started):** register matching (`matched_subscription_id`); `content_hash` as a secondary duplicate signal; optional currency CHECK constraint; apply-time preflight for `service_role` USAGE on schema `k`; the two Stage 3 policy items (refunds currently below the 0.4 create threshold → mapped but not auto-created; `event_type`/sign mapping lives in `mapping.ts`, not the parser).

**No D-01 fired this lane** — no production mutation was ever proposed.

---

## Preserved unrelated durable state (unchanged this session)

- **PRV (Platform Reconciliation View):** remains committed + pushed to `origin/feature/platform-reconciliation-view-readonly` (v3.14) — **unmerged, undeployed**. Production deploy gated (`PK APPROVES PRV READ-ONLY PRODUCTION DEPLOY`). Not touched here.
- **cc-0015 friction-pool-view:** remains parked — **Stage C/D/E HELD**; source filter + sort + FAB category-desc hydration deferred; ICE-PROC-002 +4-week review ≈ 2026-06-26. Not resumed here.

---

## State at session close

- **Production mutations this session: 0.** No `apply_migration`, no `execute_sql` write, no deploy, no EF change, no cron change, no Supabase connection.
- **D-01 fires: 0** (no production mutation proposed).
- Docs-only close on `main`: this session note + `docs/00_sync_state.md` (v3.15 callout + index row) + `docs/00_action_list.md` (header + closure-budget cycle line + parked-lane carries). Memory: `cc-0020-subscription-email-ingest-lane` entry maintained (outside the repo).
- **Next major work:** both lanes await explicit PK/CCH release — cc-0020 Stage 3a DB-layer validation (needs a local/ephemeral Postgres) then Stage 4; Instagram only if PK reopens.
