# v2.85 — cc-0017c APPLIED-WITH-VCHECK-CORRECTION (Friction.* Wave 0 COMPLETE)

**Session date:** 2026-05-19 Sydney morning (UTC+10)
**Predecessor:** v2.84 (cc-0017c brief authored + 2× D-01 deferred per Path C)
**Successor:** v2.86 (Wave 0d brief authoring — NOW BLOCKING)
**Status:** APPLIED-WITH-VCHECK-CORRECTION ✅

**Migration versions live:**
- `20260519004545` — `cc_0017c_friction_register_lockdown_and_backfill` (main: Section A FK swap + Section B 8 REVOKEs + Section C 0-row UPDATE)
- `20260519005322` — `cc_0017c_v_b4_smoke_cleanup` (V-B4 smoke artefact removal via postgres-owner DELETE)

**D-01 cycle (fresh, v2.85):**
- review_id: `d18fa6db-3a5c-42ff-8aef-55fcb4fb4f92`
- verdict: **AGREE** (empty pushback_points, empty corrected_action; procedural escalate only)
- close-the-loop: status=resolved at 2026-05-19 00:54:22.90659 UTC
- resolved_by: `cc-0017c-v2.85-applied-with-vcheck-correction`

---

## Outcome

cc-0017c Wave 0c applied successfully via single atomic `apply_migration`, completing the friction.* foundational schema (Waves 0a + 0b + 0c). All 9 V-checks completed with 8 PASS + 1 PARTIAL (V-A3 REVOKE-first acceptable per brief). V-B4 brief defect surfaced during execution and was remediated via PK-directed Path 1 inline rewrite — corrected 12-param signature confirmed `emit_event` SECURITY DEFINER bypass functional post Section B REVOKE lockdown.

Production state at close: event=29, case=29, source=3/3, action_decision NULL=21/act_now=1/track=7/closed_class=0/done=0. Smoke event+case cleanly removed via postgres-owner DELETE in dedicated cleanup migration. Friction Register Consolidation Plan v1 reaches Wave 0 completion.

D-01 fires v2.85: 1 (fresh fire returning AGREE verdict — substantively cleanest review narrative across all three D-01 cycles for cc-0017c, validating v2.84 honest limitation hypothesis). T-MCP-02 cum 75 → 76. State-capture exceptions: 1 (unchanged — clean approve verdict consumed no override).

**Note on the close commit:** Initial 4-way sync close `push_files` attempt was truncated mid-construction (response-length budget hit while drafting per-session file content). `list_recent_commits` confirmed no partial artefact landed on main (HEAD still at v2.84 close `2cdef219`). Production mutations (apply_migration + V-checks + close-the-loop UPDATE) had completed before the truncated push attempt, so repo state remained consistent. Clean repush succeeded as this commit. New lesson candidate L-v2.85-e captures the pattern.

---

## Sequence

1. **Session open** — read sync_state v2.84 + action_list v2.84 + cc-0017c brief index + preflight-pset.md + migration-sql.md + vchecks.md.
2. **G3 review presented to PK** — apply plan, D1/D2/D3 decision matrix, pre-flight requirements, V-check sequence.
3. **PK directive (G3 preparation)** — run P-1 to P-5 read-only; fire fresh D-01 if all match expected; do not apply until explicit approval; hard-stop on drift.
4. **P-set rerun** (5 read-only probes via `execute_sql`):
   - **P-1** grants: matches design-time reference exactly. postgres ALL; service_role INSERT+SELECT on event + INSERT+SELECT+UPDATE on case; no PUBLIC/auth/anon.
   - **P-2** FK validity: 29 events across 3 sources (health_check=5, manual=8, reconciliation=16), all FK_VALID, 0 orphans. Forward drift +7 on reconciliation since v2.83 fact-finding (cron 85 daily fires) — within brief's "or higher" envelope; benign.
   - **P-3** backfill candidates: `backfill_candidate_count=0`, `done_count_audit=0`, null=21, track=7, act_now=1, defer_intentionally=0, total=29. No hard-stops.
   - **P-4** baseline: event=29, case=29, active_source=3, total_source=3.
   - **P-5** CHECK definition: `CHECK ((source = ANY (ARRAY['reconciliation'::text, 'health_check'::text, 'manual'::text])))` — exact match expected.
5. **Fresh D-01 fire** — `ask_chatgpt_review` action_type=sql_destructive, verbatim P-set in `current_evidence`, 9 enumerated `known_weak_evidence` items. review_id `d18fa6db-3a5c-42ff-8aef-55fcb4fb4f92`. Verdict **agree**. risk_level=medium. confidence=high. `pushback_points=[]`. `corrected_action=""`. escalate=true (procedural — standard bridge auto-escalate for `sql_destructive`). 3 verified_claims + 2 assumptions (both addressed in brief design: FK structural verification + Wave 0d dependency).
6. **PK directive: approved to apply this session.** D1=yes, D2=fresh (used), D3=migration name confirmed.
7. **`apply_migration`** — name `cc_0017c_friction_register_lockdown_and_backfill`, single atomic SQL body. Result: `{"success":true}`. Version `20260519004545`.
8. **V-check execution** — sequential via `execute_sql`:
   - V-A1 ✅ PASS — `event_source_check` `check_remaining=0`.
   - V-A2 ✅ PASS — `event_source_fk` definition exact match expected.
   - V-A3 ✅ PARTIAL — DO block returned empty (no unhandled exception bubble; REVOKE-first per brief acceptance).
   - V-B1 ✅ PASS — service_role INSERT on event = 0 grants.
   - V-B2 ✅ PASS — service_role INSERT/UPDATE on case = 0 rows.
   - V-B3 ✅ PASS — service_role retains exactly SELECT on both tables (no over-revoke).
   - **V-B4 ❌ FAIL** — `42883: function friction.emit_event(p_source, p_source_event_id, p_category, p_severity, p_reported_by, p_payload, p_occurred_at) does not exist`. **Hard-stop per PK directive item 4.**
9. **Diagnostic probe (single read-only)** — `pg_get_function_identity_arguments` revealed actual deployed signature: 12 named params (source, condition_key, problem_key, source_event_id, observed_at, related_object, observation_text, raw_payload, reported_by, severity_override, category_override, dynamic_context); RETURNS TABLE(event_id uuid, case_id uuid, case_disposition text); SECURITY DEFINER=true. Brief defect characterised: V-B4 written against pre-cc-0017b memorial 7-param signature; never updated when cc-0017b unified emit_event with 12-param shape. **Migration itself structurally correct** (V-A1/A2/B1/B2/B3 all green); V-B4 failure is brief defect, not migration defect.
10. **Hard-stop report to PK** with 4 path options.
11. **PK directive: Path 1** — inline V-B4 rewrite with correct 12-param signature; cleanup-scoped smoke values specified verbatim (`source=manual`, `condition_key=manual_fab`, `problem_key=cc_0017c_v_b4_smoke`, `source_event_id=cc-0017c/v-b4/<uuid>`, `category_override=unclassified`, `severity_override=info`, tagged jsonb fields); run V-B4 corrected → V-C1 → V-C2 → cleanup → confirm final counts; close as APPLIED-WITH-VCHECK-CORRECTION on PASS; hard-stop again if corrected V-B4 fails (real post-lockdown emit_event failure).
12. **V-B4 (corrected) ✅ PASS** — `SELECT * FROM friction.emit_event(...)` 12-param call from service_role context returned event_id=`f4c94457-2320-4195-bde5-50f03f01459d`, case_id=`fd573ea1-b3b3-4ab3-a17a-91624b2c5257`, case_disposition=`created_new`. **Empirical confirmation: SECURITY DEFINER bypass functional post Section B REVOKE.**
13. **V-C1 ✅ PASS** — still_unresolved_closed_class=0, resolved_with_kind_set=0, done_count_audit=0.
14. **V-C2 ✅ PASS** — NULL=22 (21 pre-smoke + 1 smoke), act_now=1 (exact brief match), track=7 (exact brief match). Deltas vs brief-expected fully explained: +7 cron drift + 1 smoke. act_now/track exact match = strongest proof 0-row UPDATE was a no-op.
15. **Cleanup migration applied** — `cc_0017c_v_b4_smoke_cleanup` version `20260519005322`. DELETE event_id `f4c94457-...` + DELETE case_id `fd573ea1-...` (with NOT EXISTS guard). Postgres-owner context required because service_role lost DELETE grant post lockdown. Result: `{"success":true}`.
16. **Final state probe** — event=29, case=29, active_source=3, total_source=3, smoke_event_remaining=0, smoke_case_remaining=0, null=21, act_now=1, track=7, closed_class=0, done=0. Pristine post-smoke state.
17. **Close-the-loop UPDATE** — `m.chatgpt_review` id=`d18fa6db-3a5c-42ff-8aef-55fcb4fb4f92` → status=`resolved`, escalation_resolved_at=`2026-05-19 00:54:22.90659+00`, resolved_by=`cc-0017c-v2.85-applied-with-vcheck-correction`, action_taken includes full V-check matrix + migration versions + final state. **L40 column-name discipline maintained** (`id` not `review_id`; `escalation_resolved_at` not `resolved_at`).
18. **Initial 4-way sync close attempt — TRUNCATED mid-construction.** Response-length budget hit while drafting per-session file content. No commit landed on main.
19. **Truncation detection** — on session resume, called `list_recent_commits` (limit=5) on Invegent-content-engine. HEAD was `2cdef219ff7a17da6568ab388417a6550238f127` (v2.84 close commit). Confirmed no partial v2.85 artefact landed. **Production mutations were complete before the truncated push attempt**, so repo + DB state remained consistent.
20. **Clean repush** — this commit. 3-file atomic `push_files` with compacted content (mitigation per L-v2.85-e). Dashboard PHASES 38th consecutive deferral (carried per PK directive item 5). No `decisions.md` change (Wave 0c is execution of v2.79-signed plan).

---

## V-check final matrix

| Check | Status | Result |
|---|---|---|
| V-A1 | ✅ PASS | event_source_check CHECK dropped (count=0) |
| V-A2 | ✅ PASS | event_source_fk definition exact match expected |
| V-A3 | ✅ PARTIAL | DO block clean; REVOKE-first per brief acceptance |
| V-B1 | ✅ PASS | service_role INSERT on event = 0 grants |
| V-B2 | ✅ PASS | service_role INSERT/UPDATE on case = 0 rows |
| V-B3 | ✅ PASS | service_role retains exactly SELECT on both tables |
| V-B4 | ✅ PASS (corrected) | Brief 7-param signature failed; PK Path 1 inline rewrite with 12-param signature confirmed emit_event SECURITY DEFINER bypass post lockdown (smoke event_id `f4c94457-...` case_id `fd573ea1-...` case_disposition `created_new`) |
| V-C1 | ✅ PASS | 0/0/0 (still_unresolved_closed_class, resolved_with_kind_set, done_count_audit) |
| V-C2 | ✅ PASS | act_now=1/track=7 exact match brief; NULL=22 fully explained (21 pre-smoke + 1 smoke) |

---

## D-01 cycle ladder across cc-0017c

| Fire | review_id | Brief | Verdict | Pushback | Path | Status |
|---|---|---|---|---|---|---|
| v1.0 (2026-05-18) | `a37eff28-...` | v1.0 | partial | corrected_action Option A (drop 'done') | A: satisfy via v1.1 | resolved v2.84 |
| v1.1 (2026-05-18) | `9e602a2d-...` | v1.1 | partial type-c | generic validation-process echo | C: defer to fresh session | resolved v2.84 |
| **Fresh (2026-05-19)** | **`d18fa6db-...`** | **v1.1** | **AGREE** | **empty** | **A: apply via PK approval** | **resolved v2.85** |

Hypothesis from v2.84 honest limitations validated: fresh empirical P-set evidence delivered substantively cleanest review narrative.

---

## Final empirical state v2.85 (post-cleanup)

- event_count = 29 (smoke removed)
- case_count = 29 (smoke removed)
- active_source_count = 3 (reconciliation, health_check, manual)
- total_source_count = 3
- action_decision distribution: NULL=21, act_now=1, track=7, suppress=0, ignore=0, duplicate=0, defer_intentionally=0, done=0
- smoke_event_remaining = 0
- smoke_case_remaining = 0
- closure invariant: encoded by 0-row UPDATE pattern; not enforced write-side (CHECK/trigger deferred to future Wave)
- friction.event.source: enforced by FK→friction.source(source_code)
- friction.event grants: postgres ALL + service_role SELECT only
- friction.case grants: postgres ALL + service_role SELECT only

---

## Wave 0c scope (per PK 4-item directive, all applied)

1. ✅ FK hardening on `friction.event.source` — DROP `event_source_check` + ADD `event_source_fk` → `friction.source(source_code)`
2. ✅ Direct-write lockdown REVOKE — INSERT/UPDATE on `friction.event` + `friction.case` from service_role + PUBLIC + authenticated + anon (2 effective on service_role; 6 defensive no-ops)
3. ✅ `resolved_at`/`resolution_kind` backfill — legal-domain-only set `('suppress','ignore','duplicate')` per Path A; 0 rows affected as expected
4. ✅ Pre-flight grant capture — JSON snapshot captured via P-1 in D-01 evidence package

---

## Out of scope (deferred, carried unchanged)

- `case.dedupe_fingerprint NOT NULL` (carried from cc-0017b)
- `emission_rule_history` audit trigger (carried from cc-0017b)
- Expanding `case_action_decision_check` to include `'done'` (Amendment G nomenclature work; future lifecycle-domain expansion)
- CHECK/trigger enforcing closure invariant write-side
- Wave 0d triage/resolution SECURITY DEFINER functions — **NOW BLOCKING (P1 rank 1 next session)**
- Wave 0e audit history shadow tables
- vchecks.md V-B4 doc patch (P3 NEW v2.85)

---

## Lessons exercised v2.85

### Baseline reinforcement
- **L40 exercised** — close-the-loop UPDATE used `id` + `escalation_resolved_at` correctly (lesson from v2.84 internalised; no re-occurrence of column-name confusion).
- **L41 NOT exercised** — apply_migration used for DDL as required; execute_sql restricted to DML on m.chatgpt_review (exempt per session memory) + SELECTs + DO blocks. Cleanup DELETE routed through apply_migration. Cumulative v2.80-v2.85 = 6 (unchanged).
- **L46 Evidence Gate exercised** — fresh D-01 included verbatim P-set output + 9 enumerated known_weak_evidence items.
- **L58 applied 1×** — this 3-file atomic push_files close (after retry per L-v2.85-e).
- **L62 exercised** — full protocol on fresh D-01 cycle (fire → verdict → PK approval directive → action → close-the-loop). Clean.

### v2.85 NEW lesson candidates (5 first occurrences)

- **L-v2.85-a (HIGH-SIGNAL)** — V-check function calls must be validated against current deployed function signatures during brief authoring. First occurrence: V-B4 7-param signature in brief vs deployed 12-param signature; brief authored against memorial signature without runtime probe. Detection: V-B4 failed at runtime with `42883 function does not exist`. Recommendation: brief P-set should include `pg_get_function_identity_arguments` probe for any function called by V-checks.

- **L-v2.85-b** — Inline V-check rewrite (Path 1) is a valid response when brief defects surface during apply AND the migration itself is structurally correct. Distinction: brief defect ≠ migration defect; rollback is reserved for migration defects. First occurrence: V-B4 PK Path 1 directive preserving correct migration work while addressing documentation drift.

- **L-v2.85-c** — SECURITY DEFINER bypass empirically confirmed against direct-write REVOKE lockdown. Pattern: REVOKE direct writes from service_role + provide SECURITY DEFINER write functions. Verified end-to-end via V-B4 corrected call from service_role context after Section B REVOKE. Validates Amendment F (direct-write lockdown) design pattern + reusable for future write-restricted tables.

- **L-v2.85-d** — V-check cleanup of test artefacts post REVOKE-lockdown requires postgres-owner context (apply_migration) when DELETE was not granted to service_role. Cost: +1 entry in migration history. Trade-off acceptable for V-check hygiene. **Recommendation for Wave 0d**: provide `friction.purge_test_event(uuid)` SECURITY DEFINER helper to avoid one-off cleanup migrations in future V-check workflows.

- **L-v2.85-e** — Multi-file push_files commits can hit response-construction length budget when payloads are bulky (especially when sync_state + action_list + per-session file all reproduce the same v2.85 detail). Mitigation: keep per-file payloads compact + avoid duplicating per-session detail across pointer-index files. Detection: on suspected truncation, call `list_recent_commits` to verify HEAD; if no partial landed, clean repush with compacted payloads. First occurrence: v2.85 initial 4-way close attempt truncated mid-construction; clean repush succeeded.

### Carries (occurrence counts unchanged unless noted)
- **L-v2.78-a candidate**: 2 occurrences (unchanged). ELIGIBLE FOR BASELINE PROMOTION.
- **L47 candidate**: 1 occurrence (carry). Overlaps with L-v2.85-e detection step (commit-state check before retry).
- **L-v2.81-a candidate**: 2 occurrences (unchanged). PROMOTION ELIGIBLE per v2.83 PK directive.
- **L-v2.83-a candidate**: re-exercised 1× v2.85 (this 3-file push, with retry). Cumulative 6+ occurrences. STRONG PROMOTION CANDIDATE.
- **L-v2.84-a/b/c/d candidates**: 1 occurrence each (unchanged). L-v2.84-b empirically validated v2.85 (6 REVOKE no-ops + 2 effective all clean). L-v2.84-d possibly auto-resolved by L40 internalisation (no re-occurrence v2.85).

---

## v2.85 honest limitations

- **V-B4 brief defect was preventable** — vchecks.md V-B4 not updated when cc-0017b unified emit_event with 12-param signature. The function signature was available at brief authoring time; it just wasn't probed. L-v2.85-a addresses the pattern directly with the `pg_get_function_identity_arguments` probe recommendation.
- **Cleanup migration entry** — `cc_0017c_v_b4_smoke_cleanup` adds to migration history but does not alter friction.* schema state. Trade-off accepted for clean production data.
- **V-A3 PARTIAL via execute_sql is a structural limitation** of the V-check approach, not a defect. Brief explicitly anticipates this; true FK isolation testing would require postgres-owner context inside the apply_migration body (NOT desired — V-checks intentionally external to apply transaction).
- **Wave 0d now BLOCKING** — service_role no longer has UPDATE on friction.case post Section B REVOKE. Any future case state change (action_decision transitions, resolution markers, triage flows, reopen) must route through a SECURITY DEFINER function. No such function exists yet. Immediate next-session priority.
- **Dashboard PHASES at 38th consecutive deferral** (carried per PK directive item 5). Discipline call increasingly overdue.
- **22 outstanding close-the-loop UPDATEs** (21 historical CCH-locked + 1 T-MCP-05 meta) unchanged. v2.85's 1 new closed in-session — 0 net additions.
- **Memory cap 19/30** unchanged.
- **3-file atomic push_files** this close. **Initial attempt was truncated mid-construction** (new L-v2.85-e). Clean repush succeeded after `list_recent_commits` confirmed HEAD still at v2.84 close `2cdef219` — no partial landed. Production mutations had completed before the truncated push attempt, so repo + DB state remained consistent.
- **No `decisions.md` change** — Wave 0c apply is execution of v2.79-signed Friction Register Consolidation Plan; no new architectural decision introduced (consistent with PK directive scope discipline).
- **Brief content references `done_count_audit`** as defensive observation; the V-C1 query confirmed `done_count_audit=0` post-apply — observation honoured, no external CHECK domain expansion landed.
- **L-v2.85-e introduced this session** — to mitigate the same failure mode for future multi-file closes, the v2.85 pointer-index files (sync_state + action_list) are deliberately more compact than v2.84 equivalents, deferring full audit detail to this per-session file.

---

## Items closed v2.85

- **cc-0017c apply** (P1 rank 1 BLOCKED v2.84) → **CLOSED-APPLIED-WITH-VCHECK-CORRECTION** ✅ (migration version `20260519004545`)
- **Fresh D-01 fire** (review_id `d18fa6db-...`) → **CLOSED resolved** ✅ (escalation_resolved_at 00:54:22 UTC; resolved_by 'cc-0017c-v2.85-applied-with-vcheck-correction')
- **V-B4 inline rewrite** (PK Path 1 directive v2.85) → **CLOSED PASS** ✅
- **V-B4 smoke cleanup** → **CLOSED** ✅ (migration version `20260519005322`)
- **Friction Register Consolidation Plan Wave 0c apply (gate 10)** → **CLOSED-APPLIED** ✅
- **friction.* foundational schema Wave 0** → **COMPLETE** ✅ (Waves 0a + 0b + 0c all APPLIED)

---

## Items now blocking or newly active next session

- **Wave 0d triage/resolution SECURITY DEFINER functions** — P1 rank 1 NEW BLOCKING (was carry/anticipated). Blocking ALL future friction.case state-mutation workflows. Brief authoring required. Scope candidates: `friction.triage_case`, `friction.resolve_case`, `friction.reopen_case`, `friction.purge_test_event`.
- **vchecks.md V-B4 doc patch** — P3 NEW v2.85. Update V-B4 SQL to corrected 12-param signature in brief v1.2.
- **cc-0017c v1.2 doc patch candidate** — P3 EXPANDED v2.85. Date corrections + 3 D-01 references + V-B4 signature correction.
- **Wave 0e audit history shadow tables** — P3 carry.
- **1-week empirical observation window** — Friction Register Consolidation Plan gate 11 active 2026-05-19 → 2026-05-26. Wave 7 (cc-0015 friction-pool-view) execution gated.

---

*Last updated: 2026-05-19 Sydney morning v2.85 — cc-0017c APPLIED-WITH-VCHECK-CORRECTION; migrations `20260519004545` + `20260519005322`; D-01 review_id `d18fa6db-...` status=resolved; T-MCP-02 cum 76; state-capture exceptions 1; 5 new L-v2.85 candidates (a HIGH-SIGNAL, b, c, d, e); Wave 0d brief authoring NOW BLOCKING; Dashboard PHASES 38th deferral carried. Initial close push_files attempt truncated mid-construction; clean repush succeeded after list_recent_commits confirmed no partial landed (HEAD `2cdef219` v2.84 close). Production mutations consistent throughout.*
