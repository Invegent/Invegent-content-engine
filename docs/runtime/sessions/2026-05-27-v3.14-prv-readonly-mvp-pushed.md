# 2026-05-27 Sydney — v3.14: Platform Reconciliation View (PRV) read-only MVP committed + pushed (feature branch, unmerged, undeployed)

**Outcome:** The local read-only Platform Reconciliation View MVP is built, live-validated, committed, and pushed to a feature branch — **no merge, no PR, no production deploy.** Lane = CCD-PRV-001 → CCD-PRV-014 (CCD/laptop terminal).

## What shipped (local + feature-branch only)
- **Route `/recon`** in the content-engine `dashboard/` app (the cc-0013 Next.js 15 App Router, server-component-only reconciliation dashboard) — a read-only Platform Reconciliation View. Internal/unlinked (not in the nav).
- **Server-only direct Postgres transport** `dashboard/lib/db.ts`. Rationale (CCD-PRV-001 finding): the cc-0013 dashboard reads `op.*` via supabase-js REST, but schema `op` is **NOT** in the PostgREST exposed-schemas allowlist → every REST call returns PGRST106. A direct PG connection bypasses the REST schema allowlist. Safety belts: `import "server-only"`, session forced `default_transaction_read_only=on`, SELECT-only statement guard, sanitised errors (never logs the DSN).
- Reads the 3 cc-0012 `op.*` views — `op.v_reconciliation_summary`, `op.v_freshness_rollup`, `op.v_drift_rollup` — surfacing identity (client × platform), ICE-side (expected/matched/last evidence), platform-side (observed/observer health), mismatch state (drift type/severity, freshness status, attention), and timestamps. Reuses the cc-0013 `_components` primitives.
- **Live render validated** locally: HTTP 200, no error state; counts — summary trailing-7d expected 56 / matched 16 / late 0 / suppressed 12, on-time 28.57%; freshness 14 (client × platform) pairs; drift 39 findings / 26 actionable. **CCB visual validation PASSED.**

## DB (governed; least-privilege read role)
- **Migration `20260527011420_prv_readonly_login_role`** applied via apply_migration: created LOGIN role `prv_readonly` — NOSUPERUSER / NOCREATEDB / NOCREATEROLE / NOREPLICATION / NOINHERIT / NOBYPASSRLS; `GRANT CONNECT ON DATABASE postgres`; `GRANT USAGE ON SCHEMA op`; `GRANT SELECT ON ALL TABLES IN SCHEMA op` (+ default privileges). **No password in the migration** (set out-of-band by PK).
- D-01 `d86b7db8-552e-49b1-b86e-f6600224141c` (sql_destructive; agree/proceed; risk medium / confidence high / escalate false) + PK exact phrase `PK APPROVES PRV DB/RPC MUTATION`.
- Migration file backfilled **byte-exact** (file md5 `a5771ad7b2ceeaf2247d77e47d18d3a9` == DB `statements[1]`; 326 bytes; L-v3.06-a discipline; DB→repo drift closed same-session).
- **Close-the-loop on `m.chatgpt_review`:** `d86b7db8` → completed (stamped); prior partial/escalated `57fd738b` + `43140c7f` → `resolved` (resolved_by `superseded-by-refire-d86b7db8-pass (CCD-PRV-005)`, escalation_resolved_at set). Distribution moved escalated 49→47 / resolved 36→38; completed 56 unchanged; window probe confirmed exactly 3 rows touched, no collateral.

## Transport credential path (took 3 corrections; PK set the DSN out-of-band)
1. Direct host `db.<ref>.supabase.co` is IPv6-only from the laptop → `getaddrinfo ENOENT`.
2. Pooler shard `aws-0-ap-southeast-2` → Supavisor "tenant/user not found" (wrong shard).
3. DSN containing a `{ } < >` char → `Invalid URL` (pg's `new URL` parse).
- **Working DSN shape:** session pooler `aws-1-ap-southeast-2.pooler.supabase.com:5432`, username `prv_readonly.mbkmaxqhsohbtwsqolns`, URL-safe password. `.env.local` stays local-only + untracked (gitignored `dashboard/.gitignore:22:.env*.local`).

## Git
- Commit **`7287a1e00e78b019b1573fb5f4e33f1674a8ecf7`** — `feat: add local platform reconciliation view`. 6 files: `dashboard/lib/db.ts`, `dashboard/app/recon/page.tsx`, `dashboard/.env.example`, `dashboard/package.json`, `dashboard/package-lock.json`, `supabase/migrations/20260527011420_prv_readonly_login_role.sql`. typecheck / lint / build all exit 0.
- Pushed to **`origin/feature/platform-reconciliation-view-readonly`** (new branch; upstream set). **No PR, no merge** — `main` / `origin/main` remain `911d0d4`. Push-safety (CCD-PRV-012): no GitHub Actions / no Vercel project connected to `Invegent-content-engine` (3 Vercel projects = invegent-dashboard/-portal/-web, none for this repo) → the push triggered no CI/deploy.

## Hard stops honored
0 deploy · 0 merge · 0 PR · `op` NOT exposed to PostgREST · no `.env.local`/secret committed or printed · no UI polish beyond the validated MVP · cc-0015 Stage C/D/E untouched · deployed `invegent-dashboard` roadmap untouched.

## Deferred / next
- **Cosmetic polish:** KPI cards, colored freshness/severity badges, table width / horizontal-scroll.
- **Next read-only increments** (already grant-covered by `prv_readonly`, no new DB work): surface `op.v_per_client_rollup` + `op.v_per_platform_rollup`.
- **Production deploy = separate future PK gate:** `PK APPROVES PRV READ-ONLY PRODUCTION DEPLOY`. (The content-engine `dashboard/` was never deployed — cc-0013 Stage E deferred — so a deploy is net-new scope.)
- **Roadmap PHASES reflection:** the deployed roadmap is in the separate `invegent-dashboard` repo (Vercel-connected; editing+pushing = deploy); gated per D-FR-RECON-001 §4.4 — NOT touched here. The content-engine local `app/(dashboard)/roadmap/page.tsx` is a stale (2026-04-13 `329e0c4`), non-deployed legacy copy — intentionally not edited.
