# 2026-05-26 Sydney — v3.13.1: cc-0015 Stage A.5 recategorisation — 3 dashboard-UI cases moved to dashboard_ui

**Actor:** CCD (Claude Code / laptop terminal). Read-only candidate list → PK approval → guarded UPDATE migration → verification → repo backfill → docs closeout.
**Type:** 1 guarded UPDATE migration (production data mutation) + content-engine docs. **0 schema change, 0 deploy, 0 Repair-Board-v0 change.**
**Governance:** sql_destructive D-01 `bbe9a0fa-b872-4e2a-998f-743aa89036e0` (verdict=agree, risk=low, confidence=high, escalate=false, no pushback) + PK exact approval phrase; close-the-loop UPDATE on `m.chatgpt_review` (status=completed).

---

## Stage A.5 — APPLIED + verified (manual, PK-confirmed backfill recategorisation)

The Stage A.5 backfill (`operator_friction` → `dashboard_ui`), pending since Stage A, is now done.

- **Candidate prep (read-only, prior turn):** ran the brief's Stage A.5 diagnostic. `operator_friction` had only 6 cases total; 3 matched the dashboard-UI keyword set. Confirmed `friction.category` columns + that all target category codes are live. Surfaced the 3 candidates + reasoning + the 3 deliberate exclusions for PK review.
- **PK approval phrase:** "Approved: apply cc-0015 Stage A.5 recategorisation — move 53f3e533, 2cf0cd4f, b7369dc9 to dashboard_ui".
- **Applied:** `apply_migration` **`20260526222125_cc_0015_a5_recat_operator_friction_to_dashboard_ui`** — a single guarded `UPDATE friction.case SET category='dashboard_ui' WHERE category='operator_friction' AND case_id IN (3 ids)`. No `fn_triage_case`, no other table, no schema change.

### Cases moved (operator_friction → dashboard_ui)

| case_id | title (surface) |
|---|---|
| `53f3e533-ea5e-4040-b2a5-a966d18d6474` | "the top side submenu keeps moving, should be stable" (sidebar/nav chrome) |
| `2cf0cd4f-5907-4dfb-ace2-db8300c661c6` | "the overview on clients is very spaced out… make it compact" (Overview layout) |
| `b7369dc9-f0d1-4f70-903c-6a590c21a657` | "content studio - content series the rows should be condensed" (Content-Studio layout) |

### Correctly left behind (NOT moved)

- `22056313-…` "for the content series, we need to work on youtube" — content/pipeline feature request → stays `operator_friction`.
- `ce4442dc-…` "cc-0016 Stage B no-attachment smoke" — test artifact (`ignored`) → stays `operator_friction`.
- `66722f67-…` "cc-0016 Stage B attachment smoke" — test artifact (`ignored`) → stays `operator_friction`.
- `101935ba-…` "why do i need to approve drafts in overview" — approval-workflow/auto-approver logic (matched only on "overview") → stays `client_commitment`.

### Verification (read-only, post-apply)

| Check | Result |
|---|---|
| 3 targeted cases now `dashboard_ui` | ✅ (triage_state/action_decision unchanged — no collateral edits) |
| `dashboard_ui` count | 0 → **3** |
| `operator_friction` count | 6 → **3** |
| `client_commitment` | **16 unchanged** |
| `pipeline_integrity` | **22 unchanged** |
| total `friction.case` | **44 unchanged** (exactly 3 moved) |

### Governance + repo

- **D-01:** sql_destructive `bbe9a0fa-b872-4e2a-998f-743aa89036e0` fired **before** applying — verdict `agree`, risk `low`, confidence `high`, `escalate=false`, no pushback. No L46 escalation classification needed (clean agree/proceed). Close-the-loop UPDATE on `m.chatgpt_review` → `status=completed`, `resolved_by='PK (approval phrase) + CCD (apply)'`, `escalation_resolved_at` set (1 row).
- **Repo (L-v3.06-a same-session):** migration file backfilled verbatim to `supabase/migrations/20260526222125_cc_0015_a5_recat_operator_friction_to_dashboard_ui.sql` — body md5 `5fdb7de72b362c1cdf0cbb8400fbe8b0` (LF, no trailing newline) **== DB `statements` md5** (byte-exact; only a cosmetic trailing newline added). Committed + pushed to content-engine `main` **`41576f8`** (`6623504..41576f8`).
- **Rollback (if ever needed):** `UPDATE friction.case SET category='operator_friction' WHERE category='dashboard_ui' AND case_id IN (the same 3 ids)` — prior value known = `operator_friction` for all 3.

### End result

The 3 dashboard-UI cases now surface under the live **Dashboard UI** saved view on `/operations?category=dashboard_ui` (Stage B frontend) — Stage A.5 + Stage B frontend now connect end-to-end.

## cc-0015 state after v3.13.1 (NOT complete)

| Stage | Status |
|---|---|
| A — schema | APPLIED + verified |
| **A.5 — recategorisation** | **APPLIED + verified (this session)** |
| F — operator help copy | SHIPPED + production smoke PASS |
| B — RPC (`fn_recent_cases` filters) | APPLIED + verified |
| B — frontend (`/operations` filters + saved views + category-desc hydration) | DEPLOYED to production |
| C — batch resolution | HELD |
| D — pool dashboard widget / status-strip counts | HELD |
| E — pool session tracking UI | HELD |
| Source filter + sort | DEFERRED |
| FAB category-description hydration | DEFERRED (the `/operations` triage form is done) |
| G — process doc (`ICE-PROC-002`) | not authored |

**cc-0015 is NOT complete.**

## Hard stops / scope (honored)

Documentation + the single approved guarded UPDATE only · 0 `fn_triage_case` · 0 schema change · 0 deploy · 0 Repair-Board-v0 change · D-01 fired before the mutation · candidate batch small + unambiguous (3) · `dashboard_ui` gate confirmed present · cc-0015 not claimed complete.
