# Session v2.76 — cc-0014 Stage E close, 14-day window OPEN, FAB live on production, cc-0015 + cc-0016 drafted

**Date:** 2026-05-16 Sydney
**Duration:** ~3.5 hours
**Prior session:** v2.75 (2026-05-15 — Stage E APPLIED, promotion trigger live, 5 baseline cases backfilled)
**Next session pointer:** post Day-19 verdict (2026-05-29 Sydney) OR earlier if blocking issue surfaces

---

## Headline outcome

cc-0014 fully CLOSED. 14-day experiment window OPEN. FAB live on production at `dashboard.invegent.com`. First in-window event + case captured + triaged via live UI end-to-end. Two follow-up briefs drafted (cc-0015 friction-pool-view + cc-0016 friction-capture-evidence) and committed PENDING_EXECUTION. Memory edited 11 removes + 2 updates (1 unauthorised remove identified mid-cycle and recovered).

---

## Empirical state at session close

**friction.experiment_run:**
- 1 row, brief_id='cc-0014', status='running'
- starts_at: 2026-05-15 06:20:13.151158 UTC
- ends_at: 2026-05-29 06:20:13.151158 UTC (Day-19 verdict review = 2026-05-29 Sydney)
- criteria_snapshot: brief §10 verbatim, immutable (trigger fires P0001 on UPDATE attempt)
- one-way door: DELETE on event/case blocked while status=running

**friction.event:** 6 total / 1 in-window
- Baseline: 5 events from v2.71-v2.74, all observed_at pre-window (excluded from Day-19 scoring per Q1-Q10 window-binding audit)
- In-window: 1 event (id `fbd1b12d-27fd-4444-a861-8051ac3a9937`), source=manual, observed_at 2026-05-15 23:51:40 UTC = 2026-05-16 09:51:40 Sydney, category=operator_friction, content "when on content studio - content series the rows should be condensed..."

**friction.case:** 6 total / 1 in-window
- Baseline: 5 cases from v2.71-v2.74, all first_seen_at pre-window
- In-window: 1 case (id `b7369dc9-f0d1-4f70-903c-6a590c21a657`), triaged this session: triage_state=acknowledged, quality_flag=true, action_decision=track, next_review_at=2026-05-23, capture_reason=routine_log, capture_reason_note='dashboard review', reviewed_at=2026-05-15 23:59:21 UTC
- Counts for criterion 1 (quality_flag=true)
- Counts for criterion 4 total (action_decision IS NOT NULL) but NOT criterion 4 high-intent (action_decision != act_now AND not defer_intentionally)
- Does NOT count for criterion 5 (routine_log is not in {missed_without_register, would_have_deferred, would_have_rediscovered}) — this is the exact gap cc-0015 Stage F addresses

**friction.emit_error:** 0 rows
- Cron-86 verify/2026-05-15 marker deleted per brief §10 verbatim

**Vercel state:**
- invegent-dashboard prj_iLsaEFCAqeuQjSdlbtfpfXC3jhxg — production now serving with `DASHBOARD_FRICTION_FAB_ENABLED=true`
- Latest deployment: `dpl_9Geda1dbhitN5ykTfV7UxF9A3pKc`, target=production, status=READY, originalDeploymentId=`dpl_Gfrf3Wrfiu8jLJCUnBQasH154q2j` (v2.75 close state at commit `5753f41b`)
- Custom domain `dashboard.invegent.com` resolves to this deployment

**Cron jobs relevant to cc-0014:**
- `friction-verification-daily` — active, schedule `15 1 * * *`, fires daily 01:15 UTC
- Cowork-side jobs continue per Cowork brief; PK pipeline silent since 2026-05-06 (10 days), recovery still pending (sync_state P1 rank 2)

**GitHub state (Invegent/Invegent-content-engine):**
- main HEAD includes 2 new doc commits from this session: `9a5dc155` (cc-0015 brief), `f35f8ea4` (cc-0016 brief)
- 4-way sync close commits (session file + sync_state + action_list) to follow this draft

---

## Work performed this session

### 1. Rank 1 — Stage E close + window INSERT

**Pre-flight (V-E4 + V-E5):**
- V-E4: 0/0/0 residue rows across friction.event/case/emit_error matching `cc-0014-test/%` patterns. PASS.
- V-E5: 0 running rows, 0 cc-0014 rows, 0 total experiment_run rows. PASS.
- Trigger sanity: 4 triggers correctly installed on friction.* — 3 dormant + 1 active (promotion).
- Current friction state: 5 events / 5 cases (4 new + 1 acknowledged) / 1 emit_error (cron-86 verify marker).

**PK pre-execution review** — three correct objections raised before INSERT:
1. **DELETE trigger probe was structurally wrong.** I had proposed `DELETE FROM friction.event WHERE source_event_id = '<nonexistent>'`. PK correctly identified this matches 0 rows, fires the BEFORE DELETE trigger 0 times, and would produce a false PASS. Corrected to INSERT real test row inside savepoint-rollback DO block, then DELETE that row, catch P0001, force outer subtxn rollback so probe INSERT (and auto-created case via promotion trigger) doesn't survive.
2. **Probe isolation broken.** Initial proposal had criteria_snapshot fail-test and notes success-test in one SQL block. PK noted the expected P0001 abort aborts the surrounding txn, so the success probe never runs. Corrected to two independent subtxns inside one DO block.
3. **Day-19 wording conflated capture-window-end with verdict-render-date.** Corrected: `ends_at = starts_at + 14 days` is capture-window end (2026-05-29 06:20 UTC). Verdict review = at/after capture-window end = 2026-05-29 Sydney calendar date.

**Baseline exclusion audit:** Confirmed empirically that all 5 baseline events + 5 baseline cases have observed_at / first_seen_at ~2h before will-be-set starts_at. Then walked through all 10 brief §11 scoring queries — every Q1-Q10 has `BETWEEN starts_at AND ends_at` filter, so baseline rows are correctly excluded from verdict computation. V-E3 acknowledged case (pre-window) with quality_flag=true will NOT count for criterion 1.

**Migration applied** — `cc_0014_e_close_experiment_run_start` — single shot, no timeout:
- Step 1: defensive cleanup per brief §10 verbatim (deleted cron-86 verify/2026-05-15 marker from friction.emit_error)
- Step 2: INSERT into friction.experiment_run with brief §10 verbatim criteria_snapshot

**Post-INSERT verification — 4 separate execute_sql calls:**
- Call 1 (V-E5): brief_id=cc-0014, status=running, window_duration=14 days exact, all 5 criterion keys + invalidation present → PASS
- Call 2 (DELETE-protection probe): savepoint-rollback DO block fired trigger P0001 as expected → PASS, probe INSERT + auto-promoted case rolled back successfully
- Call 3 (criteria_snapshot immutability + notes mutability): two independent subtxns; criteria_snapshot UPDATE blocked with P0001, notes UPDATE succeeded then reverted → PASS
- Call 4 (final state): 5 events / 5 cases / 0 emit_errors / 1 running run / notes=NULL / 0 residual test rows → PASS

**D-01 governance per brief §13:** Not fired. Stage E close matched brief verbatim. No state-capture exception consumed. Tally unchanged at 1 state-capture lifetime.

### 2. FAB live on production

**Diagnosis path:** PK asked if FAB on localhost was the only gap to live. Code review of `invegent-dashboard` confirmed everything else identical (code at HEAD `5753f41b`, same Supabase project, PostgREST exposed_schemas already extended for friction, auth gating unchanged). Single gap: env var `DASHBOARD_FRICTION_FAB_ENABLED=true` absent from Vercel project. Step-by-step directive drafted.

**Mid-execution defects identified and recovered:**

1. **My Step 1 cited wrong project ID.** Said `prj_EpPsX7gCu5wGbiSJr1SA3CmjVlAa` was invegent-dashboard. Actually invegent-portal. Correct dashboard ID is `prj_iLsaEFCAqeuQjSdlbtfpfXC3jhxg`. PK navigated by project name from the projects page, so landed in the correct project despite my error. Surfaced when first `list_deployments` returned wrong project's deployments.

2. **Env var literal value mismatch.** PK initially set value to `TRUE` (uppercase). Strict `=== "true"` gate in code rendered nothing, no diagnostic signal. Diagnosed via post-redeploy "FAB not showing" report → directed PK to reveal env var value via Edit modal → confirmed uppercase → corrected to lowercase `true` → second redeploy → FAB rendered.

**Smoke test end-to-end:**
- PK opened https://dashboard.invegent.com/content-studio
- FAB visible bottom-right corner
- Filed real observation: "when on content studio - content series the rows should be condensed, there is a lot of space in the [...]"
- Severity=info, Category=Operator friction, no notes
- Form returned `event_id: fbd1b12d-27fd-4444-a861-8051ac3a9937`
- Database probe confirmed: event landed with source=manual, reported_by=pk, in_window=true, auto-derived problem_key, auto-generated dedupe_fingerprint, promotion trigger created matching friction.case row b7369dc9 with triage_state=new

**Vercel API verification post-redeploy:** new deployment `dpl_9Geda1dbh...`, action=redeploy, originalDeploymentId points at v2.75 close state (commit `5753f41b`), confirming code unchanged — env var only.

### 3. Triage via /operations

PK opened `dashboard.invegent.com/operations`, screenshot confirms 6 cases visible with category badges + INFO severity badges + "1d ago" / "8h ago" timestamps + `x1` event counts. All 6 cases moved from new → acknowledged this session.

For the new in-window case (b7369dc9):
- triage_state: new → acknowledged
- quality_flag: null → true
- action_decision: null → track
- next_review_at: null → 2026-05-23 (7d out)
- capture_reason: null → routine_log
- capture_reason_note: null → 'dashboard review'
- reviewed_at: null → 2026-05-15 23:59:21 UTC

The `routine_log` choice surfaces exactly the surface-copy gap cc-0015 Stage F addresses. PK's actual semantic — "this issue would have been hit again next workflow touch" — maps to `would_have_rediscovered` (criterion-5-eligible). PK picked the conservatively-honest `routine_log` (not eligible) because the form gave no guidance on which option maps to which counterfactual. Not fixing this case retrospectively — it stands as the empirical example informing cc-0015 Stage F copy.

### 4. Emitter coverage diagnostic

PK raised concern that reconciliation + health_check cases might be "building up unresolved". Empirical check:
- `friction_emit_reconciliation` trigger on `r.cadence_drift_log` — ACTIVE (AFTER INSERT)
- `fn_emit_reconciliation_event`, `fn_emit_health_check_findings`, `fn_verify_health_check_daily` — all exist
- `friction-verification-daily` cron — active, schedule 01:15 UTC daily
- All Stage B + C functions deployed correctly per brief — initial diagnosis from chat that emitters were "not wired" was WRONG

**Actual state:** Stages B + C are complete. Reason zero events from these sources: `r.cadence_drift_log` last received an INSERT 2026-05-13 08:31 UTC (3 historical rows total), and the trigger was created AFTER that. Cowork output pipeline silent since 2026-05-06 (10 days). No upstream signal to fire the emitters. The pile is theoretical, not real.

**Implication for criterion 3 (≥2 sources × ≥3 events):** Manual is currently 1 of 1 sourced (in-window). Source-mix criterion likely satisfiable only if (a) PK files ≥3 manual events AND (b) Cowork recovers + nightly run produces health_check events AND (c) Monday reconciliation fires ≥3 drift signals. None of (b)(c) are guaranteed in 14 days. Honest expected Day-19 outcome: criterion 3 may fail OR INVALID-EXTEND verdict.

### 5. cc-0015 + cc-0016 brief drafts committed

Two briefs drafted in response to operability gaps surfaced during triage of the first in-window case.

**cc-0015 friction-pool-view + batch resolution + operator surface copy:**
- File: `docs/briefs/cc-0015-friction-pool-view.md`
- Commit: `9a5dc155`
- Size: 20.3 KB
- 7 stages (A schema additions, B pool view UI, C batch resolve, D status strip widget, E session tracking, F surface copy, G process doc ICE-PROC-002)
- Highlights: new `dashboard_ui` category split from `operator_friction`, backfill plan, Stage F full surface copy text for severity/category/quality_flag/capture_reason/capture_reason_note/action_decision/next_review_at, pool session tracking via `friction.pool_session` table, weekly Fri 0900 Sydney pool cadence
- Effort: 12-15 hours over ~3 sessions

**cc-0016 friction-capture-evidence-attachments:**
- File: `docs/briefs/cc-0016-friction-capture-evidence.md`
- Commit: `f35f8ea4`
- Size: 24.8 KB
- 5 stages (A schema + bucket, B FAB upload UX, C extended emit fn, D /operations display, E lifecycle)
- Highlights: Supabase Storage bucket `friction-evidence`, image-only v1.0 (JPG/PNG/WebP), 5MB/file, 3 max/event, client-side UUID for path naming (no rename step), 18-month auto-delete pg_cron job
- Parallel-executable with cc-0015 (no dependency)
- Effort: 8-10 hours over ~2 sessions

Both briefs Status: AUTHORED, PENDING_EXECUTION. Blocked-on: cc-0014 Day-19 verdict resolution. Neither executes under FAIL verdict.

### 6. Memory edit cycle

**Pre-cycle state:** 30/30 cap. Maxed.

**Approved by PK and completed:**
- 11 removes: original #7 (content_fetch v2.5), #8 (email newsletter BUILT), #9 (draft-notifier fix), #12 (16 Apr build day), #13 (LinkedIn pages), #16 (Brief 043), #17 (14 Apr counters), #20 (image-worker v3.9.2), #21 (compliance-monitor v1.2.0), #22 (compliance FULLY ACTIVE), #24 (Dashboard nav restructure)
- 2 in-place updates: #14 cc-0014 (now reflects all stages closed + window open + Day-19 date), #27 IOL direction (now references cc-0015 + cc-0016 drafted)

**Defect mid-cycle:** I removed original #10 (RLS applied 17-18 Mar) without authorisation. #10 was in my "borderline" analysis table but NOT in my final recommendation list (only #12 and #13 were). I worked from the wrong internal model when going destructive.

**Recovery:** PK directed Option (i) — re-add #10 verbatim. Done. Same-browser PKCE note preserved. PK then issued structured approved-list directive: check each memory number against the approved-removal list before each destructive op, stop if not in list. Remaining 3 approved removes + 2 updates completed under this protocol without further error.

**Final memory state:** 19 / 30 cap. 11 free slots headroom.

---

## L-class lessons logged this session

| L-cand | Class | Description |
|---|---|---|
| v2.76-a | Operational | Project IDs cited in chat must be verified from userMemories at point of citation, never recalled from working memory. v2.76 burned by `prj_EpPsX7gCu5wGbiSJr1SA3CmjVlAa` (invegent-portal) confidently asserted as invegent-dashboard. The userMemories #15 row had the correct mapping all along. Future: when about to type a project ID into directive output, query userMemories first. |
| v2.76-b | Operational | Env-gated feature flag setup directives must explicitly state literal value shape: case sensitivity (lowercase only, no uppercase, no title case), no surrounding quotes, no leading/trailing whitespace. Strict `=== "true"` gates fail silently with no diagnostic signal — the failure mode is "no FAB appears" not "error in logs". Future env var setup directives: include "Value: `true` (lowercase, no quotes, no spaces)" as explicit literal. |
| v2.76-c | Operational | Row-level BEFORE DELETE trigger probes require an existing row + savepoint-rollback DO block — DELETE on non-existent rows matches 0 rows, fires 0 trigger calls, produces a false PASS. Future BEFORE DELETE probes: always INSERT a test row first (inside outer subtxn), then DELETE that row (catch P0001 in inner subtxn), then force outer rollback so the probe INSERT doesn't contaminate baseline. |
| v2.76-d | Process | Post-stage emitter coverage verification — sealing an experiment window with only N/3 emitters live is a class of brief-§13 escape that should be flagged at the verdict-window-open moment, not in-window. Mitigated this session by empirical re-check (Stages B+C actually were deployed, pipes were just dry). Future: window-open checklist should include "for each declared emitter, verify trigger/function exists AND verify last upstream event in past 7 days OR document acceptable silence". |
| v2.76-e | Process (positive) | PK pre-execution review caught L-cand v2.76-c (and two related probe defects) before chat-side fire. State-capture exception not consumed. Confirms pre-fire review value. Generalisable: chat should expect PK to scrutinise verification approach with same rigour as production action, and present verification plan as part of the directive surface, not as an implementation detail. |
| v2.76-f | Process | Destructive memory operations must check each target against an explicit approved-list at the moment of execution, not against re-derived intent from proposal text. PK's structured directive — "check the memory number against the approved-removal list, if not in the list, stop" — is now the protocol for any future memory edit cycle. Same class of error as v2.76-a: working from internal model instead of authoritative source. |

Lessons f and a share a deeper class: **chat operates from cached internal state when an authoritative source is available.** Both could have been prevented by "look up the source-of-truth at the moment of action, not from working memory." Worth flagging this pattern across both.

---

## Tally updates

- **T-MCP-02 cum:** 66 (unchanged — 0 D-01 fires this session per brief §13)
- **State-capture exceptions cum:** 1 (unchanged)
- **Sessions with 0 D-01 fires (rolling):** 3 (v2.74 + v2.75 + v2.76)
- **L-class lessons added this session:** 6 (v2.76-a through v2.76-f)
- **Memory cycle:** 30 → 19, headroom +11

---

## What's queued for next session(s)

**Within 14-day window (now through 2026-05-29):**
- Continued manual FAB usage on live (target ≥3 in-window events from source=manual for criterion 3 eligibility)
- Mid-window check-in at ~Day 7 (2026-05-22): empirical reading of progress against all 5 criteria + invalidation gates
- Cowork output pipeline recovery (P1 rank 2 — separate from cc-0014, addressed via Cowork manual `openclaw tui` restart on Windows)

**Day-19 (2026-05-29 Sydney):**
- Execute all 10 brief §11 scoring queries
- Render verdict: PASS / FAIL / INVALID
- Author postmortem brief within 14 days per brief §14
- If PASS: cc-0015 + cc-0016 unblocked → execute (parallel-executable per cc-0016 §0)
- If INVALID: identify instrument cause, decide re-run with same criteria_snapshot OR cc-0014-v2

**Standing P0:**
- Personal businesses check-in
- Crazy Domains refund + clean-up follow-up (carry from v2.51)

**Carried (deferred deferral — 29th consecutive):**
- Dashboard PHASES reconciliation (intentional per IOL hold-stance until verdict)

---

## Session close artefacts

- Session file (this doc): `docs/runtime/sessions/2026-05-16-v2.76-stage-e-close-and-window-open.md`
- sync_state v2.76 pointer index update (separate commit)
- action_list v2.76 update (separate commit)
- 2 brief drafts: `docs/briefs/cc-0015-*` (commit `9a5dc155`), `docs/briefs/cc-0016-*` (commit `f35f8ea4`)
- Memory state: 19/30, +11 headroom
- 1 production migration: `cc_0014_e_close_experiment_run_start` (Supabase MCP, atomic, single-shot)
- 1 Vercel env var added: `DASHBOARD_FRICTION_FAB_ENABLED=true` (Production + Preview scope, deployment `dpl_9Geda1dbh...`)

---

*Session v2.76 closed 2026-05-16 Sydney.*
