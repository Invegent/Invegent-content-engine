# v2.88 — cc-0017e Wave 0e v1.0 brief AUTHORED

**Date:** 2026-05-19 Sydney evening
**Predecessor:** v2.87 (cc-0017d v1.1 doc patch CLOSED at `f0367405c04dd21e4e08bc4c8beebd4d77635229`)
**Status:** Brief authoring close — sync close in progress at time of writing
**Agent:** Claude (chat)

---

## 1. Outcome

cc-0017e Wave 0e v1.0 multi-file brief **AUTHORED-PENDING-APPLY** at commit chain `8502fc49 → 1659b293 → d349bdfe`. 8 files landed across 3 commits per L-v2.85-e split-commit mitigation. Brief covers: `friction.case_history` shadow table + Wave 0d mutation function patches for in-function history INSERTs + `fn_triage_case` external-compatibility patch + 8-row acknowledged legacy backfill + V-Z3 residue cross-check convention codification.

**Hard stops adhered v2.88:** 0 production mutations, 0 `apply_migration` calls, 0 D-01 fires, 0 Wave 0f scope creep.

**Open anomaly surfaced at sync-close verification:** `preflight-pset.md` §P3.2 and `d01-postapply-deferred.md` §3-4 reference `m.chatgpt_review.review_id` and `proposal_text`. **Actual column names are `id` and `proposal`** (verified via `information_schema.columns` probe at sync-close). **v1.1 doc patch required BEFORE any D-01 fire or apply.**

---

## 2. Commit chain

| # | SHA | Parent | Files | Purpose |
|---|---|---|---|---|
| 1 | `8502fc49a0d981e95f8fed6bd7c3ece438fc669c` | (pre-v2.88 HEAD) | 1 | Main brief (`create_or_update_file`) |
| 2 | `1659b293da007ced41a6d0b08def1061dd38a414` | `8502fc49` | 4 | Substantive sub-files (atomic `push_files`) |
| 3 | `d349bdfecc1629dbaeca0d5cea579e69d9d03461` | `1659b293` | 3 | Process sub-files (atomic `push_files`) |

Total: 8 files, ~115 KB documentation.

## 3. File inventory

**Main brief** (parent `docs/briefs/`):

| File | Blob SHA | Size (B) |
|---|---|---|
| `cc-0017e-friction-case-history-and-compat.md` | `a50e26e663dd6725044df0aaebd9e9672797ab88` | 21,698 |

**Sub-files** (`docs/briefs/cc-0017e/`):

| File | Blob SHA | Size (B) |
|---|---|---|
| `migration-sql.md` | `d1946d7afb64f0a656f3469d2aee5530fceb58dd` | 27,203 |
| `vchecks.md` | `eef59ec5b2c38b3a7fac3c2aa565ecfc9f5a1ef9` | 19,661 |
| `risks-and-grants.md` | `b52f1d8b8cf9d4aff5d2afdce0002c7e86fe2f91` | 9,346 |
| `preflight-pset.md` | `268f973b416ee87ff73201e2c7b9b096cccf0e2d` | 7,996 |
| `hardstop-rollback.md` | `1e3ddd072df43a72373396203352f42bb6452e83` | 9,077 |
| `d01-postapply-deferred.md` | `237ae8986302150e14423db2737945a685af4cdb` | 9,113 |
| `lessons-metadata-changelog.md` | `e5ffac0fbff076da68fa9a948dc065a30b2c7fa5` | 10,992 |

All 8 blob SHAs cross-verified between commit response (immediately post-commit) and `list_directory` (at sync-close verification). No drift.

---

## 4. PK directive scope (locked v2.88)

| ID | Item | Disposition |
|---|---|---|
| **A** | `friction.case_history` shadow table | IN ✅ — DDL designed per `emission_rule_history` in-schema precedent (7-column shape: history_id uuid PK, case_id uuid NN FK, changed_at timestamptz NN, changed_by text NN, change_kind text NN, before_row jsonb, after_row jsonb) |
| **C** | `fn_triage_case` compatibility patch | IN ✅ — defensive prospective protection for external callers (Edge Functions, PostgREST RPC consumers, app code). P6 corrected P3's false-positive in-DB caller finding. |
| **D** | 8-row triaged_at/triaged_by backfill | IN ✅ — exactly 8 acknowledged legacy cases confirmed via P4 (all with NULL triaged_at AND NULL triaged_by AND reviewed_at SET). Source: `COALESCE(reviewed_at, updated_at)`; sentinel: `'legacy_backfill'`. |
| **H** | V-Z3 residue cross-check convention | IN ✅ — codified inline in `cc-0017e/vchecks.md` §X as canonical statement. No new process doc. |
| **A-extended** | All 5 cc-0017d mutation functions patched to INSERT case_history | IN ✅ — `triage_case`, `resolve_case`, `reopen_case`, `mark_duplicate`, `record_first_view` each get explicit `INSERT INTO friction.case_history` post-UPDATE. Trigger-based pattern explicitly rejected per P5b (mirrors emission_rule_history). `record_first_view` skips INSERT on idempotent path. |
| B | Operator-action audit events via emit_event | OUT/DEFER — Wave 0f candidate |
| E | fn_triage_case deprecation/rename | OUT/DEFER — requires external-caller inventory first |
| F | Open/resolved write-side CHECK | OUT/DEFER — requires invariant-formalisation gate |
| G | emit_event rule relaxation/test-mode bypass | OUT/DEFER — direct-INSERT fallback (cc-0017d v1.1 Drift 1) is sufficient |

---

## 5. Empirical preconditions (8 read-only probes v2.88)

| Probe | Target | Key finding |
|---|---|---|
| P1 | `friction.*` table inventory | 9 tables; `emission_rule_history` confirmed as in-schema shadow precedent |
| P1b | `friction.case` + `emission_rule_history` + `emission_rule` columns | 28 / 7 / 12 columns; triaged_at + triaged_by columns confirmed at positions 22, 23 |
| P2 | `friction.*` function inventory + triage-write flag | 19 functions; only `triage_case` + `mark_duplicate` write triaged_at/by; `fn_triage_case` body does NOT |
| P3 | `fn_triage_case` caller substring match | 1 hit on `resolve_case` — **false positive corrected by P6** (HINT-string match, not code-path call) |
| P4 | triage_state distribution + NULL-triage breakdown | 21 new + 8 acknowledged = 29 total (baseline preserved); 8 backfill targets confirmed |
| P5a | `fn_triage_case` body | Sets reviewed_at=now() + updated_at=now() but never touches triaged_at/triaged_by |
| P5b | Trigger landscape on case + event + emission_rule | Only DELETE-prevention triggers on case + event; emission_rule has zero triggers → in-function INSERT is the canonical shadow-table pattern |
| P6 | cc-0017d mutation function bodies (5 functions) | All use `friction."case"%ROWTYPE` (L-v2.86-c) and single-`%` RAISE placeholders (L-v2.86-a); HINT-string false positive corrected |

---

## 6. Open design decisions (PK to override at D-01 or v1.1)

7 design decisions defaulted at authoring time. PK may override at D-01 cycle or via v1.1 patch. Captured in main brief §9 + d01-postapply-deferred.md.

1. **case_history change_kind enum:** `{triage, resolve, reopen, mark_duplicate, first_view, compat_legacy_triage, backfill}` — no `'create'` in v1.0
2. **fn_triage_case patch arg surface:** Add `p_actor text DEFAULT NULL` as 11th arg (signature-compatible)
3. **Patch transition condition:** triaged_at/by set only on first 'new'→non-'new' AND current value IS NULL (idempotent)
4. **Backfill triaged_by sentinel:** `'legacy_backfill'`
5. **Backfill triaged_at source:** `COALESCE(reviewed_at, updated_at)` — resolves to reviewed_at for all 8 targets
6. **Scope of mutation-function history writes:** ALL 5 cc-0017d functions + fn_triage_case
7. **V-Z3 convention codification location:** in-brief at `cc-0017e/vchecks.md` §X — no new process doc

---

## 7. Apply prerequisites (carry from v2.87)

- cc-0017d Wave 0d CLOSED-APPLIED-WITH-VCHECK-CORRECTION (v2.86) ✅
- cc-0017d v1.1 doc patch CLOSED (v2.87 at `f0367405`) ✅
- cc-0017c lockdown applied (v2.85) ✅
- friction schema event_source_fk in place ✅
- service_role REVOKE on event+case for direct DML in place ✅

**Apply gating v2.88+:** cc-0017e v1.1 doc patch (m.chatgpt_review column-name correction) BLOCKS any D-01 fire / apply_migration. Once v1.1 patched, apply session can proceed under PK directive.

---

## 8. Counters end-of-session v2.88

| Counter | Value | Delta from v2.87 |
|---|---|---|
| Memory cap | 19/30 | unchanged |
| T-MCP-02 cumulative | ~85 | unchanged |
| State-capture exceptions cumulative | 1 | unchanged |
| Close-the-loop UPDATEs outstanding | 22 | unchanged |
| Production mutations v2.88 | 0 | — |
| `apply_migration` calls v2.88 | 0 | — |
| D-01 fires v2.88 | 0 | — |
| Wave 0f scope creep | 0 | — |
| Dashboard PHASES consecutive deferrals | 41 | +1 (was 40) |
| Gate 11 (1-week observation 2026-05-19 → 2026-05-26) | Day 1 of 7 | unchanged (same calendar day) |
| friction.* baseline (total_cases / total_events) | 29 / 29 | unchanged ✅ verified |
| friction.* tables | 9 | unchanged (case_history not yet applied) |
| Acknowledged legacy backfill target count | 8 | unchanged ✅ verified |

---

## 9. Read-only probes consumed v2.88

| Phase | Probes |
|---|---|
| Authoring | P1, P1b, P2, P3, P4, P5a, P5b, P6 (8 SELECTs) |
| Pre-commit verification | (none — used `list_recent_commits` + `list_directory`) |
| Post-commit verification round 1 | m.chatgpt_review column-name probe; D-01 fire query; baseline count check |
| Post-commit verification round 2 (after identical directive re-receipt) | D-01 fire query + baseline count check (combined) |

Total v2.88 read-only SQL probes: ~12. Zero DML. Zero DDL. Zero `apply_migration`.

---

## 10. Lesson candidates surfaced v2.88

Captured in `docs/briefs/cc-0017e/lessons-metadata-changelog.md` for promotion review at next lesson cycle.

| Candidate | Source | Rationale |
|---|---|---|
| **L-v2.88-a** | P3/P6 correction | HINT-string substring matches in `pg_proc.prosrc` queries produce false-positive caller hits; body inspection required to disambiguate code-path vs hint-string vs comment vs error-message-template |
| **L-v2.88-b** | V-Z3 codification (item H) | Shadow-table operation alignment cross-check: operations exercised in V-D positive smoke must equal shadow-table rows added, grouped by operation type — catches silent INSERT failures |
| **L-v2.88-c** | preflight-pset P1 design | Authoring-time probes are snapshots; pre-apply P-set MUST re-verify critical probe results to detect between-authoring-and-apply state drift |
| **L-v2.88-d** | P5b finding | For shadow tables in locked-down schemas, in-function INSERT pattern is preferred over trigger-based history population — captures semantic change_kind + aligns with SECURITY DEFINER pattern |

**L-v2.85-e re-applied:** 3rd consecutive occurrence (v2.86 + v2.87 + v2.88). 1+2 split commit pattern for sync close. **Promotion-confirmed per the "after one more cycle" v2.87 note.**

**No new lesson candidates from anomaly surface** (the m.chatgpt_review column-name error is a one-off authoring oversight; column-naming-probe-before-template-write is implicitly covered by L-v2.85-a HIGH-SIGNAL "function signature probe at brief authoring" — same principle extended to table columns).

---

## 11. Anomaly disposition — cc-0017e v1.1 doc patch candidate

**Defect:** `preflight-pset.md` §P3.2 and `d01-postapply-deferred.md` §3-4 reference:
- `m.chatgpt_review.review_id` → actual column is `id`
- `m.chatgpt_review.proposal_text` → actual column is `proposal`

**Detection method:** Sync-close verification SQL `SELECT review_id, status, verdict, ... FROM m.chatgpt_review` returned ERROR 42703 (column does not exist). Forced schema probe of `information_schema.columns` → corrected column names captured.

**Functional impact at v1.0:** Zero — brief is documentation only; no SQL executes against m.chatgpt_review until D-01 fire (apply session).

**Impact if uncorrected at apply time:** D-01 idempotency check SQL (P3.2) would fail with ERROR 42703 before D-01 fires. Close-the-loop UPDATE template (§4) would fail. Apply session would surface this within first few queries — recoverable but disruptive.

**Disposition recommendation:** **v1.1 doc patch** parallel to cc-0017d v1.1 pattern. Scope: 2 files corrected (`preflight-pset.md`, `d01-postapply-deferred.md`). Optional 3rd file: `lessons-metadata-changelog.md` to add v1.1 changelog entry. **MUST land BEFORE any D-01 fire for cc-0017e apply.**

**Severity:** LOW. Documentation correction with clear forensic trail. No production impact at AUTHORED state.

**Path to apply session:** PK directive → cc-0017e v1.1 doc patch (2-3 file commit) → apply session opens → preflight P-set re-verifies → D-01 fires with correct schema → apply_migration → V-check matrix.

---

## 12. Next session priorities (v2.88)

| Rank | Item | Priority | Gating | Next action |
|---|---|---|---|---|
| 1 | **cc-0017e v1.1 doc patch** (m.chatgpt_review column-name correction) | **P1 NEW v2.88** | None — direct doc patch | chat → PK directive |
| 2 | **cc-0017e apply session** | **P1 NEW v2.88** | Gated on v1.1 doc patch landing | chat → PK directive after v1.1 lands |
| 3 | **Reconciliation daily cadence diagnostic** | P1 carry | First post-cc-0017d cron 85 fire | chat → PK |
| 4 | **Health_check V-C3 + signal-production diagnostic** | P1 carry | V-C3 still PENDING | Cowork → chat |
| 5 | **Platform Reconciliation View brief authoring** | P2 carry | PK greenlight required | PK → chat |

**Standing P0:** Personal businesses check-in. Crazy Domains follow-up carry from v2.51.

---

## 13. v2.88 honest limitations

- **Brief-authoring session only.** No production schema change, no migration applied, no V-check run, no fresh empirical evidence beyond the 8 read-only probes.
- **`case_history` table does NOT yet exist in production.** Authored design only; applies in future apply session.
- **8 acknowledged legacy cases remain with NULL triaged_at/triaged_by.** Backfill scoped but not executed.
- **fn_triage_case external-caller surface is not enumerable from inside the database.** Item C is defensive prospective protection; no evidence-based caller list.
- **V-Z3 convention introduced but not empirically validated.** First use will be at cc-0017e apply session V-check matrix.
- **v1.1 doc patch required BEFORE any D-01 or apply.** The brief is functional at the SQL design layer but operationally blocked on m.chatgpt_review column-name correction.
- **22 outstanding close-the-loop UPDATEs** unchanged.
- **Dashboard PHASES 41st consecutive deferral** carried (was 40 at v2.87; +1 at v2.88). No file-touch.
- **decisions.md untouched.** No architectural decisions changed v2.88. Open design decisions captured in brief §9, not promoted to architectural decisions.
- **1+2 split commit pattern** (per-session standalone + sync_state+action_list atomic) re-applied for sync close — L-v2.85-e 3rd consecutive occurrence.
- **Session compacted once** mid-authoring (between commit 2 and commit 3). Compaction summary preserved continuity; commit 3 completed post-compaction at `d349bdfe`.
- **Identical PK directive re-received** twice in successive turns at sync-close verification. Re-verified state matched bit-for-bit; flagged loop pattern to PK; PK directed sync close to proceed.

---

## 14. Session commit map (close-of-session view)

**Authoring commits (3):** `8502fc49` → `1659b293` → `d349bdfe`

**Sync-close commits (this v2.88 sync close):**
- Commit A: per-session detail (this file) — standalone via `create_or_update_file`
- Commit B: sync_state + action_list — atomic via `push_files`

Per L-v2.85-e 3rd-consecutive-occurrence pattern.

---

*End of v2.88 per-session record.*
