# 2026-06-12 — v3.39: F-FORMAT-MIX-NOFORMAT CLOSED (diagnosed — subsumed) + F-ADVISOR-RESPIN-ORPHAN-SLIDES 2nd instance

**Status:** PK-approved register-only close. Read-only investigation; 0 production mutation.
**Actors:** CCH (investigation + this record + patch authoring). CCD to apply the two >80KB `00_` register patches per the v3.35 governance rule (CCD ownership for >80KB register/authority patches; GitHub bridge has no str_replace).
**Authority impact:** none (read-only finding; as-built decision flow + T1 unchanged).

---

## 1. Lane summary

The Monitor Format Mix panel's "No Format" bucket (≈15.6% overall; Care for Welfare ≈50.5%) is **NOT a pipeline format failure**. All 51 No-Format rows in the live 30-day window are intentional relevance/compliance skips — the v3.32 class — surfaced by a panel semantics gap, not a data error. Not a new architecture or Advisor defect.

## 2. Evidence (read-only, 2026-06-12, production `mbkmaxqhsohbtwsqolns`)

- **Panel reproduction:** 51/325 drafts in 30d window = 15.7% `recommended_format IS NULL` (panel showed 15.6%; window slid). Dashboard query accurately reports table state.
- **Signature, 51/51:** `approval_status='dead'` + `draft_format.compliance_skip=true` + `draft_format.reason` populated + `dead_reason` column NULL + `recommended_format` unset because the draft is killed at the AI relevance step BEFORE final formatting. 0 unexplained rows.
- **Per client:** CFW 47/93 = 50.5%; Invegent 4/65 = 6.2%; NDIS-Yarns 0/82; Property-Pulse 0/85. Narrow-scope clients only — matches v3.32 exactly.
- **Per platform (NULL rows):** LinkedIn 33, Facebook 14, Instagram 4.
- **Sample reasons:** "not relevant to CFW scope — [SA clubfoot publication / emergency rescue item / Philippine health foundation / violent incident]"; Invegent: "not relevant to Invegent v0.1 scope".
- **Chain trace (6 samples):** slot filled `image_quote` → ai_job `succeeded` (model ran, token spend) → relevance kill → slot `skipped` → 0 queue / 0 publish rows.
- **Downstream exhaustive:** 0 publish/queue rows reference any NULL-format draft in window. Not publish-blocking. One render anomaly (§4).
- **Weekly CFW skip trend:** 5 → 10 → 11 → 14 → 7 (~11/wk, steady; consistent with v3.32's ~12/wk).
- **CFW live pipeline healthy:** 32 published in window across 5 formats (24 image_quote + text/kinetic/stat/stat_voice); 5 approved; disproves any client-format-config cause.
- **Dashboard source** (`invegent-dashboard` `actions/classifier-metrics.ts` @ blob `53fa9023`): Format Mix base CTE = `COALESCE(pd.recommended_format,'(none)')` over ALL `m.post_draft` rows in 30d — **no `approval_status` filter** → dead/compliance-skipped drafts counted inside a Format Mix panel. Semantics gap, not mis-query.

## 3. Disposition (PK-approved 2026-06-12)

**F-FORMAT-MIX-NOFORMAT → CLOSED (diagnosed — subsumed by existing carries). Priority stays P3.**
Carried under: (a) CFW intentional-skip observability / dashboard-semantics carry (P3) — owns the future panel tweak (split "(none)" into skipped-vs-genuine or exclude dead drafts; NOT implemented, future gated); (b) CFW feed-scope review (P3, optional) — owns the volume; (c) F-CFW-COMPLIANCE-PREGEN (P3) — owns the generate-then-skip token cost.
Explicitly NOT done this lane: no dashboard change, no Advisor change, no format-policy change, no Option C action, no SQL/DB objects, no production mutation.

## 4. F-ADVISOR-RESPIN-ORPHAN-SLIDES — 2nd evidence point (PK-approved amendment)

CFW draft `8e90cbd4-00f7-448c-af06-f1816a8e99ec` (LinkedIn, created 2026-06-01) was compliance-skipped/dead (`compliance_skip=true`, reason "not relevant to CFW scope") yet carries **12 succeeded Creatomate renders** in `m.post_render_log` and 0 publish/queue rows. Suggests the orphan-render issue may be a **repeated pattern rather than a one-off** (1st instance: draft `096e3b72`, v3.34). **Stays P3** unless further evidence shows material provider-credit waste.

## 5. Register patches for CCD to apply (surgical, read-HEAD-first)

### 5a. `docs/00_action_list.md`

**(i) F-FORMAT-MIX-NOFORMAT Active-table row** — replace the row's status/owner/next-action cells so the row reads:

> | **F-FORMAT-MIX-NOFORMAT (v3.38-recon)** | Live Format Mix panel "No Format" bucket ≈15.6% overall / CFW ≈50.5%. **DIAGNOSED v3.39 (read-only):** NOT a pipeline format failure — all 51 No-Format rows in the 30d window are intentional relevance/compliance skips (`approval_status='dead'`, `draft_format.compliance_skip=true`, reason populated 51/51, `recommended_format` unset because the draft is killed before final formatting; CFW 47 + Invegent 4; NY/PP zero; 0 downstream publish/queue rows). Dashboard query accurately reports table state; the panel has a **semantics gap** (Format Mix counts dead/compliance-skipped drafts — no `approval_status` filter). | **P3 — CLOSED v3.39 (diagnosed — subsumed by existing carries)** | Subsumed under the CFW intentional-skip observability/dashboard-semantics carry (P3); volume = CFW feed-scope review (P3); cost = F-CFW-COMPLIANCE-PREGEN (P3). NOT a new architecture/Advisor defect. Dashboard tweak NOT implemented (future, gated). Session record `docs/runtime/sessions/2026-06-12-v3.39-f-format-mix-noformat-close.md`. | n/a (closed) | — |

**(ii) F-ADVISOR-RESPIN-ORPHAN-SLIDES row** — append to the description cell after "1 instance in the 7-day window.":

> **AMENDED v3.39 (2nd instance):** CFW draft `8e90cbd4-00f7-448c-af06-f1816a8e99ec` (LinkedIn, 2026-06-01) compliance-skipped/dead yet produced 12 succeeded Creatomate renders + 0 publish/queue rows — repeated-pattern signal rather than one-off; stays P3 unless material provider-credit waste shows.

and append to the status cell: "; 2nd instance recorded v3.39 (F-FORMAT-MIX-NOFORMAT lane)."

**(iii) Closed list** — prepend before `**Closed v3.38:**`:

> **Closed v3.39:** F-FORMAT-MIX-NOFORMAT → CLOSED (diagnosed — subsumed by existing carries; read-only investigation, register-only close under PK approval). F-ADVISOR-RESPIN-ORPHAN-SLIDES amended with 2nd instance (`8e90cbd4`). 0 code / 0 DB / 0 dashboard / 0 Advisor / 0 policy / 0 Option C / 0 provider / 0 worker / 0 production change. Authority impact: none.

**(iv) Header** — bump `Last updated:` line: prepend a v3.39 segment ahead of the v3.38 text: `**v3.39 — F-FORMAT-MIX-NOFORMAT CLOSED (diagnosed — subsumed) + F-ADVISOR-RESPIN-ORPHAN-SLIDES 2nd instance (register-only, 0 production mutation).** Prior — ` (then the existing v3.38 text continues unchanged).

### 5b. `docs/00_sync_state.md`

Prepend this banner above the v3.38 banner:

> **✅ v3.39 F-FORMAT-MIX-NOFORMAT CLOSED — diagnosed, subsumed by existing carries (read-only investigation + register-only close, 2026-06-12):** The Monitor Format Mix "No Format" bucket (≈15.6% overall / CFW ≈50.5%) is NOT a pipeline format failure. All 51 No-Format rows in the live 30d window (CFW 47/93 = 50.5%; Invegent 4/65 = 6.2%; NDIS-Yarns 0; Property-Pulse 0) are intentional relevance/compliance skips: `approval_status='dead'` + `draft_format.compliance_skip=true` + reason populated 51/51 + `dead_reason` column NULL + `recommended_format` unset because the draft is killed at the AI relevance step before final formatting (trace: slot `image_quote` → ai_job succeeded → kill → slot `skipped` → 0 queue/publish). Panel query reproduced exactly (51/325 = 15.7%) — dashboard accurately reports table state but the Format Mix base CTE has a **semantics gap** (counts dead/compliance-skipped drafts; no `approval_status` filter). Disposition: carried under the existing CFW intentional-skip observability/dashboard-semantics carry (P3); volume = CFW feed-scope review (P3); cost = F-CFW-COMPLIANCE-PREGEN (P3). NOT a new architecture or Advisor defect; dashboard tweak NOT implemented (future, gated). **F-ADVISOR-RESPIN-ORPHAN-SLIDES amended (2nd instance):** CFW draft `8e90cbd4-00f7-448c-af06-f1816a8e99ec` compliance-skipped/dead yet 12 succeeded Creatomate renders + 0 publish/queue rows — repeated-pattern signal; stays P3. Weekly CFW skip trend 5→10→11→14→7 (~11/wk steady). Evidence + patch text: `docs/runtime/sessions/2026-06-12-v3.39-f-format-mix-noformat-close.md`. **0 code / 0 DB / 0 migration / 0 dashboard / 0 Advisor / 0 policy / 0 Option C / 0 provider / 0 worker / 0 production mutation — 7 read-only SELECTs + 2 repo file reads; 0 D-01 (read-only lane).** Authority impact: none. cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT / Asset-Production lanes preserved unchanged.

## 6. Constraint compliance (this lane, end-to-end)

0 code change / 0 DB write / 0 migration / 0 dashboard change / 0 Advisor change / 0 format-policy change / 0 Option C action / 0 SQL objects / 0 provider call (SELECT-only reads) / 0 worker invocation / 0 cron change / 0 production mutation. Tool activity: 7 read-only SELECTs against production + 2 repo file reads + this session-record commit. 0 D-01 fired (read-only lane; register-only docs close under explicit PK approval 2026-06-12).
