# v3.07 — Doc-patch session: cc-0017c v1.2 backfilled + corrected; large-brief patches + lesson promotions deferred (laptop-free, 0 production mutation)

**Session date:** 2026-05-25 Sydney (UTC+10)
**Predecessor:** v3.06 (deployed↔repo drift close)
**Status:** CLOSED — doc-patch only ✅
**Scope constraint (PK):** off-laptop; laptop-free, zero production mutation, small commits, no blind-patching, commit only confirmed facts, leave explicit notes for anything uncertain.

---

## Outcome

Repo-only documentation session. **0 production mutation / 0 D-01 / 0 EF deploy / 0 cron / 0 enablement / 0 dashboard edit / 0 memory edit / 0 Supabase.** Cleared one stale brief (cc-0017c) against its apply source-of-truth; deferred the remaining located-brief doc patches and all lesson promotions on no-fabrication grounds (insufficient documented scope, and/or full-file re-emit risk, and/or unresolved canonical convention).

**Honest through-line:** of the queued doc-patch + lesson-promotion debt, only cc-0017c was genuinely shovel-ready (small file + documented v1.2 scope). The rest was less ready than the one-line action_list entries implied.

## Done

- **cc-0017c main brief → v1.2** (`docs/briefs/cc-0017c-friction-register-lockdown-and-backfill.md`). Was stale at v1.1 `AUTHORED_PENDING_D01_REFIRE` / "Apply: Still GATED"; corrected to **APPLIED-WITH-VCHECK-CORRECTION** against source-of-truth `docs/runtime/sessions/2026-05-19-v2.85-cc0017c-applied-with-vcheck-correction.md`. Backfilled, all confirmed facts:
  - apply date **2026-05-19** (session v2.85); migrations **`20260519004545`** (main) + **`20260519005322`** (V-B4 smoke cleanup)
  - **3 D-01 refs:** v1.0 `a37eff28-2ba1-4a7a-8fbd-3e9aba738c79` (partial, Option A); v1.1 `9e602a2d-…` (partial/type-c — **full UUID not captured in source; recorded truncated, tail not invented**); fresh apply `d18fa6db-3a5c-42ff-8aef-55fcb4fb4f92` (**AGREE**, resolved v2.85, escalation_resolved_at 2026-05-19 00:54:22 UTC)
  - V-B4 **12-param** `emit_event` signature pointer added (correction itself lives in subfile — see deferrals)
  - Commits **`6d5c3ae6`** (v1.2) → **`81dcb683`** (self-caught correction: softened an unverified cc-0017d session-version tag — confirmed APPLIED, exact version not reconfirmed this session; cc-0017e v2.90/v2.91 kept, action_list-supported). Final content sha **`ab0f4442`**. **No SQL change.**

## Deferred to CC surgical pass (no-fabrication discipline)

- **cc-0017a / cc-0010A / cc-0011 / cc-0012 brief doc patches.** Reasons: (1) **insufficient documented scope** — only cc-0017c had an explicit v1.2 scope statement (in its v2.85 apply session); the others are undetailed carries; (2) **full-file re-emit risk** — briefs are **72 KB / 107 KB / 61 KB / 49 KB** and the GitHub MCP exposes no str_replace, so any edit re-emits the entire file (L41-class clobber/transcription risk, esp. with embedded migration SQL). cc-0017a facts ARE confirmed and ready for a surgical CC edit: applied **v2.81**, migration **`20260518065610`** (`cc_0017a_friction_foundational_schema`), D-01 **`adcc8385-b9be-4573-8d64-b40510202940`** (partial/type-c, Path B), 20/20 V-checks PASS.
- **cc-0017c/vchecks.md subfile (V-B4 12-param fix).** Located + grounded, but subfile edit carries the same re-emit consideration → CC.
- **Lesson promotions** (L41 + L-v2.85-e pair; L-v2.83-a; L-v2.85-a) → **next-lesson-cycle / CC (PK chose option c).** Not promoted by assumption. Blocker: canonical lessons (L37–L65) are **not defined in any located file** (repo root / `docs` per index / `docs/runtime` / `12_project_handoff.md`); the action_list only *names* the range and tracks candidate status. Promotion mechanic + baseline-numbering convention undocumented; cannot confirm L65 is current max. Promoting would require inventing the canonical home + next number → declined.

## Path-hunt update

- **vchecks.md LOCATED:** `docs/briefs/cc-0017c/vchecks.md` (it is the cc-0017c §5.3 subfile, not a top-level file). The confirmed correct replacement for V-B4 is the **12-param** `friction.emit_event(source, condition_key, problem_key, source_event_id, observed_at, related_object, observation_text, raw_payload, reported_by, severity_override, category_override, dynamic_context)` → `RETURNS TABLE(event_id, case_id, case_disposition)` SECURITY DEFINER.
- **decisions.md (runtime)** still unlocated; parked per PK steer (the v3.06 sync_state no longer carries the old cc-0014 wrong-framing on the sync_state leg, so that part appears already-moot).

## Hard stops respected v3.07

0 production mutation / 0 apply_migration / 0 EF deploy / 0 cron change / 0 enablement / 0 hard delete / 0 friction.* write / 0 Supabase / 0 dashboard edit / 0 memory edit / 0 decisions.md change / 0 D-01. Dashboard PHASES leg N/A (doc-only, no phase change). No lesson promoted. No large brief re-emitted. cc-0015 not started; cc-0016 Stage E not started; Q-005 untouched; YT/IG state untouched.

## Process

- **L41 reinforced — as a STOP, not just a technique.** Only cc-0017c (5.8 KB) was small enough to safely full-rewrite in chat; the large briefs (49–107 KB) were explicitly NOT re-emitted because full-file rewriting them via the available MCP is the exact clobber anti-pattern L41 exists to prevent. The two `00_` index edits used sha-passed `create_or_update_file` (explicit blob SHAs to detect concurrent edits).
- **L-v2.85-e applied:** the two `00_` payloads kept compact; full detail lives in this session file; the two big `00_` writes sequenced one-per-completion to avoid response-construction truncation.
- **No-fabrication (L-v3.05-a family):** three deferrals taken specifically to avoid asserting unconfirmed facts; the truncated v1.1 UUID and the unverified cc-0017d version were flagged in-doc rather than guessed; one self-caught over-claim (cc-0017d version) corrected in a follow-up commit.

## Next session

- CC surgical pass: cc-0017a / cc-0010A / cc-0011 / cc-0012 brief apply-record patches + cc-0017c/vchecks.md V-B4 12-param fix (str_replace, laptop).
- Lesson promotions once canonical-home + numbering convention is known.
- Locate runtime `decisions.md`; re-check cc-0014 framing leg.

---

*Per-session file 2026-05-25 v3.07 doc-patch close. cc-0017c v1.2 apply-record backfill + correction (commits `6d5c3ae6`→`81dcb683`, content sha `ab0f4442`). Large-brief patches + vchecks subfile + lesson promotions deferred to CC. vchecks.md located at `docs/briefs/cc-0017c/vchecks.md`. decisions.md unlocated (parked). 0 production mutation / 0 D-01. Dashboard PHASES leg N/A.*
