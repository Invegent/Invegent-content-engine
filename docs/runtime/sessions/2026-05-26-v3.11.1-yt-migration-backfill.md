# 2026-05-26 Sydney — v3.11.1: YouTube recovery migration files backfilled to repo (DB→repo drift closed)

**Actor:** CCD (Claude Code / laptop terminal).
**Type:** repo-only / documentation-only — **0 DB change, 0 deploy, 0 production mutation** (read-only SELECTs only).

---

## Outcome

The three YouTube recovery/backfill migrations from the v3.09–v3.11 arc were applied via Supabase MCP `apply_migration`, which writes the DB's `supabase_migrations.schema_migrations` history but does **not** create a repo file — leaving a DB→repo migration drift (the L-v3.06-a class). Closed it by recovering each migration's exact SQL **verbatim from `schema_migrations.statements`** (the same method v3.06 used) and committing one `supabase/migrations/` file per migration. No DB change — the versions are already recorded as applied.

## Files backfilled (verbatim from DB; md5-verified)

| Version | Migration | DB `statements[1]` md5 (== file SQL body) |
|---|---|---|
| `20260526054427` | `yt_failed_backlog_recovery_unit_b_b1_only_20260526` (B1, 5 quota) | `be87df0e4f7bf590ef586bba01c953d5` ✅ |
| `20260526091149` | `yt_failed_backlog_recovery_unit_b2_token_casualty_20260526` (B2, 17 token-casualty) | `85f8c87e61e07fb780ef385776512168` ✅ |
| `20260526094403` | `yt_pub_publish_audit_gap_backfill_20260526` (audit-gap, 11 rows) | `b8e0758ee3e2c8c5de8c70f48c3f3aca` ✅ |

Each file = a minimal reconstruction header (provenance) + the verbatim DB statement; the file's SQL body md5 equals the DB `statements[1]` md5 (byte-exact, no semantic edits).

## Source-of-truth note (brief artifacts vs applied DB statements)

The B2 and audit-gap brief artifacts (`docs/briefs/results/yt-publisher-failed-no-retry-unit-b2-recovery.sql`, `…publish-audit-gap-backfill.sql`) **differ** from the applied DB statements (artifact DO-block md5 `6f5ec2c6…`/`bbbe375a…` ≠ DB `85f8c87e…`/`b8e0758e…`): the artifacts carry more verbose comments and trailing read-only post-apply verification SELECTs that were never part of the applied migration. **SQL semantics are identical.** The `supabase/migrations/` files hold the DB statements (source of truth). B1 had no repo artifact — the DB statement is its sole source (CCH-applied; CTE-based DO block, guard `= 5`).

## State after v3.11.1

- DB→repo migration drift for the YouTube recovery arc **CLOSED** — all 3 `apply_migration`-applied migrations now have `supabase/migrations/` files.
- No change to recovery state: **B1 + B2 recovery remain complete**; **F-YT-PUB-PUBLISH-AUDIT-GAP remains closed** (v1.10.0 + 11-row backfill); **B3 (9 never-connected) remains onboarding debt** (untouched).

## Hard stops / scope (honored)

Read-only SELECTs only (recovered `statements` from `schema_migrations`); 0 DB mutation, 0 deploy, 0 apply, 0 B3 change. No SQL semantic edits — files are verbatim DB statements (md5-verified). `00_action_list.md` full-size (not a stub). Recovery logic not reopened.
