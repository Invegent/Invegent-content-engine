# 2026-05-26 Sydney — v3.12: cc-0015 started — Stage A applied + Stage F prepared (Stage B/C/D held for re-brief)

**Actor:** CCD (Claude Code / laptop terminal). Stage A DML via Supabase `apply_migration`; Stage F prep in the `invegent-dashboard` repo.
**Type:** 1 friction-schema migration (additive) + Stage F frontend prep (prep branch, not merged) + readiness pass.
**Governance:** plan_review D-01 `58d89efe-0fa7-49a1-979a-668608a5cdd8` (escalate_explicit_flag → PK) + PK approval phrase `Approved: apply cc-0015 Stage A pool schema additions`. Close-the-loop recorded.

---

## Eligibility (gate)

PK confirmed cc-0014's early-close (D-IOL-001, Day 4, projected INVALID-not-FAIL, register → standing infra) satisfies cc-0015's start gate — the formal Day-19 verdict never ran. cc-0015 is GO.

## Readiness findings (read-only)

- Backend: `dashboard_ui` category + `friction.pool_session` were absent (preflight clean); `friction.category` columns matched the brief INSERT; `fn_recent_cases(p_limit)` + `fn_triage_case` present (extendable backward-compatibly); `fn_pool_counts`/`fn_start/end_pool_session` absent (Stage D/E unbuilt). `friction.case` extended **post-brief** by cc-0017d/e (`effort_level`, `resolution_kind`, `reopen_count`, `predecessor_case_id`, `dedupe_fingerprint`, `first_viewed_at`).
- Dashboard `/operations`: case list (`fn_recent_cases(50)`) + cc-0016 evidence display + a usability patch; `friction-fab.tsx` / `friction-form.tsx` / `status-strip.tsx` exist; **no** filter bar / saved views / batch / help-copy yet.
- **Divergence:** `/operations/pools` already ships a read-only **"Pool / Repair Window v0"** (commit `9a4b446`, 2026-05-23) — pipeline-incident pools encoded in `friction.case.notes` (`pool_key:`) + editorial CATALOG, explicitly **"NOT cc-0015."** cc-0015 v1.0 (2026-05-16) predates it and specifies a *different* category-based pool view. **Two parallel pooling models = unresolved design tension → Stage B/C/D HELD for a re-brief.**
- Brief internal contradiction: line 10 ("one new column on `friction.case` / no new tables") vs §3 (no column; adds the `pool_session` table) — **§3 authoritative**.

## Stage A — APPLIED + verified

- Migration `cc_0015_a_pool_schema_additions` (version `20260526110657`) via `apply_migration`. Additive: new `friction.category` `dashboard_ui` + new `friction.pool_session` table (+ `session_date DESC` / `pool_label` indexes + grants). Idempotent preflight (aborts if either object exists). No change to existing categories, `friction.case`, the RPCs, or the Repair-Board v0.
- **V-A1–A4 PASS:** `dashboard_ui` active; `pool_session` exists with generated `duration_minutes`; grants `authenticated`=SELECT only / `service_role`=SELECT,INSERT,UPDATE; no CHECK on `cases_addressed`.
- Migration file **backfilled same-session** to `supabase/migrations/20260526110657_cc_0015_a_pool_schema_additions.sql` (L-v3.06-a — no DB→repo drift).
- **Stage A.5 backfill recategorisation** (`operator_friction` → `dashboard_ui`) is a separate manual PK-confirmed per-case `fn_triage_case` step — NOT done here.

## Stage F — PREPARED (awaits PK V-F3)

- `lib/friction-help-copy.ts` (single source-of-truth help dict, verbatim from brief §8) on dashboard prep branch `feat/cc-0015-stage-f-operator-copy` (commit `4c31549`). **Not pushed, not merged, not deployed.**
- Brief drift: placed at `lib/` (not `app/lib/`) per the repo's `@/*`→root alias.
- Next Stage F step (after PK reads the copy aloud, V-F3): build the single `FrictionFieldHelp` component + wire it into the FAB (`friction-form.tsx`) + triage form (`case-row.tsx`).

## Held / next

- **Stage B/C/D HELD** pending a re-brief reconciling cc-0015's category-based pool view against the shipped notes-based Pools/Repair-Board v0 (supersede / coexist / re-scope). **v0 NOT treated as superseded.**
- Stage D `fn_pool_counts` + Stage E `fn_start/end_pool_session` + Stage G (`ICE-PROC-002`) remain future.

## Hard stops / scope (honored)

A-then-(held): Stage A applied only after the plan_review D-01 + PK approval phrase. No Stage B/C/D. No deploy. Stage F prep branch not pushed/merged. Repair-Board v0 not superseded. Migration file backfilled same-session. Both governance rows closed-loop.
