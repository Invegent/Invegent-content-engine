# 2026-05-26 Sydney — v3.12: cc-0015 Stage F operator help copy — CCB visual PASS, merged + deployed to production

**Actor:** CCD (Claude Code / laptop terminal). Dashboard merge/deploy via git + Vercel; closeout in content-engine.
**Type:** dashboard frontend merge + production deploy (Stage F only) + content-engine docs. **0 SQL, 0 migration, 0 Repair-Board-v0 change in this pass.**
**Governance:** Stage F copy V-F3 PK-approved; CCB visual V-F1/V-F2 PASS on preview. (Stage A's plan_review D-01 `58d89efe` was resolved + applied in the earlier v3.12 pass — see Stage A status below.)

---

## Stage F — SHIPPED (operator help copy)

- **Built:** `components/friction-field-help.tsx` — one dict-driven help component (reads `FRICTION_HELP` from `lib/friction-help-copy.ts`); renders field guidance + an expandable per-option list (✅ "counts as prevented loss" marker on capture_reason). Tailwind-classed → renders in both the inline-styled FAB modal and the Tailwind triage form.
- **Wired:** FAB (`friction-form.tsx`) — severity, category. Triage (`case-row.tsx`) — quality_flag, category, action_decision, next_review_at, capture_reason, capture_reason_note. Additive only.
- **Copy:** V-F3 PK-approved + de-experimented (no Day-19 / criterion-5 / incrementality-verdict / `friction.pool_session` operator-facing copy); dict + brief §8 kept in lockstep.
- **CCB visual V-F1/V-F2: PASS** on the preview (commit `f9d51a9`) — FAB help visible, triage help visible on all in-scope fields, expander works, counts marker present, no stale wording, no missing in-scope help, no layout break.
- **Merged:** FF-only into dashboard `main` → `f9d51a96ffb811b84bbbf39b42637d10e7037f6d`; `tsc --noEmit` clean (0 errors).
- **Deployed:** Vercel **production** `dpl_FftxSjeBNyurxZetJedi3zDsan6b` — READY (~39s build), live at **`dashboard.invegent.com`** (target=production). Production liveness OK (`/login` 200, `/` + `/operations` 307→auth, no 5xx).
- **Production visual smoke:** CCD could not perform it (no browser; `/operations` auth-gated). **CCB performed the production smoke — PASS (2026-05-26):** FAB help visible, triage-row help visible, "What do the options mean?" expands, "Counts as prevented loss?" marker correct, no stale Day-19/criterion-5/incrementality-verdict/`friction.pool_session` copy, no visual issues, no missing help. Stage F live + clean on `dashboard.invegent.com`.

## Stage A status — APPLIED + VERIFIED (earlier v3.12 pass; re-verified this pass, NOT inferred from Stage F)

Concrete evidence (fresh read-only 2026-05-26):
- apply_migration **`cc_0015_a_pool_schema_additions`** (version `20260526110657`); migration file committed to `supabase/migrations/` on `origin/main` (`e6158b6`).
- plan_review D-01 **`58d89efe`** — verdict partial → **completed**, resolved_by "PK (approval phrase) + CCD (apply)".
- **V-A1** `dashboard_ui` category exists + active ✅ · **V-A2** `friction.pool_session` exists, `duration_minutes` generated ✅ · **V-A3** grants `authenticated`=SELECT-only / `service_role`=SELECT,INSERT,UPDATE ✅ · **V-A4** no CHECK on `cases_addressed` ✅.

So Stage A is genuinely applied (not pending). **Stage A.5 backfill recategorisation** (`operator_friction`→`dashboard_ui`) remains a separate manual PK-confirmed step (NOT done).

## Re-scoped / held (cc-0015 is NOT complete)

- **Category per-option DB descriptions (§8.2) — RE-SCOPED to a Stage F follow-up** (PK decision). Current wiring surfaces category field guidance + the `unclassified` warning; live per-option `friction.category.description` display is deferred (needs DB-backed category hydration). Not a blocker; did not block V-F1/V-F2.
- **FAB `capture_reason` absence — accepted** (the FAB has no capture_reason field; field absent, not omitted help).
- **Stage B/C/D — HELD** pending the re-brief reconciling cc-0015's category-based pool view vs the shipped notes-based Pools/Repair-Board v0 (`/operations/pools`, `9a4b446`). **v0 NOT superseded / NOT changed.**
- **Stage D/E/G** (fn_pool_counts / pool-session fns / `ICE-PROC-002`) — future.

**cc-0015 is NOT complete.** Done: Stage A (schema) + Stage F (operator copy, shipped). Open: Stage B/C/D (re-brief), category-description follow-up, Stage A.5 backfill, Stage D/E/G.

## Hard stops / scope (honored)

Stage F only · FF-only merge (verified) · production deploy READY (verified) · 0 SQL / 0 migration this pass · Repair-Board v0 untouched · no category per-option DB descriptions added · Stage B/C/D not started · Stage A status verified from evidence (not inferred) · cc-0015 not claimed complete · production visual smoke flagged as not-CCD-performed (not fabricated).
