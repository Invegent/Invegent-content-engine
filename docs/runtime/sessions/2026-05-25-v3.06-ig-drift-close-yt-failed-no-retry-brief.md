# 2026-05-25 Sydney — v3.06: deployed-vs-repo drift close (IG publisher + 7 backfilled migrations) + F-YT-FAILED-NO-RETRY brief

**Session type:** source-of-truth reconciliation + brief authoring. Repo-only writes; **zero production mutation** (no DB schema/data change, no EF deploy, no cron/enablement change, no dashboard edit).
**Predecessor:** v3.05 (2026-05-24 — YouTube OAuth Production restore).
**Outcome:** The 2026-05-24 deployed state is now faithfully recorded in GitHub. The OWED IG-publish-restore arc is substantially discharged (publisher-code leg + all 7 chat-applied migration files), a sync_state factual error on cc-0019 was caught and corrected, and the F-YT-FAILED-NO-RETRY remediation is now a written brief.

---

## What triggered this

PK asked for a deep dive on the 24 May YouTube + Instagram fixes (what broke, what was done, is it durable, anticipatory work). The audit (read-only, verified against live code + deployed EFs + DB) surfaced that **deployed `instagram-publisher` was v2.4.0 while repo `main` was v2.0.0** — a 4-version drift where the deployed function is the real fix and a redeploy from repo would silently revert it. PK directed: (1) IG repo push first (highest rollback risk), (2) F-YT-FAILED-NO-RETRY brief modelled on IG v2.4.0, (3) 4-way close including an IG migration-file drift check.

---

## Actions (in order)

### 1. IG publisher repo push — `bc78511e`
- Verified deployed `instagram-publisher` Supabase fn version 36 (updated 2026-05-24) = **v2.4.0**; repo `main` = **v2.0.0**.
- Pushed the exact deployed source to `supabase/functions/instagram-publisher/index.ts` (CRLF→LF normalised; no logic change), passing the prior blob SHA `a271206…` for concurrent-edit detection.
- Re-fetched at `bc78511e` and verified byte-faithful on every structural anchor: `VERSION='instagram-publisher-v2.4.0'`, all four version-history blocks (v2.1.0/2.2.0/2.3.0/2.4.0), `MAX_PUBLISH_ATTEMPTS=5`, `pollContainerReady`, `publishWithReadinessRetry`, the v2.1.0 throttle + `destination_id` gate, the RE-B dead-state catch, `PUBLISH_NOT_READY_RE=/\b(?:9007|2207027)\b/`, clean `Deno.serve(app.fetch)` EOF; size 46,772 B (no truncation).
- **Rollback hazard closed:** a future `supabase functions deploy` from repo now produces working v2.4.0.

### 2. F-YT-FAILED-NO-RETRY brief — `bee872aa`
- Authored `docs/briefs/yt-publisher-failed-no-retry.md` (status: draft / NOT implemented), explicitly modelled on the IG v2.4.0 pattern just pushed.
- Design: `classifyYouTubeFailure` (auth / transient / terminal) → bounded per-draft retry (attempt cap → terminal) for transient/per-draft defects; **channel-level auth hold** (reusing `c.client_publish_profile.paused_until`, the IG 2207051 analogue) exempt from the per-draft cap so a token outage parks the backlog and self-heals instead of terminalising content. Zero-DDL preferred (counters in `draft_format` jsonb; hold enforced in-EF since YT is direct-read, no lock RPC). Unit B = one-time bounded recovery of the 30 `failed` drafts (triage token-casualties vs genuine terminals).
- Execution gated on plan_review + ef_deploy + sql_destructive D-01 + PK approval.

### 3. Migration-file drift check + backfill — `8738c3a0`
- Compared `supabase_migrations.schema_migrations` (applied) vs `supabase/migrations/` (repo). Repo stopped at `20260523`; **7 migrations applied 2026-05-24 had no `.sql` file** (all chat-applied via `apply_migration`, which writes the DB but commits no file).
- Recovered the exact applied SQL from `schema_migrations.statements[1]` and committed all 7 with provenance headers (repo-only; each version already recorded as applied → no re-execution):
  - `20260524064439_ig_fresh_restart_soft_purge_queued`
  - `20260524070529_ig_fresh_restart_void_stale_drafts_and_repurge` (incl. additive `approval_status` `'voided'` CHECK extension)
  - `20260524071850_ig_fresh_restart_void_stale_residual`
  - `20260524091020_cc_0019_publish_eligibility_gate`
  - `20260524105605_yt_token_verify_reset_one_pp_draft`
  - `20260524110210_yt_token_verify_reset_one_ny_draft`
  - `20260524112024_yt_backlog_release_fresh_softretire_stale`
- Extended beyond the literal "IG" three because stopping there would leave a fresh 4-file OWED — contrary to PK's "avoid another OWED" intent. The recovery mechanism was uniform across all 7.

---

## ⚠️ Correction caught: cc-0019 IS implemented (sync_state v3.05 was wrong)

- Live DB verified: `m.is_publish_eligible(uuid,text)` **exists** and `m.fill_pending_slots` **carries the gate** (references `is_publish_eligible`). Applied migration `20260524091020_cc_0019_publish_eligibility_gate` confirms it.
- The migration's own header records governance: D-01 sql_destructive `a2e5f3f5-b712-4ab2-a463-7b40d3cd151f` (agree) + PK approved. So cc-0019 was **implemented and governed**, not "brief only."
- sync_state v3.05 + action_list v3.05 both assert "cc-0019 brief only, NOT implemented" — **factually stale.** Corrected in this close. *(Not reconstructing the exact session in which it was applied; recording the verified live fact + the governance evidence in the migration header.)*

---

## Verified live state at session time (read-only)

- **IG:** publisher v2.4.0 (deployed). Profiles: CFW + Invegent `publish_enabled=true`; NY + PP `publish_enabled=false`; all have `destination_id`, none paused. Enqueue cron **jobid 64 paused**; publisher cron jobid 53 active. IG queue = 174 `dead` + 131 `purged`, nothing live → "proven but parked." 2 published 24 May (restore tests), none since.
- **YT:** publisher v1.6.0. Profiles NY + PP `publish_enabled=true`, tokens present, none paused. Backlog drained: 30 `published`, 1 `generated`, **30 `failed`** (F-YT-FAILED-NO-RETRY casualties + genuine non-uploadable), 14 `archived_stale`, 3 `pending`. 9 published in last 4 days (most recent 24 May 13:15 UTC).

---

## Hard stops respected v3.06

- 0 production mutation: 0 apply_migration, 0 EF deploy, 0 cron change, 0 client enablement change, 0 row mutation, 0 hard delete.
- 0 friction.* write / Q-005 untouched / cc-0015 not started / cc-0016 Stage E not started / CFW+Invegent untouched.
- 0 decisions.md change / 0 memory edits.
- Dashboard roadmap/PHASES leg **N/A** (operational/recording session, no phase change — same precedent as v3.05).
- 0 D-01 (no production mutation in this session; the YT brief's plan_review is the next step, fired separately).
- Drift backfill is repo-only reconciliation of already-applied state — not a production mutation, no D-01 required.

---

## Carries / next

- **F-YT-FAILED-NO-RETRY** — brief authored; next step is its `plan_review` D-01 (firing immediately after this close per PK).
- **cc-0019** — now correctly recorded as implemented; its execution is therefore already DONE (was mis-tracked as pending). Remaining cc-0019 follow-ups (cron-aware predicate enhancement) stay future.
- **IG operational resume** — enqueue cron 64 still paused; re-enable decision (start CFW/Invegent) is PK's, lower-risk now that M12 root cause is closed.
- **F-YT-EXPIRY-DISPLAY-FAKE** — candidate cleanup, pairs with the YT brief's detection tie-in.
- **YT token durability** — spot-check past ~31 May.
- cc-0015 Gate 11 watch — rank 1, window closes 2026-05-26 (tomorrow).
- Q-005 — OPEN, non-blocking.
- **T-MCP-02:** 0 D-01 this session (re-baseline still pending from v3.05 ~101–105 range).

---

## Commits

| Commit | What |
|---|---|
| `bc78511e` | IG publisher v2.0.0→v2.4.0 repo sync (deployed-state reconciliation) |
| `bee872aa` | F-YT-FAILED-NO-RETRY brief (`docs/briefs/yt-publisher-failed-no-retry.md`) |
| `8738c3a0` | Backfill 7 chat-applied 2026-05-24 migration files |
| *(this)* | v3.06 4-way close: session file + sync_state + action_list |
