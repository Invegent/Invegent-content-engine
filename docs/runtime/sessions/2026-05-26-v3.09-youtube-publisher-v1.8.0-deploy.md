# 2026-05-26 Sydney — v3.09: youtube-publisher v1.8.0 DEPLOYED + verified live (Unit A); Unit B / B1-only recovery APPLIED + CCD-verified

**Actors:** Unit A (deploy) — **CCD** (Claude Code / laptop terminal, `C:\Users\parve\Invegent-content-engine`). Unit B1 (recovery DML) — **CCH** (chat via Supabase MCP `apply_migration`), **read-only-verified by CCD** this pass.
**Type:** **1 EF deploy (CCD) + 1 B1-only recovery migration (CCH, CCD-verified); 0 DML by CCD; 0 B2; 0 deploy beyond Unit A.**
**Governance:** Unit A — D-01 `ef_deploy` `818ee949` (review ID prefix — NOT a git object) + PK directive. Unit B1 — D-01 `sql_destructive` `33eeb164-8f1a-47b9-b9c8-071dbd441d7b` (`verdict=agree`, `status=completed`, `requires_pk_escalation=false`, created `2026-05-26 05:42:46Z`). A-then-B ordering — **A done, B1 done, B2/B3 held.**

---

## Outcome

`youtube-publisher` **v1.7.0 → v1.8.0** deployed via the Supabase CLI and verified live (**Unit A**, CCD). Then **Unit B1-only** (the 5 quota/transient drafts) of the 3-bucket failed-backlog recovery was **applied** (migration `yt_failed_backlog_recovery_unit_b_b1_only_20260526`, CCH via MCP) and **independently read-only-verified by CCD** this pass. **B2 (17 token-casualty) and B3 (9 never-connected) were deliberately left untouched.** This is `F-YT-FAILED-NO-RETRY` (failure classification + bounded retry + channel-level auth hold; brief `docs/briefs/yt-publisher-failed-no-retry.md`). **Full Unit B is NOT complete — only B1 ran; B2 is held for a separate Unit B-2 step.**

> **Recording history (no-fabrication trail):** an earlier pass this cycle recorded B1 as **HELD** because CCD found no execution evidence (the deploy directive had forbidden running Unit B, and B1 was applied by CCH out-of-band). A later directive supplied the CCH evidence; CCD then ran the read-only DB checks below, confirmed them, and corrected the record to **B1 APPLIED + verified**. B1 was never asserted on say-so — only after the migration row, the D-01 row, and the live distributions were independently confirmed.

## What was deployed

| Item | Value |
|---|---|
| Function | `youtube-publisher` |
| Version | `youtube-publisher-v1.8.0` (was `v1.7.0`) |
| Project | `mbkmaxqhsohbtwsqolns` (ap-southeast-2) |
| Source | local `main` @ `b983a38` (fast-forwarded from `origin/main`; deployed off the working tree, **not** the avatar feature branch) |
| EF commit | `631fa93a458ea3c92aafade1d368bcb102d34ad4` — *"feat(youtube-publisher): v1.8.0 F-YT-FAILED-NO-RETRY …"*; on-disk file **byte-identical** to this commit (empty `git diff`) |
| Brief commit | `2ea0f83cd6168a778ea5d8ddd06894387d0fd15b` — *"docs(brief): reconcile yt-publisher-failed-no-retry to youtube-publisher v1.8.0"* |
| Flag | `--no-verify-jwt` **preserved** |
| CLI | Supabase CLI v2.75.0 |
| Command | `supabase functions deploy youtube-publisher --no-verify-jwt --project-ref mbkmaxqhsohbtwsqolns` |

## Verification evidence

- **Deploy output:** `Deployed Functions on project mbkmaxqhsohbtwsqolns: youtube-publisher`. (The `WARNING: Docker is not running` line is benign — the CLI bundled/uploaded the asset without a local Docker build.)
- **Live GET** `https://mbkmaxqhsohbtwsqolns.supabase.co/functions/v1/youtube-publisher` (run twice, unauthenticated):
  ```json
  {"ok":true,"function":"youtube-publisher","version":"youtube-publisher-v1.8.0"}
  ```
  `HTTP 200`. The unauthenticated 200 confirms `--no-verify-jwt` is in effect on the deployed function.
- Pre-deploy live version (`v1.7.0`) is per the brief baseline + the directive's known-state, not independently re-read before the deploy; post-deploy `v1.8.0` is directly verified above.

## Pre-deploy gate (why this deploy was safe to run)

The deploy pushes the **local working tree**, not a named commit, so the on-disk file was proven first:
- Branch/remote correct (`Invegent/Invegent-content-engine`); tree clean except untracked `.claude/`.
- On-disk `supabase/functions/youtube-publisher/index.ts` declares `const VERSION = 'youtube-publisher-v1.8.0';` and is byte-identical to EF commit `631fa93a` (the gate PK set: "no deploy unless the local file proves v1.8.0").
- EF commit `631fa93a` confirmed an ancestor of `origin/main`; brief commit `2ea0f83c` confirmed present.
- (History note: the first directive targeted the avatar branch `feat/ai-worker-v2.13.0-avatar-youtube`, where the publisher is still **v1.7.0** and no v1.8.0 / the cited commits existed — that deploy was correctly **halted**. The corrected target was `main`, verified above.)

## Unit B / B1-only — APPLIED + CCD-verified (B2/B3 untouched)

Unit B is the one-time recovery of the (then 31) live `failed` drafts, in 3 buckets (brief §"Unit B"). **Only B1 was applied this cycle:**
- **B1 — 5 drafts** (`429 quota`, transient, guard-pass) → reset `video_status='generated'`, cleared error/backoff/dead_reason, `youtube_upload_attempts=0`. **APPLIED** — migration `yt_failed_backlog_recovery_unit_b_b1_only_20260526` (version `20260526054427`).
- **B2 — 17 drafts** (`invalid_grant`, `published` anomaly) → **UNTOUCHED / held for a separate Unit B-2** (reset + `published→approved` normalisation, when PK directs).
- **B3 — 9 drafts** (never-connected channel, `No refresh token`) → **UNTOUCHED**; onboarding debt, permanently out of scope.

**CCD read-only verification (2026-05-26, this pass — `execute_sql`, SELECT-only, no DML):**
- **Migration row exists:** `supabase_migrations.schema_migrations` → `name='yt_failed_backlog_recovery_unit_b_b1_only_20260526'`, `version='20260526054427'`, `has_statements=true`.
- **D-01 review exists:** `m.chatgpt_review` `33eeb164-8f1a-47b9-b9c8-071dbd441d7b` → `action_type='sql_destructive'`, `verdict='agree'`, `confidence='high'`, `risk_level='medium'`, `requires_pk_escalation=false`, `action_taken='proceed'`, `status='completed'`, `created_at=2026-05-26 05:42:46Z` (≈1.7 min before the migration apply at `05:44:27Z` — correct review→apply order).
- **`video_status` distribution (video formats):** `failed` **31 → 26** (−5), `archived_stale` 15, `generated` 6, `pending` 5, `published` 35, null 1. The −5 on `failed` = the 5 B1 quota drafts recovered (they re-entered the `generated` pool and the live v1.8.0 publisher has since drained some → `published` 33→35).
- **Remaining 26 `failed` bucket:** 17 `Token refresh failed … invalid_grant` / `published` (= **B2, untouched**) + 9 `No refresh token found …` / `published` (= **B3, untouched**). **Zero `quota`/429 rows remain** → B1 was the only bucket recovered, exactly as scoped.
- **Generated-pool guard clean:** of the 6 `generated`, **0** carry a `youtube_video_id`, **0** have a YouTube `m.post_publish` row, **0** carry a stale `youtube_upload_error` → no double-publish exposure; B1 reset cleared error/backoff correctly.

**Conclusion:** the migration row + the D-01 row + the live distributions all corroborate the CCH B1-only report (5 quota recovered; B2 17 + B3 9 untouched). **No B2 recovery occurred or is claimed. Full Unit B is NOT complete.**

## Hard stops (honored)

**CCD this pass:** 0 DML / 0 SQL mutation (read-only `execute_sql` SELECTs only) · 0 B2 · 0 deploy beyond Unit A · 0 cron · 0 dashboard · 0 OAuth/callback · 0 decisions.md · no broadening into cc-0019 / dashboard / avatar architecture / HeyGen architecture. The B1 recovery migration was applied by **CCH** (chat via MCP); CCD only read-only-verified it.

## Carry

- **A done; B1 done + CCD-verified; B2/B3 held.** When PK directs **Unit B-2**: `sql_destructive` D-01 + PK approval phrase → `apply_migration` the snapshot-first `DO`-block bounded to the 17 B2 ids (reset + `published→approved`) → re-verify → record. B3 (9 never-connected) is onboarding debt, not retry debt.
- The avatar-pipeline STATUS BLOCK's pipeline table still lists the publisher at its v3.08-milestone version; live is now v1.8.0 (this cycle).
