# 2026-05-19 Sydney evening — v2.87: cc-0017d v1.1 doc-only patch CLOSED

**Session slug:** `v2.87-cc0017d-v1.1-doc-patch`
**Session theme:** Doc-only patch close. Bring cc-0017d brief v1.0 source-of-truth into alignment with v2.86 production-apply + V-check reality.
**Prior session:** `2026-05-19-cc0017d-applied-with-vcheck-correction.md` (v2.86 close commit `3dc99fb`)
**Doc patch commit:** [`f0367405c04dd21e4e08bc4c8beebd4d77635229`](https://github.com/Invegent/Invegent-content-engine/commit/f0367405c04dd21e4e08bc4c8beebd4d77635229)
**Sync-close commit:** this commit (per-session detail standalone) + follow-on atomic push_files for sync_state + action_list (per L-v2.85-e mitigation)

---

## Outcome

cc-0017d v1.1 doc-only patch **CLOSED** at commit `f0367405c04dd21e4e08bc4c8beebd4d77635229`. Exactly 4 files patched and read-back verified. No production mutations. No D-01 fire. No Wave 0e work started. cc-0017d v1.1 source-of-truth now matches the v2.86 production-apply + V-check reality. Next active item: **Wave 0e case history / audit brief authoring** (promoted from rank 2 P2 → rank 1 P1).

---

## What landed in the doc patch (commit `f0367405`)

Single atomic push_files commit. Parent: `bf571eba3df93ceb2dca4fc153f0b73b84014dfa`. Tree: `fee6fa04b4e9c0e5f7ce414d5a0d9f3cea7b1606`. Branch: `main`.

| # | Path | Blob SHA | Size | Change summary |
|---|---|---|---|---|
| 1 | `docs/briefs/cc-0017d-friction-case-mutation-functions.md` | `04b1edf10c9a33bfc02be8d9242ea66669dadd76` | 16,336 B | Header `Version: v1.1` + `Status: CLOSED-APPLIED-WITH-VCHECK-CORRECTION (v2.86, 2026-05-19 Sydney evening)`; v1.1 patch summary block added below header; brief structure table v1.1 status notes added; `purge_test_case` constraints annotated with cross-fingerprint internal-prefix exception; `Loose resolve_case` rejected-from-scope item annotated with v1.1 status (strict retained at apply). |
| 2 | `docs/briefs/cc-0017d/migration-sql.md` | `2a8e02877aa9286f492ad113d12951d20a42e893` | 26,673 B | v1.1 Addendum prepended at top documenting: **Substitution class 1** (24× RAISE EXCEPTION `%%`→`%` — positional placeholder vs literal-percent escape distinction), **Substitution class 2** (6× `friction.case%ROWTYPE` → `friction."case"%ROWTYPE` — reserved SQL:2016 keyword strict-parsing in PL/pgSQL DECLARE), **Path B-prime apply sequence** (3 attempts, 2 inline fix rounds before success), and **L-v2.86-a HIGH-SIGNAL pre-apply discipline recommendation** (transactional EXEC with marker `PERFORM` block inside `DO $$ ... EXCEPTION WHEN OTHERS THEN NULL; ... $$` wrapper to force runtime RAISE parsing). v1.0 SQL bodies preserved as authored below the addendum for historical-record auditability. Function 4 `mark_duplicate` body annotated with v1.1 inline note re internal-prefix audit namespace. Functions 4 + 6 COMMENTs updated. |
| 3 | `docs/briefs/cc-0017d/vchecks.md` | `15b924fea2ec8a69face39ee5b307e0929361d93` | 27,782 B | v1.1 Addendum appended at end documenting: **Drift 1** — V-D-setup fell back to direct `INSERT INTO friction.case` because cc-0017b emission_rule CHECK rejects manual-source test-prefix payloads (fallback fixture SQL pattern preserved; consequences for V-F1 `out_events_deleted` count); **Drift 2** — `mark_duplicate` writes audit under internal prefix `cc-0017d/mark_duplicate/...` intentionally outside the strict test-prefix regex (orphaned audit row implication + detection pattern); **Drift 3** — corrective cleanup v1 (PK-specified slash variant, zero-effect due to hyphen-prefix fixture-naming drift in `dedupe_fingerprint`) and cleanup v2 (PK-approved adjusted pattern dropping trailing `/`, removed exactly 1 row). Fixture-naming slash-prefix convention reaffirmed; corrected V-F1 expected matrix table added; recommended V-Z3 cross-check for future briefs introducing `mark_duplicate`-class functions. Inline v1.1 notes at V-D-setup, V-D4, V-F1, V-Z1. |
| 4 | `docs/briefs/cc-0017d/lessons-metadata-changelog.md` | `ec2927232123646533a8e024514ceb36f08e4459` | 20,032 B | Lesson numbering reconciled to authoritative v2.86 a–e set per sync_state: **L-v2.86-a** (HIGH-SIGNAL, NEW from apply, pre-apply syntactic validation via transactional EXEC); **L-v2.86-b** (NEW from authoring, `out_`-prefix on RETURNS TABLE columns, REALISED); **L-v2.86-c** (NEW from apply, reserved SQL keyword ROWTYPE quoting); **L-v2.86-d** (NEW from authoring, cross-column CHECK pre-validation inline in PL/pgSQL, REALISED via `track_or_defer_requires_next_review` pre-validation in `friction.triage_case`); **L-v2.86-e** (NEW from apply, V-check fixture-data convention alignment). v1.0 draft "legacy/new coexistence" candidate (had been authored under a draft v1.0 L-v2.86-a numbering) recorded as retired/not-promoted. Metadata table: Version v1.1; Status CLOSED-APPLIED-WITH-VCHECK-CORRECTION; applied + v1.1 patched dates; v1.0 apply migrations list (5: main + fixture seed + cleanup v1 + cleanup v2 + chatgpt_review close); V-check final outcome 22/23 strict PASS + V-F1 PARTIAL; D-01 review_id `206d2258-…` AGREE / resolved / resolved_by `cc-0017d-close-v2.86`; status timeline. v1.1 changelog entry appended with full per-file scope documentation. |

**Out of scope at v1.1 (unchanged):** `risks-and-grants.md`, `preflight-pset.md`, `hardstop-rollback.md`, `d01-postapply-deferred.md`. The latter is now historical record (D-01 fired and resolved at v2.86).

---

## Sequence this session

1. **Session resume.** Compacted transcript loaded. Per-session pointer index + action list confirmed at v2.86. The v1.1 doc patch commit `f0367405…` had already landed in the prior turn before compaction; this session's task was the doc-patch close.
2. **PK directive re-confirmation.** Single atomic push_files commit. Exactly 4 files. No production mutations. No D-01. No Wave 0e work. Verify all 4 files landed and report commit SHA.
3. **No redundant commit created.** Recognized that the v1.1 doc patch commit had already landed at `f0367405…`. Proceeded to read-back verification instead of issuing a redundant commit.
4. **Read-back verification.** Fetched all 4 files from `main`. Blob SHAs matched the push_files response byte-for-byte. Spot-checks confirmed v1.1 content in each: header bumps, addenda, lesson reconciliation, metadata updates.
5. **Status report to PK.** Reported commit SHA + per-file SHAs + scope compliance against the 9-item directive from the doc-patch session.
6. **PK directive for sync close.** Three-file sync close: this per-session file + sync_state v2.87 + action_list v2.87. Recording requirements specified. Wave 0e to follow only after sync close verifies cleanly.
7. **Sync close commit sequence.** L-v2.85-e split-commit mitigation re-applied: this per-session detail file lands first as a standalone commit; sync_state + action_list bumps follow in a separate atomic 2-file push_files commit.

---

## Production mutations

**ZERO.** No `apply_migration` calls. No `execute_sql` DML. No edge-function deploys. No `m.chatgpt_review` writes. The v1.1 doc patch was authored before this session in the prior turn; this session's only chat-side actions were read-back GETs against `main` and the sync-close commits to the docs tree.

---

## D-01 fires

**ZERO.** Doc-only patch per PK explicit scope. No production mutation → no D-01 requirement under L62.

---

## What this session does NOT touch (per PK directive)

- **No Wave 0e work.** Wave 0e (case history / audit) brief authoring is the **next active item** but is explicitly deferred until this sync close verifies cleanly. After sync close lands and verifies, Wave 0e authoring opens as the next task.
- **No production mutations.** Schema state at end of v2.87 = schema state at end of v2.86.
- **No changes to other cc-0017d sub-files** beyond the 4 listed above.
- **No `decisions.md` change.**
- **No dashboard PHASES update.** 40th consecutive deferral carried (was 39 at v2.86).

---

## Source-of-truth alignment achieved

The v1.0 brief was authored on 2026-05-19 Sydney morning before production apply. Production apply on 2026-05-19 Sydney evening surfaced two compile defects (RAISE %% / ROWTYPE quoting) and three V-check expectation drift classes (V-D-setup direct-INSERT fallback / mark_duplicate internal-prefix audit / fixture-naming hyphen vs slash). These were captured in the action_list and sync_state at v2.86 close but NOT in the brief itself — at v2.86 the brief still showed `Version: v1.0` and `Status: AUTHORED_PENDING_D01`, even though production state was CLOSED-APPLIED-WITH-VCHECK-CORRECTION.

With the v1.1 doc patch landed at `f0367405…`:
- Brief header reflects v1.1 + CLOSED-APPLIED-WITH-VCHECK-CORRECTION
- migration-sql.md documents both substitution classes inline at the top, with v1.0 SQL preserved as authored below
- vchecks.md documents all three drift classes at the end, with corrected V-F1 expected matrix
- Lessons reconciled to authoritative v2.86 a–e set per sync_state (no more numbering divergence between the brief and the sync_state)
- Metadata table reflects the full v1.0 + v2.86 apply + v1.1 patch timeline

A future operator (or future Claude session) reading the brief in isolation now finds the same operational reality the sync_state and action_list have already captured. **cc-0017d v1.1 source-of-truth matches the production apply / V-check reality.** No re-litigation required in subsequent waves.

---

## Carries into v2.87

| Item | State |
|---|---|
| **Wave 0e — case history / audit brief authoring** | **P1 NEW (promoted from rank 2 P2 v2.86 → rank 1 P1 v2.87).** Next active item. Opens after sync close verifies cleanly. |
| **1-week observation window** | ACTIVE 2026-05-19 → 2026-05-26. Wave 7 (cc-0015) gated on this. Day 1 of 7 → Day 1 of 7 (same calendar day; sync close adds no elapsed observation time). |
| **First cc-0017d-post-apply cron 85 fire** | PENDING (carries from v2.86). |
| **22 outstanding close-the-loop UPDATEs** | Unchanged. v2.87 added 0; v2.86's 1 was closed in-session at v2.86. |
| **Memory cap** | 19/30 unchanged. |
| **T-MCP-02 cumulative** | ~85 unchanged (read-only GETs against `main` during this session do not consume T-MCP-02). |
| **State-capture exceptions cumulative** | 1 unchanged. |
| **Dashboard PHASES** | 40th consecutive deferral (carries from 39th at v2.86). No file-touch. |
| **Plan gate 11** (observation window 2026-05-19 → 2026-05-26) | ACTIVE. |
| **Plan gate 13** (Wave 0e case history / audit) | OPEN; **next**. |
| **L-v2.85-e mitigation** | Re-applied at this v2.87 sync close (1+2 split commit). Cumulative occurrences: 2 (v2.86 + v2.87). |

---

## Lessons exercised v2.87

**No new lesson exercises.** Doc-only patch + sync close: no production action, no V-check, no D-01 cycle. L-v2.85-e mitigation re-applied for the 1+2 split commit close (second occurrence at consecutive sessions; promotion-eligible after one more cycle).

No new L-v2.87 candidates surfaced.

---

## Honest limitations v2.87

- **Doc-only patch + sync close.** No production schema change, no migration applied, no V-check run, no fresh empirical evidence. The schema state at end of v2.87 = schema state at end of v2.86. v2.87 changes are purely at the docs-tree layer.
- **v1.0 SQL preserved verbatim in `migration-sql.md` below the v1.1 Addendum.** Future authors using this brief as a template MUST apply both substitution classes (RAISE `%%`→`%` + `friction.case%ROWTYPE` → `friction."case"%ROWTYPE`) before any production apply attempt. The deployed `cc_0017d_friction_case_mutation_functions` migration contains the corrected form, NOT the v1.0 SQL as written below the addendum. This is a deliberate authoring choice to preserve the historical-record audit trail; the v1.1 Addendum at the top of `migration-sql.md` flags it loudly.
- **V-D-setup block in `vchecks.md` still shows the `emit_event` path** as the documented setup, with an inline v1.1 note + addendum-end Drift 1 documenting the fallback direct-INSERT path. Future-author convention: when `emit_event` emission_rule rejects test-prefix payloads, fall back to direct INSERT and document at the V-D-setup block.
- **V-Z3 cross-check not retroactively added** to cc-0017d V-checks (recommended in the v1.1 Addendum for future briefs introducing a `mark_duplicate`-class function; v2.86 corrective cleanup v2 already resolved the residue empirically for cc-0017d itself).
- **1+2 split commit pattern** carries the L58 atomic-close-the-loop / L-v2.85-e length-budget trade-off into this session as well. The per-session file and the sync_state+action_list bumps are causally one logical close split into two commits for response-truncation safety.
- **Wave 0e is the next active item but is not started in this session.** A v2.88+ session will pick it up after this v2.87 sync close verifies cleanly.
- **No `decisions.md` change.** This is a doc-patch close; no new architectural decisions, no scope amendments, no overrides.

---

## Commit-level provenance

- **v1.1 doc patch (4 files):** `f0367405c04dd21e4e08bc4c8beebd4d77635229` — landed in the turn immediately before compaction.
- **v2.87 sync close, part 1 of 2 (this file):** this commit — standalone per-session detail.
- **v2.87 sync close, part 2 of 2 (sync_state + action_list atomic):** to follow as a 2-file push_files commit immediately after this commit lands and is verified.

**Verification of v1.1 doc patch (this session, read-only):**

```
GET docs/briefs/cc-0017d-friction-case-mutation-functions.md → sha 04b1edf1… (matches)
GET docs/briefs/cc-0017d/migration-sql.md                     → sha 2a8e0287… (matches)
GET docs/briefs/cc-0017d/vchecks.md                           → sha 15b924fe… (matches)
GET docs/briefs/cc-0017d/lessons-metadata-changelog.md        → sha ec292723… (matches)
```

All four match the SHAs returned in the v1.1 push_files commit response. Files are intact on `main`. Doc-patch close confirmed.

---

*End of v2.87 per-session detail.*
