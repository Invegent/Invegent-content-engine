# ICE Audit Run — 2026-04-28 — Data Auditor

## F-2026-04-28-D-001  ·  HIGH  ·  closed-action-taken
**Role:** Data Auditor
**Raised:** 2026-04-28 04:00 UTC (audit run: runs/2026-04-28-data.md)
**Area:** k registry coverage
**Object:** Phase B slot-driven tables and views

### Issue
Several recently-added Phase B / slot-driven objects have no table purpose registered, including `m.slot`, `m.signal_pool`, `m.slot_fill_attempt`, `m.slot_alerts`, `m.hot_breaking_pool`, `m.slots_in_critical_window`, and related policy/config tables in `t` and `c`. These are not old low-traffic artifacts; the snapshot says they were created between 2026-04-22 and 2026-04-27 and overlap with the active Phase B + slot-driven build. Missing table purpose on newly-created critical pipeline objects weakens the `k` registry as the system map.

### Evidence
- Snapshot section: `Table Registry Coverage` shows 56 ICE objects missing purpose, with `c` at 52.9% documented and `m` at 64.2%.
- Snapshot section: `Top 30 tables missing purpose` lists every top undocumented object as created in the last 7 days.
- Snapshot note: these undocumented objects "overlap with the Phase B + slot-driven build" and are "the highest-priority documentation gaps because the system actively depends on them."

### Recommended action
Backfill table-level purposes first for the active Phase B operational path: `m.slot`, `m.signal_pool`, `m.slot_fill_attempt`, `m.slot_alerts`, the slot/pool health views, and the client/reference policy tables that control matching, dedupe, freshness, reuse, and synthesis.

### Resolution
**Closed 2026-04-28 by operator — Action taken (extended scope).** Backfilled table-level purposes for ALL 56 ICE tables missing purpose, not just the 31 Phase B tables originally scoped. Two migrations applied:
1. `20260428040000_audit_f001_phase_b_table_purposes_backfill` (commit `491e157`) — 31 Phase B / slot-driven tables created 22-27 April.
2. `20260428043000_audit_f001_followup_older_table_purposes_backfill` (commit `8b2d669`) — 25 older tables created pre-22 April.

**Coverage: 72.0% → 100.0%** across all 7 ICE schemas. **200/200 ICE tables now have a registered purpose.** The follow-up extension was operator-initiated ("while we're at it, fill in the 25 too") — chose to close completely rather than carry to backlog.

Schema verification done on 3 uncertain entries during closure: `c.brand_stakeholder` is persona definitions (not real humans); `c.brand_avatar` is HeyGen generation records per stakeholder; `c.client_avatar_profile` is active avatar+voice production selection; `c.platform_channel` is global catalogue (no client_id).

**Forward discipline rule:** new tables ship with purpose registered at creation, not retroactively (Lesson #35 candidate).

---

## F-2026-04-28-D-002  ·  MEDIUM  ·  closed-action-pending
**Role:** Data Auditor
**Raised:** 2026-04-28 04:00 UTC (audit run: runs/2026-04-28-data.md)
**Area:** k registry coverage
**Object:** k.column_registry coverage for c and f schemas

### Issue
Column-purpose coverage is effectively absent for two active ICE-owned schemas: `c` and `f`. The snapshot reports 0 documented columns out of 479 in `c`, and 0 documented columns out of 195 in `f`. This looks less like normal documentation backlog and more like either a registry refresh/write gap or a never-started column-purpose pass for two core business schemas.

### Evidence
- Snapshot section: `Column Registry Coverage` shows `c`: 479 total columns, 0 with purpose, 479 missing, 0.0% documented.
- Snapshot section: `Column Registry Coverage` shows `f`: 195 total columns, 0 with purpose, 195 missing, 0.0% documented.
- Snapshot note: "Two ICE schemas (`c` and `f`) are at 0% column coverage," and this is "suspicious."

### Recommended action
First verify whether `k.column_registry` refresh/write logic is correctly covering `c` and `f`. If the registry is working, backfill only the high-leverage non-obvious business-control columns first rather than obvious keys/timestamps.

### Resolution
**Closed 2026-04-28 by operator — Action pending (overnight job designed).** Verified during closure: `k.column_registry` refresh IS working — `c.client_publish_profile` has 15+ rows with full type / nullable / FK metadata, just empty `column_purpose` fields. The 0% is genuine documentation backlog, not a broken pipeline. Manual documentation of 674 columns is not session-feasible (~11 hours). Three-phase overnight-assisted plan designed at `docs/audit/decisions/f002_column_backfill_plan.md` (commit `78b7eeda`): (1) overnight snapshot of column metadata + sample values, (2) ChatGPT proposes purposes (no write access — proposals only), (3) operator approves batches in chat, Claude applies UPDATE migrations. Prioritisation: P1 booleans/enums → P2 thresholds → P3 JSONB configs first; surrogate keys and timestamps may not need explicit purposes. Build estimate: ~2 hr before first overnight run. Build started same day; see addenda below.

#### Addendum 2026-04-28 afternoon — Phase A (P1) applied

Migration `audit_f002_p1_column_purposes_corrected` (version 20260428055331, commit `4e12cab`).

**Coverage of c+f schemas: 0% → 11.7%** (79 of 674 columns documented). c at 13.8%, f at 6.7%.

CC produced 83 P1 proposals (above 30–60 expected range due to ~50 booleans + ~33 enum text columns in P1 scope). ChatGPT reviewed proposals before apply and caught 5 safety issues:

1. **4 LOW-confidence rows in CC's draft migration** — removed from apply, deferred to operator joint session at `docs/audit/decisions/f002_p1_low_confidence_followup.md`. The 4: `c.brand_avatar.avatar_gen_status`, `c.client_avatar_profile.avatar_status`, `c.client_brand_asset.asset_type`, `f.raw_metric_point.entity_type` (all four had no populated rows or only one observed value, so purpose was inferred not verified).
2. **Consent wording too prescriptive** — `c.brand_avatar.consent_required` and `c.client_avatar_profile.voice_clone_enabled` rewritten to not imply false=no-consent-needed or that an enabled flag is itself the consent record.
3. **Pipeline-specificity overstating** — `c.client_publish_profile.r6_enabled`, `c.client_platform_profile.use_markdown`, `c.client_support_item.is_featured` softened to leave specific downstream behaviour to source code.
4. **Transient operational state in column purposes** — D156/D162 paused-layer references removed from `c.external_reviewer.is_active` and `c.external_reviewer_rule.is_active` (paused-state belongs in decisions log, not column docs).
5. **Inline post-apply verification missing** — added `RAISE EXCEPTION` if updated count != 79; verification passed.

**Forward discipline captured as Lesson #37:** ChatGPT external review of CC proposals before apply is the pattern for column documentation passes. CC drafts proposals + draft migration; ChatGPT reviews proposals; chat applies corrected migration via Supabase MCP. Caught 5 real safety issues in F-002 Phase A; pattern is reusable for any registry-data pass where wording precision matters.

Phase B (P2 — numeric thresholds, limits, counts, scores, weights, priorities) prompt sent to CC same afternoon — Phase B addendum below.

#### Addendum 2026-04-28 evening — Phase B (P2) applied

Migration `audit_f002_p2_column_purposes_corrected` (version 20260428163000, commit `0299c9a`).

**Coverage of c+f schemas: 11.7% → 16.2%** (30 additional columns documented; total 109/674). c at 18.6%, f at 10.3%.

CC produced 31 P2 proposals (within 30-50 expected range — Phase A discipline applied). The 1 LOW-confidence row was correctly self-isolated into a Deferred section, NOT carried into the draft migration. ChatGPT reviewed proposals before apply and caught 14 row wording issues. Same patterns as Phase A:

1. **Removed external/stale platform claims** —
   - `c.client_audience_policy.min_boost_audience_size`: removed Meta Ads platform floor (1000) reference (external + potentially stale)
   - `c.client_publish_profile.max_per_day`: removed UTC day claim (publisher scheduling not verified UTC)
2. **Softened precedence assertions** —
   - `c.client_publish_profile.min_gap_minutes` / `min_post_gap_minutes_override`: changed "takes precedence when set" to "may be superseded when configured" (override goes both ways in production data — sometimes relaxes, sometimes tightens; precedence is a publisher-code claim that wasn't verified)
3. **Removed unverified arithmetic / speculation** —
   - `f.canonical_content_body.word_count`: removed `allow_missing_body` reference (that gates on body presence, not length)
   - `f.ingest_run.fetched_count`: removed `= inserted + updated + skipped + lost-to-errors` invariant (unverified)
   - `f.ingest_run.error_count`: removed speculation about why it's always 0
4. **Softened code-path claims (Phase A pattern continued)** —
   - `c.client_brand_profile.max_output_tokens`: removed `assemblePrompts()` function reference
   - `c.client_platform_profile.max_chars` / `min_chars`: removed "Read by ai-worker"
   - `c.client_publish_profile.max_queued_per_platform`: removed "ai-worker pauses producing"
   - `f.video_analysis.view_count`: removed "popularity signal during video-format synthesis"
5. **Removed interpretation in column purpose** —
   - `c.client_match_weights.fitness_weight`: softened "defaults from t-schema apply"
   - `c.onboarding_submission.info_request_count`: removed "friction metric" interpretation

**1 LOW row deferred** (correctly NOT in migration per Phase A discipline): `c.client_match_weights.quality_weight` — column name conflicts with table_purpose factor enumeration ("vertical fit / recency / freshness"). Captured at `docs/audit/decisions/f002_p2_low_confidence_followup.md`.

**Forward discipline captured as Lesson #38:** chat-side migration verification uses count-delta, not time-window. First Phase B apply attempt failed verification — the `RAISE EXCEPTION if count(updated_at within last 2 min) != 30` block returned 109 because `k.refresh_column_registry`'s ON CONFLICT DO UPDATE bumps `updated_at` on every row regardless of whether `column_purpose` changed (intentional refresh behaviour). Capture before-count, run UPDATEs, expect after-count − before-count = expected delta. This pattern is immune to refresh side-effects. Now the chat-side default for any closure migration that updates `k.column_registry` rows.

Phase C (P3 — JSONB configs, ~32 undocumented columns) prompt sent to CC same evening. F-002 closure status remains `closed-action-pending` until P3 completes; will transition to `closed-action-taken` on P3 apply.

---

## F-2026-04-28-D-003  ·  MEDIUM  ·  closed-action-taken
**Role:** Data Auditor
**Raised:** 2026-04-28 04:00 UTC (audit run: runs/2026-04-28-data.md)
**Area:** Migration discipline
**Object:** recent Stage 12 migration history

### Issue
The applied migration history shows two Stage 12 migrations apparently reapplied or duplicated under similar names on 2026-04-27. `stage_12_053_fill_pending_slots_ai_job_upsert` appears at both 04:36 and 05:42, while `stage_12_052_drop_shadow_filter` / `stage_12_052_drop_shadow_filter_for_phase_b_reactivation` appear at both 04:20 and 05:25. This may be intentional during Phase B testing, but without an explicit note it creates ambiguity about which migration is canonical.

### Evidence
- Snapshot section: `Migration History` shows `20260427054214 stage_12_053_fill_pending_slots_ai_job_upsert`.
- Snapshot section: `Migration History` also shows `20260427043659 stage_12_053_fill_pending_slots_ai_job_upsert`.
- Snapshot section: `Migration History` shows both `20260427052534 stage_12_052_drop_shadow_filter` and `20260427042009 stage_12_052_drop_shadow_filter_for_phase_b_reactivation`.
- Snapshot note: these "appear to have been applied twice with similar names" and may indicate a discipline gap.

### Resolution
**Closed 2026-04-28 by operator — Action taken.** Investigated via SQL hash comparison during closure. The 052 pair is fine — two related-but-distinct migrations with different names AND different SQL (`drop_shadow_filter_for_phase_b_reactivation` 1478 chars vs `drop_shadow_filter` 1007 chars). The 053 pair IS a discipline gap — same migration name `stage_12_053_fill_pending_slots_ai_job_upsert`, different SQL (20360 chars vs 18707 chars), different hashes (`ed8145a0...` vs `3af78ee9...`), applied 1 hour apart. The second was a fix iteration of the first (consistent with Lesson #33 — destructure error fix). Forward rule locked at `docs/audit/decisions/migration_naming_discipline.md` (commit `1157671d`): a migration name is a permanent identity; fix iterations get a new sequential number AND a distinguishing suffix. Captured as Lesson #36 candidate in standing memory.

Detector function `k.fn_check_migration_naming_discipline()` deployed via audit slice 1 migration (commit `27ff3b3`) — returns same-name-different-SQL violations on demand. Currently returns 1 row (this historical violation). Future audit cycles close this row as `closed-redundant` referencing F-2026-04-28-D-003.

---

## Summary
- **3 findings raised:** 1 High, 2 Medium
- **3 findings closed:** 2 action-taken (F-001, F-003), 1 action-pending (F-002 — P1 + P2 applied same day, P3 still pending)
- **0 observations**
- **Coverage impact:** k.table_registry purpose coverage **72.0% → 100.0%** (all 7 ICE schemas at 100%, 200/200 ICE tables documented). k.column_registry c+f coverage **0% → 16.2%** (109/674 columns; 79 from P1 + 30 from P2).
- **Forward discipline captured:** Lessons #35 (new tables ship with docs at creation), #36 (migration names are permanent), #37 (ChatGPT external review of CC proposals before apply), #38 (count-delta verification not time-window).
- **Audit loop validation:** First cycle completed end-to-end through two CC iterations same day. Snapshot → ChatGPT pass → 3 findings → operator approval → 3 closures + 1 partial (P1 + P2 applied, P3 pending). F-001 closed beyond original scope (entire registry to 100%, not just Phase B). F-002 captured with buildable design, Phase A applied same day, Phase B applied same evening with same review pattern producing same kinds of safety issues — pattern is reproducible. F-003 locked forward discipline + automated detector. Loop works, ChatGPT review pattern is the steady state for any registry-data closure where wording precision matters, count-delta is the steady state for verification of column_registry UPDATEs. Slice 1 (audit recurrence prevention) added structural protection: PENDING_DOCUMENTATION sentinel + 14-day grace + DEFERRED escape hatch + F-003 detector.
