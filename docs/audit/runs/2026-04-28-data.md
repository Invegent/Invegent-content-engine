# ICE Audit Run — 2026-04-28 — Data Auditor

## F-2026-04-28-D-001  ·  HIGH  ·  closed-action-taken
**Role:** Data Auditor
**Raised:** 2026-04-28 04:00 UTC (audit run: runs/2026-04-28-data.md)
**Area:** k registry coverage
**Object:** Phase B slot-driven tables and views

### Issue
Several recently-added Phase B / slot-driven objects have no table purpose registered, including `m.slot`, `m.signal_pool`, `m.slot_fill_attempt`, `m.slot_alerts`, `m.hot_breaking_pool`, `m.slots_in_critical_window`, and related policy/config tables in `t` and `c`. These are not old low-traffic artifacts; the snapshot says they were created between 2026-04-22 and 2026-04-27 and overlap with the active Phase B + slot-driven build. Missing table purpose on newly-created critical pipeline objects weakens the `k` registry as the system map.

### Evidence
- Snapshot section: `Table Registry Coverage` shows 56 ICE objects missing purpose, with `c` at 52.9% documented and `m` at 64.2%.
- Snapshot section: `Top 30 tables missing purpose` lists every top undocumented object as created in the last 7 days.
- Snapshot note: these undocumented objects "overlap with the Phase B + slot-driven build" and are "the highest-priority documentation gaps because the system actively depends on them."

### Recommended action
Backfill table-level purposes first for the active Phase B operational path: `m.slot`, `m.signal_pool`, `m.slot_fill_attempt`, `m.slot_alerts`, the slot/pool health views, and the client/reference policy tables that control matching, dedupe, freshness, reuse, and synthesis.

### Resolution
**Closed 2026-04-28 by operator — Action taken (extended scope).** Backfilled table-level purposes for ALL 56 ICE tables missing purpose, not just the 31 Phase B tables originally scoped. Two migrations applied:
1. `20260428040000_audit_f001_phase_b_table_purposes_backfill` (commit `491e157`) — 31 Phase B / slot-driven tables created 22-27 April.
2. `20260428043000_audit_f001_followup_older_table_purposes_backfill` (commit `8b2d669`) — 25 older tables created pre-22 April.

**Coverage: 72.0% → 100.0%** across all 7 ICE schemas. **200/200 ICE tables now have a registered purpose.** The follow-up extension was operator-initiated ("while we're at it, fill in the 25 too") — chose to close completely rather than carry to backlog.

Schema verification done on 3 uncertain entries during closure: `c.brand_stakeholder` is persona definitions (not real humans); `c.brand_avatar` is HeyGen generation records per stakeholder; `c.client_avatar_profile` is active avatar+voice production selection; `c.platform_channel` is global catalogue (no client_id).

**Forward discipline rule:** new tables ship with purpose registered at creation, not retroactively (Lesson #35 candidate).

---

## F-2026-04-28-D-002  ·  MEDIUM  ·  closed-action-taken
**Role:** Data Auditor
**Raised:** 2026-04-28 04:00 UTC (audit run: runs/2026-04-28-data.md)
**Area:** k registry coverage
**Object:** k.column_registry coverage for c and f schemas

### Issue
Column-purpose coverage is effectively absent for two active ICE-owned schemas: `c` and `f`. The snapshot reports 0 documented columns out of 479 in `c`, and 0 documented columns out of 195 in `f`. This looks less like normal documentation backlog and more like either a registry refresh/write gap or a never-started column-purpose pass for two core business schemas.

### Evidence
- Snapshot section: `Column Registry Coverage` shows `c`: 479 total columns, 0 with purpose, 479 missing, 0.0% documented.
- Snapshot section: `Column Registry Coverage` shows `f`: 195 total columns, 0 with purpose, 195 missing, 0.0% documented.
- Snapshot note: "Two ICE schemas (`c` and `f`) are at 0% column coverage," and this is "suspicious."

### Recommended action
First verify whether `k.column_registry` refresh/write logic is correctly covering `c` and `f`. If the registry is working, backfill only the high-leverage non-obvious business-control columns first rather than obvious keys/timestamps.

### Resolution
**Closed 2026-04-28 by operator — Action taken (three-phase same-day apply).** Verified during closure: `k.column_registry` refresh IS working — `c.client_publish_profile` has 15+ rows with full type / nullable / FK metadata, just empty `column_purpose` fields. The 0% is genuine documentation backlog, not a broken pipeline. Manual documentation of 674 columns is not session-feasible (~11 hours). Three-phase overnight-assisted plan designed at `docs/audit/decisions/f002_column_backfill_plan.md` (commit `78b7eeda`): (1) overnight snapshot of column metadata + sample values, (2) ChatGPT proposes purposes (no write access — proposals only), (3) operator approves batches in chat, Claude applies UPDATE migrations. Prioritisation: P1 booleans/enums → P2 thresholds → P3 JSONB configs first; surrogate keys and timestamps may not need explicit purposes.

All three phases applied same day; see addenda below. **Final coverage of c+f schemas: 0% → 20.2% (136/674 columns documented).** c at 22.3%, f at 14.9%. F-002 transitions to `closed-action-taken`.

#### Addendum 2026-04-28 afternoon — Phase A (P1) applied

Migration `audit_f002_p1_column_purposes_corrected` (version 20260428055331, commit `4e12cab`).

**Coverage of c+f schemas: 0% → 11.7%** (79 of 674 columns documented). c at 13.8%, f at 6.7%.

CC produced 83 P1 proposals (above 30–60 expected range due to ~50 booleans + ~33 enum text columns in P1 scope). ChatGPT reviewed proposals before apply and caught 5 safety issues:

1. **4 LOW-confidence rows in CC's draft migration** — removed from apply, deferred to operator joint session at `docs/audit/decisions/f002_p1_low_confidence_followup.md`. The 4: `c.brand_avatar.avatar_gen_status`, `c.client_avatar_profile.avatar_status`, `c.client_brand_asset.asset_type`, `f.raw_metric_point.entity_type`.
2. **Consent wording too prescriptive** — `c.brand_avatar.consent_required` and `c.client_avatar_profile.voice_clone_enabled` rewritten.
3. **Pipeline-specificity overstating** — `c.client_publish_profile.r6_enabled`, `c.client_platform_profile.use_markdown`, `c.client_support_item.is_featured` softened.
4. **Transient operational state in column purposes** — D156/D162 paused-layer references removed from `c.external_reviewer.is_active` and `c.external_reviewer_rule.is_active`.
5. **Inline post-apply verification missing** — added `RAISE EXCEPTION` if updated count != 79; verification passed.

**Forward discipline captured as Lesson #37:** ChatGPT external review of CC proposals before apply.

#### Addendum 2026-04-28 evening — Phase B (P2) applied

Migration `audit_f002_p2_column_purposes_corrected` (version 20260428064115, commit `0299c9a`).

**Coverage of c+f schemas: 11.7% → 16.2%** (30 additional columns documented; total 109/674). c at 18.6%, f at 10.3%.

CC produced 31 P2 proposals (within 30-50 expected range). The 1 LOW-confidence row was correctly self-isolated into a Deferred section, NOT carried into the draft migration. ChatGPT reviewed proposals before apply and caught 14 row wording issues. Same patterns as Phase A: removed external/stale platform claims (Meta Ads floor, UTC day), softened precedence assertions (min_gap_minutes / override), removed unverified arithmetic / speculation (fetched_count invariant, error_count speculation), softened code-path claims (assemblePrompts, ai-worker reads), removed interpretation ("friction metric", "defaults from t-schema").

**1 LOW row deferred** (correctly NOT in migration per Phase A discipline): `c.client_match_weights.quality_weight` — column name conflicts with table_purpose factor enumeration. Captured at `docs/audit/decisions/f002_p2_low_confidence_followup.md`.

**Forward discipline captured as Lesson #38:** chat-side migration verification uses count-delta, not time-window. First Phase B apply attempt failed verification because `k.refresh_column_registry`'s ON CONFLICT DO UPDATE bumps `updated_at` on every row regardless of whether `column_purpose` changed. Capture before-count, run UPDATEs, expect after-count − before-count = expected delta. Now the chat-side default for any closure migration that updates `k.column_registry` rows.

#### Addendum 2026-04-28 evening — Phase C (P3) applied; F-002 closes

Migration `audit_f002_p3_column_purposes_corrected` (version 20260428080943, commit pending in this same push).

**Coverage of c+f schemas: 16.2% → 20.2%** (27 additional columns documented; total 136/674). c at 22.3%, f at 14.9%.

CC produced 29 P3 proposals (28 effective + 1 surrogate excluded). The 1 LOW-confidence row (`f.raw_content_item.payload_hash`, 0/2123 populated) was correctly self-isolated into a Deferred section.

**Two-layer review introduced.** Phase C's JSONB scope introduced a new failure mode: CC's V3 schema verification sampled per-row, not cross-row, so single-row shape claims missed cross-row diversity. Chat-side sanity check ran live SQL queries on shape and caught 3 of 4 of CC's stated observations were wrong:

1. **`c.client_channel.config`** — CC: "empty `{}` across all 4 rows." Reality: 2 of 4 rows are YouTube OAuth credential storage (`channel_id`, `refresh_token`, `token_updated_at`). Wording rewritten as platform-dependent (HIGH→MEDIUM).
2. **`c.client.profile`** — CC: "only `{ ai: ... }`." Reality: also carries WordPress publish credentials (`wp_*` keys, `website_publish_enabled`). Wording rewritten to mention both.
3. **`f.video_analysis.raw_metadata`** — CC: sample row only `thumbnailUrl: null`. Reality: 2 keys (`hasDataApi` boolean + `thumbnailUrl` string) across 9 populated rows. Wording rewritten as sparse 2-key shape.
4. **`c.client_publish_profile_audit.old_value/new_value`** — CC: "top-type is string." Reality: mixed scalars (boolean/string/null). Wording rewritten as JSONB scalars.

ChatGPT then reviewed proposals with chat findings and added 4 more wording edits:

5. `f.feed_source.config` — removed "Polled by ingest-worker per row" code-path claim
6. `f.raw_content_item.payload` — removed unverified "Append-only — never updated after insert" assertion
7. `c.content_series.outline_json` — softened element-shape claim (HIGH→MEDIUM)
8. `c.client_platform_profile.output_fields` — softened "alt-text or hashtag slots" inferred meta shape

**1 LOW row deferred** (correctly NOT in migration): `f.raw_content_item.payload_hash` — designed-but-unimplemented or deprecated. Captured at `docs/audit/decisions/f002_p3_low_confidence_followup.md`.

**Forward discipline captured as Lesson #39:** chat-side sanity-check on JSONB columns must sample across rows. CC's V3 single-row sampling missed cross-row diversity in 3 of 4 cases. Future JSONB documentation briefs adopt this V3 wording: "For JSONB object columns with more than one populated row, sample/group across rows and report union of top-level keys plus notable per-row/platform/source variation. A single-row shape is insufficient unless populated_count = 1."

**Phase D scope discovery (NOT blocking F-002 closure):** CC's P3 regex captured jsonb columns but missed 7 pure pg ARRAY columns in c+f. Tracked at `docs/audit/decisions/f002_phase_d_missing_array_columns.md`. Will be a small mop-up batch in a future session; does not block F-002 closure since the original brief's "high-leverage business-control coverage" target is met.

**F-002 transitions to closed-action-taken.** Three phases applied same day (P1 + P2 + P3). c+f schema column coverage 0% → 20.2% (136/674). Pattern is reproducible: CC drafts → chat sanity-checks → ChatGPT reviews → chat applies corrected via Supabase MCP.

#### Addendum 2026-04-28 evening (4th shift) — F-002 LOW-confidence joint resolution

Migration `audit_f002_low_confidence_joint_resolution` (version 20260428100257, commit `21f4c61c`).

The 6 LOW-confidence rows from Phases A/B/C (4+1+1) resolved in a joint operator+chat session during the 4th shift. The 3 followup files were updated with RESOLUTION blocks and kept (not deleted) per operator instruction as historical record of the deferred-then-resolved decisions.

**10 column updates + 1 table_purpose update applied:**

- **Phase A (4 LOW rows):** `c.brand_avatar.avatar_gen_status` (A — reserved-infra HeyGen lifecycle, all rows currently `'empty'`); `c.client_avatar_profile.avatar_status` (B — DEFERRED 2026-10-31, state machine pending first video-content client); `c.client_brand_asset.asset_type` (A — reserved-infra for planned asset-management UI; not currently consumed by image-worker); `f.raw_metric_point.entity_type` (A — reserved-infra for Phase 2.1 insights-worker activation).
- **Phase B (1 LOW row + propagating tightenings):** `c.client_match_weights.quality_weight` set as "Weight applied to the quality scoring factor in the R5 matching layer". **Table_purpose for `c.client_match_weights` corrected** from "vertical fit / recency / freshness" to "fitness / quality / recency" — the original table_purpose was wrong per the R5 matching spec. `fitness_weight` purpose tightened (removed the "fitness/vertical-fit" conflation); `recency_weight` purpose tightened ("bundler signal scoring" → "R5 matching layer" for consistency).
- **Phase C (1 LOW row, SPLIT decision across 3 related columns):** `f.raw_content_item.payload_hash` resolved as **DEPRECATED** (DEFERRED until 2027-04-30, schedule column-drop in future schema cleanup pass — 0 of 2,124+ rows populated, payload-hash dedup never implemented; deduplication achieved via `url_hash` and downstream `content_hash` on `f.canonical_content_body`). `f.raw_metric_point.payload_hash` and `f.raw_timeseries_point.payload_hash` resolved as **KEEP designed-but-unimplemented** (existing purposes tightened with "currently unwritten — designed but not implemented; reassess when analytics ingestion activates" qualifier). Split rationale: raw_content_item has 2,124+ rows demonstrating the dedup design wasn't implemented; the other two are empty future analytics infrastructure where the design might still ship when those tables go live.

**Coverage of c+f schemas: 20.2% → 21.1%** (6 additional columns documented; total 142/674). c at 23.2%, f at 15.9%.

**Followup files retained** per operator instruction ("do not remove the followup files until the split decision for Row 6 is reflected clearly"). The 3 markdown files in `docs/audit/decisions/` now have RESOLUTION blocks at the top documenting the resolution and pointing to this migration. They serve as the historical record of how each LOW row was decided, including the SPLIT decision for the 3 payload_hash columns.

**No new lessons captured.** This 4th-shift work closes the same audit cycle 1 already closed end-of-evening; no new forward-discipline rules emerged. Lessons #35–#39 already cover the joint-resolution pattern transitively.

---

## F-2026-04-28-D-003  ·  MEDIUM  ·  closed-action-taken
**Role:** Data Auditor
**Raised:** 2026-04-28 04:00 UTC (audit run: runs/2026-04-28-data.md)
**Area:** Migration discipline
**Object:** recent Stage 12 migration history

### Issue
The applied migration history shows two Stage 12 migrations apparently reapplied or duplicated under similar names on 2026-04-27. `stage_12_053_fill_pending_slots_ai_job_upsert` appears at both 04:36 and 05:42, while `stage_12_052_drop_shadow_filter` / `stage_12_052_drop_shadow_filter_for_phase_b_reactivation` appear at both 04:20 and 05:25. This may be intentional during Phase B testing, but without an explicit note it creates ambiguity about which migration is canonical.

### Evidence
- Snapshot section: `Migration History` shows `20260427054214 stage_12_053_fill_pending_slots_ai_job_upsert`.
- Snapshot section: `Migration History` also shows `20260427043659 stage_12_053_fill_pending_slots_ai_job_upsert`.
- Snapshot section: `Migration History` shows both `20260427052534 stage_12_052_drop_shadow_filter` and `20260427042009 stage_12_052_drop_shadow_filter_for_phase_b_reactivation`.
- Snapshot note: these "appear to have been applied twice with similar names" and may indicate a discipline gap.

### Resolution
**Closed 2026-04-28 by operator — Action taken.** Investigated via SQL hash comparison during closure. The 052 pair is fine — two related-but-distinct migrations with different names AND different SQL (`drop_shadow_filter_for_phase_b_reactivation` 1478 chars vs `drop_shadow_filter` 1007 chars). The 053 pair IS a discipline gap — same migration name `stage_12_053_fill_pending_slots_ai_job_upsert`, different SQL (20360 chars vs 18707 chars), different hashes (`ed8145a0...` vs `3af78ee9...`), applied 1 hour apart. The second was a fix iteration of the first (consistent with Lesson #33 — destructure error fix). Forward rule locked at `docs/audit/decisions/migration_naming_discipline.md` (commit `1157671d`): a migration name is a permanent identity; fix iterations get a new sequential number AND a distinguishing suffix. Captured as Lesson #36 candidate in standing memory.

Detector function `k.fn_check_migration_naming_discipline()` deployed via audit slice 1 migration (commit `27ff3b3`) — returns same-name-different-SQL violations on demand. Currently returns 1 row (this historical violation). Future audit cycles close this row as `closed-redundant` referencing F-2026-04-28-D-003.

---

## Summary
- **3 findings raised:** 1 High, 2 Medium
- **3 findings closed:** ALL action-taken (F-001, F-002, F-003)
- **0 observations**
- **Coverage impact:** k.table_registry purpose coverage **72.0% → 100.0%** (all 7 ICE schemas at 100%, 200/200 ICE tables documented). k.column_registry c+f coverage **0% → 21.1%** (142/674 columns; 79 P1 + 30 P2 + 27 P3 + 6 from 4th-shift joint LOW resolution — see addendum). 6 LOW rows resolved via joint operator+chat session (4+1+1 across phases). 7 ARRAY columns deferred to Phase D mop-up.
- **Forward discipline captured:** Lessons #35 (new tables ship with docs at creation), #36 (migration names are permanent), #37 (ChatGPT external review of CC proposals before apply), #38 (count-delta verification not time-window), #39 (chat sanity-check on JSONB shape must sample across rows).
- **Audit cycle 1 complete.** Snapshot → ChatGPT pass → 3 findings → operator approval → 3 closures, all action-taken. F-001 closed beyond original scope (entire registry to 100%, not just Phase B). F-002 closed via three-phase same-day apply (P1 + P2 + P3) producing same-day coverage of 20.2% on c+f schemas, with the two-layer review pattern (chat sanity + ChatGPT) catching distinct safety issues at each phase. 4th-shift joint LOW resolution lifted coverage further to 21.1% (6 LOW rows resolved + 1 wrong table_purpose corrected). F-003 locked forward discipline + automated detector. Loop works end-to-end. Slice 1 (audit recurrence prevention) added structural protection: PENDING_DOCUMENTATION sentinel + 14-day grace + DEFERRED escape hatch + F-003 detector. The pattern of `CC drafts → chat sanity-checks → ChatGPT reviews → chat applies corrected via Supabase MCP` is now the steady-state for any registry-data closure where wording precision matters.
