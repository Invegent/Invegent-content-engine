# Phase B +24h Observation Checkpoint — 2026-05-01 00:30 UTC

**Trigger:** T01 in `docs/00_action_list.md`. Phase B body-health gate deployed at 2026-04-30 03:48:25.383415+00 UTC. The +24h observation window targets ~03:48 UTC Sat 1 May, but observation begins at the +21h mark.

**Mode:** Manual chat session via Supabase MCP. PK on phone, no laptop available.

**Reference:** `docs/runtime/runs/phase-b-patch-image-quote-body-health-2026-04-30T033748Z.md` — defines the four observation queries verbatim.

---

## Result summary

| Target | Status | Detail |
|---|---|---|
| Zero new `exceeded_recovery_attempts` | ✅ PASS | 0 alerts of this kind since deploy |
| Shadow `ai_job` failure rate <5% | ✅ PASS | 3/3 succeeded (100%); shadow volume low post-gate as expected |
| Zero new `slot_fill_no_body_content` errors | ✅ PASS | 0 alerts of this kind since deploy |
| `pool_thin` skip rate not spiking (especially Invegent) | ✅ PASS | 0 pool_thin alerts since deploy across all clients |
| **Bonus**: total alerts of any kind since deploy | ✅ ZERO | 21h clean window across the entire alerting surface |

**Verdict at +21h: clean.** Body-health gate is holding. Shadow ai_job volume is low because the `EXISTS (...)` filter is correctly excluding canonicals upstream of synthesis — that's the intended behaviour, not a sample-size concern.

---

## Implications for T02 (Gate B exit decision)

If the data continues to look like this through the actual +24h mark (~03:48 UTC Sat 1 May, ~3h after this checkpoint), Gate B exits Sat 2 May on schedule per the decision rule documented in the Phase B run state.

The rule: exit on schedule if obs clean. Obs is clean by every metric. Final call remains PK's tomorrow.

If the next 3h surface ANY new alerts, this verdict is revisited and T02 forks to either:
- Extend Gate B 5–7 days, OR
- Temporarily disable image_quote at format-mix layer

Neither contingency triggered as of this observation.

---

## Sample data (for verification)

```sql
-- A1: exceeded_recovery_attempts since deploy
SELECT alert_kind, severity, COUNT(*) FROM m.slot_alerts
WHERE created_at >= '2026-04-30 03:48:25.383415+00'::timestamptz
  AND alert_kind = 'exceeded_recovery_attempts'
GROUP BY alert_kind, severity;
-- → empty

-- A2: shadow ai_job failure rate
SELECT status, COUNT(*) FROM m.ai_job
WHERE is_shadow = true
  AND created_at >= '2026-04-30 03:48:25.383415+00'::timestamptz
GROUP BY status;
-- → succeeded: 3 (100%)

-- A3: slot_fill_no_body_content alerts
SELECT alert_kind, severity, COUNT(*) FROM m.slot_alerts
WHERE created_at >= '2026-04-30 03:48:25.383415+00'::timestamptz
  AND alert_kind = 'slot_fill_no_body_content'
GROUP BY alert_kind, severity;
-- → empty

-- A4: pool_* alerts (any pool-related)
SELECT alert_kind, severity, c.client_name, COUNT(*) FROM m.slot_alerts sa
LEFT JOIN c.client c ON c.client_id = sa.client_id
WHERE sa.created_at >= '2026-04-30 03:48:25.383415+00'::timestamptz
  AND sa.alert_kind ILIKE '%pool%'
GROUP BY alert_kind, severity, c.client_name;
-- → empty

-- A5 (defensive): total alerts of ANY kind since deploy
SELECT alert_kind, severity, COUNT(*) FROM m.slot_alerts
WHERE created_at >= '2026-04-30 03:48:25.383415+00'::timestamptz
GROUP BY alert_kind, severity;
-- → empty (zero alerts of any kind in 21h)
```

---

## Status

T01 ✅ done at +21h. T02 await PK tomorrow when full +24h has elapsed. Default verdict: exit Gate B on schedule Sat 2 May.
