# T02 — Gate B Exit Extended 24h (2026-05-02 Saturday afternoon Sydney)

## Decision

Extend Phase B body-health gate observation by 24 hours. Default exit was Sat 2 May (~+48h post-deploy at 2026-04-30 03:48Z). Revised exit target: Sun 3 May.

## Reasoning

ChatGPT Review MCP (T-MCP-01 first real fire, review_id `2bab95d5-36bb-47f8-88e4-75b4887d458f`) raised one substantive concern on the +21h observation: shadow ai_job sample of 3/3 succeeded is statistically thin against the <5% failure target. One failure on n=3 yields empirical rate of 33%, well above target.

The other two pushback points from ChatGPT (abstract silent-regression risk, doc-hygiene confusion with T07/T08 IG locks) were weaker. The 4 non-shadow signals in the obs panel — zero alerts of any kind across ~50h of prod traffic, zero new pool_thin (Invegent specifically), zero new slot_fill_no_body_content, zero new exceeded_recovery_attempts — carry more statistical weight than the shadow jobs alone.

Path A (accept correction) chosen over Path B (override) on two grounds:

1. Cost of waiting is zero — no production action depends on exit today.
2. Avoid setting precedent of overriding the first real MCP escalation. Build trust in the rule before testing exception cases.

## Exit conditions for Sun 3 May ratification

Broader 5-signal panel must remain clean at the +48h+ checkpoint:

- Zero new `exceeded_recovery_attempts`
- Shadow `ai_job` failure rate < 5% across the cumulative window since deploy
- Zero new `slot_fill_no_body_content`
- Zero new `pool_thin` (especially Invegent)
- Zero alerts of any kind since deploy

If all 5 hold, ratify and close T02. If any signal fails, escalate via `ask_chatgpt_review` and reassess.

## References

- Brief: `docs/runtime/runs/phase-b-patch-image-quote-body-health-2026-04-30T033748Z.md`
- +21h obs: `docs/audit/runs/2026-05-01-phase-b-+24h-obs.md`
- MCP review row: `m.chatgpt_review` id `2bab95d5-36bb-47f8-88e4-75b4887d458f`
  - status = `escalated`
  - routing_decision = `escalate_explicit_flag`
  - verdict = `partial`, risk_level = `medium`, confidence = `medium`
  - corrected_action: "Extend observation period beyond +48h to gather additional data and confirm stability before exiting Gate B."
  - resolution: pending close-the-loop UPDATE (`action_taken='extend_observation_24h_accept_correction'`, `resolved_by='pk_session_2026_05_02'`, `escalation_resolved_at=now()`)

## T-MCP-01 closure context

This is the first real fire of the `ask_chatgpt_review` MCP tool from a fresh claude.ai chat session. T-MCP-01 closed:

- Tool loaded via `tool_search` in fresh session ✅
- Fired with structured `proposal` + `context` + `action_type=plan_review` ✅
- Returned schema-conformant response (verdict=partial, risk_level=medium, confidence=medium, routing_decision=escalate_explicit_flag) ✅
- Wrote row to `m.chatgpt_review` with full telemetry ✅
- Backend-enforced routing fired (`escalate_explicit_flag`) per `requires_pk_escalation=true` from ChatGPT ✅
- Telemetry: gpt-4o-mini, schema_version=v1, 1252 input + 253 output tokens, 3947ms latency, response_jsonb populated, idempotency_key set, lifecycle timestamps tracked ✅
- B34 (`estimated_cost_usd` null in row) confirmed as cosmetic-only — actual cost ~$0.0003 per call at this proposal+context size ✅

T-MCP-04 (operationalise D-01 standing rule via Today/Next 5 prompt) behaviourally honoured this session — chat asked first, PK chose path based on MCP output. Codification in standing prompt template is the open deliverable.

## Out-of-scope (recorded for visibility)

- The carried-forward IG locks (NDIS-Yarns IG `publish_enabled=false`, cron jobid 53 `active=false`, PP IG `publish_enabled=false` since Meta subcode 2207051 on 25 Apr) are unrelated to T02 and unchanged by this decision.
- Workstream 2 status unchanged (80% — SQL v5 LIVE, Path B + B28 applied; v1.6.0 EF deferred).
- Next session active focus: B31 reconstruction with mandatory `ask_chatgpt_review` before any deploy.
