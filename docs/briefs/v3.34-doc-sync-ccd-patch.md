# v3.34 Doc-Sync — CCD Patch Spec (PK-approved; CCH cannot apply — file size)

**Status:** PK approved the v3.34 doc sync verbatim (2026-06-11). CCH validated all anchors against HEAD but is BLOCKED from applying: the GitHub bridge is full-file-write only, both targets exceed the >80KB chat re-emission limit (165KB / 157KB), and the full file bodies were never loaded into CCH context — full-file re-emission from chat would fabricate unread sections. Route: **CCD applies via local `str_replace`**, direct-push to main.

**Approved scope (exactly two files):** `docs/00_sync_state.md` + `docs/00_action_list.md`. No other edits. 0 production mutation / 0 DB / 0 D-01 / 0 deploy / 0 provider call / 0 cron change.

**Pre-flight (mandatory, L41):**
- Verify HEAD blob SHAs before editing. As validated by CCH 2026-06-11: `docs/00_sync_state.md` = `7d86fa73e36168976f245e90f64ac6e7dd8023e2` (164,887 B); `docs/00_action_list.md` = `ffaa9287ad1a5a81a8050b81b0c042a3070e169d` (156,753 B). If either has moved, STOP and re-validate anchors (each anchor below was unique, count=1, at these SHAs).
- All six `old_str` anchors below are exact substrings at those SHAs. Apply with `str_replace` semantics (must match exactly once).

**Post-apply verification (mandatory):**
- Expected file deltas: sync_state +2,971 B; action_list +5,430 B.
- Expected post-patch md5 (UTF-8 file bytes): sync_state `efd004340b6549c718d48a28921fb2e6`; action_list `b7e4149d44bb269c01e2cd3181aeb34a`. (Valid only if applied to the exact HEAD SHAs above with byte-identical replacement strings from this spec.)
- Confirm: v3.33 banner intact in sync_state; `Prior — v3.33` chain intact in action_list; exactly 5 rows containing `(NEW v3.34)`.
- Commit message: `docs: v3.34 doc sync — content decision trace audit close (00_ files only)`. Single commit, both files.

---

## OP-1 — `docs/00_sync_state.md` — insert v3.34 banner above v3.33

`old_str` (anchor, unique):

```
> **✅ v3.33 Asset Production routing synthesis — NO production routing defect (read-only, 2026-06-10):**
```

`new_str` = the following banner block, then `---`, then a blank line, then the anchor verbatim:

> **✅ v3.34 Content decision trace audit COMPLETE (read-only, 2026-06-11):** Full 7-day bidirectional trace audit of production content decisions; report at `docs/runtime/sessions/2026-06-11-content-decision-trace-audit.md` (commit `6fd5f4d`). **Headline: all 5 representative backward traces GREEN** (publish→draft→ai_job→slot/fill-attempt→canonical→content_item→raw→feed_source→ingest_run) across all 4 clients and 4 platforms; digest tables are NOT in the live chain (`digest_item_id` NULL on all window drafts; legacy seed-and-enqueue crons disabled). **Decision model confirmed:** `materialise_slots` stamps deterministic format preference (FB pinned `image_quote` via `preferred_format_facebook`; **YouTube hardcoded `video_short_avatar` in function source** — the true home of the A2 override); `fill_pending_slots` is deterministic (`format_preference[1]` else `image_quote`, gated by `t.format_*_policy`); **ai-worker `callFormatAdvisor` is the final format decision-maker** and routinely overrides the fill choice (proven: slot `image_quote` → final `text` / `video_short_stat`); avatar is the sole deterministic exception, with `advisor_would_have` recorded as prose only. **Format = narrative proxy** (kinetic=scenes[], stat=stat_reveal, avatar=first-person narration; no independent narrative dimension). **Avatar path classified: arbitrary-first** (`lookupAvatar` LIMIT 1, no ORDER BY, `stakeholder_role` never emitted by ai-worker) despite a 15-row role-aware `c.brand_avatar`×`brand_stakeholder` inventory (NY 7 / PP 8 active realistic). **F-HEYGEN-RENDER-TELEMETRY first-row verification SATISFIED** — 2 genuine heygen rows in `m.post_render_log` (2026-06-10, drafts `dcab6fba` PP + `d870f20a` NY, succeeded, provider_job_id + durations present); `avatar_identity` null on both (submits pre-dated v2.1.1); register shows heygen-worker **v2.1.1** A-LE (avatar-identity telemetry patch atop v2.1.0). The single unrecoverable per-post fact: which avatar face/voice was used on the 9 in-window avatar publishes. **New P3 carries (see action_list):** F-ADVISOR-RESPIN-ORPHAN-SLIDES, F-YT-QUEUE-ORPHAN-RECURRENCE, F-FORMAT-MIX-UNWIRED, F-AIW-PREF-COL-HARDCODE, F-EFDRIFT-REGISTER-STALE-YTPUB. Window health: 64 publish rows (61 published / 3 PP-FB `facebook_photo_http_500` later succeeded); CFW null-format dead drafts re-confirmed 100% intentional compliance skips (v3.32 class). **Recommendation: no-build recommended at this time** — instrumentation already landed (v2.1.1); single branded host = config-only; role-aware avatar = wiring-only; **narrative engine not justified by current evidence — the narrative layer remains an open architecture question, not a closed design decision.** **0 production mutation / 0 DB write / 0 D-01 / 0 deploy / 0 provider call / 0 cron change / 0 OBS / 0 Stage 0B / 0 cc-0020.** Repo: report file only (`6fd5f4d`) + this `00_` close. cc-0015 / cc-0020 / OBS-0A / PRV / IG / YT / Asset-Production lanes preserved.

---

## OP-2 — `docs/00_action_list.md` — Last-updated chain prefix swap

`old_str` (anchor, unique — the line-7 prefix, ending exactly at "defect."):

```
> Last updated: 2026-06-10 Sydney (**v3.33 — Asset Production routing synthesis (read-only): NO production routing defect.
```

`new_str`:

```
> Last updated: 2026-06-11 Sydney (**v3.34 — Content decision trace audit COMPLETE (read-only): report `docs/runtime/sessions/2026-06-11-content-decision-trace-audit.md` (commit `6fd5f4d`). All 5 backward traces GREEN (publish→…→feed_source→ingest_run); digest tables not in the live chain; ai-worker `callFormatAdvisor` is the final format decision-maker; the YouTube avatar preference is hardcoded in `m.materialise_slots` (the true home of A2); format = narrative proxy; avatar path = arbitrary-first (`lookupAvatar` LIMIT 1, no ORDER BY; `stakeholder_role` never emitted) despite NY 7 / PP 8 role-tagged active avatars. F-HEYGEN-RENDER-TELEMETRY first-row verification SATISFIED → CLOSED (2 heygen rows in `m.post_render_log` 2026-06-10, drafts `dcab6fba` + `d870f20a`). Conclusion recorded: **no-build recommended at this time**; **narrative engine not justified by current evidence** — the narrative layer remains an open architecture question, not a closed design decision. 5 NEW P3 carries: F-ADVISOR-RESPIN-ORPHAN-SLIDES, F-YT-QUEUE-ORPHAN-RECURRENCE, F-FORMAT-MIX-UNWIRED, F-AIW-PREF-COL-HARDCODE, F-EFDRIFT-REGISTER-STALE-YTPUB; A2 carry amended with three branches. 0 production mutation / 0 DB / 0 D-01 / 0 deploy / 0 provider call / 0 cron change. Prior — v3.33 — Asset Production routing synthesis (read-only): NO production routing defect.
```

(The remainder of the original line continues verbatim after the anchor — preserved untouched.)

## OP-3 — `docs/00_action_list.md` — close F-HEYGEN first-row carry (cells only)

`old_str` (anchor, unique — the status/evidence/owner/notes cells of the F-HEYGEN table row):

```
| **P3 — OPEN (pending natural render)** | Deploy verified (v3.31); functional first-row evidence not yet observed (baseline 0 heygen rows). No worker invocation / no HeyGen call permitted to force it. | CCD/CCH → observe | Passive observation only; close when the first heygen row is confirmed. |
```

`new_str`:

```
| **P3 — CLOSED (v3.34)** | CLOSED 2026-06-11 (v3.34 trace audit, read-only): first 2 genuine `render_engine='heygen'` rows observed in `m.post_render_log` (2026-06-10; drafts `dcab6fba` PP + `d870f20a` NY; both `succeeded`; `provider_job_id` populated; `credits_used` null; durations present; no duplicates; Creatomate logging unaffected in the same window). Renders occurred naturally via cron — no worker invocation, no HeyGen call. `avatar_identity` null/absent on both (submits pre-dated heygen-worker v2.1.1); identity telemetry expected from the next fresh submit — folded into the A2 carry. | CCD/CCH | Closed — passive observation satisfied. |
```

## OP-4 — `docs/00_action_list.md` — amend A2 carry row (cells only)

`old_str` (anchor, unique):

```
| **P3 — policy decision, no defect** | OPEN (decision only; no routing change) | chat → PK |
```

`new_str`:

```
| **P3 — policy decision, no defect** | OPEN — AMENDED v3.34: A2 is enforced at slot creation (`m.materialise_slots` hardcodes `video_short_avatar` for YouTube), not only in ai-worker; avatar identity is arbitrary-first (`lookupAvatar` LIMIT 1, no ORDER BY, `stakeholder_role` never emitted) despite NY 7 / PP 8 role-tagged active avatars in `c.brand_avatar`. Three branches: (i) keep volume + pin single branded host (config-only — deactivate all but one active avatar per client); (ii) keep volume + role-aware wiring (ai-worker emits `stakeholder_role`; lookup filter already exists — wiring-only); (iii) relax to content-fit (remove the `materialise_slots` hardcode — sql_destructive D-01). Hold the decision until ≥1 fresh post-v2.1.1 submit shows `avatar_identity` populated. | chat → PK |
```

## OP-5 — `docs/00_action_list.md` — insert 5 new carry rows after the A2 row

`old_str` (anchor, unique — the A2 row terminator):

```
no production routing change without a separate gate. |
```

`new_str` = anchor verbatim, then a newline, then these five table rows (one per line):

```
| **F-ADVISOR-RESPIN-ORPHAN-SLIDES (NEW v3.34)** | Draft `096e3b72` (Invegent×LinkedIn) carries TWO `m.post_visual_spec` rows (carousel + text — advisor re-ran); 6 carousel slide renders executed (5 succeeded / 1 failed, Creatomate) for a draft whose FINAL `recommended_format='text'` — renderer consumed a non-final advisor spec → wasted Creatomate credits + orphan `m.post_carousel_slide` rows. 1 instance in the 7-day window. Candidate fix (future, gated): renderer re-checks `recommended_format` before slide render, or a visual-spec supersede marker. | **P3 — OPEN** | v3.34 audit evidence (read-only); no fix implemented. | chat → PK | No action without its own gate. |
| **F-YT-QUEUE-ORPHAN-RECURRENCE (NEW v3.34)** | `enqueue-publish-queue-every-5m` keeps inserting YouTube `m.post_publish_queue` rows that youtube-publisher (draft-predicate, queue-bypassing) never consumes; published avatar drafts `d870f20a`/`dcab6fba` sit `queued` forever; the v3.29-archived class is regenerating (3 new orphans in window incl. 1 never-publishable `text`). Candidate fix (future, gated): exclude `platform='youtube'` in the enqueue cron, OR make youtube-publisher queue-consuming — PK's call; sql_destructive D-01 either way. | **P3 — OPEN** | v3.34 audit evidence (read-only); no fix implemented. | chat → PK | No cron change without its own gate. |
| **F-FORMAT-MIX-UNWIRED (NEW v3.34)** | `t.platform_format_mix_default` (evidence-based portfolio mix, seeded 2026-04-22) is wired into nothing; actual output is near-monoculture (FB/IG ≈ all `image_quote`, YT = 100% avatar) set by `preferred_format_facebook` + the YT hardcode + advisor drift. Park as design input for any future format-policy work — depends on the A2 decision; do NOT wire independently. | **P3 — PARKED (design input)** | v3.34 audit evidence (read-only). | chat → PK | Linked to the A2 carry. |
| **F-AIW-PREF-COL-HARDCODE (NEW v3.34)** | ai-worker `fetchFormatContext` reads the `preferred_format_facebook` column for EVERY platform (column name hardcoded); `preferred_format_linkedin`/`_instagram` are dead config. Harmless today (only FB rows set). Fold into the next ai-worker patch window — not standalone-deploy-worthy. | **P3 — OPEN (bundle with next ai-worker patch)** | v3.34 audit evidence (ai-worker `8204a5c7` source read). | CCD (next ai-worker window) | No standalone deploy. |
| **F-EFDRIFT-REGISTER-STALE-YTPUB (NEW v3.34)** | `m.vw_ef_drift_current` shows youtube-publisher deploy 1.6.0 vs live v1.11.0 (v3.20 health GET evidence) — stale register row, likely missed targeted refresh. Fix = one `drift-check?write=true&slug=youtube-publisher` targeted refresh — write-flagged, so needs explicit PK go-ahead; bundle with the next drift-check tick or any deploy session. (Existing insights-worker B-RR repo-behind-deployed carry unchanged.) | **P3 — OPEN** | v3.34 audit evidence (register read vs v3.20 health GET). | CCD/CCH → PK gate | Write-flagged refresh; do not fire ad hoc. |
```

## OP-6 — `docs/00_action_list.md` — closure-budget cycle line

`old_str` (anchor, unique):

```
**v3.27 cycle: ~1 EF deploy + docs closure**
```

`new_str` = the following line, then a newline, then the anchor verbatim:

```
**v3.34 cycle: ~read-only trace audit + 1 doc commit (`6fd5f4d`) + this `00_` close. 0 production mutation. 0 DB write. 0 D-01. 0 deploy. 0 provider call. 0 cron change.**
```

---

*Authored by CCH 2026-06-11. Anchors validated count=1 each against the HEAD SHAs above. This spec file may be deleted after the patch lands.*
