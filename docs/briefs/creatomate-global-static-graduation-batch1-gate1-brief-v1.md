# Brief — Creatomate Global: Static Graduation Batch 1 — Announcement Card Production Gate + Carousel Cover Integration Verdict

**Created:** 2026-07-30 Sydney
**Author:** brief-author (draft)
**Executor:** Claude Code (orchestrator + subagent chain)
**Status:** draft — awaiting PK gate 1
**Result file:** `docs/briefs/results/creatomate-global-static-graduation-batch1-part-a-b-result-v1.md` (created on completion)

---

## Task

This is one PK-named outcome with two parts that must each reach their own named stop condition, not be silently merged or resolved past.

**Part A** advances `generic_announcement_card_1x1_v1` (`a75e7139-1eec-4bba-a8c1-40b8e07b2b0e`) from its current state — worker-compatible and technically render-proven since image-worker v3.34.0's deploy (`docs/00_sync_state.md:16-20`; `docs/briefs/results/static-template-graduation-batch1-image-worker-deploy-result-v1.md`), but not selected by any real production draft — through a real production-shaped Property Pulse draft, PK visual verdict, native-platform publish proof, and a **prepared but explicitly unapplied** selector-ranking change with proven rollback, stopping at the registry/selector apply gate.

**Part B** determines whether ICE currently has a complete carousel production path by tracing, with citations, what produces the cover/body/closing slides, how they are grouped/ordered, which publisher(s) support the multi-image payload, whether a real carousel draft exists, and whether `select_template` is ever called with `format_key='carousel'` — stopping at an evidence-grounded verdict (real-draft proof + complete graduation packet, OR blocked with the exact missing component), never claiming carousel completeness from a cover-only render.

**A structural finding from this evidence pass changes what Part A's own wording can mean as literally stated, and must be read before execution starts (see Notes and Open Question 1):** the only code path that renders a real `m.post_draft`-backed image (the production `image_quote` branch) hardcodes `p_variant_intent: null` at the single `select_template` call site (`supabase/functions/image-worker/index.ts:987`), and the only override mechanism that exists anywhere in the deployed worker is the supervised `governed_image_quote_smoke` entrypoint's `variant_intent`/`format` parameters — which never write `m.post_draft` and are the exact kind of "forced test-only substitution" Part A's own wording excludes (confirmed: the smoke entrypoint uses a synthetic seed and a distinct shadow-stamp label, never a real draft id — `index.ts:840-889`). As deployed today, there is no mechanism for a real production draft to select `generic_announcement_card_1x1_v1` without either (a) a genuine selector-ranking change (which Part A explicitly reserves for the end, unapplied, behind the registry/selector apply gate) or (b) a new, small, PK-approved code change exposing a scoped override on the real production branch (its own T2 ef-builder lane, not silently folded into this one). This brief cannot resolve that fork; Part A's execution needs PK's ruling on it before the "real production-shaped draft" step can proceed (Open Question 1).

## Source context

- `docs/00_sync_state.md:16-20` (v6.74 marker) — image-worker v3.33.0→v3.34.0 deployed (merge `a7577c2`); maps `generic_announcement_card_1x1_v1` + `generic_carousel_cover_1x1_v1` into `TMR_WINNER_TEXT_FIELDS`; `generic_stat_hero_card_1x1_v1` stays deferred (confirmed fail-closed live); explicitly states this deploy does **not** graduate either template into production selection.
- `docs/briefs/results/static-template-graduation-batch1-image-worker-deploy-result-v1.md` — full post-deploy proof table: both new mappings render `ok:true` through the DEPLOYED worker; both outputs independently verified 1080×1080; governed background/logo bindings intact; production selector's unforced default winner confirmed unchanged (`select_template('property-pulse', ..., p_variant_intent:null, p_format:'image_quote')` still returns `generic_market_insight_card_1x1_v1` / `48cba556…`); zero registry/fit-status mutation; both new templates appear only in `alternatives[]`, never selected.
- `docs/briefs/results/static-template-graduation-batch1-image-worker-compat-result-v1.md:32` — the load-bearing finding this brief inherits: `generic_announcement_card_1x1_v1` is already DB-eligible (`fit_status='strong_candidate'`, `assignment_status='visually_approved'`) and, at time of that read, ranked **6th of 6** in its tied `strong_candidate` batch by `created_at` — it cannot become the production-selected winner under the current caller. Also: `generic_carousel_cover_1x1_v1`'s `format_key='carousel'` **is never called by any production path at all** (independently re-confirmed this pass, see below). Also records the CTA/SlideNumber-placeholder disclosure (§9 open issue 2) and the residual-backstop-removal risk (§9 open issue 1).
- `docs/briefs/static-template-graduation-batch1-gate1-brief-v1.md` (open questions #2, #3, #4) — the carousel cover/body/closing coupling question was explicitly **not ruled on** at the original Gate 1 and was carried forward, unresolved, into execution; this brief's Part B is the first lane positioned to actually answer it with evidence rather than carry it again.
- `docs/briefs/results/static-template-graduation-batch1-result-v1.md` — confirms rungs 1/3/4/6 of the ratified 13-rung ladder are already satisfied for both `announcement_card` and `carousel_cover` (including a passed `visual_approval` proof_event dated 2026-07-03, evidence referenced) — Part A's "obtain PK visual verdict" step should be understood against this existing rung-6 evidence, not assumed to require a fresh approval mechanism from zero.
- `docs/briefs/results/creatomate-registry-integrity-graduation-contract-v1.md` — the canonical 9-state proof ladder (§1.2) and 13-rung checklist (§4) this batch should keep following rung-by-rung; §3.4/§3.5/§7 model the exact rollback-proven-before-apply + live `select_template` before/after diff + fail-closed rowcount-assertion pattern Part A's "prove current winner remains rollback-ready / prove rollback before promotion" steps should reuse, not reinvent; §0.3 additionally flags an untracked, undocumented `select_template` schema-drift fact (a `scope='client'` extension with no matching migration file) — a live-vs-migration-file divergence that any future selector-ranking work on this same RPC should re-check, not assume the migration file is authoritative.
- `docs/briefs/results/creatomate-template-graduation-matrix-v1.md` — full 27-row live inventory (2026-07-29): row 5 (`48cba556…`, `generic_market_insight_card_1x1_v1`) is the confirmed production winner for PP's `image_quote`; row 2 (`a75e7139…`, announcement_card), row 15 (`c9a59faa…`, carousel_cover), row 16 (`c4c0fc9d…`, carousel_body), row 10 (`8aeb946c…`, carousel_closing) were all classified `BLOCKED — proof-required` (zero render/draft/publish history) before this batch's compat build; row 1 (`54b305c8…`, stat_hero_card) likewise, and independently structurally blocked beneath the mapping gap (§5.2 of the readiness result doc — a static-shape `BackgroundSolid` element never produces `Background.source` at all).
- `supabase/functions/image-worker/b1_production.ts:205-272` — the live `TMR_WINNER_TEXT_FIELDS` allowlist: exactly four entries exist (`generic_market_insight_card_1x1_v1`, `generic_quote_card_1x1_v1`, `generic_announcement_card_1x1_v1`, `generic_carousel_cover_1x1_v1`); `generic_carousel_body_1x1_v1` and `generic_carousel_closing_1x1_v1` have **no entry at all** (grep-confirmed, zero hits) — exactly matching PK's own asserted ground truth that carousel body/closing are not covered by the compatibility build. CTA (announcement_card) and SlideNumber (carousel_cover) are both coded as genuinely optional (`required_for_render=false`, omitted-not-undefined when absent) — a real production draft does not need new PK-authored brand copy for these two fields the way `generic_quote_card_1x1_v1`'s Attribution/SourceLabel fields fail-closed without one (lines 230-246, contrast).
- `supabase/functions/image-worker/index.ts:963-989` — the production `image_quote` branch's ONE `select_template` call (line 987) hardcodes `p_variant_intent: null`; this is the entire structural basis for the mechanism gap named in the Task section above.
- `supabase/functions/image-worker/index.ts:825-889` — the supervised `governed_image_quote_smoke` entrypoint: takes optional `variant_intent`/`format`/`expected_provider_template_id` body params (added in the v3.34.0 diff), uses a synthetic seed and a deliberately distinct shadow-stamp label (`B1_SMOKE_LABEL`, not `B1_PRODUCTION_LABEL`), and — per the deploy-result's own checklist — never touches `m.post_draft`. This is the mechanism that produced the compat-build's two technical render proofs; it is not, on its own, a "real production-shaped draft."
- `supabase/functions/image-worker/index.ts:1089-1137` — the **legacy, live, entirely separate** carousel production path: drafts with `recommended_format='carousel'` are picked up by a dedicated loop (no approval-status gate, by design — `index.ts:1090-1096`) that calls `callContentAdvisor` (an LLM call producing 3-5 typed `SlideSpec` entries: `hook | point | cta`) then `buildCarouselSlideScript` (`index.ts:741-765` — a raw Creatomate elements-array render script, **not** a `provider_template_id` call at all), writes each slide via `upsert_carousel_slide` into `m.post_carousel_slide`, and sets `post_draft.image_status` to `generated/partial/failed` across the whole slide set. **This loop never calls `select_template`** (confirmed: zero occurrences of `select_template` anywhere in this code block, and an exhaustive grep of every call site of `select_template` across `supabase/functions/**` finds `p_format` values of only `'image_quote'` and `'video_short_stat'` — never `'carousel'`, anywhere in the repo).
- `supabase/functions/publisher/index.ts:49-490` (Facebook, the native FB publisher) and `supabase/functions/instagram-publisher/index.ts:38-391,607-825` (Instagram) — both independently read `m.post_carousel_slide` (ordered by `slide_index`) and implement real multi-image carousel publish (FB: sequential unpublished-photo upload then a multi-attachment post; IG: child-container-per-slide → parent CAROUSEL container → publish, with a documented single-image fallback if the slide set is empty). **Both publishers already consume the legacy slide table; neither has any code path that reads a `provider_template_id`-based render for a carousel.**
- `docs/00_sync_state.md` v6.70/v6.73 markers (NDIS Facebook/Instagram containment release) — corroborate, from a completely independent lane, that `recommended_format='carousel'` is a genuinely live, currently-used production format (NDIS Facebook 7/90d, Instagram 3/90d before containment paused it) — i.e., the legacy carousel path above is not dormant or theoretical.
- `supabase/migrations/20260703035154_create_select_template_v1.sql:41-43,104-106,183,306-353` — the live `select_template` ranking mechanics: candidates are scanned `created_at ASC, id ASC`; rank buckets are (i) exact `variant_intent` match + `strong_candidate`, (ii) exact match + other, (iii) `strong_candidate` (no intent match), (iv) other; winner = first row of the concatenated bucket list. This is the exact mechanism Part A's "show the exact current ranking / proposed ranking after promotion" must be demonstrated against, live, not assumed from a prior day's snapshot (see Open Question 4).
- `docs/creative-library/property-pulse.json` — grepped for all four candidate `provider_template_id`s; zero matches. The vendored creative-library registry does not track this batch at all — consistent with the already-documented vendored-registry drift finding on this same file for row 5 (`docs/briefs/results/creatomate-template-graduation-matrix-v1.md` §2.2/§4.4).
- `docs/briefs/cc-0079-linkedin-youtube-zero-publish-triage-v1.md:60-62` — grounds "native publisher" as the platform's own dedicated edge function (e.g. `linkedin-publisher`) in contrast to a Zapier-bridge alternative (`linkedin-zapier-publisher`) that does not reconcile `approval_status` the same way; used here to scope what "prove the correct native publisher path" means for Part A (the platform's own EF, not any bridge/relay path).
- `CLAUDE.md` — orchestration contract: agent roster (`db-rls-auditor`, `security-auditor`, `apply-harness-auditor`, `branch-warden`, `deploy-verifier` for the eventual apply/deploy steps this batch may spawn), Convention 2 (conditional sequence approvals) and Convention 3 (risk tiers — DML/DDL ≥ T2, callers/grants/deploy/publish/secrets → T3) this lane's execution must be tiered under, the CCF-02 findings contract, and the standing deploy/DB gotchas (`--no-verify-jwt`, explicit `REVOKE ... FROM anon, authenticated`, migration-name permanence).

## Scope

### Part A — Announcement card graduation

**In scope:**
1. Resolving, with PK, the mechanism gap named in the Task section: how a real, `m.post_draft`-backed Property Pulse announcement draft is meant to select `generic_announcement_card_1x1_v1` today, without either a premature selector-ranking change or continuing to rely on the smoke entrypoint (which does not produce a real draft).
2. Once that mechanism is named, producing exactly one real production-shaped PP `image_quote` draft that renders `generic_announcement_card_1x1_v1` through the deployed image-worker's real production path (or the PK-approved narrow mechanism), independently verifying: all rendered fields (Headline/Subtitle/CTA-if-present/Footer), the governed background asset, the governed logo, and 1080×1080 output dimensions — recomputed from the produced file, not the render call's own self-report.
3. Obtaining PK's visual verdict on that specific real draft (in addition to, not instead of, the already-recorded 2026-07-03 proof-wall `visual_approval` proof_event — `docs/briefs/results/static-template-graduation-batch1-result-v1.md:34-42`).
4. Identifying and proving the correct **native** publisher path for whichever platform PK approves first (the platform's own dedicated edge function, not a bridge/relay), and publishing exactly one supervised post on that platform.
5. Confirming publish audit evidence (`m.post_publish` row, `status='published'`, matched to the real draft and provider template).
6. Establishing platform suitability for the platform just proven (a `c.creative_template_platform_suitability` row for `a75e7139…` × that platform — this is itself a DML write and inherits Convention 3's T3 tier; it must run its own full review chain, not be folded silently into the render/publish steps).
7. Preparing (drafting, hash-pinning, external-reviewing) the explicit selector-ranking change that would make `generic_announcement_card_1x1_v1` the winner — **without applying it**.
8. Proving, in the same prepared packet, that the current winner (`generic_market_insight_card_1x1_v1`) remains immediately rollback-ready if the ranking change is later applied and needs reversing.
9. Proving the rollback of the prepared change itself (a dry-run/aborted-transaction proof that the reverse of the proposed ranking edit restores the pre-change state byte-exact) — before any promotion, per the registry-integrity contract's own rollback-before-apply pattern (`docs/briefs/results/creatomate-registry-integrity-graduation-contract-v1.md` §3.3/§3.5/§7).

**Out of scope (Part A):**
- Actually applying the selector-ranking change / flipping `fit_status`/`assignment_status`/`created_at` to make `generic_announcement_card_1x1_v1` a live default winner — that is the named next-gate action, explicitly held behind a fresh PK apply-gate decision.
- Promoting on the strength of the already-existing supervised smoke renders alone (PK's own instruction: "Do not promote solely from a supervised smoke render").
- Any second platform beyond the one PK approves first for this lane's publish proof.
- `generic_stat_hero_card_1x1_v1` — remains deferred; PK's boundary forbids adding stat-hero support in this lane.
- Modifying `buildTmrRenderPlan` in any way.

### Part B — Carousel cover integration proof

**In scope:**
1. Confirming, with live DB evidence (`db-rls-auditor` handoff — see Evidence gaps), what currently produces the cover slide, body slide(s), and closing slide for a real PP/NDIS carousel post — distinguishing explicitly between the two systems this evidence pass already found in code: (a) the legacy, live `recommended_format='carousel'` loop (`callContentAdvisor` + `buildCarouselSlideScript`, writing `m.post_carousel_slide`, never touching `select_template`/the TMR registry) and (b) the TMR/registry `generic_carousel_cover_1x1_v1`/`generic_carousel_body_1x1_v1`/`generic_carousel_closing_1x1_v1` provider templates (rows 15/16/10), of which only the cover has any worker-code mapping at all, and even that mapping has zero live production caller.
2. Confirming how slides are grouped/ordered in each system (legacy: `slide_index` on `m.post_carousel_slide`; TMR: no live grouping mechanism found in code — flagged, not assumed, as a gap).
3. Confirming which publisher(s) support the multi-image payload — already grounded in code for Facebook (`publisher/index.ts`) and Instagram (`instagram-publisher/index.ts`), both reading `m.post_carousel_slide`; confirming (or ruling out) whether LinkedIn/YouTube have any equivalent multi-image path (not found in this pass — named as an evidence gap, not asserted absent).
4. Confirming, live, whether a real carousel draft currently exists for Property Pulse specifically (memory/sync-state evidence found this for NDIS, not independently for PP — `db-rls-auditor` handoff).
5. Confirming, live, whether `select_template` has ever been called with `format_key='carousel'` in production — this pass's static grep found zero occurrences across the entire repo (every call site pins `p_format` to `'image_quote'` or `'video_short_stat'`); this should be re-confirmed against `m.post_render_log`'s actual `render_spec` payloads (`db-rls-auditor` handoff) since a static grep cannot see historical/removed call sites.
6. Answering, explicitly, whether `generic_carousel_cover_1x1_v1` can be used without adding unsupported body/closing mappings — this pass's evidence strongly suggests the honest answer is: only as a **standalone single image**, never as the cover of an actual multi-slide carousel post, because (i) the live multi-image delivery mechanism (the legacy loop + `m.post_carousel_slide` + FB/IG publishers) has zero wiring to the TMR provider-template system at all, and (ii) `generic_carousel_body_1x1_v1`/`generic_carousel_closing_1x1_v1` have no worker mapping and zero render history (`c4c0fc9d…`/`8aeb946c…`, both still `BLOCKED — proof-required` per the graduation matrix). The lane must state this as the packet's verdict if live re-verification confirms it, not soften it.

**Out of scope (Part B):**
- Adding any carousel body/closing field mapping (PK's explicit boundary).
- Claiming carousel production is complete from a cover-only render.
- Wiring the TMR carousel_cover template into the legacy `m.post_carousel_slide`/publisher delivery mechanism — that would be new integration code, requiring its own grounded approval, not something this evidence-gathering lane may propose as a fait accompli.
- Any change to `select_template`, `buildTmrRenderPlan`, or the legacy carousel loop's code.

## Allowed actions

- Re-verify, live (`db-rls-auditor`), every claim in this brief that this evidence pass could only ground statically: current `select_template` ranking position for `generic_announcement_card_1x1_v1` (the "6th of 6" figure is from 2026-07-29, same day the registry-integrity packet retired row 17 out of the same tied bucket — it must be re-pulled fresh, not assumed unchanged), live `platform_support`/`client_publish_profile`/`client_format_config` state for Property Pulse per platform, whether a real PP carousel draft/`m.post_carousel_slide` set exists, and whether any historical `m.post_render_log.render_spec` shows a `format_key='carousel'` `select_template` call.
- Build and submit exactly one real Property Pulse `image_quote` draft through whichever mechanism PK approves for Part A's mechanism-gap decision (Open Question 1), under the same governed-asset/fail-loud guards the existing production branch already enforces.
- Call the native platform publisher PK approves first, for exactly one supervised publish, per the standing image-workflow acceleration rules (`CLAUDE.md` — Image workflow acceleration §P3/§2 non-negotiables: PK visual verdict is the only deciding act; licence/rights unaffected here since this is PP's own governed asset, not new sourcing).
- Draft (not apply) the `c.creative_template_platform_suitability` INSERT for the approved platform, and the selector-ranking-change SQL packet (fit_status/assignment_status/created_at as applicable), each with a rollback file proven before any apply gate, modeled directly on `docs/briefs/results/creatomate-registry-integrity-graduation-contract-v1.md` §3.3/§3.5/§7's pattern.
- Run the full risk-tiered review chain each write requires under Convention 3 (T3: full chain, independent re-verification, explicit PK gate or Convention-2 sequence, named live pre-check STOPs, rollback proven before apply) — `db-rls-auditor` for every live read/claim, `apply-harness-auditor` (shadow, advisory) on any packet carrying a safety harness before freeze, `ask_chatgpt_review` on the final diff/packet hash-pinned, `branch-warden` on any code touch, `deploy-verifier` only if a further EF deploy is proposed (none is named in this brief).
- Grep/read image-worker, publisher, and instagram-publisher source further as needed to confirm slide-grouping/ordering and multi-image mechanics for Part B, without editing any of it.

## Forbidden actions

- Do NOT modify `buildTmrRenderPlan` (PK's explicit boundary).
- Do NOT add `generic_stat_hero_card_1x1_v1` support in any form (PK's explicit boundary; also independently structurally blocked per `docs/briefs/results/static-template-graduation-batch1-result-v1.md` §5.2).
- Do NOT add carousel body/closing field mappings without a new, separately-grounded PK approval (PK's explicit boundary).
- Do NOT activate both `generic_announcement_card_1x1_v1` and `generic_market_insight_card_1x1_v1` as simultaneous production winners in one uncontrolled change, and do NOT apply any selector-ranking change at all in this lane — Part A stops at the prepared-and-rollback-proven packet, before the apply gate.
- Do NOT claim carousel production is complete from a `generic_carousel_cover_1x1_v1`-only render (PK's explicit boundary).
- Do NOT broaden this lane to any client other than Property Pulse (PK's explicit boundary) — this includes not touching NDIS's already-paused carousel containment state (`docs/00_sync_state.md` v6.70/v6.73 markers — NDIS Facebook/Instagram carousel remains capability-blocked pending its own separately-gated release decision; nothing in this brief authorizes touching that).
- Do NOT promote `generic_announcement_card_1x1_v1` solely from a supervised smoke render (PK's explicit instruction) — a real `m.post_draft`-backed render is required first.
- Do NOT treat the existing 2026-07-03 `visual_approval` proof_event as satisfying Part A's "obtain PK visual verdict" step on its own — it grounds rung 6 of the ladder but PK's task explicitly re-asks for a verdict on the real production-shaped draft this lane produces.
- Do NOT perform any DDL, GRANT/REVOKE, or migration outside the two named DML packets (platform-suitability INSERT, ranking-change SQL) — and do not apply either without its own full T3 chain + explicit PK gate.
- Do NOT deploy any edge function in this lane — no code change to image-worker/publisher/instagram-publisher is named or needed; if the mechanism-gap decision (Open Question 1) resolves toward a code change, that is its own future, separately-gated ef-builder lane, not silently folded in here.
- Standing hold-states from `docs/00_sync_state.md` that remain active and must not be touched by this lane: **v6.75 B-roll Rotation Governance v1** — both review gates clean but still NOT APPLIED, pending PK ratification of D1/D2; this brief's lane must not be treated as authorization to apply that unrelated packet. **NDIS all-platform containment** (Facebook/Instagram released; LinkedIn/YouTube still paused per v6.73) — unrelated to Property Pulse but the carousel finding in this brief touches the same capability-enforcement machinery NDIS's containment relies on; do not modify `classify_format_capability`, the capability-exempt-format function, or any publisher-side capability gate as part of answering Part B.

## Success criteria

### Part A
- PK has explicitly ruled on the mechanism-gap question (Open Question 1) before any draft is produced.
- Exactly one real, `m.post_draft`-backed Property Pulse `image_quote` draft renders `generic_announcement_card_1x1_v1` through the deployed image-worker via the PK-approved mechanism (not the smoke entrypoint alone) — fields, governed background, governed logo, and 1080×1080 output independently re-verified from the produced file.
- PK has given an explicit visual verdict on that specific draft.
- The native publisher path for the first approved platform is proven (real invocation, not a bridge/relay), one supervised post is published, and a matching `m.post_publish` row with `status='published'` exists.
- A `c.creative_template_platform_suitability` row for the approved platform exists (its own T3 chain completed) — this satisfies "establish platform suitability for the proven platform."
- A selector-ranking-change packet exists, hash-pinned, externally reviewed, with a rollback file proven (byte-exact restore, dry-run/aborted-transaction) BEFORE any apply — and is **not applied**.
- The exact current `select_template` ranking (live, re-pulled) and the exact proposed post-promotion ranking are both shown, side by side, naming every tied `strong_candidate` in the batch and the tiebreak order, plus the recent-template-repetition implication of adding a 7th (or however many) reachable static winner to PP's `image_quote` rotation.
- The current winner (`generic_market_insight_card_1x1_v1`) is proven, live, to remain the unforced selection today and to be immediately rollback-ready if the prepared change is ever applied and reversed.
- Zero selector/registry mutation has actually occurred; the platform-suitability INSERT (if executed under its own T3 gate within this lane) is the only DML this lane may apply, and only after its own independent review chain.

### Part B
- The legacy `recommended_format='carousel'` production path (content-advisor slide generation → `buildCarouselSlideScript` → `m.post_carousel_slide` → FB/IG native multi-image publish) is named, cited, and distinguished explicitly from the TMR/registry carousel_cover/body/closing templates.
- Live confirmation (not static-only) of: whether a real PP carousel draft exists today; whether `select_template` has ever been invoked with `format_key='carousel'` in `m.post_render_log`; current `platform_support`/`client_format_config` carousel reachability for PP on each of its four platforms.
- An explicit, evidence-grounded verdict: either (a) a real carousel draft using the TMR templates end-to-end, with a complete graduation packet — or (b) BLOCKED, naming the exact missing carousel-family component(s) (this pass's evidence points strongly toward (b): body/closing have no worker mapping/render history at all, and the TMR cover template has zero live wiring into the only multi-image delivery mechanism that exists).
- No claim anywhere in the packet that carousel production is complete from a cover-only render, regardless of how the verdict lands.

## Stop condition

**Part A stops at the registry/selector apply gate** — report the prepared-and-rollback-proven ranking-change packet, the full evidence table (current ranking / proposed ranking / rollback proof / publish audit), and STOP. Do not apply the ranking change, do not activate both templates, without a fresh, explicit PK apply-gate decision.

**Part B stops at the carousel-cover integration verdict** — report the real-draft-or-blocked finding with every named missing component, and STOP. Do not begin any carousel body/closing mapping work or any TMR-to-legacy-pipeline integration work without a new, separately-grounded PK approval.

After PK authorizes announcement-card promotion (a later, separate gate than this brief covers): verify one natural (unforced) selector win, verify the real render and publish, confirm the incumbent's rollback readiness once more, record/commit/push per Convention 1, then clear the session. Report both parts per the result template, then stop.

---

## Notes

1. **The mechanism gap (Open Question 1) is the single most important thing for PK to read before Part A executes.** It is not this brief's invention — it falls directly out of `index.ts:987`'s hardcoded `p_variant_intent: null` in the only code path that writes a real `m.post_draft`-backed render, combined with PK's own instruction to avoid "forced test-only substitutions." Nothing in the evidence base resolves it; a genuine product/execution decision is needed.
2. **The two-carousel-systems finding (Part B) is the load-bearing structural fact for this whole outcome.** ICE has a real, live, currently-used carousel delivery mechanism (legacy, non-TMR) and a completely separate, unproven-beyond-cover TMR carousel family. They share only the string `'carousel'`. Any framing of Part B's question that assumes the TMR carousel_cover template is "the" carousel system risks a false-positive completeness claim — this brief's Scope section is written to keep that distinction explicit throughout execution.
3. **CTA/SlideNumber remain Creatomate's own placeholder defaults** in every render proof to date (`docs/briefs/results/static-template-graduation-batch1-image-worker-compat-result-v1.md` §9 open issue 2). Because both fields are coded as genuinely optional (not `required_for_render`), a real production-shaped draft can render validly without new brand copy — but if PK wants real PP-authored CTA text for the production draft itself (rather than omitting the field, or using the Creatomate default), that is a content decision for PK to make when approving Part A's mechanism, not something to infer.
4. **The residual-backstop-removal risk named in the compat-build result (§9 open issue 1)** — that `generic_announcement_card_1x1_v1`'s prior `tmr_winner_unmapped` fail-closed throw is no longer the only backstop if the 5 other `strong_candidate` `image_quote` templates were ever to all fail `resolve_slot_assets` on one render — is unchanged by this brief and should be re-stated, not re-litigated, in the eventual result doc.
5. **`select_template`'s live-vs-migration-file drift** (the undocumented `scope='client'` "v0.x (A2)" extension, `docs/briefs/results/creatomate-registry-integrity-graduation-contract-v1.md` §0.3) is a standing, independent schema-drift fact this lane should be aware of when reading `pg_get_functiondef` for the live ranking, since the tracked migration file alone is not sufficient evidence of current selector behaviour.

## Open questions (PK Gate 1)

1. **Mechanism gap (blocking, Part A):** how should a real, `m.post_draft`-backed Property Pulse announcement draft actually select `generic_announcement_card_1x1_v1`, given the production branch hardcodes `p_variant_intent: null` and the only override mechanism (the smoke entrypoint) never writes a real draft? Options this evidence pass can name but not choose between: (a) a small, scoped, PK-approved code change exposing an explicit per-draft override on the real production branch (its own T2 ef-builder lane); (b) treat a specific, disclosed use of the smoke entrypoint as sufficient evidence for this purpose despite PK's "no forced test-only substitutions" wording (a policy call, not a technical one); (c) some other mechanism not found in this pass.
2. **First platform for Part A's publish proof:** PK's task says "the first approved platform" without naming it. Property Pulse's live per-platform `image_quote` reachability/suitability was not independently re-verified this pass.
3. **Carousel framing, if Part B's verdict is BLOCKED as this evidence pass expects:** does PK want the next Creatomate Global outcome to be a genuinely new integration project wiring `generic_carousel_cover_1x1_v1` (and eventually body/closing) into the existing `m.post_carousel_slide`/FB-IG multi-image delivery mechanism — or to leave the legacy carousel path as-is and treat the TMR carousel family as a separate, lower-priority track?
4. **Ranking re-verification timing (not a PK decision, an execution precondition):** the "6th of 6 tied" figure that grounds Part A's selector-safety framing is from 2026-07-29 — the exact live composition of the tied `strong_candidate` batch for `image_quote` must be re-pulled fresh at execution start, not assumed unchanged.
