# Brief — M9 Prerequisite P-1 + M8.1: Dashboard-Visibility Combined Lane

**Created:** 2026-08-05 Sydney
**Author:** chat (brief-author)
**Executor:** Claude Code (orchestrated) — **cross-repo: the audit half (P-1) and the build half (M8.1) both execute against the `invegent-dashboard` repo, a SEPARATE repo from this one (`invegent-content-engine`). This brief is authored and persisted in THIS repo's `docs/briefs/`, per the house convention already used for M8/M8.2 (`docs/briefs/m8-asset-gap-dashboard-panel-gate1-brief-v1.md:5` names "Executor: Claude Code (ef-builder lane, `invegent-dashboard` repo)" while the brief itself lives here). See `docs/dashboard-review-2026-05/12_dashboard-revamp-master-plan.md:7` ("the dashboard lives in the **`invegent-dashboard`** repo, not this one") and `docs/governance/orchestrator-operating-manual-v1.md:293-297` (a prior PK-caught case of an orphaned unpushed feature in "the *separate* `invegent-dashboard` repo — not the CE repo").**
**Status:** draft
**Tier:** **T2** (the combined lane's higher-tier half governs: M8.1 is a bounded, additive dashboard-code change reading an existing table, same class as M8 itself — `docs/briefs/m8-asset-gap-dashboard-panel-gate1-brief-v1.md:7`; P-1 alone would be T1/read-only-audit. Per `CLAUDE.md`'s risk-tiered review chains, "escalation up free" — the combined lane runs at the higher tier.)
**Lane class (CCF-02):** PRODUCT_PROOF (clears one milestone-blocking prerequisite and one already-scoped closeout follow-on; not a new architecture proposal, not a safety/security gate, not side-proving).
**Result file:** `docs/briefs/results/m9-p1-m8.1-dashboard-visibility-combined-result-v1.md` (created on completion)

---

## Task

Execute two already-scoped, currently-unstarted dashboard-visibility items together, in one lane, against the `invegent-dashboard` repo:

1. **Prerequisite P-1 (M9)** — a dedicated read (or a `dashboard-ia-lint` pass, once that agent is proven) of five named `invegent-dashboard` components/actions to determine and RECORD, as a finding either way, whether an operator can currently see a brand's Day-1 format-mix governance-readiness state anywhere in the dashboard. P-1 is a **blocking prerequisite on M9's replay execution** — it does not itself execute the replay, and is satisfied by EITHER a "visible" or a "not visible, here is the gap" finding (`docs/briefs/m9-zero-code-day1-onboarding-package-v1.md:219-233`).
2. **M8.1** — add true legacy-vs-governed routing detection to the Asset Gap tab's execution-path badge by reading `client_creative_governance` presence/proof-posture per (client, platform, format), replacing the current honest `unclassified` fallback with a real `legacy_routed` state where the evidence supports it (`docs/briefs/results/m8-asset-gap-dashboard-panel-result-v1.md:265-271`). M8.1 was named at M8's closeout as its own non-blocking follow-on, explicitly not implemented by M8 itself, and remains **not started** as of the latest register entry (`docs/00_sync_state.md:60-61`, v6.138).

Both items concern the SAME underlying governance question — whether/how `client_creative_governance` state is exposed to an operator in the dashboard — but this brief does not assert a documented, PK-authored rationale for combining them into one lane beyond that topical overlap; see Open Questions.

## Source context

**P-1 evidence (M9):**
- `docs/briefs/m9-zero-code-day1-onboarding-package-v1.md:199-237` (§5, "Dashboard visibility") — defines P-1 verbatim as a PK ruling (2026-08-05): the named components/actions are `invegent-dashboard/components/clients/{WeekFormatAllocation,WeekFormatPlanTab,CreativeConfigGapCard,PublishingPlanPyramid}.tsx` and `invegent-dashboard/actions/{production-readiness-queue,client-creative-config-audit}.ts`; these were "confirmed present by name/grep; not read in full in this pass" by the authoring session (`:207-211`).
- `docs/briefs/m9-zero-code-day1-onboarding-package-v1.md:241-251` (§6, operator decisions table) — P-1's row: owner "orchestrator (read-only audit) or `dashboard-ia-lint` once proven"; gate tier "blocking prerequisite, must clear before replay."
- `docs/briefs/m9-zero-code-day1-onboarding-package-v1.md:311-325,327-331` (§9, dependencies) — P-1 is one of two hard blocking gates on M9's replay (the other is the separate, not-yet-obtained §6 schedule-expansion PK approval); P-1 does not substitute for it.
- `docs/briefs/m9-zero-code-day1-onboarding-package-v1.md:352-364` (§11 item 3) — confirms the dashboard-visibility ambiguity was RESOLVED into P-1 as a named blocking prerequisite (not left an open question about what-to-do); what remains open is only P-1's *execution*.
- `docs/00_sync_state.md:16-29` (v6.139) — M9's spec was PK-accepted 2026-08-05 ("package specification complete; replay proof outstanding"); confirms P-1 was converted into a blocking prerequisite at that ratification and that M9 "stays OPEN."
- `CLAUDE.md:78-81` — `dashboard-ia-lint` is a built, committed, read-only **candidate**, "not yet proven," stays candidate "until it has audited at least one real dashboard diff" — relevant because P-1's own definition names it as an optional execution method, once proven.

**M8.1 evidence (M8 follow-on — DISTINCT from M8.2):**
- `docs/briefs/results/m8-asset-gap-dashboard-panel-result-v1.md:247-254` (§6) — the origin of M8.1: "the 'legacy-routed' state PK named in the task outcome was deliberately NOT implemented," no in-scope data source in M8's own scope carried legacy-vs-governed routing evidence (`client_creative_governance` is a third data source M8 explicitly scoped out); the badge instead uses an honest `unclassified` fourth state.
- `docs/briefs/results/m8-asset-gap-dashboard-panel-result-v1.md:260-271` (§7, "Next recommended step") — M8.1 defined precisely: "Add true legacy-vs-governed routing detection to the execution-path badge by reading `client_creative_governance` presence/proof-posture per (client, platform, format) ... Replaces `unclassified` with a real `legacy_routed` state where the evidence supports it; `unclassified` remains the honest fallback everywhere else." Explicitly does not block M8 (PK direction). **Status at that citation: "not started."**
- `docs/briefs/results/m8-asset-gap-dashboard-panel-result-v1.md:304-307` (§8, verification) — reconfirms the badge gap was "named at closeout as M8.1, explicitly non-blocking per PK."
- `docs/00_sync_state.md:60-61` (v6.138) — "M8.1 — dashboard legacy-route authority integration remains OPEN, unstarted, non-blocking (unchanged scope, see the M8 result doc §7)." This is the most recent register confirmation of M8.1's status found in this pass.
- `docs/00_sync_state.md:105-107` (v6.136) — earlier statement of the same follow-on, consistent wording ("Follow-ons preserved, not implemented").
- **Explicit non-conflation with M8.2:** `docs/briefs/m8.2-scheduled-demand-contract-repair-gate1-brief-v1.md:45` (Out of scope) names "M8.1 (legacy-route authority integration, separate follow-on)" as explicitly excluded from M8.2's scope, and its Notes section (`:91-92`) states "M8.1 ... remains a separate, non-blocking, unstarted follow-on — untouched by this lane." M8.2 (`scheduled_demand` boolean/type contract repair) is a DIFFERENT, already-closed item (`docs/00_sync_state.md:33-58`, v6.138: "M8.2 CLOSED same day") and is not part of this brief's scope.

**Repo-boundary and precedent-lane-shape evidence:**
- `docs/dashboard-review-2026-05/12_dashboard-revamp-master-plan.md:7` — "the dashboard lives in the **`invegent-dashboard`** repo, not this one."
- `docs/governance/orchestrator-operating-manual-v1.md:293-297` — a named prior incident where a lane's unpushed feature commits were found in "the *separate* `invegent-dashboard` repo — not the CE repo," and the resulting standing rule: "before folding: check every repo the lane touched for unpushed commits."
- `docs/briefs/m8-asset-gap-dashboard-panel-gate1-brief-v1.md` (full file) — the closest real precedent for this exact shape: a CE-repo-authored, dashboard-repo-executed, T2, PRODUCT_PROOF lane, with `Forbidden actions` line 120-121 ("Touching `invegent-content-engine` in any way") and a `dashboard-ia-lint` pre/post-diff pass required.
- `docs/briefs/authoritative-weekly-schedule-editor-phase-1-brief-v1.md:119` — precedent naming a `branch-warden` **two-repo** stale-ref gate: `fetch --prune` on BOTH the CE repo and `invegent-dashboard`, reporting HEAD/parity/ahead-behind for both, before building off either.

**Hold-state evidence (v6.140 watch ruling):**
- `docs/00_sync_state.md:9-12` and `docs/briefs/cgu-final-control-tower-watch-ruling-v1.md:1-42` (verbatim PK ruling, 2026-08-05) — Phase-2 execution HELD to ~2026-08-11 20:20 Sydney; "perform no schedule DML or cap increases"; "Do not open new heavy CGU Final implementation lanes before the Phase-2 ruling."
- `docs/briefs/cgu-final-control-tower-watch-ruling-v1.md:64-67` (§2 item 4, operational reading) — the explicitly-named "remain queued" list under this restriction is "M1/M2/M4/M6/M7/M12/M13/M14/M16"; M8/M9 are NOT in that enumerated list (M8 was already closed the same register cycle, M9's spec was already accepted). The same item also states "Docs-only follow-ups already in flight (e.g. M11b Gate-1 briefs, M11c decision residue) remain at PK's discretion — this ruling does not newly authorize them." **This ruling does not explicitly name or exempt a P-1/M8.1 dashboard-visibility lane either way** — see Open Questions.

## Scope

**In scope:**
- **P-1 half:** a dedicated, session-fresh read of the five named `invegent-dashboard` files (§5 of the M9 package, full paths cited above), producing a recorded finding — "visible" or "not visible, here is the gap" — of whether an operator can currently see a brand's Day-1 format-mix governance-readiness state (asset-pool depth, governance-row state, ladder position per M9 §4) anywhere in the dashboard. Optionally run via `dashboard-ia-lint` if PK judges it sufficiently proven by execution time (see Open Questions); otherwise a manual read.
- **M8.1 half:** add a real `legacy_routed` execution-path-badge state to the Asset Gap tab, derived from `client_creative_governance` presence/proof-posture per (client, platform, format), replacing `unclassified` only where the evidence supports it; `unclassified` remains the fallback everywhere else (exact scope per `docs/briefs/results/m8-asset-gap-dashboard-panel-result-v1.md:265-271`).
- A fresh `dashboard-ia-lint` pass against the M8.1 diff — this doubles as that agent's first real-merged-diff proving opportunity, named as still outstanding in `docs/briefs/results/m8-asset-gap-dashboard-panel-result-v1.md:283-285`.

**Out of scope:**
- Any CE-repo (`invegent-content-engine`) code/DB/migration/RPC change. This is a dashboard-repo-only lane; CE-repo touches are limited to this brief and its eventual result doc.
- M8.2 (`scheduled_demand` contract repair) — already closed, a different item, not reopened here.
- The §6 schedule-expansion PK approval or any part of M9's actual replay execution — P-1 clears only its own blocking-prerequisite status; it does not authorize or perform the replay.
- Any schedule DML, cap raise, or Phase-2 CGU Final mutation — none of this lane's scope touches those surfaces, but see Forbidden actions for the explicit carry-forward of the v6.140 hold language regardless.
- Resolving whether `c.client_creative_governance`'s live-gate rewire has landed since 2026-07-07 (a separate, already-named open item, `docs/briefs/m9-zero-code-day1-onboarding-package-v1.md:354-356`) — M8.1 reads the table's PRESENCE/proof-posture as data for a UI badge; it does not depend on, and does not resolve, whether that table gates any production allocation path.

## Allowed actions

- Read-only inspection of the named `invegent-dashboard` files/tables for both P-1 and M8.1 (fresh, session-specific reads — not reuse of this brief's or the M9 package's characterizations).
- One bounded dashboard-repo code change for M8.1: the Asset Gap tab's execution-path-badge derivation logic plus its `client_creative_governance` read, scoped as narrowly as M8's own precedent (`docs/briefs/m8-asset-gap-dashboard-panel-gate1-brief-v1.md:107-116` — same file-count discipline: named component + named server-action files only, no unrelated files touched).
- Invoke `dashboard-ia-lint` on the M8.1 design and again on its final diff (per the outstanding proving opportunity named above), and/or on P-1's finding if used as its execution method.
- Run local typecheck/build/tests in an isolated `invegent-dashboard` worktree.
- Record P-1's finding (either outcome) and M8.1's implementation result in one combined result doc, per the CCF-02 recording-compression convention (Convention 1, `CLAUDE.md`).

## Forbidden actions

- **Do not begin execution of this lane until PK, at Gate 1, explicitly confirms it is not restricted by the v6.140 ruling's "no new heavy CGU Final implementation lanes" language** (`docs/briefs/cgu-final-control-tower-watch-ruling-v1.md:35-36,64-67`) — M8/M9 are not named in that ruling's explicit "remain queued" list, but the ruling also does not explicitly exempt this lane, and M8.1 is a genuine (if bounded) code build. This brief does not resolve that ambiguity; PK must.
- No schedule DML, no cap increase, no Phase-2 CGU Final apply-packet authoring or mutation of any kind — unchanged standing hold from v6.140, carried forward even though this lane's own scope does not naturally touch that surface.
- No CE-repo (`invegent-content-engine`) code, DB, schema, or migration change of any kind — CE-repo touches are limited to this brief and its result doc (mirrors `docs/briefs/m8-asset-gap-dashboard-panel-gate1-brief-v1.md:120-121`'s "Touching `invegent-content-engine` in any way" forbidden action).
- No widening of PostgREST schema exposure, no new grant, no DB posture change in either repo's target database.
- No new `exec_sql` call site with interpolated/dynamic SQL if M8.1's `client_creative_governance` read goes through that RPC pattern — fixed literal string only, per the established injection-sink-hygiene precedent (`docs/briefs/m8-asset-gap-dashboard-panel-gate1-brief-v1.md:122-124`).
- No presenting P-1's finding as anything other than what it is — a recorded audit finding, not a design decision or an authorization to proceed with M9's replay.
- No treating M8.1's `legacy_routed` badge as a raw backend fact — it must carry the same "derived/inferred" labelling discipline M8 itself established for `unclassified` (`docs/briefs/m8-asset-gap-dashboard-panel-gate1-brief-v1.md:126-127`).
- No merge to `invegent-dashboard` `main` and no deploy of either repo — this lane's terminal act is push to a designated feature branch; merge/deploy remain separate, later PK gates (unchanged standing rule).
- No assuming this session already has a working `invegent-dashboard` worktree/checkout — verify first (see Success criteria / Notes).

## Success criteria

**P-1 half:**
- A recorded finding — visible, or not-visible-with-the-gap-named — for whether an operator can see a brand's Day-1 format-mix governance-readiness state (asset-pool depth, governance-row state, ladder position) anywhere across the five named `invegent-dashboard` files, based on a FRESH read of those files at execution time (not this brief's or the M9 package's prior characterization).
- The finding is written into the combined result doc in a form M9's own §5/§9/§11 can point to as "P-1 cleared," independent of and not substituting for the separate §6 schedule-expansion approval.

**M8.1 half:**
- The Asset Gap tab's execution-path badge gains a real `legacy_routed` state, derived from a fresh live read of `client_creative_governance` presence/proof-posture per (client, platform, format); `unclassified` remains the fallback wherever no governance-row evidence exists.
- `dashboard-ia-lint` returns PASS or WARN (not BLOCK) against the design and the final diff; any WARN is recorded, not silently dropped.
- Typecheck/build clean; `branch-warden` confirms the diff is confined to the approved file set before commit, using the two-repo gate pattern (`docs/briefs/authoritative-weekly-schedule-editor-phase-1-brief-v1.md:119`).
- Zero CE-repo diff.

## Stop condition

Report both halves' results in one combined result doc per `docs/briefs/_template_result.md`, honestly distinguishing what was found vs. what was built, then stop. Push to the designated `invegent-dashboard` feature branch is this lane's terminal act; merge to `main` and any subsequent M9-replay authorization remain separate, later PK gates.

---

## Notes

- **Cross-repo execution precondition, named not assumed:** this brief cannot confirm from this (CE-repo, read-only) drafting pass whether the EXECUTING session/orchestrator has an available `invegent-dashboard` worktree/checkout or credentials. M8 and M8.2 (`docs/briefs/results/m8-asset-gap-dashboard-panel-result-v1.md`, `docs/briefs/results/m8.2-scheduled-demand-contract-repair-result-v1.md`) demonstrate this access existed in a PRIOR session, but that is not evidence it exists in the session that picks up this brief. **Named handoff: `branch-warden`, run first, in two-repo mode**, to confirm both repos' HEAD/parity/ahead-behind before any ef-builder work starts (precedent: `docs/briefs/authoritative-weekly-schedule-editor-phase-1-brief-v1.md:119`).
- **Combining rationale, named honestly:** no PK-authored document was found that explicitly directs combining P-1 and M8.1 into one lane. The two share a topical connection (both concern dashboard visibility of `client_creative_governance`/format-mix governance state) but P-1's own named component list (`WeekFormatAllocation`, `WeekFormatPlanTab`, `CreativeConfigGapCard`, `PublishingPlanPyramid`, `production-readiness-queue.ts`, `client-creative-config-audit.ts`) does not literally include the Asset Gap tab files M8.1 touches. See Open Questions.
- **`dashboard-ia-lint` status:** still listed as an unproven candidate in `CLAUDE.md:78-81` at drafting time; verify its status has not changed before deciding whether P-1 may be executed via that agent versus a manual read.
- **Scope-boundary self-note:** M8.1 is a genuine dashboard-code build (not docs-only). `brief-author`'s own promotion record (`CLAUDE.md`, brief-author row) names "the first code-lane or DB-lane brief it drafts" as a flagged watch item for candidate-level scrutiny. A prior code+DB-lane brief-author draft was found in this repo (`docs/briefs/authoritative-weekly-schedule-editor-phase-1-brief-v1.md`, 2026-07-27, verification note at line 122) that already received such scrutiny — this brief does not claim to be the first, only flags that it is itself a code-lane draft warranting the same scrutiny, consistent with CLAUDE.md's standing condition regardless of ordinal.

---

## Open questions (PK decisions needed)

1. **v6.140 applicability.** Does the v6.140 ruling's "no new heavy CGU Final implementation lanes before the Phase-2 ruling" restriction cover this combined P-1+M8.1 dashboard-visibility lane? The ruling's explicit "remain queued" list (M1/M2/M4/M6/M7/M12/M13/M14/M16) does not name M8/M9, and it separately allows "docs-only follow-ups already in flight ... at PK's discretion," but M8.1 is a genuine (if bounded) code build, not docs-only, and neither category exactly fits this lane. — *PK decision needed.*
2. **Combining rationale.** Is there a documented rationale for combining P-1 and M8.1 into one lane (shared surface, shared gate, sequencing efficiency), or is the combination purely the orchestrator's/PK's current-session convenience choice? — *PK decision needed.*
3. **P-1 execution method.** Should P-1 be executed via a manual read or via `dashboard-ia-lint`, given the agent is still listed as an unproven candidate at drafting time? — pre-execution technical confirmation, not a PK policy call.
4. **Cross-repo access.** Does the executing session/orchestrator have a working `invegent-dashboard` worktree/checkout and credentials, given this brief was drafted from a read-only pass over the CE repo alone? — named precondition, verify before starting.

## Evidence gaps (named, not invented)

- The five P-1-named `invegent-dashboard` files were not read in full by this drafting pass (no tool access to that repo) — the M9 package itself states they were only "confirmed present by name/grep." Handoff: fresh read at execution time.
- `c.client_creative_governance`'s current schema/row population and whether its v2 live-gate rewire has landed were not independently re-verified — handoff to `db-rls-auditor`.
- The current `invegent-dashboard` `main` HEAD was not freshly checked against the commit hashes cited in prior M8/M8.2 result docs — handoff to `branch-warden` (two-repo mode).
- Whether the executing session has `invegent-dashboard` filesystem/worktree access at all was not verified — handoff to `branch-warden` / orchestrator environment check.
