# Brief — kinetic_voice palette hygiene (M11-fold closure design) — Gate-1

**Created:** 2026-08-05 Sydney
**Author:** chat
**Executor:** {PK | Claude Code | chat}
**Status:** draft
**Result file:** `docs/briefs/results/kinetic-voice-palette-hygiene-m11-fold-closure-design-result-v1.md` (created on completion)

---

## Task

Produce a **read-only, docs-only investigation and closure-design** for `kinetic_voice` (`video_short_kinetic_voice`, and its symmetric sibling `video_short_stat_voice`, which shares every code site and mechanism) advisor-palette eligibility hygiene — the item PK named, historically tracked as candidate must-have **M15**, then **folded by PK ruling into must-have M11** (governed legacy-path migration & retirement, M11b closure sub-scope) on 2026-08-04 (`docs/briefs/creatomate-global-ultimate-final-delta-audit-v1.md:236-239,400,404`). This is a PK-ordered **mandatory precondition** before PP YouTube kinetic returns to *unsupervised* scheduling (`docs/briefs/results/cgu-v1-final-reread-and-verdict-v1.md:30,46`) — it is **not itself** a decision to lift supervision, and it is **not, alone, sufficient** to do so (see Scope). Deliver a design document naming: the live current-state facts (freshly re-verified, not the 2026-08-04 snapshot), every code/config site carrying the eligibility, a proposed bounded removal mechanism, a review-tier recommendation, and a rollback plan. **Do not implement, apply, or deploy anything** — this brief's own execution gate is separately held (see Forbidden actions).

## Source context

- `docs/briefs/results/cgu-v1-final-reread-and-verdict-v1.md:27-34,45-46,55-56` — names the diagnosis ("advisor `_voice` palette deviation on `89545735…`") from the PP YT kinetic supervised recovery, and the resulting carry: "`kinetic_voice` palette hygiene (REQUIRED before PP kinetic returns to unsupervised scheduling — PK order)".
- `docs/briefs/post-cgu-v1-optimum-schedule-expansion-proposal-v1.md:43-54` — slot `89545735…` (2026-08-03T07:00Z natural kinetic slot, `status=skipped`) is the slot on which the deviation was diagnosed; also records that an earlier docs-only pass found **zero repo-file matches for the literal phrase "palette hygiene"** — the phrase surfaced as a live DB/queue note, not a stored document, so its exact original wording is not independently document-grounded (see Open questions).
- `docs/briefs/post-cgu-v1-optimum-schedule-expansion-proposal-v2.md:46,71` — states the **compound** unsupervised-scheduling condition explicitly: "supervised until `kinetic_voice` palette hygiene closes **AND** one natural scheduled cycle succeeds unattended"; automated PP-kinetic slot count stays **0** until both are true.
- `docs/briefs/creatomate-global-ultimate-final-delta-audit-v1.md:194-214` (§0e, four verdict carries) and `:216-247` (§0f, PK ratification) — original M15 candidate text plus the PK ruling: **"M15 is folded into M11"**; "unsupported `kinetic_voice` eligibility is **removed** unless/until that format receives its own governed implementation and proof."
- `docs/briefs/creatomate-global-ultimate-final-delta-audit-v1.md:388-407` (§2.2 must-have table, M11 row + struck-through M15 row) — M11's finite acceptance test explicitly incorporates the folded content; M15 row reads "MERGED into M11 … tracked under M11."
- `docs/briefs/creatomate-global-ultimate-final-delta-audit-v1.md:414-418` (§2.3) — sharpens the ruling: `video_short_kinetic_voice` and any B-roll/imagery-backed kinetic variant are out of scope of every visual approval recorded to date; "unsupported `kinetic_voice` eligibility is actively removed, not merely left unaddressed, unless/until it gets its own governed implementation + proof."
- `docs/briefs/creatomate-global-ultimate-final-delta-audit-v1.md:459-461,480-486` (§3, sequencing) — **M11b** (the closure work this brief's design feeds) is scoped under **Phase 2**, which explicitly "does not start before §6's schedule-expansion plan is approved"; M11a (inventory) is the completed preflight input.
- `docs/00_sync_state.md:9-13` and `docs/briefs/cgu-final-control-tower-watch-ruling-v1.md` (full text) — the current, live v6.140 PK control-tower ruling: Phase-2 held to the seven-day watch close (~2026-08-11 20:20 Sydney); "no schedule DML or cap raises"; "Do not open new heavy CGU Final implementation lanes before the Phase-2 ruling"; **but** explicitly names "Docs-only follow-ups already in flight (e.g. M11b Gate-1 briefs, M11c decision residue) remain at PK's discretion — this ruling does not newly authorize them" (line 66-67 of the ruling doc) — i.e. PK naming this task now is read as PK exercising that named discretion for a Gate-1 brief, not as authorization to execute.
- `docs/briefs/results/m11a-legacy-routing-inventory-result-v1.md:81-96` — the concrete technical inventory as of 2026-08-04: `video_short_stat_voice`/`video_short_kinetic_voice` never carry `select_template` evidence in **0/24 sampled rows** across both clients/formats — permanent, code-level exclusion, not a transition-in-progress state; and the "eligibility-location handoff for a future M11b closure lane": `ai-worker/index.ts:346`, `youtube-publisher/index.ts:306`, `image-worker/index.ts:607`, publisher `asset_backstop.ts` files, and `VIDEO_FORMATS`/`ELIGIBLE_FORMATS`/`isKinetic` checks in several migrations (e.g. `supabase/migrations/20260801200000_ws4_d4_pp_yt_kinetic_registration_v1.sql`). No `classify_format_capability` entry exists for kinetic_voice in either capability-classification migration; DB confirms **0 true-governed kinetic_voice occurrences (0/15 PP, 0/9 NDIS, 90d)** — "eligibility without implementation."
- `docs/briefs/results/s9-youtube-containment-release-result-v1.md:106-112` — live real-data evidence (as of that lane): exactly one real system-wide draft (`4f877c79…`, property-pulse, `video_short_kinetic_voice`, `final_format_authority='advisor'`, generated, approved, `scheduled_for` in the past, unpublished) currently satisfies the publisher's live eligibility criteria — a concrete, named example of the exact eligibility-without-implementation problem this hygiene item targets. **Must be freshly re-checked, not assumed still in this state** (see Allowed actions §pre-checks).
- `supabase/functions/ai-worker/index.ts:343-348` (`VIDEO_FORMATS` Set), `:1188-1206` (`fetchFormatContext` — the `t."5.3_content_format"` / `c.client_format_config` query building the advisor's candidate set, including the **fail-open** `NOT EXISTS` branch that offers ALL buildable formats when a client has zero `client_format_config` rows), `:1230-1252` (`callFormatAdvisor` — the literal `formatPalette` variable and prompt, confirming "palette" is the advisor's own in-code term for its candidate-format set) — this is the direct technical grounding for what "advisor `_voice` palette deviation" means: the advisor's candidate format palette can include the ungoverned `video_short_kinetic_voice` key.
- `supabase/functions/video-worker/index.ts:1-23` (v3.17.1 changelog) — explicitly scopes "kinetic_voice palette hygiene (separate lane — no `t."5.3_content_format"` / config touch)" **out of** the audio-gate fix, naming the same two surfaces (`t."5.3_content_format"`, config) as the ones this hygiene item is expected to touch.
- `supabase/functions/video-worker/index.ts:1617-1651` — the governed-vs-legacy fork: `B1_VIDEO_KINETIC_GOVERNED_FORMAT` is the exact literal `'video_short_kinetic'`; the `_voice` variant does not match and falls through to the legacy `isKinetic`/`isStat` composed-in-code path — "explicitly out of scope — WS-4 §11/§15 Q3, a later PK-elected mission" (confirms building a governed kinetic_voice implementation is its own separate, not-yet-elected future lane, distinct from this hygiene item).
- `supabase/functions/youtube-publisher/index.ts:306` and `supabase/functions/image-worker/index.ts:607` — `ELIGIBLE_FORMATS`/`VIDEO_FORMATS` constant arrays independently listing `video_short_kinetic_voice`/`video_short_stat_voice` as eligible; sites this brief's design must re-verify at current HEAD (M11a's citations are dated 2026-08-04, before this task's own execution).
- `docs/00_session_state.md:197` — example live `c.client_format_config` UPDATE naming `ice_format_key IN ('video_short_kinetic_voice','video_short_stat_voice')` as a real, per-client toggleable row set (NDIS example) — confirms the config-table surface is a genuine mutable control, not merely a code constant.

## Scope

**In scope:**
- Fresh, session-specific **read-only** verification (live, not the 2026-08-04 M11a snapshot) of:
  - the format advisor's current candidate palette for property-pulse × YouTube (does `video_short_kinetic_voice` still appear, and via which path — a `t."5.3_content_format".is_buildable=true` row, an enabled `c.client_format_config` row, or the fail-open "zero config rows" branch);
  - current live `t."5.3_content_format"` and `c.client_format_config` rows for `video_short_kinetic_voice` and `video_short_stat_voice`, across all four clients (PP primary; NDIS/CFW/Invegent for completeness, since NDIS also showed live `_voice` occurrences per M11a);
  - current status of the one previously-named live eligible draft (`4f877c79…`) and any other draft currently sitting with format `video_short_kinetic_voice`/`video_short_stat_voice` and `final_format_authority='advisor'`, unpublished or scheduled;
  - current-HEAD line numbers/content of every code eligibility site named by M11a (`ai-worker/index.ts`, `youtube-publisher/index.ts`, `image-worker/index.ts`, publisher `asset_backstop.ts` files, the named migrations) — re-confirmed, not copy-pasted from the prior citation.
- Producing a **written closure-design document** (docs-only) that names: the exact proposed removal mechanism(s) (config-row disable vs `is_buildable=false` vs code-constant edit vs a combination, with rationale for the choice); the full ordered list of sites that must change together for the removal to be complete (config + every code site — a partial fix that only touches one surface is explicitly a defect this design must avoid); a recommended review tier (T2 or T3, with reasoning under CLAUDE.md's Convention 3 tiering rule); a rollback plan; and an explicit statement that closing this item is **necessary but not sufficient** for PP kinetic's return to unsupervised scheduling (the compound condition also requires one natural unattended cycle to succeed, `post-cgu-v1-optimum-schedule-expansion-proposal-v2.md:71`).
- Naming, in the design document itself, the gates that stand between this design and any execution (see Forbidden actions) so a future executor/PK reader does not need to reconstruct them.

**Out of scope:**
- Any code edit, DB write (DDL/DML), migration, deploy, or config-value change of any kind. This brief authorizes **investigation and design only**.
- Building a governed `video_short_kinetic_voice`/`video_short_stat_voice` implementation as an alternative to removal — explicitly a separate, later, not-yet-elected mission (`video-worker/index.ts:1638`, "WS-4 §11/§15 Q3, a later PK-elected mission"; delta-audit's own "unless/until it gets its own governed implementation + proof").
- Any decision to lift PP kinetic's supervised-scheduling status, or any schedule/cap DML — a separate, later, explicit PK decision gated on **both** halves of the compound condition, not on this item alone.
- The other M11b closure sub-items named by M11a's own findings (undeclared-legacy carousel for CFW/NDIS/Invegent; the base-key pre-governance-row legacy window) — those are separate, already-flagged M11b scope items (`m11a-legacy-routing-inventory-result-v1.md` Findings 1-2) and are not this task.
- Any of the must-haves the v6.140 ruling names as "remain queued" (M1/M2/M4/M6/M7/M12/M13/M14/M16) — not this task.
- Progressing this design into an execution/apply lane. That is a distinct future Gate-1/Gate-2 cycle of its own.

## Allowed actions

- Read-only DB queries (via `db-rls-auditor`) against `t."5.3_content_format"`, `c.client_format_config`, `m.post_draft`, `m.post_render_log` for the fresh live-state checks named in Scope.
- `Read`/`Grep`/`Glob` the current repo HEAD for every named code eligibility site, plus a fresh grep for `kinetic_voice`/`stat_voice`/`ELIGIBLE_FORMATS`/`VIDEO_FORMATS`/`isKinetic` to catch any site introduced or moved since 2026-08-04.
- Consult `branch-warden` (read-only) to confirm current HEAD/branch state before any commit of the design document itself.
- Write the closure-design result document per the house result template; propose, but do not create, any migration/patch file (a proposed diff/SQL sketch may be included in the design doc as illustrative text, clearly labeled NOT-FOR-APPLY, but must not be a file the orchestrator could accidentally apply).
- Surface every genuinely unresolved technical or policy question explicitly (see Open questions below) rather than resolving it unilaterally.

## Forbidden actions

- **No code edit, DB write, migration apply, or deploy of any kind.** This is a T1, docs-only, read-only lane; any implementation step belongs to a separate, later Gate-1/Gate-2 cycle.
- **No schedule DML or cap raise of any kind** — standing v6.140 PK ruling (`docs/00_sync_state.md:9-13`; `docs/briefs/cgu-final-control-tower-watch-ruling-v1.md` §1,§2.2).
- **Do not open this as a "heavy CGU Final implementation lane."** The v6.140 ruling forbids that before the Phase-2 ruling; it explicitly leaves only "M11b Gate-1 briefs" (i.e. exactly this kind of docs-only design work) "at PK's discretion," and states plainly that the ruling "does not newly authorize" more than that (`cgu-final-control-tower-watch-ruling-v1.md:64-67`).
- **Do not treat closing this item, by itself, as sufficient to lift PP kinetic's supervised-scheduling status.** The compound condition requires ALSO one natural unattended scheduled cycle to succeed (`post-cgu-v1-optimum-schedule-expansion-proposal-v2.md:71`) — the design document must state this explicitly, not imply otherwise.
- **Do not design or recommend a governed kinetic_voice/stat_voice implementation as a substitute for removal** — that is a separate, not-yet-elected future mission (`video-worker/index.ts:1638`).
- **Do not expand into the other M11b closure sub-items** (undeclared-legacy carousel; base-key pre-governance window) named by M11a's Findings 1-2 — flag them if newly relevant, do not attempt to close them here.
- **Do not proceed past this brief's own design output to any execution/apply gate.** Execution requires, at minimum: (1) PK Gate-1 approval of this brief; (2) confirmation that the §6 schedule-expansion plan has since been PK-approved (`creatomate-global-ultimate-final-delta-audit-v1.md:459-461`, the standing §0f sequencing directive); (3) confirmation that the v6.140 Phase-2 watch has closed with its own PK ruling — OR an explicit, fresh, named PK election to expedite this specific narrow item ahead of the watch (never assumed or inferred from this brief).
- Standard CLAUDE.md deploy/DB gotchas apply to any *future* execution lane this design feeds (verify_jwt flag on any EF deploy, REVOKE-from-`anon`/`authenticated` where relevant, migration-name permanence, HEAD re-verification before commit) — named here for forward reference; this brief does not itself authorize deploy.

## Success criteria

- A closure-design result document exists that states, with a citation for each: (a) the live, freshly-verified current state of PP's (and NDIS/CFW/Invegent's, briefly) advisor candidate palette and `t."5.3_content_format"`/`c.client_format_config` rows for the two `_voice` keys; (b) the live current status of the previously-named eligible draft `4f877c79…` and any other currently-eligible `_voice`-format draft; (c) a current-HEAD-verified, line-numbered enumeration of every code site carrying kinetic_voice/stat_voice eligibility; (d) one concrete proposed removal mechanism (or a small ranked set of options) with rationale; (e) a recommended review tier (T2/T3) with reasoning tied to CLAUDE.md's Convention 3 rule; (f) a written rollback plan; (g) an explicit statement of the compound unsupervised-scheduling condition and that this item alone does not satisfy it.
- Zero code, DB, schedule, or config mutation occurred while producing it.
- Every active hold-state relevant to this task (v6.140 watch, §0f Phase-2 gate) is named in the document, not silently assumed cleared.
- Any technical ambiguity this brief could not fully resolve from documents alone (see Open questions) is named explicitly in the result document's own open-questions section, not silently resolved.

## Stop condition

When the closure-design document is complete and every success criterion above is met, report the result per the house result template (`docs/briefs/_template_result.md`), then **stop**. Do not begin any implementation, DB write, or deploy step. The next gate — whether to authorize execution — is a separate, later PK decision that additionally depends on the §6 schedule-expansion approval and the v6.140 watch closing (or an explicit PK expedite election).

---

## Notes

- The phrase "palette hygiene" itself was found by an earlier docs-only pass to have **zero repo-file matches** as a literal string before it surfaced as a live DB/queue note (`post-cgu-v1-optimum-schedule-expansion-proposal-v1.md:50-54`). This brief's own reading — that "palette" refers to the format advisor's candidate-format set (`ai-worker/index.ts`'s own `formatPalette` variable and "candidate palette" comments) — is well-grounded in code, but the executor should still name this reading explicitly in the design doc's assumptions rather than treat it as beyond doubt.
- `video_short_stat_voice` shares 100% of the same code sites, the same 0/24-governed evidence, and the same M11-fold ruling language as `video_short_kinetic_voice`. PK named only "kinetic_voice" in this task; the design document should note the stat_voice symmetry as an open question for PK rather than silently including or excluding it.
- The live draft `4f877c79…` (property-pulse, `video_short_kinetic_voice`, advisor-selected, approved, past-`scheduled_for`, unpublished as of the s9 containment-release lane) is a real, concrete artifact a removal design must account for — e.g. does removing eligibility retroactively affect an already-generated draft, or only future advisor selections? This should be answered by the fresh live check, not assumed.
- The task's phrasing "folded into M15" is the reverse of the documented lineage: kinetic_voice palette hygiene originally DEFINED candidate M15, and M15 was then folded INTO M11 by PK ruling (delta-audit-v1.md:236-239,404). Current tracking home is M11(b), not M15 (M15 is struck through). Noted here rather than silently corrected in the task statement.

---

## Open questions (PK decisions needed)

1. **stat_voice symmetry.** Should this lane's scope include `video_short_stat_voice` alongside `video_short_kinetic_voice`? Both share identical code sites, identical 0/24-governed evidence, and the same M11-fold ruling text; scoping only one leaves the other's identical defect open, but PK's task statement names only "kinetic_voice." — *PK decision needed.*
2. **In-flight draft handling.** What should happen to any in-flight eligible `_voice`-format draft found by the fresh live check (e.g. `4f877c79…` if still live) — leave it, void it, or publish-then-close? A removal that silently orphans or auto-voids an in-flight draft is a new side effect this task should name, not create by omission. — *PK decision needed.*
3. **Terminology confirmation.** Is there a more specific/technical origin document for the phrase "palette hygiene" and "advisor `_voice` palette deviation" beyond what this brief could ground (an earlier docs-only pass found zero literal repo matches for the phrase before it surfaced as a live DB/queue note)? This brief's grounding of "palette" = the format advisor's candidate-format set is strongly evidenced in code but not confirmed against an original source document — pre-execution technical confirmation, not a PK policy call.

## Evidence gaps (named, not invented)

- This draft does NOT assert the current (2026-08-05) live state of the advisor candidate palette, `t."5.3_content_format".is_buildable`, or `c.client_format_config.is_enabled` rows for the two `_voice` keys — all grounding is dated 2026-08-04 (M11a) or earlier. Handoff: `db-rls-auditor`.
- This draft does NOT assert the current status of draft `4f877c79…` or whether any newer `_voice`-format draft has since become eligible. Handoff: `db-rls-auditor`.
- The code eligibility sites named by M11a were re-confirmed at current HEAD for `ai-worker/index.ts`, `youtube-publisher/index.ts`, `image-worker/index.ts`, and `video-worker/index.ts` during this drafting pass, but the publisher `asset_backstop.ts` files and the full migration list were not independently re-read — inherited from M11a's 2026-08-04 citation.
- This draft does NOT assert whether the §6 schedule-expansion plan has since been approved or whether the v6.140 watch has since closed — both gate any execution step beyond this brief's own design output. Handoff: `register-reconciler` / fresh read of `docs/00_sync_state.md` at execution time.
